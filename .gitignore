# Snapshots produced by `pnpm clarity-safety snapshot`. They contain the
# encrypted-secrets master.key and a Postgres / PGlite dump — must never be
# committed. See runbook/README.md "Where to store snapshots safely" (Plan 03).
.planning/snapshots/

# Node
node_modules/
# Exception: the fake-instance test fixture intentionally checks in a
# fake node_modules/ tree so the snapshot filter exclusion test (S2)
# has something to assert on.
!scripts/safety/test/fixtures/fake-instance/**/node_modules/
!scripts/safety/test/fixtures/fake-instance/**/node_modules/**
# Same exception for fake-paperclip-clone (Plan 01-05 Task 1 — pg-dump-locator
# tests need a fake @embedded-postgres bundle tree to assert bundled-binary
# discovery for Linux/macOS AND the deliberate absence of pg_dump.exe in the
# Windows bundle. The bundle dirs LIVE under node_modules/ by pnpm convention.)
!scripts/safety/test/fixtures/fake-paperclip-clone/**/node_modules/
!scripts/safety/test/fixtures/fake-paperclip-clone/**/node_modules/**

# Build / IDE / OS
dist/
.DS_Store
Thumbs.db

# MemPalace per-project files (issue #185)
mempalace.yaml
entities.json

# pnpm pack tarballs (transient build artifacts from local pack-and-install spikes)
*.tgz

# Claude Code tool state (harness-managed; not project artifact)
.claude/

# session/tooling artifacts (untracked 2026-06-15 milestone close)
.playwright-mcp/
.superpowers/
.vscode/
/*.png
/snap.json
/*chat-messages*.json
.codex/
/deploy-*.sh
