---
plan: 02-05
phase: 2
phase_name: scaffold-and-surfaces
wave: 5
type: gap-closure
parent_plan: 02-03c
depends_on: ["02-03c"]
gap_closure: true
autonomous: false
files_modified:
  - scripts/build-ui.mjs
  - dist/ui/index.js (verify only)
  - src/ui/surfaces/reader/breadcrumb.tsx (potentially)
  - src/ui/surfaces/reader/ref-card.tsx (potentially)
  - src/ui/surfaces/reader/activity-timeline.tsx (potentially)
  - src/ui/primitives/clarity-surface-root.tsx (potentially)
requirements:
  - "02-03c drill found 4 React key warnings on ClaritySurfaceRoot, Breadcrumb, AnchoredToCards, ActivityTimeline (all visible only with populated data)"
  - "Plan 02-03c success criteria #3 — React key warnings are either gone OR formally classified as host-bug-out-of-scope"
must_haves:
  truths:
    - "Browser console on Countermoves COU-1 Reader tab shows ZERO 'Each child in a list should have a unique key prop' warnings, OR each remaining warning has a documented host-bug attribution + filed upstream issue + applied workaround"
    - "esbuild jsx-runtime emit shape verified to use jsxs (not jsx) for static-array children"
    - "If host-shim is at fault, workaround applied with stable React.Fragment wrapping at every affected .map() site"
  artifacts:
    - path: "scripts/diag/02-05-jsx-emit-probe.sh"
      provides: "Probe to inspect dist/ui/index.js for jsx vs jsxs emit shape"
      exports: []
  key_links: []
---

<objective>
Eliminate the 4 React key warnings observed in the Plan 02-03c drill on Countermoves COU-1 Reader tab. Either fix them in our code (preferred — likely an esbuild jsx-runtime config issue) or document them as a host bug with an upstream issue + applied workaround.

Output: a re-runnable rehearsal verdict where Plan 02-04 + 02-05 land together cleanly with zero key warnings.
</objective>

<context>
@.planning/phases/02-scaffold-and-surfaces/02-03c-REACT-KEYS.md
@.planning/phases/02-scaffold-and-surfaces/02-03c-HOST-CONTEXT.md
@scripts/build-ui.mjs

The hypothesis lives in 02-03c-REACT-KEYS.md "Hypothesized root cause" section. Three diagnostic steps:

1. Read dist/ui/index.js and grep for `jsx(` vs `jsxs(` calls inside .map() callbacks. esbuild should emit `jsxs` for static-array children.
2. Cross-reference paperclipai/paperclip@master ui/src/plugins/slots.tsx applyJsxRuntimeKey + getShimBlobUrl("react/jsx-runtime") to see how the host's shim handles arrays.
3. If our build is wrong: fix esbuild jsx config in scripts/build-ui.mjs.
   If host shim is wrong: file upstream + apply React.Fragment workaround at every .map() site.

The drill plugin uuid is `0d4fc40a-0541-4b67-8979-9d346cb9c07b` on Countermoves; install pattern is the same as 02-03c (build → pack → scp → install-helper.sh from ~/paperclip).
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Diagnose — verify jsx vs jsxs emit shape</name>
  <files>scripts/diag/02-05-jsx-emit-probe.sh (NEW), .planning/phases/02-scaffold-and-surfaces/02-05-DIAGNOSIS.md (NEW)</files>
  <action>
    1. Build the UI bundle. Grep dist/ui/index.js for jsx-runtime calls inside the affected components (Breadcrumb, AnchoredToCards, ActivityTimeline, ClaritySurfaceRoot).
    2. Compare against host's applyJsxRuntimeKey semantics in ~/paperclip/ui/src/plugins/slots.tsx.
    3. Write 02-05-DIAGNOSIS.md: which side is wrong (us / host / both), evidence cited.
  </action>
  <verify>
    <automated>test -f .planning/phases/02-scaffold-and-surfaces/02-05-DIAGNOSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - 02-05-DIAGNOSIS.md exists with verdict: OUR-BUILD / HOST-SHIM / BOTH
    - Evidence cites specific line numbers in dist/ui/index.js + slots.tsx
  </acceptance_criteria>
  <done>Root cause known; fix path obvious.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply fix per diagnosis</name>
  <files>(depends on Task 1 outcome)</files>
  <action>
    Branch on Task 1 verdict:
    - OUR-BUILD: adjust scripts/build-ui.mjs jsx config; rebuild; rerun grep; confirm jsxs emit.
    - HOST-SHIM: file upstream issue at paperclipai/paperclip; apply React.Fragment-with-stable-keys workaround at every affected .map() site.
    - BOTH: do both fixes.

    Add a unit test that bundles a tiny test component with .map() and asserts the bundle uses jsxs for arrays. (Or asserts no jsx-runtime warning patterns.)
  </action>
  <verify>
    <automated>node --test test/ui/jsx-runtime-emit.test.mjs (if added)</automated>
    <manual>Re-run rehearsal drill; console shows zero key warnings.</manual>
  </verify>
  <acceptance_criteria>
    - Unit test passes
    - Browser console clean (or remaining warnings have upstream-issue links in 02-03c-REACT-KEYS.md "Open verifications" addendum)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-rehearse against COU-1 (mini drill)</name>
  <how-to-verify>
    Same install pattern as 02-03c Task 4. Browser DevTools open, Ctrl+F5, navigate to COU-1 Reader tab. Console should show ZERO key warnings (or only warnings classified in 02-03c-REACT-KEYS.md addendum).
  </how-to-verify>
  <resume-signal>"approved — keys clean" closes Plan 02-05.</resume-signal>
</task>

</tasks>

<success_criteria>
- Browser console on Countermoves COU-1 Reader tab shows zero React key warnings (or each remaining warning is formally attributed to host bug with filed upstream issue).
- 02-03c-REACT-KEYS.md updated with the verified diagnosis + closure date.
- runbook/REHEARSAL.md gets a mini-drill row.
</success_criteria>
