---
plan: 02-07
phase: 2
phase_name: scaffold-and-surfaces
wave: 5
type: gap-closure
parent_plan: 02-03c
depends_on: ["02-03c"]
gap_closure: true
autonomous: true
files_modified:
  - src/ui/surfaces/reader/activity-timeline.tsx
  - src/worker/handlers/issue-reader.ts (potentially — activity event shape)
  - src/shared/types.ts (potentially — ActivityEvent type)
  - test/ui/reader-view.test.mjs (new assertion)
requirements:
  - "02-03c drill found ActivityTimeline rendering 'commentb2a22e50-d772-4b70-bb50-4f4e93c2e9843d agoConfirmed operational — …' for the COU-1 single-comment activity feed"
must_haves:
  truths:
    - "ActivityTimeline renders <comment-author or human-readable timestamp> + <relative-time-ago> + <comment excerpt>, NOT a raw UUID prefix"
    - "If the source activity event row has no formatted timestamp, the timeline falls back to something readable (e.g. '<unknown time>' or omits the time entirely) rather than displaying the comment id"
  artifacts: []
  key_links: []
---

<objective>
Fix the ActivityTimeline date-formatting bug surfaced by the Plan 02-03c drill against COU-1. Currently rendering literal `commentb2a22e50-d772-4b70-bb50-4f4e93c2e9843d agoConfirmed operational — …` for a single comment event — the comment row's `id` field is leaking into the formatted-timestamp slot.

Output: Reader's Activity timeline shows `<author or "Comment"> · <relative time> · <excerpt>` cleanly.
</objective>

<context>
@src/ui/surfaces/reader/activity-timeline.tsx
@src/worker/handlers/issue-reader.ts (activity assembly section)
@src/shared/types.ts (ActivityEvent type)

The bug looks like a join between the comment.id field and the comment.created_at field where the date-format helper received the wrong field. The "ago" suffix appears mid-string suggesting the date-format ran on a value AND a working "ago" string concatenation happened.

Pre-existing bug — not introduced by 02-03c — but masked until now because issue.reader silently returned emptyResult per the 02-03b prop-shape bug.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trace the field mapping in issue-reader.ts → activity-timeline.tsx</name>
  <action>
    1. Read issue-reader.ts's activity-event assembly (likely a `.map(comment => ({...}))` block).
    2. Read activity-timeline.tsx's render — what fields does it expect on each event?
    3. Diagnose the field-mismatch.
    4. Fix in the worker handler (preferred) OR in the UI's date-format helper.
    5. Add a unit test that asserts the activity-event shape and the rendered output.
  </action>
  <verify>
    <automated>node --test test/ui/reader-view.test.mjs test/worker/issue-reader.test.mjs</automated>
    <manual>Re-rehearsal drill: COU-1 Reader tab Activity section shows "Eric · 5 mins ago · Confirmed operational — …" or similar.</manual>
  </verify>
  <acceptance_criteria>
    - ActivityTimeline never renders a raw UUID
    - Every event shows readable timestamp + author + excerpt
  </acceptance_criteria>
</task>

</tasks>

<success_criteria>
- COU-1 Reader Activity section is human-readable.
- Mini-drill row in runbook/REHEARSAL.md confirming.
</success_criteria>
