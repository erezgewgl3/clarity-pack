---
plan: 02-10
phase: 2
phase_name: scaffold-and-surfaces
wave: 8
type: execute
depends_on: ["02-09"]
autonomous: true
gap_closure: true
non_blocking: true
parent_plan: 02-09
input_source: runbook/REHEARSAL.md §"2026-05-15 — Plan 02-09 re-drill" Section C (Console) + Routed to Plan 02-10 block
files_modified:
  - src/ui/surfaces/situation-room/clarity-surface-root.tsx (DEV-07 root-cause investigation — likely children-as-array without Fragment keys)
  - src/ui/surfaces/reader/anchored-to-cards.tsx (DEV-07 same investigation; mempalace 02-08 said "defense-in-depth UI patches" landed but warnings persisted at drill)
  - scripts/build-ui.mjs (DEV-08 — source-grep dist/ui for `vite` / `import.meta.hot` and add esbuild option to truly exclude)
  - test/ui/clarity-surface-root-keys.test.mjs (NEW — JSDOM render asserts no React-key warnings reach console)
  - test/build/no-vite-client-in-prod.test.mjs (NEW — static analysis of dist/ui/index.js asserts absence of `wss://`, `127.0.0.1:13100`, `__vite__`, `import.meta.hot`)
requirements: []  # polish only; no new requirements
must_haves:
  truths:
    - "Console at Reader-tab and Situation-Room page loads is CLEAN of all clarity-pack-originating warnings: zero React-key warnings, zero Vite WebSocket connection-failed messages."
    - "Static test on dist/ui/index.js asserts no Vite-client substrings reach production bundle (build-time regression lock)."
goal: |
  Close the two console-noise polish items deferred from Plan 02-09 re-drill (2026-05-15):
  DEV-07 (React key warnings on ClaritySurfaceRoot + AnchoredToCards) and DEV-08 (Vite HMR
  WebSocket noise at wss://127.0.0.1:13100/?token=…). Both items are explicitly non-blocking
  per the plan 02-09 verification language ("document as 02-09's own deferred polish for a
  Plan 02-10"). This plan exists so Phase 3 can start with a clean console baseline.
non_blocking_rationale: |
  Phase 2 closed APPROVED on Plan 02-09 re-drill 2026-05-15 with these items already
  documented as deferred polish. They are CONSOLE NOISE only — no functional defect, no user
  rendering bug, no production data risk. Phase 3 (Daily Bulletin) doesn't depend on either
  fix. This plan can interleave with Phase 3 planning or execute first as an aperitif —
  operator's choice.
---

<objective>
Plan 02-10 — close DEV-07 React key warnings + DEV-08 Vite HMR WebSocket leak. Both are pre-existing console noise items the Plan 02-09 re-drill (2026-05-15) confirmed remain and routed explicitly to this plan. Non-blocking for Phase 3.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DEV-07 — root-cause React key warnings now that components actually render</name>
  <files>src/ui/surfaces/situation-room/clarity-surface-root.tsx, src/ui/surfaces/reader/anchored-to-cards.tsx, test/ui/clarity-surface-root-keys.test.mjs (NEW)</files>
  <read_first>
    - runbook/REHEARSAL.md §"2026-05-15 — Plan 02-09 re-drill" Section C — exact warning text
    - Plan 02-08 commits 1548d63 + 5963a27 — "defense-in-depth UI patches" that mitigated but didn't root-cause
    - src/ui/surfaces/situation-room/clarity-surface-root.tsx + src/ui/surfaces/reader/anchored-to-cards.tsx — current source
  </read_first>
  <behavior>
    Component-level investigation:
    1. Render each component under JSDOM with the same shape of props the drill captured.
    2. Spy on `console.error` to catch React's key warning verbatim.
    3. Find the array-render-site that lacks keys (almost certainly children-prop fan-out, not `.map()` — Plan 02-04 SUMMARY confirms zero `.map()` without keys).
    4. Fix by either: (a) adding `<Fragment key={...}>` wrappers, OR (b) restructuring to avoid the array-children pattern.
    5. Test asserts ZERO `console.error` calls with "Each child in a list should have a unique 'key' prop" substring.

    The mitigation patches from 02-08 (null-safety) addressed crashes, not key warnings. This task addresses the original warning source.
  </behavior>
  <acceptance_criteria>
    - JSDOM test for ClaritySurfaceRoot: zero React-key warnings on `console.error`
    - JSDOM test for AnchoredToCards: zero React-key warnings on `console.error`
    - Live verification at Plan 02-10 close (drill or local Paperclip): Reader tab + Situation Room console clean of key warnings
  </acceptance_criteria>
  <done>To be filled by executor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DEV-08 — eliminate Vite HMR client from production UI bundle</name>
  <files>scripts/build-ui.mjs, test/build/no-vite-client-in-prod.test.mjs (NEW)</files>
  <read_first>
    - runbook/REHEARSAL.md §"2026-05-15 — Plan 02-09 re-drill" Section C — exact wss URL + token shape
    - Plan 02-08 commit `bcfc471` — what the esbuild `define` block tried to do
    - scripts/build-ui.mjs — current esbuild config
    - dist/ui/index.js — grep for `vite`, `import.meta.hot`, `__vite__`, `wss://127.0.0.1:13100`, `13100`
  </read_first>
  <behavior>
    Investigation path (Plan 02-08 Task 3 attempted this with esbuild `define` for `process.env.NODE_ENV === 'production'`; that didn't eliminate the WebSocket attempt because the leak isn't gated by NODE_ENV — it's a Vite client smuggled in via a transitive import):

    1. Source-grep dist/ui/index.js for the WebSocket URL (`13100`, `wss://`). If present, walk back via the bundled source map (or string-search SDK sources in node_modules/@paperclipai/plugin-sdk) to identify which package smuggled the Vite client.

    2. If the leak is from a transitive plugin-sdk import: open an upstream issue on Paperclip SDK and add a `banner: { js: '/* clarity-pack stripped vite HMR client */' }` + explicit `define: { 'globalThis.__vite__': 'undefined', 'import.meta.hot': 'undefined' }` + esbuild plugin that strip-replaces `wss://127.0.0.1` to `wss://invalid.example.invalid` at bundle time (HARD lock).

    3. If the leak is OUR code: remove it.

    4. Add `test/build/no-vite-client-in-prod.test.mjs` that runs after `node scripts/build-ui.mjs` and asserts dist/ui/index.js contains NONE of: `wss://127.0.0.1`, `13100`, `__vite__`, `import.meta.hot`. This is the regression lock.

    Note: per the 2026-05-15 console screenshot, the noise is `failed to connect to websocket (Error: WebSocket closed without opened.)` — not a security issue, but visually unprofessional and clutters error monitoring.
  </behavior>
  <acceptance_criteria>
    - dist/ui/index.js contains zero Vite-client substrings (asserted by test)
    - Live verification at Plan 02-10 close: no WebSocket console errors on Reader tab or Situation Room page load
  </acceptance_criteria>
  <done>To be filled by executor.</done>
</task>

<task type="checkpoint:human-verify" gate="non-blocking">
  <name>Task 3: light verification (no full drill needed)</name>
  <what-built>
    Tasks 1-2 close the two console-noise items from the Plan 02-09 re-drill.
  </what-built>
  <how-to-verify>
    Operator: Eric. Target: any local Paperclip clone OR Countermoves (whichever is faster). Bookend: SKIPPED (Countermoves staging exemption continues; no production touched).

    1. `npm pack` + `scp` + uninstall + install-helper.sh + plugin list (same pattern as 02-09 re-drill — see runbook/REHEARSAL.md §2026-05-15)
    2. Hard-refresh ANY issue page with Reader tab + the Situation Room page
    3. Console clean check: NO React-key warnings; NO `wss://127.0.0.1:13100` connection errors

    Resume signal: `approved — polish clean` if both checks pass.

    On approval, Plan 02-10 SUMMARY status → APPROVED; STATE.md notes 4 polish plans (02-05/02-06/02-07/02-10) now have a complete-set status. The 3 remaining (02-05 + 02-06 + 02-07) are independent of 02-10 and can still close in any order.
  </how-to-verify>
  <resume-signal>Type "approved — polish clean" on PASS.</resume-signal>
</task>

</tasks>

<threat_model>
Plan 02-10 is build-time + console-cleanup. No new attack surface, no new capabilities, no new manifest fields.
</threat_model>
