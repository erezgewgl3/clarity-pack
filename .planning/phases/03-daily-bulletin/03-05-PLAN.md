---
phase: 3
phase_name: daily-bulletin
plan: 03-05
type: execute
wave: 3
subsystem: worker
gap_closure: true
autonomous: true
depends_on: ["03-02", "03-03"]
tags: [llm-pipeline, agent-sessions, gap-closure, bulletin, tldr, editor-agent]

files_modified:
  - src/worker/agents/session-llm-adapter.ts
  - test/worker/agents/session-llm-adapter.test.mjs
  - src/worker/jobs/compile-bulletin.ts
  - src/worker/agents/editor.ts
  - src/worker.ts
  - src/manifest.ts
  - test/worker/bulletin/compile-bulletin-end-to-end.test.mjs

requirements:
  - BULL-05
  - BULL-06
  - BULL-09

# Dependency graph
requires:
  - phase: 03-daily-bulletin
    provides: "compile-pass-1.ts LlmAdapter interface + compilePass1 cap-then-call kernel; compile-bulletin.ts two-pass pipeline with editorAgentId resolution; compile-tldr.ts LlmAdapter + compileTldr kernel; circuit-breaker recordFailure/recordSuccess; manifest agents[] Editor-Agent declaration (status:'paused')"
provides:
  - "session-llm-adapter.ts — sessionLlmAdapter(ctx,{agentId,companyId}) factory returning a real LlmAdapter whose complete() opens an agent session, accumulates chunk events, resolves on done"
  - "compile-bulletin job constructs the real adapter per-company from ctx.agents + editorAgentId — ctx.llm fiction removed from the production path"
  - "editor heartbeat path wires the same adapter into compileTldr — Phase 2 Reader TL;DR unblocked"
  - "worker.ts: the registerCompileBulletinJob(ctx as unknown as ...) cast removed"
affects: [03-04-errata-banner-dst]

must_haves:
  truths:
    - "A live Editor-Agent session produces bulletin pass-1 text: sessionLlmAdapter.complete({maxTokens,prompt}) opens a session via ctx.agents.sessions.create, sends the prompt, accumulates every chunk event's message, and resolves the accumulated string when the terminal `done` event fires"
    - "A session that emits `error` rejects complete() with a tagged error so compilePass1's existing recordFailure path fires; a session that never emits a terminal event rejects after a bounded timeout — the compile job never hangs forever"
    - "A paused/terminated/pending_approval Editor-Agent causes complete() to reject with a tagged AGENT_NOT_INVOKABLE error BEFORE any session is opened — surfaced through recordCompileFailure + the Plan 03-04 failed-compile banner, never a silent hang"
    - "On a fresh install the Editor-Agent ships status:'paused'; the compile job resumes it (ctx.agents.resume) immediately before the first session so the first bulletin can actually compile"
    - "The compile-bulletin job builds the real adapter from ctx.agents + the per-company editorAgentId and passes it to compilePass1 — ctx.llm is no longer read on the production path"
    - "The editor heartbeat path builds the same adapter from the resolved agentId and passes it to compileTldr — the Reader's stuck 'Compiling TL;DR…' resolves"
    - "worker.ts registers the compile job without the `as unknown as CompileBulletinCtx` cast masking a missing llm member"
    - "Every existing stub-based test (compile-pass-1, compile-tldr, verifier, end-to-end) still passes unchanged — the LlmAdapter interface is byte-identical; only its production implementation is new"
  artifacts:
    - path: "src/worker/agents/session-llm-adapter.ts"
      provides: "sessionLlmAdapter(ctx, {agentId, companyId, taskKeyPrefix?, timeoutMs?}) → LlmAdapter. complete() guards agent status via ctx.agents.get, creates a session, sendMessage with an onEvent that appends chunk.message (skipping stream==='stderr'), wraps the terminal done/error event in a Promise, closes the session in a finally, enforces a default timeout."
      exports: ["sessionLlmAdapter", "SessionLlmAdapterCtx", "AGENT_NOT_INVOKABLE", "SESSION_TIMEOUT_MS"]
      min_lines: 60
    - path: "test/worker/agents/session-llm-adapter.test.mjs"
      provides: "node --test suite with a fake ctx.agents whose sessions.sendMessage replays scripted onEvent callbacks: chunk/chunk/done (resolves accumulated text), error (rejects), no-terminal (rejects on timeout), paused agent (rejects before create), stderr chunk excluded from accumulation, session.close called in all paths."
      min_lines: 80
  key_links:
    - from: "src/worker/jobs/compile-bulletin.ts"
      to: "src/worker/agents/session-llm-adapter.ts"
      via: "const llm = sessionLlmAdapter(ctx, {agentId: editorAgentId, companyId: company.id}); compilePass1(ctx, {..., llm})"
      pattern: "sessionLlmAdapter\\("
    - from: "src/worker/agents/editor.ts"
      to: "src/worker/agents/session-llm-adapter.ts"
      via: "build adapter from payload.agentId, pass as compileTldr arg `llm`"
      pattern: "sessionLlmAdapter\\("
    - from: "src/worker/agents/session-llm-adapter.ts"
      to: "ctx.agents.sessions"
      via: "create + sendMessage({prompt,onEvent}) + close"
      pattern: "ctx\\.agents\\.sessions\\.(create|sendMessage|close)"
    - from: "src/manifest.ts"
      to: "host plugin validator"
      via: "capabilities[] gains agent.sessions.create/list/send/close"
      pattern: "agent\\.sessions\\."
---

<objective>
Close the production-LLM-wiring gap surfaced by the Plan 03-03 Countermoves drill (2026-05-15): the bulletin and TL;DR compile pipelines were built against an injectable synchronous `LlmAdapter` wired in production to `ctx.llm` — and **`ctx.llm` does not exist** on the SDK 2026.512.0 `PluginContext`. No bulletin can compile in production, and the Reader's "Compiling TL;DR…" is permanently stuck for the identical reason.

This plan implements the production LLM-invocation mechanism the spike already identified: `ctx.agents.sessions.*`. It introduces ONE new file — a real `LlmAdapter` backed by an agent chat session — and wires it into BOTH the compile-bulletin job and the Phase 2 compile-tldr heartbeat path. The `LlmAdapter` interface is preserved byte-for-byte, so `compilePass1`, `verifyDraft`, `publishBulletin`, `compileTldr`, and every stub-based test are structurally untouched.

Authoritative source: `.planning/phases/03-daily-bulletin/03-LLM-INVOCATION-RESEARCH.md` (the implementation-ready spike — Mechanism 1, "Concrete restructure of Plan 03-02's compile path", lines 178-203). Capability strings and the `PluginAgentSessionsClient` shape verified directly against the installed SDK `types.d.ts` (lines 1110-1195) and `@paperclipai/shared` `constants.d.ts` (`PLUGIN_CAPABILITIES`).

Purpose: this gap BLOCKS Plan 03-04 — Phase 3's closure drill cannot confirm "a bulletin actually compiles and publishes" against a pipeline that has no LLM. 03-05 must run AFTER 03-02/03-03 (Wave 3) and BEFORE 03-04 (Wave 4).
Output: a production compile pipeline that actually invokes an LLM under full agent governance (Decision #3 / coexistence guarantee #4 preserved — runs as a real audited agent run with budget caps and pause/terminate). After this plan, Plan 03-04's blocked Wave-3 drill is executable.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative source for this gap-closure plan — READ FIRST.
@.planning/phases/03-daily-bulletin/03-LLM-INVOCATION-RESEARCH.md

# The two consumers of the LlmAdapter interface that this plan rewires.
@src/worker/jobs/compile-bulletin.ts
@src/worker/bulletin/compile-pass-1.ts
@src/worker/agents/compile-tldr.ts
@src/worker/agents/editor.ts
@src/worker.ts
@src/manifest.ts

<interfaces>
<!-- Verified against the installed @paperclipai/plugin-sdk@2026.512.0 types.d.ts -->
<!-- (lines 1110-1195) and @paperclipai/shared constants.d.ts PLUGIN_CAPABILITIES. -->
<!-- Use these directly — no codebase/node_modules exploration needed. -->

ctx.agents (PluginAgentsClient) — relevant members:
```typescript
get(agentId: string, companyId: string): Promise<Agent | null>;
// Resume a paused agent (sets status to idle). Throws if terminated,
// pending_approval, or not found. Requires capability `agents.resume`.
resume(agentId: string, companyId: string): Promise<Agent>;
sessions: PluginAgentSessionsClient;
```

ctx.agents.sessions (PluginAgentSessionsClient):
```typescript
// Requires `agent.sessions.create`.
create(agentId: string, companyId: string, opts?: { taskKey?: string; reason?: string }): Promise<AgentSession>;
// Requires `agent.sessions.list`.
list(agentId: string, companyId: string): Promise<AgentSession[]>;
// Returns IMMEDIATELY with { runId }. Agent output is delivered
// asynchronously via the onEvent callback. Requires `agent.sessions.send`.
sendMessage(sessionId: string, companyId: string, opts: {
  prompt: string;
  reason?: string;
  onEvent?: (event: AgentSessionEvent) => void;
}): Promise<AgentSessionSendResult>;
// Requires `agent.sessions.close`.
close(sessionId: string, companyId: string): Promise<void>;
```

AgentSession / AgentSessionEvent / AgentSessionSendResult:
```typescript
interface AgentSession { sessionId: string; agentId: string; companyId: string;
  status: "active" | "closed"; createdAt: string; }
interface AgentSessionEvent {
  sessionId: string; runId: string; seq: number;
  eventType: "chunk" | "status" | "done" | "error";
  stream: "stdout" | "stderr" | "system" | null;
  message: string | null;                       // chunk text lives here
  payload: Record<string, unknown> | null;
}
interface AgentSessionSendResult { runId: string; }
// Agent.status is one of: 'idle' | 'running' | 'paused' | 'terminated'
//   | 'pending_approval' (the union the manifest's status:'paused' draws from).
```

The LlmAdapter contract this plan must satisfy UNCHANGED — defined identically
in src/worker/bulletin/compile-pass-1.ts:46-48 and src/worker/agents/compile-tldr.ts:77-84:
```typescript
export type LlmAdapter = {
  complete(args: { maxTokens: number; prompt: string }): Promise<string>;
};
```

Capability strings — EXACT, verified members of PLUGIN_CAPABILITIES:
  agent.sessions.create  agent.sessions.list  agent.sessions.send  agent.sessions.close
  agents.resume   ← ALREADY present in src/manifest.ts capabilities[].
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing test suite for the session-backed LlmAdapter</name>
  <files>test/worker/agents/session-llm-adapter.test.mjs</files>
  <behavior>
    Write `node --test` tests against `sessionLlmAdapter` (importing the not-yet-created
    `src/worker/agents/session-llm-adapter.ts`). Build a fake `ctx` whose
    `agents.get` returns a scripted `Agent` and whose `agents.sessions` is a
    fake with `create`/`sendMessage`/`close`. `sendMessage` synchronously
    replays a scripted sequence of `AgentSessionEvent`s through the supplied
    `onEvent`, then resolves `{ runId }`.

    - Test 1 (happy path): script chunk("Hello ") + chunk("world") + done →
      `complete({maxTokens, prompt})` resolves to `"Hello world"`.
    - Test 2 (stderr excluded): script chunk("good",stream:'stdout') +
      chunk("BAD",stream:'stderr') + done → resolves to `"good"` only.
    - Test 3 (session error): script chunk("partial") + error → `complete()`
      REJECTS; the rejection message contains the error event's message.
    - Test 4 (timeout): `sendMessage` replays only chunk("x") and NO terminal
      event → `complete()` with a short injected `timeoutMs` REJECTS with a
      timeout-tagged error.
    - Test 5 (paused agent): `agents.get` returns `{status:'paused'}` →
      `complete()` REJECTS with an AGENT_NOT_INVOKABLE-tagged error and
      `sessions.create` is NEVER called. Repeat the assertion for
      `'terminated'` and `'pending_approval'`.
    - Test 6 (session always closed): in the happy path, the error path, AND
      the timeout path, the fake's `close` is called exactly once with the
      created sessionId.
    - Test 7 (prompt + maxTokens forwarded): the prompt passed to `complete`
      is the prompt forwarded to `sendMessage`; `taskKey` passed to `create`
      is non-empty (idempotency/dedupe key).
  </behavior>
  <action>
    Create `test/worker/agents/session-llm-adapter.test.mjs`. Follow the project's
    established `node --test` + `*.test.mjs` convention (see
    `test/worker/bulletin/compile-pass-1.test.mjs` for the fake-ctx style). Each
    test constructs a fresh fake ctx. The fake `agents.sessions.sendMessage`
    must accept `{prompt, reason, onEvent}`, invoke `onEvent` for each scripted
    event in order (use `queueMicrotask` or a `setImmediate` so the async
    Promise-wrapping in the adapter is genuinely exercised), then resolve
    `{ runId: 'run-test' }`. For Test 4, the scripted sequence simply omits the
    terminal event so the adapter's timeout must fire — pass a small
    `timeoutMs` (e.g. 50) into the `sessionLlmAdapter` factory options.

    This is the RED commit: the suite MUST fail because
    `src/worker/agents/session-llm-adapter.ts` does not exist yet. Commit with
    `test(03-05): RED — session-backed LlmAdapter spec`.
  </action>
  <verify>
    <automated>node --test test/worker/agents/session-llm-adapter.test.mjs 2>&1 | grep -E "fail|cannot find module|ERR_MODULE" || true</automated>
  </verify>
  <done>The new test file exists with the 7 behaviors above; `node --test test/worker/agents/session-llm-adapter.test.mjs` fails with a module-not-found (or assertion) error — RED confirmed. Commit `test(03-05): RED …` landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement sessionLlmAdapter (the real LlmAdapter)</name>
  <files>src/worker/agents/session-llm-adapter.ts</files>
  <behavior>
    `sessionLlmAdapter(ctx, opts)` returns an object satisfying the existing
    `LlmAdapter` type `{ complete({maxTokens, prompt}): Promise<string> }`. All
    7 RED tests from Task 1 pass.
  </behavior>
  <action>
    Create `src/worker/agents/session-llm-adapter.ts` per
    `.planning/phases/03-daily-bulletin/03-LLM-INVOCATION-RESEARCH.md` lines
    184-195 (the `sessionLlmAdapter` sketch) and the canonical
    `plugin-llm-wiki/src/wiki/core.ts:3838-3982` pattern cited there.

    Exports:
    - `SESSION_TIMEOUT_MS` — default session timeout constant (e.g. 120_000).
      A session that never emits a terminal event must not hang the compile job
      forever (research Open-Follow-up #2).
    - `AGENT_NOT_INVOKABLE` — a string tag used in the thrown error message so
      callers (and tests) can recognise the paused-agent failure.
    - `SessionLlmAdapterCtx` — the narrow ctx type this needs: `{ agents: Pick<
      PluginAgentsClient,'get'|'sessions'>; logger?: PluginLogger }`. Keep it
      narrow so both the compile-bulletin job ctx and the editor heartbeat ctx
      structurally satisfy it.
    - `sessionLlmAdapter(ctx: SessionLlmAdapterCtx, opts: { agentId: string;
      companyId: string; taskKeyPrefix?: string; timeoutMs?: number }): LlmAdapter`.

    `complete({ maxTokens, prompt })` body:
      1. Guard the agent: `const agent = await ctx.agents.get(opts.agentId,
         opts.companyId)`. If `agent` is null OR `agent.status` is one of
         `'paused' | 'terminated' | 'pending_approval'`, throw
         `new Error(\`${AGENT_NOT_INVOKABLE}: editor-agent status=${agent?.status ?? 'not_found'}\`)`
         — BEFORE creating any session. (`agents.invoke`/session calls would
         throw host-side anyway; failing fast here gives a clean tagged error
         and avoids a wasted session row.)
      2. `const session = await ctx.agents.sessions.create(opts.agentId,
         opts.companyId, { taskKey: \`${opts.taskKeyPrefix ?? 'clarity-pack:compile'}:${Date.now()}\`,
         reason: 'Clarity Pack compile' })`.
      3. Wrap the streaming completion in a Promise:
         - `let out = ''`
         - In `onEvent`: if `eventType==='chunk' && stream!=='stderr' && message`
           → `out += message`. If `eventType==='done'` → resolve `out`. If
           `eventType==='error'` → reject `new Error(\`session error: ${event.message ?? 'unknown'}\`)`.
         - Arm a `setTimeout(timeoutMs ?? SESSION_TIMEOUT_MS)` that rejects with
           a timeout-tagged error; clear it in both resolve and reject paths.
         - Call `ctx.agents.sessions.sendMessage(session.sessionId,
           opts.companyId, { prompt, reason: 'compile pass', onEvent })`. Note
           `maxTokens` is part of the `LlmAdapter` contract but the SDK
           `sendMessage` signature has no token field — keep `maxTokens` in the
           signature for interface fidelity; the input-token cap is still
           enforced upstream by `compilePass1`/`compileTldr` BEFORE `complete()`
           is ever called, so no cap regression. Add a one-line code comment
           saying exactly this.
      4. `finally`: `await ctx.agents.sessions.close(session.sessionId,
         opts.companyId)` wrapped in its own try/catch (a close failure must
         not mask the real result/error) — logger?.warn on close failure.

    Defensive resolution timing (research Open-Follow-up #2): resolve/reject is
    driven by the terminal `onEvent`, NOT by the `sendMessage` Promise resolving
    — `await sendMessage(...)` resolving only means "send accepted". The
    Promise-wrapping + timeout handles all three orderings (terminal-before,
    terminal-with, terminal-after the sendMessage resolution).

    Commit `feat(03-05): GREEN — session-backed LlmAdapter via ctx.agents.sessions`.
  </action>
  <verify>
    <automated>node --test test/worker/agents/session-llm-adapter.test.mjs</automated>
  </verify>
  <done>All 7 Task-1 tests pass. `src/worker/agents/session-llm-adapter.ts` exports `sessionLlmAdapter`, `SessionLlmAdapterCtx`, `AGENT_NOT_INVOKABLE`, `SESSION_TIMEOUT_MS`. The returned object structurally satisfies the existing `LlmAdapter` type (a `tsc` typecheck of a throwaway assignment, or the existing project typecheck, confirms it). Commit `feat(03-05): GREEN …` landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire the adapter into both compile paths + resume the Editor-Agent; remove the ctx.llm fiction</name>
  <files>src/worker/jobs/compile-bulletin.ts, src/worker/agents/editor.ts, src/worker.ts, src/manifest.ts, test/worker/bulletin/compile-bulletin-end-to-end.test.mjs</files>
  <behavior>
    - The compile-bulletin job, after resolving `editorAgentId`, resumes the
      agent if needed and passes a `sessionLlmAdapter(...)`-built `llm` into
      `compilePass1`.
    - The editor heartbeat path builds the same adapter from the resolved
      agentId and passes it as the `llm` arg to `compileTldr`.
    - `worker.ts` registers the compile job WITHOUT the `as unknown as
      CompileBulletinCtx` cast masking the missing `llm`.
    - The manifest declares the 4 `agent.sessions.*` capabilities.
    - The end-to-end test gains a case proving the job builds a real adapter
      and a paused agent routes through `recordCompileFailure` (no hang).
  </behavior>
  <action>
    **compile-bulletin.ts** (per research lines 192-194):
    - Import `sessionLlmAdapter, AGENT_NOT_INVOKABLE` from
      `../agents/session-llm-adapter.ts`.
    - After `editorAgentId` is resolved (currently compile-bulletin.ts:139-156)
      and before the compile begins: guard + resume. Call
      `const agent = await ctx.agents.get(editorAgentId, company.id)`. If
      `agent?.status === 'paused'`, call `await ctx.agents.resume(editorAgentId,
      company.id)` (capability `agents.resume` — already declared) inside a
      try/catch; a resume failure logs a warn and `continue`s (the agent may be
      `terminated`/`pending_approval`, which `resume` rejects — that is a real
      operator-action failure, not a compile bug). This closes research
      Open-Follow-up #1 ("the manifest ships the Editor-Agent paused; the first
      compile WILL hit a paused agent").
    - Build the adapter once per company:
      `const llm = sessionLlmAdapter(ctx, { agentId: editorAgentId,
      companyId: company.id, taskKeyPrefix: \`clarity-pack:bulletin:cycle-${cycleNumber}\` });`
    - Pass it into the existing `compilePass1` call (compile-bulletin.ts:253-259):
      add `llm` to the args object. `compilePass1` already does `args.llm ??
      ctx.llm` — supplying `args.llm` makes `ctx.llm` dead on this path.
    - Drop `llm?: LlmAdapter` from `CompileBulletinCtx` (compile-bulletin.ts:72)
      and remove the now-unused `type LlmAdapter` import — the type-level lie
      that a single ctx-wide `llm` exists independent of an agent is gone
      (research line 194, "the latter is cleaner").
    - An `AGENT_NOT_INVOKABLE` rejection (or any pass-1 throw) already routes
      through the existing `catch` → `recordCompileFailure` block
      (compile-bulletin.ts:260-269) — NO new failure machinery. Confirm the
      catch is unchanged.

    **editor.ts** (research Open-Follow-up #3 — Phase 2 retrofit, in-scope here):
    - Import `sessionLlmAdapter` from `./session-llm-adapter.ts`.
    - In `handleEditorHeartbeat`, before the per-issue loop, build
      `const llm = sessionLlmAdapter(ctx, { agentId: payload.agentId,
      companyId: payload.companyId, taskKeyPrefix: 'clarity-pack:tldr' });`
      and pass `llm` in the `compileTldr(ctx, { ... })` args object
      (editor.ts:117-124). Remove `llm?: LlmAdapter` from `EditorHeartbeatCtx`
      (editor.ts:82) and the now-unused `LlmAdapter` import if unused. Do NOT
      resume the agent on the heartbeat path — heartbeat compiles are
      best-effort and a paused agent simply yields an `AGENT_NOT_INVOKABLE`
      throw that the existing per-issue `catch` logs and skips
      (editor.ts:125-129). The bulletin job owns the resume because the
      bulletin is the scheduled, must-succeed surface.

    **worker.ts**:
    - The `registerCompileBulletinJob(ctx as unknown as CompileBulletinCtx)`
      call (worker.ts:113) — once `CompileBulletinCtx` no longer has the
      synthetic `llm` member, narrow the cast: keep a single-purpose cast only
      if `ctx` still does not structurally satisfy `CompileBulletinCtx` (it
      should, since every member is now a real `PluginContext` field). Prefer
      `registerCompileBulletinJob(ctx as CompileBulletinCtx)` or no cast at all;
      drop `as unknown as` — the whole point of removing the `llm` fiction is
      that the cast no longer needs to manufacture a missing member. If a
      genuine residual structural gap remains, leave a precise one-line comment
      naming the exact missing member rather than a blanket `as unknown as`.

    **manifest.ts**:
    - Add to `capabilities[]` (after the existing `agents.*` block, near
      manifest.ts:55-57): `'agent.sessions.create'`, `'agent.sessions.list'`,
      `'agent.sessions.send'`, `'agent.sessions.close'`. These are exact
      members of `PLUGIN_CAPABILITIES`. `agents.resume` is ALREADY present
      (manifest.ts:56) — do not duplicate. Add a comment: `// Plan 03-05 —
      production LLM invocation via ctx.agents.sessions (03-LLM-INVOCATION-RESEARCH.md)`.
    - Leave `status: 'paused'` on the Editor-Agent declaration AS-IS — the
      compile job's resume step (above) is the chosen mitigation; flipping the
      manifest default to non-paused would violate the coexistence-friendly
      "Eric reviews the agent before anything runs" intent (manifest.ts:223-225).

    **compile-bulletin-end-to-end.test.mjs**:
    - Add one test: a fake ctx whose `agents.get` returns `{status:'idle'}` and
      whose `agents.sessions` replays chunk(valid-BulletinDraft-JSON)+done — the
      job compiles and publishes end-to-end through the REAL `sessionLlmAdapter`
      (no stub `llm` injected into ctx). Add a second test: `agents.get` returns
      `{status:'paused'}` and `agents.resume` throws → the job records a
      compile failure and does NOT hang or publish. Keep all existing e2e tests
      green (they inject `args.llm` stubs into `compilePass1` indirectly via the
      job — if any existing test relied on `ctx.llm`, update it to either pass
      a stub `agents.sessions` fake or assert the new path; do NOT weaken an
      assertion).

    Commit `feat(03-05): wire session LlmAdapter into bulletin + tldr compile, resume Editor-Agent, drop ctx.llm`.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs"</automated>
  </verify>
  <done>Full suite green (the Plan 03-02 baseline was 504 tests / 502 pass / 2 skip — expect ≥504 + the new session-adapter + 2 e2e cases, 0 fail). `grep -n "sessionLlmAdapter" src/worker/jobs/compile-bulletin.ts src/worker/agents/editor.ts` shows the adapter wired into both paths. `grep -n "as unknown as CompileBulletinCtx" src/worker.ts` returns nothing. `grep -n "ctx.llm" src/worker` shows `ctx.llm` is no longer read on either production path. `grep -n "agent.sessions" src/manifest.ts` shows the 4 new capabilities. Project typecheck + worker build both green. Commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Eric runs the Wave-3 production-compile drill on Countermoves</name>
  <what-built>
    A production LLM-invocation path: `sessionLlmAdapter` (a real `LlmAdapter`
    backed by `ctx.agents.sessions.*`) is wired into both the compile-bulletin
    job and the Editor-Agent heartbeat TL;DR path. The compile job resumes the
    Editor-Agent (which ships `status:'paused'`) before the first compile. The
    `ctx.llm` fiction and the `as unknown as CompileBulletinCtx` cast are gone.
    Manifest declares `agent.sessions.create/list/send/close`. This is the
    Wave-3 drill that Plan 03-03 left blocked — it must PASS before Plan 03-04's
    Phase 3 closure drill can run.
  </what-built>
  <how-to-verify>
    **Pre-drill (run from the local dev machine):**
    1. `node --test "test/**/*.test.mjs"` — full suite green, 0 fail.
    2. Run the worker build (`node scripts/build-worker.mjs`) and the UI build —
       both green. Pack the plugin tarball.

    **Bookend per CLAUDE.md (Countermoves = staging; snapshot bookend SKIPPED
    per the Phase 3 staging scope — same posture as Plan 03-04 §"Bookended"):**
    Query MemPalace `clarity_pack/runbook` for the current Countermoves install
    command before running it (do NOT extrapolate from spec).
    3. Install the new build onto Countermoves via the recorded
       `pnpm paperclipai plugin install` path (run from `~/paperclip`, per the
       recorded workspace-script gotcha).

    **Live drill — Editor-Agent resume + bulletin compiles:**
    4. In classic Paperclip admin UI, confirm the Editor-Agent for company `COU`
       exists and note its status (expected: `paused` on a fresh install).
    5. Trigger a compile: either wait for the next `*/1` `compile-bulletin`
       cron fire past `next_due_at`, or set `next_due_at` to a past instant via
       Hostinger psql (`UPDATE plugin_clarity_pack_cdd6bda4bd.bulletins SET
       next_due_at = now() - interval '1 minute' WHERE company_id = '<COU id>'`)
       and wait ≤60s for the next fire.
    6. Confirm in the admin UI the Editor-Agent status flipped `paused → idle`
       (the job's `ctx.agents.resume` step ran).
    7. Navigate to `https://countermoves.gl3group.com/COU/plugins/clarity-pack/bulletin`.
       Confirm a `Bulletin No. N` issue exists, is `compile_status='published'`,
       has the 3 canonical tags, and the rendered body has real editorial prose
       with the standing numbers populated (not the all-zero fallback, unless
       COU genuinely has zero data).
    8. Confirm an agent run is visible in Paperclip's run history for the
       Editor-Agent (governance parity — the compile ran as a real audited run).

    **Live drill — Reader TL;DR populates:**
    9. Open any COU issue's `Reader` tab. Confirm the TL;DR resolves to real
       compiled text — the "Compiling TL;DR…" state is no longer permanently
       stuck (this is the Phase 2 retrofit landing).

    **Failure-path sanity (optional but recommended):**
    10. Pause the Editor-Agent in admin UI, force another compile fire, and
        confirm a `bulletin_compile_failures` row appears with a reason
        mentioning the agent status — i.e. the failure is surfaced, not silent
        (this is what Plan 03-04's failed-compile banner will render).

    If step 7 or 9 fails, capture the worker log line and the failing
    `bulletin_compile_failures.reason` and report — a failure here becomes a
    follow-up gap, not a silent pass.
  </how-to-verify>
  <resume-signal>Type "approved" if a bulletin compiled + published and the Reader TL;DR populated on Countermoves; otherwise describe what failed (worker log + any compile_failure reason).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → host agent-session RPC | The plugin worker opens an agent session and streams events back; the agent's adapter (the LLM) runs host-side under agent governance. |
| LLM-streamed text → compile pipeline | Untrusted free-text streamed from the agent crosses into `compilePass1`'s `JSON.parse` + `validateDraftSchema` + the pass-2 verifier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-30 | Denial of Service | session that never emits `done` | mitigate | `sessionLlmAdapter.complete()` arms a `SESSION_TIMEOUT_MS` timer that rejects the Promise; the compile job's existing `recordCompileFailure` + 15-min retry handles it. The job can never hang on a stuck session. |
| T-03-31 | Elevation of Privilege | resuming the Editor-Agent from the compile job | mitigate | Resume uses the already-declared `agents.resume` capability and only flips `paused → idle`; `terminated`/`pending_approval` agents reject resume (host-enforced) and the job records a failure rather than escalating. No new privilege beyond what the manifest already grants. |
| T-03-32 | Tampering | LLM streams malformed/hostile JSON as the BulletinDraft | mitigate | Unchanged from Plan 03-02: the accumulated `answer` string still flows through `JSON.parse` → `validateDraftSchema` → the deterministic pass-2 `verifyDraft` which re-runs every standing-number SQL. The adapter only accumulates text; it never trusts it. |
| T-03-33 | Repudiation | bypassing the Editor-Agent (direct LLM API call) | accept | Explicitly NOT done — Mechanism 4 (direct `ctx.http.fetch` to Anthropic) was rejected in the research as a Decision #3 / coexistence-guarantee-#4 violation. Sessions run as real audited agent runs; the audit trail is intact. |
| T-03-34 | Information Disclosure | session `taskKey` / prompt content leakage | accept | `taskKey` carries only a cycle number and a timestamp; the prompt carries the SQL-grounded facts table (the operator's own org data) to the operator's own configured agent adapter — no new disclosure surface beyond what Plan 03-02 already established. |
</threat_model>

<verification>
- Full `node --test "test/**/*.test.mjs"` suite green, 0 fail (baseline 504; expect ≥504 plus the new session-adapter suite and 2 e2e cases).
- `sessionLlmAdapter` is wired into BOTH `compile-bulletin.ts` and `editor.ts` (grep confirms).
- `ctx.llm` is no longer read on either production path; `CompileBulletinCtx` and `EditorHeartbeatCtx` no longer carry a synthetic `llm` member.
- `as unknown as CompileBulletinCtx` is gone from `worker.ts`.
- `src/manifest.ts` `capabilities[]` contains all 4 `agent.sessions.*` strings; `agents.resume` is present (was already).
- The `LlmAdapter` interface in `compile-pass-1.ts` and `compile-tldr.ts` is byte-identical to the Plan 03-02 baseline — every stub-based test still passes unchanged.
- Eric's Wave-3 Countermoves drill (Task 4) PASSES: a bulletin compiles + publishes and the Reader TL;DR populates.
</verification>

<success_criteria>
- One new file (`session-llm-adapter.ts`) implements a real `LlmAdapter` over `ctx.agents.sessions.*`; the synchronous `LlmAdapter` interface is preserved, so `compilePass1`/`verifyDraft`/`publishBulletin`/`compileTldr` and all stub tests are structurally untouched.
- The compile-bulletin job and the Editor-Agent heartbeat path both invoke an LLM in production under full agent governance (audited run, budget caps, pause/terminate — Decision #3 / coexistence guarantee #4 preserved).
- The Editor-Agent's `status:'paused'` manifest default is handled: the compile job resumes it before the first compile, so a fresh install can produce its first bulletin.
- A stuck or erroring session fails the compile gracefully through the existing circuit-breaker + `recordCompileFailure` plumbing — never a silent hang.
- The Wave-3 production-compile drill that Plan 03-03 left blocked is executed and PASSES, unblocking Plan 03-04's Phase 3 closure drill.
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-bulletin/03-05-SUMMARY.md`.

This plan dovetails with Plan 03-04: 03-04's closure drill (`Task 3: Eric runs
Phase 3 closure drill`) asserts errata, the failed-compile banner, and
plugin-disable coexistence against a pipeline that actually compiles bulletins.
That precondition does not hold until 03-05 lands — 03-05 is the Wave-3
prerequisite. Once 03-05's drill PASSES, Plan 03-04 (Wave 4) can run its
closure drill on a Countermoves instance that is genuinely producing bulletins,
and the failed-compile banner 03-04 builds will render the very
`bulletin_compile_failures` rows that 03-05's `AGENT_NOT_INVOKABLE` / session-
error / timeout paths now produce.
</output>
