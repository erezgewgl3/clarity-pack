---
plan: 03-06
phase: 3
phase_name: daily-bulletin
wave: 5
type: execute
subsystem: worker
gap_closure: true
autonomous: false
depends_on: ["03-04", "03-05"]
tags: [llm-pipeline, agent-issue-handoff, gap-closure, bulletin, tldr, editor-agent, circuit-breaker]

files_modified:
  - src/worker/agents/agent-task-delivery.ts
  - test/worker/agents/agent-task-delivery.test.mjs
  - src/worker/agents/circuit-breaker.ts
  - test/worker/agents/circuit-breaker-durable.test.mjs
  - src/worker/jobs/compile-bulletin.ts
  - src/worker/agents/editor.ts
  - src/worker/agents/session-llm-adapter.ts
  - src/manifest.ts
  - src/worker.ts
  - test/worker/bulletin/compile-bulletin-end-to-end.test.mjs
  - test/helpers/host-faithful-ctx.mjs

requirements:
  - BULL-05
  - BULL-06
  - BULL-09
  - EDITOR-05
  - READER-02

# Dependency graph
requires:
  - phase: 03-daily-bulletin
    provides: "compile-pass-1.ts LlmAdapter interface + compilePass1 cap-then-call kernel + extractJsonObject + validateDraftSchema; compile-bulletin.ts two-pass pipeline with editorAgentId resolution; compile-tldr.ts LlmAdapter + compileTldr kernel; editor.ts handleEditorHeartbeat + EDITOR_AGENT_KEY; circuit-breaker recordFailure/recordSuccess + BULLETIN_COMPILE_AGENT_KEY + editor_agent_failures durable audit table; session-llm-adapter.ts sessionLlmAdapter (now superseded for task-delivery); manifest agents[] Editor-Agent declaration (status:'paused')"
provides:
  - "agent-task-delivery.ts — deliverAgentTask(ctx,{agentId,companyId,operationKind,operationId,title,prompt,timeoutMs}) → string. Creates a scoped operation issue assigned to the Editor-Agent (ctx.issues.create with assigneeAgentId + plugin:* originKind + surfaceVisibility:'plugin_operation'), requests an immediate wakeup, polls for the agent's JSON result comment, returns the raw result string. The real task-delivery mechanism that replaces the discarded session prompt."
  - "compile-bulletin job builds a deliverAgentTask-backed LlmAdapter per company — the bulletin compile prompt now reaches the Editor-Agent as an assigned operation issue, not a discarded session prompt"
  - "editor heartbeat path builds the same delivery-backed adapter for compileTldr — Reader TL;DR uses the operation-issue handoff (tldr-compile operationKind)"
  - "Editor-Agent manifest instructions rewritten issue-driven — the agent looks for plugin:clarity-pack:operation:* assigned issues, reads the prompt from the issue body, and outputs raw BulletinDraft / TL;DR JSON as a comment"
  - "circuit-breaker isCircuitOpen(agentKey) predicate (in-memory) + isCircuitOpenDurable(ctx,agentKey) (reads last MAX_CONSECUTIVE_FAILURES editor_agent_failures rows) — the resume-defeats-breaker loop is closed"
  - "manifest capabilities[] gains issues.wakeup"
affects: []

must_haves:
  truths:
    - "BULL-05/06/09 (bulletin compile reaches the agent): compile-bulletin's LLM call now delivers the compile prompt to the Editor-Agent as an ASSIGNED operation issue (ctx.issues.create with assigneeAgentId=editorAgentId, originKind='plugin:clarity-pack:operation:bulletin-compile', surfaceVisibility='plugin_operation'); the agent's heartbeat finds the assigned issue, reads the prompt from the issue body, and produces the BulletinDraft JSON — the discarded-session-prompt root cause (PR #3106) is bypassed by riding 'Step 3 — Get Assignments', which PR #3106 leaves unchanged"
    - "The worker reads the agent's result back by polling ctx.issues.listComments for the operation issue until a JSON-bearing comment appears or a bounded timeout elapses; the result string flows UNCHANGED through extractJsonObject → JSON.parse → validateDraftSchema → verifyDraft → publishBulletin — numerics, verifier, publish, and schema are structurally untouched"
    - "READER-02 (Reader TL;DR populates): the editor heartbeat path uses the SAME operation-issue mechanism (originKind 'plugin:clarity-pack:operation:tldr-compile') so the Reader's stuck 'Compiling TL;DR…' resolves to real compiled text — the identical root cause is fixed on the same delivery layer"
    - "Operation issues are idempotent per cycle/scope: before creating a bulletin-compile issue the job searches ctx.issues.list({companyId, assigneeAgentId, originKindPrefix:'plugin:clarity-pack:operation:', originId, includePluginOperations:true}) for an existing in-flight issue for the same originId (cycle-N / tldr scopeId) and reuses it — a job re-fire mid-compile never spawns a duplicate operation issue"
    - "Operation issues stay OFF Eric's classic human board: surfaceVisibility='plugin_operation' + the plugin:* originKind keep them out of the default issue surface (coexistence guarantee #2 — the classic UI is not polluted)"
    - "Editor-Agent manifest instructions are issue-driven: they tell the agent that on each heartbeat it looks for an assigned issue whose originKind starts 'plugin:clarity-pack:operation:'; for ':bulletin-compile' / ':tldr-compile' the issue body IS the compile prompt and the agent outputs ONLY the requested JSON object as a comment (no prose preamble, no fences); the 'Editorial Desk' voice + 'Insufficient context' graceful-skip survive for narrative output"
    - "Circuit-breaker resume is breaker-aware: compile-bulletin resumes the Editor-Agent ONLY when it is paused AND the breaker is NOT open for BULLETIN_COMPILE_AGENT_KEY — a breaker-tripped pause STICKS (D-06: no auto-resume; operator must click Resume). The resume-defeats-breaker infinite loop (live attempt_n 466→470) is closed"
    - "Breaker state is durable across worker restarts: the resume gate calls isCircuitOpenDurable(ctx, BULLETIN_COMPILE_AGENT_KEY) which reads the last MAX_CONSECUTIVE_FAILURES editor_agent_failures rows for that agent_key — if they are all failures with no intervening reset, the breaker is treated as open even though the in-memory counter was lost on restart"
    - "governance parity preserved: the compile runs as a real, audited agent run against a real assigned issue (budget caps, pause/terminate, audit trail all apply); NO direct-HTTP LLM call is introduced (Mechanism 4 stays rejected — Decision #3 / coexistence guarantee #4)"
    - "Every existing stub-based test (compile-pass-1, compile-tldr, verifier, publish, end-to-end) still passes — the LlmAdapter interface is byte-identical; only the production task-delivery implementation behind complete() changes"
  artifacts:
    - path: "src/worker/agents/agent-task-delivery.ts"
      provides: "deliverAgentTask(ctx, opts) → Promise<string> and a deliveryLlmAdapter(ctx, opts) → LlmAdapter factory. deliverAgentTask: (1) idempotency search via ctx.issues.list (with includePluginOperations:true) for an existing operation issue with the same originId; (2) ctx.issues.create({companyId, assigneeAgentId, title, description:prompt, originKind:'plugin:clarity-pack:operation:<kind>', originId, surfaceVisibility:'plugin_operation', status:'todo'}) if none; (3) ctx.issues.requestWakeup(issueId, companyId, {reason}) to wake the agent now; (4) poll ctx.issues.listComments(issueId, companyId) every POLL_INTERVAL_MS until a comment authored by the agent yields a JSON object that BOTH parses AND passes validateDraftSchema, or RESULT_TIMEOUT_MS elapses; (5) return the raw result comment body. deliveryLlmAdapter wraps deliverAgentTask behind the existing { complete({maxTokens,prompt}): Promise<string> } LlmAdapter shape."
      exports: ["deliverAgentTask", "deliveryLlmAdapter", "AgentTaskDeliveryCtx", "AGENT_TASK_DELIVERY_TIMEOUT", "OPERATION_ORIGIN_KIND_PREFIX", "operationOriginKind"]
      min_lines: 110
    - path: "test/worker/agents/agent-task-delivery.test.mjs"
      provides: "node --test suite with a fake ctx.issues (create/list/listComments/requestWakeup). Tests: (1) happy path — create + wakeup + a JSON-bearing comment appears on a later poll → deliverAgentTask resolves the raw JSON string; (2) idempotency — an existing operation issue with the same originId is REUSED, ctx.issues.create is NOT called again, and the list call carries includePluginOperations:true; (3) timeout — no JSON comment ever appears → rejects with a timeout-tagged error after RESULT_TIMEOUT_MS (short injected value); (4) prose-only / invalid-JSON comments are skipped — a comment with a stray '{' but no schema-valid draft does NOT resolve; the schema-valid JSON one on a later poll does; (5) originKind/surfaceVisibility — the created issue carries originKind 'plugin:clarity-pack:operation:bulletin-compile' and surfaceVisibility 'plugin_operation'; (6) deliveryLlmAdapter.complete() forwards the prompt as the issue description and returns the result string."
      min_lines: 130
    - path: "src/worker/agents/circuit-breaker.ts"
      provides: "Extended — exports isCircuitOpen(agentKey:string):boolean (true when the in-memory counter >= MAX_CONSECUTIVE_FAILURES) and isCircuitOpenDurable(ctx, agentKey):Promise<boolean> (SELECTs the last MAX_CONSECUTIVE_FAILURES editor_agent_failures rows ORDER BY id DESC for that agent_key; returns true when that many rows exist and each consecutive value is >= 1 with the most recent >= MAX_CONSECUTIVE_FAILURES — i.e. the breaker tripped and no recordSuccess has since reset it). Existing recordFailure/recordSuccess UNCHANGED."
      exports: ["isCircuitOpen", "isCircuitOpenDurable", "MAX_CONSECUTIVE_FAILURES", "BULLETIN_COMPILE_AGENT_KEY", "recordFailure", "recordSuccess", "resetCircuitBreakerState"]
    - path: "test/worker/agents/circuit-breaker-durable.test.mjs"
      provides: "node --test suite: isCircuitOpen flips true after MAX_CONSECUTIVE_FAILURES recordFailure calls and false after recordSuccess; isCircuitOpenDurable returns true when a fake db returns 3 failure rows with the latest consecutive>=3, false when fewer rows exist or the latest consecutive<3 (post-reset)."
      min_lines: 60
  key_links:
    - from: "src/worker/jobs/compile-bulletin.ts"
      to: "src/worker/agents/agent-task-delivery.ts"
      via: "const llm = deliveryLlmAdapter(ctx, {agentId: editorAgentId, companyId: company.id, operationKind:'bulletin-compile', operationId:`cycle-${cycleNumber}`}); compilePass1(ctx,{...,llm})"
      pattern: "deliveryLlmAdapter\\("
    - from: "src/worker/jobs/compile-bulletin.ts"
      to: "src/worker/agents/circuit-breaker.ts"
      via: "resume gated by `if (agent?.status==='paused' && !(await isCircuitOpenDurable(ctx, BULLETIN_COMPILE_AGENT_KEY))) { resume }`"
      pattern: "isCircuitOpenDurable\\("
    - from: "src/worker/agents/editor.ts"
      to: "src/worker/agents/agent-task-delivery.ts"
      via: "build deliveryLlmAdapter from payload.agentId with operationKind:'tldr-compile', pass as compileTldr arg `llm`"
      pattern: "deliveryLlmAdapter\\("
    - from: "src/worker/agents/agent-task-delivery.ts"
      to: "ctx.issues"
      via: "create({assigneeAgentId, originKind, surfaceVisibility}) + requestWakeup + listComments"
      pattern: "ctx\\.issues\\.(create|requestWakeup|listComments)"
    - from: "src/manifest.ts"
      to: "host plugin validator"
      via: "capabilities[] gains issues.wakeup; agents[].instructions.content rewritten issue-driven"
      pattern: "issues\\.wakeup"
---

<revision_history>
**Revision 1 (2026-05-16)** — targeted fixes from a `gsd-plan-checker` PASS-WITH-CONCERNS pass. No task count, TDD structure, threat model, or anything the checker did not flag was changed.

- **B-1 (BLOCKER):** the Task 2 idempotency `ctx.issues.list` search (`deliverAgentTask` step 1; threat T-03-45) now passes `includePluginOperations: true`. Because the operation issue is created with `surfaceVisibility:'plugin_operation'` (off the default surface), a `list` without this flag searches only the default surface, finds nothing, and `create` then spawns a duplicate operation issue on every job re-fire — defeating T-03-45 and must_haves truth #4 (per-cycle idempotency). The flag's exact name/semantics were verified against the installed SDK (`@paperclipai/plugin-sdk@2026.512.0` `types.d.ts:1018` — `includePluginOperations?: boolean` on `PluginIssuesClient.list`); an executor instruction to re-verify before relying on it was added to Task 2.
- **W-1:** the `<interfaces>` `IssueComment` block was corrected against `@paperclipai/shared` `issue.d.ts:322-339` — `createdAt`/`updatedAt` are `Date` objects (not strings) and `authorAgentId` is `string | null` (always present, never optional). The executor must write `Date`-aware comparison code, not string comparison.
- **W-2:** Task 5 step 11's circuit-breaker forced-failure method was replaced. Pausing/terminating the agent is slow and ambiguous (a terminated agent times out 3×300s ≈ 15+ min and may not trip the new Path-(d) breaker). It now seeds three `editor_agent_failures` rows directly via psql (exact INSERT given) and asserts `isCircuitOpenDurable` reads an open circuit.
- **W-3:** the `PAPERCLIP_TASK_ID` run-scoping check was elevated to Task 5's STATED PRIMARY RISK, with explicit STOP / re-plan failure-handling — it is the single biggest behavioural unknown in this plan.
- **W-4:** Task 2's result-readback done-criteria was tightened: the worker resolves on the first comment whose body BOTH parses as JSON AND passes `validateDraftSchema` — not merely "the first comment containing a `{`" — so an incidental progress-comment with a stray `{...}` cannot be consumed as the result.
</revision_history>

<objective>
Close the architecture-level LLM-invocation defect surfaced by the Plan 03-04 Phase 3 closure drill (Countermoves, 2026-05-16). Plan 03-05 built `sessionLlmAdapter` to drive the Editor-Agent via `ctx.agents.sessions.sendMessage({prompt})` — but the host **silently discards the `prompt`** before it reaches the agent (upstream PR #3106, open/unmerged). The Editor-Agent only ever sees the `reason` wake label, runs its ordinary org-chart heartbeat, finds an empty inbox, and emits prose. `compilePass1` correctly rejects the prose; the job loops every minute. Plan 03-05's "Mechanism 1" was a misreading of `plugin-llm-wiki`, which actually uses a **scoped-issue handoff** (Mechanism 3).

This plan adopts Path (d) from `03-AGENT-INVOCATION-GAP-RESEARCH.md` — the **scoped-issue handoff**. The bulletin-compile prompt becomes the *body of an operation issue assigned to the Editor-Agent*. The agent's heartbeat finds the assigned issue (riding "Step 3 — Get Assignments", which PR #3106 explicitly leaves unchanged), reads the prompt from the issue body, and produces the `BulletinDraft` JSON as a comment. The worker polls for that comment and feeds it through the **unchanged** `extractJsonObject → validateDraft → verifyDraft → publishBulletin` pipeline. The identical fix is applied to the Phase 2 Reader TL;DR path (`compileTldr`), which rode the same broken adapter.

Scope (both surfaces, per Eric's confirmation):
1. **Bulletin compile** — replace the `sessionLlmAdapter` task-delivery in `compile-bulletin.ts` with the operation-issue handoff.
2. **Reader TL;DR** — apply the same mechanism to the editor heartbeat / `compileTldr` path.
3. **Editor-Agent manifest instructions** — rewrite issue-driven.
4. **Circuit-breaker resume-loop fix** — make resume breaker-aware AND durable (read the `editor_agent_failures` table), closing the resume-defeats-breaker infinite loop (live `attempt_n` hit 470).

Authoritative source: `.planning/phases/03-daily-bulletin/03-AGENT-INVOCATION-GAP-RESEARCH.md` — "Path (d)", the "Concrete Contained Build Delta", and the "Secondary Bug" section. The SDK `PluginIssuesClient` shape (`create` with `assigneeAgentId`/`originKind`/`surfaceVisibility`, `requestWakeup`, `listComments`, and `list` with `includePluginOperations`) was verified directly against the installed `@paperclipai/plugin-sdk@2026.512.0` `types.d.ts` (lines 1009-1078) and `@paperclipai/shared` `constants.d.ts` (`PluginIssueOriginKind = `plugin:${string}``, `IssueSurfaceVisibility = 'default' | 'plugin_operation'`).

Purpose: BULL-05/06/09 are not deliverable as architected until the compile prompt actually reaches the agent. This is the last gap before Phase 3 closes.
Output: a production compile pipeline (and Reader TL;DR path) that delivers work to the Editor-Agent as a real audited assigned issue — governance parity STRENGTHENED — plus a circuit breaker that genuinely latches. After this plan and a passing re-drill, Phase 3 closes.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Authoritative source for this gap-closure plan — READ FIRST.
@.planning/phases/03-daily-bulletin/03-AGENT-INVOCATION-GAP-RESEARCH.md
# The debug write-up + verbatim agent output that triggered this plan.
@.planning/debug/bulletin-compile-agent-heartbeat-gap.md
# The PRIOR (now-superseded) spike — note what it got wrong (Mechanism 1 vs 3).
@.planning/phases/03-daily-bulletin/03-LLM-INVOCATION-RESEARCH.md

# The consumers + the mechanism this plan rewires.
@src/worker/jobs/compile-bulletin.ts
@src/worker/bulletin/compile-pass-1.ts
@src/worker/agents/compile-tldr.ts
@src/worker/agents/editor.ts
@src/worker/agents/session-llm-adapter.ts
@src/worker/agents/circuit-breaker.ts
@src/worker.ts
@src/manifest.ts

<interfaces>
<!-- Verified against the installed @paperclipai/plugin-sdk@2026.512.0 types.d.ts -->
<!-- (PluginIssuesClient lines 1009-1078) and @paperclipai/shared constants.d.ts. -->
<!-- Use these directly — no codebase/node_modules exploration needed. -->

ctx.issues (PluginIssuesClient) — relevant members:
```typescript
// Requires `issues.read`.
list(input: {
  companyId: string;
  projectId?: string;
  assigneeAgentId?: string;
  originKind?: PluginIssueOriginKind;       // `plugin:${string}` — exact match
  originKindPrefix?: string;                // prefix match — use for the operation-issue search
  originId?: string;
  status?: Issue["status"];
  includePluginOperations?: boolean;        // <-- MUST be true for the operation-issue search;
                                            //     plugin_operation issues are off the default
                                            //     surface and list() omits them unless this is set
}): Promise<Issue[]>;
get(issueId: string, companyId: string): Promise<Issue | null>;
// Requires `issues.create`.
create(input: {
  companyId: string;
  title: string;
  description?: string;                     // <-- the compile prompt goes HERE
  status?: Issue["status"];                 // 'backlog'|'todo'|'in_progress'|'in_review'|'done'|'blocked'|'cancelled'
  assigneeAgentId?: string;                 // <-- assign to the resolved Editor-Agent UUID
  surfaceVisibility?: IssueSurfaceVisibility;// 'default' | 'plugin_operation'  — use 'plugin_operation'
  originKind?: PluginIssueOriginKind;       // `plugin:${string}`
  originId?: string | null;                 // our cycle-N / tldr-scopeId dedupe key
}): Promise<Issue>;
// Requires `issues.wakeup`.  Wakes the assigned agent immediately.
requestWakeup(issueId: string, companyId: string, options?: {
  reason?: string; idempotencyKey?: string | null;
}): Promise<PluginIssueWakeupResult>;
// Requires `issue.comments.read`.
listComments(issueId: string, companyId: string): Promise<IssueComment[]>;
// Requires `issue.comments.create`.  (Not used by the worker here — the AGENT writes the result comment.)
createComment(issueId: string, body: string, companyId: string, options?: { authorAgentId?: string }): Promise<IssueComment>;
```

Shared type facts (from @paperclipai/shared constants.d.ts):
```typescript
type PluginIssueOriginKind = `plugin:${string}`;            // any plugin:* literal is valid
const ISSUE_SURFACE_VISIBILITIES = ['default', 'plugin_operation'];
type IssueSurfaceVisibility = 'default' | 'plugin_operation';
// 'plugin_operation' is the off-the-human-board value — operation issues use it.
// Consequence: ctx.issues.list() does NOT return plugin_operation issues by default —
// the idempotency search MUST pass includePluginOperations: true (see B-1, Task 2).
```

IssueComment — the result-readback shape (VERIFIED against @paperclipai/shared issue.d.ts:322-339):
```typescript
interface IssueComment {
  id: string;
  companyId: string;
  issueId: string;
  authorType: IssueCommentAuthorType;
  authorAgentId: string | null;   // ALWAYS present (never optional); null when the author is a user
  authorUserId: string | null;
  body: string;
  createdAt: Date;                // a Date OBJECT — not an ISO string. Compare with Date arithmetic.
  updatedAt: Date;                // a Date OBJECT — not an ISO string.
  // ...other fields (presentation, metadata, derivedAuthorAgentId, …) not used by the worker here.
}
// listComments returns IssueComment[]. The worker matches a comment authored by the
// Editor-Agent (authorAgentId === editorAgentId — the field is non-optional, so a direct
// === comparison is safe; null means a non-agent author and is simply not a match) AND
// whose body yields a schema-valid BulletinDraft/TL;DR JSON. Any "comment must be newer
// than issue creation" guard must compare Date objects (e.g. comment.createdAt.getTime()),
// NOT string comparison.
```

The LlmAdapter contract — UNCHANGED, byte-identical, defined in
src/worker/bulletin/compile-pass-1.ts:45-47 and src/worker/agents/compile-tldr.ts:77-84:
```typescript
export type LlmAdapter = {
  complete(args: { maxTokens: number; prompt: string }): Promise<string>;
};
```

Capability strings — EXACT members of PLUGIN_CAPABILITIES:
  issues.create, issues.read, issue.comments.read, issue.comments.create — ALL already in src/manifest.ts.
  issues.wakeup ← MUST be ADDED (for ctx.issues.requestWakeup).
The agent.sessions.* capabilities stay declared for now (sessionLlmAdapter is deprecated, not deleted).

Durable breaker — the table circuit-breaker.ts:isCircuitOpenDurable reads
(migrations/0002_tldrs_and_editor.sql:59-65):
```sql
plugin_clarity_pack_cdd6bda4bd.editor_agent_failures
  (id bigserial PK, agent_key text, failed_at timestamptz, reason text, consecutive int)
-- recordFailure appends a row each failure; `consecutive` is the in-memory counter
-- value AT THAT TIME (1,2,3,...). recordSuccess does NOT write a row — it only
-- zeroes the in-memory counter. So "the last MAX_CONSECUTIVE_FAILURES rows are all
-- failures AND the most recent consecutive >= MAX_CONSECUTIVE_FAILURES" is the
-- durable signal that the breaker tripped and was never reset.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing spec for the operation-issue task-delivery layer + the durable breaker predicate</name>
  <files>test/worker/agents/agent-task-delivery.test.mjs, test/worker/agents/circuit-breaker-durable.test.mjs</files>
  <behavior>
    Write `node --test` tests against TWO not-yet-written surfaces:

    **agent-task-delivery.test.mjs** — tests `deliverAgentTask` and
    `deliveryLlmAdapter` from `src/worker/agents/agent-task-delivery.ts`. Build a
    fake `ctx.issues` with `create`/`list`/`listComments`/`requestWakeup`:
    - Test 1 (happy path): `list` returns `[]` (no existing operation issue);
      `create` records the input and returns `{id:'op-1', ...}`; `requestWakeup`
      resolves; `listComments` returns `[]` on the first poll and
      `[{id:'c1', authorAgentId:'<agentId>', body:'<a valid BulletinDraft JSON>'}]`
      on the second → `deliverAgentTask(...)` resolves to the raw JSON string.
    - Test 2 (idempotency): `list` returns an existing operation issue whose
      `originId` matches → `create` is NEVER called; `deliverAgentTask` reuses
      that issue and still polls `listComments` for the result. ALSO assert the
      object passed to `ctx.issues.list` carries `includePluginOperations === true`
      and `originKindPrefix === 'plugin:clarity-pack:operation:'` — without the
      flag the search misses the off-board operation issue and a duplicate is
      created (the B-1 regression).
    - Test 3 (timeout): `listComments` always returns `[]` → `deliverAgentTask`
      with a short injected `timeoutMs` REJECTS with a timeout-tagged error.
    - Test 4 (non-result comments skipped): `listComments` returns a comment
      whose body contains a stray `{` but does NOT parse / does NOT pass
      `validateDraftSchema` (e.g. a progress note like `working on it {step 1}`)
      on poll 1, and a schema-valid `BulletinDraft` JSON comment on poll 2 →
      `deliverAgentTask` resolves to the poll-2 comment; the stray-brace comment
      never resolves it.
    - Test 5 (operation-issue shape): the object passed to `ctx.issues.create`
      carries `assigneeAgentId === <agentId>`, `originKind ===
      'plugin:clarity-pack:operation:bulletin-compile'`, `surfaceVisibility ===
      'plugin_operation'`, and `description === <the prompt>`; `requestWakeup`
      was called with the created issue id.
    - Test 6 (deliveryLlmAdapter): `deliveryLlmAdapter(ctx, {...}).complete({
      maxTokens, prompt})` forwards `prompt` as the issue `description` and
      resolves to the agent's result string — i.e. it satisfies the `LlmAdapter`
      interface.

    **circuit-breaker-durable.test.mjs** — tests the new `isCircuitOpen` /
    `isCircuitOpenDurable` exports from `src/worker/agents/circuit-breaker.ts`:
    - `isCircuitOpen('k')` is false initially; true after
      MAX_CONSECUTIVE_FAILURES `recordFailure` calls for `'k'`; false again
      after `recordSuccess('k')`. (Call `resetCircuitBreakerState()` first.)
    - `isCircuitOpenDurable(ctx,'k')` with a fake `ctx.db.query` returning 3
      rows whose latest `consecutive` is 3 → true; returning 2 rows → false;
      returning 3 rows whose latest `consecutive` is 1 (a post-reset re-fail) →
      false.
  </behavior>
  <action>
    Create both test files following the project's `node --test` + `*.test.mjs`
    convention (see `test/worker/agents/session-llm-adapter.test.mjs` and
    `test/worker/bulletin/compile-pass-1.test.mjs` for the fake-ctx style and
    the host-faithful-fakes discipline).

    For `agent-task-delivery.test.mjs`: the fake `ctx.issues.listComments` must
    be a small scripted queue — return a different array per call so the
    polling loop is genuinely exercised across ≥2 polls. Inject a short
    `timeoutMs` (e.g. 200) and a short `pollIntervalMs` (e.g. 20) into the
    `deliverAgentTask` / `deliveryLlmAdapter` options so Test 3 finishes fast.
    Reuse a real minimal `BulletinDraft` JSON literal (masthead/actionInbox/
    departments/standingNumbers/lineageThreads) as the agent's result body so it
    actually passes `validateDraftSchema` (Test 4 depends on a genuine
    schema-valid vs schema-invalid distinction, not just a brace count). For the
    `ctx.issues.list` fake, record the exact input object so Test 2 can assert
    `includePluginOperations === true`.

    For `circuit-breaker-durable.test.mjs`: the fake `ctx` only needs
    `db.query` (for `isCircuitOpenDurable`) — model it returning a scripted row
    array; `isCircuitOpen` needs no ctx at all.

    This is the RED commit: BOTH suites MUST fail — `agent-task-delivery.ts`
    does not exist, and `isCircuitOpen`/`isCircuitOpenDurable` are not yet
    exported. Commit `test(03-06): RED — operation-issue task-delivery + durable breaker spec`.
  </action>
  <verify>
    <automated>node --test test/worker/agents/agent-task-delivery.test.mjs test/worker/agents/circuit-breaker-durable.test.mjs 2>&1 | grep -E "fail|cannot find module|ERR_MODULE|is not a function" || true</automated>
  </verify>
  <done>Both new test files exist with the behaviors above; the run fails with module-not-found (agent-task-delivery) and/or "is not a function" (isCircuitOpen/isCircuitOpenDurable) — RED confirmed. Commit `test(03-06): RED …` landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the operation-issue task-delivery layer + the durable breaker predicate</name>
  <files>src/worker/agents/agent-task-delivery.ts, src/worker/agents/circuit-breaker.ts</files>
  <behavior>
    `src/worker/agents/agent-task-delivery.ts` exports `deliverAgentTask`,
    `deliveryLlmAdapter`, `AgentTaskDeliveryCtx`, `AGENT_TASK_DELIVERY_TIMEOUT`,
    `OPERATION_ORIGIN_KIND_PREFIX`, `operationOriginKind`. `circuit-breaker.ts`
    additionally exports `isCircuitOpen` and `isCircuitOpenDurable`. All Task-1
    tests pass.
  </behavior>
  <action>
    **Pre-flight (B-1 verification).** Before relying on the idempotency search,
    confirm the `includePluginOperations` parameter exists and has the assumed
    semantics: open the installed SDK type
    `node_modules/@paperclipai/plugin-sdk/dist/types.d.ts` and verify
    `PluginIssuesClient.list`'s input declares `includePluginOperations?: boolean`
    (verified at types.d.ts:1018 as of 2026-05-16) and that the surrounding
    doc comments confirm it controls whether `plugin_operation`-visibility issues
    are returned. If the parameter is absent or renamed in the installed
    version, STOP and flag — the idempotency guard cannot work without it (a
    `list` that omits off-board operation issues makes `create` spawn a
    duplicate on every re-fire). Do not invent an alternative silently.

    **agent-task-delivery.ts** — implement Path (d) from
    `03-AGENT-INVOCATION-GAP-RESEARCH.md` ("Concrete Contained Build Delta"
    items 2 + the "Result readback contract" follow-up). This is the canonical
    `plugin-llm-wiki/createOperationIssue` + comment-readback pattern.

    Constants / helpers:
    - `OPERATION_ORIGIN_KIND_PREFIX = 'plugin:clarity-pack:operation:'`.
    - `operationOriginKind(kind: 'bulletin-compile' | 'tldr-compile'): PluginIssueOriginKind`
      → `` `${OPERATION_ORIGIN_KIND_PREFIX}${kind}` `` (the `plugin:${string}`
      template type accepts it).
    - `AGENT_TASK_DELIVERY_TIMEOUT = 300_000` — default result-readback timeout.
      A `claude_local` compile run end-to-end is slower than a session stream;
      research Open-Follow-up #2 says to drill the real number on Countermoves,
      so 5 min is a generous v1 ceiling. A timeout routes through the existing
      `recordCompileFailure` + 15-min retry, exactly like any compile failure.
    - A `POLL_INTERVAL_MS` constant (e.g. 5_000 default; overridable via opts
      for tests).

    `AgentTaskDeliveryCtx` — the narrow ctx slice: `{ issues: Pick<
    PluginIssuesClient, 'list' | 'create' | 'requestWakeup' | 'listComments'>;
    logger?: PluginLogger }`. Keep it narrow so both the compile-bulletin ctx
    and the editor heartbeat ctx structurally satisfy it.

    `deliverAgentTask(ctx, opts)` where `opts = { agentId, companyId,
    operationKind, operationId, title, prompt, timeoutMs?, pollIntervalMs? }`:
      1. **Idempotency search.** `ctx.issues.list({ companyId: opts.companyId,
         assigneeAgentId: opts.agentId, originKindPrefix:
         OPERATION_ORIGIN_KIND_PREFIX, originId: opts.operationId,
         includePluginOperations: true })`. The `includePluginOperations: true`
         flag is MANDATORY (B-1): the operation issue is created with
         `surfaceVisibility:'plugin_operation'`, so a `list` without the flag
         searches only the default human surface, returns `[]`, and step 2 then
         creates a DUPLICATE operation issue on every job re-fire — defeating
         the idempotency contract (must_haves truth #4, threat T-03-45). If a
         non-terminal issue (status not in `done`/`cancelled`) is returned,
         REUSE it (`issue = existing[0]`) — do NOT create a duplicate. Log the
         reuse.
      2. **Create** (only if no reusable issue): `ctx.issues.create({
         companyId: opts.companyId, title: opts.title, description: opts.prompt,
         assigneeAgentId: opts.agentId, status: 'todo', surfaceVisibility:
         'plugin_operation', originKind: operationOriginKind(opts.operationKind),
         originId: opts.operationId })`. The compile prompt is the issue
         **description** — that is what the heartbeat delivers to the agent.
      3. **Wake the agent now**: `await ctx.issues.requestWakeup(issue.id,
         opts.companyId, { reason: \`clarity-pack ${opts.operationKind}\`,
         idempotencyKey: opts.operationId })` inside a try/catch — a wakeup
         failure is non-fatal (the next scheduled heartbeat still picks the
         issue up); log a warn.
      4. **Poll for the result.** Loop until `timeoutMs` elapses: every
         `pollIntervalMs`, `const comments = await ctx.issues.listComments(
         issue.id, opts.companyId)`. For each comment authored by the agent
         (`comment.authorAgentId === opts.agentId` — `authorAgentId` is a
         non-optional `string | null`, so a direct `===` is safe; a `null`
         author is a non-agent comment and simply does not match), the comment
         qualifies as THE RESULT only if its body BOTH (a) yields a JSON object
         via `extractJsonObject` + `JSON.parse`, AND (b) passes
         `validateDraftSchema` — import both `extractJsonObject` and
         `validateDraftSchema` from `../bulletin/compile-pass-1.ts`. A comment
         with a stray `{` that fails parse or fails the schema is a progress /
         status comment and MUST be skipped — it is not the result. The FIRST
         comment satisfying BOTH (a) and (b) is the result: return
         `comment.body` (the raw string — `compilePass1`/`compileTldr` callers
         still run `extractJsonObject` + `JSON.parse` + `validateDraftSchema`
         themselves; returning the raw body keeps the `LlmAdapter` contract — "a
         completion string" — intact and the downstream pipeline byte-identical).
         (For the `tldr-compile` operationKind, the schema gate is the TL;DR
         validator `compileTldr` uses, not `validateDraftSchema` — the executor
         picks the correct validator per `operationKind`; the principle is the
         same: the result comment must pass the *real* schema check, never a
         bare brace test.) If a comment-recency guard is used (only accept
         comments created after the operation issue), compare `Date` objects —
         `comment.createdAt` is a `Date`, use `.getTime()`, not string compare.
      5. On timeout, throw `new Error(\`agent-task-delivery timeout: no
         schema-valid JSON result comment on operation issue ${issue.id} after
         ${timeoutMs}ms\`)`.

    `deliveryLlmAdapter(ctx, opts)` where `opts = { agentId, companyId,
    operationKind, operationId, title?, timeoutMs?, pollIntervalMs? }` returns
    `{ complete({ maxTokens, prompt }) }` satisfying `LlmAdapter`. `complete`
    calls `deliverAgentTask(ctx, { ...opts, prompt, title: opts.title ??
    \`Clarity Pack ${opts.operationKind} — ${opts.operationId}\` })`. `maxTokens`
    is part of the contract for fidelity but unused here — the input-token cap
    is still enforced upstream by `compilePass1`/`compileTldr` BEFORE
    `complete()` is called (one-line comment, same note as
    `session-llm-adapter.ts`).

    **circuit-breaker.ts** — ADD two exports, leave `recordFailure`/
    `recordSuccess`/`resetCircuitBreakerState` UNCHANGED:
    - `isCircuitOpen(agentKey: string): boolean` → `(counters.get(agentKey) ?? 0)
      >= MAX_CONSECUTIVE_FAILURES`.
    - `isCircuitOpenDurable(ctx: { db: { query<T>(sql,params): Promise<T[]> } },
      agentKey: string): Promise<boolean>` →
      `SELECT consecutive FROM plugin_clarity_pack_cdd6bda4bd.editor_agent_failures
       WHERE agent_key = $1 ORDER BY id DESC LIMIT $2` with params
      `[agentKey, MAX_CONSECUTIVE_FAILURES]`. Return `true` IFF the query
      returned exactly `MAX_CONSECUTIVE_FAILURES` rows AND `rows[0].consecutive
      >= MAX_CONSECUTIVE_FAILURES` (the most recent failure was the trip — and
      because `recordSuccess` writes no row, a later success cannot have reset
      it; the *next* failure after a reset would carry `consecutive = 1`, which
      fails the check). Wrap in try/catch — a query error returns `false`
      (fail-open on the durability check; the in-memory `isCircuitOpen` is the
      primary gate, this is the restart backstop). Document the rationale in a
      block comment, citing `circuit-breaker.ts:13-15`'s "v2 reads the last N
      rows on boot" note that this pulls forward.

    Commit `feat(03-06): GREEN — operation-issue task delivery + durable circuit-breaker predicate`.
  </action>
  <verify>
    <automated>node --test test/worker/agents/agent-task-delivery.test.mjs test/worker/agents/circuit-breaker-durable.test.mjs</automated>
  </verify>
  <done>All Task-1 tests pass. `agent-task-delivery.ts` exports the 6 named symbols; `circuit-breaker.ts` additionally exports `isCircuitOpen` + `isCircuitOpenDurable`. The idempotency `ctx.issues.list` call passes `includePluginOperations: true`. The result-readback resolves ONLY on a comment whose body parses as JSON AND passes the schema validator — a stray-brace progress comment is skipped. `deliveryLlmAdapter(...)` structurally satisfies the existing `LlmAdapter` type. Project typecheck of the two new/changed files is clean. Commit `feat(03-06): GREEN …` landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire operation-issue delivery into both compile paths, make the resume breaker-aware, deprecate sessionLlmAdapter</name>
  <files>src/worker/jobs/compile-bulletin.ts, src/worker/agents/editor.ts, src/worker/agents/session-llm-adapter.ts, src/worker.ts, test/worker/bulletin/compile-bulletin-end-to-end.test.mjs, test/helpers/host-faithful-ctx.mjs</files>
  <behavior>
    - `compile-bulletin.ts` builds a `deliveryLlmAdapter(...)`-backed `llm` per
      company (operationKind `'bulletin-compile'`, operationId `cycle-${N}`) and
      passes it into `compilePass1` — the discarded-session-prompt path is gone.
    - The Editor-Agent resume is now gated: resume ONLY when the agent is
      `paused` AND `isCircuitOpenDurable(ctx, BULLETIN_COMPILE_AGENT_KEY)` is
      false. A breaker-tripped pause sticks.
    - The editor heartbeat path builds the same delivery adapter (operationKind
      `'tldr-compile'`, operationId = the issue scopeId) for `compileTldr`.
    - `sessionLlmAdapter` is marked deprecated (kept, not deleted).
    - The end-to-end test + the host-faithful ctx helper gain coverage for the
      operation-issue path; all existing tests stay green.
  </behavior>
  <action>
    **compile-bulletin.ts**:
    - Replace the `import { sessionLlmAdapter }` line with
      `import { deliveryLlmAdapter } from '../agents/agent-task-delivery.ts';`.
    - Add `isCircuitOpenDurable` to the existing
      `import { ... } from '../agents/circuit-breaker.ts'` line.
    - In the resume block (currently compile-bulletin.ts:259-273), change the
      condition from `if (agent?.status === 'paused')` to:
      `if (agent?.status === 'paused' && !(await isCircuitOpenDurable(ctx,
      BULLETIN_COMPILE_AGENT_KEY)))`. Add a comment: a paused agent whose
      breaker is open was paused BY the breaker — auto-resuming it is the
      resume-defeats-breaker loop (live attempt_n 466→470, see
      03-AGENT-INVOCATION-GAP-RESEARCH.md "Secondary Bug"). When the breaker is
      open, leave the agent paused, log a warn, and `continue` the company
      (D-06: operator must click Resume). When the breaker is NOT open, the
      paused state is the manifest's coexistence-friendly default → resume is
      legitimate (first-install case).
    - Replace the adapter construction (currently compile-bulletin.ts:374-377)
      with: `const llm = deliveryLlmAdapter(ctx, { agentId: editorAgentId,
      companyId: company.id, operationKind: 'bulletin-compile', operationId:
      \`cycle-${cycleNumber}\`, title: \`Compile Daily Bulletin — cycle
      ${cycleNumber}\` });`. The `compilePass1(ctx, {..., llm})` call is
      otherwise unchanged — `compilePass1`, `verifyDraft`, `publishBulletin`,
      and `extractJsonObject` are all structurally untouched.
    - `ctx.agents.sessions` is no longer used by this job; `CompileBulletinCtx`
      keeps `agents: PluginAgentsClient` (still needed for `get`/`resume`/
      `managed.reconcile`/`pause`). No ctx type field is removed.

    **editor.ts**:
    - Replace `import { sessionLlmAdapter, type SessionLlmAdapterCtx }` with
      `import { deliveryLlmAdapter, type AgentTaskDeliveryCtx } from
      './agent-task-delivery.ts';`.
    - `EditorHeartbeatCtx` — swap the `SessionLlmAdapterCtx` intersection for
      `AgentTaskDeliveryCtx` (the `{issues: ...}` slice). Note `EditorHeartbeatCtx`
      already declares an `issues` member with `get`/`comments` — widen it so it
      also satisfies `AgentTaskDeliveryCtx`'s `issues` (`list`/`create`/
      `requestWakeup`/`listComments`). Use `PluginIssuesClient` for the `issues`
      member if that is cleaner than hand-listing methods.
    - In `handleEditorHeartbeat`, the per-heartbeat adapter is now built
      PER ISSUE (operationId must be unique per TL;DR scope). Move the adapter
      construction inside the `for (const issueId of issueIds)` loop:
      `const llm = deliveryLlmAdapter(ctx, { agentId: payload.agentId,
      companyId: payload.companyId, operationKind: 'tldr-compile', operationId:
      \`tldr-${issueId}\`, title: \`Compile TL;DR — ${issueId}\` });` then pass
      `llm` to `compileTldr`. Keep the existing "do NOT resume on the heartbeat
      path" comment — best-effort; a paused agent yields a delivery timeout that
      the per-issue catch logs and skips.

    **session-llm-adapter.ts**:
    - Add a top-of-file deprecation note: `// DEPRECATED (Plan 03-06) — the
      session `prompt` is discarded by the host before it reaches the agent
      (upstream PR #3106, open). Task delivery moved to the operation-issue
      handoff in agent-task-delivery.ts. Kept for reference / a possible future
      re-enable if PR #3106 merges. Not imported by any production path.` Do
      NOT delete the file or its tests.

    **worker.ts**:
    - No structural change is expected — `registerCompileBulletinJob` already
      receives the real `ctx`. Confirm the compile-bulletin and editor
      registrations still typecheck against the updated ctx types; if a cast
      was present only to satisfy a now-removed member, simplify it. Do not add
      new casts.

    **manifest.ts** — handled in Task 4 (instructions + capability). Touching it
    here only if a typecheck demands it; otherwise leave for Task 4.

    **compile-bulletin-end-to-end.test.mjs** + **test/helpers/host-faithful-ctx.mjs**:
    - The host-faithful ctx helper already has an `issues` fake with
      `create`/`list`. Extend it: `requestWakeup` (record + resolve) and
      `listComments` (return a scripted result — by default a single comment
      whose body is the canned `BulletinDraft` JSON the existing e2e fake LLM
      used to return, authored by the resolved editor agent id, with
      `createdAt`/`updatedAt` as real `Date` objects to match the SDK shape).
      The `list` fake must honour `includePluginOperations` — when the test
      seeds a prior operation issue, the fake returns it only when the flag is
      set, mirroring the host. This makes the operation-issue handoff resolvable
      in-process: the "agent" is simulated by the helper pre-seeding the result
      comment. Document at the top of the helper that this fake models the HOST
      CONSTRAINT (issues API shape) — NOT the agent's real behaviour (the
      research's explicit lesson: fakes cannot model "the agent ignores the
      prompt"; only the live drill in Task 5 can).
    - Add one e2e test: a company at-or-past `next_due_at`, an `idle` agent, the
      ctx helper seeded so `listComments` yields a valid `BulletinDraft` JSON
      comment → the job compiles + publishes end-to-end through the REAL
      `deliveryLlmAdapter` (no `sessionLlmAdapter`, no stub `llm`).
    - Add one e2e test: `isCircuitOpenDurable` returns true (seed the
      `editor_agent_failures` fake rows or stub the durable check) AND the agent
      is `paused` → the job does NOT call `ctx.agents.resume`, records nothing
      new, and does NOT publish (resume-defeats-breaker loop is closed).
    - Keep ALL existing e2e + compile tests green. If an existing test injected
      a `sessionLlmAdapter`-shaped fake or `ctx.agents.sessions`, update it to
      the operation-issue fake — do NOT weaken an assertion.

    Commit `feat(03-06): operation-issue task delivery wired into bulletin + tldr; breaker-aware resume`.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs"</automated>
  </verify>
  <done>Full suite green, 0 fail (baseline per STATE.md was 660 tests / 658 pass / 2 skip; expect ≥660 + the Task-1 suites + 2 new e2e cases). `grep -n "deliveryLlmAdapter" src/worker/jobs/compile-bulletin.ts src/worker/agents/editor.ts` shows the adapter wired into both paths. `grep -n "isCircuitOpenDurable" src/worker/jobs/compile-bulletin.ts` shows the resume gate. `grep -rn "sessionLlmAdapter" src/worker/jobs src/worker/agents/editor.ts` returns nothing (production paths no longer import it). `session-llm-adapter.ts` carries the DEPRECATED note. Project typecheck + worker/UI builds green. Commit landed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Rewrite the Editor-Agent manifest instructions issue-driven; add the issues.wakeup capability</name>
  <files>src/manifest.ts, test/worker/agents/agent-task-delivery.test.mjs</files>
  <behavior>
    - The manifest `agents[].instructions.content` for the Editor-Agent is
      rewritten to be issue-driven: the agent processes assigned
      `plugin:clarity-pack:operation:*` issues and outputs raw JSON.
    - The manifest `capabilities[]` gains `issues.wakeup`.
    - A small assertion locks the manifest's operation-originKind contract so it
      cannot silently drift from `agent-task-delivery.ts`.
  </behavior>
  <action>
    **manifest.ts — `agents[0].instructions.content`** (per
    `03-AGENT-INVOCATION-GAP-RESEARCH.md` "Concrete Contained Build Delta" item
    1, mirroring the Wiki Maintainer's issue-driven `AGENTS.md`). Rewrite to:
    - State the agent's role: the Clarity Pack Editorial Desk.
    - On each heartbeat, look in the inbox for an issue assigned to you whose
      `originKind` starts with `plugin:clarity-pack:operation:`.
    - For `plugin:clarity-pack:operation:bulletin-compile`: the issue
      **description** IS a compile prompt. Follow it exactly. Output **only**
      the requested `BulletinDraft` JSON object — as a comment on that issue.
      No prose preamble, no markdown fences, no sign-off. The prompt itself
      carries the facts table and the `{{NUMBER:key}}` placeholder rules; do not
      invent numbers.
    - For `plugin:clarity-pack:operation:tldr-compile`: the issue description is
      a TL;DR compile prompt. Output **only** the requested TL;DR (the format
      the prompt specifies) as a comment on that issue.
    - The "Editorial Desk" voice / sign-off rule still applies to NARRATIVE
      prose the agent writes *inside* a draft (e.g. department `editorialSummary`
      text) — but an operation issue's *comment output* is the raw JSON object
      only.
    - Keep the graceful-skip: if you cannot produce a useful result, comment the
      literal string `Insufficient context` and stop.
    - Keep it concise — the host injects it as the agent's system instructions.
    - Add a code comment above the rewritten string: `// Plan 03-06 — issue-
      driven instructions. The Editor-Agent is woken by an ASSIGNED operation
      issue (agent-task-delivery.ts), not a session prompt — the session prompt
      is discarded by the host (PR #3106). See 03-AGENT-INVOCATION-GAP-RESEARCH.md.`

    **manifest.ts — `capabilities[]`**: add `'issues.wakeup'` (exact
    `PLUGIN_CAPABILITIES` member) next to the existing `issues.create` /
    `issue.comments.create` block, with a comment: `// Plan 03-06 —
    ctx.issues.requestWakeup wakes the Editor-Agent immediately when an
    operation issue is created (agent-task-delivery.ts).` Leave the four
    `agent.sessions.*` capabilities in place (sessionLlmAdapter is deprecated,
    not removed — dropping them is a later cleanup, keeping the manifest delta
    the host re-validates minimal, per research Build Delta item 4).
    Leave `status: 'paused'` on the Editor-Agent declaration AS-IS — the
    breaker-aware resume in Task 3 is the chosen mitigation.

    **agent-task-delivery.test.mjs** — add a small contract test: import the
    manifest and `OPERATION_ORIGIN_KIND_PREFIX`; assert the manifest's
    `agents[0].instructions.content` includes the literal
    `OPERATION_ORIGIN_KIND_PREFIX` string (so the agent instructions and the
    worker's originKind cannot drift apart silently), and assert the manifest
    `capabilities` array includes `'issues.wakeup'`.

    Commit `feat(03-06): issue-driven Editor-Agent instructions + issues.wakeup capability`.
  </action>
  <verify>
    <automated>node --test test/worker/agents/agent-task-delivery.test.mjs && node scripts/build-manifest.mjs 2>&1 | tail -5</automated>
  </verify>
  <done>`src/manifest.ts` Editor-Agent `instructions.content` is issue-driven (references `plugin:clarity-pack:operation:` and "output only the JSON"); `capabilities[]` includes `'issues.wakeup'`. The manifest contract test passes (instructions reference the originKind prefix; capabilities include `issues.wakeup`). Manifest build is clean. Commit landed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Eric reinstalls on Countermoves and re-runs the Phase 3 closure drill</name>
  <what-built>
    The operation-issue task-delivery layer (`agent-task-delivery.ts`): the
    bulletin-compile prompt and the Reader TL;DR prompt are now delivered to the
    Editor-Agent as ASSIGNED Paperclip operation issues
    (`originKind: plugin:clarity-pack:operation:*`,
    `surfaceVisibility: plugin_operation` — off the human board). The agent's
    heartbeat finds the assigned issue, reads the prompt from the issue body,
    and posts the `BulletinDraft` / TL;DR JSON as a comment; the worker polls
    that comment and feeds it through the unchanged
    `extractJsonObject → verifyDraft → publishBulletin` pipeline. The
    Editor-Agent manifest instructions are rewritten issue-driven. The
    circuit-breaker resume is now breaker-aware AND durable
    (`isCircuitOpenDurable` reads the `editor_agent_failures` table), closing
    the resume-defeats-breaker infinite loop. `sessionLlmAdapter` is deprecated.

    This is the re-drill of the Plan 03-04 Task 3 closure drill that surfaced
    the gap. The host-faithful fakes model the issues-API CONSTRAINT, not the
    agent's real BEHAVIOUR — only this live drill proves the agent actually
    processes the assigned operation issue. Phase 3 cannot close until this
    PASSES.
  </what-built>
  <primary-risk>
    **`PAPERCLIP_TASK_ID` run-scoping is the single biggest behavioural unknown
    in this plan.** Path (d)'s entire mechanism depends on this: when the
    Editor-Agent's heartbeat finds the assigned operation issue, its run MUST be
    scoped to that issue — i.e. the host sets `PAPERCLIP_TASK_ID` to the
    operation issue's id, so the agent has a scoped issue to act on and reads
    the compile prompt from that issue's body. This *should* happen per the
    heartbeat protocol's "Step 3 — Get Assignments" → "Step 4 — Pick Work"
    (research Open-Follow-up #5), but it has NOT been verified on a live host.
    If it does NOT happen — if an assigned operation issue does not scope the
    run — the agent re-enters the EXACT "no scoped issue to act on / inbox
    empty" state that caused the original defect (the `sessionLlmAdapter`
    discarded-prompt failure), and the whole Path (d) handoff is wrong. Step 8
    below is the verification gate for this risk; its failure-handling is a hard
    STOP, not a follow-up.
  </primary-risk>
  <how-to-verify>
    **Pre-drill (local dev machine):**
    1. `node --test "test/**/*.test.mjs"` — full suite green, 0 fail.
    2. Run the worker build, the UI build, and the manifest build — all green.
       Pack the plugin tarball.

    **Bookend per CLAUDE.md (Countermoves = staging; snapshot bookend SKIPPED
    per the Phase 3 staging scope — same posture as Plans 03-04 / 03-05):**
    3. Query MemPalace `clarity_pack/runbook` for the current Countermoves
       install command BEFORE running it (do NOT extrapolate from spec — the
       plugin is currently UNINSTALLED, halted to stop the retry loop). Run the
       recorded `pnpm paperclipai plugin install` path from `~/paperclip`.

    **Live drill — bulletin compiles via the operation issue:**
    4. In classic Paperclip admin UI, confirm the Editor-Agent for company `COU`
       exists; note its status. (A fresh install ships it `paused`.)
    5. Trigger a compile: set `next_due_at` to a past instant via Hostinger
       psql (`UPDATE plugin_clarity_pack_cdd6bda4bd.bulletins SET next_due_at =
       now() - interval '1 minute' WHERE company_id = '<COU id>'`) and wait
       ≤60s for the next `compile-bulletin` cron fire.
    6. Confirm the job created an OPERATION ISSUE assigned to the Editor-Agent:
       in psql, `SELECT id, title, origin_kind, surface_visibility, assignee_agent_id
       FROM issues WHERE origin_kind LIKE 'plugin:clarity-pack:operation:%'
       ORDER BY created_at DESC LIMIT 5;` — expect a `bulletin-compile` row,
       `surface_visibility = 'plugin_operation'`, assigned to the Editor-Agent.
    7. Confirm the operation issue does NOT appear on Eric's normal/classic
       issue board (it is `plugin_operation`-scoped, off-board — coexistence
       guarantee #2).
    8. **PRIMARY-RISK GATE — `PAPERCLIP_TASK_ID` run-scoping.** Confirm the
       Editor-Agent ran AND its run was SCOPED TO THE OPERATION ISSUE: a real
       agent run is visible in Paperclip's run history; the run is scoped to
       that operation issue (`PAPERCLIP_TASK_ID` set to the operation issue's
       id — check the run's environment / task context in the run detail); and
       the operation issue has a comment from the agent carrying the
       BulletinDraft JSON. **If the agent runs UNSCOPED — i.e. the run history
       shows the heartbeat ran with no `PAPERCLIP_TASK_ID`, or the agent's
       output again reports "no assignments / no scoped issue to act on" / an
       empty inbox — STOP. Do not proceed to steps 9-12. Plan 03-06 needs a
       re-plan: Path (d) assumed an assigned operation issue scopes the run, and
       that assumption is wrong. Capture the run-history detail, the operation
       issue's comments, and the worker log, and report it as a re-plan
       trigger, not a pass.**
    9. Navigate to `https://countermoves.gl3group.com/COU/bulletin`. Confirm a
       `Bulletin No. N` issue is `compile_status='published'`, carries the 3
       canonical tags, and the rendered body has REAL editorial prose with the
       standing numbers populated (verified numerics — not the all-zero
       fallback unless COU genuinely has zero data).

    **Live drill — Reader TL;DR populates:**
    10. Open any COU issue's `Reader` tab. Confirm the TL;DR resolves to real
        compiled text — the "Compiling TL;DR…" stuck state is gone. Confirm a
        `tldr-compile` operation issue exists for that issue's scope.

    **Live drill — circuit breaker holds (resume does NOT defeat it):**
    11. Trip the breaker by seeding three failure rows directly via Hostinger
        psql — fast and unambiguous (a terminated/paused agent would instead
        time out 3×300s ≈ 15+ min and may not exercise the Path-(d) breaker
        path at all). Run:
        ```sql
        INSERT INTO plugin_clarity_pack_cdd6bda4bd.editor_agent_failures
          (agent_key, failed_at, reason, consecutive)
        VALUES
          ('bulletin-compile', now() - interval '3 minutes', 'drill: forced failure 1', 1),
          ('bulletin-compile', now() - interval '2 minutes', 'drill: forced failure 2', 2),
          ('bulletin-compile', now() - interval '1 minute',  'drill: forced failure 3', 3);
        ```
        (`agent_key` must match `BULLETIN_COMPILE_AGENT_KEY` — confirm the exact
        literal in `circuit-breaker.ts` before running; adjust if it differs.)
        Then pause the Editor-Agent in classic Paperclip UI so the drill state
        is "paused agent + open durable breaker". Confirm in psql the three
        rows are present.
    12. Wait through ≥2 `compile-bulletin` cron fires. Confirm the Editor-Agent
        STAYS `paused` — the job did NOT auto-resume it, because
        `isCircuitOpenDurable(ctx, BULLETIN_COMPILE_AGENT_KEY)` reads the three
        seeded rows as an OPEN circuit and the breaker-aware resume gate leaves
        the agent paused (D-06). Confirm the worker log shows the
        "breaker open — not resuming" warn, and `attempt_n` in
        `bulletin_compile_failures` does NOT run away (contrast: the pre-fix
        live run hit `attempt_n` 470). Then DELETE the three seeded drill rows,
        click Resume in classic UI, and confirm the next cron fire compiles
        cleanly.

    **Re-run the Plan 03-04 closure-drill assertions** (errata footer, failed-
    compile banner, plugin-disable coexistence) against the now-compiling
    pipeline — these were blocked because nothing compiled. Confirm each holds.

    If step 9, 10, or 12 fails: capture the worker log line, the operation
    issue's comments, and the failing `bulletin_compile_failures.reason`, and
    report — a failure becomes a follow-up gap, not a silent pass. (Step 8 is
    NOT a follow-up gap — see the primary-risk gate above: a step-8 failure is a
    hard STOP / re-plan.)
  </how-to-verify>
  <resume-signal>Type `approved — phase 3 closed` if (step 8) the agent's run was scoped to the operation issue (`PAPERCLIP_TASK_ID` set), a bulletin compiled + published via the operation issue, the Reader TL;DR populated, and the circuit breaker held (the three seeded failure rows kept the agent paused and the loop did NOT resume it). If step 8 failed (agent ran unscoped / no scoped issue), report `re-plan needed — PAPERCLIP_TASK_ID not scoped` with the run-history detail. Otherwise describe what failed (worker log + operation-issue comments + any compile_failure reason).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → host issues RPC | The plugin worker creates an operation issue assigned to the Editor-Agent and polls its comments; the agent's adapter (the LLM) runs host-side under agent governance. |
| agent comment body → compile pipeline | Untrusted free-text the Editor-Agent posts as a comment crosses into `extractJsonObject` + `JSON.parse` + `validateDraftSchema` + the pass-2 verifier. |
| operation issue → classic UI surface | The operation issue is created in `public.issues`; `surfaceVisibility:'plugin_operation'` must keep it off Eric's human board. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-40 | Denial of Service | operation issue whose agent never posts a JSON comment | mitigate | `deliverAgentTask` polls with an `AGENT_TASK_DELIVERY_TIMEOUT` (300s default) ceiling; on timeout it rejects, routing through the existing `recordCompileFailure` + 15-min retry. The compile job can never hang on a stuck operation issue. |
| T-03-41 | Tampering | agent posts malformed/hostile JSON as the BulletinDraft comment | mitigate | Unchanged from Plan 03-02: the comment body still flows through `extractJsonObject` → `JSON.parse` → `validateDraftSchema` → the deterministic pass-2 `verifyDraft` which re-runs every standing-number SQL. The delivery layer only returns the raw string; it never trusts it. Result-readback additionally requires the comment to PASS `validateDraftSchema` before it is accepted as the result, so a stray-brace progress comment is never consumed. |
| T-03-42 | Information Disclosure | operation issue (carrying the SQL-grounded facts table in its body) leaks onto Eric's classic board | mitigate | `surfaceVisibility:'plugin_operation'` + the `plugin:clarity-pack:operation:*` originKind keep it off the default issue surface (coexistence guarantee #2). Drilled in Task 5 step 7. The facts table is the operator's own org data delivered to the operator's own configured agent — no new disclosure surface. |
| T-03-43 | Elevation of Privilege | breaker-aware resume mis-resumes a breaker-paused agent | mitigate | The resume is gated on `isCircuitOpenDurable` — a breaker-tripped pause sticks across worker restarts (durable `editor_agent_failures` read). Only a `paused`-with-breaker-closed agent (the manifest's first-install default) is auto-resumed; everything else requires the operator's classic-UI Resume gesture (D-06). |
| T-03-44 | Repudiation | compile bypasses the Editor-Agent / no audit trail | mitigate | The compile now runs as a REAL assigned-issue agent run — visible operation issue, visible run history, budget caps, pause/terminate all apply. This is *more* audit-faithful than the session approach. No direct-HTTP LLM call is introduced (Mechanism 4 stays rejected — Decision #3 / coexistence guarantee #4). |
| T-03-45 | Tampering | a job re-fire spawns duplicate operation issues for the same cycle | mitigate | `deliverAgentTask` searches `ctx.issues.list({assigneeAgentId, originKindPrefix, originId, includePluginOperations:true})` for an existing non-terminal operation issue for the same `originId` (`cycle-N` / `tldr-<issueId>`) and reuses it — idempotent per cycle/scope. The `includePluginOperations:true` flag is load-bearing: the operation issue is `plugin_operation`-visibility, so a search without it would return `[]` and a duplicate would be created on every re-fire (B-1). |
</threat_model>

<verification>
- Full `node --test "test/**/*.test.mjs"` suite green, 0 fail (baseline 660; expect ≥660 plus the agent-task-delivery suite, the circuit-breaker-durable suite, and 2 new e2e cases).
- `deliveryLlmAdapter` is wired into BOTH `compile-bulletin.ts` and `editor.ts` (grep confirms); no production path imports `sessionLlmAdapter`.
- `compile-bulletin.ts` resume is gated on `isCircuitOpenDurable(ctx, BULLETIN_COMPILE_AGENT_KEY)`.
- `agent-task-delivery.ts` creates operation issues with `assigneeAgentId`, `originKind: plugin:clarity-pack:operation:*`, `surfaceVisibility: 'plugin_operation'`, and an idempotency search on `originId` that passes `includePluginOperations: true`.
- The result-readback in `agent-task-delivery.ts` accepts ONLY a comment whose body parses as JSON AND passes the schema validator — never a bare-brace comment.
- The `LlmAdapter` interface in `compile-pass-1.ts` and `compile-tldr.ts` is byte-identical to the Plan 03-02 baseline; `extractJsonObject`, `validateDraftSchema`, `verifyDraft`, `publishBulletin`, and all stub-based tests are structurally untouched.
- `src/manifest.ts` Editor-Agent `instructions.content` is issue-driven; `capabilities[]` includes `issues.wakeup`.
- `session-llm-adapter.ts` carries a DEPRECATED note and is no longer imported by production code.
- Eric's Countermoves re-drill (Task 5) PASSES: the agent's run is scoped to the operation issue (`PAPERCLIP_TASK_ID` set — primary-risk gate), a bulletin compiles + publishes via the operation issue, the Reader TL;DR populates, and the circuit breaker holds (three seeded `editor_agent_failures` rows keep the agent paused; the loop does NOT resume it).
</verification>

<success_criteria>
- The bulletin compile prompt reaches the Editor-Agent as an assigned operation issue (Path (d) from `03-AGENT-INVOCATION-GAP-RESEARCH.md`), bypassing the discarded-session-prompt root cause; the Reader TL;DR path uses the identical mechanism.
- The verified-numerics contract is intact — the worker still computes every number; the operation issue body carries them as data; pass-2 `verifyDraft` re-runs every SQL.
- Governance parity is intact and strengthened — the compile is a real audited assigned-issue agent run; no direct-HTTP LLM call exists.
- The resume-defeats-breaker infinite loop is closed — resume is gated on a durable breaker check; a breaker-tripped pause sticks across worker restarts.
- The Editor-Agent manifest instructions are issue-driven and cannot silently drift from the worker's originKind contract.
- The Phase 3 closure drill passes on Countermoves — the agent's run is scoped to the operation issue — and Phase 3 closes.
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-bulletin/03-06-SUMMARY.md`.

On a passing Task 5 drill: this plan closes the BULLETIN-COMPILE-AGENT-HEARTBEAT
blocker and unblocks the Plan 03-04 closure assertions (errata, failed-compile
banner, plugin-disable coexistence) — all of which were stuck because nothing
compiled. With 03-06's drill PASS, ROADMAP.md flips Phase 3 to COMPLETE,
STATE.md updates `phase_3_status`, and Phase 4 (Employee Chat) becomes
unblocked. The full Daily Bulletin contract — DST-safe scheduling, idempotency,
verified numerics, two-pass verifier, errata first-class, failed-compile banner,
operation-issue agent delivery, breaker-aware resume, plugin-disable-preserves-
data — is then verified end-to-end on a live instance.

Note for the runbook (research Open-Follow-up #3): watch upstream PR #3106. If
it merges and Countermoves updates, the session-prompt path becomes viable
again — but the operation-issue handoff should remain the mechanism (it is the
canonical, audit-visible pattern). Renovate watches the SDK; add a manual PR
#3106 check so a host bump does not silently change behaviour.
</output>
