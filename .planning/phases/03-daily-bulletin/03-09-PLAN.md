---
phase: 03-daily-bulletin
plan: 09
type: execute
wave: 1
depends_on: [03-08]
autonomous: false
gap_closure: true
requirements: [BULL-05, BULL-06, BULL-09, EDITOR-05, READER-02]

files_modified:
  - src/worker/bulletin/compile-pass-1.ts
  - src/worker/agents/agent-task-delivery.ts
  - test/worker/agents/agent-task-delivery.test.mjs
  - src/manifest.ts
  - package.json

must_haves:
  truths:
    - "On the live Countermoves instance, a `compile-bulletin` job fires, the Editor-Agent files a flawless BulletinDraft (with `{{NUMBER:key}}` placeholders) as an issue document keyed `compile-result`, and the worker reads that document back, validates it STRUCTURALLY (without resolving slots), and publishes a `Bulletin No. N` issue."
    - "`deliverAgentTask`'s document-readback validates a BulletinDraft body STRUCTURE-ONLY — it NEVER resolves `{{NUMBER:key}}` slots against a facts table. An agent draft whose `editorialSummary` carries unresolved `{{NUMBER:key}}` placeholders passes the readback."
    - "`validateDraftSchema`'s production caller (`compilePass1`, `compile-pass-1.ts:281`) is UNCHANGED — it still resolves slots against the real facts table, and a numeric-mismatch draft is still rejected downstream by `verifyDraft` pass-2."
    - "A `bulletins` row reaches `compile_status='published'` and no new `plugin_version='0.5.0'` `deliverAgentTask`-timeout row is recorded in `editor_agent_failures`."
    - "The Reader TL;DR (`compileTldr`) path, which rides the same `deliverAgentTask` readback, populates at `/COU/issues/COU-N`."
  artifacts:
    - path: "src/worker/bulletin/compile-pass-1.ts"
      provides: "validateDraftStructure (structural-core BulletinDraft validator, no slot resolution) — exported; validateDraftSchema delegates to it then runs the slot-resolution pass"
      contains: "export function validateDraftStructure"
    - path: "src/worker/agents/agent-task-delivery.ts"
      provides: "isResultComment/isResultDocument readback calls validateDraftStructure (structure-only) — never validateDraftSchema with an empty facts table"
      contains: "validateDraftStructure"
    - path: "test/worker/agents/agent-task-delivery.test.mjs"
      provides: "a regression test whose BulletinDraft fixture carries a {{NUMBER:key}} placeholder inside a department.editorialSummary, proving isResultDocument returns true for a draft with unresolved placeholders"
      contains: "{{NUMBER:"
    - path: "package.json"
      provides: "version 0.5.0 so npm pack derives clarity-pack-0.5.0.tgz and the durable breaker re-scopes past the stale 0.4.0 failure row"
      contains: "\"version\": \"0.5.0\""
    - path: "src/manifest.ts"
      provides: "version 0.5.0"
      contains: "version: '0.5.0'"
  key_links:
    - from: "src/worker/agents/agent-task-delivery.ts (isResultComment)"
      to: "src/worker/bulletin/compile-pass-1.ts (validateDraftStructure)"
      via: "structure-only validation import — no facts table, no replaceSlots"
      pattern: "validateDraftStructure"
    - from: "src/worker/bulletin/compile-pass-1.ts (validateDraftSchema)"
      to: "src/worker/bulletin/compile-pass-1.ts (validateDraftStructure)"
      via: "validateDraftSchema calls validateDraftStructure then runs the slot-resolution pass with the real facts table"
      pattern: "validateDraftStructure\\("
    - from: "src/worker/bulletin/compile-pass-1.ts:281 (compilePass1)"
      to: "publishBulletin via verifyDraft pass-2"
      via: "production path UNCHANGED — validateDraftSchema still resolves slots; numerics still re-verified"
      pattern: "validateDraftSchema"
---

<objective>
Close the Phase 3 closure-drill failure recorded in `.planning/debug/bulletin-compile-agent-heartbeat-gap.md` ("Plan 03-08 closure re-drill — 2026-05-17 — DID NOT PASS"). The 03-08 Option B document-readback architecture is FULLY PROVEN on the live drill — the worker created the operation issue, the Editor-Agent read its description, filed a flawless `BulletinDraft` as an issue document at the exact key `compile-result`, and the worker's `documents.get` poll fetched it. The architecture is correct. **The bug is a one-spot validator misuse.**

`agent-task-delivery.ts` `isResultComment` (which `isResultDocument` delegates to) calls `validateDraftSchema(parsed, {})` with an EMPTY facts table to check "is the agent's body a structurally-valid `BulletinDraft`". But `validateDraftSchema` (`compile-pass-1.ts:89-120`) does NOT only check structural shape — after the object/array key checks it loops every `department.editorialSummary` and runs `replaceSlots(summary, facts)`, which **throws `UNKNOWN_SLOT:<key>`** (`facts-table.ts:81-83`) for any `{{NUMBER:key}}` placeholder absent from `facts`. With `facts={}`, EVERY placeholder is unknown → `validateDraftSchema` throws → `isResultComment` catch → returns `false`. The Option-B readback rejected the agent's flawless document ~36× on every 5s poll, `deliverAgentTask` ran to its 300s deadline, and the timeout was recorded as a compile failure. The 03-08 host-faithful e2e passed only because its fixture draft had `editorialSummary: ''` — no `{{NUMBER:key}}` placeholder anywhere.

This plan: (1) splits `validateDraftSchema` into a structural core `validateDraftStructure` (the object + masthead + array-key checks) and the slot-resolution pass, exports the structural core; (2) re-points the readback `isResultComment`/`isResultDocument` at the structure-only path; (3) adds a regression test whose fixture carries a `{{NUMBER:key}}` placeholder in an `editorialSummary` — the current fixture would NOT catch this bug; (4) bumps 0.4.0 → 0.5.0, rebuilds, packs `clarity-pack-0.5.0.tgz`; (5) Eric's live Countermoves closure re-drill.

Purpose: make a bulletin compile end-to-end and publish on the live Countermoves Hostinger instance — the Phase 3 closure gate. ~15-25 LOC.
Output: a v0.5.0 build (`clarity-pack-0.5.0.tgz`) whose `deliverAgentTask` readback validates STRUCTURE ONLY, proven by Eric's live closure re-drill.

KEEP UNCHANGED (proven — do NOT re-open):
- The entire Option B document-handoff architecture: operation-issue create, the `RESULT_DELIVERY_INSTRUCTION` in the issue description, `documents.get(issueId, 'compile-result', companyId)` poll, the off-key `documents.list` scan, the comment fallback. SDK shapes are verified correct. This plan touches ONLY the validator.
- `compilePass1`'s production validation path (`compile-pass-1.ts:281`) — it must STILL resolve `{{NUMBER:key}}` slots against the real `args.factsTable`. Slot resolution there is correct and is the intended behaviour. `validateDraftSchema` keeps its full slot-resolving behaviour for that caller.
- `verifyDraft` pass-2, `publishBulletin`, `computeFactsTable`, `computeStandingNumbers`, `buildPrompt`, `replaceSlots`, `extractJsonObject` — all untouched.
- The version-scoped durable breaker (migration 0005, `circuit-breaker.ts` `CLARITY_PACK_VERSION = manifest.version`/`isCircuitOpenDurable`) — unchanged; the 0.4.0→0.5.0 bump auto-re-scopes it past the one stale `plugin_version='0.4.0'` failure row.
- `deliverAgentTask` steps 1-3 (idempotency search, `ctx.issues.create`, `requestWakeup`) and steps 4-5 readback structure — BYTE-IDENTICAL except the single validator-call swap inside `isResultComment`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-daily-bulletin/03-CONTEXT.md
@.planning/phases/03-daily-bulletin/03-RESULT-READBACK-RESEARCH.md
@.planning/debug/bulletin-compile-agent-heartbeat-gap.md
@.planning/phases/03-daily-bulletin/03-08-SUMMARY.md
@CLAUDE.md
</context>

<threat_model>
1. **Behaviour change leaking into the production path.** The risk of splitting `validateDraftSchema` is silently dropping slot resolution from `compilePass1`. MITIGATION: `validateDraftSchema` keeps its existing signature `(body, facts)` and its existing behaviour — it now just delegates the structural checks to `validateDraftStructure(body)` and then runs the SAME slot-resolution loop. `compile-pass-1.ts:281`'s call is untouched; pass-2 `verifyDraft` re-checks every numeric regardless. Task 1's acceptance criteria grep-pins that `compilePass1` still calls `validateDraftSchema` (not the structure-only path) and that the slot-resolution loop still runs inside `validateDraftSchema`.
2. **The readback admits a malformed draft.** `validateDraftStructure` still asserts the object + masthead + 4-array key checks (`actionInbox`, `departments`, `standingNumbers`, `lineageThreads`) — a draft missing any required key is still rejected by the readback. It only stops rejecting drafts whose ONLY "fault" is unresolved `{{NUMBER:key}}` placeholders — which are not a fault at the readback stage (the agent is CONTRACTED to emit them; `verifyDraft` resolves and verifies them downstream). The publish gate (`verifyDraft` pass-2) is unchanged.
3. **A hallucinated number publishes.** Cannot happen — `verifyDraft` pass-2 in the compile path re-executes every standing-number SQL query against `ctx.db.query` and rejects on any numeric mismatch BEFORE `publishBulletin`. This plan does not touch `verifyDraft`. The readback validating structure-only does not weaken the numeric gate; that gate lives downstream.
4. **The regression test fixture still does not exercise the bug.** The 03-08 fixture had `editorialSummary: ''`. MITIGATION: Task 1 acceptance criteria require the new/adjusted readback fixture to contain the LITERAL substring `{{NUMBER:` inside a `department.editorialSummary` — grep-verifiable. The test asserts `isResultDocument` returns `true` for that draft. If this fixture had existed in 03-08, the bug would have been caught locally.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Split validateDraftSchema into a structural core, re-point the readback at it, and add a {{NUMBER:key}} regression fixture</name>
  <read_first>
    - src/worker/bulletin/compile-pass-1.ts (the file being split — read lines 1-130 and lines 270-290 in full; the structural core is lines ~92-111, the slot-resolution pass is ~113-119, `compilePass1`'s call is line 281)
    - src/worker/bulletin/facts-table.ts (read `replaceSlots`, lines ~68-87 — confirm it throws `UNKNOWN_SLOT:<key>` on a missing key; this is WHY `validateDraftSchema(parsed, {})` throws on every real draft)
    - src/worker/agents/agent-task-delivery.ts (the readback bug site — read lines 1-100 the imports + header, and lines 190-260 `isResultComment`/`isResultDocument`/`deliverAgentTask` readback; line 85 imports `validateDraftSchema`, line 224 mis-calls it with `{}`)
    - test/worker/agents/agent-task-delivery.test.mjs (the regression-test target — read in full; the existing readback-test fixture uses `departments: [{ name: 'Sales', items: [], editorialSummary: '' }]` at line ~58 — an EMPTY editorialSummary, which is exactly why the green suite never caught this bug)
  </read_first>
  <action>
THE BUG (confirmed): `agent-task-delivery.ts:224` calls `validateDraftSchema(parsed, {})` with an empty facts table. `validateDraftSchema` (`compile-pass-1.ts:113-119`) loops `d.departments` and runs `replaceSlots(dept.editorialSummary, facts)`; `replaceSlots` (`facts-table.ts:81-83`) THROWS `UNKNOWN_SLOT:<key>` for any `{{NUMBER:key}}` placeholder not in `facts`. With `facts={}` every placeholder is unknown → `validateDraftSchema` throws → `isResultComment` catch → returns `false` → the readback rejects every flawless agent draft. The readback only needs STRUCTURE; slot resolution is a downstream `compilePass1` concern.

STEP 1 — Split `validateDraftSchema` in `src/worker/bulletin/compile-pass-1.ts`:

Extract the structural core (currently `compile-pass-1.ts:93-111` — the `!body || typeof body !== 'object'` guard, the `masthead` object check, and the four `Array.isArray` checks for `actionInbox`/`departments`/`standingNumbers`/`lineageThreads`) into a NEW EXPORTED function:

```ts
/**
 * Throws if `body` is not a structurally well-formed BulletinDraft — asserts
 * the object shape, masthead, and the four required arrays. Does NOT resolve
 * `{{NUMBER:key}}` slots: slot resolution is a downstream `compilePass1`
 * concern (it needs the real facts table). The Option-B result readback
 * (`agent-task-delivery.ts`) calls THIS, never the slot-resolving
 * `validateDraftSchema` — an agent draft legitimately carries unresolved
 * `{{NUMBER:key}}` placeholders that `verifyDraft` pass-2 resolves later.
 */
export function validateDraftStructure(
  body: unknown,
): asserts body is BulletinDraft {
  // ... the existing lines 93-111 checks, verbatim ...
}
```

Then rewrite `validateDraftSchema` so it calls the structural core first, then runs the SAME slot-resolution pass it runs today — its signature `(body: unknown, facts: FactsTable)` and external behaviour stay IDENTICAL for `compilePass1`:

```ts
export function validateDraftSchema(
  body: unknown,
  facts: FactsTable,
): asserts body is BulletinDraft {
  validateDraftStructure(body);
  // Resolve every department prose summary — replaceSlots throws (with a
  // tagged `.slot`) if a placeholder references an unknown factsTable key.
  for (const dept of (body as BulletinDraft).departments as Array<{
    editorialSummary?: unknown;
  }>) {
    if (typeof dept.editorialSummary === 'string') {
      replaceSlots(dept.editorialSummary, facts);
    }
  }
}
```

`compile-pass-1.ts:281`'s `validateDraftSchema(parsed, args.factsTable)` call MUST stay EXACTLY as-is — `compilePass1` still resolves slots against the real facts table. Do NOT change it.

STEP 2 — Re-point the readback in `src/worker/agents/agent-task-delivery.ts`:
- Change the import on line 85 from `import { extractJsonObject, validateDraftSchema } from '../bulletin/compile-pass-1.ts';` to `import { extractJsonObject, validateDraftStructure } from '../bulletin/compile-pass-1.ts';`.
- In `isResultComment` (line ~224), replace `validateDraftSchema(parsed, {});` with `validateDraftStructure(parsed);`.
- Update the doc-comment block above `isResultComment` (lines ~193-209) — remove the now-false sentence about calling the validator "with an EMPTY facts table"; state that the readback calls `validateDraftStructure` which checks SHAPE ONLY and never resolves `{{NUMBER:key}}` slots, and that slot resolution + numeric verification happen downstream in `compilePass1`/`verifyDraft`.
- `isResultDocument` already delegates to `isResultComment` — no change needed; it inherits the fix.
- `deliverAgentTask` steps 1-3 and the steps 4-5 poll structure stay BYTE-IDENTICAL — the ONLY change in this file is the import line, the one validator call, and the doc-comment.

STEP 3 — Regression test in `test/worker/agents/agent-task-delivery.test.mjs`:
- The existing readback-test BulletinDraft fixture uses `editorialSummary: ''` — adjust it (or add a sibling fixture used by the readback/e2e tests) so at least one `department.editorialSummary` contains a literal `{{NUMBER:key}}` placeholder, e.g. `editorialSummary: 'Sales closed {{NUMBER:deals_won}} deals this week.'`. This is the fixture shape that REPRODUCES the live bug.
- Add an explicit unit test: feed that fixture (a structurally-valid BulletinDraft whose `editorialSummary` carries an unresolved `{{NUMBER:key}}`) through `isResultDocument`/`isResultComment` (via `deliverAgentTask`'s document-poll path, or by exporting/testing the validator path) and assert it returns `true` — i.e. the readback ACCEPTS a draft with unresolved placeholders. Before this fix that draft would have been rejected.
- Ensure the existing host-faithful e2e document-poll test (`compile-result` document → `deliverAgentTask` resolves) uses a fixture whose `editorialSummary` carries a `{{NUMBER:key}}` placeholder — so the end-to-end path is also covered. Re-point any test that still asserts the empty-summary shape.
- Do NOT weaken any test asserting `validateDraftSchema` still resolves slots for the `compilePass1` path; if such a test exists it must stay green (slot resolution preserved there).
  </action>
  <verify>
    - `npx tsc --noEmit` exits 0.
    - `node --test test/worker/agents/agent-task-delivery.test.mjs` exits 0.
    - `node --test test/worker/bulletin/compile-pass-1.test.mjs` exits 0 (if the file exists — confirms `validateDraftSchema`'s slot-resolving behaviour is unchanged for `compilePass1`).
    - `node --test "test/**/*.test.mjs"` exits 0 (full suite — confirm no regression).
  </verify>
  <acceptance_criteria>
    - `src/worker/bulletin/compile-pass-1.ts` contains the literal string `export function validateDraftStructure`.
    - `src/worker/bulletin/compile-pass-1.ts` `validateDraftSchema` body contains a call `validateDraftStructure(` AND still contains `replaceSlots(` (slot resolution preserved for `compilePass1`).
    - `compile-pass-1.ts` line ~281 still contains `validateDraftSchema(parsed, args.factsTable)` (production caller unchanged) — grep `validateDraftSchema(parsed, args.factsTable` returns a hit.
    - `src/worker/agents/agent-task-delivery.ts` imports `validateDraftStructure` from `../bulletin/compile-pass-1.ts` and does NOT contain the string `validateDraftSchema` anywhere (the readback no longer references it).
    - `src/worker/agents/agent-task-delivery.ts` `isResultComment` calls `validateDraftStructure(` and does NOT call any validator with a `{}` second argument.
    - `test/worker/agents/agent-task-delivery.test.mjs` contains the literal substring `{{NUMBER:` inside a `department`/`editorialSummary` fixture.
    - `node --test test/worker/agents/agent-task-delivery.test.mjs` exits 0; `node --test "test/**/*.test.mjs"` exits 0 with 0 failures.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Bump version 0.4.0 -> 0.5.0, rebuild all three artifacts, and pack clarity-pack-0.5.0.tgz</name>
  <read_first>
    - package.json (line 2 — the `"version": "0.4.0"` field; `npm pack` derives the tarball name from it)
    - src/manifest.ts (line ~24 — the `version: '0.4.0'` field and its comment; confirm no other Option-C-era strings remain — this plan does not touch the manifest body, only the version)
    - src/worker/agents/circuit-breaker.ts (confirm `CLARITY_PACK_VERSION` is sourced from `manifest.version` — the durable breaker version-scope auto-tracks the bump; no edit needed, just confirm the chain so the stale `0.4.0` failure row is re-scoped away)
    - scripts/build-worker.mjs, scripts/build-ui.mjs (the build commands — pnpm is NOT on PATH; invoke with `node`)
    - test/worker/agents/agent-task-delivery.test.mjs (any version-string drift-guard test that asserts the manifest/package version — it must be updated to 0.5.0 if present)
  </read_first>
  <action>
1. `package.json`: change `"version": "0.4.0"` to `"version": "0.5.0"`. `npm pack` derives the tarball filename from `package.json`, and the Task 3 drill expects `clarity-pack-0.5.0.tgz`.

2. `src/manifest.ts`: change `version: '0.4.0'` to `version: '0.5.0'` and update the adjacent version comment to reference Plan 03-09 / the readback structure-only validator fix. The 0.4.0→0.5.0 bump re-scopes the durable circuit breaker (`CLARITY_PACK_VERSION` imports `manifest.version`) past the one stale `plugin_version='0.4.0'` `deliverAgentTask`-timeout row from the 03-08 drill — a fresh 0.5.0 install is not DOA on that failure history.

3. If any test in `test/worker/agents/agent-task-delivery.test.mjs` (or elsewhere) asserts a literal version string, update it to `0.5.0` so the suite stays green.

4. Rebuild all three artifacts (pnpm NOT on PATH — use `node`):
   - `node scripts/build-worker.mjs`
   - `node scripts/build-ui.mjs`
   - `npx tsc --project tsconfig.manifest.json`

5. `npm pack` — produces `clarity-pack-0.5.0.tgz` in the repo root. Compute its sha256 and record the tarball name + sha256 in the Task 2 commit message and the SUMMARY.

6. Final full verification: `npx tsc --noEmit` clean; `node --test "test/**/*.test.mjs"` exits 0; all three builds clean.
  </action>
  <verify>
    - `npx tsc --noEmit` exits 0.
    - `node --test "test/**/*.test.mjs"` exits 0 with 0 failures.
    - `node scripts/build-worker.mjs`, `node scripts/build-ui.mjs`, `npx tsc --project tsconfig.manifest.json` all exit 0.
    - `npm pack` produces a file literally named `clarity-pack-0.5.0.tgz`.
  </verify>
  <acceptance_criteria>
    - `package.json` contains the literal string `"version": "0.5.0"` and does NOT contain `"version": "0.4.0"`.
    - `src/manifest.ts` contains the literal string `version: '0.5.0'` and does NOT contain `version: '0.4.0'`.
    - A file `clarity-pack-0.5.0.tgz` exists in the repo root after `npm pack`.
    - The Task 2 commit message records the literal tarball name `clarity-pack-0.5.0.tgz` and its sha256.
    - `node --test "test/**/*.test.mjs"` exits 0 with 0 failures.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Eric's Countermoves closure re-drill — the ONLY proof Phase 3 is closed</name>
  <what-built>
A v0.5.0 build (`clarity-pack-0.5.0.tgz`) in which `deliverAgentTask`'s result readback validates a `BulletinDraft` STRUCTURE-ONLY. `validateDraftSchema` was split into `validateDraftStructure` (object + masthead + 4-array key checks, no slot resolution) and the slot-resolution pass; `isResultComment`/`isResultDocument` now call `validateDraftStructure` so an agent draft carrying unresolved `{{NUMBER:key}}` placeholders in its `editorialSummary` PASSES the readback. `compilePass1`'s production validation still resolves slots against the real facts table, and `verifyDraft` pass-2 still re-verifies every standing number before publish — both unchanged. The Option B document-handoff architecture (operation issue, `compile-result` document key, `documents.get` poll) is unchanged — it was proven on the 03-08 drill; only the validator was wrong. The 0.4.0→0.5.0 bump re-scopes the durable breaker past the one stale `plugin_version='0.4.0'` failure row.
  </what-built>
  <how-to-verify>
Per the project runbook discipline — MemPalace `clarity_pack/runbook` holds the recorded snapshot/install pattern; query it before running snapshot/install commands; do NOT extrapolate from spec. All `pnpm paperclipai plugin ...` commands must `cd ~/paperclip` first (workspace-script gotcha).

PRE-DRILL SETUP:
1. SSH to Countermoves: `ssh -i $HOME\.ssh\countermoves_vps_ed25519 eric@82.29.197.74`.
2. Bookended-by-snapshots rule (CLAUDE.md): take a verified Postgres + filesystem snapshot via the Phase 1 safety CLI BEFORE the install. Confirm the snapshot + `clarity-safety gate` pass (snapshot fresh < 15 min, restore-and-smoke verified).
3. Uninstall the currently-installed v0.4.0 build: `cd ~/paperclip && pnpm paperclipai plugin uninstall clarity-pack`.
4. Clear the stale durable-breaker row that the v0.4.0 drill timeout left behind:
   `sudo -u postgres psql -d paperclip_countermoves -c "DELETE FROM plugin_clarity_pack_cdd6bda4bd.editor_agent_failures WHERE plugin_version='0.4.0';"`
   (The 0.5.0 bump also version-scopes the breaker away from this row — this DELETE is belt-and-suspenders so a fresh 0.5.0 install is not DOA.)
5. scp `clarity-pack-0.5.0.tgz` to the box and install via the recorded runbook pattern (`bash ~/clarity-pack/scripts/install-helper.sh <tgz>`). Confirm `✓ Installed clarity-pack v0.5.0`.

DRILL — bulletin compile end-to-end:
6. The `compile-bulletin` job loops every `*/1`; the bootstrap `next_due_at` is in the past, so a compile fires within ~1 minute. Watch the worker log.
7. CONFIRM the worker creates a fresh operation issue (`plugin:clarity-pack:operation:bulletin-compile`, e.g. COU-N) assigned to the Editor-Agent. Open that issue — its DESCRIPTION must contain the compile prompt followed by the "store your finished result as an issue DOCUMENT ... using the EXACT document key `compile-result`" instruction.
8. CONFIRM the Editor-Agent runs scoped to that issue, produces a `BulletinDraft` (whose `editorialSummary` fields legitimately carry `{{NUMBER:key}}` placeholders), and files it as an issue DOCUMENT at key `compile-result` (check the issue's Documents section).
9. HARD GATE — CONFIRM the worker reads the document back and the structure-only readback ACCEPTS it: the worker log shows the document received (NOT a poll-loop rejecting it ~36×), a `bulletins` row reaches `cycle_number >= 1` with `compile_status='published'`, and a Paperclip issue `Bulletin No. N` is created. `editor_agent_failures` shows NO new `plugin_version='0.5.0'` row from a `deliverAgentTask` timeout. If the worker still times out and the document is rejected — STOP, capture the worker log + the operation issue's document body, the bulletin is not closed.
10. Open the Bulletin page at `/COU/bulletin` — confirm it renders the published bulletin (masthead, department sections, Standing Numbers panel, action inbox) per `sketches/paperclip-fix-bulletin.html`.
11. Spot-check BULL-06: pick one number in the Standing Numbers panel and confirm it is grep-able to a SQL query (the verified-numerics contract — `verifyDraft` pass-2 must have resolved the `{{NUMBER:key}}` slots against the real facts table and gated publish).
12. READER-02 / EDITOR-05 spot-check: open a Reader tab on an issue (`/COU/issues/COU-N`) — confirm the TL;DR strip populates (`compileTldr` rides the same `deliverAgentTask` document readback).

ROLLBACK PATH: if any HARD GATE fails, uninstall (`cd ~/paperclip && pnpm paperclipai plugin uninstall clarity-pack`) to halt the loop; data is preserved (no `--purge`); restore from the pre-drill snapshot only if the instance is degraded. Capture the worker log + `editor_agent_failures` rows + the operation issue's document section for the gap write-up.
  </how-to-verify>
  <resume-signal>Type `approved — phase 3 closed` if the bulletin compiles + publishes + renders and the Reader TL;DR populates. Otherwise describe what failed (worker log + the operation issue's document section + `editor_agent_failures` rows) and the planner routes a gap-closure plan.</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` clean.
- `node --test "test/**/*.test.mjs"` — 0 failures (baseline 689 from 03-08; expect a small net change from the added regression test).
- `node scripts/build-worker.mjs`, `node scripts/build-ui.mjs`, `npx tsc --project tsconfig.manifest.json` — all clean.
- `npm pack` → `clarity-pack-0.5.0.tgz`.
- Grep gates: `compile-pass-1.ts` exports `validateDraftStructure` and `validateDraftSchema` still calls both `validateDraftStructure(` and `replaceSlots(`; `compilePass1` still calls `validateDraftSchema(parsed, args.factsTable`; `agent-task-delivery.ts` imports `validateDraftStructure` and contains NO `validateDraftSchema`; the test file contains `{{NUMBER:` in an `editorialSummary` fixture; `package.json` + `manifest.ts` both show `0.5.0`.
- LIVE (Task 3): a bulletin compiles + publishes on Countermoves via the document readback; the structure-only validator accepts the agent's placeholder-bearing draft; the Reader TL;DR populates; no `0.5.0` `deliverAgentTask`-timeout rows in `editor_agent_failures`.
</verification>

<success_criteria>
- Phase 3 closes on Eric's `approved — phase 3 closed` after a passing Countermoves re-drill.
- A bulletin auto-compiles, the worker reads the agent's `compile-result` document back, the structure-only readback ACCEPTS the placeholder-bearing draft, pass-2 verifies the numerics, and a `Bulletin No. N` issue publishes and renders at `/COU/bulletin` (BULL-05, BULL-06, BULL-09).
- The Reader TL;DR path (`compileTldr`) rides the same readback and populates (READER-02).
- The circuit breaker holds — version-scoped at 0.5.0 (EDITOR-05).
- `compilePass1`'s production slot-resolution + `verifyDraft` numeric gate are provably unchanged — no behaviour change in the publish path.
</success_criteria>

<output>
After completion, create `.planning/phases/03-daily-bulletin/03-09-SUMMARY.md` per the summary template. Record: the validator split (`validateDraftStructure` extracted + exported, `validateDraftSchema` delegates + keeps slot resolution), the readback re-point, the `{{NUMBER:key}}` regression fixture, the 0.4.0→0.5.0 bump, the suite delta, the `clarity-pack-0.5.0.tgz` sha256, and the Task 3 drill outcome.
</output>
