---
context: phase
phase: 04-employee-chat
plan: 1
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-05-18T21:05:00.000Z
---

# Continue Here — Phase 04 Employee Chat, Wave 1 / Plan 04-01

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Extrapolating VPS commands from the plan | Claude proposed Countermoves snapshot/gate commands by reading the plan text instead of the runbook, and hit three errors (sudo-gated `db.env`, missing `--mode=postgres`, `gate` misused as a verifier). | advisory | Query MemPalace `clarity_pack/runbook` for "snapshot Countermoves" BEFORE proposing any VPS command. Working procedure now in MemPalace drawer + local memory `countermoves-safety-cli-invocation.md`. |
| Guessing REST route shapes | The spike probe assumed all issue routes are company-scoped; per-issue sub-routes are actually flat (`/api/issues/{id}/...`). Two probe runs failed before discovery. | advisory | Fixed in commit d36c81c. Discover new Paperclip REST routes with a curl `%{http_code}` loop, never guess. |

_Both advisory — structural fixes already shipped. No understanding-check gate needed._

<current_state>
Phase 04 (Employee Chat) executing — `/gsd:execute-phase 4`. 6 plans, 6 waves, fully
sequential. Plan 04-01 (the falsify-first spike) is at **Task 3** — the live probe (Task 2)
is DONE and PASSED.

Plan 04-01 Tasks 1 and 2 are complete. Task 3 (write the findings doc) is autonomous desk
work — no VPS, no human action. Then Waves 2-6 build the actual chat feature.
</current_state>

## Spike Result — Phase 4 Gate Verdict: GO

The route-fixed probe ran successfully on live Countermoves (run 2 in
`04-01-probe-output.txt`, lines 479-613). Raw findings:

- **D-01 / OQ-4 — PASS.** The CEO employee-agent woke on a posted comment and replied
  `"2 + 2 = **4**."` as an `issue_comments` row (NOT a document). `authorType: "agent"`,
  `authorAgentId` set, `documentsFiledByAgent: 0`. The native `issue_commented` heartbeat
  wake works on the live host. Reply comment id `97d84be4-698d-4def-a98a-0ba9f137f9c4`.
- **OQ-2 — comment row shape recorded.** Keys: `id, companyId, issueId, authorAgentId,
  authorUserId, authorType, createdByRunId, body, presentation, metadata, createdAt,
  updatedAt`. The row carries `issueId` + full `body` — 04-03's stream bridge can use the
  row directly, or derive from `event.entityId` + a `listComments` re-fetch.
- **OQ-3 — STATUS-FLIP-NOT-NEEDED.** A comment posted on a `done` topic alone re-woke the
  agent — no status flip, no `requestWakeup` required. 04-03's auto-reopen (D-06) still
  flips status for UX/status correctness, but the wake itself is free.
- **OQ-1 — NO-PATH (with a lead).** No plugin-accessible upload/write path
  (`POST /issues/{id}/documents` 404, `POST /companies/{id}/assets` 404). BUT
  `GET /api/issues/{id}/attachments` returns 200 — a read-side route 04-04 should inspect.
  CHAT-07 ships degraded as planned: attach button disabled with an explicit message.

REST route fact (verified live, in the probe + MemPalace): issue/agent COLLECTION routes
are company-scoped (`/api/companies/{id}/issues|agents`); per-issue SUB-routes are FLAT
(`/api/issues/{id}`, `/api/issues/{id}/comments|wakeup|documents|attachments`).

<completed_work>
- Task 1: Build the spike probe harness — Done (commit b91d887; route fix d36c81c).
- Task 2: Run the probe on live Countermoves — Done. Snapshot bookend `2026-05-18T20-15-56Z`
  taken; route-fixed probe ran and produced a clean PASS/GO result.
</completed_work>

<remaining_work>
- Task 3: Write `04-01-SPIKE-FINDINGS.md` (the five headers `## D-01`, `## OQ-2`, `## OQ-3`,
  `## OQ-1`, `## Phase 4 Gate Verdict` — GO; end with `## SPIKE COMPLETE`), add
  `test/phases/04-01-spike-findings.test.mjs`, create `04-01-SUMMARY.md`, update STATE.md
  + ROADMAP.md plan progress. See 04-01-PLAN.md Task 3 for the exact spec.
- Waves 2-6 (plans 04-02..04-06): not started. Cleared by the GO verdict.
  - 04-02 (autonomous, tdd), 04-03 (autonomous, tdd), 04-04 (autonomous) — autonomous builds.
  - 04-05, 04-06 (autonomous: false) — will need human checkpoints.
</remaining_work>

<decisions_made>
- Plain (unverified) snapshot is a proportionate bookend for the probe (no migration/install).
- CEO agent (`b2a22e50-d772-4b70-bb50-4f4e93c2e984`) was the spike employee-agent.
- Phase 4 Gate = GO — D-01 PASS; downstream design inputs: 04-03 folds the reply-channel
  instruction into new topic descriptions (probe used it, agent complied); 04-03 auto-reopen
  need not call `requestWakeup`; 04-04 builds the disabled-attach UI (CHAT-07 degraded).
</decisions_made>

<blockers>
- None. Task 2's human-action checkpoint is resolved.
</blockers>

## Required Reading (in order)
1. `.planning/phases/04-employee-chat/04-01-PLAN.md` — Task 3 spec (findings doc headers, test).
2. `.planning/phases/04-employee-chat/04-01-probe-output.txt` — the probe run; run 2 (lines 479-613) is the valid result.
3. `.planning/phases/04-employee-chat/04-RESEARCH.md` — OQ-1..OQ-4 definitions the findings doc resolves.

## Infrastructure State
- **Countermoves VPS**: 4 orphan `[SPIKE]`-tagged issues from probe runs (`b97725e0`+`1cff07b4` failed run, `eba2ed7a`+`fc1a0339` good run). Bulk-delete after findings written — harmless, snapshot-covered.
- `~/clarity-pack/scripts/safety/debug-snapshot.mjs` — leftover diagnostic on the VPS, safe to `rm`.
- Snapshot `2026-05-18T20-15-56Z` exists — bookend, no longer needed but harmless.
- A `spike` tmux session may still be alive on the VPS — can be killed (`tmux kill-session -t spike`); no live work pending.

<context>
The hard part (the live spike) is done and passed. Everything remaining is desk work:
finish plan 04-01 Task 3, then the chat feature builds (Waves 2-6). config: use_worktrees=false
(sequential executor on main tree), auto_advance=true, executor=opus.
</context>

<next_action>
Run `/gsd:resume-work`, then continue `/gsd:execute-phase 4`:
1. Spawn a continuation gsd-executor for plan 04-01 to do Task 3 — write `04-01-SPIKE-FINDINGS.md`
   (GO verdict, using the Spike Result section above), add `test/phases/04-01-spike-findings.test.mjs`,
   create `04-01-SUMMARY.md`, update STATE.md + ROADMAP.md. This closes Wave 1.
2. Then proceed to Wave 2 (plan 04-02, autonomous) and onward through the wave sequence.
</next_action>
