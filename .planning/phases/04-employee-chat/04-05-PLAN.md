---
phase: 04-employee-chat
plan: 05
type: execute
wave: 5
depends_on: [04]
files_modified:
  - src/ui/surfaces/chat/index.tsx
  - src/ui/surfaces/chat/roster-rail.tsx
  - src/ui/surfaces/chat/topic-strip.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - src/ui/surfaces/chat/composer.tsx
  - src/ui/surfaces/chat/context-rail.tsx
  - src/ui/surfaces/chat/reasoning-panel.tsx
  - src/ui/styles/chat.css
  - src/ui/index.tsx
  - src/ui/surfaces/chat-stub.tsx
autonomous: false
requirements: [CHAT-10]

must_haves:
  truths:
    - "An opted-in user opens the Chat route and sees the four-region shell — roster rail, topic strip, message thread, context rail — matching the sketch"
    - "Sending a message renders optimistically and reconciles to its server position on the issue.comment.created stream event; a failed send stays in the thread with a Retry affordance"
    - "Real-time updates arrive via usePluginStream; on stream error the UI falls back to usePoll with a reconnecting indicator"
    - "The attach button is disabled with an explicit Attachments are temporarily unavailable message when no upload path exists"
    - "Agent message reasoning blocks render in a collapsed Show reasoning panel; promote/pin affordances and decision-recorded typeforms render per the sketch"
  artifacts:
    - path: "src/ui/surfaces/chat/index.tsx"
      provides: "ChatPage — opt-in/company/user three-gate then the four-region shell"
      exports: ["ChatPage"]
    - path: "src/ui/surfaces/chat/message-thread.tsx"
      provides: "Stream-primary message thread with optimistic send + poll fallback"
      min_lines: 60
    - path: "src/ui/styles/chat.css"
      provides: "[data-clarity-surface=chat]-scoped stylesheet, sketch-faithful"
      contains: "[data-clarity-surface=\"chat\"]"
  key_links:
    - from: "src/ui/surfaces/chat/message-thread.tsx"
      to: "chat:<companyId> stream channel"
      via: "usePluginStream primary, usePoll fallback"
      pattern: "usePluginStream"
    - from: "src/ui/index.tsx"
      to: "src/ui/surfaces/chat/index.tsx"
      via: "ChatPage export swap + chat.css inject"
      pattern: "ChatPage"
---

<objective>
Build the Employee Chat UI surface — replace the `chat-stub.tsx` placeholder with the real four-region chat page: roster rail, topic strip, central message thread (optimistic send + stream-primary realtime + poll fallback), composer, context rail, and the reasoning panel. Scoped `chat.css` matching `sketches/paperclip-fix-employee-chat.html` line-for-line.

This consumes the worker handlers from 04-03/04-04 entirely through the bridge hooks — no raw `fetch`, no direct host RPC. Chat is a named XSS vector: all message bodies render as untrusted text, never `dangerouslySetInnerHTML`.

CHAT-07 ships per the 04-01 spike OQ-1 verdict: if NO-PATH, the attach button is permanently disabled with the explicit "Attachments are temporarily unavailable" message — that satisfies CHAT-07's graceful-degrade clause. If PATH-FOUND, wire the upload to that path with a ~10MB cap.
Output: Seven chat UI files + the scoped stylesheet + the `src/ui/index.tsx` export swap; `chat-stub.tsx` deleted.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-employee-chat/04-CONTEXT.md
@.planning/phases/04-employee-chat/04-RESEARCH.md
@.planning/phases/04-employee-chat/04-PATTERNS.md
@.planning/phases/04-employee-chat/04-01-SPIKE-FINDINGS.md
@sketches/paperclip-fix-employee-chat.html
@.claude/skills/sketch-findings-clarity-pack/SKILL.md

<interfaces>
<!-- From 04-PATTERNS.md. The exact analog UI shapes. -->

src/ui/surfaces/bulletin/index.tsx — page-surface template + the three-gate:
```typescript
export function BulletinPage(_props?: PluginPageProps): React.ReactElement {
  const { optedIn, loading: optInLoading } = useOptIn();                      // GATE 1
  if (optInLoading) return <ClaritySurfaceRoot name="bulletin"><p>Loading…</p></ClaritySurfaceRoot>;
  if (!optedIn) return <ClaritySurfaceRoot name="bulletin"><EnableClarityCta surfaceName="Bulletin" /></ClaritySurfaceRoot>;
  return <BulletinPageOptedIn />;
}
function BulletinPageOptedIn() {
  const { companyId } = useResolvedCompanyId();   // GATE 2
  const { userId } = useResolvedUserId();         // GATE 3 — MUST use resolver, not bare useHostContext().userId
  // ...
}
// Gate order LOCKED: useOptIn -> useResolvedCompanyId -> useResolvedUserId
// belt-and-suspenders: also handle a worker { error: 'OPT_IN_REQUIRED' } result.
```

src/ui/surfaces/situation-room/index.tsx SituationRoomBody — stream/poll compose:
```typescript
const { data } = usePluginData<...>('situation.snapshot', { userId, companyId });
const followerBridge = usePollWithLeader<...>({ key, fetcher, intervalMs, pauseOnHidden: true });
if (followerBridge.error?.kind === 'PLUGIN_DISABLED') { /* terminal */ }
```
For chat: usePluginStream(`chat:${companyId}`) PRIMARY -> { events, lastEvent, connecting, connected, error, close }; usePoll FALLBACK on stream error.

src/ui/surfaces/bulletin/action-inbox.tsx — usePluginAction + busy-state:
```typescript
const send = usePluginAction('chat.send');
const [busy, setBusy] = React.useState(false);
// D-10: on a thrown / { error } result the optimistic bubble STAYS with a Retry affordance.
// SPA nav via useHostNavigation().linkProps — NEVER a raw <a href> (SCAF-09).
```

src/ui/index.tsx — barrel + style inject:
```typescript
import bulletinCss from './styles/bulletin.css';
export { ChatPage } from './surfaces/bulletin/index.tsx';   // pattern for the swap
injectClarityStyles(bulletinCss, 'data-clarity-pack-bulletin-styles');   // host does NOT auto-load sibling CSS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Chat page shell, roster rail, topic strip, context rail, scoped CSS</name>
  <read_first>
    - sketches/paperclip-fix-employee-chat.html (the visual contract — cite line numbers)
    - src/ui/surfaces/bulletin/index.tsx (page-surface + three-gate template)
    - src/ui/surfaces/situation-room/agent-card.tsx (roster-rail analog)
    - src/ui/surfaces/bulletin/action-inbox.tsx (topic-strip analog — selectable card strip)
    - src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx (context-rail analog)
    - src/ui/styles/bulletin.css (scoped-stylesheet template)
    - src/ui/primitives/clarity-surface-root.tsx (ClaritySurfaceRoot — 'chat' is already a valid name)
    - src/ui/index.tsx (barrel + injectClarityStyles)
  </read_first>
  <action>
Create `src/ui/surfaces/chat/index.tsx` exporting `ChatPage` (the manifest `clarity-chat` slot `exportName` MUST stay `ChatPage`). Copy the `bulletin/index.tsx` three-gate verbatim: `useOptIn` → `useResolvedCompanyId` → `useResolvedUserId`, opted-out renders `<EnableClarityCta surfaceName="Chat" />`, belt-and-suspenders on a worker `{ error: 'OPT_IN_REQUIRED' }` result. `userId` MUST come from `useResolvedUserId`, never bare `useHostContext().userId` (production null-userId gap). Wrap the body in `<ClaritySurfaceRoot name="chat">`.

The shell is the 3-column grid `264px 1fr 340px` per `sketches/paperclip-fix-employee-chat.html:44` — roster / thread / context rail. Build:
- `roster-rail.tsx` — `usePluginData('chat.roster', {companyId, userId})`; render the employee list with status dots + unread badges per sketch ll. 48-98 (`.roster`, `.emp`, `.emp .av`, status `.s-live`/`.s-warn`/`.s-alert`/`.s-idle`, `.badge`). OMIT the "Group threads" rail section (sketch ll. 474-486) — group threads are v2 (D-03). Selecting an employee sets the active employee state threaded down to the topic strip + thread.
- `topic-strip.tsx` — `usePluginData('chat.topics', {employeeAgentId, companyId, userId})`; render the horizontal `.topics` strip per sketch ll. 139-157 (`.topic`, `.topic.active`, `.topic .id` showing `CHT-NN`, `.dot`). A `+ New topic` button in the thread head (`.head-actions`, sketch ll. 503-506) calls `usePluginAction('chat.topic.create')`.
- `context-rail.tsx` — the `.ctx` right rail per sketch ll. 342-392 + 692-738: agent card, "Active tasks owned", "You owe", "Recent attachments", "Quick actions". Drive it from the selected topic + roster data already fetched (no new handler).
- `chat.css` — copy `bulletin.css` structure: EVERY selector prefixed `[data-clarity-surface="chat"]`; the scope-root rule carries the warm-dark palette CSS variables from sketch ll. 11-22 + Geist/Geist Mono/Instrument Serif. A leading `@import url("https://fonts.googleapis.com/...")` for fonts is acceptable. Cite the sketch line numbers in CSS comments for the shell, `.roster`, `.topics`, `.messages`, `.bubble`, `.reasoning`, `.attach`, `.decision-msg`, composer.

Render all employee/topic/agent text as untrusted text — never `dangerouslySetInnerHTML`. SPA navigation via `useHostNavigation().linkProps`, never raw `<a href>`.
  </action>
  <verify>
    <automated>node --test test/ui/chat-shell.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/surfaces/chat/index.tsx` exists and exports `ChatPage`
    - `roster-rail.tsx`, `topic-strip.tsx`, `context-rail.tsx`, `chat.css` exist
    - `chat.css` — every rule prefixed `[data-clarity-surface="chat"]`; grep finds no unprefixed selector (mirror the bulletin.css CSS-scope test)
    - `index.tsx` contains `useOptIn`, `useResolvedCompanyId`, `useResolvedUserId` in that order
    - No file under `src/ui/surfaces/chat/` contains `dangerouslySetInnerHTML` or a raw `fetch(`
    - `roster-rail.tsx` does NOT render a "Group threads" section
    - `node --test` exits 0 for the new shell test
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Message thread (optimistic + stream/poll), composer, reasoning panel, attachment graceful-degrade</name>
  <read_first>
    - sketches/paperclip-fix-employee-chat.html (ll. 159-340 — messages, bubbles, reasoning, attach, decision-msg, composer)
    - src/ui/surfaces/situation-room/index.tsx (SituationRoomBody — stream/poll compose template)
    - src/ui/surfaces/bulletin/action-inbox.tsx (usePluginAction + busy-state)
    - src/ui/surfaces/reader/prose-with-ref-chips.tsx (reasoning-panel parse-then-render analog + ref-chip usage)
    - src/ui/primitives/use-poll.ts (the realtime fallback primitive)
    - src/ui/primitives/ref-chip.tsx (inline BEAAA-NNN chips)
    - src/shared/reference-resolver.ts (batch ref resolver)
    - .planning/phases/04-employee-chat/04-01-SPIKE-FINDINGS.md (OQ-1 attachment verdict)
  </read_first>
  <action>
Create `src/ui/surfaces/chat/message-thread.tsx`, `composer.tsx`, `reasoning-panel.tsx`.

`message-thread.tsx` — `usePluginData('chat.messages', {topicIssueId, companyId, userId})` for the initial thread, `usePluginStream(\`chat:${companyId}\`)` as the realtime PRIMARY (returns `{events, lastEvent, connecting, connected, error, close}`), `usePoll`/`usePollWithLeader` as the FALLBACK when `error` is non-null — show a small "reconnecting" indicator while degraded (visibility-pause is mandatory; `PLUGIN_DISABLED` is a terminal stop). Render `.messages` per sketch ll. 159-211: day dividers (ll. 162-169), `.msg`/`.bubble` (Eric right-aligned `.msg.me`, agent left), `.b-meta`, `.b-text`. Order the thread strictly by server `created_at` — never client send time (PITFALLS 11.4).

Optimistic send (D-10, CHAT-06, RESEARCH Pattern 3): the composer generates `crypto.randomUUID()` as `message_uuid` before send, the thread renders the optimistic bubble immediately keyed by `message_uuid`; on the `issue.comment.created` stream event the bubble reconciles to its server position; on send failure the bubble STAYS marked "Failed to send" with a Retry affordance — Retry re-sends the SAME `message_uuid` (dedupe). Pending UX (D-04): after send, show a `{Employee} is working…` row; after a configurable timeout (3-5 min — read `instanceConfigSchema` if 04-02 exposed it, else a constant) show the quiet "no reply yet — agent may be paused or busy" notice. Render agent-message reasoning panels (Task: parse via `reasoning-panel.tsx`), `.resolved` outcome pills, `.promote` hover affordances (`↗ Promote to task` → `usePluginAction('chat.promote')`, `⚑ Pin` → `usePluginAction('chat.pin')`), and the centered `.decision-msg` typeform (sketch ll. 293-305) for decision-recorded messages.

`composer.tsx` — the `.composer` per sketch ll. 307-340: textarea, tools row (`📎 Attach`, `⚑ Approve`, `↗ New task`, `⌗ Reference`), `⌘+Enter` send. `Send` uses `usePluginAction('chat.send')` with `{topicIssueId, body, message_uuid, companyId, userId}` and the D-10 failure handling above. **Attachment graceful-degrade (CHAT-07):** read `04-01-SPIKE-FINDINGS.md` OQ-1 verdict — if NO-PATH, the `📎 Attach` button is rendered DISABLED with the explicit tooltip/inline text "Attachments are temporarily unavailable"; if PATH-FOUND, wire the upload to that path with a ~10MB client-side size cap and a generic attachment chip (no inline preview — D-12; full previewers are Phase 5). The `⌗ Reference` tool inserts a ref-chip via the resolver; slash-commands/@-mention may degrade to plain text (Claude's discretion).

`reasoning-panel.tsx` — pure client-side parse (analog `prose-with-ref-chips.tsx`): parse the D-14 delimited reasoning block out of the comment body, render it as a collapsed `<details>` "Show reasoning" panel per sketch ll. 213-230. If a reply has no block, render a plain bubble — no hard dependency. The block is parsed as TEXT, never `eval`. Inline `BEAAA-NNN` references in message bodies and reasoning bullets render via `ref-chip.tsx` + `resolveRefs` (the `prose-with-ref-chips.tsx` usage).

All message bodies + reasoning text render as untrusted text — no `dangerouslySetInnerHTML`, no raw HTML, no `javascript:` URLs.
  </action>
  <verify>
    <automated>node --test test/ui/chat-message-thread.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `message-thread.tsx`, `composer.tsx`, `reasoning-panel.tsx` exist
    - `message-thread.tsx` contains `usePluginStream` and references `usePoll` as the fallback
    - `composer.tsx` contains the string `Attachments are temporarily unavailable` and the attach button is disabled when the OQ-1 verdict is NO-PATH
    - `composer.tsx` generates a `message_uuid` via `crypto.randomUUID` and a failed send retains a Retry affordance
    - `reasoning-panel.tsx` renders a `<details>` element and is collapsed by default
    - No file under `src/ui/surfaces/chat/` contains `dangerouslySetInnerHTML`
    - The thread orders messages by server `created_at` (grep / test asserts no client-time sort)
    - `node --test` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire ChatPage into the UI barrel and delete the stub</name>
  <read_first>
    - src/ui/index.tsx (the barrel — bulletin entry is the precedent)
    - src/ui/surfaces/chat-stub.tsx (the placeholder being replaced)
  </read_first>
  <action>
In `src/ui/index.tsx`: swap `export { ChatPage } from './surfaces/chat-stub.tsx';` to `export { ChatPage } from './surfaces/chat/index.tsx';` (mirrors how `BulletinPage` points at `./surfaces/bulletin/index.tsx`). Add the scoped-stylesheet inject — `import chatCss from './styles/chat.css';` and `injectClarityStyles(chatCss, 'data-clarity-pack-chat-styles');` parallel to the bulletin lines (the host does NOT auto-load sibling CSS — DEV-14; esbuild's `{ '.css': 'text' }` loader makes the import a string). Delete `src/ui/surfaces/chat-stub.tsx`. Run the UI bundle build to confirm the chat surface bundles with React externalized.
  </action>
  <verify>
    <automated>node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `src/ui/index.tsx` exports `ChatPage` from `./surfaces/chat/index.tsx`
    - `src/ui/index.tsx` injects `chat.css` via `injectClarityStyles`
    - `src/ui/surfaces/chat-stub.tsx` no longer exists
    - `node scripts/build-ui.mjs` exits 0 and the chat surface is in the bundle
    - `pnpm typecheck` clean
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Eric visual-fidelity drill on live Countermoves</name>
  <what-built>
    The full Employee Chat surface — four-region shell, roster, topic strip, optimistic message thread with stream-primary realtime + poll fallback, composer with attachment graceful-degrade, reasoning panels, promote/pin affordances. Replaces the chat-stub placeholder.
  </what-built>
  <how-to-verify>
    This installs an upgraded plugin onto live Countermoves — the bookended-by-snapshots rule applies.
    1. SSH to Countermoves and take a verified snapshot FIRST (`node scripts/safety/cli.mjs snapshot --db-url <from /etc/paperclip/db.env>` then `gate`).
    2. Build + pack the plugin (`pnpm build`, `pnpm pack`) and install the new tgz via `pnpm paperclipai plugin install` from `~/paperclip` (the workspace-script-must-run-from-paperclip gotcha).
    3. As an opted-in user open `/<companyPrefix>/chat` (e.g. `/COU/chat` — plugin pages are `/<companyPrefix>/<routePath>`, NOT `/plugins/clarity-pack/...`).
    4. Verify against `sketches/paperclip-fix-employee-chat.html`: the 3-column shell, the roster rail with status dots (NO "Group threads" section), the topic strip with CHT-NN ids, the message thread, the composer.
    5. Send a message to an employee. Confirm: optimistic bubble appears immediately; a real agent reply arrives via the stream (or the poll fallback shows a reconnecting indicator); the thread does not reorder.
    6. Confirm the `📎 Attach` button state matches the 04-01 OQ-1 verdict (disabled + "Attachments are temporarily unavailable" if NO-PATH).
    7. Expand a "Show reasoning" panel; click `↗ Promote to task` and `⚑ Pin` on an agent message; confirm a promote creates a real linked issue.
    8. Open the Chat surface in two tabs — confirm no CSS bleed into classic Paperclip UI.
  </how-to-verify>
  <resume-signal>Type "approved" or describe visual/functional gaps. Confirm the pre-install snapshot was verified.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent markdown / Eric's typed text → DOM | Chat is a named XSS / exfiltration vector — message content is untrusted input rendered in same-origin trusted JS. |
| UI → host APIs | A raw fetch would bypass the capability gate (capability-bypass footgun). |
| client-supplied attachment file → host | If OQ-1 yields an upload path, client content-type + size are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering / Elevation | stored XSS via agent markdown or Eric's typed message | mitigate | All message bodies + reasoning blocks render as untrusted TEXT — no `dangerouslySetInnerHTML`, no raw HTML, no inline event handlers, no `javascript:` URLs. The Task 1/2 acceptance criteria grep-assert no `dangerouslySetInnerHTML` anywhere under src/ui/surfaces/chat/. The reasoning-block parse operates on text, never `eval`. |
| T-04-19 | Elevation of Privilege | UI calls a Paperclip HTTP API directly | mitigate | All host RPC via usePluginData / usePluginAction / usePluginStream; ESLint `no-raw-fetch-in-ui` stays in force; acceptance criteria grep-assert no raw `fetch(` under src/ui/surfaces/chat/. |
| T-04-20 | Tampering | CSS bleed-through into host UI | mitigate | Every chat.css rule prefixed `[data-clarity-surface="chat"]` via ClaritySurfaceRoot name="chat"; a CSS-scope test mirrors the bulletin.css scope test (SCAF-06 / COEXIST-01). |
| T-04-21 | Information Disclosure | optimistic UI shows a "delivered" message that failed | mitigate | D-10 — a failed send leaves the bubble marked "Failed to send" with Retry, never silently delivered (PITFALLS 11.3); never silent loss of Eric's typed text. |
| T-04-22 | Tampering | client-supplied attachment content-type / oversized file | mitigate | If OQ-1 yields an upload path: a ~10MB client-side size cap (D-12) and never trusting the client content-type. If NO-PATH: the attach button is disabled — no file ever crosses the boundary. |
</threat_model>

<verification>
- `node --test` for the chat shell + message-thread UI tests passes.
- `node scripts/build-ui.mjs` and `pnpm typecheck` clean.
- `grep -r "dangerouslySetInnerHTML" src/ui/surfaces/chat/` returns nothing.
- `grep -r "fetch(" src/ui/surfaces/chat/` returns nothing (bridge-only).
- chat.css is fully `[data-clarity-surface="chat"]`-scoped.
- `chat-stub.tsx` is deleted; `src/ui/index.tsx` points `ChatPage` at the real surface.
- Eric's Countermoves visual-fidelity drill passes (Task 4).
</verification>

<success_criteria>
- CHAT-01: the four-region chat surface renders and matches the sketch.
- CHAT-04: realtime via `usePluginStream` with a `usePoll` fallback and a reconnecting indicator.
- CHAT-06: optimistic send + reconcile + Retry on failure.
- CHAT-07: attach button disabled with the explicit message when no upload path exists (or wired with a 10MB cap if one exists).
- CHAT-10: reasoning panel renders collapsed; CHAT-09 affordances render.
</success_criteria>

<output>
After completion, create `.planning/phases/04-employee-chat/04-05-SUMMARY.md`.
</output>
