---
phase: 04-employee-chat
plan: 06
type: execute
wave: 6
depends_on: [05]
files_modified:
  - scripts/coexistence-checks/08-chat-disable.mjs
  - test/coexistence/coexistence-checklist.test.mjs
  - test/coexistence/08-chat-disable.test.mjs
autonomous: false
requirements: [CHAT-11]

must_haves:
  truths:
    - "An automated coexistence check disables the plugin and asserts every chat message remains visible as an ordinary threaded comment in classic Paperclip"
    - "The chat-disable check runs as part of the CI coexistence checklist on every PR"
    - "Phase 4 closes on a passing closure drill against live Countermoves"
  artifacts:
    - path: "scripts/coexistence-checks/08-chat-disable.mjs"
      provides: "CHAT-11 automated coexistence check — chat comments survive plugin disable"
      contains: "issue_comments"
  key_links:
    - from: "test/coexistence/coexistence-checklist.test.mjs"
      to: "scripts/coexistence-checks/08-chat-disable.mjs"
      via: "checklist runner includes the new check"
      pattern: "08-chat-disable"
---

<objective>
Close Phase 4: build the CHAT-11 coexistence check — an automated test that disables the plugin and asserts every chat message is still visible as an ordinary threaded comment in classic Paperclip — extend the CI coexistence checklist to run it, and run the Phase 4 closure drill on live Countermoves.

CHAT-11 is satisfied structurally by D-02 (message content lives in `public.issue_comments`, never a Clarity Pack table) — this plan PROVES it with an automated check, filling in the `chat-comment coexistence` assertion stubbed in Plan 02-04.

Purpose: Make coexistence guarantee #5 testable and CI-enforced for the chat surface, and close the phase on a green drill.
Output: One coexistence check script + its test + the checklist extension; the Phase 4 closure drill verdict.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-employee-chat/04-CONTEXT.md
@.planning/phases/04-employee-chat/04-RESEARCH.md
@.planning/phases/04-employee-chat/04-PATTERNS.md
@.planning/phases/04-employee-chat/04-01-SPIKE-FINDINGS.md

<interfaces>
<!-- The Phase 2/3 coexistence-check pattern this plan extends. -->

scripts/coexistence-checks/ — existing checks (07-bulletin-disable.mjs is the closest analog,
added by Plan 03-04). Each check is an ESM script exporting a runnable assertion that the
CI checklist runner invokes. 07-bulletin-disable.mjs proves a bulletin issue survives plugin
disable; 08-chat-disable.mjs proves the same for chat topic comments.

test/coexistence/coexistence-checklist.test.mjs — the CI runner; Plan 03-04 extended it to
include 07. This plan adds 08 to the same list.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the CHAT-11 chat-disable coexistence check</name>
  <read_first>
    - scripts/coexistence-checks/07-bulletin-disable.mjs (the exact analog — bulletin survives plugin disable)
    - test/coexistence/coexistence-checklist.test.mjs (the CI checklist runner)
    - .planning/phases/04-employee-chat/04-RESEARCH.md (CHAT-11 row — "Satisfied for free by D-02")
  </read_first>
  <action>
Create `scripts/coexistence-checks/08-chat-disable.mjs` mirroring `07-bulletin-disable.mjs`. The check:
  1. Establishes a known chat topic issue with at least one chat comment (either against a test DB fixture the way 07 does, or by referencing a seeded topic — follow 07's exact setup mechanism).
  2. Simulates / performs the plugin-disable path.
  3. Asserts: the chat comment rows still exist in `public.issue_comments` and are still associated with their topic issue — i.e. they remain visible as ordinary threaded comments in classic Paperclip. The assertion is that NO chat message content lived in a plugin-namespace table (CHAT-02 / D-02) so disabling the plugin removes nothing user-visible.
  4. Additionally assert the inverse safety: the plugin-namespace `chat_topics` / `chat_messages` tables are NOT dropped on disable (additive-only, COEXIST-03) — disable leaves data intact.

Create `test/coexistence/08-chat-disable.test.mjs` — a `node --test` test that runs the check and asserts its pass.

Then extend `test/coexistence/coexistence-checklist.test.mjs` to include `08-chat-disable` in the checklist set, exactly as Plan 03-04 added `07`. The checklist must FAIL the build if the chat-disable check regresses.
  </action>
  <verify>
    <automated>node --test test/coexistence/08-chat-disable.test.mjs test/coexistence/coexistence-checklist.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/coexistence-checks/08-chat-disable.mjs` exists and contains the string `issue_comments`
    - `test/coexistence/08-chat-disable.test.mjs` exists
    - `test/coexistence/coexistence-checklist.test.mjs` references `08-chat-disable`
    - `node --test` exits 0 for both files
    - The check asserts both: chat comments survive disable AND plugin-namespace chat tables are not dropped
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP / REQUIREMENTS traceability and version</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 4 section + the Phase Progress table + Traceability)
    - .planning/REQUIREMENTS.md (CHAT-01..11 traceability rows)
    - src/manifest.ts (version field)
  </read_first>
  <action>
Update `.planning/REQUIREMENTS.md` Traceability rows for CHAT-01..CHAT-11 — mark each Implemented with the plan that delivered it (CHAT-01 → 04-04/04-05; CHAT-02/04/05/06 → 04-03; CHAT-03 → 04-02; CHAT-07/10 → 04-05; CHAT-08/09 → 04-04; CHAT-11 → 04-06). Update the ROADMAP.md Phase 4 plan list to the final 6-plan set and the Phase Progress table row. Note in the ROADMAP Phase 4 section the two scope corrections surfaced by Plan 04-01 / RESEARCH (the word "private" is not achievable per D-07; CHAT-07 attachments ship per the OQ-1 verdict) so the Phase 5 transition picks them up. Confirm `src/manifest.ts` version is `0.7.0`.
  </action>
  <verify>
    <automated>node --test test/phases/04-traceability.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md shows every CHAT-01..CHAT-11 as Implemented with a Phase 4 plan reference
    - ROADMAP.md Phase 4 plan list shows the 6 plans
    - A test asserting all 11 CHAT rows are Implemented passes; `node --test` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 4 closure drill on live Countermoves</name>
  <what-built>
    The complete Employee Chat surface plus the CHAT-11 automated coexistence check. This drill confirms the whole phase end-to-end on live Countermoves and the chat-disable check passes.
  </what-built>
  <how-to-verify>
    This drill exercises a live install + disable on Countermoves — the bookended-by-snapshots rule applies.
    1. SSH to Countermoves and take a verified snapshot FIRST (`node scripts/safety/cli.mjs snapshot --db-url <from /etc/paperclip/db.env>` then `gate`).
    2. Confirm clarity-pack v0.7.0 is installed (the 04-05 drill installed it; reinstall if needed via `pnpm paperclipai plugin install` from `~/paperclip`).
    3. Open `/<companyPrefix>/chat` as an opted-in user. Send a message to an employee on a fresh topic; confirm the agent replies and the reply renders in the thread.
    4. Open classic Paperclip and navigate to the topic issue directly — confirm the chat messages appear there as ordinary threaded comments (CHAT-11 / D-07: topic issues are NOT private; they are visible in classic UI — that is what satisfies CHAT-11).
    5. In Paperclip's classic plugin-admin UI, DISABLE clarity-pack. Re-open the topic issue in classic Paperclip — confirm every chat comment is STILL visible as an ordinary threaded comment (CHAT-11). Confirm no data was destroyed.
    6. Re-enable the plugin. Confirm the chat surface still works and the chat_topics / chat_messages data survived.
    7. Confirm `node --test test/coexistence/coexistence-checklist.test.mjs` passes locally (the CHAT-11 check is green in CI).
  </how-to-verify>
  <resume-signal>Type "approved — phase 4 closed" or describe what failed. Confirm the pre-drill snapshot was verified.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin disable → host data | A destructive uninstall hook would violate coexistence guarantee #3/#5. |
| coexistence check → host DB | The check reads issue_comments + plugin-namespace tables to assert survival. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-23 | Tampering | a destructive disable hook drops chat data | mitigate | 08-chat-disable.mjs asserts both that chat comments survive in public.issue_comments AND that the plugin-namespace chat tables are not dropped on disable; the check runs in CI on every PR and fails the build on regression (COEXIST-06). |
| T-04-24 | Repudiation | a silent coexistence regression ships | mitigate | The chat-disable check is added to the CI coexistence checklist runner — a regression fails the build, not a manual review. |
| T-04-25 | Tampering | the closure drill install corrupts live Countermoves | mitigate | Task 3 is bookended by a verified snapshot per the CLAUDE.md bookended-by-snapshots rule — a botched install/disable is restorable in minutes. |
</threat_model>

<verification>
- `node --test` for the coexistence test files passes.
- The CI coexistence checklist includes and runs `08-chat-disable`.
- REQUIREMENTS.md / ROADMAP.md traceability reflects all 11 CHAT requirements Implemented.
- Eric's Countermoves closure drill passes (Task 3).
</verification>

<success_criteria>
- CHAT-11: an automated check proves chat messages survive plugin disable as ordinary threaded comments; it runs in CI.
- All CHAT-01..CHAT-11 are Implemented and traced.
- Phase 4 closes on a passing live Countermoves closure drill.
</success_criteria>

<output>
After completion, create `.planning/phases/04-employee-chat/04-06-SUMMARY.md`.
</output>
