---
phase: 04.1-chat-true-task
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike/chat-true-task-spike-probe.mjs
  - .planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [CTT-01, CTT-02, CTT-03]
requirements_introduced:
  - id: CTT-01
    text: "From the Employee Chat composer, the operator can turn an intention into a TRUE task — a real Paperclip issue that is assigned to an employee-agent, has a status lifecycle, and appears in the normal Issues list (not parented under a chat-topic plumbing tree, not unassigned)."
    phase: "4.1"
    extends: "CHAT-09"
  - id: CTT-02
    text: "The existing agent-message ↗ Promote-to-task affordance produces the SAME assigned, findable top-level task as the operator-side composer path. One shared mechanism, one consistent result."
    phase: "4.1"
    extends: "CHAT-09"
  - id: CTT-03
    text: "A chat topic supports sustained multi-turn conversation — the assigned employee-agent reliably re-wakes on every operator message, not only the first."
    phase: "4.1"
    extends: "CHAT-09"
  - id: CTT-04
    text: "The chat thread shows genuine conversational messages only — Paperclip agent-task-lifecycle / system bookkeeping comments (disposition, recovery owner, finish_successful_run_handoff) never appear as chat messages, and are filterable/diagnosable via an operator-toggled diagnostics path."
    phase: "4.1"
    extends: null
  - id: CTT-05
    text: "A chat topic issue is never stranded in a stuck task-completion state by the plugin's own behaviour: the chat-topic issue is held non-terminal (never marked done by the plugin) and a watchdog flips it back off done/cancelled/blocked if observed there."
    phase: "4.1"
    extends: null
  - id: CTT-06
    text: "If a chat topic does still end up host-stuck (residual disposition / recovery-owner state the plugin cannot reverse), the chat surface shows the operator a clear ⚠ TOPIC STUCK HOST-SIDE banner — never silent, never auto-recover."
    phase: "4.1"
    extends: null
  - id: CTT-07
    text: "Archiving a chat topic is a plugin-side concept: it sets chat_topics.archived = true and drops the topic from the chat UI without marking the host issue done (which would re-engage the disposition machinery). Archived topics remain reversible via a +N archived strip pill."
    phase: "4.1"
    extends: null
  - id: CTT-08
    text: "The chat context rail tracks spun-off true-task status live: each task created from a topic appears in the Active tasks owned section with a status pill that updates on every chat poll."
    phase: "4.1"
    extends: null
user_setup: []

must_haves:
  truths:
    - "PROBE-OQ3 (D-12) is answered live on Countermoves: the exact ctx.issues.requestWakeup(reason, contextSource) argument set the host's isVerifiedIssueTreeControlInteractionWake() gate accepts for a plugin-originated wake is recorded, with the queued/runId result captured for at least one verified call."
    - "PROBE-D14-DISCRIM is answered live on Countermoves: the actual authorType / presentation.kind values stamped on a real disposition / recovery-owner / finish_successful_run_handoff comment are inspected and recorded; the body-pattern fallback list is sized against the real comment bodies."
    - "PROBE-OQ1-STATUS is answered live on Countermoves: an in_progress chat-topic issue with a fresh comment is observed to either (a) wake the assigned employee-agent natively (Phase 4 D-01 holds for multi-turn) or (b) require the requestWakeup nudge from PROBE-OQ3. The status value the agent itself tends to set after a successful run is recorded — that is the value the D-11 watchdog must override."
    - "PROBE-OQ2-FILTER is answered live on Countermoves: ctx.issues.list({companyId, originKind:'plugin:clarity-pack'}) is called and the returned originId field shape is recorded so the planner can confirm whether the D-08 active-tasks query can filter by an originId prefix `chat-task:<topicIssueId>:` (preferred path) or whether a 0007 chat_topic_tasks side table is required."
    - "FLAG-1 reconciliation evidence: the spike attempts to provoke the host-side disposition / recovery-owner state on a probe chat-topic issue while holding it non-terminal, and records whether a chat topic CAN enter the bounded missing-disposition recovery state even when status is held in_progress."
    - "FLAG-2 evidence: a grep + read of src/worker/jobs/situation-snapshot.ts is recorded, confirming whether any Situation Room handler does an issues.list by assigneeAgentId that would now include chat-topic issues (initial reading: it reads current_task_summary, NOT a per-agent issues.list — see acceptance-criteria #4)."
    - "REQUIREMENTS.md carries new traceability rows CTT-01..CTT-08 marked Pending; their phase column says Phase 4.1 — the executor will mark them Implemented at phase closure (Plan 04.1-07)."
  artifacts:
    - path: "scripts/spike/chat-true-task-spike-probe.mjs"
      provides: "Throwaway probe script — Node ESM, REST/MCP calls against live Countermoves; mirrors scripts/spike/chat-spike-probe.mjs from Plan 04-01."
      contains: "PROBE-OQ3"
    - path: ".planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md"
      provides: "Structured findings doc — PASS/FAIL per probe, locked argument set for D-12, recorded authorType/presentation values for D-14, OQ-1 status decision, OQ-2 originId filter decision, FLAG-1 reconciliation, FLAG-2 grep result."
      contains: "PROBE-OQ3"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Traceability rows for CTT-01..CTT-08 appended under the existing CHAT-11 row, marked Pending with phase Phase 4.1."
      contains: "CTT-01"
  key_links:
    - from: "Spike probe on Countermoves"
      to: "Plans 04.1-02..04.1-06 implementation"
      via: "04.1-01-SPIKE-FINDINGS.md locked findings"
      pattern: "PROBE-OQ3"
---

<objective>
Falsify-first spike. Before any chat-true-task handler, helper, UI, or migration is written, prove on the live Countermoves Paperclip instance that the two genuine unknowns of Phase 4.1 hold:

1. The exact `ctx.issues.requestWakeup(reason, contextSource)` argument set the host's `isVerifiedIssueTreeControlInteractionWake()` gate accepts for a plugin-originated wake (D-12 — RESEARCH §Pitfall 2, OQ-3).
2. The actual `authorType` / `presentation.kind` values stamped on a real Paperclip disposition / recovery-owner / `finish_successful_run_handoff` comment (D-14 — RESEARCH §"D-14 runtime-comment discriminator EXISTS" — HIGH confidence in source, must be VERIFIED on the host version installed on Countermoves before the filter ships).

Plus three lower-risk live confirmations that affect plan shape:
3. PROBE-OQ1-STATUS — does an `in_progress` chat-topic issue with a fresh comment natively re-wake the assignee on multi-turn, or is the explicit `requestWakeup` nudge required? (OQ-1 + Pitfall 3.)
4. PROBE-OQ2-FILTER — does `ctx.issues.list({companyId, originKind})` return `originId` precisely enough to filter the D-08 active-tasks query by prefix, or do we need a `0007_chat_topic_tasks.sql` side table? (OQ-2.)
5. FLAG-1 + FLAG-2 reconciliation evidence — provoke the recovery state on a probe topic to size how often D-13's residual banner will actually trigger; grep `situation-snapshot.ts` to confirm chat-topic issues don't inflate work-status aggregation.

This plan builds ZERO production code. It is a probe script + a findings document + the REQUIREMENTS.md traceability rows. Plans 04.1-02..04.1-06 are GATED on the findings: if PROBE-OQ3 cannot find a working argument set AND PROBE-OQ1-STATUS finds native multi-turn wake also unreliable, D-12 is host-blocked and Plan 04.1-03 (chat-topic lifecycle) must re-scope per FLAG-1 / CHAT-04 precedent.

Purpose: Phase 3 cost five gap-closure plans (03-06..03-10) because Plan 03 did NOT falsify-first. Plan 02-01 and Plan 04-01 ran the discipline and saved their phases. Phase 4.1's planning was explicit about treating D-12 and D-14 as genuine unknowns — this plan is that discipline.

Output:
- `scripts/spike/chat-true-task-spike-probe.mjs` — throwaway probe (mirrors 04-01's spike probe style).
- `.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md` — locked findings consumed by Plans 04.1-02..04.1-06.
- `REQUIREMENTS.md` — CTT-01..CTT-08 traceability rows appended (Pending → Implemented at Phase 4.1 closure).
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-chat-true-task/04.1-CONTEXT.md
@.planning/phases/04.1-chat-true-task/04.1-RESEARCH.md
@.planning/phases/04.1-chat-true-task/04.1-PATTERNS.md
@.planning/phases/04-employee-chat/04-FOLLOWUP-chat-true-task.md
@.planning/phases/04-employee-chat/04-01-PLAN.md
@.planning/phases/04-employee-chat/04-01-SPIKE-FINDINGS.md
@CLAUDE.md
@runbook/operator-gotchas.md

<interfaces>
<!-- SDK shapes the spike probes. Verified in RESEARCH.md against installed types. -->

From @paperclipai/plugin-sdk/dist/types.d.ts (SDK 2026.512.0):
```typescript
// PluginIssuesClient
get(issueId: string, companyId: string): Promise<Issue | null>;   // l.1022
create(input: { companyId, parentId?, title, description?, status?, assigneeAgentId?, originKind?, originId? }): Promise<Issue>;   // l.1023-1048
update(issueId: string, patch: Pick<Issue,'status'|'title'|'description'>, companyId: string): Promise<Issue>;   // l.1049-1053
list(opts: { companyId, originKind?, originKindPrefix?, includePluginOperations?, assigneeAgentId? }): Promise<Issue[]>;   // l.1010-1021
requestWakeup(issueId: string, companyId: string, opts?: { reason?: string; contextSource?: string }): Promise<PluginIssueWakeupResult>;   // l.1065-1069
listComments(issueId: string, companyId: string): Promise<IssueComment[]>;   // l.1075
createComment(issueId: string, body: string, companyId: string, opts?: { authorAgentId?: string }): Promise<IssueComment>;   // l.1076-1078

// PluginIssueWakeupResult — l.881-884
{ queued: boolean; runId: string | null }

// IssueComment (from packages/shared/src/types/issue.ts via shared constants):
// authorType: 'user' | 'agent' | 'system'   ← D-14 discriminator
// presentation?.kind: 'message' | 'system_notice'   ← D-14 secondary
// authorUserId, authorAgentId, body, createdAt, id

// ISSUE_TREE_CONTROL_INTERACTION_WAKE_REASONS (host gate accepts only these):
//   'issue_commented' | 'issue_reopened_via_comment' | 'issue_comment_mentioned'

// TERMINAL_ISSUE_STATUSES = { 'done', 'cancelled' }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the throwaway spike probe + register CTT-01..CTT-08 in REQUIREMENTS.md</name>
  <read_first>
    - scripts/spike/chat-spike-probe.mjs (the analog from Plan 04-01 — copy its REST-client + try/catch-per-step pattern + structured JSON output)
    - .planning/phases/04-employee-chat/04-01-SPIKE-FINDINGS.md (the findings-doc format the executor is mimicking — verdict tables, per-probe sections, operator instructions)
    - .planning/phases/04.1-chat-true-task/04.1-RESEARCH.md §Pitfall 2 (requestWakeup gate strings), §Pattern 3 (the requestWakeup body the probe is exercising), §Open Question 3 (the falsify-first plan for D-12), §Pattern 2 (the IssueComment shape D-14 inspects), §Open Question 1 (in_progress as the holding status), §Open Question 2 (originId filter precision), §FLAG-1 + FLAG-2 (the reconciliation evidence to capture)
    - .planning/phases/04.1-chat-true-task/04.1-PATTERNS.md §"src/worker/chat/topic-watchdog.ts" (the exact requestWakeup call shape being probed — RESEARCH §Pattern 3 verbatim)
    - .planning/REQUIREMENTS.md (end of file — the traceability table the executor extends; the format is `| CHAT-NN | Phase 4 | Implemented (Plan …) |`)
    - runbook/operator-gotchas.md (the snapshot/gate bookend pattern — CLAUDE.md's bookended-by-snapshots rule applies because the probe runs against the live BEAAA — Countermoves)
    - test/phases/04-traceability.test.mjs (the traceabilityRow regex shape — `^|\s*${id}\s*|([^|]*)|([^|]*)|` — so the CTT-NN rows the executor adds are picked up by the Plan 04.1-07 traceability test)
  </read_first>
  <action>
**Build the throwaway probe — `scripts/spike/chat-true-task-spike-probe.mjs` (Node ESM, NOT under `src/`, NEVER bundled).** Model on `scripts/spike/chat-spike-probe.mjs` from Plan 04-01: REST/MCP calls against the live Paperclip instance, every step wrapped in try/catch that records outcome rather than crashing, structured JSON summary printed at the end. Env: `PAPERCLIP_API_URL` + bearer `PAPERCLIP_API_KEY` + `PAPERCLIP_COMPANY_ID` (BEAAA/Countermoves COU-* — confirm by reading runbook/operator-gotchas.md for the live company id pattern).

The probe runs **four sub-probes**:

**PROBE-D14-DISCRIM (D-14 — runtime-comment discriminator).** Inspect at least one REAL disposition / recovery-owner / `finish_successful_run_handoff` comment on Countermoves and record its actual `authorType` + `presentation.kind` fields. Two paths to find one:
  (a) Cheap path — query the live Paperclip REST surface for recent `issue_comments` rows on chat-topic issues already on the box (the "Hire a CMO" / CMO-topic CHT-1116 drill from 04-FOLLOWUP-chat-true-task.md left at least one such comment on the box at the time of writing). Search by issue id or filter comments where the body matches one of the known runtime phrases.
  (b) Provoke path — if no live runtime comment is findable, create a probe chat-topic issue assigned to a real employee-agent and post a comment that asks the agent to perform a real action (e.g. "Reply with a one-paragraph plan and then close this conversation as done"). After the agent run-completes, the host's recovery service WILL post a `finish_successful_run_handoff` notice. Poll `listComments` and capture the row.
Record: the raw JSON of one notice comment row including `id`, `authorType`, `authorUserId`, `authorAgentId`, `presentation`, and `body` (first 200 chars). Verify against RESEARCH §"D-14 discriminator EXISTS" — `authorType === 'system'`. If the live host carries a DIFFERENT discriminator (RESEARCH MEDIUM-confidence flag), the findings doc records the actual value the production filter must key on.

**PROBE-OQ3 (D-12 — requestWakeup gate strings).** Create a probe chat-topic issue assigned to a real employee-agent (NOT the Editor-Agent), post one comment to wake it, wait for the reply (Phase 4 D-01 native wake — should land per 04-01 spike findings), then exercise `requestWakeup` with the three legal `reason` values one by one. Specifically:
  1. Post comment #2 on the issue WITHOUT calling requestWakeup; wait up to 4 minutes; record whether the agent re-wakes (the multi-turn case the 04-01 spike DID NOT cover — Pitfall 3 in RESEARCH).
  2. If no re-wake, call `requestWakeup(issueId, companyId, { reason: 'issue_commented', contextSource: 'clarity-pack:chat' })`. Capture the `{queued, runId}` result. Wait up to 4 minutes.
  3. If still no re-wake, retry with `reason: 'issue_reopened_via_comment'` (paired with a status flip from `in_progress` to `in_progress` — no-op flip, but exercises the resume mode codepath). Capture result + wait.
  4. If still no re-wake, retry with `reason: 'issue_comment_mentioned'`. Capture result + wait.
  5. If still no re-wake, capture and report — D-12 is genuinely host-blocked and FLAG-1 / CHAT-04 reconciliation applies; Plan 04.1-03 will need to lean entirely on D-09 / D-11 + D-13 host-stuck banner without an explicit wake nudge.
The findings doc records the FIRST `reason` value that produced `{queued: true, runId: <non-null>}` AND a subsequent observable agent reply — that is the locked argument set Plan 04.1-03 implements.

**PROBE-OQ1-STATUS (D-09 + D-11 — non-terminal holding status).** Create a second probe chat-topic issue at `status: 'in_progress'` from the start (RESEARCH OQ-1 recommendation). Post a comment. Observe whether the agent natively re-wakes WITHOUT a requestWakeup call (i.e. is `in_progress` + plain comment sufficient for multi-turn?). Record: the status value the agent itself sets after the run completes (the watchdog must override THIS value). If the agent flips it to `done`, that confirms D-11's necessity. If the agent leaves it at `in_progress`, the watchdog's job is simpler.

**PROBE-OQ2-FILTER (D-08 active-tasks query path).** On the probe company, create TWO probe true-task issues (top-level, no parentId, `assigneeAgentId` set, `originKind: 'plugin:clarity-pack'`, distinct `originId`s — one `chat-task:<topicA>:<commentA>`, one `chat-task:<topicB>:<commentB>`). Then call `ctx.issues.list({ companyId, originKind: 'plugin:clarity-pack' })` (via REST equivalent) and inspect the returned rows — does each row carry an `originId` field, and is it returned as a queryable value? Try `ctx.issues.list({ companyId, originKind: 'plugin:clarity-pack', originId: 'chat-task:<topicA>:<commentA>' })` (exact match) and any `originIdPrefix` filter the SDK exposes. Record: whether the precise filter works, an exact-match-only filter works, or only a coarse `originKind` filter works (in which case the active-tasks handler client-side-filters by `originId.startsWith(...)`, RESEARCH OQ-2 — this is the recommendation in PATTERNS.md `chat-active-tasks.ts`).

**FLAG-2 grep (no probe call — pure read).** Run `grep -nE "issues\.list|assigneeAgentId" src/worker/jobs/situation-snapshot.ts src/worker/jobs/*.ts src/worker/handlers/situation-room.ts 2>/dev/null` (or via the Grep tool). Confirm — already verified by the planner in CONTEXT — that the snapshot reads `current_task_summary` (line ~190 in situation-snapshot.ts) and does NOT do an `issues.list({assigneeAgentId})` walk. If any other Situation Room surface DOES, the findings doc records it as a 04.1-03 follow-up task.

Wrap every step in try/catch that records the outcome rather than crashing; partial findings are still findings. Print a structured JSON summary at the end (`{probe, verdict, payload}` per sub-probe).

**Bookended-by-snapshots discipline (CLAUDE.md):** Because the probe creates real issues + assigns them + may post several comments on the live BEAAA — Countermoves — Eric must invoke `clarity-safety snapshot` BEFORE running the probe and `clarity-safety gate` after, per the rehearsed runbook. The probe script's `--help` output must clearly say: "RUN AFTER `clarity-safety snapshot` ON THE TARGET BOX. The probe creates 3-5 real issues + several comments on the named company. Clean them up manually after, or restore from the bookend snapshot."

**Write `.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md`** — mirror the format of `.planning/phases/04-employee-chat/04-01-SPIKE-FINDINGS.md` (read that file in `<read_first>`). Required sections:
  - **TL;DR verdict** — one of `GO` / `GO-WITH-FLAGS` / `RE-SCOPE`.
  - **Per-probe PASS/FAIL table** — PROBE-D14-DISCRIM, PROBE-OQ3, PROBE-OQ1-STATUS, PROBE-OQ2-FILTER, FLAG-1 reconciliation, FLAG-2.
  - **Locked findings** — the exact `requestWakeup` argument set Plan 04.1-03 implements; the exact discriminator values Plan 04.1-04's `classifyComment` keys on (and the body-pattern fallback list sized against the real notice bodies); the chosen non-terminal holding status (per D-11 watchdog); whether a `migrations/0007_*.sql` is needed (per D-08 active-tasks); whether FLAG-1 reconciles as `host-blocked-residual` (CHAT-04 precedent) or `prevented-by-D09-D11`.
  - **Raw evidence** — at least one raw JSON comment row from PROBE-D14-DISCRIM, the `{queued, runId}` result(s) from PROBE-OQ3, and the issue.status transitions observed in PROBE-OQ1-STATUS.

**Append the CTT-01..CTT-08 traceability rows to `.planning/REQUIREMENTS.md`.** Open the file, find the existing CHAT-11 traceability row near the end, and append (in order, alphabetical within the new CTT block) eight rows in the exact existing format:

```
| CTT-01 | Phase 4.1 | Pending |
| CTT-02 | Phase 4.1 | Pending |
| CTT-03 | Phase 4.1 | Pending |
| CTT-04 | Phase 4.1 | Pending |
| CTT-05 | Phase 4.1 | Pending |
| CTT-06 | Phase 4.1 | Pending |
| CTT-07 | Phase 4.1 | Pending |
| CTT-08 | Phase 4.1 | Pending |
```

Also append a `**CTT — Chat → True Task (CTT)**` definition block in the "Surface 4 — Employee Chat (CHAT)" requirements section listing the eight requirement texts from this plan's `requirements_introduced` frontmatter. The Phase 4.1 closure plan (Plan 04.1-07) will flip the eight rows to `Implemented (Plan 04.1-NN …)` and the `04-traceability.test.mjs` analog for CTT will RED→GREEN against the same row format the existing CHAT test uses.

DO NOT touch any other CHAT/SAFE/SCAF/etc row.
  </action>
  <verify>
    <automated>node --check scripts/spike/chat-true-task-spike-probe.mjs && grep -E "^\| CTT-0[1-8] \| Phase 4\.1 \| Pending \|" .planning/REQUIREMENTS.md | wc -l | grep -E "^\s*8\s*$"</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/spike/chat-true-task-spike-probe.mjs` exists and `node --check` exits 0.
    - The probe's `--help` (or top-of-file comment block) contains the literal string "RUN AFTER `clarity-safety snapshot`" (bookended-by-snapshots discipline — CLAUDE.md).
    - The probe sub-probe sections are clearly demarcated; `grep -n "PROBE-D14-DISCRIM\|PROBE-OQ3\|PROBE-OQ1-STATUS\|PROBE-OQ2-FILTER" scripts/spike/chat-true-task-spike-probe.mjs` returns four hits.
    - `.planning/REQUIREMENTS.md` has exactly eight new rows matching `^\| CTT-0[1-8] \| Phase 4\.1 \| Pending \|` and an `**CTT — Chat → True Task (CTT)**` definitions block listing CTT-01..CTT-08 with the requirement text from `requirements_introduced`.
    - No other traceability rows are modified.
  </acceptance_criteria>
  <done>The throwaway probe is committable + ready for Eric to run on Countermoves; the CTT-01..CTT-08 requirement IDs exist in REQUIREMENTS.md so the rest of Phase 4.1's plans can reference them in their `requirements:` frontmatter.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Eric runs the probe on Countermoves; writes the locked findings</name>
  <what-built>
    A throwaway Node ESM probe (`scripts/spike/chat-true-task-spike-probe.mjs`) that exercises the live Countermoves Paperclip instance against the two genuine unknowns of Phase 4.1 (D-12 wake-gate strings + D-14 runtime-comment discriminator) plus three lower-risk live confirmations (OQ-1 holding status, OQ-2 originId filter, FLAG-1 reconciliation), and the eight CTT-01..CTT-08 traceability rows registered in REQUIREMENTS.md.
  </what-built>
  <how-to-verify>
On the Countermoves Hostinger VPS (per the recorded SSH pattern: `ssh -i $HOME/.ssh/countermoves_vps_ed25519 eric@82.29.197.74` — confirm by querying MemPalace clarity_pack/runbook):

1. **Bookend snapshot (CLAUDE.md):**
   ```bash
   cd ~/clarity-pack
   node scripts/safety/cli.mjs snapshot --db-url $(cat /etc/paperclip/db.env | grep DB_URL | cut -d= -f2-)
   node scripts/safety/cli.mjs gate
   ```
   Confirm both PASS.

2. **Run the probe:**
   ```bash
   export PAPERCLIP_API_URL=https://countermoves.gl3group.com
   export PAPERCLIP_API_KEY=$(cat ~/.paperclip/auth.json | jq -r '.profiles[0].token')
   export PAPERCLIP_COMPANY_ID=<the live COU company id — read from `paperclipai company list` on the box>
   node scripts/spike/chat-true-task-spike-probe.mjs > 04.1-01-probe-output.txt 2>&1
   ```

3. **Inspect the JSON summary at the end of `04.1-01-probe-output.txt`.** Specifically verify:
   - PROBE-D14-DISCRIM returned at least one runtime-comment row with an explicit `authorType` value (`system` per RESEARCH HIGH confidence, or whatever the live host stamps).
   - PROBE-OQ3 returned ONE `{queued: true, runId: <non-null>}` result for at least one of the three legal `reason` values, OR returned `{queued: false}` for all three (D-12 host-blocked path — FLAG-1 reconciliation kicks in).
   - PROBE-OQ1-STATUS recorded the status the agent flipped to after a run completes.
   - PROBE-OQ2-FILTER recorded whether `ctx.issues.list({originKind, originId})` filtered precisely.

4. **Eric writes `.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md`** following the format from Plan 04-01's findings doc, with the locked findings table the rest of Phase 4.1 implements against.

5. **Clean up:** delete the probe issues created on Countermoves (`paperclipai issue delete <id>` per probe issue), OR restore from the bookend snapshot if any state is inconsistent.

6. **Type "approved — findings written" once `04.1-01-SPIKE-FINDINGS.md` exists and contains the locked argument set for D-12, the discriminator values for D-14, and the OQ-1/OQ-2/FLAG-1 decisions. Type "RE-SCOPE — D-12 host-blocked" if PROBE-OQ3 found no working argument set AND PROBE-OQ1-STATUS found native multi-turn wake also unreliable — Plan 04.1-03 will need to fold per CHAT-04 precedent.**
  </how-to-verify>
  <resume-signal>Type "approved — findings written" or "RE-SCOPE — D-12 host-blocked"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin (this probe) → Paperclip REST API | The probe runs OUTSIDE the worker; it authenticates with a bearer token and is therefore subject to the same trust model as any `paperclip-api` client (PLUGIN_SPEC §15). Untrusted-input vector: the probe creates issues + comments on the live BEAAA box. |
| Probe-authored comment body → ANY subsequent reader (chat thread, classic Paperclip view) | The probe's posted comments become ordinary `public.issue_comments` rows and survive on the live box. |
| `requestWakeup` host-call | The probe exercises the host's `isVerifiedIssueTreeControlInteractionWake()` gate. A malformed call might be silently dropped (RESEARCH §Pitfall 2) — not a security issue, but the operator must know the call did not have authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01-01 | T (Tampering) | Probe-created issues left on live BEAAA | mitigate | The probe creates issues with titles containing `[SPIKE]` so Eric can `paperclipai issue delete` them at cleanup; the bookended snapshot (CLAUDE.md) provides a rollback path if cleanup is incomplete. |
| T-04.1-01-02 | I (Info disclosure) | The probe prints the bearer token in error messages | mitigate | The probe MUST NOT echo `process.env.PAPERCLIP_API_KEY` in any try/catch — sanitize logged errors. Acceptance-criteria: `grep -n "PAPERCLIP_API_KEY" scripts/spike/chat-true-task-spike-probe.mjs` shows ONLY the `process.env.PAPERCLIP_API_KEY` read at the top — never in a console.log / error message. |
| T-04.1-01-03 | D (Denial of service) | Probe issues consume Paperclip agent budget | accept | The probe creates ≤5 issues + ≤10 comments + ≤4 `requestWakeup` calls on the named company; budget consumption is bounded and well under any agent cap. Operator clean-up after the drill is expected. |
| T-04.1-01-04 | E (Elevation of privilege) | The probe attempts to call `requestWakeup` on an issue not assigned to a plugin-owned agent | accept | The host gate is the enforcement layer; a rejected call returns `{queued: false}` — the probe surfaces this as a recorded result rather than an error. The capability `issues.wakeup` is already declared in the manifest (line 297). |

`security_enforcement` not declared in this project — RESEARCH.md §"Standard Stack" notes the plugin's same-origin trust model is the host concern, not the plugin's. ASVS L1 = adequate for a probe that touches the live box under operator-supervised conditions.
</threat_model>

<verification>
Plan 04.1-01 succeeds when:
- The probe script + the findings doc exist.
- The findings doc has the locked argument set Plan 04.1-03 can implement against.
- The CTT-01..CTT-08 rows exist in REQUIREMENTS.md.
- Eric typed "approved — findings written" or "RE-SCOPE — D-12 host-blocked" so the next-wave planning has an unambiguous gate verdict.
</verification>

<success_criteria>
Plan 04.1-01 produces the only inputs Plans 04.1-02..04.1-06 cannot synthesize from spec alone: the locked `requestWakeup` argument set and the locked runtime-comment discriminator values. Without those two findings, the rest of Phase 4.1 is speculation (Phase 3's cost demonstrates the price of skipping this).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-chat-true-task/04.1-01-SUMMARY.md` per the GSD summary template — recording the per-probe verdict, the locked findings used downstream, any deviations from the plan, the commit hash range, and the final test suite count delta (0 — this plan adds tests only via Plan 04.1-02 onward).
</output>
</content>
</invoke>