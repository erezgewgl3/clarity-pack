# Plan 04.1-01 Spike Findings

**STATUS: STUB — awaiting Countermoves probe run.**

This stub is committed alongside `scripts/spike/chat-true-task-spike-probe.mjs`
and the CTT-01..CTT-08 REQUIREMENTS.md rows. The per-probe verdicts and locked
findings will be filled in by the continuation agent after Eric runs the probe
on Countermoves and pastes the structured JSON summary back into the session.

**Date:** (probe-run date to be filled in)
**Target:** live Countermoves Paperclip instance (`https://countermoves.gl3group.com`, company `<live COU id>`)
**Operator:** Eric
**Probe:** `scripts/spike/chat-true-task-spike-probe.mjs`
**Probe output:** `.planning/phases/04.1-chat-true-task/04.1-01-probe-output.txt`
**Spike employee-agent:** (filled in from probe `findings.oq3.employeeAgentId`)
**Snapshot bookend:** (pre-run snapshot id to be recorded — CLAUDE.md bookended-by-snapshots rule)

This is a falsify-first spike. It builds ZERO production code. It probes the two
genuine unknowns of Phase 4.1 (D-12 wake-gate strings + D-14 runtime-comment
discriminator) plus three lower-risk live confirmations (OQ-1 holding status,
OQ-2 originId filter, FLAG-1 reconciliation + FLAG-2 grep) against the live host
and records an empirical verdict per question. Plans 04.1-02..04.1-06 are gated
on the verdict at the bottom of this doc.

---

## TL;DR Verdict

**(TBD)** — one of `GO` / `GO-WITH-FLAGS` / `RE-SCOPE`. Filled in by the
continuation agent.

---

## Per-Probe PASS/FAIL Table

| Probe | Question | Verdict | Locked finding |
|-------|----------|---------|----------------|
| PROBE-D14-DISCRIM | D-14 — actual `authorType` / `presentation.kind` on a real runtime notice | TBD | TBD |
| PROBE-OQ3 | D-12 — `requestWakeup(reason, contextSource)` argument set the host gate accepts | TBD | TBD |
| PROBE-OQ1-STATUS | D-09/D-11 — non-terminal holding status + agent's post-run status target | TBD | TBD |
| PROBE-OQ2-FILTER | D-08 — does `ctx.issues.list({originKind, originId})` filter precisely? | TBD | TBD |
| FLAG-1 reconciliation | Can a topic enter recovery while non-terminal? | TBD | TBD |
| FLAG-2 grep | Does any Situation Room surface `issues.list({assigneeAgentId})`? | PASS (planner-confirmed) | No FLAG-2 exposure — `situation-snapshot.ts` reads `current_task_summary`, not an `issues.list({assigneeAgentId})` walk. |

---

## PROBE-D14-DISCRIM — runtime-comment discriminator

**VERDICT: TBD**

### Evidence

(raw JSON of one notice comment row will be pasted here from
`probe-output.findings.d14Discrim.noticeRow` — including `id`, `authorType`,
`authorUserId`, `authorAgentId`, `presentation`, and `body` first 200 chars.)

### Locked finding

(The exact discriminator value Plan 04.1-04's `classifyComment` keys on, and
the body-pattern fallback list sized against the real notice bodies.)

### Design input for downstream plans

- **Plan 04.1-04** `classifyComment` keys on (TBD).
- Body-pattern fallback narrowed to (TBD) if the host field discriminator is
  reliable.

---

## PROBE-OQ3 — D-12 requestWakeup gate strings

**VERDICT: TBD**

### Evidence

(The `{queued, runId}` matrix across the three legal reasons will be pasted
here from `probe-output.findings.oq3.attempts[]`.)

| reason | contextSource | HTTP status | queued | runId | observed reply |
|--------|---------------|-------------|--------|-------|----------------|
| `issue_commented` | `clarity-pack:chat` | TBD | TBD | TBD | TBD |
| `issue_reopened_via_comment` | `clarity-pack:chat` | TBD | TBD | TBD | TBD |
| `issue_comment_mentioned` | `clarity-pack:chat` | TBD | TBD | TBD | TBD |

### Locked finding

**Multi-turn native re-wake (comment #2 alone, no requestWakeup):** TBD.

**Locked `requestWakeup` argument set Plan 04.1-03 implements:** TBD.

### Design input for downstream plans

- **Plan 04.1-03** chat-send extension calls `ctx.issues.requestWakeup(topicIssueId, companyId, {reason: TBD, contextSource: TBD})` on every operator send.
- **OR** if multi-turn native wake works: no `requestWakeup` needed; D-09 + D-11 are sufficient.
- **OR** if host-blocked: FLAG-1 / CHAT-04 reconciliation applies; Plan 04.1-03 leans on D-09 + D-11 + D-13 banner only.

---

## PROBE-OQ1-STATUS — non-terminal holding status + agent flip target

**VERDICT: TBD**

### Evidence

(Status transitions observed will be pasted here from
`probe-output.findings.oq1Status.statusTransitions[]`.)

| At | Status |
|----|--------|
| (probe create) | `in_progress` |
| ... | TBD |

**Status after the agent's reply:** TBD.

### Locked finding

**Chosen non-terminal holding status (per D-09/D-11):** `in_progress` (recommended)
unless evidence dictates otherwise.

**Watchdog target — the status Plan 04.1-04's D-11 watchdog must override:** TBD.

### Design input for downstream plans

- **Plan 04.1-04** topic-watchdog overrides (TBD) → `in_progress` on every
  `chat.messages` poll.

---

## PROBE-OQ2-FILTER — D-08 active-tasks query path

**VERDICT: TBD**

### Evidence

(Filter behaviour will be pasted here from
`probe-output.findings.oq2Filter.filters`.)

| Filter | HTTP status | rows | precision |
|--------|-------------|------|-----------|
| `originKind: 'plugin:clarity-pack'` only | TBD | TBD | TBD |
| `originKind` + `originId` (exact) | TBD | TBD | TBD |
| `originKind` + `originIdPrefix` | TBD | TBD | TBD |

### Locked finding

**Recommended query path for `chat-active-tasks.ts`:** TBD.

**Is a `migrations/0007_chat_topic_tasks.sql` side table needed?** TBD.

### Design input for downstream plans

- **Plan 04.1-03** `chat-active-tasks.ts` data handler queries via (TBD).
- **Plan 04.1-03 migration** — 0007 needed: TBD.

---

## FLAG-1 reconciliation — can a chat topic enter recovery while held non-terminal?

**VERDICT: TBD** — `host-blocked-residual` (CHAT-04 precedent) OR `prevented-by-D09-D11`.

### Evidence

(Whether the probe could provoke the host-side disposition / recovery-owner
state on a chat-topic issue while holding it non-terminal will be summarized
here from PROBE-OQ3's probe-issue end state.)

### Locked finding

**Reconciliation choice:** TBD.

- If `prevented-by-D09-D11`: D-09 (never let the topic go terminal) +
  D-11 (watchdog) reliably prevent recovery state entry. D-13's host-stuck
  banner is a defensive fallback, not the primary path.
- If `host-blocked-residual`: the bounded missing-disposition recovery loop
  can still engage even with status held in_progress. D-13's banner is the
  primary surfacing path; CTT-06 ships as the steady-state degraded path
  (CHAT-04 precedent).

### Design input for downstream plans

- **Plan 04.1-03** topic-watchdog cadence and trigger.
- **Plan 04.1-04** D-13 host-stuck banner — primary or fallback path.

---

## FLAG-2 — Situation Room cross-effect

**VERDICT: PASS** (planner-confirmed, no probe call needed).

### Evidence

`src/worker/jobs/situation-snapshot.ts` reads `current_task_summary`
(~line 190) and does NOT do an `issues.list({assigneeAgentId})` walk.
Confirmed by the Phase 4.1 planner in `04.1-CONTEXT.md` FLAG-2.

### Locked finding

No FLAG-2 exposure as of 2026-05-20. If a future Situation Room surface adds
an `issues.list({assigneeAgentId})` call, Plan 04.1-03 must add an `originKind`
exclusion filter for chat-topic issues.

---

## Phase 4.1 Gate Verdict

**(TBD)** — one of:

- `GO` — Plans 04.1-02..04.1-06 cleared to proceed exactly as planned.
- `GO-WITH-FLAGS` — proceed, but with one or more reconciliations recorded
  (e.g., FLAG-1 `host-blocked-residual` → CTT-06 banner is primary; no
  `requestWakeup` → D-09 + D-11 sole backstop; etc).
- `RE-SCOPE` — D-12 host-blocked AND multi-turn native wake unreliable;
  Plan 04.1-03 must fold per CHAT-04 precedent.

### Locked findings used downstream

(filled in by the continuation agent — explicit cross-references to
Plans 04.1-02 .. 04.1-06 implementation tasks.)

---

## SPIKE STUB COMPLETE

The probe script + REQUIREMENTS.md CTT rows are committed. Awaiting operator
probe run on Countermoves. Continuation agent will replace `TBD` values with
the live findings and flip the gate verdict before this doc is committed as
the locked findings input to Plans 04.1-02..04.1-06.
