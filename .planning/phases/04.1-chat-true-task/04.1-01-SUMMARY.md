---
phase: 04.1-chat-true-task
plan: 01
subsystem: spike
tags: [spike, falsify-first, agent-wake, requestWakeup, runtime-noise, system-notice, true-task, gate-verdict]

# Dependency graph
requires:
  - phase: 04-employee-chat
    provides: "installed clarity-pack v0.7.8 on Countermoves; 04-01 native issue_commented wake (turn 1) proven; chat surface live but multi-turn unreliable; safety CLI snapshot/gate; paperclip-api REST client pattern"
  - phase: 03-daily-bulletin
    provides: "Editor-Agent under standard governance; proven snapshot/restore path on the live box"
provides:
  - "04.1-01-SPIKE-FINDINGS.md (status: locked, verdict: GO) -- empirical verdicts per probe from the 2026-05-20T08:23:41Z Countermoves run"
  - "Phase 4.1 Gate Verdict: GO -- Plans 04.1-02..04.1-06 cleared to proceed"
  - "scripts/spike/chat-true-task-spike-probe.mjs -- throwaway probe harness (Task 1; not bundled, not under src/)"
  - "CTT-01..CTT-08 traceability rows registered in .planning/REQUIREMENTS.md (Pending; flipped to Implemented by Plan 04.1-07)"
  - "design inputs locked for downstream plans -- see Locked Plan-Impact table below"
affects: [04.1-02-shared-helper, 04.1-03-topic-lifecycle, 04.1-04-classify-comment, 04.1-05-active-tasks-side-table, 04.1-06-ui-build, 04.1-07-closure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Falsify-first spike on the live BEAAA host before any feature code (Plan 02-01 / Plan 04-01 discipline carried forward)"
    - "Throwaway probe lives in scripts/spike/ -- never under src/, never bundled, deleted-after-findings-locked"
    - "Findings doc carries verdict + per-probe table + Raw evidence appendix; test/phases/ pins the contract"
    - "Plan-impact summary table cross-references each downstream plan with the exact finding it absorbs"

key-files:
  created:
    - .planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
    - test/phases/04.1-01-spike-findings.test.mjs
    - scripts/spike/chat-true-task-spike-probe.mjs
  modified:
    - .planning/REQUIREMENTS.md (CTT-01..CTT-08 rows + CTT definitions block appended)
    - .planning/STATE.md (Plan 04.1-01 complete; Wave 1 closed)
    - .planning/ROADMAP.md (Plan 04.1-01 checkbox flipped)

key-decisions:
  - "Phase 4.1 Gate Verdict = GO -- multi-turn native wake PROVEN on Countermoves (comment #2 alone re-wakes the assigned employee-agent; no requestWakeup needed)"
  - "Plan 04.1-03 drops the requestWakeup call in ensureTopicWakeable; keeps only the defensive D-11 status-flip-off-done path"
  - "Plan 04.1-04 classifyComment dual-keyed: primary authorType === 'system'; secondary presentation.kind === 'system_notice'; body-pattern fallback expanded to 5 phrases (RESEARCH's 4 + 'Paperclip needs a disposition before this issue can continue')"
  - "Plan 04.1-04 topic-watchdog is defensive-only -- the agent leaves chat-topics at in_progress after a successful run; there is no agent-set status to override"
  - "Plan 04.1-05 ships migrations/0007_chat_topic_tasks.sql as the defensive default -- REST origin filters are broken on this host (returns 500-cap regardless of originId/originIdPrefix; even exact match returns 0)"
  - "Plan 04.1-06 D-16 diagnostics view renders presentation.metadata.sections as typed rows (issue_link, agent_link, key_value, run_link), not raw body strings"
  - "FLAG-1 reconciles as 'caught-by-watchdog' -- not host-blocked, not prevented-by-D09-D11. The host disposition-recovery service does engage post-run, and the D-11 watchdog catches the flip on its next poll. CTT-05 is the steady-state path; CTT-06 banner is last-resort only"
  - "FLAG-2 PASS -- situation-snapshot.ts reads current_task_summary, never does issues.list({assigneeAgentId}); chat-topic issues do not leak into Situation Room aggregation"

patterns-established:
  - "Falsify-first spike with locked-findings-by-continuation-agent: operator runs the probe live on Countermoves; the JSON summary is pasted back; a continuation agent locks the findings doc + traceability test + SUMMARY without re-running anything"
  - "Plan-impact summary table inside the findings doc -- explicit mapping from probe verdict to downstream-plan absorption, so executors of 04.1-02..04.1-06 read one row to know what changes from spec"

requirements-completed: []  # CTT-01..CTT-03 registered (Pending); Plan 04.1-07 flips them all to Implemented at phase closure

# Metrics
duration: "3 tasks across 2 sessions -- Task 1 autonomous build, Task 2 Eric live drill on Countermoves, Task 3 continuation lock"
completed: 2026-05-20
---

# Phase 4.1 Plan 01: Falsify-First Spike Summary

**Multi-turn native agent re-wake PROVEN live on Countermoves; D-14 runtime-comment discriminator dual-keyed on `authorType='system'` + `presentation.kind='system_notice'`; REST origin filters broken so the active-tasks side table ships. Phase 4.1 Gate Verdict: GO.**

## Performance

- **Duration:** 3 tasks across 2 sessions (build + live drill + continuation lock)
- **Started (Task 1 autonomous build):** 2026-05-20 morning (planning + research already locked from Plan 04.1 phase planning 2026-05-19/20)
- **Probe run (Task 2 Eric live):** 2026-05-20T08:23:41Z -> 2026-05-20T08:27:43Z (4 min on Countermoves)
- **Completed (Task 3 continuation):** 2026-05-20
- **Tasks:** 3 (Task 1 autonomous, Task 2 non-autonomous human-verify, Task 3 continuation lock)
- **Files created:** 3 (`scripts/spike/chat-true-task-spike-probe.mjs`, `04.1-01-SPIKE-FINDINGS.md`, `test/phases/04.1-01-spike-findings.test.mjs`)
- **Files modified:** 3 (`.planning/REQUIREMENTS.md`, `.planning/STATE.md`, `.planning/ROADMAP.md`)

## Accomplishments

- Phase 4.1's two genuine unknowns (D-12 wake-gate strings + D-14 runtime-comment discriminator) falsified on the live host BEFORE any chat-true-task handler, helper, UI, or migration is written. Phase 3's five gap-closure plans cost demonstrates the price of NOT running this discipline.
- Multi-turn native re-wake PROVEN: a bare comment alone re-wakes the assigned employee-agent on turn N>1. RESEARCH §Pitfall 3 closed.
- D-14 runtime-notice discriminator captured verbatim on a real provoked recovery notice (`fa25ef4d-...`): `authorType='system'`, `presentation.kind='system_notice'`, structured `metadata.sections` typed rows.
- CTT-01..CTT-08 requirement IDs registered in REQUIREMENTS.md (Pending). Plan 04.1-07 will flip them to Implemented at phase closure.
- Plan-impact summary table inside the findings doc gives each downstream plan executor a one-row reading of what changes from spec.

## Task Commits

Each task was committed atomically:

1. **Task 1: spike probe + CTT-01..CTT-08 registration** — `9703dbe` (`test(04.1-01): falsify-first spike probe + CTT-01..CTT-08 registration`). Autonomous. Built the throwaway probe + the STUB findings doc + appended CTT-01..CTT-08 Pending rows to REQUIREMENTS.md.
2. **Task 2 (split A): lock GO verdict** — `eb44303` (`test(04.1-01): lock GO verdict — multi-turn native wake confirmed, D-14 discriminator dual-keyed`). Continuation. Replaced the STUB with the locked findings against the 2026-05-20T08:23:41Z probe run.
3. **Task 3 (Part A): traceability test** — `30cba12` (`test(04.1-01): traceability test for spike findings (RED→GREEN)`). Continuation. 9 tests pinning the findings doc's contract; all GREEN.

**Plan metadata + SUMMARY + STATE/ROADMAP update:** committed separately as `docs(04.1-01): SUMMARY.md — Wave 1 complete, GO verdict` and `docs(04.1-01): mark Wave 1 complete in STATE + ROADMAP`.

## Files Created/Modified

- `scripts/spike/chat-true-task-spike-probe.mjs` — Throwaway Node ESM probe; REST client + four sub-probes (D14-DISCRIM / OQ3 / OQ1-STATUS / OQ2-FILTER) + FLAG-2 grep; structured JSON summary; bookended-by-snapshots discipline in header. **NOT under `src/`, NEVER bundled.**
- `.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md` — Status: locked. Verdict: GO. Carries TL;DR + per-probe PASS/FAIL table + per-probe sections with locked findings + plan-impact summary table + Raw evidence appendix with verbatim probe JSON. The locked design inputs for Plans 04.1-02..04.1-06.
- `test/phases/04.1-01-spike-findings.test.mjs` — 9 tests pinning the findings doc contract (file exists, frontmatter `status: locked` + `verdict: GO`, all required section headers, every sub-probe verdict word verbatim, D-14 PASS recorded, OQ3 probe issue id + D-14 notice row id preserved in raw evidence, ends with `## SPIKE COMPLETE` marker). Mirrors `test/phases/04-01-spike-findings.test.mjs`.
- `.planning/REQUIREMENTS.md` — CTT-01..CTT-08 traceability rows + CTT definitions block (Task 1).
- `.planning/STATE.md` + `.planning/ROADMAP.md` — Plan 04.1-01 marked complete; Wave 1 closed.

## Decisions Made

See `key-decisions` in frontmatter. Most load-bearing:

- **Phase 4.1 Gate Verdict = GO** — multi-turn native wake works; the whole phase rests on this and it holds.
- **`requestWakeup` is OUT of the critical path** — the SDK's `ctx.issues.requestWakeup` returned HTTP 404 on the REST surface for all three legal `reason` values, but the bait comments themselves natively re-woke the agent. Plan 04.1-03 drops the wake call.
- **D-14 dual-keyed + 5-phrase body fallback** — defense-in-depth on the runtime-noise filter. RESEARCH's HIGH-confidence prediction (`authorType === 'system'`) confirmed AND a structured `presentation` envelope captured; the new disposition phrase (`"Paperclip needs a disposition before this issue can continue"`) folded into the body-pattern fallback list.
- **Plan 04.1-05 ships the 0007 side table** — REST origin filters are broken (rowCount=500 cap regardless of originId; exact match returns 0). Side table is the defensive default; client-side `originId.startsWith(...)` filter kept as belt-and-braces for the upgrade-path case.
- **FLAG-1 reconciles as `caught-by-watchdog`** — the host recovery service DOES engage (the OQ3 attempt-2 evidence proves it) but D-11's defensive sweep catches it. CTT-05 is the steady-state path; CTT-06 banner is last-resort only.

## Cross-Reference: Locked Findings → Downstream Plans

| Downstream Plan | What it absorbs from 04.1-01 |
|-----------------|------------------------------|
| **Plan 04.1-02** (createTrueTask + chat.promote unification) | OQ3 PASS-NATIVE; OQ2 WEAK-REST-LIMIT. `createTrueTask` writes to `chat_topic_tasks` (the Plan 04.1-05 side table) immediately after `ctx.issues.create`. `originId` convention `chat-task:<topicIssueId>:<commentId>` confirmed by probe. |
| **Plan 04.1-03** (chat-topic lifecycle) | OQ3 PASS-NATIVE; OQ1 AGENT-LEAVES-IN-PROGRESS. `ensureTopicWakeable` DROPS `requestWakeup`. Keeps only the defensive D-11 status-flip-off-done check. Topic issues created at `in_progress`. No requestWakeup nudge on `chat.send`. |
| **Plan 04.1-04** (classifyComment + topic-watchdog + diagnostics fields) | D-14 PASS; OQ1 AGENT-LEAVES-IN-PROGRESS; FLAG-1 MOOT. `classifyComment` primary on `authorType === 'system'`, secondary on `presentation.kind === 'system_notice'`, body-pattern fallback list = 5 phrases. Topic-watchdog defensive-only; `topicStuck` triggers on N≥2 failed flips. |
| **Plan 04.1-05** (chat.taskOwned + chat.topic.archive + 0007 migration) | OQ2 WEAK-REST-LIMIT. Ship `migrations/0007_chat_topic_tasks.sql` (additive, plugin-namespace). `chat.taskOwned` data handler queries the side table, NOT `issues.list`. Defensive client-side fallback kept. |
| **Plan 04.1-06** (UI build) | D-14 structured metadata. D-16 diagnostics view renders `presentation.metadata.sections` as typed rows. CTT-06 banner wired to `topicStuck`. |
| **Plan 04.1-07** (closure) | All of above. Coexistence check ensures `chat_topic_tasks` side table is plugin-namespace and survives plugin disable; 04.1-VERIFICATION.md captures the full CTT-01..CTT-08 traceability. |

## Deviations from Plan

**None for Tasks 2 + 3 continuation** — the locked findings doc + traceability test + SUMMARY were written exactly as Plan 04.1-01's spec required.

The probe run itself was clean (no probe-script defects surfaced; the four sub-probes ran end-to-end). One ambient observation worth recording: `ctx.issues.requestWakeup` has no public REST surface on this host (all three attempts returned 404). That is not a defect in the probe — it is the finding. The probe correctly recorded the 404 outcome and continued to the next attempt; PROBE-OQ3's verdict is `PASS-NATIVE` precisely BECAUSE the bait comments themselves natively re-woke the agent, making the REST 404s irrelevant to the critical path.

## Issues Encountered

None during continuation. The probe captured a SYSTEM "Missing disposition" notice as the `observedReply` for OQ3 attempt 2 — that is NOT an agent reply but a structurally distinct artifact (the host's disposition-recovery service post-run). The locked findings doc explicitly notes this nuance under PROBE-OQ3 evidence rather than silently treating it as a reply. This was the source of the D-14 evidence (a real provoked recovery notice on a chat-topic issue).

## Authentication Gates

Task 2 was a `checkpoint:human-verify` — Eric ran the probe on the live Countermoves VPS, bookended by a verified snapshot, and pasted the JSON summary back into the session. This is normal flow for a live spike, not a deviation. The continuation agent inherits the JSON payload directly; the operator does not need to re-run anything to lock the findings.

## Followups

Non-blocking — for Eric to address at his leisure, NOT a blocker for Plans 04.1-02..04.1-06:

1. **Spike issues to clean up on Countermoves** — all tagged `[SPIKE 04.1]` in their titles, greppable:
   - `0dc3bea1-b60b-4f54-a2f0-cfd8c97f37e5` (OQ3 wake-gate probe — `COU-1757`)
   - `67f53651-d162-45dc-8897-34eb0d24e73a` (OQ1 status probe)
   - `496474c9-fbb9-4720-86b8-b0679640e9d3` (OQ2 task A)
   - `1958673d-69b2-4f63-ac44-4e461584c563` (OQ2 task B)

   Either `paperclipai issue delete <id>` per id, or roll back to the pre-probe snapshot the operator took at the start of the run.

2. **Stale comment in probe header (`scripts/spike/chat-true-task-spike-probe.mjs`)** — lines 49 & 56 reference `node scripts/safety/cli.mjs gate` as a standalone bookend, which is wrong (gate wraps an inner command + requires a verified snapshot; it's not a standalone bookend). Fix at low priority; the probe is throwaway anyway.

3. **Contaminated-codex artifact (`04.1-01-probe-output-contaminated-codex.txt`)** on the VPS — preserved as evidence of what happens when the agent runtime is broken. Safe to delete after Wave 5 closes.

## Verification

- `node --test test/phases/04.1-01-spike-findings.test.mjs` — 9 tests, 9 pass, 0 fail (verified 2026-05-20 during continuation).
- `node --check scripts/spike/chat-true-task-spike-probe.mjs` — passed (Task 1 verification, 2026-05-20).
- Findings doc carries the explicit `verdict: GO` in frontmatter that 04.1-02..04.1-06 reference.
- Countermoves was snapshotted before the probe run (operator-confirmed, 2026-05-20).
- Probe JSON preserved verbatim in the Raw evidence appendix so every claim is auditable.

## Self-Check: PASSED

- FOUND: `.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md`
- FOUND: `test/phases/04.1-01-spike-findings.test.mjs`
- FOUND: `scripts/spike/chat-true-task-spike-probe.mjs`
- FOUND: commit `9703dbe` (Task 1)
- FOUND: commit `eb44303` (Task 2 lock findings)
- FOUND: commit `30cba12` (Task 3 traceability test)

## Next Phase Readiness

- **Plan 04.1-02** (createTrueTask + chat.promote unification — Wave 2, autonomous TDD) is unblocked. The originId convention is confirmed; the side-table integration point is specified by the locked findings.
- **Plan 04.1-03** (chat-topic lifecycle — Wave 2, autonomous TDD) is unblocked. The wake mechanism is settled (no `requestWakeup`); the holding status is confirmed (`in_progress`).
- **Plan 04.1-04** (classifyComment + topic-watchdog — Wave 2, autonomous TDD) is unblocked. The discriminator is dual-keyed; the body-pattern fallback list is sized; the watchdog behaviour is specified.
- **Plan 04.1-05** (side table + chat.taskOwned + chat.topic.archive — Wave 3, autonomous TDD) is unblocked. Side-table-as-defensive-default is the locked choice.
- **Plan 04.1-06** (UI build — Wave 4, non-autonomous) and **Plan 04.1-07** (closure — Wave 5, non-autonomous) inherit the design inputs through Plans 04.1-02..04.1-05.

---
*Phase: 04.1-chat-true-task*
*Plan: 01 — falsify-first spike*
*Completed: 2026-05-20*
