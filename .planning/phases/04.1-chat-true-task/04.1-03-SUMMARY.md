---
phase: 04.1-chat-true-task
plan: 03
subsystem: worker
tags: [chat, topic-watchdog, lifecycle, defensive-sweep, fire-and-forget, opt-in-guard, tdd]

# Dependency graph
requires:
  - phase: 04.1-chat-true-task
    provides: "Plan 04.1-01 spike findings (locked) — PROBE-OQ3 PASS-NATIVE (multi-turn native re-wake works; requestWakeup REST 404), PROBE-OQ1 AGENT-LEAVES-IN-PROGRESS (watchdog is defensive-only)"
  - phase: 04-employee-chat
    provides: "chat-send.ts auto-reopen pattern (lines 92-110 — replaced), chat-topics.ts buildTopicDescription + child create (lines 86-97 + 176-185 — extended), opt-in-guard wrapActionHandler, ChatTopicsRepoCtx, host-faithful-db test wrapper"
provides:
  - "src/worker/chat/topic-watchdog.ts — ensureTopicWakeable + isTopicStuck + NON_TERMINAL_CONVERSATION_STATUS exports. Single source of truth for the D-11 flip-off-done sweep shared by chat.send (this plan) AND chat.messages (Plan 04.1-04)."
  - "chat.send auto-reopen REPLACED with `void ensureTopicWakeable(ctx, topicIssueId, companyId)` — fire-and-forget after the comment lands; never delays or fails the send."
  - "chat-topics buildTopicDescription EXTENDED with D-11 CONVERSATION CONTAINER instruction; 'private' removed (ROADMAP scope correction #1); child topic issues created at NON_TERMINAL_CONVERSATION_STATUS ('in_progress', single source of truth)."
  - "27 new node:test cases across topic-watchdog.test.mjs (17), chat-send.test.mjs (~4 net new), chat-topics.test.mjs (6 new). Full suite 1000 → 1024 tests; 1022 pass / 0 fail / 2 pre-existing skip."
affects: [04.1-04, 04.1-05, 04.1-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Watchdog helper as a fire-and-forget side-effect — invoked via `void` after the primary mutation lands so the helper's internal try/catch + warn-log shielding cannot delay or fail the caller. A 50ms-delayed get test pins the contract."
    - "Single-source-of-truth status constant — NON_TERMINAL_CONVERSATION_STATUS is exported once from topic-watchdog.ts and imported by chat-topics.ts. The initial child-topic status AND the watchdog flip target agree by construction; no thrash possible."
    - "Spike-locked regression guards — node:test cases that pin the spike findings (PROBE-OQ3 PASS-NATIVE: NEVER calls requestWakeup) so a future 'helpful' edit re-introducing the dropped call fails the suite immediately."
    - "Defensively-typed UI signal helper — isTopicStuck accepts a structural shape (status / activeRecoveryAction / successfulRunHandoff) rather than the SDK's strict Issue type which may not expose recovery fields."

key-files:
  created:
    - "src/worker/chat/topic-watchdog.ts"
    - "test/worker/chat/topic-watchdog.test.mjs"
  modified:
    - "src/worker/handlers/chat-send.ts (auto-reopen block REPLACED with ensureTopicWakeable; top-of-file rationale updated)"
    - "src/worker/handlers/chat-topics.ts (buildTopicDescription D-11 instruction, 'private' removed, child status NON_TERMINAL_CONVERSATION_STATUS)"
    - "test/worker/chat/chat-send.test.mjs (U2 watchdog-still-fires + U3 fire-and-forget + U5 no-watchdog-on-failure + PROBE-OQ3 regression guard; makeCtx gains _getCalls + getDelayMs)"
    - "test/worker/chat/chat-topics.test.mjs (6 new chat.topic.create assertions; existing D-02-wake-contract test updated to expect status:'in_progress')"

key-decisions:
  - "Drop ctx.issues.requestWakeup entirely from ensureTopicWakeable — the spike's PROBE-OQ3 PASS-NATIVE finding makes the call unnecessary AND the REST surface returns 404 on the live Countermoves host. The Plan 04.1-03 PLAN.md must-have text says 'plus a wake nudge'; the Wave 1 locked findings override (the prompt explicitly instructs the executor to honor the findings)."
  - "NON_TERMINAL_CONVERSATION_STATUS exported as a const literal 'in_progress' from topic-watchdog.ts and imported by chat-topics.ts for the initial child-topic create — this prevents the two sites from drifting (a future edit changing the watchdog target to 'todo' would not silently leave chat-topics creating at the old value)."
  - "The watchdog flips OFF status ∈ {done, cancelled, blocked} → in_progress. 'backlog' / 'todo' / 'in_review' are non-terminal and left alone (an agent may legitimately move through them); only the terminal-or-recovery set is corrected."
  - "Fire-and-forget via `void ensureTopicWakeable(...)` — the helper internally try/catches every step so the void is safe (no unhandled rejection), and a slow / failing watchdog cannot make chat.send latent or fail. The 50ms-delayed-get test ('chat.send returns BEFORE a slow watchdog completes') pins this contract."
  - "isTopicStuck triggers on activeRecoveryAction set OR successfulRunHandoff.exhausted true — pinned by isTopicStuck-TRUE-RECOVERY + isTopicStuck-TRUE-EXHAUSTED-HANDOFF. Plan 04.1-04 surfaces this signal into chat.messages response (topicStuck field); UI banner (Plan 04.1-06 CTT-06) reads it. Single bool, sourced once."

patterns-established:
  - "Helper-with-handler-with-tests trio (continuation of Plan 04.1-02): src/worker/chat/topic-watchdog.ts + test/worker/chat/topic-watchdog.test.mjs alongside the two handler call-sites (chat-send.ts now, chat-messages.ts via Plan 04.1-04). The helper is testable in isolation; the handler tests verify only the wiring."
  - "Spike-finding regression guard pattern — a dedicated node:test case asserts the LITERAL absence of a behaviour the spike falsified ('NEVER calls ctx.issues.requestWakeup'). Future contributors who add the call back fail the suite immediately and read the spike-findings doc."

requirements-completed: [CTT-03, CTT-05, CTT-06]

# Metrics
duration: 22min
completed: 2026-05-20
---

# Phase 4.1 Plan 03: topic-watchdog (D-09/D-11) + chat.send wake-on-every-send + chat-topics converse-only Summary

**ensureTopicWakeable defensive flip-off-done helper extracted as the single source of truth for chat.send (fire-and-forget) and (Plan 04.1-04) chat.messages (per-poll); chat-topics extended with D-11 CONVERSATION CONTAINER instruction and `in_progress` initial status — the watchdog flip target and the initial status agree by construction, so the chat-topic lifecycle has no thrash.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-05-20T~09:10Z
- **Completed:** 2026-05-20T~09:32Z
- **Tasks:** 3 (autonomous TDD; RED → GREEN for each)
- **Files modified:** 5 (2 created, 3 modified)
- **Commits:** 6 (`aa75677..dc7438e`)
- **Test suite:** 1000 → 1024 (+24 cases; 1022 pass / 0 fail / 2 pre-existing skip)

## Accomplishments

- `src/worker/chat/topic-watchdog.ts` (NEW) — `ensureTopicWakeable(ctx, topicIssueId, companyId)` performs the D-11 defensive flip-off-done sweep (get → status-check → update IF status ∈ {done, cancelled, blocked} → flip to `in_progress`). Every step in try/catch + warn-log; never throws to caller. `isTopicStuck(issue)` returns the UI-SPEC Pattern G trigger boolean. `NON_TERMINAL_CONVERSATION_STATUS = 'in_progress'` exported so chat-topics.ts agrees by construction.
- `src/worker/handlers/chat-send.ts` — auto-reopen try/catch block (former lines 92-110) REPLACED with a single `void ensureTopicWakeable(ctx, topicIssueId, companyId)` call. Fire-and-forget after the comment + chat_messages insert land. Top-of-file comment block rewritten with the spike-locked D-09/D-11/PROBE-OQ3 PASS-NATIVE rationale.
- `src/worker/handlers/chat-topics.ts` — `buildTopicDescription` extended with the D-11 'CONVERSATION CONTAINER' instruction block (RESEARCH §Pitfall 6 verbatim — strong, unambiguous wording). The (incorrect) word 'private' is removed (ROADMAP scope correction #1). Child topic-issue create now uses `status: NON_TERMINAL_CONVERSATION_STATUS` (= 'in_progress') — single source of truth; parent `Chat — <employee>` issue keeps `status: 'todo'`.
- 27 new node:test cases (topic-watchdog: 17; chat-send: ~4 net new; chat-topics: 6) — well above the plan's ≥14 minimum.

## Task Commits

Each task was committed atomically using TDD RED → GREEN pairs:

1. **Task 1: ensureTopicWakeable + isTopicStuck helpers** —
   - `aa75677` (RED) — `test(04.1-03): RED — topic-watchdog tests (ensureTopicWakeable + isTopicStuck)` (17 cases)
   - `216890c` (GREEN) — `feat(04.1-03): topic-watchdog ensureTopicWakeable + isTopicStuck (D-09/D-11)`

2. **Task 2: chat-send.ts replaces auto-reopen with ensureTopicWakeable** —
   - `cc3501f` (RED) — `test(04.1-03): RED — chat.send fires ensureTopicWakeable fire-and-forget` (3 new + PROBE-OQ3 regression guard)
   - `813b6f1` (GREEN) — `feat(04.1-03): chat.send fires ensureTopicWakeable fire-and-forget (D-09/D-11)`

3. **Task 3: chat-topics.ts D-11 converse-only + in_progress** —
   - `5c821a2` (RED) — `test(04.1-03): RED — chat-topics D-11 description + in_progress initial status` (6 new)
   - `dc7438e` (GREEN) — `feat(04.1-03): chat-topics D-11 converse-only + in_progress initial status`

## Files Created/Modified

- `src/worker/chat/topic-watchdog.ts` — NEW. Exports `ensureTopicWakeable` (fire-and-forget defensive sweep), `isTopicStuck` (UI signal helper), `NON_TERMINAL_CONVERSATION_STATUS` (literal 'in_progress').
- `test/worker/chat/topic-watchdog.test.mjs` — NEW. 17 cases covering happy paths, flip paths, defensive paths, and the PROBE-OQ3 regression guard.
- `src/worker/handlers/chat-send.ts` — Auto-reopen block REPLACED with single watchdog call. Imports `ensureTopicWakeable`. Top-of-file rationale updated.
- `src/worker/handlers/chat-topics.ts` — `buildTopicDescription` extended with D-11 instruction (CONVERSATION CONTAINER block); 'private' removed. Child create uses `NON_TERMINAL_CONVERSATION_STATUS`. Imports the const from topic-watchdog.ts. Top-of-file rationale extended.
- `test/worker/chat/chat-send.test.mjs` — `makeCtx` gains `_getCalls` tracker + `getDelayMs` option for the fire-and-forget test. Existing "auto-reopens to in_progress" tests survive (now exercise the helper). New U2 anti-regression (watchdog STILL fires on in_progress), U3 fire-and-forget (50ms delay test), U5 no-watchdog-on-send-failure. PROBE-OQ3 regression guard test (across 5 statuses) replaces the prior OQ-3 STATUS-FLIP-NOT-NEEDED test with the spike-locked rationale.
- `test/worker/chat/chat-topics.test.mjs` — 6 new chat.topic.create assertions: D-11 instruction presence, Phase-4 framing survival, 'private' absence, child status 'in_progress', parent stays 'todo', other fields unchanged. The existing 'D-02 wake contract' test updated to expect `status: 'in_progress'`.

## Decisions Made

See key-decisions in frontmatter. Operationally critical:

- **Dropping requestWakeup overrode the PLAN.md text**, honoring the Wave 1 locked spike findings as the prompt explicitly instructed. The PLAN.md must-have list said 'plus a wake nudge'; the findings said the nudge is unnecessary AND the REST returns 404. The prompt's `<scope_simplification_from_wave1>` section was authoritative.
- **NON_TERMINAL_CONVERSATION_STATUS export** is the single-source-of-truth pattern that prevents the initial-create status and the watchdog flip target from drifting in future edits.
- **isTopicStuck is structurally typed** (not via SDK Issue) because the SDK type may not expose `activeRecoveryAction` / `successfulRunHandoff` directly — RESEARCH §Pattern 3 noted the same.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 — Scope simplification from Wave 1 findings] Dropped `ctx.issues.requestWakeup` from `ensureTopicWakeable`**

- **Found during:** Task 1 (RED test design)
- **Issue:** PLAN.md `must_haves.truths` line 20 contemplated calling `ctx.issues.requestWakeup` with a locked argument set from spike findings. The locked spike findings (`04.1-01-SPIKE-FINDINGS.md` PROBE-OQ3 verdict PASS-NATIVE) proved (a) multi-turn native re-wake works without the call AND (b) the REST surface returns HTTP 404 on the live host version anyway. Keeping the call would be a no-op REST 404 on every send.
- **Fix:** Implemented `ensureTopicWakeable` with ONLY the defensive D-11 flip-off-done branch. Added a dedicated regression guard test (`'NEVER calls ctx.issues.requestWakeup'`) across 5 status paths so a future "helpful" edit re-introducing the call fails the suite immediately.
- **Files modified:** `src/worker/chat/topic-watchdog.ts`, `test/worker/chat/topic-watchdog.test.mjs` (regression guard).
- **Verification:** 17/17 topic-watchdog tests + 14/14 chat-send tests + full chat suite + tsc clean. The prompt's `<scope_simplification_from_wave1>` section explicitly instructed this override.
- **Committed in:** `216890c` (Task 1 GREEN).

**2. [Rule 3 — Blocking] `chat-send.ts` acceptance criterion `grep -cE "requestWakeup" returns 0` not literal-zero (3 comment hits remain)**

- **Found during:** Task 2 verification step
- **Issue:** The PLAN.md acceptance grep expects `chat-send.ts` to carry zero references to `requestWakeup`. The Wave 1 findings require updating the top-of-file rationale to explain WHY the call was dropped — which mentions `requestWakeup` in prose 3 times.
- **Fix:** Verified the substantive intent is met by a stricter grep — `grep -nE "ctx\.issues\.requestWakeup|\.requestWakeup\("` returns 0 (no functional calls). The three textual mentions are all explanatory comments documenting the drop. The substantive contract (no call) is enforced by the regression-guard test in topic-watchdog.test.mjs across 5 status paths.
- **Files modified:** none additional.
- **Verification:** `grep -nE "ctx\.issues\.requestWakeup|\.requestWakeup\(" src/worker/handlers/chat-send.ts` returns 0 hits.
- **Committed in:** `813b6f1` (Task 2 GREEN).

**3. [Rule 1 — Bug] Test regex for 'Do NOT mark it' was not whitespace-tolerant**

- **Found during:** Task 3 GREEN verification (first test run)
- **Issue:** The instruction text in `buildTopicDescription` is line-wrapped: `'IMPORTANT: ... Do\nNOT mark it`'`. The initial test regex `/Do NOT mark it/i` failed against a newline between 'Do' and 'NOT'.
- **Fix:** Updated regex to `/Do\s+NOT\s+mark\s+it/i` to tolerate the line wrap.
- **Files modified:** `test/worker/chat/chat-topics.test.mjs`.
- **Verification:** 21/21 chat-topics tests GREEN; 151/151 full chat suite; 1022/1024 full suite (2 pre-existing skips).
- **Committed in:** `dc7438e` (rolled into Task 3 GREEN).

---

**Total deviations:** 3 auto-fixed (1 scope-simplification per Wave 1 findings, 1 blocking interpretation, 1 minor test-fix).
**Impact on plan:** All three were instructed by the prompt or required for correctness. No scope creep — the simplification REDUCED scope per the spike's load-bearing finding. The acceptance-criterion interpretation preserves the substantive contract (no functional call) via a stricter grep + a regression-guard test.

## Issues Encountered

- The original test regex line-wrap issue (Deviation 3) — caught immediately on the first GREEN run and fixed inline. Self-contained; no rework outside Task 3.
- No host-side issues; no defects surfaced; no flaky tests.

## User Setup Required

None — no external service configuration required.

## Cross-Plan Contract Delivery

Plan 04.1-04 (`chat-messages.ts` watchdog hook) can now:
- `import { ensureTopicWakeable, isTopicStuck, NON_TERMINAL_CONVERSATION_STATUS } from '../chat/topic-watchdog.ts';`
- Call `ensureTopicWakeable` from the per-poll handler — same fire-and-forget pattern as chat.send.
- Read `isTopicStuck(issue)` to populate the `topicStuck` field on the `chat.messages` response (Plan 04.1-04 PATTERNS.md lines 463-465). The "N≥2 failed flips" trigger semantics (per 04.1-01-SPIKE-FINDINGS Plan-04-04 row) belong in chat-messages.ts — this helper just returns the current-state boolean.

## Next Phase Readiness

- **Plan 04.1-04 (chat.messages classifyComment + watchdog wiring) UNBLOCKED.** Imports the three exports above; touches `src/worker/handlers/chat-messages.ts` only — zero `files_modified` overlap with Plan 04.1-03 (same wave 2, parallel-safe per PLAN.md).
- **Plan 04.1-02 + 04.1-03 together** satisfy the worker-tier reliability primitives for CTT-03 / CTT-05 / CTT-06; the UI surfaces ship in Plan 04.1-06 (CTT-06 banner reads `topicStuck`).
- No blockers; no carry-over defects.

## Self-Check

Files claimed exist:
- `src/worker/chat/topic-watchdog.ts` — FOUND
- `test/worker/chat/topic-watchdog.test.mjs` — FOUND
- `src/worker/handlers/chat-send.ts` — FOUND (modified)
- `src/worker/handlers/chat-topics.ts` — FOUND (modified)
- `test/worker/chat/chat-send.test.mjs` — FOUND (modified)
- `test/worker/chat/chat-topics.test.mjs` — FOUND (modified)

Commits claimed exist (`git log --oneline aa75677..dc7438e`):
- `aa75677` — FOUND (RED Task 1)
- `216890c` — FOUND (GREEN Task 1)
- `cc3501f` — FOUND (RED Task 2)
- `813b6f1` — FOUND (GREEN Task 2)
- `5c821a2` — FOUND (RED Task 3)
- `dc7438e` — FOUND (GREEN Task 3)

Test suite delta: 1000 → 1024 cases (+24); +27 new across the three target files (some pre-existing chat-send tests consolidated). Above the plan's ≥14 minimum.

## Self-Check: PASSED

---
*Phase: 04.1-chat-true-task*
*Plan: 03*
*Completed: 2026-05-20*
