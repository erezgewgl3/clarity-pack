---
phase: 04.1-chat-true-task
plan: 04
subsystem: worker
tags: [chat, comment-classify, runtime-noise-filter, watchdog-cadence, host-stuck-signal, opt-in-guard, tdd, spike-locked]

# Dependency graph
requires:
  - phase: 04.1-chat-true-task
    provides: "Plan 04.1-01 spike findings (locked) — PROBE-D14-DISCRIM PASS dual-keyed (authorType='system' + presentation.kind='system_notice') + 5-phrase body fallback including the new 'Paperclip needs a disposition' captured verbatim on Countermoves COU-1757."
  - phase: 04.1-chat-true-task
    provides: "Plan 04.1-03 helpers — ensureTopicWakeable (defensive flip-off-done), isTopicStuck (UI-SPEC Pattern G trigger), NON_TERMINAL_CONVERSATION_STATUS const, from src/worker/chat/topic-watchdog.ts."
  - phase: 04-employee-chat
    provides: "src/worker/handlers/chat-messages.ts (extended in place), opt-in-guard wrapDataHandler, ChatTopicsRepoCtx, host-faithful-db test wrapper."
provides:
  - "src/worker/chat/comment-classify.ts — PURE function classifyComment + CommentClass type + frozen RUNTIME_PHRASES list (5 entries). The single source of truth for D-14/D-15 host-field-first conversation/runtime-noise classification."
  - "chat.messages handler EXTENDED — D-14/D-15 runtime-noise filter (default ON, opt-out via includeDiagnostics:true for D-16 diagnostics view); D-11 watchdog cadence (ensureTopicWakeable fire-and-forget on every poll); D-13 host-stuck signal (topicStuck + recoveryOwner in response shape — Plan 04.1-06's HostStuckBanner data path)."
  - "31 new node:test cases across two files (17 comment-classify + 14 net new chat-messages U-tests). Full suite 1024 → 1055 tests; 1053 pass / 0 fail / 2 pre-existing skip."
affects: [04.1-05, 04.1-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pure-function discriminator with a frozen exported config — RUNTIME_PHRASES is `readonly string[]` frozen via Object.freeze, exported so future plans can extend by editing this file (and updating the contract test) but cannot mutate at runtime."
    - "Two-tier discriminator (host-field primary, body fallback) — PRIMARY authorType==='system' (HIGH confidence, host-stamped); SECONDARY presentation.kind==='system_notice' (correlated; D-16 envelope); FALLBACK 5-phrase RUNTIME_PHRASES body match (defense-in-depth)."
    - "Pitfall 4 anti-regression pin — Test 11 / Test 11b assert the literal Plan 04.1-02 marker wording 'Task created — <id>, assigned to <name>.' classifies as 'conversation'. A future RUNTIME_PHRASES addition that accidentally matches the marker fails this test BEFORE it could strip a real marker from a live chat thread. U5 in chat-messages.test.mjs cross-pins the same invariant at the integration tier."
    - "Fire-and-forget watchdog at handler head — `void ensureTopicWakeable(ctx, topicIssueId, companyId)` invoked AFTER the opt-in gate, BEFORE listComments. The helper internally try/catches every step so the void is safe; a slow / failing watchdog never delays the messages response. U7 pins this with a 50ms-delayed get and a 85ms wall-clock budget."
    - "Best-effort stuck-read with graceful degrade — ctx.issues.get(topicIssueId, companyId) read once for isTopicStuck + recoveryOwnerName extraction; a thrown read degrades to topicStuck:false, recoveryOwner:null so the messages response is still returned (U13)."
    - "Response-shape grown additively, not breakingly — kind/topicIssueId/messages preserved; topicStuck + recoveryOwner added. Plan 04-05's existing UI keeps working with the old fields; Plan 04.1-06's banner reads the new ones."

key-files:
  created:
    - "src/worker/chat/comment-classify.ts (102 lines, 1 pure function + 1 frozen const + 1 type export)"
    - "test/worker/chat/comment-classify.test.mjs (227 lines, 17 tests)"
  modified:
    - "src/worker/handlers/chat-messages.ts (in-place extension at five precise places per PATTERNS.md DELTA: imports, CommentLike type, includeDiagnostics param, fire-and-forget watchdog, second .filter() in the pipeline, stuck-read block, response-shape additions)"
    - "test/worker/chat/chat-messages.test.mjs (preserved 9 original cases; +14 net new U1..U13 cases covering filter / watchdog / stuck-signal / opt-in-first / shape contract / graceful-degrade)"

key-decisions:
  - "RUNTIME_PHRASES locked at 5 entries per the Wave 1 scope refinement — RESEARCH.md's four phrases ('needs a disposition', 'blocked on a recovery owner', 'finish_successful_run_handoff', 'exhausted the bounded corrective handoff') PLUS the verbatim 5th phrase 'Paperclip needs a disposition before this issue can continue' captured live on Countermoves COU-1757 comment fa25ef4d-... at 2026-05-20T08:25:40Z per 04.1-01-SPIKE-FINDINGS PROBE-D14-DISCRIM. Frozen via Object.freeze so a downstream module cannot mutate the contract."
  - "Discriminator order PRIMARY → SECONDARY → FALLBACK is LOCKED per RESEARCH §Pattern 2 + Wave 1 findings. The body match never overrides a host field; a system-authored comment with empty body still classifies as runtime-noise (Test 14)."
  - "Defensive null/undefined default — classifyComment({authorType:null, body:null, presentation:null}) returns 'conversation'. We never strip a legitimate-but-incomplete row; the discriminator only filters on a POSITIVE system stamp."
  - "Empty body guard in the fallback — an empty body short-circuits before the some()/includes scan to avoid any potential 'empty string matches every pattern' edge (defensive)."
  - "ensureTopicWakeable invoked AFTER the opt-in gate, BEFORE listComments — never wake an agent for an opted-out user (U11 pins this with a listComments wrapper that fails the test if called for OPT_IN_REQUIRED)."
  - "Stuck-read is a SECOND ctx.issues.get on the same topicIssueId (the watchdog reads once internally; the stuck-read is awaited because the response shape needs the result). U7's 85ms wall-clock budget reflects this: the watchdog read is fire-and-forget; the stuck-read is awaited. Total elapsed ≈ ONE 50ms get, not TWO."
  - "Response shape grown additively — { kind, topicIssueId, messages, topicStuck, recoveryOwner }. U12 pins the exact key set via an expected-keys whitelist so a future surprise field surfaces immediately."

patterns-established:
  - "Spike-locked discriminator pure function — the file's top-of-file block cites the exact spike-findings provenance (PROBE-D14-DISCRIM comment fa25ef4d-... at 2026-05-20T08:25:40Z) so a future contributor reading the file knows where the dual-keyed contract came from. The same pattern proved effective in Plan 04.1-03's topic-watchdog.ts (which cites PROBE-OQ3 PASS-NATIVE for the dropped requestWakeup call)."
  - "Filter-at-the-pipeline-edge — the runtime-noise filter is a SECOND .filter() chained right after the existing typeof-id guard, BEFORE .sort() and .map(). Adding a third filter for some future class (e.g. 'archived') would slot into the same pipeline shape with zero rearrangement."
  - "Opt-in-before-side-effects ordering — wrapDataHandler runs the opt-in gate first; then the handler body's first side-effect is the void watchdog call; then the parameter validation early returns; then listComments. U11 verifies the order by failing if any host call fires for an opted-out user."

requirements-completed: [CTT-04, CTT-06]

# Metrics
duration: 9min
completed: 2026-05-20
commits: 4
commit-range: 1f42a9c..31ac0a4
tests-added: 31
tests-total: 1055
tests-pass: 1053
tests-skip: 2
---

# Phase 4.1 Plan 04: classifyComment + chat.messages runtime-noise filter + watchdog-on-poll + host-stuck signal Summary

**Pure-function comment classifier (D-14/D-15 host-field-first discriminator + 5-phrase body fallback) wired into chat.messages — the chat thread now shows conversational messages only by default; the D-11 watchdog runs on every poll; the D-13 host-stuck signal (topicStuck + recoveryOwner) is in the response shape ready for Plan 04.1-06's HostStuckBanner. The conversation/lifecycle separation at the worker tier is complete: chat.send wakes on every send (Plan 04.1-03), chat.messages wakes on every poll (this plan), runtime-bookkeeping comments never reach the UI.**

## Commits

| # | Hash | Type | Summary |
|---|---|---|---|
| 1 | `1f42a9c` | test | RED — classifyComment pure function tests (17 cases). |
| 2 | `0b9d154` | feat | classifyComment + CommentClass + frozen RUNTIME_PHRASES (102 lines, D-14 host-field-first + body fallback). |
| 3 | `53c40ff` | test | RED — chat.messages runtime-noise filter + watchdog + topicStuck (U1..U13, 13 new cases, 10 failing). |
| 4 | `31ac0a4` | feat | chat.messages EXTENDED — D-11 watchdog cadence + D-13/D-14/D-15 + D-16 diagnostics opt-in. |

## Test Suite Delta

- **Total:** 1024 → 1055 (+31).
- **comment-classify.test.mjs:** 17 new cases (PRIMARY / SECONDARY / FALLBACK / Pitfall-4 marker pin / defensive null/empty / RUNTIME_PHRASES contract).
- **chat-messages.test.mjs:** 13 new U1..U13 cases (filter default OFF / D-16 opt-in / body-pattern fallback / marker integration / watchdog cadence / watchdog flip-off-done / fire-and-forget timing / topicStuck false / topicStuck true via recovery / topicStuck true via exhausted handoff / opt-in-first / response shape / graceful degrade); original 9 cases preserved with the `authorType` field added to fixtures.
- **Full chat sub-suite:** 182/182 pass.
- **Full project suite:** 1053 pass / 0 fail / 2 pre-existing skip.
- **tsc --noEmit:** clean.

## Locked Findings Applied

Per the prompt's `<scope_refinement_from_wave1>` instructions, all six refinements landed verbatim:

1. **Dual-keyed discriminator** — PRIMARY `authorType === 'system'`, SECONDARY `presentation?.kind === 'system_notice'`, FALLBACK RUNTIME_PHRASES (5 entries). Order pinned by Tests 1-10 + Test 14.
2. **RUNTIME_PHRASES = 5 entries** — RESEARCH.md's 4 + the verbatim 5th phrase captured live on Countermoves. Exported as `readonly string[]` frozen via Object.freeze. Contract pinned by the `RUNTIME_PHRASES exports five phrases...` test.
3. **Marker-comment wording pin** — Test 11 / Test 11b assert `'Task created — abc12345, assigned to CEO.'` (and `BEAAA-202 / CFO`) classify as 'conversation'. U5 cross-pins the same invariant at the chat-messages integration tier.
4. **chat-messages extension at the 5 precise places** — imports + CommentLike type + includeDiagnostics param + void ensureTopicWakeable + .filter() + stuck-read block + response-shape additions.
5. **Response shape ADDITIVE only** — `kind:'messages' + topicIssueId + messages` preserved; `topicStuck + recoveryOwner` added. U12 pins the exact key set.
6. **Opt-in gate runs BEFORE the watchdog hook** — wrapDataHandler wraps the entire handler body including the void ensureTopicWakeable call; an opted-out user returns OPT_IN_REQUIRED before any ctx.issues call fires. U11 pins this.

## Deviations from Plan

**None.** Plan executed exactly as written, with the Wave 1 scope refinements applied verbatim where the original PLAN.md text differed (the 5th phrase, the dual-keyed discriminator wording). Both refinements were explicitly authorized by the prompt's `<scope_refinement_from_wave1>` block.

One small implementation choice worth noting:

- **Empty body short-circuit in the body-pattern fallback** — added `body.length > 0 &&` before the some()/includes scan. Defensive: an empty body string cannot match any non-empty phrase via `includes()`, but the guard avoids any future change to RUNTIME_PHRASES introducing an empty-string entry that would match everything. Test 13 (EMPTY-BODY-USER) verifies an empty user comment classifies as conversation; Test 14 (EMPTY-BODY-SYSTEM) verifies the authorType discriminator wins regardless.

## Requirements Status

- **CTT-04 (runtime noise filtered)** — IMPLEMENTED at the worker tier. The chat thread shows conversational messages only by default; the D-16 diagnostics toggle exposes the filtered rows opt-in for an operator who wants to diagnose. UI rendering of `.runtime-noise-comment` styling ships with Plan 04.1-06.
- **CTT-06 (host-stuck signal)** — DATA PATH COMPLETE. `topicStuck: boolean` + `recoveryOwner: string | null` are in the chat.messages response shape ready for Plan 04.1-06's HostStuckBanner.
- Together with Plan 04.1-03's helpers, the lifecycle backstop (D-11 watchdog) now runs on BOTH every send (Plan 04.1-03) AND every poll (this plan).

## Self-Check: PASSED

Files created (verified to exist on disk):
- `src/worker/chat/comment-classify.ts` — FOUND.
- `test/worker/chat/comment-classify.test.mjs` — FOUND.

Files modified (verified to compile + test):
- `src/worker/handlers/chat-messages.ts` — FOUND, extended in place.
- `test/worker/chat/chat-messages.test.mjs` — FOUND, extended with 13 new tests.

Commits verified via `git log --oneline -6`:
- `1f42a9c` test(04.1-04): RED — classifyComment pure function tests
- `0b9d154` feat(04.1-04): classifyComment pure function (D-14 host-field-first + body fallback)
- `53c40ff` test(04.1-04): RED — chat.messages runtime-noise filter + watchdog + topicStuck
- `31ac0a4` feat(04.1-04): chat.messages runtime-noise filter + watchdog cadence + host-stuck signal (D-11/D-13/D-14/D-15)

Acceptance grep counts (all >= plan thresholds):
- `classifyComment` in chat-messages.ts: 4 (≥2 required)
- `ensureTopicWakeable` in chat-messages.ts: 3 (≥2 required)
- `isTopicStuck` in chat-messages.ts: 3 (≥2 required)
- `void ensureTopicWakeable` in chat-messages.ts: 1 (exactly 1 required)
- `includeDiagnostics` in chat-messages.ts: 3 (≥2 required)
- `topicStuck` in chat-messages.ts: 7 hits (response shape + variable + checks)
- `authorType === 'system'` in comment-classify.ts: 2 (1 in code, 1 in doc-comment)
- `presentation?.kind === 'system_notice'` in comment-classify.ts: 2 (1 code, 1 doc)
- Pitfall-4 marker test (`Task created —`): 4 hits across comment-classify.test.mjs + chat-messages.test.mjs

Test suite full pass: 1055 tests / 1053 pass / 0 fail / 2 pre-existing skip. tsc --noEmit clean.

**NEXT:** Plan 04.1-05 (chat_topic_tasks side table + chat.taskOwned data handler) — UNBLOCKED. Imports from this plan: nothing (Plan 04.1-05 is independent on `files_modified`). Plan 04.1-06 (UI surfaces — HostStuckBanner reads topicStuck/recoveryOwner from this plan's response shape) UNBLOCKED.
