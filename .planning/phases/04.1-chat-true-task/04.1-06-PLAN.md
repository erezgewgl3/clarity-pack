---
phase: 04.1-chat-true-task
plan: 06
type: execute
wave: 4
depends_on: [02, 03, 04, 05]
files_modified:
  - src/ui/surfaces/chat/true-task/true-task-toggle.tsx
  - src/ui/surfaces/chat/true-task/true-task-dialog.tsx
  - src/ui/surfaces/chat/true-task/inline-task-card.tsx
  - src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx
  - src/ui/surfaces/chat/active-tasks-owned.tsx
  - src/ui/surfaces/chat/archive-topic-button.tsx
  - src/ui/surfaces/chat/archived-topics-pill.tsx
  - src/ui/surfaces/chat/diagnostics-toggle.tsx
  - src/ui/surfaces/chat/host-stuck-banner.tsx
  - src/ui/surfaces/chat/composer.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - src/ui/surfaces/chat/context-rail.tsx
  - src/ui/surfaces/chat/topic-strip.tsx
  - src/ui/surfaces/chat/index.tsx
  - src/ui/styles/chat.css
  - package.json
autonomous: false
requirements: [CTT-01, CTT-02, CTT-04, CTT-06, CTT-07, CTT-08]
user_setup: []

must_haves:
  truths:
    - "Composer Send-as-task toggle (D-01): when armed, hitting Send opens the confirm dialog instead of calling chat.send; placeholder + send-button copy + ARMED visual state match UI-SPEC Pattern A exactly."
    - "Confirm dialog (D-02): native <dialog> element (NO shadcn import; UI-SPEC Registry Safety), pre-filled title + assignee select (D-06 default = chatted employee, reassignable from chat.roster), source-message preview block, Create / Keep editing buttons; calls chat.createTrueTask; on success closes + disarms + clears composer."
    - "Inline task card (D-03) renders inline at the marker comment's place in the thread, distinct from .bubble (uses `.inline-task-card` per chat.css additions); shows TASK CREATED eyebrow + title + assignee + BEAAA-NNN ref-chip + live status pill."
    - "Active tasks owned (D-08) replaces the existing stub in context-rail.tsx with the live <ActiveTasksOwned> component reading chat.taskOwned; reuses .task-row + .st.* classes."
    - "Archive topic affordance (D-10) inserted as the FIRST `.qa` row in context-rail's Quick actions block; two-step in-row confirmation (YES + KEEP TOPIC); on success the topic is hidden from the strip; an `+N archived` muted pill at the right end of the strip lets the operator un-archive."
    - "Runtime-noise diagnostics toggle (D-16) sits in the thread header head-actions; OFF by default; armed state threads includeDiagnostics:true through to chat.messages; revealed runtime comments render as `.runtime-noise-comment` (NOT a bubble)."
    - "Host-stuck banner (D-13) renders inside .messages as a position:sticky strip below the .auto-refresh indicator when chat.messages returns topicStuck:true; NO retry button, NO auto-recover — banner unmounts silently on next 15s poll when topicStuck:false."
    - "All new CSS scoped under [data-clarity-surface=\"chat\"]; ~140 additional lines appended at the bottom of src/ui/styles/chat.css under a `/* ---- Phase 4.1 — chat → true task ---- */` heading; every selector exists per UI-SPEC §"CSS additions" table."
    - "package.json bumped from 0.7.8 → 0.8.0 (Phase 4.1 is a feature bump, not a patch); the rebuilt esbuild UI + worker bundles compile clean; clarity-pack-0.8.0.tgz packs successfully."
    - "Operator visual-fidelity drill on Countermoves PASSES — the seven new affordances render to match UI-SPEC; the live multi-turn chat reliably wakes the agent on every operator message; the archive control hides + un-archives; the diagnostics toggle reveals/hides runtime noise; the host-stuck banner appears when provoked."
  artifacts:
    - path: "src/ui/surfaces/chat/true-task/true-task-toggle.tsx"
      provides: "Composer toggle component (UI-SPEC Pattern A)."
      exports: ["TrueTaskToggle"]
      contains: "true-task-toggle"
    - path: "src/ui/surfaces/chat/true-task/true-task-dialog.tsx"
      provides: "Native <dialog> confirm modal (UI-SPEC Pattern B)."
      exports: ["TrueTaskDialog"]
      contains: "showModal"
    - path: "src/ui/surfaces/chat/true-task/inline-task-card.tsx"
      provides: "In-thread task card (UI-SPEC Pattern C)."
      exports: ["InlineTaskCard"]
      contains: "inline-task-card"
    - path: "src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx"
      provides: "Status pill thin wrapper."
      exports: ["ChatTaskStatusPill"]
      contains: "ChatTaskStatusPill"
    - path: "src/ui/surfaces/chat/active-tasks-owned.tsx"
      provides: "Polled list of spun-off tasks (UI-SPEC Pattern D)."
      exports: ["ActiveTasksOwned"]
      contains: "chat.taskOwned"
    - path: "src/ui/surfaces/chat/archive-topic-button.tsx"
      provides: "Two-step archive control (UI-SPEC Pattern E)."
      exports: ["ArchiveTopicButton"]
      contains: "chat.topic.archive"
    - path: "src/ui/surfaces/chat/archived-topics-pill.tsx"
      provides: "+N archived strip-end pill (UI-SPEC Pattern E tail)."
      exports: ["ArchivedTopicsPill"]
      contains: "archived-topics-pill"
    - path: "src/ui/surfaces/chat/diagnostics-toggle.tsx"
      provides: "Header diagnostics toggle (UI-SPEC Pattern F)."
      exports: ["DiagnosticsToggle"]
      contains: "diagnostics-toggle"
    - path: "src/ui/surfaces/chat/host-stuck-banner.tsx"
      provides: "Sticky host-stuck notice (UI-SPEC Pattern G)."
      exports: ["HostStuckBanner"]
      contains: "host-stuck-banner"
    - path: "src/ui/styles/chat.css"
      provides: "Phase 4.1 CSS additions appended."
      contains: "Phase 4.1 — chat → true task"
    - path: "package.json"
      provides: "Version bump to 0.8.0; clarity-pack-0.8.0.tgz pack."
      contains: "0.8.0"
  key_links:
    - from: "Composer Send button when taskArmed"
      to: "TrueTaskDialog → chat.createTrueTask"
      via: "usePluginAction('chat.createTrueTask')"
      pattern: "chat.createTrueTask"
    - from: "ContextRail Active tasks owned section"
      to: "ActiveTasksOwned → chat.taskOwned"
      via: "usePluginData('chat.taskOwned')"
      pattern: "chat.taskOwned"
    - from: "MessageThread"
      to: "HostStuckBanner"
      via: "chat.messages topicStuck field (Plan 04.1-04)"
      pattern: "topicStuck"
---

<objective>
Wave 4 — the UI build. Wires every Phase 4.1 worker key into the chat surface UI: composer toggle + confirm dialog (D-01/D-02), inline task card + status pill (D-03), active-tasks-owned rail extension (D-08), archive topic control + archived-topics pill (D-10), diagnostics toggle (D-16), host-stuck banner (D-13). Appends the ~140 CSS lines (UI-SPEC Pattern A–G). Bumps the version. Ends in an operator visual-fidelity drill on Countermoves.

Purpose: This is the surface change Eric sees. Every Phase 4.1 decision has a worker-tier implementation by now (Plans 02–05); this plan exposes them. UI-SPEC defines the exact visual contract; the executor follows it line-by-line.

Output:
- 9 NEW UI files + 5 MODIFIED UI files + 1 CSS modification + package.json bump.
- A re-built `clarity-pack-0.8.0.tgz`.
- An operator-visual-fidelity drill on Countermoves at the end (CLAUDE.md bookended-by-snapshots discipline).

Wave: 4. Depends_on: 02 (chat.createTrueTask), 03 (lifecycle), 04 (chat.messages topicStuck + diagnostics), 05 (chat.topic.archive + chat.taskOwned). All four must land before this plan can wire.

This plan is intentionally LARGER than most (the UI tasks are tightly coupled — one chat surface, all affordances visible together, the visual drill is a single Countermoves session). The Task structure groups by Pattern so a single executor session lands each Pattern as a coherent commit.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-chat-true-task/04.1-CONTEXT.md
@.planning/phases/04.1-chat-true-task/04.1-RESEARCH.md
@.planning/phases/04.1-chat-true-task/04.1-PATTERNS.md
@.planning/phases/04.1-chat-true-task/04.1-UI-SPEC.md
@.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
@.claude/skills/sketch-findings-clarity-pack/SKILL.md
@CLAUDE.md

<interfaces>
<!-- Worker handler keys this UI consumes (all landed in Plans 02-05). -->

```typescript
// usePluginAction keys:
'chat.createTrueTask'(params: {
  companyId; userId; topicIssueId;
  sourceCommentId: string | null;
  title; body;
  assigneeAgentId; employeeName;
}): Promise<{ ok: true; issueId: string; topicIssueId: string } | { error: string }>;

'chat.promote'(params: {
  companyId; userId; topicIssueId; commentId;
  assigneeAgentId; employeeName;     // NEW required per Plan 04.1-02
}): Promise<{ ok: true; issueId: string; topicIssueId: string } | { error: string }>;

'chat.topic.archive'(params: { companyId; userId; topicIssueId; archived: boolean })
  : Promise<{ ok: true; topicIssueId; archived } | { error: string }>;

// usePluginData keys:
'chat.taskOwned'(params: { companyId; userId; topicIssueId })
  : { kind: 'taskOwned'; topicIssueId; tasks: ActiveTask[] } | { error: string };

'chat.messages'(params: { companyId; userId; topicIssueId; includeDiagnostics?: boolean })
  : { kind: 'messages'; topicIssueId; messages; topicStuck: boolean; recoveryOwner: string | null } | { error: string };
```

From src/ui/primitives/ (Phase 2):
```typescript
import { RefChip } from '../../primitives/ref-chip.tsx';
import { usePoll } from '../../primitives/use-poll.ts';
import { useHostNavigation } from '@paperclipai/plugin-sdk/ui/hooks';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the seven new UI components (Patterns A–G) — components-first, no wiring yet</name>
  <files>src/ui/surfaces/chat/true-task/true-task-toggle.tsx, src/ui/surfaces/chat/true-task/true-task-dialog.tsx, src/ui/surfaces/chat/true-task/inline-task-card.tsx, src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx, src/ui/surfaces/chat/active-tasks-owned.tsx, src/ui/surfaces/chat/archive-topic-button.tsx, src/ui/surfaces/chat/archived-topics-pill.tsx, src/ui/surfaces/chat/diagnostics-toggle.tsx, src/ui/surfaces/chat/host-stuck-banner.tsx</files>
  <read_first>
    - .planning/phases/04.1-chat-true-task/04.1-UI-SPEC.md (the visual contract — Patterns A through G; the Copywriting Contract section locks every label; the Component Inventory section lists every file).
    - .planning/phases/04.1-chat-true-task/04.1-PATTERNS.md §"Pattern Assignments" for each NEW UI file (lines 555-887 — the analog file + the component shape spelled out).
    - sketches/paperclip-fix-employee-chat.html (the visual ground truth for every reused class — `.tool-btn` l.677-680, `.task-row` ll. 368-379, `.qa` ll. 388-392, `.btn` / `.btn.ghost` ll. 130-138, `.decision-msg` ll. 294-306, `.auto-refresh` sticky pattern).
    - src/ui/surfaces/chat/composer.tsx (the `.tool-btn` + `.composer-tools` row pattern + the optimistic-send shape `doSend` ll. 72-126 — analog for the dialog action wiring).
    - src/ui/surfaces/chat/message-thread.tsx (the `<article className="msg">` shell + `.auto-refresh` sticky + the `PromoteActions` two-step button at ll. 422-529 — analog for `<ArchiveTopicButton>`).
    - src/ui/surfaces/chat/context-rail.tsx (the `.task-row` and `.qa` blocks the new components plug into).
    - src/ui/surfaces/chat/topic-strip.tsx (the `.topic` button render loop — analog for `<ArchivedTopicsPill>`).
    - src/ui/primitives/ref-chip.tsx (the `BEAAA-NNN` chip used by InlineTaskCard + ActiveTasksOwned rows).
    - .claude/skills/sketch-findings-clarity-pack/SKILL.md (the warm-dark editorial idiom — Geist + Geist Mono + Instrument Serif, scoped under `[data-clarity-surface="chat"]`).
  </read_first>
  <action>
Build each component per its PATTERNS.md spec verbatim. Order them in this commit sequence so each landing is independently testable:

**Component 1: `<TrueTaskToggle>` (Pattern A)** — `src/ui/surfaces/chat/true-task/true-task-toggle.tsx` per PATTERNS.md lines 555-596. Stateful button receiving `{armed, disabled?, onToggle}`. Reuses `.tool-btn` class + adds `.true-task-toggle[data-armed="true"]` for the gold-accent ARMED state. `aria-pressed` reflects `armed`.

**Component 2: `<ChatTaskStatusPill>`** — `src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx` per PATTERNS.md lines 695-714. Thin wrapper mapping ISSUE_STATUSES → existing `.st.todo|.st.review|.st.done|.st.blocked` classes. `aria-label="Status: …"` for accessibility.

**Component 3: `<InlineTaskCard>` (Pattern C)** — `src/ui/surfaces/chat/true-task/inline-task-card.tsx` per PATTERNS.md lines 652-687. Renders inside `<article className="msg">` with `role="group"`. Eyebrow `↗ TASK CREATED` (color `--you` per UI-SPEC §Color). Body title (Geist 500 weight). Assignee row. Meta row with `<RefChip>` + `<ChatTaskStatusPill>` + timestamp. NEVER styled as a `.bubble`.

**Component 4: `<TrueTaskDialog>` (Pattern B)** — `src/ui/surfaces/chat/true-task/true-task-dialog.tsx` per PATTERNS.md lines 600-648 + UI-SPEC Pattern B. Native `<dialog>` element wrapped in a `useEffect` that calls `dialog.showModal()` / `dialog.close()` on open prop change. NO shadcn import. Layout: heading + meta-label "FROM CHAT" eyebrow + TITLE field (controlled input, defaults to first ~80 chars of body, max 200 chars) + ASSIGNEE select (options from `chat.roster` — re-fetched at dialog open; Editor-Agent excluded) + FROM MESSAGE quote-block (read-only `<pre>` or `<blockquote>`-styled div containing the full composer body) + actions row: `Keep editing` (`.btn.ghost`) + `Create task` (`.btn`). The Create button calls `usePluginAction('chat.createTrueTask')` with the inputs from the PATTERNS.md spec lines 622-645. Error handling reuses the existing `resultError` pattern from `message-thread.tsx:449-455`. Loading state: button label flips to `Creating…`, disabled.

**Component 5: `<ActiveTasksOwned>` (Pattern D)** — `src/ui/surfaces/chat/active-tasks-owned.tsx` per PATTERNS.md lines 718-769. usePluginData + usePoll pairing at the 15s cadence the existing MessageThread uses, with visibility-pause. Renders one `.task-row` per task with `<RefChip>` + title + `<ChatTaskStatusPill>`. Empty state: `<p className="active-tasks-owned-empty">`.

**Component 6: `<ArchiveTopicButton>` (Pattern E)** — `src/ui/surfaces/chat/archive-topic-button.tsx` per PATTERNS.md lines 773-802 + UI-SPEC Pattern E. Two-state in-row button: OFF state = `.qa` button with label `⌗ Archive this topic`; ARMED (confirming) state = an inline pill with YES (`.btn`) + KEEP TOPIC (`.btn.ghost`). On YES → `usePluginAction('chat.topic.archive')` with `archived:true`; on success → 2s `✓ Topic archived` feedback → `onArchived()` callback dismisses the row. Reuses the `PromoteActions` busy + feedback pattern.

**Component 7: `<ArchivedTopicsPill>` (Pattern E tail)** — `src/ui/surfaces/chat/archived-topics-pill.tsx` per PATTERNS.md lines 805-826. Renders nothing when archivedCount===0. Otherwise renders a `.topic.archived-topics-pill` button labeled `+{N} archived` (with plural handling). `aria-pressed` for the expanded state.

**Component 8: `<DiagnosticsToggle>` (Pattern F)** — `src/ui/surfaces/chat/diagnostics-toggle.tsx` per PATTERNS.md lines 830-851. `.btn.ghost` (OFF) / `.btn diagnostics-toggle[data-armed="true"]` (ARMED) toggle. Label `⏿ Diagnostics` / `⏿ Diagnostics ON`. `aria-pressed`.

**Component 9: `<HostStuckBanner>` (Pattern G)** — `src/ui/surfaces/chat/host-stuck-banner.tsx` per PATTERNS.md lines 858-884 + UI-SPEC Pattern G. `role="alert"`. Sticky inside `.messages` (CSS handles position). Heading `⚠ TOPIC STUCK HOST-SIDE` (Geist Mono uppercase tracked). Two body lines per the locked copywriting. ONE action: `Open in classic Paperclip` via `useHostNavigation().linkProps('/issues/<topicIssueId>')`. NO retry button. NO auto-recover.

Each component is a pure presentational + hook-driven React 19 component. NO React.lazy. NO Suspense boundaries. NO shadcn import. NO third-party imports beyond the SDK + React.

Commit per component (or grouped 2-3 at a time): `feat(04.1-06): build <ComponentName>`. Suite size unchanged (no test files in this Task — UI components are exercised by the visual drill + the existing message-thread/composer test suite is updated in Task 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - All 9 component files exist; each exports its named component per the must_haves frontmatter.
    - `grep -E "from '@paperclipai/plugin-sdk/ui/hooks'" src/ui/surfaces/chat/true-task/*.tsx src/ui/surfaces/chat/active-tasks-owned.tsx src/ui/surfaces/chat/archive-topic-button.tsx src/ui/surfaces/chat/host-stuck-banner.tsx | wc -l` returns ≥ 4 (the four components calling usePluginAction / usePluginData / useHostNavigation).
    - `grep -rE "from '@radix-ui|from 'shadcn|import .* DialogPrimitive" src/ui/surfaces/chat/true-task/` returns ZERO hits (the FORCED stack constraint — no radix/shadcn primitives in the bundle).
    - `grep -nE "dialog\.showModal\(\)" src/ui/surfaces/chat/true-task/true-task-dialog.tsx` returns ONE hit (the native dialog API).
    - `grep -nE "dangerouslySetInnerHTML" src/ui/surfaces/chat/` returns ZERO hits (XSS posture — UI-SPEC §Accessibility).
    - `npx tsc --noEmit` exits clean.
    - The esbuild UI build (`node scripts/build-ui.mjs` or `pnpm build:ui`) exits clean.
  </acceptance_criteria>
  <done>Nine new presentational + hook-driven components exist, build clean, and are ready for Task 2 to mount.</done>
</task>

<task type="auto">
  <name>Task 2: Mount the new components — modify composer, message-thread, context-rail, topic-strip, index; append the chat.css block; bump version</name>
  <files>src/ui/surfaces/chat/composer.tsx, src/ui/surfaces/chat/message-thread.tsx, src/ui/surfaces/chat/context-rail.tsx, src/ui/surfaces/chat/topic-strip.tsx, src/ui/surfaces/chat/index.tsx, src/ui/styles/chat.css, package.json</files>
  <read_first>
    - .planning/phases/04.1-chat-true-task/04.1-PATTERNS.md §"src/ui/surfaces/chat/composer.tsx (MODIFIED — wire toggle into tools row)" (lines 890-955), §"message-thread.tsx (MODIFIED)" (lines 958-976), §"context-rail.tsx (MODIFIED)" (lines 980-1013), §"topic-strip.tsx (MODIFIED)" (lines 1017-1036), §"index.tsx (MODIFIED)" (lines 1040-1055), §"chat.css (MODIFIED)" (lines 1059-1091).
    - .planning/phases/04.1-chat-true-task/04.1-UI-SPEC.md §"CSS additions" (the 26 class names with their analog rules), §"Interaction Flow" (the end-to-end execution flow per affordance).
    - src/ui/surfaces/chat/composer.tsx (the WHOLE file — Task 2 replaces the stub `↗ New task` button at the composer-tools row with `<TrueTaskToggle>`, threads `taskArmed` state, opens `<TrueTaskDialog>` on Send when armed, updates placeholder + send-button label).
    - src/ui/surfaces/chat/message-thread.tsx (lines 283-349 — the render block; lines 449-455 the resultError pattern; lines 422-529 the PromoteActions to wire chat.promote's new assigneeAgentId + employeeName params through).
    - src/ui/surfaces/chat/context-rail.tsx (lines 51-54 the stub to replace; lines 68-77 the Quick actions block to insert into).
    - src/ui/surfaces/chat/topic-strip.tsx (lines 64-66 the topics extraction; lines 86-99 the render loop).
    - src/ui/surfaces/chat/index.tsx (lines 217-226 the head-actions block).
    - src/ui/styles/chat.css (the whole file — Task 2 APPENDS a new section at the bottom; never modifies prior rules).
    - package.json (line 3 — version bump from 0.7.8 to 0.8.0).
  </read_first>
  <action>
**Composer DELTA** (PATTERNS.md 890-955):
- Imports: add `import { TrueTaskToggle } from './true-task/true-task-toggle.tsx';` and `import { TrueTaskDialog } from './true-task/true-task-dialog.tsx';`.
- State: add `const [taskArmed, setTaskArmed] = React.useState(false);` and `const [dialogOpen, setDialogOpen] = React.useState(false);` after the existing optimistic-send state.
- Props: add `topicTitle`, `assigneeAgentId`, `employeeName` to the Composer prop signature (they are threaded from `index.tsx`).
- `handleSend`: when `taskArmed`, open the dialog instead of calling chat.send — per PATTERNS.md 906-918.
- Tools-row: REPLACE the stub `↗ New task` button at lines 181-198 with `<TrueTaskToggle armed={taskArmed} onToggle={() => setTaskArmed((a) => !a)} />`. Keep the `📎 Attach` button + `.attach-unavailable` span unchanged.
- Send-button label: when armed, label is `Open task form` instead of `Send`.
- Placeholder: when armed, `Task for {employeeName}… Enter to open the task form`.
- Mount the dialog at the JSX tree's end (next to the closing root div) — pass `defaultTitle`, `body`, `topicIssueId`, `topicTitle`, `assigneeAgentId`, `employeeName`, `companyId`, `userId`, `onClose` (closes dialog), `onSuccess` (closes dialog + disarms toggle + clears composer body).

**MessageThread DELTA** (PATTERNS.md 958-976):
- Imports: add `import { InlineTaskCard } from './true-task/inline-task-card.tsx';` and `import { HostStuckBanner } from './host-stuck-banner.tsx';`.
- Result-type: extend the existing MessagesResult type at lines 58-61 with `topicStuck?: boolean; recoveryOwner?: string | null`.
- Read the two new fields from the chat.messages response into local variables `topicStuck` and `recoveryOwner`.
- Inside the `.messages` div, BELOW the existing `.auto-refresh` sticky strip (line ~299): render `{topicStuck ? <HostStuckBanner topicIssueId={topicIssueId} recoveryOwner={recoveryOwner ?? null} /> : null}`.
- Inside the `ordered.map(...)` loop: when a comment's body starts with `'Task created — '` (the EXACT marker prefix from Plan 04.1-02's helper), render `<InlineTaskCard>` instead of `<PersistedMessage>`. Parse the issueId + employeeName from the marker body via a regex `/Task created — ([^,]+), assigned to ([^.]+)\./` — the values are also threaded via the InlineTaskCard's props if a backing task is found in `chat.taskOwned` (preferred — the live status pill needs the real task row). If no backing task is found, fall back to a static card with `· — ·` status (the optimistic post-create state before chat.taskOwned has caught up).
- When `includeDiagnostics` is true (threaded from `index.tsx`), runtime-noise comments are now in the messages array (Plan 04.1-04). Render them in a separate `.runtime-noise-comment` block (NOT a bubble) with the `runtime-noise-comment-header` showing `SYSTEM · {comment.authorType} · {comment.presentation?.kind ?? 'message'}`.
- PromoteActions DELTA: thread the new required `assigneeAgentId` + `employeeName` params through to `chat.promote` calls (lines 457-481). These come from the active topic + roster (already in scope in MessageThread or threaded from index.tsx).

**ContextRail DELTA** (PATTERNS.md 980-1013):
- Imports: add `<ActiveTasksOwned>` and `<ArchiveTopicButton>`.
- Props extend: add `companyId`, `userId`, `onArchived` (callback to refresh topic list after archive).
- REPLACE lines 51-54 stub with the `<ActiveTasksOwned>` conditional render.
- INSERT `<ArchiveTopicButton>` as the FIRST `.qa` row inside `.quick` block (line 69) — before the existing `⌕ Search` and `⏸ Pause heartbeat` rows.

**TopicStrip DELTA** (PATTERNS.md 1017-1036):
- State: add `const [showArchived, setShowArchived] = React.useState(false);` at the top of the component.
- Filter: derive `visible = showArchived ? allTopics : allTopics.filter((t) => !t.archived);` and `archivedCount = allTopics.filter((t) => t.archived).length;`.
- Render the existing topic loop AGAINST `visible` instead of `allTopics`.
- Mount `<ArchivedTopicsPill archivedCount={archivedCount} expanded={showArchived} onToggle={() => setShowArchived((s) => !s)} />` at the end of the strip after the loop closes.
- When `showArchived` is true, render archived topics with `opacity: 0.6` and an inline `Unarchive` hover action (calls `chat.topic.archive` with `archived:false`).

**Index (root) DELTA** (PATTERNS.md 1040-1055):
- Imports: add `<DiagnosticsToggle>`.
- State: add `const [diagnostics, setDiagnostics] = React.useState(false);`.
- Insert `<DiagnosticsToggle armed={diagnostics} onToggle={() => setDiagnostics((a) => !a)} />` in the `<div className="head-actions">` at line 217.
- Thread `diagnostics` down through `<MessageThread>` props so the `chat.messages` poll passes `includeDiagnostics: diagnostics`.

**chat.css DELTA** (PATTERNS.md 1059-1091 + UI-SPEC §"CSS additions"):
APPEND at the bottom of `src/ui/styles/chat.css` — under a new heading comment `/* ---- Phase 4.1 — chat → true task ---- */` — the 26 class definitions per UI-SPEC's CSS additions table, mapped to the analog rules in PATTERNS.md's class-by-analog table. Every selector scoped under `[data-clarity-surface="chat"]`. Estimated ~140 lines, additive only. Do NOT mutate existing rules.

**package.json DELTA**:
- Bump version from `0.7.8` to `0.8.0`.
- (No new deps — UI-SPEC Registry Safety: no shadcn, no radix, no new packages.)

**Rebuild + pack**:
- `pnpm run build` (or whatever the standard build script is — confirm by reading the `scripts` block in package.json).
- `pnpm pack` → `clarity-pack-0.8.0.tgz`.
- Record the sha256 of the tarball in the SUMMARY.

Commit sequence:
- `feat(04.1-06): wire TrueTaskToggle + dialog into composer`
- `feat(04.1-06): InlineTaskCard + HostStuckBanner + PromoteActions assignee params in MessageThread`
- `feat(04.1-06): ActiveTasksOwned + ArchiveTopicButton in ContextRail`
- `feat(04.1-06): TopicStrip archive filter + +N archived pill`
- `feat(04.1-06): DiagnosticsToggle in head-actions; thread includeDiagnostics`
- `feat(04.1-06): chat.css Phase 4.1 additions (26 classes, additive)`
- `chore(04.1-06): version bump 0.7.8 → 0.8.0; pack clarity-pack-0.8.0.tgz`
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/build-ui.mjs && node -e "const p=require('./package.json'); if(p.version!=='0.8.0'){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - The 5 MODIFIED UI files (composer, message-thread, context-rail, topic-strip, index) all `grep`-cite the corresponding new component imports.
    - `grep -c "true-task-toggle\|true-task-dialog\|inline-task-card\|active-tasks-owned\|archive-topic-button\|archived-topics-pill\|diagnostics-toggle\|host-stuck-banner" src/ui/surfaces/chat/index.tsx src/ui/surfaces/chat/composer.tsx src/ui/surfaces/chat/message-thread.tsx src/ui/surfaces/chat/context-rail.tsx src/ui/surfaces/chat/topic-strip.tsx` returns ≥ 8 hits (each new component referenced where it should mount).
    - `grep -c "Phase 4.1 — chat → true task" src/ui/styles/chat.css` returns 1 hit.
    - `grep -E "^\.true-task-toggle\b|^\.true-task-dialog\b|^\.inline-task-card\b|^\.active-tasks-owned-empty\b|^\.archive-topic-button\b|^\.archived-topics-pill\b|^\.diagnostics-toggle\b|^\.runtime-noise-comment\b|^\.host-stuck-banner\b" src/ui/styles/chat.css | wc -l` returns ≥ 9 (every NEW selector exists).
    - package.json version === `0.8.0`.
    - `npx tsc --noEmit` exits clean.
    - UI build + worker build + manifest build all exit clean.
    - `pnpm pack` produces `clarity-pack-0.8.0.tgz` and the file exists.
    - All existing chat-suite unit tests still pass (no UI changes broke a worker test).
  </acceptance_criteria>
  <done>The UI surface is built; the 0.8.0 tarball is ready for the Countermoves drill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Eric runs the visual-fidelity drill on Countermoves — multi-turn chat + true-task + archive + diagnostics + host-stuck banner</name>
  <what-built>
    The seven new chat-surface affordances of Phase 4.1: composer Send-as-task toggle, native-dialog confirm modal, in-thread task card, live active-tasks-owned rail, archive topic button + +N archived pill, diagnostics toggle, host-stuck banner. Packed as `clarity-pack-0.8.0.tgz` ready to install over `0.7.8` on Countermoves.
  </what-built>
  <how-to-verify>
On the Countermoves Hostinger VPS (SSH per the recorded pattern):

1. **Bookend snapshot (CLAUDE.md):**
   ```bash
   cd ~/clarity-pack
   node scripts/safety/cli.mjs snapshot --db-url $(cat /etc/paperclip/db.env | grep DB_URL | cut -d= -f2-)
   node scripts/safety/cli.mjs gate
   ```
   Confirm both PASS.

2. **Gated upgrade 0.7.8 → 0.8.0** (per the Phase 4 closure drill pattern):
   ```bash
   # ssh-copy the 0.8.0 tarball to ~ on the VPS first
   cd ~/paperclip
   pnpm paperclipai plugin install ~/clarity-pack-0.8.0.tgz
   ```
   Confirm the install reports success + the migrations check shows no new 0007 migration (unless Plan 04.1-05 added one per the spike fallback).

3. **Smoke (Plan 02-01 smoke spike — chat surface still loads):**
   - Navigate to `https://countermoves.gl3group.com/<COU>/chat` (the per-company route).
   - Confirm the four-region shell renders (roster left, topic strip, message thread, context rail right).
   - Confirm the existing Phase 4 affordances still work (send a normal message; receive a reply).

4. **D-01 / D-02 / D-03 — Send as task happy path:**
   - In an existing chat topic, click `↗ Send as task` toggle in the composer.
   - Verify the toggle visually arms (gold border + dot).
   - Verify placeholder reads `Task for {employee}…`.
   - Type a real task body (e.g. `Lock the broker comm % at 12% per Bedrock-Re negotiated rate.`).
   - Hit Enter.
   - Verify the confirm dialog opens, pre-filled.
   - Verify the title field is editable; tweak it.
   - Verify the FROM MESSAGE block shows the full body.
   - Click `Create task`.
   - Verify the dialog closes, the toggle disarms, the composer clears.
   - Verify an inline task card renders in the thread (eyebrow `TASK CREATED`, BEAAA-NNN identifier, status pill).
   - Verify the new task appears in the Active tasks owned rail with a status pill.
   - Verify the new task appears in `https://countermoves.gl3group.com/<COU>/issues` — the normal Issues list — assigned to the chatted employee (NOT the Editor-Agent), at top-level (NOT nested under the chat-topic plumbing tree). **This is the bug fix the followup demanded — it MUST hold.**

5. **D-04 — Agent-message Promote upgrade:**
   - On the agent's reply message, click `↗ Promote to task`.
   - Verify the same path runs (since the rewrite delegates to createTrueTask): a new top-level assigned issue, marker comment, inline task card.
   - Verify the new task appears in the Issues list assigned to the chatted employee.

6. **D-12 — Multi-turn re-wake (the load-bearing fix):**
   - In the same topic, send a second message (a normal message, not a task).
   - Wait for the agent's reply.
   - Send a THIRD message.
   - Wait for the agent's reply.
   - Verify the agent REPLIES TO EACH — not just the first. **This is the CHT-1116 bug the followup identifies — it MUST be gone.**

7. **D-09 / D-10 — Archive a topic:**
   - In the context rail's Quick actions, click `⌗ Archive this topic`.
   - Verify the row mutates in place to `Archive {topic}? · YES · KEEP TOPIC`.
   - Click YES.
   - Verify the topic disappears from the topic strip.
   - Verify a `+1 archived` muted pill appears at the right end of the strip.
   - Click `+1 archived`. Verify the strip expands; archived topics render at opacity 0.6.
   - In classic Paperclip, confirm the host topic issue's status remains `in_progress` (NOT done) — D-10's load-bearing invariant.

8. **D-16 — Diagnostics toggle:**
   - In the thread header, click `⏿ Diagnostics`.
   - Verify the toggle arms (`⏿ Diagnostics ON`).
   - Verify any previously-filtered runtime-noise comments now appear inline in the thread, dashed-bordered + Geist Mono + dim — NOT bubbles.
   - Click again to disarm; verify the runtime noise re-hides.

9. **D-13 — Host-stuck banner** (best-effort provoke):
   - If a topic was previously provoked into recovery state during the 04.1-01 spike (or if a deliberately-stuck topic exists from the followup's CMO drill), select it.
   - Verify the `⚠ TOPIC STUCK HOST-SIDE` banner renders sticky below the live indicator with the locked copy and an `Open in classic Paperclip` link.
   - Verify NO retry button is shown.
   - (If no provokable stuck topic exists, this step is recorded as "not exercised" — the unit tests on Plan 04.1-04's chat.messages already prove the banner-trigger plumbing; the live drill confirms only the visual.)

10. **Visual fidelity vs sketch:**
    - Compare each new affordance against `sketches/paperclip-fix-employee-chat.html` (where it has an analog) + UI-SPEC's Pattern A–G renders. Note any deviation in the operator drill log.

11. **Type ONE of:**
    - "approved — phase 4.1 closure unblocked" — every step PASSED.
    - "approved-with-followups: <list>" — visual or functional gaps that don't block Phase 4.1 closure but should be tracked for a polish plan.
    - "DO NOT PASS — <root cause>" — at least one of steps 4–9 failed in a way that requires a gap-closure plan (route to Plan 04.1-08 — a name reserved for gap-closure).
  </how-to-verify>
  <resume-signal>Type "approved — phase 4.1 closure unblocked", "approved-with-followups: ...", or "DO NOT PASS — ..."</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI composer (operator-typed body + title) → chat.createTrueTask | Untrusted typed text crosses the bridge. The dialog's TITLE input is character-limited (200) on the client; the worker handler's reqStr enforces non-empty. The body is also typed by the operator. Both surface in the issue + marker comment as React text. |
| UI dialog ASSIGNEE select → chat.createTrueTask | The select's options come from chat.roster (already opt-in-gated + company-scoped). The operator can reassign at-create-time (D-06). Host gate is the final enforcement. |
| UI archive button → chat.topic.archive | One boolean flip on a plugin-namespace table. Opt-in + company-scope guards. |
| UI diagnostics toggle → chat.messages includeDiagnostics:true | Reveals filtered comments to the operator on opt-in. RESEARCH §"Pattern 2" + Plan 04.1-04 verified the discriminator. Filtered comments are not destroyed; the toggle only shows them. |
| UI HostStuckBanner → useHostNavigation linkProps | A link to a known internal Paperclip route. The host's navigation gate validates the route. No raw fetch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06-01 | T (Tampering) | Operator-supplied title in `<TrueTaskDialog>` injected as raw HTML into the issue | mitigate | The dialog input is a controlled React `<input>` element; React text rendering downstream prevents HTML injection. The 200-char limit is a UX bound, not a security bound. Host stores as text. |
| T-04.1-06-02 | T (Tampering) | `<InlineTaskCard>` parses the marker comment body with a regex — a malformed/forged comment could mis-render | mitigate | The marker body is plugin-authored (Plan 04.1-02's createTrueTask). The regex match is permissive (an agent posting a similar string just renders as an inline card with `· — ·` status — no real BEAAA-NNN link if the chat.taskOwned lookup yields no match). The InlineTaskCard NEVER `dangerouslySetInnerHTML` the parsed values (XSS posture). |
| T-04.1-06-03 | S (Spoofing) | An agent posts a comment matching `Task created — BEAAA-X, assigned to FAKE.` to fake a task creation in the thread | mitigate | The Active-tasks-owned rail is sourced from `chat.taskOwned` (host-validated issues, not the comment body) — a fake marker has no backing task and shows as a degraded card with no working RefChip link. Acceptance-criteria already covers this in Plan 04.1-04 §T-04.1-04-01. |
| T-04.1-06-04 | I (Info disclosure) | The diagnostics toggle exposes system comments | accept | By operator opt-in. Same-tenant trust model. Plan 04.1-04 §T-04.1-04-02 documented. |
| T-04.1-06-05 | I (Info disclosure) | The HostStuckBanner exposes `recoveryOwner` which may be a real operator name | mitigate | The field is null unless the host populates it; if present, it is rendered as React text. The chat surface is single-tenant by host design. |
| T-04.1-06-06 | E (Elevation of privilege) | UI raw-fetch bypass of usePluginAction (calling a worker key without the bridge) | mitigate | ESLint rule `no-raw-fetch-in-ui` (SCAF-02, Phase 2) catches `fetch(`/`XMLHttpRequest` in `src/ui/`. Every action call in this plan's UI files goes through `usePluginAction` / `usePluginData`. Confirmed by `grep -nE "\\bfetch\\(" src/ui/surfaces/chat/true-task/*.tsx src/ui/surfaces/chat/*.tsx` returning 0 hits in the new code. |
| T-04.1-06-07 | T (Tampering) | A click on the archive button accidentally maps to a different topic's id (state-race) | mitigate | The button is rendered with `topic.issueId` from the active topic in scope; React props bind the value at render time. The two-step confirm makes accidental archive structurally impossible (operator confirms YES). |

ASVS L1 baseline. No high-severity threat is `accept`-disposed without compensating controls.
</threat_model>

<verification>
Plan 04.1-06 succeeds when:
- The 9 new component files + 5 modified UI files + chat.css + package.json all reflect the spec.
- `npx tsc --noEmit` + UI build + worker build + manifest build + `pnpm pack` all clean.
- The operator drill on Countermoves passes — multi-turn chat works, true-task creates an assigned top-level findable issue, archive hides without status mutation, diagnostics reveals + hides runtime noise, host-stuck banner renders when provoked, no raw fetches in the UI.
- The Phase 4 followup's two intertwined problems are visibly gone.
</verification>

<success_criteria>
- CTT-01 (operator true-task) operationally observable on Countermoves.
- CTT-02 (agent-message Promote unified) observable.
- CTT-04 (runtime noise filtered) observable + diagnostics path exercised.
- CTT-06 (host-stuck banner) renders when provoked.
- CTT-07 (archive) observable.
- CTT-08 (active tasks rail) observable with live status pills.
- Suite unchanged (UI components are exercised by the drill; the worker tests covered the data paths).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-chat-true-task/04.1-06-SUMMARY.md` per the GSD summary template — recording commit range, tarball sha256, drill verdict, any approved-with-followups list, version 0.8.0 reference, and CTT-01/02/04/06/07/08 progress.
</output>
</content>
