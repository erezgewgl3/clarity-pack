---
phase: 04.1-chat-true-task
plan: 07
type: execute
wave: 5
depends_on: [06]
files_modified:
  - scripts/coexistence-checks/09-true-task.mjs
  - scripts/coexistence-checks/run-all.mjs
  - test/phases/04.1-traceability.test.mjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md
autonomous: false
requirements: [CTT-01, CTT-02, CTT-03, CTT-04, CTT-05, CTT-06, CTT-07, CTT-08]
user_setup: []

must_haves:
  truths:
    - "A new CI coexistence check 09-true-task.mjs (mirrors 08-chat-disable.mjs) asserts: (a) the new chat-task / chat-topic-task tables (if any) are NEVER DROPped by any migrations/*.sql; (b) the worker code NEVER calls ctx.issues.delete or ctx.issues.deleteComment in any Phase 4.1 code path; (c) the chat-topic-archive handler does NOT contain `ctx.issues.update` (the D-10 invariant — disabling the plugin while a topic is archived must NOT have side-effected the host issue's status); (d) the manifest declares NO destructive uninstall hook. Added to scripts/coexistence-checks/run-all.mjs so it runs in CI on every PR."
    - "A new traceability test test/phases/04.1-traceability.test.mjs (mirrors 04-traceability.test.mjs) asserts: every CTT-01..CTT-08 row exists in .planning/REQUIREMENTS.md, is marked Implemented, and carries a 04.1-NN plan reference. RED→GREEN — fails before Task 1's REQUIREMENTS.md edit, passes after."
    - "REQUIREMENTS.md CTT-01..CTT-08 rows are flipped from Pending → Implemented (Plan 04.1-NN — <one-liner>) per the Phase 4 closure pattern."
    - "ROADMAP.md Phase 4.1 entry is marked [x] COMPLETE with the closing date and a one-paragraph summary referencing the operator drill verdict, the suite-size delta, and any flag reconciliations (FLAG-1: host-blocked residual reconciled per CHAT-04 precedent if applicable; FLAG-2: situation-snapshot.ts confirmed read-only / no inflation)."
    - "STATE.md is updated to reflect Phase 4.1 closed, suite count refreshed, the next phase (Phase 5) unblocked."
    - "A new verification doc 04.1-VERIFICATION.md captures the operator drill verdict + per-CTT-NN proof + flag reconciliations + the locked findings from 04.1-01 referenced + the version-bump pin (0.8.0) + tarball sha256."
    - "Operator closure drill on Countermoves PASSES — the coexistence guarantee holds (disable plugin → all chat messages survive as ordinary issue_comments; chat-topic issues remain visible; archived flag preserved in plugin namespace; true-task issues survive as ordinary Paperclip issues with their assignees intact)."
  artifacts:
    - path: "scripts/coexistence-checks/09-true-task.mjs"
      provides: "Static CI check: Phase 4.1 surfaces preserve data on disable."
      contains: "ctx.issues.delete"
    - path: "scripts/coexistence-checks/run-all.mjs"
      provides: "EXTENDED to include 09-true-task.mjs."
      contains: "09-true-task"
    - path: "test/phases/04.1-traceability.test.mjs"
      provides: "Traceability gate — CTT-01..CTT-08 must be Implemented + cite a 04.1-NN plan."
      contains: "CTT-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CTT-01..CTT-08 rows flipped from Pending to Implemented."
      contains: "CTT-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4.1 entry marked complete."
      contains: "Phase 4.1"
    - path: ".planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md"
      provides: "Closure verification doc — drill verdict + per-CTT proof + flag reconciliations."
      contains: "Phase 4.1"
  key_links:
    - from: "scripts/coexistence-checks/run-all.mjs"
      to: "09-true-task.mjs"
      via: "added to CHECKS array"
      pattern: "09-true-task"
    - from: "test/phases/04.1-traceability.test.mjs"
      to: "REQUIREMENTS.md CTT-01..CTT-08 rows"
      via: "regex on traceability table"
      pattern: "CTT-0[1-8]"
---

<objective>
Wave 5 — phase closure. Extend the CI coexistence checklist with a Phase 4.1 surface check, gate the CTT-01..CTT-08 traceability with a node --test, flip every CTT row to Implemented + commit, mark ROADMAP/STATE complete, and run the operator closure drill on Countermoves.

Purpose: This is the same pattern Phase 4 used to close (Plan 04-06): one CI check + one traceability test + REQUIREMENTS/ROADMAP/STATE updates + one operator drill. Phase 4 cost two and a half closure plans because the verifier was retrofitted; Phase 4.1 does it inline.

Output:
- `scripts/coexistence-checks/09-true-task.mjs` — NEW (mirrors 08-chat-disable.mjs).
- `scripts/coexistence-checks/run-all.mjs` — EXTENDED with 09.
- `test/phases/04.1-traceability.test.mjs` — NEW (mirrors 04-traceability.test.mjs).
- `.planning/REQUIREMENTS.md` — CTT-01..CTT-08 flipped to Implemented.
- `.planning/ROADMAP.md` — Phase 4.1 marked [x], Plans listed, closure paragraph.
- `.planning/STATE.md` — refreshed.
- `.planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md` — NEW.

Wave: 5. Depends_on: 06 (the operator drill must have passed before this plan finalizes; if 06 routed to a gap-closure 04.1-08, that plan must close first and feed back here).
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-chat-true-task/04.1-CONTEXT.md
@.planning/phases/04.1-chat-true-task/04.1-RESEARCH.md
@.planning/phases/04.1-chat-true-task/04.1-PATTERNS.md
@.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
@.planning/phases/04.1-chat-true-task/04.1-06-SUMMARY.md
@.planning/phases/04-employee-chat/04-VERIFICATION.md
@scripts/coexistence-checks/08-chat-disable.mjs
@scripts/coexistence-checks/run-all.mjs
@test/phases/04-traceability.test.mjs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 09-true-task.mjs coexistence check + extend run-all.mjs</name>
  <files>scripts/coexistence-checks/09-true-task.mjs, scripts/coexistence-checks/run-all.mjs</files>
  <read_first>
    - scripts/coexistence-checks/08-chat-disable.mjs (the WHOLE file — the verbatim analog for shape, regex, stripSqlComments helper, walk helper).
    - scripts/coexistence-checks/03-disable-preserves-data.mjs (the upstream generic "no DROP / DELETE on plugin tables" check).
    - scripts/coexistence-checks/run-all.mjs (the CHECKS array — Task adds 09 to it).
    - src/worker/handlers/chat-topic-archive.ts (Plan 04.1-05 — the file 09-true-task.mjs asserts contains zero ctx.issues.update calls — D-10 invariant).
    - migrations/*.sql (every migration — the check asserts NONE of them DROPs the plugin-namespace tables OR `public.issue_comments` OR adds DROP/DELETE/drop-column to existing rows).
    - src/manifest.ts (the check asserts NO destructive uninstall hook is declared — the existing Phase 4 check already verifies this; Plan 4.1 confirms it remains the case).
  </read_first>
  <action>
**Create `scripts/coexistence-checks/09-true-task.mjs`** — copy verbatim from `08-chat-disable.mjs` and adapt. Six assertions:

1. **NO-DROP of plugin chat tables in migrations**: Same as 08 — `migrations/*.sql` must not contain `DROP TABLE` / `DROP SCHEMA` / `DROP COLUMN` against `plugin_clarity_pack_cdd6bda4bd.chat_topics` (and `chat_messages`, `chat_employee_parents`, AND `chat_topic_tasks` IF Plan 04.1-05 added the 0007 migration per the spike fallback).
2. **NO-DROP of public.issue_comments**: Same as 08.
3. **NO-DELETE in worker code**: Walk `src/worker/` and assert no `ctx.issues.delete` / `ctx.issues.deleteComment` / `ctx.db.execute('DELETE FROM …chat_topics|chat_messages|chat_topic_tasks')`. Specifically for Phase 4.1 surfaces — the new files at `src/worker/handlers/chat-true-task.ts`, `src/worker/handlers/chat-topic-archive.ts`, `src/worker/handlers/chat-active-tasks.ts`, `src/worker/chat/true-task.ts`, `src/worker/chat/topic-watchdog.ts`, `src/worker/chat/comment-classify.ts` MUST not contain any DELETE / DROP statements.
4. **D-10 INVARIANT — archive handler does not touch host status**: assert `src/worker/handlers/chat-topic-archive.ts` contains ZERO `ctx.issues.update` calls AND ZERO `'done' | 'cancelled' | 'blocked'` literal string status writes. This is the load-bearing Phase 4.1 invariant — archiving must NEVER mutate host issue status.
5. **NO destructive uninstall hook in manifest**: assert `src/manifest.ts` does not declare an uninstall hook that calls DROP / DELETE / TRUNCATE.
6. **PHASE-4.1 SOURCE FILES SURVIVE**: assert the six Phase 4.1 worker files (the list in #3) exist (defensive — guarantees the script is checking the current generation of files).

Print `OK 09-true-task: <N> assertions passed` on success; `COEXIST-09 violation: <reason>` on failure with exit code 1.

**Extend `scripts/coexistence-checks/run-all.mjs`** — add `'09-true-task.mjs'` to the CHECKS array, in order. Confirm by reading the existing array structure first; the addition is one new array entry.

Run `node scripts/coexistence-checks/09-true-task.mjs` — exits 0 with `OK`. Run `node scripts/coexistence-checks/run-all.mjs` — all checks (including 09) pass.

Commit: `feat(04.1-07): COEXIST-09 true-task coexistence check (CI gate)`.
  </action>
  <verify>
    <automated>node scripts/coexistence-checks/09-true-task.mjs && node scripts/coexistence-checks/run-all.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/coexistence-checks/09-true-task.mjs` exists and exits 0 (under the current source tree).
    - `grep -c "09-true-task" scripts/coexistence-checks/run-all.mjs` returns 1 hit (the CHECKS-array addition).
    - `grep -nE "ctx\.issues\.update" src/worker/handlers/chat-topic-archive.ts` returns 0 hits (the D-10 invariant — the check would fail otherwise).
    - run-all.mjs exits 0.
  </acceptance_criteria>
  <done>The Phase 4.1 coexistence gate runs in CI on every PR; the D-10 invariant is automatically enforced.</done>
</task>

<task type="auto">
  <name>Task 2: 04.1-traceability.test.mjs RED → GREEN + flip REQUIREMENTS.md CTT-01..CTT-08 to Implemented</name>
  <files>test/phases/04.1-traceability.test.mjs, .planning/REQUIREMENTS.md</files>
  <read_first>
    - test/phases/04-traceability.test.mjs (the WHOLE file — the verbatim analog: the `traceabilityRow` regex helper, the per-id loop, the three assertions (row-exists / Implemented / plan-cite).
    - .planning/REQUIREMENTS.md (the CTT-01..CTT-08 block landed in Plan 04.1-01 — Task 2 here flips them from Pending to Implemented).
    - .planning/phases/04.1-chat-true-task/04.1-06-SUMMARY.md (the per-CTT-NN delivering-plan citation — confirms which Plan 04.1-NN closed which requirement).
  </read_first>
  <action>
**Create `test/phases/04.1-traceability.test.mjs`** — copy verbatim from `test/phases/04-traceability.test.mjs` and adapt:
- Change `CHAT_IDS = Array.from({ length: 11 }, (_, i) => 'CHAT-' + String(i+1).padStart(2,'0'))` → `CTT_IDS = Array.from({ length: 8 }, (_, i) => 'CTT-' + String(i+1).padStart(2,'0'))`.
- Change the regex's expected phase field from `Phase 4` to `Phase 4.1`.
- Change the plan-cite regex from `04-\d{2}` to `04\.1-\d{2}`.
- Change every reference to CHAT-NN to CTT-NN in test names + error messages.

The three assertions stay structurally identical: (a) every CTT-01..CTT-08 row exists; (b) every row is marked Implemented; (c) every row cites a `04.1-NN` plan reference.

**TDD RED step:** Run `node --test test/phases/04.1-traceability.test.mjs` — MUST fail (the CTT rows landed in Plan 04.1-01 are still `Pending`). Commit: `test(04.1-07): RED — 04.1 traceability test (CTT-01..CTT-08)`.

**TDD GREEN step:** EDIT `.planning/REQUIREMENTS.md`. For each of CTT-01..CTT-08, flip the row from:
```
| CTT-01 | Phase 4.1 | Pending |
```
to:
```
| CTT-01 | Phase 4.1 | Implemented (Plan 04.1-02 — chat.createTrueTask handler + UI in Plan 04.1-06) |
```

The plan citation for each row is sourced from the per-plan SUMMARIES — typical mapping:
- CTT-01 → Plan 04.1-02 (worker) + Plan 04.1-06 (UI).
- CTT-02 → Plan 04.1-02 (chat.promote rewrite) + Plan 04.1-06 (UI thread).
- CTT-03 → Plan 04.1-03 (lifecycle helpers + chat.send wake nudge).
- CTT-04 → Plan 04.1-04 (chat.messages classify filter) + Plan 04.1-06 (diagnostics toggle UI).
- CTT-05 → Plan 04.1-03 (ensureTopicWakeable watchdog flip + topic D-11 description).
- CTT-06 → Plan 04.1-04 (topicStuck signal) + Plan 04.1-06 (host-stuck banner UI).
- CTT-07 → Plan 04.1-05 (chat.topic.archive handler) + Plan 04.1-06 (UI).
- CTT-08 → Plan 04.1-05 (chat.taskOwned handler) + Plan 04.1-06 (rail UI).

If any plan was deferred or its drill produced an approved-with-followups verdict, the row text MUST reflect it (e.g. `Implemented with followup: <link>` per Phase 4's CHAT-04 host-blocked precedent — see ROADMAP entry).

Run `node --test test/phases/04.1-traceability.test.mjs` — GREEN. Commit: `docs(04.1-07): CTT-01..CTT-08 → Implemented`.
  </action>
  <verify>
    <automated>node --test test/phases/04.1-traceability.test.mjs && grep -cE "^\| CTT-0[1-8] \| Phase 4\.1 \| Implemented" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test/phases/04.1-traceability.test.mjs` exists and passes.
    - `grep -cE "^\| CTT-0[1-8] \| Phase 4\.1 \| Implemented \(Plan 04\.1-\d{2}" .planning/REQUIREMENTS.md` returns 8.
    - No row remains `Pending`.
    - `tsc --noEmit` (no TS change but worth confirming) clean.
  </acceptance_criteria>
  <done>The traceability gate green-fences Phase 4.1; the CTT rows can never silently regress.</done>
</task>

<task type="auto">
  <name>Task 3: Update ROADMAP.md + STATE.md + write 04.1-VERIFICATION.md</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (the Phase 4.1 entry around line 110-121 — Task 3 marks it [x] complete; adds the Plans list with checkmarks; updates the Phase Progress table at the bottom).
    - .planning/STATE.md (refresh the position + decisions + blockers — at this writing, the position should advance to Phase 5).
    - .planning/phases/04-employee-chat/04-VERIFICATION.md (the verbatim analog for 04.1-VERIFICATION.md — header + per-requirement proof + closure metadata + the host-blocked / approved-with-followups reconciliation precedent for CHAT-04).
    - .planning/phases/04.1-chat-true-task/04.1-06-SUMMARY.md (the drill verdict + version-bump pin + tarball sha256 to cite).
    - .planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md (the FLAG-1 reconciliation — if the spike said D-12 is host-blocked, the verification doc records it as `host-blocked-residual` per CHAT-04 precedent).
  </read_first>
  <action>
**Update `.planning/ROADMAP.md`** — find the Phase 4.1 entry (around line 110-121):
1. Flip `- [ ] **Phase 4.1: Chat → True Task**` → `- [x] **Phase 4.1: Chat → True Task**` (with the completion date).
2. Update the Phase 4.1 details block:
   - Add `Plans: 7 plans` (or whatever the actual count is — 04.1-01 through 04.1-07 = 7).
   - Add the Plans list (per the Phase 4 pattern):
     ```
     Plans:
     - [x] 04.1-01-PLAN.md — Falsify-first spike. Wave 1, non-autonomous.
     - [x] 04.1-02-PLAN.md — createTrueTask + chat.createTrueTask + chat.promote rewrite. Wave 2, autonomous TDD.
     - [x] 04.1-03-PLAN.md — Lifecycle helpers + chat.send wake nudge + chat-topics D-11. Wave 2, autonomous TDD.
     - [x] 04.1-04-PLAN.md — Runtime-noise classifier + chat.messages watchdog hook + topicStuck signal. Wave 2, autonomous TDD.
     - [x] 04.1-05-PLAN.md — chat.topic.archive + chat.taskOwned handlers. Wave 3, autonomous TDD.
     - [x] 04.1-06-PLAN.md — UI surface build + version 0.8.0 + Countermoves drill. Wave 4, non-autonomous.
     - [x] 04.1-07-PLAN.md — Coexistence + closure. Wave 5, non-autonomous.
     ```
3. Update the Phase Progress table at the bottom:
   - Phase 4.1 row: `7/7 | COMPLETE & VERIFIED YYYY-MM-DD — operator drill PASSED, CTT-01..CTT-08 Implemented, ...`
   - Phase 5 row may shift state from `Not started` to `Unblocked`.
4. Update the bottom-of-roadmap *Last updated* line with the closure date and a one-paragraph summary noting (a) the operator drill verdict, (b) any FLAG-1 reconciliation (host-blocked residual per CHAT-04 precedent if applicable), (c) the version pin (0.8.0), and (d) FLAG-2 confirmed (situation-snapshot reads `current_task_summary`, no inflation).

**Update `.planning/STATE.md`** — refresh the current phase + suite count + commit log. (STATE.md is 34K tokens per the earlier read attempt; only the sections that move on phase closure need editing — the Position block + the Last commits block. Use the `gsd-sdk query state` pattern if available; otherwise edit in place.)

**Write `.planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md`** — model on `.planning/phases/04-employee-chat/04-VERIFICATION.md`. Sections:
- **Verdict**: PASSED (or PASSED-WITH-FOLLOWUPS / FAILED — per the Plan 04.1-06 drill outcome).
- **Per-requirement proof table** — for each CTT-01..CTT-08, the cite (commit hash / file / test file / drill step).
- **FLAG-1 reconciliation** — D-12 outcome from spike findings: either D-12 fires `{queued:true}` and the wake nudge works (FLAG-1 closed as `prevented-by-D09-D11-D12`), or D-12 is host-blocked and the residual case is reconciled as `host-blocked-residual` per CHAT-04 precedent (verifier does NOT treat it as an SC#4 failure).
- **FLAG-2 reconciliation** — grep of situation-snapshot.ts shows no `issues.list({assigneeAgentId})` walk; chat-topic issues do NOT inflate work-status aggregation.
- **Coexistence proof** — disable rehearsal: a snapshot taken with N chat messages + M true-task issues + K archived topics → disable plugin → issue_comments count unchanged + true-task issues remain in classic Paperclip with their assignees → re-enable plugin → archived flag still set + active tasks rail still populated.
- **Tarball pin**: `clarity-pack-0.8.0.tgz` sha256: `<from 04.1-06-SUMMARY>`.
- **Suite delta**: `<base>` (941 at Phase 4 close) → `<close>` tests after Phase 4.1.

Commit: `docs(04.1-07): ROADMAP + STATE updated; 04.1-VERIFICATION.md written`.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*Phase 4.1: Chat → True Task\*\*" .planning/ROADMAP.md && test -f .planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - ROADMAP.md Phase 4.1 line is `[x]` + closure date.
    - ROADMAP.md Phase Progress table Phase 4.1 row is `7/7 | COMPLETE ...`.
    - STATE.md refreshed.
    - `.planning/phases/04.1-chat-true-task/04.1-VERIFICATION.md` exists with the verdict + per-CTT proof + FLAG-1/FLAG-2 reconciliations + version pin + tarball sha + suite delta.
  </acceptance_criteria>
  <done>The phase's planning artifacts reflect closure; the next-phase planner has a clean handoff state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Eric runs the Phase 4.1 closure drill on Countermoves — coexistence + final approval</name>
  <what-built>
    The Phase 4.1 closure: CI coexistence check 09-true-task.mjs, traceability test 04.1-traceability.test.mjs (RED→GREEN), CTT-01..CTT-08 flipped to Implemented, ROADMAP/STATE/VERIFICATION updated.
  </what-built>
  <how-to-verify>
On the Countermoves Hostinger VPS — bookended snapshot first (CLAUDE.md):

1. **Bookend snapshot:**
   ```bash
   cd ~/clarity-pack
   node scripts/safety/cli.mjs snapshot --db-url $(cat /etc/paperclip/db.env | grep DB_URL | cut -d= -f2-)
   node scripts/safety/cli.mjs gate
   ```

2. **Pre-disable inventory** (record numbers for the disable check):
   - `psql "$DB_URL" -c "SELECT count(*) FROM public.issue_comments"` → record count A.
   - `psql "$DB_URL" -c "SELECT count(*) FROM public.issues WHERE origin_kind = 'plugin:clarity-pack'"` → record count B (this includes chat-topic issues, parent issues, AND the new true-task issues created in Plan 04.1-06's drill — confirm by listing a few).
   - `psql "$DB_URL" -c "SELECT count(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topics WHERE archived = true"` → record count C (the archived topics from drill 6's archive test).

3. **Disable plugin**:
   ```bash
   cd ~/paperclip
   pnpm paperclipai plugin disable clarity-pack
   ```
   - Refresh the chat surface in classic Paperclip — confirm the chat URL `/<COU>/chat` no longer routes (or routes to the host's "this plugin is disabled" view, depending on Paperclip's UX).
   - Open a chat-topic issue in classic Paperclip's normal issue view — confirm the messages are still visible as ordinary threaded comments (CHAT-11 / COEXIST-08 invariant).
   - Open a true-task issue (BEAAA-NNN created in drill 6) — confirm it is still visible, still assigned to its employee, still has its status (CTT-01 coexistence — the issue is plain Paperclip, no plugin tables involved).
   - Re-run the inventory: counts A, B unchanged. (count C is plugin-namespace and depends on whether disable preserves the namespace — per COEXIST-03 and the existing 08-chat-disable.mjs assertion, plugin tables ARE preserved on disable. Confirm count C unchanged.)

4. **Re-enable plugin**:
   ```bash
   pnpm paperclipai plugin enable clarity-pack
   ```
   - Refresh the chat surface. Confirm:
     - The topic strip shows the same topics minus the archived ones (count C archived topics still hidden — the chat_topics.archived flag survived the disable).
     - Click `+N archived` — the archived topics reappear at opacity 0.6.
     - Open a chat topic — the messages are still threaded; chat.messages still returns them; classifier still filters runtime noise; watchdog still fires.
     - The Active tasks owned rail still shows the true-task issues created in drill 6.

5. **Run the CI checks against the now-installed state**:
   ```bash
   cd ~/clarity-pack
   node scripts/coexistence-checks/run-all.mjs
   ```
   All 9 checks pass.

6. **Type ONE of:**
    - "approved — phase 4.1 closed" — all five verification steps PASSED.
    - "approved-with-followups: <list>" — closure approved with non-blocking follow-ups noted in 04.1-VERIFICATION.md.
    - "DO NOT PASS — <root cause>" — at least one verification step failed in a way that requires gap-closure plan (route to Plan 04.1-08 — same name reserved across the phase for gap-closure).
  </how-to-verify>
  <resume-signal>Type "approved — phase 4.1 closed", "approved-with-followups: ...", or "DO NOT PASS — ..."</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → coexistence-checks/09-true-task.mjs | Static repo-scan checks; no host runtime. Trust boundary is purely "does the repo source still satisfy the disable-preserves-data contract." |
| Operator → disable / re-enable plugin on Countermoves | Bookended-by-snapshot (CLAUDE.md). The closure drill is a controlled-rollback exercise; misbehaviour rolls back to the snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-07-01 | T (Tampering) | The CI check 09-true-task.mjs is bypassable by skipping run-all.mjs | mitigate | The run-all.mjs CHECKS array addition forces 09 to run in every CI invocation; CI itself MUST call run-all (this is the COEXIST-06 contract from Phase 2). |
| T-04.1-07-02 | D (Denial of service) | The disable rehearsal on Countermoves disrupts ongoing chat conversations | accept | This is an operator-supervised drill; the bookended snapshot rolls back if needed. Eric is the only user. |
| T-04.1-07-03 | I (Info disclosure) | The 04.1-VERIFICATION.md captures the operator's name in the recoveryOwner reconciliation | accept | Same-tenant trust model. The verification doc lives in `.planning/` which is part of the repo. |

ASVS L1 baseline. No high-severity threat is `accept`-disposed.
</threat_model>

<verification>
Plan 04.1-07 succeeds when:
- 09-true-task.mjs exists, exits 0 on the current source tree, and is included in run-all.mjs.
- 04.1-traceability.test.mjs RED→GREEN — the CTT rows are now Implemented.
- ROADMAP.md / STATE.md / 04.1-VERIFICATION.md reflect closure.
- Operator drill on Countermoves PASSES — disable preserves data; re-enable restores plugin behaviour; archived flag survives; true-task issues are normal Paperclip issues.
- The Phase 4.1 followup's two intertwined problems are PROVEN solved at the system level (not just at the unit level).
</verification>

<success_criteria>
- Phase 4.1 closes with all 8 CTT requirements Implemented + traceability-gated + coexistence-gated.
- Operator approves on Countermoves.
- ROADMAP advances; Phase 5 is unblocked.
- No silent regressions possible (the traceability test + the coexistence check both run in CI).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-chat-true-task/04.1-07-SUMMARY.md` per the GSD summary template — recording commit range, final suite count, the closure verdict, any approved-with-followups list, the closure date, and the next-phase handoff note.
</output>
</content>
