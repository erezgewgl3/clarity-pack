---
phase: 04.1-chat-true-task
plan: 08
type: execute
wave: 4
gap_closure: true
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  # CSS
  - src/ui/styles/chat.css
  # New UI components
  - src/ui/surfaces/chat/actions-row.tsx
  - src/ui/surfaces/chat/archive-panel.tsx
  - src/ui/surfaces/chat/archived-banner.tsx
  # Reshaped / renamed UI components
  - src/ui/surfaces/chat/true-task/true-task-dialog.tsx
  - src/ui/surfaces/chat/true-task/inline-task-card.tsx
  - src/ui/surfaces/chat/composer.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - src/ui/surfaces/chat/context-rail.tsx
  - src/ui/surfaces/chat/topic-strip.tsx
  - src/ui/surfaces/chat/archived-topics-pill.tsx
  - src/ui/surfaces/chat/diagnostics-toggle.tsx
  - src/ui/surfaces/chat/promote-actions.tsx
  - src/ui/surfaces/chat/index.tsx
  # Component being deleted (file path tracked for cleanup)
  - src/ui/surfaces/chat/true-task/true-task-toggle.tsx
  # Version bumps (two-source rule from memory plugin-version-bump-two-sources)
  - package.json
  - src/manifest.ts
  # Tests
  - test/ui/chat-actions-row.test.mjs
  - test/ui/chat-archive-panel.test.mjs
  - test/ui/chat-archived-banner.test.mjs
  - test/ui/chat-true-task-dialog.test.mjs
  - test/ui/chat-inline-task-card.test.mjs
  - test/ui/chat-composer.test.mjs
  - test/ui/chat-message-thread.test.mjs
autonomous: false
requirements: [CTT-01, CTT-02, CTT-04, CTT-06, CTT-07, CTT-08]
user_setup: []

must_haves:
  truths:
    # Cold task creation (the workflow break Eric named)
    - "A `+ Create task` PRIMARY button sits in a new ACTIONS ROW between the topic strip and the messages scroller. Always visible. Click opens the task dialog in COLD mode."
    - "In COLD mode, the dialog title field is AUTOFOCUSED and EMPTY. The TOPIC dropdown defaults to 'Standalone (not linked to any topic)' — NOT to the currently-open topic. The currently-open topic is selectable in the dropdown but is not the default."
    - "A cold task creates a top-level Paperclip issue with `originKind: 'plugin:clarity-pack'` and `originId: 'cold-task:<userId>:<unix-ms>'` — distinct from `chat-task:<topic>:<comment>` so it is NOT picked up by `chat.taskOwned` for any topic."
    - "Hitting `⌘+Enter` (Mac) or `Ctrl+Enter` (Windows) anywhere in the dialog creates the task. `Esc` cancels. `⌘+T` global keyboard shortcut opens the dialog cold (no chat context needed)."
    # Promote-from-message (UI-SPEC's actual sweet spot, now isolated)
    - "On every AGENT REPLY bubble (and the user's own bubbles), a `→ Promote to task` action surfaces on HOVER (opacity 0 → 1 transition; positioned at top-right of the bubble). Clicking it opens the SAME dialog as cold mode BUT in PROMOTE mode."
    - "In PROMOTE mode: heading reads 'Promote message to task' (not 'Create a task'); title is pre-filled with the message's title-extracted form (first ~80 chars + ellipsis if longer); TOPIC dropdown defaults to the SOURCE topic (the one the message lives in); a `FROM THIS MESSAGE · {employee} · {time}` quoted block renders below the textarea showing the full source body; resulting issue carries the existing `chat-task:<topic>:<comment>` originId AND inserts into `chat_topic_tasks` (Plan 04.1-05 wiring is unchanged)."
    - "The OLD `↗ Send as task` composer-tools-row TOGGLE is REMOVED. The OLD `Open Task Form` send-button-label flip is REMOVED. `src/ui/surfaces/chat/true-task/true-task-toggle.tsx` is DELETED (rm -f + git rm). The composer textarea has ONE meaning: send a chat message. The send button always says SEND. The placeholder is just `Message {employee}…`."
    # Archive panel (the question Eric asked — surface the dropdown panel pattern)
    - "Clicking the `+N archived` pill opens an ARCHIVE PANEL anchored below the topic strip. The panel shows ALL archived topics for this employee+company, scoped by user opt-in."
    - "Each archive row is CLICK-TO-OPEN — the WHOLE row is the click target (cursor: pointer). Clicking the row navigates to the topic in ARCHIVED-OPEN state (the read-only banner appears at the top of `.messages`, the composer is dashed-border and DISABLED with placeholder text 'Unarchive to send messages')."
    - "An `Unarchive` button surfaces on hover (`opacity: 0 → 1`) at the right of each row. Clicking it (with `event.stopPropagation()` so it does NOT trigger the row's open handler) calls `chat.topic.archive({archived: false})` and the topic returns to the active strip."
    - "Header shows `ARCHIVED TOPICS` + close ✕. Body has a real-time search input (filters by title, case-insensitive substring match, instant). Footer shows `Showing N of M · View all archived →` — the `View all` link is a STUB for Phase 4.2 (per memory phase-4.2-deferred-from-4.1; the click handler logs `console.warn('Phase 4.2 — full archive view not yet implemented')` and does nothing else)."
    - "When NO archived topics exist, the panel renders an empty state: `No archived topics. Topics you archive will appear here.` Don't show the search field or footer in this case."
    - "The `+N archived` pill is replaced when N === 0 (don't render it at all). When N > 0 the pill IS rendered with the count."
    - "Default sort order: NEWEST ARCHIVED FIRST. Implementation: SQL `ORDER BY chat_topics.archived_at DESC` if the column exists; if `archived_at` is not in the existing schema, ship a tiny additive migration `migrations/0008_chat_topics_archived_at.sql` adding the column with `DEFAULT now()` (additive-only per CLAUDE.md coexistence guarantee #3)."
    # Archived-topic open state (read-only browsing)
    - "When an archived topic is opened (via archive-panel row click), the chat surface renders a sticky banner at the top of `.messages` reading `📁 ARCHIVED — read-only · last active {relative} · {N} messages · {M} task(s) spawned` plus a prominent `Unarchive` button on the right. The banner uses the `--warn` color family (warn-soft bg + warn border) — NOT the alert color (which is reserved for host-stuck)."
    - "While an archived topic is open: composer is VISUALLY DISABLED (dashed border, dim text, cursor: not-allowed) AND FUNCTIONALLY DISABLED (the textarea is read-only OR the send button is disabled — pick whichever React idiom is cleaner; just ensure no `chat.send` call can fire). Placeholder reads `Unarchive to send messages.`"
    - "The topic in the strip itself shows it's archived: dashed border, opacity 0.62, `📁` suffix after the title. Clicking it does NOT auto-unarchive — it just stays open in archived state."
    - "Unarchive from the banner OR from the panel row's hover button BOTH call `chat.topic.archive({archived: false})` and the topic immediately returns to the active strip + the composer re-enables. The plugin-side flag flip is the ONLY DB write. The host issue's status is NEVER touched on archive or unarchive (D-10 invariant — pinned by Plan 04.1-05 Test 6; this plan inherits the invariant)."
    # Contrast (WCAG)
    - "CSS tokens `--ink-3` and `--ink-4` are bumped to pass WCAG AA against `--bg`. `--ink-3` changes from `#7E7869` (~4.3:1) to `#A09786` (~6.0:1 — AA body / AAA UI). `--ink-4` changes from `#52503F` (~2.4:1) to `#6D6857` (~3.5:1 — decorative only; NEVER use for body text)."
    - "Every existing use of `--ink-4` for BODY TEXT in chat.css is migrated to `--ink-3` (composer hints, attach pill label, kbd hints, day dividers, etc.). Decorative-only uses (subtle borders, separator dots) may keep `--ink-4`."
    # Layout (rail width + ordering)
    - "The right context-rail width changes from ~280px to 360px (`grid-template-columns: 264px 1fr 360px`). Labels like `Active tasks owned`, `Archive this topic`, `Search this employee's chats`, `Pause heartbeat` MUST NOT TRUNCATE at a 1280px viewport at browser zoom 100%."
    - "The ACTIONS ROW order is: `[+ Create task] [+ New topic] | spacer | [⏿ Diagnostics]`. Task and topic creation sit together (sibling semantics); diagnostics is right-aligned (meta affordance). No kbd-hint strip in the row (move it to button tooltips and an optional `?` overlay deferred to 4.2)."
    - "Diagnostics toggle MOVES from the thread-header's `.head-actions` to the actions row. The `.head-actions` block remains in the markup but is empty (`+ New topic` moved out of there too)."
    # Inline task card (toned down — provenance not message)
    - "The inline task card NO LONGER renders as a full-border + gradient + heavy-weight box. New treatment: a 2px LEFT-RULE accent in `--you`, NO background fill, NO border on top/right/bottom. Eyebrow text `↗ TASK CREATED` is 9px (was 9.5px) at `--you` color with `opacity: 0.85`. Title is `--ink-2` (NOT `--ink`) at 13px weight 400 (was 15px weight 500). BEAAA-NNN ref renders as a gold UNDERLINED LINK (no chip box). Status pill renders OUTLINED ONLY (no background fill)."
    - "The card is FULL-WIDTH `align-self: stretch` (not centered), matches the message column padding. Reads as a quiet 'X happened' provenance marker, not a hero element."
    # Version bump (the two-source lesson learned in 04.1-06)
    - "`package.json` `version` is bumped from `0.8.0` to `0.8.1` (patch bump — this is a UI gap-closure on top of 0.8.0)."
    - "`src/manifest.ts` `version` literal is bumped from `0.8.0` to `0.8.1` in the SAME commit as the package.json bump (per memory `plugin-version-bump-two-sources` — both sources must change together)."
    - "After build + npm pack, the tarball is `clarity-pack-0.8.1.tgz`. The Paperclip host's `paperclipai plugin list` after install MUST report `version=0.8.1` (NOT 0.8.0). Verified by grepping `dist/manifest.js` for `'0.8.1'` and confirming presence."
    # Test coverage
    - "≥ 25 new node:test cases across the 7 new/updated UI test files. Coverage MUST include:"
    - "  - chat-actions-row.test.mjs: + Create task button present + click handler; + New topic preserved; diagnostics toggle present + click toggles aria-pressed"
    - "  - chat-archive-panel.test.mjs: opens on +N pill click; renders archived topics list; row click invokes onOpen with topicIssueId; Unarchive button stop-propagates AND calls archive(false); search filters case-insensitive substring; empty state renders when 0 archives; View all link is no-op stub"
    - "  - chat-archived-banner.test.mjs: renders banner + Unarchive button when topic is archived; composer is disabled (read-only or send-disabled); banner click does not fire stray actions"
    - "  - chat-true-task-dialog.test.mjs: COLD mode (autofocus title, Standalone default, ⌘+Enter creates with cold-task originId); PROMOTE mode (title pre-filled, source topic default, FROM-MESSAGE block visible, chat-task originId); ⌘+T global shortcut opens cold dialog; Esc closes"
    - "  - chat-inline-task-card.test.mjs: left-rule treatment renders (no full border); title uses --ink-2 not --ink; ref-chip is anchor not span"
    - "  - chat-composer.test.mjs: NO Send-as-task toggle in DOM; placeholder is `Message {employee}…`; send button label is `SEND` (NOT `OPEN TASK FORM`); composer is disabled when topic.archived is true"
    - "  - chat-message-thread.test.mjs: → Promote hover affordance present on agent bubbles (opacity 0 default); rendering an archived topic shows the archived-banner; rendering a stuck topic shows the host-stuck banner"
    # Build + cleanup
    - "`tsc --noEmit` clean. `node scripts/build-worker.mjs` + `node scripts/build-ui.mjs` + manifest tsc all emit cleanly. Full suite (existing 1091 + new ≥25) all GREEN."
    - "`true-task-toggle.tsx` file is REMOVED from disk (not just the contents — the file itself). Imports referencing it in `composer.tsx` are removed."
    - "`clarity-pack-0.8.1.tgz` packs successfully (sha256 captured in the SUMMARY)."
    # Operator drill (CHECKPOINT)
    - "Operator visual-fidelity drill on Countermoves PASSES — the redesigned surface renders per the sketch `sketches/paperclip-fix-chat-true-task.html`; the 7-variant flow (default, cold dialog, promote dialog, diagnostics on, host-stuck banner, archive panel, archived topic open) all work end-to-end on a real chat; the multi-turn re-wake fix (Wave 1 finding) continues to hold; the D-10 invariant (host issue status untouched on archive/unarchive) verifies in classic Paperclip; D-04 unification still produces a top-level assigned task; cold task creation produces a task that does NOT appear in any topic's Active tasks owned rail."

  artifacts:
    - path: "src/ui/styles/chat.css"
      provides: "Bumped contrast tokens (--ink-3 / --ink-4); 360px right rail; actions-row CSS; archive-panel CSS; archived-banner CSS; toned-down inline-task-card; removed Send-as-task toggle CSS."
      contains: "--ink-3: #A09786"
    - path: "src/ui/surfaces/chat/actions-row.tsx"
      provides: "NEW component. [+ Create task] [+ New topic] [Diagnostics] action row between topic strip and messages."
      exports: ["ChatActionsRow"]
      contains: "+ Create task"
    - path: "src/ui/surfaces/chat/archive-panel.tsx"
      provides: "NEW component. Dropdown panel anchored to +N archived pill. Click-to-open rows; hover Unarchive button; real-time search; empty-state; View all stub for 4.2."
      exports: ["ArchivePanel"]
      contains: "click to open · read-only"
    - path: "src/ui/surfaces/chat/archived-banner.tsx"
      provides: "NEW component. Sticky read-only banner shown when an archived topic is open. Unarchive button."
      exports: ["ArchivedBanner"]
      contains: "ARCHIVED — read-only"
    - path: "src/ui/surfaces/chat/true-task/true-task-dialog.tsx"
      provides: "RESHAPED. Dual-mode (cold + promote). Autofocus title; ⌘+Enter creates; topic dropdown with Standalone default for cold; FROM-MESSAGE block for promote."
      contains: "Standalone"
    - path: "src/ui/surfaces/chat/true-task/inline-task-card.tsx"
      provides: "RESHAPED. Left-rule provenance treatment (no full border, no gradient)."
      contains: "border-left"
    - path: "src/ui/surfaces/chat/composer.tsx"
      provides: "STRIPPED. NO Send-as-task toggle. Single-purpose chat textarea."
      contains: "Message"
    - path: "src/ui/surfaces/chat/message-thread.tsx"
      provides: "EXTENDED. → Promote hover affordance on bubbles; renders archived-banner when topic.archived; renders inline-task-card with new toned-down treatment."
      contains: "Promote to task"
    - path: "src/ui/surfaces/chat/archived-topics-pill.tsx"
      provides: "UPDATED. Click opens ArchivePanel (not inline reveal)."
      contains: "ArchivePanel"
    - path: "src/ui/surfaces/chat/diagnostics-toggle.tsx"
      provides: "MOVED to actions-row.tsx; component itself unchanged."
      contains: "Diagnostics"
    - path: "src/ui/surfaces/chat/promote-actions.tsx"
      provides: "UPDATED. Opens true-task-dialog in PROMOTE mode (was opening confirm-dialog)."
      contains: "PROMOTE"
    - path: "src/ui/surfaces/chat/index.tsx"
      provides: "REWIRED. Mounts ActionsRow + ArchivePanel + ArchivedBanner. Removes head-actions Diagnostics. ⌘+T global keyboard handler."
      contains: "ChatActionsRow"

  key_links:
    - from: "src/ui/surfaces/chat/actions-row.tsx + Create task button"
      to: "src/ui/surfaces/chat/true-task/true-task-dialog.tsx COLD mode"
      via: "onClick={() => openTaskDialog({mode:'cold'})}"
      pattern: "mode:'cold'"
    - from: "src/ui/surfaces/chat/message-thread.tsx → Promote hover action"
      to: "src/ui/surfaces/chat/true-task/true-task-dialog.tsx PROMOTE mode"
      via: "onClick={() => openTaskDialog({mode:'promote', sourceMessage})}"
      pattern: "mode:'promote'"
    - from: "src/ui/surfaces/chat/archived-topics-pill.tsx +N pill click"
      to: "src/ui/surfaces/chat/archive-panel.tsx"
      via: "onClick={() => openArchivePanel()}"
      pattern: "ArchivePanel"
    - from: "src/ui/surfaces/chat/archive-panel.tsx row click"
      to: "src/ui/surfaces/chat/archived-banner.tsx + composer disabled state"
      via: "onOpen(topicIssueId) sets the active topic + archived=true in topic state"
      pattern: "ARCHIVED — read-only"
    - from: "src/ui/surfaces/chat/archive-panel.tsx + archived-banner.tsx Unarchive buttons"
      to: "chat.topic.archive worker handler (Plan 04.1-05)"
      via: "ctx.actions.invoke('chat.topic.archive', {archived: false, topicIssueId, companyId, userId})"
      pattern: "chat.topic.archive"

  acceptance_criteria:
    # Files removed
    - test: "grep -c 'TrueTaskToggle' src/ui/surfaces/chat/composer.tsx"
      expect: "0"
      why: "the Send-as-task toggle import + render must be GONE from composer"
    - test: "test -f src/ui/surfaces/chat/true-task/true-task-toggle.tsx && echo present || echo removed"
      expect: "removed"
      why: "the file itself is deleted"
    # New files present
    - test: "test -f src/ui/surfaces/chat/actions-row.tsx && echo found"
      expect: "found"
    - test: "test -f src/ui/surfaces/chat/archive-panel.tsx && echo found"
      expect: "found"
    - test: "test -f src/ui/surfaces/chat/archived-banner.tsx && echo found"
      expect: "found"
    # Contrast tokens bumped
    - test: "grep -c '#A09786' src/ui/styles/chat.css"
      expect: ">=1"
      why: "--ink-3 token bumped to #A09786 (AA body)"
    - test: "grep -c '#6D6857' src/ui/styles/chat.css"
      expect: ">=1"
      why: "--ink-4 token bumped to #6D6857 (decorative only)"
    # Right rail widened
    - test: "grep -E 'grid-template-columns:\\s*[0-9]+px\\s+1fr\\s+360px' src/ui/surfaces/chat/index.tsx src/ui/styles/chat.css | wc -l"
      expect: ">=1"
      why: "the chat shell grid uses 360px for the right rail"
    # Version bump (two sources)
    - test: "grep -c '\"version\": \"0.8.1\"' package.json"
      expect: "1"
    - test: "grep -c \"version: '0.8.1'\" src/manifest.ts"
      expect: "1"
    # Build clean
    - test: "npx tsc --noEmit && echo CLEAN"
      expect: "CLEAN"
    # Suite passes
    - test: "node --test 'test/**/*.test.mjs' 2>&1 | tail -1 | grep -E 'fail [0-9]+' | grep -v 'fail 0$'"
      expect: ""
      why: "zero failing tests in the full suite"
    # Tarball builds and reports the right version
    - test: "test -f clarity-pack-0.8.1.tgz && echo packed"
      expect: "packed"
    - test: "grep -c \"version: '0.8.1'\" dist/manifest.js"
      expect: "1"
      why: "the build emitted the bumped version literal"

  user_setup: []
---

<objective>
Apply the visual-fidelity drill findings from Plan 04.1-06 as a comprehensive UI redesign of the Chat → True Task surface. This plan IS the gap closure for the 5+ findings surfaced during the 2026-05-20 drill — it supersedes Plan 04.1-06's UI artifacts where the design pattern was wrong (Send-as-task toggle, ambient topic anchoring, low contrast, narrow rail) and folds in the new archive UX (panel + click-to-open + read-only state) we discussed with Eric.

The redesign separates THREE distinct user intents at the affordance level:
1. **Talk to an employee** — composer textarea, single-purpose
2. **Promote a message to a task** — per-message hover action on agent replies
3. **Create a cold task** — `+ Create task` primary button in always-visible actions row

Plus the archive-recall flow Eric specifically asked about: `+N archived` pill click opens a dropdown panel with click-to-open rows (read-only state with banner) + hover Unarchive button.

This plan does NOT change worker-tier behavior. The handlers (chat.createTrueTask, chat.promote, chat.taskOwned, chat.topic.archive) all stay exactly as Plans 04.1-02 / 04.1-05 shipped them. The 0007_chat_topic_tasks side table stays. The D-10 / D-08 / D-14 invariants stay. This is a pure UI replacement.

Visual contract: the sketch `sketches/paperclip-fix-chat-true-task.html` (all 7 variants) is the design ground truth. Build to match.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04.1-chat-true-task/04.1-CONTEXT.md
@.planning/phases/04.1-chat-true-task/04.1-RESEARCH.md
@.planning/phases/04.1-chat-true-task/04.1-PATTERNS.md
@.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
@.planning/phases/04.1-chat-true-task/04.1-02-SUMMARY.md
@.planning/phases/04.1-chat-true-task/04.1-03-SUMMARY.md
@.planning/phases/04.1-chat-true-task/04.1-04-SUMMARY.md
@.planning/phases/04.1-chat-true-task/04.1-05-SUMMARY.md
@.planning/phases/04.1-chat-true-task/04.1-06-SUMMARY.md
@sketches/paperclip-fix-chat-true-task.html
@CLAUDE.md
@runbook/operator-gotchas.md
</context>

<memories_to_honor>
- `feedback_vps-commands-self-contained` — every VPS bash block in the operator drill MUST start with the canonical env re-export (DB_URL via sudo grep, API_URL/KEY/COMPANY_ID).
- `plugin-version-bump-two-sources` — when bumping the version, edit BOTH package.json AND src/manifest.ts in the same commit. Verify `dist/manifest.js` carries the new literal after build.
- `phase-4.2-deferred-from-4.1` — items deferred from this plan (full archive view, multi-select, paused-agent banner, ref-chip peek cards, etc.) live in this memory. The `View all archived →` link in the archive panel is a NO-OP STUB for 4.2; document this in code comments and SUMMARY.md so 4.2 picks it up.
- `paperclipai-workspace-script-gotcha` — install drill runs `pnpm paperclipai plugin install` from `~/paperclip`, NOT from `~/clarity-pack`.
- `countermoves-safety-cli-invocation` — safety CLI on VPS runs via `node scripts/safety/cli.mjs` with `--db-url` flag.
</memories_to_honor>

<tasks>

<task type="auto">
  <name>Task 1: CSS foundation — contrast tokens + 360px rail + new component styles + tone-down inline task card</name>
  <read_first>
    - src/ui/styles/chat.css (the existing 388-line Phase 4.1 section appended by Plan 04.1-06 — read it in full to understand the existing selectors before modifying)
    - sketches/paperclip-fix-chat-true-task.html (the visual contract — extract the exact selectors + values for actions-row, archive-panel, archived-banner, toned-down inline-task-card)
    - .planning/phases/04.1-chat-true-task/04.1-UI-SPEC.md (the original UI-SPEC for context; many decisions are now SUPERSEDED by the redesign — this plan's must_haves are authoritative where they conflict with UI-SPEC)
  </read_first>
  <action>
**Modify `src/ui/styles/chat.css`** in two layers:

**Layer A — token bumps (apply at the existing `:root` block OR at the top of the Phase 4.1 section):**
```css
/* Plan 04.1-08: WCAG AA contrast bump */
[data-clarity-surface="chat"] {
  --ink-3: #A09786;  /* was #7E7869 (~4.3:1) → now ~6.0:1 AA body / AAA UI */
  --ink-4: #6D6857;  /* was #52503F (~2.4:1) → now ~3.5:1 decorative ONLY */
}
```
Then audit every existing rule in chat.css that uses `--ink-4` for TEXT. Migrate body-text usages to `--ink-3`. Leave decorative-only uses (subtle borders, dot separators, divider lines) on `--ink-4`. The greppable guarantee from Task 1 acceptance: `grep -c '#A09786' src/ui/styles/chat.css >= 1`.

**Layer B — new + revised component styles.** Append a new `/* ---- Plan 04.1-08 — redesign ---- */` section AT THE END of the file. Port the CSS from the sketch verbatim (it's already production-shaped):
- `.chat-actions-row` (the new row with Create task + New topic + Diagnostics)
- `.chat-archive-panel` (anchored dropdown — the click-to-open + hover Unarchive variant)
- `.chat-archived-banner` (sticky `--warn` banner for archived topic open state)
- `.chat-task-dialog` (cold + promote shared shell — autofocus, ⌘+Enter footer hint, FROM MESSAGE block)
- `.chat-inline-task-card` — REPLACE the existing rules with the toned-down variant: 2px left-rule in `--you`, no border, no gradient, `--ink-2` title at 13px weight 400, ref-chip as gold underlined anchor

**Layer C — rail width.** Change the `.shell` grid-template-columns from `264px 1fr ~280px` to `264px 1fr 360px`. Find the existing rule in chat.css and update.

**Layer D — DELETE** the existing `.true-task-toggle.armed` rules + the OPEN-TASK-FORM send-button-flip rules. Their JS counterparts are removed in Task 3.

Naming convention: scope ALL new selectors under `[data-clarity-surface="chat"]` (CSS coexistence guarantee). No new tokens beyond the two contrast bumps; reuse existing palette.

After modifications, run `npx tsc --noEmit` (no TS file changed yet but sanity) and confirm the file passes the project's CSS lint if one exists.
  </action>
  <verify>
- `grep -c '#A09786' src/ui/styles/chat.css` ≥ 1
- `grep -c '#6D6857' src/ui/styles/chat.css` ≥ 1
- `grep -c 'border-left:\\s*2px solid var(--you)' src/ui/styles/chat.css` ≥ 1 (inline task card left-rule)
- `grep -c '360px' src/ui/styles/chat.css` ≥ 1 (rail width)
- `grep -c '.chat-actions-row' src/ui/styles/chat.css` ≥ 1
- `grep -c '.chat-archive-panel' src/ui/styles/chat.css` ≥ 1
- `grep -c '.chat-archived-banner' src/ui/styles/chat.css` ≥ 1
- Existing `.true-task-toggle.armed` rules either gone or repurposed
- File builds clean
  </verify>
  <done>chat.css carries the contrast bump + new components + toned-down task card + 360px rail.</done>
  <acceptance_criteria>
    - "grep -c '#A09786' src/ui/styles/chat.css → ≥1"
    - "grep -c '360px' src/ui/styles/chat.css → ≥1"
    - "grep -c 'border-left:\\s*2px solid var(--you)' src/ui/styles/chat.css → ≥1"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build new components — ActionsRow + ArchivePanel + ArchivedBanner</name>
  <read_first>
    - sketches/paperclip-fix-chat-true-task.html (the JSX/component shape is inferred from the HTML; treat the sketch markup as the visual contract)
    - src/ui/surfaces/chat/diagnostics-toggle.tsx (existing — reuse component shape, will be relocated from head-actions to actions-row)
    - src/ui/surfaces/chat/topic-strip.tsx (existing — to understand how + New topic is invoked today; the new actions-row hosts a SIBLING `+ New topic` button OR delegates to the existing one)
    - .planning/phases/04.1-chat-true-task/04.1-05-SUMMARY.md (chat.taskOwned + chat.topic.archive worker contracts — what the panel calls)
  </read_first>
  <action>
**Build three new files:**

**a) `src/ui/surfaces/chat/actions-row.tsx`** — exports `ChatActionsRow({ onCreateTask, onNewTopic, diagnosticsOn, onDiagnosticsToggle })`:
- Wrapper: `<div className="chat-actions-row" data-clarity-surface="chat">`
- Three buttons in this order: `[+ Create task]` (primary, gold) → `[+ New topic]` (ghost) → spacer → `[⏿ Diagnostics]` (ghost; toggles to solid when on)
- `+ Create task` invokes `onCreateTask()` (which opens task dialog in cold mode in the parent)
- `+ New topic` invokes `onNewTopic()` (delegates to existing topic-create flow)
- Diagnostics button uses `aria-pressed={diagnosticsOn}` + visual state flip
- Add a global `useEffect` for `⌘+T` (Mac) / `Ctrl+T` (Windows) shortcut: `e.metaKey||e.ctrlKey && e.key === 't'` → `e.preventDefault()` + `onCreateTask()`. Skip if the user is typing in a contenteditable / input (`document.activeElement?.matches('input, textarea, [contenteditable="true"]')`).
- NO inline kbd hints in the DOM — keyboard shortcuts surface via `title` attributes only

**b) `src/ui/surfaces/chat/archive-panel.tsx`** — exports `ArchivePanel({ open, archivedTopics, onClose, onOpenTopic, onUnarchive })`:
- `open: boolean` — if false, render nothing
- `archivedTopics: Array<{topicIssueId, topicId, title, employeeName, messageCount, lastActiveAt, archivedAt}>` — caller passes the data (fetched via `chat.taskOwned` or a new lightweight `chat.archivedTopics` data handler — see Task 6 for the data wiring decision)
- Empty state: when `archivedTopics.length === 0`, render only the header + the "No archived topics. Topics you archive will appear here." line — no search field, no footer
- Search: local-state `searchQuery`; filter the list with `topic.title.toLowerCase().includes(searchQuery.toLowerCase())`. No debounce — it's a small list, instant is fine
- Each row is a `<button class="chat-archive-panel__row" type="button">` (semantic button — accessible). Click handler is `onOpenTopic(topicIssueId)`. Inside the row: title, sub-row with employee + N messages + relative-time + archive date, hint `↵ Click to open · read-only` (only visible on hover via CSS)
- Unarchive button is INSIDE the row but uses `onClick={(e) => { e.stopPropagation(); onUnarchive(topicIssueId); }}`. CSS controls its opacity (0 default, 1 on row hover)
- Footer: `Showing {N} of {N}` + `View all archived →` (the link is a no-op stub: `onClick={() => console.warn('[clarity-pack] Phase 4.2 — full archive view not yet implemented. Tracked in memory phase-4.2-deferred-from-4.1.')}`). Add an HTML comment: `<!-- TODO(4.2): full archive view per memory phase-4.2-deferred-from-4.1 -->`
- Use `useEffect` with `addEventListener('keydown', ...)` for `Escape` → `onClose()`
- Use `useRef` + click-outside detection to close on backdrop click

**c) `src/ui/surfaces/chat/archived-banner.tsx`** — exports `ArchivedBanner({ topicTitle, messageCount, tasksSpawned, lastActiveAt, onUnarchive })`:
- Sticky banner: `<div className="chat-archived-banner">` positioned `sticky; top: 0` inside `.messages`
- Layout: `[📁] [text content] [Unarchive button]` — three columns
- Text content: title line `ARCHIVED — read-only`, body `last active {relative} · {N} messages · {M} task(s) spawned. You can browse the conversation but can't send new messages until you unarchive.`
- Unarchive button: prominent `--warn` color, calls `onUnarchive()` which dispatches `chat.topic.archive({archived: false, topicIssueId, companyId, userId})`

All three components: TypeScript, React function components, follow project conventions (look at Plan 04.1-06's components for style consistency — but DROP the bad patterns like the toggle's armed-state classes).
  </action>
  <verify>
- All three files exist on disk
- Each exports its named function component
- `tsc --noEmit` clean
- Sketch matches component output in the eventual mount (validated in Task 7 manual check)
  </verify>
  <done>Three new UI components created and importable.</done>
  <acceptance_criteria>
    - "test -f src/ui/surfaces/chat/actions-row.tsx → exists"
    - "test -f src/ui/surfaces/chat/archive-panel.tsx → exists"
    - "test -f src/ui/surfaces/chat/archived-banner.tsx → exists"
    - "grep -c 'export.*ChatActionsRow' src/ui/surfaces/chat/actions-row.tsx → 1"
    - "grep -c 'export.*ArchivePanel' src/ui/surfaces/chat/archive-panel.tsx → 1"
    - "grep -c 'export.*ArchivedBanner' src/ui/surfaces/chat/archived-banner.tsx → 1"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Reshape true-task-dialog + composer + inline-task-card; DELETE true-task-toggle</name>
  <read_first>
    - src/ui/surfaces/chat/true-task/true-task-dialog.tsx (existing — current shape is single-mode; needs to become dual-mode cold/promote)
    - src/ui/surfaces/chat/true-task/true-task-toggle.tsx (existing — this file is BEING DELETED in this task)
    - src/ui/surfaces/chat/true-task/inline-task-card.tsx (existing — needs tone-down)
    - src/ui/surfaces/chat/composer.tsx (existing — needs TrueTaskToggle import + render REMOVED)
    - src/ui/surfaces/chat/promote-actions.tsx (existing — currently opens confirm-dialog; rewire to call openTaskDialog(mode:'promote'))
    - sketches/paperclip-fix-chat-true-task.html dialog markup (cold + promote variants)
  </read_first>
  <action>
**a) RESHAPE `src/ui/surfaces/chat/true-task/true-task-dialog.tsx`:**
- Props become `{ mode: 'cold' | 'promote', sourceMessage?: {body, employee, time}, sourceTopic?: {topicIssueId, title, topicId}, defaultAssignee?: string, topics: Topic[], onClose, onCreate }`
- COLD mode: heading `Create a task`, title field empty + autofocused, topic dropdown defaults to `Standalone (not linked to any topic)` (the first option, value `null`), FROM-MESSAGE block hidden
- PROMOTE mode: heading `Promote message to task`, title field pre-filled with `titleFromBody(sourceMessage.body)` (reuse the helper from `src/worker/chat/true-task.ts`), topic dropdown defaults to `sourceTopic.topicIssueId`, FROM-MESSAGE block visible showing `sourceMessage.body` with the `FROM THIS MESSAGE · {employee} · {time}` eyebrow
- Topic dropdown lists ALL topics for this employee+company, with `Standalone (not linked to any topic)` as the first option (value `null`)
- Submit handler:
  - COLD: invokes `ctx.actions.invoke('chat.createTrueTask', { companyId, userId, topicIssueId: null, sourceCommentId: null, title, body, assigneeAgentId, employeeName })` BUT note: chat.createTrueTask requires `topicIssueId` to be a string per Plan 04.1-02. We need a NEW worker contract for cold tasks OR a flag on the existing handler. SEE TASK 6 FOR THIS DECISION.
  - PROMOTE: invokes `chat.createTrueTask` exactly as today (Plan 04.1-02 shape)
- Keyboard: `⌘+Enter` / `Ctrl+Enter` submits from anywhere in the dialog; `Esc` closes
- Footer kbd hint: `⌘⏎ create · Esc cancel`
- A user-visible hint below the topic dropdown:
  - COLD + Standalone: `Standalone tasks won't appear in any topic's Active tasks owned rail.`
  - PROMOTE: `This task will appear in the source topic's Active tasks owned rail.`

**b) DELETE `src/ui/surfaces/chat/true-task/true-task-toggle.tsx`:**
- `git rm src/ui/surfaces/chat/true-task/true-task-toggle.tsx`
- Remove its import from composer.tsx

**c) STRIP `src/ui/surfaces/chat/composer.tsx`:**
- Remove `TrueTaskToggle` import + render
- Remove the `armed` state + the `onArmedChange` prop if it existed
- Remove the conditional `Open Task Form` / `Send` button-label flip — button always says `SEND`
- Placeholder reverts to `Message {employee}…`
- Composer now accepts a new `disabled?: boolean` prop. When `true`: textarea is `readOnly` (or wrapped div is `contentEditable={false}`), styling uses the new `.chat-composer--disabled` class, placeholder shows `Unarchive to send messages.`, send button is disabled

**d) TONE DOWN `src/ui/surfaces/chat/true-task/inline-task-card.tsx`:**
- Wrapper className stays `.inline-task-card` (CSS owns the visual change — see Task 1)
- Make `ref` a real link (`<a>` or `<button class="ref-chip">` with proper aria) — clicking should open the task in classic Paperclip in v1 OR show inline peek card in v2 (defer the peek to 4.2 per memory)
- Drop any rendering of a status PILL with background fill; the CSS now makes it outlined-only

**e) REWIRE `src/ui/surfaces/chat/promote-actions.tsx`:**
- Replace `confirm-dialog` invocation with `openTaskDialog({mode: 'promote', sourceMessage: {body, employee, time}, sourceTopic})`
- The Promote button now appears ON HOVER on agent reply bubbles (the visual is in chat.css via `.msg .actions`); the component just renders the button and wires the click

All TS, all conventions match existing code style.
  </action>
  <verify>
- `test -f src/ui/surfaces/chat/true-task/true-task-toggle.tsx` returns NOT FOUND
- `grep -c 'TrueTaskToggle' src/ui/surfaces/chat/composer.tsx` is 0
- `grep -c 'Standalone' src/ui/surfaces/chat/true-task/true-task-dialog.tsx` ≥ 1
- `grep -c "mode:\\s*'cold'\\|mode:\\s*'promote'" src/ui/surfaces/chat/true-task/true-task-dialog.tsx` ≥ 2
- `tsc --noEmit` clean
  </verify>
  <done>Dialog dual-mode, composer single-purpose, inline-task-card toned down, toggle file deleted.</done>
  <acceptance_criteria>
    - "test -f src/ui/surfaces/chat/true-task/true-task-toggle.tsx → REMOVED"
    - "grep -c 'TrueTaskToggle' src/ui/surfaces/chat/composer.tsx → 0"
    - "grep -c 'Standalone' src/ui/surfaces/chat/true-task/true-task-dialog.tsx → ≥1"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Wire archive panel + archived banner + cold-task originId — including the small worker decision</name>
  <read_first>
    - src/worker/handlers/chat-true-task.ts (Plan 04.1-02 — `chat.createTrueTask` handler signature)
    - src/worker/chat/true-task.ts (the shared createTrueTask helper)
    - src/worker/handlers/chat-topic-archive.ts (Plan 04.1-05 — chat.topic.archive)
    - src/worker/db/chat-topics-repo.ts (the listChatTopicTasksForTopic helper; needs a sibling listArchivedChatTopicsForEmployee)
    - migrations/0006_chat.sql + 0007_chat_topic_tasks.sql (existing schema for chat_topics including the archived flag)
  </read_first>
  <action>
**a) Worker contract decision: cold tasks.**

`chat.createTrueTask` (Plan 04.1-02) requires `topicIssueId: string`. Cold tasks have NO topic. Three options:
- **Option A (chosen):** Make `topicIssueId` accept `null`. When `null`: the helper uses `originId: cold-task:<userId>:<unix-ms>` (NOT `chat-task:...`); skips the marker-comment write (no topic to mark); skips the chat_topic_tasks side-table insert (not linked). The chat.taskOwned handler stays as-is and will NOT return cold tasks for any topicIssueId.
- Option B: Add a separate `chat.createColdTask` handler — clean separation but doubles the surface.
- Option C: Skip cold tasks entirely — operators always create via Promote — but that breaks Eric's workflow.

Ship Option A. Modify `src/worker/chat/true-task.ts` to handle `topicIssueId === null` per the contract above. Modify the action handler shape: `topicIssueId: string | null` (required field but accepts null).

Update `src/worker/handlers/chat-true-task.ts`:
- When `topicIssueId === null`: validate it's NOT a string-coercion thing (check `params.topicIssueId === null` literally, not falsy), pass `null` to the helper, return `{ ok: true, issueId, mode: 'cold' }`
- When `topicIssueId` is a string: existing behavior (Plan 04.1-02), return `{ ok: true, issueId, mode: 'chat-anchored' }`

Update `src/worker/chat/true-task.ts`:
- Add a `mode` derivation: `const isCold = input.topicIssueId === null`
- `originId`: `isCold ? \`cold-task:\${userId}:\${Date.now()}\` : \`chat-task:\${topicIssueId}:\${sourceCommentId ?? 'composer'}\``
- `parentId`: undefined either way (top-level)
- Marker comment: skip if `isCold`
- Side-table insert (Plan 04.1-05 retrofit): skip if `isCold`

Existing tests for chat.createTrueTask MUST CONTINUE TO PASS. Add new tests for the `topicIssueId: null` cold path.

**b) Archived-topics data handler.**

The archive panel needs to fetch the list of archived topics for the current employee + company. Today Plan 04.1-04's chat.messages handler scopes to a single topic; there's no list-archived endpoint.

Add a new data handler `chat.archivedTopics`:
- Input: `{ companyId, userId, employeeAgentId }`
- Output: `{ kind: 'archivedTopics', topics: Array<{ topicIssueId, topicId, title, messageCount, lastActiveAt, archivedAt }> }`
- Query: `SELECT topic_id, issue_id, title, last_activity_at, archived_at, /* message count via subquery or join */ FROM plugin_clarity_pack_cdd6bda4bd.chat_topics WHERE company_id = $1 AND employee_agent_id = $2 AND archived = true ORDER BY archived_at DESC`
- Opt-in gated via wrapDataHandler (T-04-15)
- Missing param: returns `{ error: '..._REQUIRED' }` (data handler convention)
- If `archived_at` column doesn't exist in chat_topics yet, add `migrations/0008_chat_topics_archived_at.sql` with `ALTER TABLE plugin_clarity_pack_cdd6bda4bd.chat_topics ADD COLUMN IF NOT EXISTS archived_at timestamptz DEFAULT NULL;` — additive, idempotent. AND update `setChatTopicArchived` in chat-topics-repo.ts to SET `archived_at = NOW()` when archiving and `archived_at = NULL` when un-archiving.

Register the new handler in `src/worker.ts` alongside `chat.taskOwned`.

**c) Wire archive panel in `index.tsx`.**
- Add state: `archivePanelOpen: boolean`, `archivedTopics: ArchivedTopic[] | null`
- Fetch on panel open: `ctx.data.invoke('chat.archivedTopics', { companyId, userId, employeeAgentId })` → set archivedTopics
- Pass to `<ArchivePanel open={archivePanelOpen} archivedTopics={archivedTopics ?? []} onClose={...} onOpenTopic={openArchivedTopic} onUnarchive={callUnarchive} />`
- `openArchivedTopic(topicIssueId)`: set `activeTopicId = topicIssueId`, set `activeTopicArchived = true`, close panel, scroll to top of messages
- `callUnarchive(topicIssueId)`: `ctx.actions.invoke('chat.topic.archive', {archived: false, topicIssueId, companyId, userId})` → refetch chat-topics + chat-archivedTopics + close panel + set archived=false on active topic if it was open

**d) Wire archived-banner in `message-thread.tsx`.**
- When `activeTopic.archived === true`, render `<ArchivedBanner ... />` at the top of `.messages`
- Composer receives `disabled={activeTopic.archived === true}`
- Topic strip renders archived-state topic with dashed border + 0.62 opacity + 📁 suffix (CSS already provides this from Task 1)
  </action>
  <verify>
- Cold-task path: an opted-in caller invoking chat.createTrueTask with `topicIssueId: null` produces an issue with originId starting `cold-task:`, no marker comment, no chat_topic_tasks row
- chat.archivedTopics data handler returns the archived topics, sorted newest-first
- Archive panel opens on +N pill click, populated with the fetched topics
- Row click opens the topic in archived state with the banner + disabled composer
- Unarchive button restores the topic to active strip
- tsc clean
- All existing Plan 04.1-02 / 04.1-05 tests still GREEN
  </verify>
  <done>Cold tasks work end-to-end; archive panel + banner wired; archived_at column added (if needed).</done>
  <acceptance_criteria>
    - "grep -c 'cold-task:' src/worker/chat/true-task.ts → ≥1"
    - "grep -c 'chat.archivedTopics' src/worker.ts → ≥1"
    - "grep -c 'ArchivedBanner' src/ui/surfaces/chat/message-thread.tsx → ≥1"
    - "grep -c 'ArchivePanel' src/ui/surfaces/chat/index.tsx → ≥1"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Move Diagnostics to actions row + rewire archived-topics pill + index.tsx full rewire</name>
  <read_first>
    - src/ui/surfaces/chat/index.tsx (the main chat shell — where head-actions currently mounts diagnostics)
    - src/ui/surfaces/chat/diagnostics-toggle.tsx (existing component — stays the same, just moves)
    - src/ui/surfaces/chat/archived-topics-pill.tsx (existing — currently does inline reveal; rewire to open panel)
    - src/ui/surfaces/chat/topic-strip.tsx (where the pill lives today)
  </read_first>
  <action>
**a) Move Diagnostics.**
- In `index.tsx`: REMOVE the `<DiagnosticsToggle ... />` from inside `.head-actions`
- Pass `diagnosticsOn` + `onDiagnosticsToggle` into `<ChatActionsRow ... />` so it renders the toggle in the actions row
- The `head-actions` div remains in markup but is now empty (could be removed if no other child planned for 4.x — but keep for now)

**b) Rewire `archived-topics-pill.tsx`.**
- Was: clicking inline-reveals archived topics in the strip (the old Plan 04.1-06 behavior)
- Now: clicking calls `props.onOpenArchivePanel()` — the pill becomes a simple button that opens the panel via the parent

**c) Rewire `index.tsx`:**
- Mount `<ChatActionsRow />` BETWEEN `<TopicStrip />` and `<MessageThread />`
- Mount `<ArchivePanel />` as a SIBLING of the chat shell (positioned absolutely so it can overlay the strip)
- Add the `archivePanelOpen` + `archivedTopics` state from Task 4
- Add the `⌘+T` global handler (or delegate it to ChatActionsRow which already has it)
- Add the `Escape` global handler to close the archive panel
- Ensure the archived banner renders inside MessageThread (already wired in Task 4)

**d) Topic strip pill count.**
- `<TopicStrip />` already shows topics; the `+N archived` pill is mounted inside it. Update to receive `archivedCount` as a prop (caller computes from data, NOT from the topics array currently being rendered).
- If `archivedCount === 0`, do not render the pill at all.
  </action>
  <verify>
- Diagnostics toggle renders in actions row, NOT in head-actions
- +N archived pill click opens the panel (verify via React Testing Library or by manually inspecting the click handler chain)
- index.tsx imports both ChatActionsRow and ArchivePanel
- tsc clean
  </verify>
  <done>Diagnostics is in actions row; pill opens panel; index.tsx fully rewired.</done>
  <acceptance_criteria>
    - "grep -c 'ChatActionsRow' src/ui/surfaces/chat/index.tsx → ≥1"
    - "grep -c 'ArchivePanel' src/ui/surfaces/chat/index.tsx → ≥1"
    - "grep -c 'DiagnosticsToggle' src/ui/surfaces/chat/index.tsx — must be 0 in the head-actions context (check the JSX tree position)"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 6: Tests — ≥25 new node:test cases across 7 UI test files</name>
  <read_first>
    - test/ui/chat-message-thread.test.mjs (existing — for style consistency)
    - test/worker/chat/chat-true-task.test.mjs (existing — for the mode='cold' new test cases)
    - test/worker/chat/chat-true-task.test.mjs + test/worker/chat/chat-promote.test.mjs (the existing handler-tier coverage that MUST CONTINUE TO PASS unchanged)
    - sketches/paperclip-fix-chat-true-task.html (the visual contract — derive expected DOM structure)
  </read_first>
  <action>
**Write 7 new test files** (paths in the files_modified list). Minimum case counts:

**a) `test/ui/chat-actions-row.test.mjs` (≥ 5 cases):**
- + Create task button is in DOM
- + Create task click invokes onCreateTask
- + New topic click invokes onNewTopic
- Diagnostics button has aria-pressed reflecting diagnosticsOn
- ⌘+T global shortcut invokes onCreateTask (simulate keydown)

**b) `test/ui/chat-archive-panel.test.mjs` (≥ 7 cases):**
- Renders 0 rows when archivedTopics is empty + shows empty state copy
- Renders N rows when archivedTopics has N items, sorted newest-archived-first
- Row click invokes onOpenTopic with the right topicIssueId
- Unarchive button click invokes onUnarchive with the right topicIssueId
- Unarchive button click does NOT trigger row click (stopPropagation)
- Search input filters by title (case-insensitive substring)
- View all link is a no-op stub that does NOT navigate (just logs warn)

**c) `test/ui/chat-archived-banner.test.mjs` (≥ 3 cases):**
- Renders with the right copy when archived=true
- Unarchive button click invokes onUnarchive
- Composer in same parent is in disabled state (this is an integration test — keep the assertion loose)

**d) `test/ui/chat-true-task-dialog.test.mjs` (≥ 6 cases):**
- COLD mode: title autofocused empty, topic dropdown defaults to Standalone, no FROM-MESSAGE block
- PROMOTE mode: title pre-filled from sourceMessage, topic defaults to source topic, FROM-MESSAGE block visible
- ⌘+Enter (Mac) submits from the title field
- Ctrl+Enter (Windows) submits from the body textarea
- Esc closes
- Submit with COLD mode + Standalone topic calls onCreate with topicIssueId: null

**e) `test/ui/chat-inline-task-card.test.mjs` (≥ 2 cases):**
- Card renders with left-rule (border-left:2px) — check computed style or className indicator
- ref-chip renders as anchor or button, NOT span

**f) `test/ui/chat-composer.test.mjs` (≥ 4 cases):**
- TrueTaskToggle is NOT in DOM (regression guard)
- Placeholder is `Message {employee}…`
- Send button label is `SEND` (NOT `OPEN TASK FORM`)
- When prop `disabled=true`: textarea is read-only AND placeholder is `Unarchive to send messages.`

**g) `test/ui/chat-message-thread.test.mjs` extension (≥ 3 new cases on top of existing):**
- → Promote button renders on agent reply bubbles (opacity 0 default, 1 on hover — check className presence)
- → Promote button does NOT render on user bubbles
- When activeTopic.archived === true: ArchivedBanner is rendered at the top of messages

Use the same test idiom as existing UI tests (likely React Testing Library + JSDOM). If the project doesn't have RTL, use the existing inline-render-and-query pattern from prior chat tests.

Run the full suite: `node --test 'test/**/*.test.mjs'`. Must pass 100%. Suite count baseline 1091; target ≥ 1116 after this task.
  </action>
  <verify>
- All 7 test files exist
- Each meets its minimum case count
- Full suite passes
- Baseline 1091 → ≥ 1116 (≥ +25)
  </verify>
  <done>≥25 new tests across 7 files; full suite GREEN.</done>
  <acceptance_criteria>
    - "Files exist: test/ui/chat-actions-row.test.mjs, chat-archive-panel.test.mjs, chat-archived-banner.test.mjs, chat-true-task-dialog.test.mjs, chat-inline-task-card.test.mjs, chat-composer.test.mjs, chat-message-thread.test.mjs"
    - "Full suite GREEN: node --test 'test/**/*.test.mjs' exits 0"
    - "New test count ≥ 25 (baseline 1091 → ≥ 1116)"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 7: Version bump (both sources), build, pack, commit</name>
  <read_first>
    - package.json (current version: 0.8.0)
    - src/manifest.ts (lines ~234, current hardcoded version: 0.8.0)
    - memory plugin-version-bump-two-sources (the lesson learned in Plan 04.1-06 — DON'T just bump package.json)
    - scripts/build-worker.mjs + scripts/build-ui.mjs (existing build commands)
  </read_first>
  <action>
**a) Bump BOTH version sources in a SINGLE commit.**

Edit `package.json`: `"version": "0.8.0"` → `"version": "0.8.1"`
Edit `src/manifest.ts` line ~234: `version: '0.8.0',` → `version: '0.8.1',`

Commit message: `chore(04.1-08): bump version 0.8.0 → 0.8.1 (gap-closure on UI redesign)`

**b) Rebuild.**
```bash
node scripts/build-worker.mjs
node scripts/build-ui.mjs
npx tsc --project tsconfig.manifest.json
```

Verify `dist/manifest.js` contains `version: '0.8.1'` (or however the build emits the literal): `grep -c "0.8.1" dist/manifest.js` should be ≥ 1.

**c) Run the full test suite + tsc.**
```bash
npx tsc --noEmit
node --test 'test/**/*.test.mjs'
```

Must both pass.

**d) Pack.**
```bash
rm -f clarity-pack-0.8.1.tgz clarity-pack-0.8.0.tgz
npm pack
```

Capture the npm shasum from the output. Capture the sha256 via PowerShell or `sha256sum clarity-pack-0.8.1.tgz`.

**e) Commit the build outputs?** dist/ is gitignored per typical convention — verify with `cat .gitignore | grep dist`. If gitignored, do NOT commit dist/. If tracked, commit it.

Final commit if tests + build all pass: should be just the source changes from earlier tasks. The version bump commit is its own atomic commit per (a).
  </action>
  <verify>
- `grep -c '"version": "0.8.1"' package.json` is 1
- `grep -c "version: '0.8.1'" src/manifest.ts` is 1
- `grep -c "0.8.1" dist/manifest.js` ≥ 1
- `test -f clarity-pack-0.8.1.tgz` is true
- All tests pass + tsc clean
  </verify>
  <done>Version bumped in both sources, build clean, tarball packed.</done>
  <acceptance_criteria>
    - "grep -c '\"version\": \"0.8.1\"' package.json → 1"
    - "grep -c \"version: '0.8.1'\" src/manifest.ts → 1"
    - "test -f clarity-pack-0.8.1.tgz → packed"
    - "grep -c \"0.8.1\" dist/manifest.js → ≥1"
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 8: Operator visual-fidelity drill on Countermoves</name>
  <what-built>
    The redesigned chat surface per `sketches/paperclip-fix-chat-true-task.html` — single-purpose composer, actions row with `+ Create task` primary button, dual-mode task dialog, archive panel (dropdown with click-to-open rows), read-only archived banner, toned-down inline task card, bumped contrast tokens, widened right rail. Plus the cold-task originId path (no topic anchoring) and the new `chat.archivedTopics` data handler. Packed as `clarity-pack-0.8.1.tgz`.
  </what-built>
  <how-to-verify>
    Eric: walk the 9-point visual-fidelity drill on Countermoves. Each block below is self-contained (per memory `feedback_vps-commands-self-contained`).

    **Deploy:**

    On Windows (in this repo root):
    ```powershell
    scp -i $HOME\.ssh\countermoves_vps_ed25519 ".\clarity-pack-0.8.1.tgz" eric@82.29.197.74:/home/eric/
    ssh -i $HOME\.ssh\countermoves_vps_ed25519 eric@82.29.197.74
    ```

    On the VPS (re-export env every time):
    ```bash
    export DB_URL="$(sudo grep -E '^(DB_URL|DATABASE_URL)=' /etc/paperclip/db.env | head -1 | cut -d= -f2-)"
    export PAPERCLIP_API_URL=http://localhost:3100
    export PAPERCLIP_API_KEY="$(jq -r '.credentials["http://localhost:3100"].token' ~/.paperclip/auth.json)"
    export PAPERCLIP_COMPANY_ID=62b33a78-4f4a-4ab7-9977-a27be86f9853

    # Bookend snapshot BEFORE
    cd ~/clarity-pack
    node scripts/safety/cli.mjs snapshot --db-url "$DB_URL"

    # Uninstall + reinstall via helper
    cd ~/paperclip
    pnpm paperclipai plugin uninstall clarity-pack
    ~/clarity-pack/scripts/install-helper.sh /home/eric/clarity-pack-0.8.1.tgz

    # Verify version label
    pnpm paperclipai plugin list
    #  ↳ MUST show: key=clarity-pack  status=ready  version=0.8.1  id=0d4fc40a-… (UUID preserved)

    # Bookend snapshot AFTER
    cd ~/clarity-pack
    node scripts/safety/cli.mjs snapshot --db-url "$DB_URL"
    ```

    **Open `https://countermoves.gl3group.com/COU/chat` and walk these in order. Paste back what each step does (verbatim observations beat thumbs-up/down):**

    1. **Actions row** — directly below the topic strip, see `[+ Create task] (gold primary) [+ New topic] (ghost) ··· spacer ··· [⏿ Diagnostics] (ghost)`. Tooltip on hovering `+ Create task` shows `⌘T` or `Ctrl+T`. The composer has NO `↗ Send as task` toggle.

    2. **Cold task creation** — click `+ Create task`. Dialog opens. Heading: `Create a task`. Title autofocused + empty. Assignee dropdown defaults to current chat employee (CEO). Topic dropdown defaults to `Standalone (not linked to any topic)`. Hint below dropdown: `Standalone tasks won't appear in any topic's Active tasks owned rail.` No FROM-MESSAGE block. Type a title. Hit `⌘+Enter` (or `Ctrl+Enter`). Dialog closes. Verify in classic Paperclip `/COU/issues`: the new task exists, is top-level (no parent), is assigned to CEO, AND has `originKind=plugin:clarity-pack` + `originId=cold-task:…` (not chat-task:). Verify the task does NOT appear in any topic's `Active tasks owned` rail.

    3. **⌘+T shortcut** — anywhere on the chat page (NOT inside an input), press `⌘+T` (Mac) or `Ctrl+T` (Windows). Cold dialog opens. Press `Esc`. Closes. (NB: if browser intercepts `Ctrl+T` for new tab, fall back to a different shortcut and flag this for Plan 04.1-09.)

    4. **Promote from message** — hover over any CEO reply bubble. The `→ Promote to task` action appears at top-right with fade-in. Click. Dialog opens. Heading: `Promote message to task`. Title pre-filled from message. Topic dropdown defaults to current source topic (CHT-1117). `FROM THIS MESSAGE · CEO · {time}` block renders below the textarea with the full message body. Hint below dropdown: `This task will appear in the source topic's Active tasks owned rail.` Click `Create task`. Dialog closes. Verify: new task in Issues list assigned to CEO, with `originId=chat-task:<topic>:<comment>`. Verify it appears in the topic's `Active tasks owned` rail in the right pane.

    5. **Inline task card** — in the thread, after creating either kind, see the toned-down provenance card: left gold rule, no full border, dim title (in --ink-2), gold underlined BEAAA-NNN link, outlined-only status pill. Should read as "X happened" event, NOT a hero element.

    6. **Right rail readable** — at browser zoom 100%, scroll the right rail. Labels `Active tasks owned`, `Archive this topic`, `Search this employee's chats`, `Pause heartbeat`, `Recent attachments` all render WITHOUT truncation. The +N archived pill (if present) is at the right end of the topic strip.

    7. **Archive panel + click-to-open** — click the `+N archived` pill. Dropdown panel opens anchored below the strip. Header: `ARCHIVED TOPICS` + ✕. Search input filters by title in real-time. Each row shows: title, employee, message count, last-active relative time, archive date. Hover any row: cursor pointer, background tint, `↵ Click to open · read-only` hint appears, `Unarchive` button surfaces on right. Click the ROW (not the button). The archived topic loads in the main thread with the read-only banner at top. Topic strip shows the topic at 0.62 opacity with `📁` suffix. Composer is dashed-border with placeholder `Unarchive to send messages.` Try to type — input is read-only. Click the `Unarchive` button in the banner. Topic returns to active strip. Composer re-enables.

    8. **Unarchive from panel hover** — open archive panel again, hover a row, click `Unarchive` directly. Topic returns to active strip WITHOUT first opening it in read-only state. Verify in classic Paperclip that the host topic-issue's status is STILL `in_progress` (D-10 invariant — unchanged across archive AND unarchive).

    9. **Diagnostics ON + host-stuck banner (if provokable)** — click `⏿ Diagnostics`. State flips to solid + label `⏿ Diagnostics ON`. Next 15s poll shows system_notice rows inline with the new dashed-border + Geist Mono styling. If a structured-notice exists, the typed metadata sections render as collapsible details. Click off → noise hidden. If any topic is host-stuck (one of the `[SPIKE 04.1]`-tagged from Wave 1): select it. Sticky banner at top with `⚠ TOPIC STUCK HOST-SIDE` + body + ONE `Open in classic Paperclip` link. NO retry, NO auto-recover.

    **Pass criteria — type ONE of:**
    - `approved — Plan 04.1-08 closure unblocked` (all 9 PASS)
    - `approved-with-followups: <list>` (minor visual gaps; Phase 4.2 polish list updated)
    - `DO NOT PASS — <root cause>` (structural break — route to a NEW Plan 04.1-09 gap closure)

    Note: Plan 04.1-07 closure can advance after `approved` lands. Plan 04.1-09 only gets a roadmap entry if you select DO NOT PASS.
  </how-to-verify>
  <resume-signal>Type "approved — Plan 04.1-08 closure unblocked" or the alternative.</resume-signal>
</task>

</tasks>

<output>
After Eric returns `approved`, write `04.1-08-SUMMARY.md` per the GSD template. Required sections beyond the standard:

- **Plan-impact callouts**: list which Plan 04.1-06 files were SUPERSEDED (so future maintainers know the lineage).
- **4.2 deferral cross-reference**: explicit reference to memory `phase-4.2-deferred-from-4.1` and the no-op stubs left in code (the `View all archived →` click handler, the paused-agent banner not yet added).
- **Two memory updates required after this plan ships:**
  1. New project memory `phase-4.1-closure-state.md` describing what 4.1 finally shipped (so 4.2 has the right baseline)
  2. Update memory `feedback_vps-commands-self-contained` if any new VPS quirk surfaced during the drill (none expected, but capture if it happens)

Commit the SUMMARY + STATE.md + ROADMAP.md updates as the final commit. Mark Plan 04.1-08 complete in ROADMAP (use the `:check_marked:` notation per existing convention).
</output>
