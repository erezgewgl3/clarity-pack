---
phase: 04.2-reader-chat-bridge
plan: 03
type: execute
wave: 1
depends_on: ["04.2-02"]
gap_closure: true
autonomous: false
requirements: [RCB-01, RCB-02, RCB-03, RCB-04, RCB-05, RCB-06, RCB-07]
version_target: "0.9.2"
user_setup: []
files_modified:
  - scripts/probes/carrier-survival.mjs
  - src/ui/surfaces/chat/deep-link.mjs
  - src/ui/surfaces/chat/deep-link.d.mts
  - src/ui/surfaces/reader/continue-in-chat-button.tsx
  - src/ui/surfaces/reader/reverse-topics-link.tsx
  - src/ui/surfaces/chat/index.tsx
  - test/ui/continue-in-chat-deeplink-contract.test.mjs
  - test/ui/chat-url-params.test.mjs
  - test/ui/continue-in-chat-button.test.mjs
  - test/ui/reverse-topics-link.test.mjs
  - test/manifest/chat-capabilities.test.mjs
  - package.json
  - src/manifest.ts
  # Conditional (only if Task 1 probe selects the worker-handoff carrier — branch documented in Task 2):
  # - migrations/0010_chat_handoffs.sql
  # - src/worker/db/chat-handoffs-repo.ts
  # - src/worker/handlers/chat-handoff-claim.ts
  # - src/worker.ts

tags: [react, reader, chat, deep-link, plugin-sdk, host-navigation, carrier, empirical-probe, regression-test, fake-vs-production]

must_haves:
  truths:
    - "The carrier chosen for the Reader→Chat deep link is documented in the SUMMARY with the LIVE-HOST observation that justifies it — actual URL-bar value, actual useHostLocation snapshot fields, actual sessionStorage state captured after a real cross-route navigation. The justification cites observed runtime behaviour, NEVER the SDK type docstring."
    - "After Tasks 1-4, clicking ContinueInChatButton on a real Paperclip Reader surface navigates to /<prefix>/chat AND the chat surface consumes the deep-link payload — landing the operator on the source topic with the source comment flash-highlighted (existing-topic route) OR on the pre-seeded New Topic dialog (new-topic-needed route). The chat surface's empty-state generic landing is the failure mode the fix eliminates."
    - "The chat surface consumes the deep-link payload on mount AND clears it immediately afterwards (via replace navigation, sessionStorage delete, or worker-handoff claim — whichever channel Task 1 selected) so that a manual page refresh does NOT re-trigger the dialog or the flash."
    - "The regression test for the Reader→Chat contract uses EITHER (a) the live paperclip-plugin-dev-server runtime, OR (b) a fake whose behavior model is justified inline by reading node_modules/@paperclipai/plugin-sdk/dist/ui/{runtime.js,hooks.js} and/or the Task 1 probe output. NO assertion in the new test cites a dist/ui/types.d.ts docstring as its source. The prior D1-D6 docstring-derived contract test (test/ui/continue-in-chat-deeplink-contract.test.mjs) is either deleted or restructured so its docstring-derived router-fake assertions cannot regress."
    - "Version bumped to 0.9.2 in BOTH package.json AND src/manifest.ts (the host reads dist/manifest.js built from src/manifest.ts — per MemPalace clarity_pack/runbook `plugin-version-bump-two-sources`)."
    - "`clarity-pack-0.9.2.tgz` is packed; tarball byte size + sha256 recorded for the operator drill bookend and the SUMMARY."
    - "Full test suite GREEN — baseline 1323 (end of Plan 04.2-02); Plan 04.2-03 replaces D1-D6 with the new empirically-grounded contract test and may add probe assertions; expected suite size within ±10 of (baseline + new tests). 0 failures; `tsc --noEmit` clean; `node scripts/check-css-scope.mjs` exit 0."
    - "The Countermoves snapshot-bookended operator re-drill on 0.9.1→0.9.2 PASSES drill paths 1-5 (assigned task / chat-spawned task / cold-task / chat-topic-issue / no-assignee — see VERIFICATION Drill Coverage table) AND the RCB-07 pre-0009-topic UI spot-check (a topic with no origin_issue_id renders with no `About…` chip and no error). The known Path 2 fixture is topic id `e7b7fee8-b432-4422-8a1c-1bb3043a9d43` on COU-2215."
    - "The 0.9.1→0.9.2 upgrade is bookended by a verified Postgres + filesystem snapshot taken immediately before, with a rehearsed-rollback path confirmed (CLAUDE.md bookended-by-snapshots rule). Plugin-namespace row counts are byte-identical across the non-destructive uninstall/install (COEXIST #6)."
  artifacts:
    - path: "scripts/probes/carrier-survival.mjs"
      provides: "Empirical carrier-survival probe (or its captured output recorded inline in 04.2-03-SUMMARY.md) — documents which channels survive the host's navigate()→useHostLocation handoff."
      contains: "carrier"
    - path: "src/ui/surfaces/chat/deep-link.mjs"
      provides: "The contract module from 04.2-02 with its carrier swapped to the empirically-confirmed channel (URL hash / sessionStorage / worker-handoff). Public buildChatDeepLink / buildTopicDeepLink / parseChatDeepLink API surface unchanged so the three consumers do not need to re-thread."
      contains: "buildChatDeepLink"
    - path: "test/ui/continue-in-chat-deeplink-contract.test.mjs"
      provides: "Empirically-grounded Reader→Chat deep-link contract regression test — replaces the prior docstring-derived D1-D6 fake. Cites runtime.js / hooks.js source lines or the Task 1 probe output as the source of every behavioural assertion."
      contains: "useHostNavigation"
    - path: "package.json"
      provides: "Version 0.9.2."
      contains: "0.9.2"
    - path: "src/manifest.ts"
      provides: "Manifest version literal 0.9.2 (the host-read source)."
      contains: "0.9.2"
  key_links:
    - from: "src/ui/surfaces/reader/continue-in-chat-button.tsx"
      to: "src/ui/surfaces/chat/index.tsx"
      via: "deep-link.mjs (carrier swap from `state` to the Task-1-confirmed channel)"
      pattern: "buildChatDeepLink|parseChatDeepLink"
    - from: "src/ui/surfaces/reader/reverse-topics-link.tsx"
      to: "src/ui/surfaces/chat/index.tsx"
      via: "deep-link.mjs (same contract; same carrier as the Continue button)"
      pattern: "buildTopicDeepLink"
    - from: "scripts/probes/carrier-survival.mjs"
      to: "Task 2 carrier selection"
      via: "live-host observation recorded in probe output"
      pattern: "SURVIVES|STRIPPED"
---

<objective>
**Second gap-closure for Phase 4.2 — closes GAP-RCB-03-CARRIER.** Plan 04.2-02 shipped 0.9.1 and closed GAP-RCB-01-STYLING (operator-confirmed on Countermoves 2026-05-23 — the gold PRIMARY Continue button is visible on the Reader surface). But the load-bearing RCB-03 deep-link is still non-functional: 04.2-02 swapped the broken `?query` carrier from 04.2-01 for an equally-broken `{ state }` carrier. Conclusive empirical evidence from the Countermoves drill 2026-05-23 on assigned task `COU-2215`:

- **URL bar after click:** bare `countermoves.gl3group.com/COU/chat` — no `?...` (the `?query` tail was stripped by the host's `resolveHref()` company-prefix step).
- **`JSON.stringify({state: history.state})` after click:** `{"state":{"usr":null,"key":"vtw64gqp"}}` — react-router's user-state envelope; `usr` is the slot where the host would forward our `clarityDeepLink` payload if it didn't drop the `state` argument before reaching react-router's `useNavigate`. `usr` is `null` → the host's `navigate` wrapper stripped it.

The Paperclip host wrapper around `useHostNavigation().navigate()` strips BOTH carriers on cross-route navigation. The 04.2-02 contract test (D1-D6) passed because its router fake was modelled from the SDK's `dist/ui/types.d.ts` docstring (which says `state` is "forwarded by the host router") — but the live host strips it. **Fake-vs-production drift, one structural layer deeper than 04.2-01.** Lesson filed: `clarity_pack/runbook` `router-fake-vs-production-host`.

**This plan ships version 0.9.2** and closes GAP-RCB-03-CARRIER by (a) empirically probing the live host to find a carrier it DOES preserve, (b) implementing the chosen carrier inside the existing `src/ui/surfaces/chat/deep-link.mjs` module (the contract single-source-of-truth design is correct — only the carrier inside it needs swapping), (c) writing a new regression test grounded in observed runtime behaviour (not docstring intent), (d) bumping the version in both required source-of-truth files and packing the tarball, and (e) operator re-drilling the full 5-path Reader↔Chat Bridge + the RCB-07 pre-0009-topic UI spot-check on live Countermoves under a snapshot bookend.

Closure of GAP-RCB-03-CARRIER unblocks:
- **RCB-03** (load-bearing — the chat surface honors the deep-link payload).
- **RCB-05** (the topic strip "About <COU-NNNN> ↗" backlink chip — built in 04.2-01, blocked behind RCB-03).
- **RCB-06** (the Reader "N conversations about this issue ↗" reverse-topics popover — UI element rendering was already operator-confirmed on `COU-2215` showing real `topicsForIssue` data; navigation is the missing half).
- **The deferred drill paths 3-5** (cold-task / chat-topic-issue / no-assignee — they share the carrier with paths 1-2 and the operator stopped after Path 1's failure).
- **The RCB-07 pre-0009-topic UI spot-check** (DB-layer verified twice; UI spot-check never drilled because every prior drill stopped at GAP-RCB-03).

On a passing drill: RCB-01..RCB-07 flip to `Implemented` in REQUIREMENTS.md, Phase 4.2 closes COMPLETE & VERIFIED at version 0.9.2.

Wave: 1 (single plan; tasks run in order — Task 1 probes, Task 2 implements the carrier baked in by Task 1's result, Task 3 writes the empirically-grounded contract test, Task 4 bumps the version + packs, Task 5 is the operator re-drill). `autonomous: false` — Task 1 requires human inspection of the probe output to choose the carrier, and Task 5 is the live operator drill on Countermoves.

Output:
- `scripts/probes/carrier-survival.mjs` — the empirical carrier-survival probe (or its captured output, depending on the probe shape Task 1 picks).
- `src/ui/surfaces/chat/deep-link.mjs` + `deep-link.d.mts` — carrier swapped to the empirically-confirmed channel; public API unchanged.
- `src/ui/surfaces/reader/continue-in-chat-button.tsx` + `src/ui/surfaces/reader/reverse-topics-link.tsx` + `src/ui/surfaces/chat/index.tsx` — wiring updates ONLY if the new carrier requires a different `navigate()` call shape (e.g. URL hash → `navigate('/COU/chat#h=…')` keeps the same shape; sessionStorage → adds a write-on-click + read-on-mount pair; worker-handoff → adds a usePluginAction + usePluginData pair).
- `test/ui/continue-in-chat-deeplink-contract.test.mjs` — NEW empirically-grounded regression test (replaces the docstring-derived D1-D6).
- `test/ui/chat-url-params.test.mjs` + `test/ui/continue-in-chat-button.test.mjs` + `test/ui/reverse-topics-link.test.mjs` + `test/manifest/chat-capabilities.test.mjs` — extended where the contract or version changes.
- `src/manifest.ts` + `package.json` — version 0.9.1 → 0.9.2; `clarity-pack-0.9.2.tgz` packed.

This plan does NOT modify, replan, or overwrite `04.2-01-PLAN.md` or `04.2-02-PLAN.md` — those plans shipped 0.9.0 / 0.9.1 and are the historical record. No new migration is needed for the URL-hash or sessionStorage carriers; the worker-handoff branch (only if both UI carriers fail Task 1's probe) ships migration 0010 — see Task 2 below.
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.43.5/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.43.5/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md
@.planning/phases/04.2-reader-chat-bridge/04.2-02-SUMMARY.md
@CLAUDE.md

<gap_source>
The single gap closed by this plan is **GAP-RCB-03-CARRIER** in `.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md` (status `failed`; severity `high`; load-bearing; blocks RCB-03 functional + RCB-05 + RCB-06 navigation). The VERIFICATION report's `fix target` section enumerates three priority-ranked carrier candidates (URL hash → sessionStorage → worker-handoff row) and the `NEW REQUIRED test discipline` section spells out the empirically-derived-fake mandate. Read both before designing Task 1's probe and Task 3's regression test. The MemPalace drawer `clarity_pack/runbook` `router-fake-vs-production-host` codifies the test discipline as a permanent guardrail.

GAP-RCB-01-STYLING is RESOLVED (closed by Plan 04.2-02 Task 1, operator-confirmed 2026-05-23). It is NOT re-addressed by this plan. The Path-1 drill on Countermoves 2026-05-23 confirmed the gold PRIMARY button renders correctly.
</gap_source>

<interfaces>
The SDK navigation/location contract from `node_modules/@paperclipai/plugin-sdk/dist/ui/types.d.ts` (Task 3 must treat this as the TYPE SHAPE only — actual behavioural assertions must cite runtime.js, hooks.js, or the Task 1 probe output, NEVER these docstrings):

```
interface HostLocation {
  pathname: string;
  search: string;
  hash: string;
  state?: unknown;
}
interface HostNavigationOptions { replace?: boolean; state?: unknown; }
interface HostNavigation {
  resolveHref(to: string): string;
  navigate(to: string, options?: HostNavigationOptions): void;
  linkProps(to: string, options?: HostNavigationLinkOptions): HostNavigationLinkProps;
}
```

The SDK hooks themselves (`node_modules/@paperclipai/plugin-sdk/dist/ui/hooks.js`, 194 lines) are thin stubs that pull the real implementation off `globalThis.__paperclipPluginBridge__.sdkUi[name]` via `getSdkUiRuntimeValue` (`runtime.js`, 29 lines). The host supplies the real `navigate` / `useHostLocation` / `resolveHref` — the SDK ships no behaviour to inspect. Task 1's probe must therefore observe the host's behaviour LIVE (via `paperclip-plugin-dev-server` and/or against the running Countermoves Paperclip instance) — there is no SDK runtime to inspect bytes of beyond what 04.2-02 already did.

`HostLocation.hash` is a first-class field on the location snapshot. If hash is preserved end-to-end through cross-route navigation, that is the cheapest fix. Worth probing first.

Existing chat surface deep-link reader (`src/ui/surfaces/chat/index.tsx` lines 281-370): `const { search, pathname, state: locationState } = useHostLocation();` followed by `parseChatDeepLink({ search, state: locationState })` in a `useEffect`, with `nav.navigate(pathname, { replace: true })` clearing both consumed channels afterwards. If the carrier swap is URL hash, the destructure adds `hash` and `parseChatDeepLink`'s argument grows a `hash` field. If sessionStorage, the destructure stays as-is but a new `sessionStorage.getItem` + `sessionStorage.removeItem` pair appears before the parse call. If worker-handoff, an opt-in-guarded `usePluginData('chat.handoff.claim', ...)` call replaces or augments the location read.

Plugin page routes are `/<companyPrefix>/<routePath>` — e.g. `/COU/chat` (MemPalace `clarity-pack-plugin-page-routes`). The plugin UI is same-origin trusted JavaScript (CLAUDE.md trust model), so `window.sessionStorage` is available from chat/index.tsx and continue-in-chat-button.tsx alike. The trust model also makes `window.location.hash` readable as a defensive cross-check against `useHostLocation().hash` if Task 1 finds them disagree.

The repo runs tests with `node --test "test/**/*.test.mjs"` — Node's runner does NOT load `.tsx` and there is NO React render harness. The existing pattern for "contract" tests is to extract pure logic into `.mjs` (the 04.2-02 `deep-link.mjs` is exactly this) and test those pure functions against each other. A "live-host" regression test (paperclip-plugin-dev-server boot) would require new test infrastructure — Task 3 must determine whether that infrastructure is feasible against the existing `node --test` runner, and if not, pick the empirically-derived-fake pattern with the fake's behaviour cited inline against the probe output.

Plan 04.2-02 introduced `migrations/0010_...` as a HYPOTHETICAL future addition only if worker-handoff is needed. Current shipped migrations end at `migrations/0009_chat_topics_origin_issue.sql`. The plugin's host runs SQL files in `migrations/` in filename order on plugin install — the install IS the schema push for this project. Confirm by re-reading the existing 0009 plumbing if the worker-handoff branch is selected.
</interfaces>

<test_discipline_mandate>
From `04.2-VERIFICATION.md` Lessons §1 and MemPalace `clarity_pack/runbook` `router-fake-vs-production-host`:

> A contract test against a host capability MUST exercise either (a) the live host runtime via `paperclip-plugin-dev-server`, OR (b) a fake empirically derived from the host's actual implementation by reading `node_modules/@paperclipai/plugin-sdk/dist/ui/runtime.js` until you can write the fake from observed behavior. A fake derived from type docstrings is a re-statement of assumptions, not a test.

Two structural failures of this discipline have already cost two drill cycles:
- 04.2-01: two independently-mocked hooks let the cross-hook contract drift apart.
- 04.2-02: ONE shared router fake — structurally better — but the fake's behavior was modelled from the SDK type docstring, NOT from observed runtime behaviour. The fake honoured `state` because the docstring said `state` is "forwarded by the host router." The live host strips it.

**This plan's regression test (Task 3) must not slip a third time.** Every behavioural assertion in the new test must cite either (a) a line of `node_modules/@paperclipai/plugin-sdk/dist/ui/runtime.js` or `hooks.js`, (b) a line of the Task 1 probe output, or (c) a captured live-host observation. No assertion may cite `node_modules/@paperclipai/plugin-sdk/dist/ui/types.d.ts` as its sole source. The prior `test/ui/continue-in-chat-deeplink-contract.test.mjs` must be either deleted (preferred — its docstring-derived D1-D6 fake is exactly the pattern this discipline forbids) or restructured so its docstring-derived router-fake assertions are removed (encode/decode-level assertions over `buildChatDeepLink`/`parseChatDeepLink` round-tripping may be kept if they do not assert anything about how the HOST carries the payload).
</test_discipline_mandate>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Empirical carrier-survival probe — pick the channel the live host preserves</name>
  <files>scripts/probes/carrier-survival.mjs</files>
  <read_first>
    - node_modules/@paperclipai/plugin-sdk/dist/ui/types.d.ts — the type shape for HostLocation (pathname / search / hash / state) and HostNavigation (resolveHref / navigate / linkProps); read it ONCE to know what fields the host snapshot exposes. Do NOT use any docstring as a behavioural assertion source — see <test_discipline_mandate>.
    - node_modules/@paperclipai/plugin-sdk/dist/ui/hooks.js — confirm (under 30 seconds) that the SDK hooks are thin `getSdkUiRuntimeValue(name)` stubs and the host supplies the real implementation. There is no SDK runtime behaviour to inspect — the probe must observe the LIVE host.
    - node_modules/@paperclipai/plugin-sdk/dist/ui/runtime.js — same; confirms the host-bridge indirection through `globalThis.__paperclipPluginBridge__.sdkUi`.
    - test/ui/continue-in-chat-deeplink-contract.test.mjs — the prior 04.2-02 contract test. Read it to understand exactly what shape of fake was wrong (docstring-derived) and what shape of assertions must be replaced in Task 3. This file is the cautionary tale.
    - .planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md — the GAP-RCB-03-CARRIER entry, with the priority-ranked carrier candidates (URL hash → sessionStorage → worker-handoff) and the empirical evidence from the 2026-05-23 drill (bare URL bar, `state.usr === null`).
    - .planning/phases/04.2-reader-chat-bridge/04.2-02-SUMMARY.md — the Task 2 "what was built (and what it pinned, in test)" + "Empirical production verdict" sections; the deep-link.mjs design rationale (carrier-vs-contract split); the "Lesson" block.
    - The MemPalace drawer `router-fake-vs-production-host` (wing=clarity_pack, room=runbook) — query via `mempalace_search query="router fake production host" wing="clarity_pack"` BEFORE designing the probe. It codifies the empirically-derived-fake discipline as a permanent guardrail and the planner of Plan 04.2-03 is named in the drawer.
    - The MemPalace drawer `clarity-pack-plugin-page-routes` (wing=clarity_pack, room=runbook) — confirms plugin page routes are `/<companyPrefix>/<routePath>`, e.g. `/COU/chat`. The probe's navigation under test must use this exact shape.
    - src/ui/surfaces/chat/deep-link.mjs lines 1-60 — the existing contract module's "why this module exists" header. This is what's being modified; the carrier is the only thing changing.
    - CLAUDE.md trust-model paragraph + bookended-by-snapshots paragraph — confirms plugin UI is same-origin trusted JS (so sessionStorage / window.location.hash are accessible).
  </read_first>
  <action>
**Goal:** determine empirically which channel the live Paperclip host preserves end-to-end through a cross-route `useHostNavigation().navigate()` → `useHostLocation()` handoff. Probe candidates in the verification's priority order; stop at the cheapest channel that survives.

**Step 1 — Determine the probe vehicle.** Two viable shapes; pick the one the existing repo infrastructure supports without inventing new tooling:

  - **Probe shape A (preferred if feasible) — `paperclip-plugin-dev-server` boot script:** if the SDK or repo ships a `paperclip-plugin-dev-server` binary or a way to boot a local Paperclip instance with the plugin loaded, write `scripts/probes/carrier-survival.mjs` as a Node script that boots the dev server, drives a real navigation (synthetic click → assertion), and prints the post-navigation `useHostLocation()` snapshot for each carrier. Check `package.json` scripts + `node_modules/@paperclipai/plugin-sdk/package.json` bin entries before assuming this exists.
  - **Probe shape B (fallback) — in-Countermoves probe page:** if Probe shape A is not feasible without new infrastructure, write a temporary plugin UI probe (a hidden surface or a debug section in the chat surface) that emits a navigation with each carrier in turn and prints `JSON.stringify({ pathname, search, hash, state, sessionStorageKeys: Object.keys(sessionStorage), windowHash: window.location.hash })` to `console.log` (or to the chat surface as a `<pre>` block the operator copies into the probe-output file). The probe is shipped in a temporary tarball used ONLY for the probe drill; do NOT include it in the final 0.9.2 pack.

Pick Shape A if any boot path exists; otherwise Shape B. Document the choice in the probe output.

**Step 2 — Probe in priority order:**

  1. **URL hash** (`/COU/chat#h=<base64-encoded-payload>`):
     - Emit `navigate('/COU/chat#h=' + encodeURIComponent(btoa(JSON.stringify(payload))))` from the Reader surface.
     - On the chat surface, capture `useHostLocation().hash` AND `window.location.hash` AND `useHostLocation().pathname` immediately on mount.
     - SURVIVES iff `useHostLocation().hash === '#h=...'` (or `window.location.hash` matches and `useHostLocation().hash` agrees) AND the payload decodes back.
     - STRIPPED iff `useHostLocation().hash === ''` or the encoded payload is missing.
  2. **sessionStorage handoff** — ONLY if URL hash STRIPPED:
     - On click: `sessionStorage.setItem('clarity-chat-deep-link', JSON.stringify(payload))`, then `navigate('/COU/chat')`.
     - On chat-surface mount: capture `sessionStorage.getItem('clarity-chat-deep-link')`; if present, `sessionStorage.removeItem(...)` after the value is read.
     - SURVIVES iff the chat-surface mount reads the payload string back.
     - NOT VIABLE only if sessionStorage is somehow blocked under the same-origin trust model (extremely unlikely — flag if so).
  3. **Worker-side handoff row** (`chat_handoffs` plugin-namespace table or in-memory worker singleton) — ONLY if both above fail:
     - The probe for this shape is heavier (requires a real worker handler + migration draft) — defer the FULL probe to Task 2's worker-handoff branch implementation; for Task 1, record NOT PROBED with the rationale "URL hash + sessionStorage both viable; worker-handoff is the cost-prohibitive last resort".

**Step 3 — Record results.** Write the probe output to `scripts/probes/carrier-survival.mjs`'s header comment (if Probe shape A) OR to a captured artifact `scripts/probes/carrier-survival-<YYYY-MM-DD>.md` (if Probe shape B). Format MUST include exactly these fields per channel:

  - **URL hash:** SURVIVES / STRIPPED — evidence: `useHostLocation().hash` value observed at chat surface mount; `window.location.hash` value.
  - **sessionStorage:** SURVIVES / NOT VIABLE / NOT PROBED — evidence: `sessionStorage.getItem('clarity-chat-deep-link')` value at chat surface mount.
  - **Worker-handoff:** NOT PROBED / PROBED — rationale or evidence.
  - **Final chosen carrier:** `<NAME>` — one-line justification citing the observation above (e.g. "URL hash SURVIVES — useHostLocation().hash matches #h=... on Countermoves COU-2215 mount").

**Step 4 — Plan-time branch declaration.** The chosen carrier determines Task 2's shape — write the carrier name at the top of `scripts/probes/carrier-survival.mjs` (or its captured-output file) in the form `CARRIER=URL_HASH` / `CARRIER=SESSION_STORAGE` / `CARRIER=WORKER_HANDOFF` so Task 2 can grep it deterministically.

If the operator must run the probe live on Countermoves (Probe shape B), Task 1 includes a human-inspection halt: assemble the probe walkthrough (canonical env re-export per MemPalace `vps-commands-self-contained`; `ssh -i $HOME\.ssh\countermoves_vps_ed25519 eric@82.29.197.74`; `pnpm paperclipai ...` runs from `~/paperclip`); pause for the operator; record the output verbatim.

Commit: `probe(04.2-03): carrier-survival results — carrier=<NAME> (GAP-RCB-03-CARRIER)`.
  </action>
  <verify>
    <automated>test -f scripts/probes/carrier-survival.mjs || ls scripts/probes/carrier-survival-*.md 2>&1 | head -1</automated>
    <human-check>The probe output records the SURVIVES/STRIPPED verdict for URL hash and (if needed) sessionStorage with verbatim observed values, names the chosen carrier, and cites the live-host observation that justifies it. The chosen carrier is documented at the top of the probe file in the `CARRIER=<NAME>` form Task 2 will grep.</human-check>
  </verify>
  <acceptance_criteria>
    - `scripts/probes/carrier-survival.mjs` exists (Probe shape A) OR `scripts/probes/carrier-survival-<YYYY-MM-DD>.md` exists (Probe shape B) — committed to the repo so Task 2 / Task 3 / Task 5 can cite it.
    - The probe output documents URL hash: SURVIVES / STRIPPED with verbatim observed `useHostLocation().hash` value AND verbatim observed `window.location.hash` value.
    - If URL hash STRIPPED, the probe output documents sessionStorage: SURVIVES / NOT VIABLE with verbatim observed `sessionStorage.getItem` value at chat surface mount.
    - The chosen carrier is named in the `CARRIER=<NAME>` form (URL_HASH / SESSION_STORAGE / WORKER_HANDOFF) and cites the observed value that justifies the pick.
    - No part of the probe output cites the SDK type docstring as evidence — every verdict cites a live-host observation.
  </acceptance_criteria>
  <done>The carrier Task 2 will implement is chosen empirically and recorded with the live-host observation that justifies the pick. The MemPalace `router-fake-vs-production-host` discipline is honoured: Task 3's regression test now has an observed-behaviour source to cite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the empirically-confirmed carrier in deep-link.mjs — swap carrier, keep contract</name>
  <files>src/ui/surfaces/chat/deep-link.mjs, src/ui/surfaces/chat/deep-link.d.mts, src/ui/surfaces/reader/continue-in-chat-button.tsx, src/ui/surfaces/reader/reverse-topics-link.tsx, src/ui/surfaces/chat/index.tsx, test/ui/chat-url-params.test.mjs, test/ui/continue-in-chat-button.test.mjs, test/ui/reverse-topics-link.test.mjs</files>
  <read_first>
    - Task 1's probe output — read the `CARRIER=<NAME>` line FIRST; it determines the branch below.
    - src/ui/surfaces/chat/deep-link.mjs (all 239 lines) — the contract module being modified. Public API: `buildChatDeepLink(input)` / `buildTopicDeepLink(prefix, topicId)` / `parseChatDeepLink({ search, state })`. Keep the public API SHAPE stable so the three consumers do not need to re-thread (they all destructure the returned `{ to, state }`).
    - src/ui/surfaces/chat/deep-link.d.mts (all 69 lines) — TypeScript declarations co-located with the .mjs. Update to match the new carrier shape (e.g. if URL hash adds a `hash` field to the return value, declare it).
    - src/ui/surfaces/reader/continue-in-chat-button.tsx — the emit-side consumer; lines 174-203 are the `onClick` that calls `nav.navigate(deepLink.to, { state: deepLink.state })` today. If the carrier is URL hash, the `state` argument disappears and `to` grows a `#h=...` tail; if sessionStorage, the click handler writes to `sessionStorage` before `nav.navigate(to)`; if worker-handoff, the click handler calls a worker action before `nav.navigate(to)`.
    - src/ui/surfaces/reader/reverse-topics-link.tsx — the same emit-side pattern at line 107-110 (`nav.navigate(deepLink.to, { state: deepLink.state })`); must use the SAME carrier as the Continue button.
    - src/ui/surfaces/chat/index.tsx lines 281-371 — the read-side consumer; the `useHostLocation()` destructure on line 304 and the `parseChatDeepLink({ search, state: locationState })` call on line 315. If URL hash, add `hash` to the destructure and pass `{ search, state, hash }` to `parseChatDeepLink`; if sessionStorage, add a `sessionStorage.getItem('clarity-chat-deep-link')` + `removeItem` pair before/around the parse; if worker-handoff, replace the location-read with an opt-in-guarded `usePluginData('chat.handoff.claim', ...)` call.
    - test/ui/chat-url-params.test.mjs — the existing RCB-03 source-grep tests; extend or update where the contract changes.
    - test/ui/continue-in-chat-button.test.mjs — the prior emit-side wiring tests; update to assert the new carrier shape (e.g. `grep -c sessionStorage.setItem continue-in-chat-button.tsx == 1` for the sessionStorage branch).
    - test/ui/reverse-topics-link.test.mjs — same; ensure the reverse-topics link uses the same carrier.
    - MemPalace `clarity_pack/decisions` query `mempalace_search query="phase 4.2 plan 04.2-02 deep-link" wing="clarity_pack"` — surface the key decisions filed at the end of 04.2-02 (the carrier-vs-contract split, the contract-module preservation).
    - .planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md — `fix files` and `fix target` sections.
    - CLAUDE.md trust-model paragraph — confirms sessionStorage / window.* are accessible from the same-origin plugin UI.
  </read_first>
  <behavior>
    The contract module's public API stays stable. The encode shape (the `ChatDeepLink` object — `{ topic, comment, employee, newTopic, seedTitle, seedBody, originIssueId }`) stays stable. The decode shape stays stable. **Only the carrier inside the module changes.**

    **Branch A — URL_HASH carrier (Task 1's first-priority choice if it survives):**

    - `buildChatDeepLink` returns `{ to: '/<prefix>/chat#h=<encodeURIComponent(base64(JSON.stringify(payload)))>', state: undefined }` (or similar — the encoded payload rides entirely in the URL fragment, which the host's `resolveHref` cannot touch because fragments are client-side-only by RFC 3986 and never sent to the server / never parsed by path-routing). The `?query` tail is dropped entirely (no longer needed as a fallback).
    - `parseChatDeepLink({ hash, search, state })` reads the `#h=<encoded>` fragment first; if present, base64-decodes and JSON-parses (with try/catch + tolerant fallback per T-04.2-02-05). The `search` fallback may be kept for backward compatibility with shared/copied 0.9.0 URLs, OR may be removed entirely — Task 3's regression test pins whichever choice is made.
    - `continue-in-chat-button.tsx` onClick: `nav.navigate(deepLink.to)` — drop the `{ state: ... }` argument since `state` no longer carries the payload.
    - `reverse-topics-link.tsx` onClick: same shape.
    - `chat/index.tsx`: `const { search, pathname, hash } = useHostLocation();` — add `hash` to the destructure; `parseChatDeepLink({ search, hash })`. After consumption, `nav.navigate(pathname, { replace: true })` (no hash) clears the fragment.
    - **No migration. No worker changes.** Pure UI fix.

    **Branch B — SESSION_STORAGE carrier (Task 1's second-priority choice if URL hash strips):**

    - `buildChatDeepLink` returns `{ to: '/<prefix>/chat', state: undefined, sessionPayload: JSON.stringify(payload) }` — the caller is responsible for writing `sessionPayload` to `window.sessionStorage` under a fixed key (`'clarity-chat-deep-link'`) BEFORE calling `nav.navigate(to)`.
    - `parseChatDeepLink({ search, state })` is augmented or replaced by `readChatDeepLinkFromSession()` — reads `sessionStorage.getItem('clarity-chat-deep-link')` and immediately `removeItem`s after JSON-parsing. The `search` fallback may be kept (consistent with Branch A's reasoning) or removed.
    - `continue-in-chat-button.tsx` onClick: `if (typeof window !== 'undefined') { window.sessionStorage.setItem('clarity-chat-deep-link', deepLink.sessionPayload); } nav.navigate(deepLink.to);`. Defensive guard against SSR / test-runner `window`-absent contexts.
    - `reverse-topics-link.tsx` onClick: same shape.
    - `chat/index.tsx`: on mount, read sessionStorage BEFORE the location-effect; if present, parse + delete the key + drive the same `setSeedDialog` / `setTopic` behaviour as today. The consume-once `consumedDeepLinkRef` guard still applies (covers refresh case where sessionStorage might have a stale value).
    - **No migration. No worker changes.** Pure UI fix.

    **Branch C — WORKER_HANDOFF carrier (Task 1's third-priority choice ONLY if both UI carriers fail):**

    - This branch ships an additive migration AND a new worker handler. Update `files_modified` in the plan frontmatter (already commented above as conditional) BEFORE writing the migration.
    - **Schema-push gate (workflow §5.7) — [BLOCKING]:** the plugin's host runs SQL files in `migrations/` in filename order on plugin install (the install IS the schema push for this project — confirm by re-reading the existing `0009_chat_topics_origin_issue.sql` plumbing and the test `test/migrations/ddl-prefix-validator.test.mjs`). So the migration must (a) be additive-only, plugin-namespace, validator-conformant per the 0006/0007/0008 precedent (no `CREATE INDEX` standalone, no procedural blocks, apostrophe-free comments, ends on a semicolon-terminated statement), (b) be idempotent (`CREATE TABLE IF NOT EXISTS chat_handoffs (...)`), (c) include the necessary columns: `id UUID PRIMARY KEY DEFAULT gen_random_uuid(), companyId TEXT NOT NULL, userId TEXT NOT NULL, payload JSONB NOT NULL, createdAt TIMESTAMPTZ NOT NULL DEFAULT now(), consumedAt TIMESTAMPTZ`. The schema-push happens automatically on the plugin install in Task 5 — no separate `drizzle-kit push` step. Document this in the task body.
    - New worker handler `chat.handoff.create` (action — writes a `chat_handoffs` row) called by `continue-in-chat-button.tsx` via `usePluginAction('chat.handoff.create')`; the action returns the row id. Continue button then `nav.navigate('/<prefix>/chat?h=<handoffId>')` (the id-only query is short enough to survive `resolveHref` if it strips long tails, but Task 1's probe should confirm; if even an id-only `?h=` strips, use a sessionStorage fallback for the id alone or fall back to Branch A/B).
    - New worker handler `chat.handoff.claim` (data — reads + marks-consumed the row by id, returns the payload) called by `chat/index.tsx` via `usePluginData('chat.handoff.claim', { id })`.
    - Both handlers are opt-in-guarded + company-scoped per the existing chat-handler pattern (see `src/worker/handlers/chat-messages.ts` for the template). Add `database.namespace.write` is already in capabilities; no manifest cap change needed.
    - Manifest action/data key registration in `src/worker.ts` (the existing chat handlers are the pattern).
    - **Largest scope branch — only take it if Branch A and Branch B both fail Task 1's probe.**

    **All branches:** the public API of `buildChatDeepLink` / `buildTopicDeepLink` may grow a field (e.g. `hash` or `sessionPayload` or `handoffId`) but MUST keep `to` and continue returning `null` for non-navigable routes. The `parseChatDeepLink` argument shape may grow a field but MUST keep returning the same `ChatDeepLink | null`. The three consumers and the chat-side `setSeedDialog` / `setTopic` behaviour stay byte-stable.

    **SECURITY:** the carrier change does not change the trust posture. `seedTitle` / `seedBody` continue to populate controlled React `<input>` / `<textarea>` `value` props only — never `dangerouslySetInnerHTML`. If Branch A: base64-decode + JSON.parse must be in a try/catch with a tolerant null return for malformed input (T-04.2-02-05). If Branch B: the `JSON.parse(sessionStorage.getItem(...))` is the same shape. If Branch C: the worker action's payload validation is opt-in-guarded + company-scoped + parameterised INSERT.

    Tests:
    - One unit test per branch covering the carrier round-trip at the encode/decode contract level (this is the encode/decode contract; the cross-hook contract belongs in Task 3). E.g. for Branch A: `buildChatDeepLink({route:'existing-topic',...})` returns `{ to: '/COU/chat#h=...', state: undefined }`; `parseChatDeepLink({ hash: '#h=...', search: '', state: undefined })` returns the original payload.
    - Update the existing wiring greps in `test/ui/continue-in-chat-button.test.mjs` and `test/ui/reverse-topics-link.test.mjs` to assert the NEW carrier wiring (e.g. for Branch B: `grep -c sessionStorage.setItem.*clarity-chat-deep-link continue-in-chat-button.tsx >= 1`).
    - `test/ui/chat-url-params.test.mjs` updated to assert the chat-surface reader uses the SAME carrier the button emits (the existing greps probably pinned `useHostLocation().search` / `state` — update to the new carrier).
  </behavior>
  <action>
**Step 1:** Grep the `CARRIER=<NAME>` line from `scripts/probes/carrier-survival.mjs` (or the captured `.md` output) to determine which branch (A / B / C) to take.

**TDD RED (one round-trip test per branch):** Add the encode/decode round-trip test for the chosen carrier to `test/ui/chat-url-params.test.mjs` (or wherever the closest existing assertion lives). Update the carrier-wiring greps in `test/ui/continue-in-chat-button.test.mjs` and `test/ui/reverse-topics-link.test.mjs`. Run `node --test test/ui/chat-url-params.test.mjs test/ui/continue-in-chat-button.test.mjs test/ui/reverse-topics-link.test.mjs` — RED on the new assertions. Commit: `test(04.2-03): RED — carrier swap to <NAME> (GAP-RCB-03-CARRIER)`.

**GREEN — Branch A (URL_HASH):** edit `src/ui/surfaces/chat/deep-link.mjs` — `buildChatDeepLink` builds a `#h=<base64(JSON(payload))>` fragment, `parseChatDeepLink({hash,search,state})` reads `hash` first. Edit `deep-link.d.mts` to match. Edit `continue-in-chat-button.tsx` onClick to call `nav.navigate(deepLink.to)` (drop the `state` argument). Edit `reverse-topics-link.tsx` onClick the same way. Edit `chat/index.tsx` to destructure `hash` from `useHostLocation()` and pass it to `parseChatDeepLink`.

**GREEN — Branch B (SESSION_STORAGE):** edit `src/ui/surfaces/chat/deep-link.mjs` to expose `writeChatDeepLinkToSession(payload)` + `readChatDeepLinkFromSession()` (or similar — keep public API tight). Edit `deep-link.d.mts` to match. Edit `continue-in-chat-button.tsx` onClick to write sessionStorage before `nav.navigate(deepLink.to)`. Edit `reverse-topics-link.tsx` onClick the same way. Edit `chat/index.tsx` to read+delete sessionStorage on mount before the location-effect.

**GREEN — Branch C (WORKER_HANDOFF):** update `files_modified` frontmatter in this plan to include `migrations/0010_chat_handoffs.sql`, `src/worker/db/chat-handoffs-repo.ts`, `src/worker/handlers/chat-handoff-claim.ts` (and the corresponding `chat.handoff.create` handler), `src/worker.ts`. Write the additive plugin-namespace migration (validator-conformant per 0006/0007/0008 precedent — confirm with `node --test test/migrations/ddl-prefix-validator.test.mjs`). Write the typed repo helper (mirror `chat-topics-repo.ts`). Write the two handlers (opt-in-guarded; company-scoped; parameterised INSERT/SELECT). Register both in `src/worker.ts`. Edit `deep-link.mjs` to expose `buildHandoffPayload(payload)` + `parseHandoffId(search|hash)` helpers. Edit the three consumers to use `usePluginAction('chat.handoff.create')` (Continue button) + `usePluginData('chat.handoff.claim', { id })` (chat surface). **The migration's schema-push is the plugin install in Task 5 — no separate push step.** Document this in the commit message.

Run `node --test test/ui/chat-url-params.test.mjs test/ui/continue-in-chat-button.test.mjs test/ui/reverse-topics-link.test.mjs test/ui/topic-strip-backlink.test.mjs` — GREEN. `tsc --noEmit` clean. (Branch C also runs `node --test test/migrations/ddl-prefix-validator.test.mjs` + any new handler tests.) Commit: `fix(04.2-03): swap Reader→Chat deep-link carrier to <NAME> (GAP-RCB-03-CARRIER, RCB-03)`.
  </action>
  <verify>
    <automated>node --test test/ui/chat-url-params.test.mjs test/ui/continue-in-chat-button.test.mjs test/ui/reverse-topics-link.test.mjs test/ui/topic-strip-backlink.test.mjs && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The carrier chosen by Task 1 is implemented in `src/ui/surfaces/chat/deep-link.mjs` with its public API (`buildChatDeepLink` / `buildTopicDeepLink` / `parseChatDeepLink` exports) shape-stable. `deep-link.d.mts` matches.
    - The three consumers (`continue-in-chat-button.tsx`, `reverse-topics-link.tsx`, `chat/index.tsx`) use the new carrier consistently — both Reader-side emit sites use the same channel as the chat-side read.
    - The chat surface's consume-once + clear behaviour is preserved (replace nav for URL hash; sessionStorage delete for Branch B; row mark-consumed for Branch C). `grep -c dangerouslySetInnerHTML src/ui/surfaces/chat/index.tsx` returns 0 (T-04.2-02-01 unchanged).
    - Branch A only: `grep -c state: src/ui/surfaces/reader/continue-in-chat-button.tsx` returns 0 (the `state` argument is dropped); `grep -c hash src/ui/surfaces/chat/index.tsx` returns at least 1 (the destructure grew).
    - Branch B only: `grep -c sessionStorage src/ui/surfaces/reader/continue-in-chat-button.tsx` returns at least 1 AND `grep -c sessionStorage src/ui/surfaces/chat/index.tsx` returns at least 1.
    - Branch C only: `migrations/0010_chat_handoffs.sql` exists; passes `node --test test/migrations/ddl-prefix-validator.test.mjs`; the two new handlers are registered in `src/worker.ts`; `files_modified` in this plan's frontmatter is updated to reflect the worker-tier additions.
    - `tsc --noEmit` clean; all updated UI tests GREEN.
  </acceptance_criteria>
  <done>The Reader→Chat deep-link carrier is the empirically-confirmed channel from Task 1; emit and read agree by construction (single shared module); the public API of deep-link.mjs is stable so the consumers do not re-thread; no SDK-docstring assumption survives in the implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Empirically-grounded regression test — replaces the docstring-derived D1-D6 fake</name>
  <files>test/ui/continue-in-chat-deeplink-contract.test.mjs</files>
  <read_first>
    - Task 1's probe output (`scripts/probes/carrier-survival.mjs` or the captured `.md`) — every behavioural assertion in this test must cite a line of this file or a `runtime.js`/`hooks.js` line as its source.
    - test/ui/continue-in-chat-deeplink-contract.test.mjs (the prior version from Plan 04.2-02) — read every line. This is what's being replaced. The D1-D6 fake honoured `state` because the SDK docstring said so. Identify every assertion whose source was a docstring; those assertions are the ones that must NOT survive.
    - node_modules/@paperclipai/plugin-sdk/dist/ui/runtime.js (29 lines — read entirely) — confirms `getSdkUiRuntimeValue` delegates to `globalThis.__paperclipPluginBridge__.sdkUi[name]`. The host supplies everything; the SDK ships no behaviour to model.
    - node_modules/@paperclipai/plugin-sdk/dist/ui/hooks.js (194 lines — read entirely) — confirms `useHostNavigation()` / `useHostLocation()` are thin `getSdkUiRuntimeValue` stubs. No behaviour to model from the SDK itself.
    - The MemPalace drawer `router-fake-vs-production-host` — re-read; the test pattern this file pins is exactly the one this Task implements.
    - src/ui/surfaces/chat/deep-link.mjs (the new Task-2 version with the swapped carrier) — the encode/decode contract the test verifies.
    - .planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md `NEW REQUIRED test discipline` section.
    - package.json scripts — confirm `node --test "test/**/*.test.mjs"` is the test runner and that there is no React render harness or plugin-dev-server-driven test path.
  </read_first>
  <behavior>
    The new test exercises the Reader→Chat deep-link contract through the carrier Task 2 implemented. Two patterns are acceptable; pick the one feasible against the existing test runner:

    - **Pattern (a) — live-host runtime test (preferred but only if feasible):** boot `paperclip-plugin-dev-server` (or the equivalent — check `package.json` scripts and `node_modules/@paperclipai/plugin-sdk/package.json` bin entries) from a `node --test` setup hook, mount the chat surface, drive a real navigation, assert the deep-link is consumed. If the existing repo infrastructure does NOT support this without inventing new tooling, document why in the test file header and use Pattern (b).
    - **Pattern (b) — empirically-derived fake:** build a router/storage fake whose behaviour model is justified inline by a comment citing either (a) the Task 1 probe output (e.g. "fakeRouter.navigate(to, opts) drops opts.state to model the live host — observed behaviour per scripts/probes/carrier-survival-2026-05-XX.md line N"), or (b) a runtime.js/hooks.js source line. The fake then exercises the SAME `buildChatDeepLink` / `parseChatDeepLink` round-trip the prior D1-D6 did, but the fake's behaviour is grounded in observation, not docstring assumption.

    Test cases (label them E-series to break with the D-series the prior contract test used — making accidental copy-paste of the docstring-derived pattern obvious in code review):

    - **E1 — EXISTING-TOPIC ROUND-TRIP:** the chosen carrier carries an `existing-topic` deep link (`topic` + `comment` + `employee`) through emit + read; the resolved `ChatDeepLink` has the original values.
    - **E2 — NEW-TOPIC ROUND-TRIP:** the carrier carries a `new-topic-needed` deep link (`newTopic=true` + `seedTitle` + `seedBody` + `originIssueId` + `employee`); all five fields resolve to the originals.
    - **E3 — REVERSE-TOPICS SHARES THE CARRIER:** `buildTopicDeepLink(prefix, topicIssueId)` round-trips through the same carrier; the chat-side read resolves the `topic`.
    - **E4 — CARRIER-SOURCE PROVENANCE (the load-bearing assertion):** for every behavioural property the test asserts about the carrier (e.g. "URL hash survives navigate()"), the test header / inline comment cites either the Task 1 probe output (preferred) or a runtime.js/hooks.js line as the source. The test contains no reference to `dist/ui/types.d.ts` docstrings as evidence.
    - **E5 — INVERTED-TEST SANITY CHECK:** add a documented sub-test (or a comment block + an assertion) that proves the test would FAIL under the prior `state`-stripping host behaviour. I.e. if the fake / test setup is rewritten optimistically (the way the 04.2-02 D-series fake was), every assertion that depends on the carrier should regress. This is the "inverted test" guard — it codifies the failure mode the test must catch.
    - **E6 — MALFORMED-INPUT TOLERANCE (T-04.2-02-05):** the chat-side read tolerates a corrupted carrier payload (bad base64 / bad JSON / missing key) without throwing; returns null.

    The prior `test/ui/continue-in-chat-deeplink-contract.test.mjs` D1-D6 — DELETE it (preferred — its docstring-derived router fake is exactly the pattern this discipline forbids and keeping it next to the new file invites copy-paste regressions). If any of its assertions had value at the `buildChatDeepLink` / `parseChatDeepLink` encode/decode level (NOT the cross-hook level), move those assertions into the E-series or into `test/ui/chat-url-params.test.mjs`.

    The test file's header comment MUST include:
    - A pointer to MemPalace `router-fake-vs-production-host` so a future reader sees the discipline.
    - A summary of the Task 1 probe finding (carrier name + the observed-value evidence).
    - A pointer to the prior 04.2-02 D1-D6 fake as the cautionary tale ("this file replaces D1-D6 — DO NOT model the fake on type docstrings; that pattern shipped GAP-RCB-03 twice").
  </behavior>
  <action>
**TDD RED first:** create the new `test/ui/continue-in-chat-deeplink-contract.test.mjs` with the E1-E6 cases scaffolded; cite the Task 1 probe output in the file header. Run `node --test test/ui/continue-in-chat-deeplink-contract.test.mjs` — MUST fail (Task 2's GREEN already landed the carrier, but the new tests are written against a re-grounded fake/runtime, so they fail until written against the Task 2 implementation). Commit: `test(04.2-03): RED — E-series empirically-grounded contract (replaces docstring-derived D1-D6; GAP-RCB-03-CARRIER)`.

**GREEN:** wire each E-test against the Task 2 carrier implementation. Add the inline `// Source: scripts/probes/carrier-survival-... line N` / `// Source: node_modules/.../runtime.js line N` comments above every behavioural assertion. Add the inverted-test sanity check (E5) — either as a separate sub-test that runs against a mutated fake, or as a structural comment block proving why the test catches the regression. Delete (or restructure) the prior D1-D6 file/sections per the rules above. Run `node --test test/ui/continue-in-chat-deeplink-contract.test.mjs test/ui/chat-url-params.test.mjs` — GREEN. Commit: `fix(04.2-03): E-series test grounded in runtime observation (closes the docstring-fake regression path)`.

**Self-check before committing:** grep the new test file for any reference to `dist/ui/types.d.ts` as a source — must return 0. Grep for `Source:` comments — must return at least 6 (one per behavioural assertion at minimum).
  </action>
  <verify>
    <automated>node --test test/ui/continue-in-chat-deeplink-contract.test.mjs test/ui/chat-url-params.test.mjs && ! grep -q "types.d.ts" test/ui/continue-in-chat-deeplink-contract.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `test/ui/continue-in-chat-deeplink-contract.test.mjs` exists with E1-E6 (not D1-D6 — the label change is deliberate to break with the docstring-derived pattern).
    - Every behavioural assertion about the carrier cites either Task 1's probe output or a `runtime.js` / `hooks.js` source line via inline comment. `grep -c types.d.ts test/ui/continue-in-chat-deeplink-contract.test.mjs` returns 0.
    - E5 (inverted-test sanity check) is present and proves the test would catch the regression that shipped GAP-RCB-03 in both 04.2-01 and 04.2-02.
    - The prior D1-D6 file/sections are deleted OR restructured so no docstring-derived router-fake assertion can be copy-pasted.
    - `node --test test/ui/continue-in-chat-deeplink-contract.test.mjs test/ui/chat-url-params.test.mjs` GREEN; `tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The Reader→Chat contract is pinned by a regression test grounded in observed runtime behaviour, not SDK docstring intent. If a future plan re-introduces a stripped carrier, this test fails.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Version bump 0.9.2 + full suite + build + pack</name>
  <files>package.json, src/manifest.ts, test/manifest/chat-capabilities.test.mjs</files>
  <read_first>
    - The MemPalace `clarity_pack/runbook` drawer `plugin-version-bump-two-sources` — the version is a hardcoded literal in BOTH `package.json` AND `src/manifest.ts`; the host reads `dist/manifest.js` built from `src/manifest.ts`. Bump BOTH or ship 0.9.2 code with a 0.9.1 label.
    - package.json — current `version` field (`0.9.1`) and the `build` / `build:worker` / `build:ui` / `build:manifest` / `test` scripts.
    - src/manifest.ts line 322 — current `version: '0.9.1',` literal — the host-read source.
    - .planning/phases/04.2-reader-chat-bridge/04.2-02-PLAN.md Task 3 + 04.2-01-PLAN.md Task 7 — the prior version-bump tasks this one mirrors (build + pack invocation, byte-size + sha256 recording).
    - test/manifest/chat-capabilities.test.mjs — the prior "manifest version is 0.9.1" assertion that needs bumping to 0.9.2 (per the Plan 04.2-02 deviation #4 precedent).
  </read_first>
  <behavior>
    - `package.json` `version` → `0.9.2`. Patch bump — single load-bearing fix (the carrier swap); no new surface; no migration unless Task 1's probe picked Branch C, in which case the migration ships under the same patch bump (additive-only plugin-namespace).
    - `src/manifest.ts` line 322 `version: '0.9.1'` → `version: '0.9.2'`.
    - `test/manifest/chat-capabilities.test.mjs` — bump the "manifest version is 0.9.1" assertion to 0.9.2 (standard version-bump pattern; the prior plans both touched this file).
    - Full test suite GREEN: `node --test "test/**/*.test.mjs"` — baseline 1323 (end of Plan 04.2-02); Plan 04.2-03 replaces D1-D6 with E1-E6 (net 0) + adds the carrier-round-trip tests from Task 2 (3-6 tests depending on branch). Expect suite size 1325-1340 with 0 failures + 2 pre-existing skips.
    - `tsc --noEmit` clean. `node scripts/check-css-scope.mjs` exit 0 (theme.css unchanged by this plan; the scope guard stays).
    - Build (`pnpm build` — worker + UI + manifest) succeeds. The pack invocation per the prior plans (`npm pack` from the repo root) produces `clarity-pack-0.9.2.tgz`. Record byte size + sha256 for the SUMMARY and for Task 5's drill bookend.
  </behavior>
  <action>
1. Edit `package.json` `version` to `0.9.2`.
2. Edit the `version:` literal in `src/manifest.ts` (line 322) to `0.9.2`.
3. Edit `test/manifest/chat-capabilities.test.mjs` — flip the version assertion from `0.9.1` to `0.9.2`.
4. Run `node --test "test/**/*.test.mjs"` — record pass/fail/skip counts (expect 0 failures; 2 pre-existing skips; total in the 1325-1340 range).
5. Run `tsc --noEmit` — must be clean.
6. Run `pnpm build`; run `npm pack` (or the repo's pack script per the prior plans) — produce `clarity-pack-0.9.2.tgz` at the repo root. Record byte size (e.g. `node -e "console.log(require('fs').statSync('clarity-pack-0.9.2.tgz').size)"`) and sha256.
7. Commit: `chore(04.2-03): version 0.9.2 — Reader↔Chat Bridge GAP-RCB-03-CARRIER closure + pack tarball`.
  </action>
  <verify>
    <automated>node -e "if(require('./package.json').version!=='0.9.2')process.exit(1)" && grep -c "version: '0.9.2'" src/manifest.ts && test -f clarity-pack-0.9.2.tgz</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` version === `0.9.2` AND `src/manifest.ts` version literal === `0.9.2` (BOTH — MemPalace `plugin-version-bump-two-sources`); `grep -c "version: '0.9.2'" src/manifest.ts` returns 1.
    - `test/manifest/chat-capabilities.test.mjs` version assertion === `0.9.2`; that test passes.
    - Full suite GREEN — 0 failures; suite size in the 1325-1340 range (Plan 04.2-02 baseline 1323 + carrier round-trip tests, net of D→E replacement); `tsc --noEmit` clean; `node scripts/check-css-scope.mjs` exit 0.
    - `clarity-pack-0.9.2.tgz` exists at the repo root; byte size + sha256 recorded for the SUMMARY.
  </acceptance_criteria>
  <done>A versioned, built, packed 0.9.2 tarball is ready for the Countermoves operator re-drill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator re-drill on Countermoves — 5-path Reader↔Chat Bridge + RCB-07 spot-check (0.9.1→0.9.2, snapshot-bookended)</name>
  <files>(no source files — live operator drill; outcome recorded in 04.2-03-SUMMARY.md and 04.2-VERIFICATION.md)</files>
  <read_first>
    - The MemPalace `clarity_pack/runbook` drawers — query the snapshot/verify/install command pattern BEFORE writing the drill steps; do NOT extrapolate from spec. Specifically: `feedback_mempalace-before-vps-commands`, `countermoves-verify-install-drill`, `countermoves-safety-cli-invocation`, `feedback_vps-drill-walkthrough-completeness`, `feedback_vps-commands-self-contained`, `paperclipai-workspace-script-gotcha`, `feedback_ssh-check-mempalace`, `clarity-pack-plugin-page-routes`, `countermoves-paperclip-live-state`, `countermoves-paperclip-parked-bugs`.
    - .planning/phases/04.2-reader-chat-bridge/04.2-02-SUMMARY.md "Install / upgrade path — PASSED" section — the exact 0.9.0→0.9.1 install/upgrade pattern that PASSED (snapshot bookend → non-destructive `uninstall` → `install-helper.sh` install, because `pnpm paperclipai plugin install` rejects re-install with 400 "already installed"; UUID `0d4fc40a-0541-4b67-8979-9d346cb9c07b` is the install identity to preserve), the known `paperclipCliWarnings` false-positive, and the `verify` skip rationale.
    - .planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md — the Drill Coverage table (paths 1-5 + RCB-07) and the GAP-RCB-03-CARRIER entry being closed.
    - CLAUDE.md "Bookended-by-snapshots rule" — the 0.9.1→0.9.2 upgrade MUST be bookended by a verified Postgres + filesystem snapshot taken immediately before, with the rollback path verified before the drill proceeds.
    - scripts/safety/cli.mjs (and surrounding `scripts/safety/`) — the safety CLI invocation pattern; MemPalace `countermoves-safety-cli-invocation` documents the `~/clarity-pack/` partial-copy gotcha and the `--db-url` requirement.
  </read_first>
  <what-built>
    Tasks 1-4 closed GAP-RCB-03-CARRIER and shipped `clarity-pack-0.9.2.tgz`:
    - Task 1 — empirical carrier-survival probe (live Countermoves or paperclip-plugin-dev-server) picked the chosen carrier from URL hash / sessionStorage / worker-handoff per observed-survival; output recorded in `scripts/probes/carrier-survival.mjs` (or its captured `.md`).
    - Task 2 — implemented the chosen carrier inside `src/ui/surfaces/chat/deep-link.mjs` (public API preserved); three consumers (`continue-in-chat-button.tsx`, `reverse-topics-link.tsx`, `chat/index.tsx`) updated to the new carrier wiring. If Branch C (worker-handoff) was selected, migration 0010 + a worker handler pair shipped (additive plugin-namespace; schema-push is the plugin install).
    - Task 3 — new empirically-grounded regression test `test/ui/continue-in-chat-deeplink-contract.test.mjs` (E1-E6 series); every behavioural assertion cites Task 1's probe output or `runtime.js`/`hooks.js` source lines — no docstring assertions. Prior D1-D6 deleted/restructured.
    - Task 4 — version 0.9.2 in `package.json` + `src/manifest.ts`; full suite GREEN; `clarity-pack-0.9.2.tgz` packed.
    This is a `checkpoint:human-verify` task: Claude assembles a complete, self-contained drill walkthrough; Eric runs it on the live Countermoves Paperclip instance and reports PASS/FAIL per path.
  </what-built>
  <how-to-verify>
    Build the pre-flight from the runbook memories FIRST (do not ship a happy-path; the Plan 04.2-02 SUMMARY's drill section is the most recent precedent). Every VPS bash block MUST start with the canonical env re-export per MemPalace `feedback_vps-commands-self-contained` — sudo grep to recover DB_URL from /etc/paperclip/db.env, then export API_URL=http://localhost:3100, API_KEY from ~/.paperclip/auth.json, COMPANY_ID for the COU company.

    `pnpm paperclipai …` runs from `~/paperclip` (cd first) per MemPalace `paperclipai-workspace-script-gotcha`. SSH is `ssh -i $HOME\.ssh\countermoves_vps_ed25519 eric@82.29.197.74` per MemPalace `feedback_ssh-check-mempalace`. Plugin pages are `/<companyPrefix>/<routePath>` — e.g. `/COU/chat` — per MemPalace `clarity-pack-plugin-page-routes`. Safety CLI runs via `node scripts/safety/cli.mjs` with `--db-url` per MemPalace `countermoves-safety-cli-invocation`.

    1. **Snapshot bookend.** Take a verified Postgres + filesystem snapshot via `node scripts/safety/cli.mjs snapshot --db-url=$DB_URL` (or the prior plans' invocation pattern — surface from MemPalace). Record the snapshot id (e.g. `2026-05-XXTHH-MM-SSZ`). Run `node scripts/safety/cli.mjs verify --db-url=$DB_URL --snapshot=<id>` to confirm the restore-and-smoke path works (rollback path confirmed) OR document why verify is being skipped (rollback path already confirmed in a recent drill per the Plan 04.2-02 SUMMARY precedent). CLAUDE.md bookended-by-snapshots rule satisfied either way.
    2. **Upgrade 0.9.1 → 0.9.2.** No in-place upgrade exists (`pnpm paperclipai plugin install` 400s on re-install) — non-destructive `uninstall` (no `--force`; record plugin-namespace row counts `chat_topics`, `chat_messages`, `chat_topic_tasks`, plus `chat_handoffs` if Branch C; expect byte-identical pre/post per COEXIST #6) → `scripts/install-helper.sh /home/eric/clarity-pack-0.9.2.tgz`. Post-install: confirm `pnpm paperclipai plugin list` shows `clarity-pack status=ready version=0.9.2 id=0d4fc40a-0541-4b67-8979-9d346cb9c07b` (UUID preserved); migration 0009 no-op re-apply; migration 0010 applied iff Branch C (idempotent CREATE TABLE IF NOT EXISTS — re-apply is also a no-op); post-install smoke passes.
    3. **5-path Reader↔Chat Bridge drill** (all on live Countermoves; the carrier swap is the decisive check):
       - **Path 1** — open assigned task `COU-2215` (or any assigned non-chat task) in Reader → header shows the gold PRIMARY Continue button (GAP-RCB-01 already confirmed closed in 04.2-02) → click → the chat surface opens the pre-seeded New Topic dialog with title/body `Continuing from <COU-NNNN>: <title>` (GAP-RCB-03-CARRIER closed — payload survived). Create → topic persists `chat_topics.origin_issue_id` → topic strip renders `About <COU-NNNN> ↗` chip (RCB-05) → click chip → navigates to source issue's Reader → Reader header shows `1 conversation about this issue ↗` (RCB-06 element rendering re-confirmed) → click → popover with the topic row → click the row → chat surface switches to the topic.
       - **Path 2** — open a chat-spawned task (`originId: chat-task:…`); known fixture: `COU-2215` has a chat-spawned task with source topic id `e7b7fee8-b432-4422-8a1c-1bb3043a9d43` (observed by Eric in the 04.2-02 drill via `ReverseTopicsLink` hover). Click Continue → chat surface opens DIRECTLY to the source topic, scrolls to + flash-highlights (1.6s) the source comment. GAP-RCB-03-CARRIER closed.
       - **Path 3** — open a cold-task (`+ Create task` Standalone, not chat-spawned) → Continue button → behaves as Path 1 (new seeded New Topic dialog).
       - **Path 4** — open a chat-topic issue (e.g. `CHT-NNNN`) in Reader → the Continue button is HIDDEN (`topic-itself` route — chat.openForIssue returns null route).
       - **Path 5** — open an issue with NO assignee → Continue button is DISABLED with tooltip `Assign this issue to an employee before opening chat.`
       - **RCB-07 spot-check** — open a chat topic that PREDATES migration 0009 (no `origin_issue_id` — query the DB for one if needed: `SELECT * FROM chat_topics WHERE origin_issue_id IS NULL LIMIT 5;`) → it renders normally with NO `About…` chip and NO error.
    4. **Carrier-specific console probe** — for the chosen carrier, capture the post-click diagnostic that proves the carrier survived (the inverse of the 04.2-02 evidence). E.g. for URL hash: console-paste `window.location.hash` after click — must contain the encoded payload. For sessionStorage: console-paste `sessionStorage.getItem('clarity-chat-deep-link')` BEFORE chat surface mount completes (or instrument the chat surface to log the consumed payload). For worker-handoff: confirm a `chat_handoffs` row exists pre-claim and is marked `consumedAt` post-claim.
    5. **On any FAIL** — the rehearsed rollback path from step 1 is available; record the failure as a Plan 04.2-04 gap-closure follow-up.

    PASS criterion: all 5 paths produce the expected navigation AND the carrier-specific console probe (step 4) shows the payload surviving cross-route AND the RCB-07 spot-check shows no chip / no error AND the 0.9.1→0.9.2 upgrade was snapshot-bookended.
  </how-to-verify>
  <action>
1. Query MemPalace `wing=clarity_pack` `room=runbook` + `room=decisions` for snapshot / uninstall / install-helper / safety CLI invocation patterns. Assemble the snapshot → (verify or skip with rationale) → gated-upgrade → 5-path drill → carrier-specific console probe → RCB-07 spot-check → rollback walkthrough. Every VPS block self-contained with the canonical env re-export. Use `pnpm paperclipai` only from `~/paperclip`.
2. Present the walkthrough to Eric. PAUSE for the operator to run it on live Countermoves.
3. Record per-path PASS/FAIL + the carrier-specific console probe verbatim + any defects in `04.2-03-SUMMARY.md`, and update `04.2-VERIFICATION.md` (the Drill Coverage table + the per-RCB verdicts + the GAP-RCB-03-CARRIER status flip).
4. On full PASS: flip RCB-01..RCB-07 to `Implemented` in `REQUIREMENTS.md` with delivering-plan references (RCB-01 → 04.2-01 + 04.2-02 operator-confirmed; RCB-02/RCB-04/RCB-07 → 04.2-01 operator-confirmed by the 04.2-03 re-drill; RCB-03/RCB-05/RCB-06 → 04.2-01 + 04.2-03 — the carrier swap that makes them functional); update `04.2-VERIFICATION.md` status to `verified` (drop `gap_closure_target_*` fields); update ROADMAP.md + STATE.md to Phase 4.2 COMPLETE.
5. On any FAIL: file gap-closure follow-ups (the Plan 04.1-06→10 iterative-drill pattern) for a Plan 04.2-04; do NOT flip REQUIREMENTS.md.
6. File the drill outcome to MemPalace (`mempalace_add_drawer`, `wing=clarity_pack`, `room=decisions`) — capture the chosen carrier + the observed-survival evidence + the per-path verdict so a future planner sees what the carrier swap delivered.
  </action>
  <verify>
    <automated>MISSING — live operator drill on Countermoves; no automated proof. Pre-drill gate: Tasks 1-4 automated verification (probe artifact present + E-series regression GREEN + full suite GREEN + tsc clean + tarball packed) must all pass before this checkpoint.</automated>
    <human-check>Eric confirms 5/5 drill paths PASS on live Countermoves, the carrier-specific console probe shows the payload surviving cross-route, the RCB-07 pre-0009-topic spot-check shows no chip and no error, and the 0.9.1→0.9.2 upgrade was snapshot-bookended.</human-check>
  </verify>
  <acceptance_criteria>
    - A verified Postgres + filesystem snapshot was taken immediately before the 0.9.1→0.9.2 upgrade; the restore-and-smoke (`verify`) path passed OR was skipped with documented rationale based on a recent rehearsed restore — rollback path confirmed either way (CLAUDE.md bookended-by-snapshots rule).
    - Post-install: `clarity-pack status=ready version=0.9.2 id=0d4fc40a-0541-4b67-8979-9d346cb9c07b` on Countermoves (UUID preserved); plugin-namespace chat data (`chat_topics`, `chat_messages`, `chat_topic_tasks`) byte-identical across the uninstall/install (COEXIST #6); migration 0010 applied iff Branch C, else no new migration.
    - Drill paths 1-5 each produce the expected behaviour, OR every FAIL is filed as a Plan 04.2-04 gap-closure follow-up.
    - GAP-RCB-03-CARRIER confirmed closed (Path 1 + Path 2 — the carrier-specific console probe shows the payload surviving cross-route; the chat surface lands on the seeded dialog / source topic + comment).
    - RCB-07 spot-check: a pre-0009 chat topic renders with no `About…` chip and no error.
    - Outcome recorded in `04.2-03-SUMMARY.md` + `04.2-VERIFICATION.md`; on full PASS, RCB-01..RCB-07 flipped to `Implemented` in `REQUIREMENTS.md` and Phase 4.2 marked COMPLETE in ROADMAP.md + STATE.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 5 paths + the RCB-07 spot-check + the carrier-specific console probe PASS, or describe the per-path failures.</resume-signal>
  <done>Phase 4.2's Reader↔Chat Bridge is verified end-to-end on the live Countermoves instance; RCB-01..RCB-07 are operator-confirmed and Implemented; Phase 4.2 is complete at version 0.9.2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader-built deep link → chat surface payload | The six deep-link params (`topic` / `comment` / `newTopic` / `seedTitle` / `seedBody` / `originIssueId`) are operator-controlled strings carried from the Reader surface, through the chosen carrier (URL hash / sessionStorage / worker-handoff row), into the chat surface. The carrier change in this plan does NOT change the trust posture — the chat surface still receives untrusted, possibly-crafted strings from the same operator who would otherwise type a URL by hand. |
| chat surface → New Topic dialog render | `seedTitle` / `seedBody` populate the pre-seeded New Topic dialog's controlled `<input>` / `<textarea>` values. Unchanged from 04.2-02. |
| chat surface → `chat.topic.create` | The chat-side seeded-create threads `originIssueId` (a deep-link param) into the `chat.topic.create` action, which writes `chat_topics.origin_issue_id` (plugin namespace, migration 0009 already shipped). Unchanged from 04.2-02. |
| (Branch C only) Reader UI → `chat.handoff.create` worker action → `chat_handoffs` row → chat UI `chat.handoff.claim` data handler | If Task 1 picks worker-handoff: the deep-link payload is persisted to a plugin-namespace row keyed by handoff-id, then read+consumed exactly once by the chat surface on mount. The row carries the same operator-controlled strings; opt-in-guarded + company-scoped + parameterised INSERT/SELECT. |

Same-origin, single-tenant, single-operator trusted-code plugin (CLAUDE.md trust model). The deep-link params are authored by the operator who would otherwise type a URL by hand. No high-severity threat is expected; the register below carries forward 04.2-02 dispositions with the carrier-specific deltas.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-03-01 | T (Tampering) | A crafted `seedTitle` / `seedBody` deep-link param reaches the New Topic dialog through the new carrier | mitigate | React escapes text content by default; `seedTitle`/`seedBody` populate controlled `<input>`/`<textarea>` `value` props, NEVER `dangerouslySetInnerHTML`. Task 3 acceptance pins `grep -c dangerouslySetInnerHTML src/ui/surfaces/chat/index.tsx` == 0 (unchanged from 04.2-02 T-04.2-02-01). The decode path is plain string decode (base64+JSON.parse for URL-hash branch; JSON.parse for sessionStorage branch; opt-in-guarded SELECT for worker-handoff branch) — no HTML interpretation in any branch. |
| T-04.2-03-02 | I (Info disclosure) | XSS via a `topic` / `comment` / `originIssueId` param rendered into the chat surface through the new carrier | mitigate | These params are used as React text content (topic ids, a `msg-<commentId>` DOM-id lookup) and as plain string fields — never as HTML. Unchanged trust posture from 04.2-02 T-04.2-02-02. |
| T-04.2-03-03 | T (Tampering) | A crafted `originIssueId` writes an arbitrary value to `chat_topics.origin_issue_id` | accept | Unchanged from Plan 04.2-01 T-04.2-01-04 / Plan 04.2-02 T-04.2-02-03: `origin_issue_id` is a free-text back-link with no privilege meaning; a bad value only mis-targets one chip's navigation. The INSERT is parameterised (no SQLi). The single-operator-per-box trust model bounds the exposure. The carrier change does not introduce new exposure — only changes how the param is CARRIED. |
| T-04.2-03-04 | S (Spoofing) | A carrier-laden chat surface re-triggers the seeded New Topic dialog on a page refresh | mitigate | The chat surface clears the consumed carrier after read: URL-hash branch — `nav.navigate(pathname, { replace: true })` drops the fragment; sessionStorage branch — `sessionStorage.removeItem` after parse; worker-handoff branch — `consumedAt` marked on the row. The existing `consumedDeepLinkRef` guard provides a second layer. Task 2 acceptance preserves this; Task 3 E5 (inverted-test) pins it. |
| T-04.2-03-05 | D (DoS) | Malformed / missing / truncated carrier payload crashes the chat surface's mount effect | mitigate | The carrier-read path uses try/catch + tolerant null return for malformed input (E6 covers this). Branch A: bad base64 / bad JSON returns null. Branch B: missing key returns null. Branch C: missing/consumed row returns null with an opt-in-guarded error path. The chat surface's existing fallback empty state is the graceful-degrade path. |
| T-04.2-03-06 | I (Info disclosure) | (URL-hash branch only) A user copies the chat URL with the encoded payload still in the fragment and shares it externally | accept | The encoded payload is base64-encoded JSON of operator-typed strings — no secrets, no identifiers beyond what's already in the issue IDs the operator has access to. Single-tenant trust model: the operator is the only person who would receive a shared URL. The replace-nav clears the fragment after consumption so copy-after-arrival is fine; copy-before-arrival is the narrow window and the payload contents are non-sensitive. |
| T-04.2-03-07 | T (Tampering) | (Worker-handoff branch only) Cross-user race — operator A's `chat_handoffs` row claimed by operator B | accept | Single-operator-per-box trust model (CLAUDE.md). Plus: the `chat_handoffs` SELECT is `userId`-scoped (the handler's `userId` param matches the row's `userId` column); a cross-user claim returns null. The `chat_handoffs` id is a UUIDv4 — not enumerable. |

ASVS L1 baseline. The `accept`-disposed threats (T-04.2-03-03, T-04.2-03-06, and conditionally T-04.2-03-07) are pre-existing or carry forward from 04.2-02 under the documented single-tenant trusted-code trust model with parameterised compensating controls. No new high-severity threat is introduced by the carrier change.
</threat_model>

<verification>
Plan 04.2-03 succeeds when:

- GAP-RCB-03-CARRIER is closed: the Reader→Chat deep-link payload survives cross-route navigation through the chosen carrier (URL hash / sessionStorage / worker-handoff) — pinned by the new E-series regression test (every assertion grounded in observed runtime behaviour, NOT SDK docstring) AND confirmed by the carrier-specific console probe on the Countermoves drill.
- The chosen carrier is documented with the live-host observation that justifies the pick (Task 1 probe output).
- `node scripts/check-css-scope.mjs` exits 0 (theme.css unchanged; SCAF-06 / COEXIST-01 not regressed).
- Version is `0.9.2` in BOTH `package.json` and `src/manifest.ts`; `clarity-pack-0.9.2.tgz` is packed with recorded byte size + sha256.
- Full suite GREEN (1325-1340 range — Plan 04.2-02 baseline 1323 + E-series replaces D-series + carrier round-trip tests); `tsc --noEmit` clean.
- The Countermoves 0.9.1→0.9.2 snapshot-bookended operator re-drill confirms drill paths 1-5 PASS, the carrier-specific console probe shows the payload surviving cross-route, and the RCB-07 pre-0009-topic UI spot-check shows no chip / no error.
- On full drill PASS: RCB-01..RCB-07 flipped to `Implemented` in `REQUIREMENTS.md`; `04.2-VERIFICATION.md` status → `verified` (gap_closure fields dropped); Phase 4.2 marked COMPLETE in ROADMAP.md + STATE.md.
- If Branch C (worker-handoff) was selected: migration 0010 applied idempotently on Countermoves install; the new `chat.handoff.create` / `chat.handoff.claim` handlers respond GREEN to opt-in-guarded calls; coexistence guarantees #3 + #6 preserved (additive plugin-namespace, no host-schema touch).
</verification>

<success_criteria>
- GAP-RCB-03-CARRIER is closed in source AND operator-confirmed on live Countermoves — the load-bearing Reader→Chat deep link works for the first time since Plan 04.2-01 shipped 0.9.0.
- The regression test that pins the carrier contract is grounded in observed runtime behaviour (Task 1 probe + runtime.js source), not SDK docstrings — codifying the MemPalace `router-fake-vs-production-host` discipline so a third repeat of the fake-vs-production drift is mechanically impossible.
- The issue↔conversation graph is fully navigable in both directions on live Countermoves: Continue button → seeded dialog / source topic; About-chip → source Reader; reverse-topics link → topic in chat. The "zero rabbit-holes" CLAUDE.md core value is operator-confirmed.
- RCB-01..RCB-07 are operator-confirmed and Implemented; Phase 4.2 closes COMPLETE & VERIFIED at version 0.9.2.
- Coexistence guarantees #3 + #6 preserved — no host-schema touch; plugin-namespace data intact across the 0.9.1→0.9.2 upgrade; conditional migration 0010 (worker-handoff branch only) is additive-only plugin-namespace.
- The 0.9.1→0.9.2 upgrade is snapshot-bookended with a rehearsed rollback path (CLAUDE.md bookended-by-snapshots rule).
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-reader-chat-bridge/04.2-03-SUMMARY.md` per the GSD summary template — recording:
- The commit range and per-task suite delta (baseline 1323 → final).
- `clarity-pack-0.9.2.tgz` byte size + sha256.
- The empirical carrier-survival probe finding (Task 1 — chosen carrier + the observed-value evidence, including the verbatim `useHostLocation().hash` / `sessionStorage` / `chat_handoffs` row state observed on the live host).
- The Task 5 per-path drill outcome + the carrier-specific console probe verbatim.
- Any deviations from the plan.
- RCB-01..RCB-07 final status with delivering-plan references.

On full drill PASS, also:
- Update `04.2-VERIFICATION.md`: status → `verified`; drop `gap_closure_target_version` + `gap_closure_target_plan`; update the Drill Coverage table per-path outcomes; flip GAP-RCB-03-CARRIER to `resolved`.
- Update `REQUIREMENTS.md`: RCB-01..RCB-07 → `Implemented (Plan 04.2-NN — <one-liner>)`.
- Update `ROADMAP.md`: Phase 4.2 marked COMPLETE & VERIFIED.
- Update `STATE.md`: Phase 4.2 complete; Phase 5 unblocked.
- File the drill outcome to MemPalace (`mempalace_add_drawer`, `wing=clarity_pack`, `room=decisions`) capturing the chosen carrier + the observed-survival evidence + per-path verdict.

On any FAIL, file the remaining gaps for a Plan 04.2-04 per the Plan 04.1-06→10 iterative-drill pattern; do NOT flip REQUIREMENTS.md.
</output>
