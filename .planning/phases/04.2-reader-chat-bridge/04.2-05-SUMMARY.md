---
phase: 04.2-reader-chat-bridge
plan: 04.2-05
title: "Plan 04.2-05 — D1-D7 polish pass (v1.0.0-rc.3)"
status: code-complete-drill-deferred
version: 1.0.0-rc.3
prior_version: 1.0.0-rc.2
artifact: clarity-pack-1.0.0-rc.3.tgz
artifact_bytes: 143180
artifact_sha256: 13a9a4d9a491e4f0d2c5eee516c6e764905b3ba56474d7e9721464690be37248
suite_baseline: 1339
suite_final: 1360
suite_delta: 21
suite_pass: 1358
suite_fail: 0
suite_skip: 2
committed: 2026-05-24
---

# Plan 04.2-05 — D1-D7 Polish Pass

**Status:** CODE-COMPLETE 2026-05-24 — operator drill deferred to Eric.
**Version trail:** 1.0.0-rc.2 → 1.0.0-rc.3.
**Tarball:** `clarity-pack-1.0.0-rc.3.tgz`, 143,180 bytes, sha256 `13a9a4d9a491e4f0d2c5eee516c6e764905b3ba56474d7e9721464690be37248`.

## What shipped

Seven atomic-commit tasks closing the seven polish defects D1-D7 filed in MemPalace `clarity_pack/decisions` during the 2026-05-24 Plan 04.2-04 operator drill on Countermoves (drawers `7435d91bc180b048de95e0ae` for D1-D6 + `50f2229a76335930ecfefd9d` for D7).

## D-defect disposition table

| ID | Defect | Disposition | Commit |
|----|--------|-------------|--------|
| D1 | Path 3 newTopic dispatch did NOT auto-select employee → CREATE TOPIC disabled. | **Implemented** — race-safe `setEmployee` lookup extended to the `if (link.newTopic)` branch; the defer guard at the top of the deep-link effect broadened from `link.topic && link.employee && …` to `link.employee && …`. Test 7 in `chat-url-params.test.mjs` pins the wiring. | `4f33c01` `fix(04.2-05): D1 — race-safe setEmployee in newTopic dispatch branch (chat/index.tsx)` |
| D2 | Seeded New Topic dialog missing `DialogTitle` for screen readers. | **Implemented** — seed dialog wrapper switched from `aria-label="…"` to `aria-labelledby="new-topic-dialog-title"`; the visible `<h3>Continue in chat — new topic</h3>` carries the matching `id`. `check-a11y` clean. | `fde4390` `fix(04.2-05): D2 — wire seed dialog aria-labelledby to its visible heading (chat/index.tsx)` |
| D3 | Active Tasks Owned rail rows + inline TASK CREATED cards not clickable to the issue's Reader. | **Implemented** — RefChip primitive renders as a host-routed anchor (`nav.linkProps('/<companyPrefix>/issues/<id>')`); the right-rail row title is also wrapped; the inline TASK CREATED card title is also wrapped. All three gated on companyPrefix+identifier availability so no `/undefined/issues/…` link can render. 14 new source-grep tests across three files. | `46a6441` `fix(04.2-05): D3 — RefChip + active-tasks rows + inline TASK CREATED cards link to /<prefix>/issues/<id>` |
| D4 | React key warnings flagged on TldrStrip / ActivityTimeline / RosterRail / ChatPageBody / ChatActionsRow / ContextRail / PersistedMessage / Composer. | **Defensive-only** — full `.map()` sweep across `src/ui/surfaces/chat/**`, `src/ui/surfaces/reader/**`, and `src/ui/primitives/toast.tsx` confirmed every callsite already carries a `key=` prop. The two remaining INDEX-only patterns inside `RuntimeNoiseRow` (message-thread.tsx lines 937 + 942) replaced with composite stable keys (`section-${i}-${title ?? ''}` and `row-${i}-${j}-${type ?? ''}`). The next operator drill will confirm the warnings are gone. | `5d82a31` `fix(04.2-05): D4/D5 — composite stable keys in RuntimeNoiseRow sections/rows` |
| D5 | `[Toast] aria-live region` may need polish. | **No change required** — `ToastProvider`'s `toasts.map((t) => <ChatToast key={t.id} …/>)` already uses a stable id. Bundled into D4's defensive sweep; nothing to fix. | (same as D4) |
| D6 | _reserved_ | n/a | — |
| D7 | Chat-topic Reader's parent breadcrumb reads "Chat — `<UUID>`" → operator mistakes it for an unstyled Continue button. | **Implemented** — new pure helper `formatParentIssueTitle(employeeName)` in `chat-topics.ts` returns `Chat thread` when employeeName matches a UUID shape, otherwise `Chat — <name>`. UI threads `employeeName: employee.name` through both `handleNewTopic` + `handleSeededCreate` createTopic calls so newly-bootstrapped parent issues get human-friendly titles. Existing parents keep their current titles (drawer notes this is acceptable). 6 formatter tests. | `59fb443` `fix(04.2-05): D7 — parent-title formatter avoids UUID-as-name fallback + thread employeeName from UI` |

## Suite delta

- Baseline (end of Plan 05-02 / clarity-pack-1.0.0-rc.2): **1339** tests / 1337 pass / 0 fail / 2 skip
- Final (clarity-pack-1.0.0-rc.3): **1360** tests / 1358 pass / 0 fail / 2 skip
- Net new: **+21** (D1 +1, D7 +6, D3 ref-chip +4, D3 active-tasks +5, D3 inline-card +5)

## Quality gates

- `tsc --noEmit` exit 0
- `node --test "test/**/*.test.mjs"` exit 0 (1358/0/0/2)
- `node scripts/check-css-scope.mjs` exit 0 (105 selectors, all scoped under `[data-clarity-surface]`)
- `node scripts/check-a11y.mjs` exit 0 (64 files, 0 violations)
- `node scripts/build-worker.mjs` clean (worker 242.2 kB)
- `node scripts/build-ui.mjs` clean (ui 290.9 kB)
- `tsc --project tsconfig.manifest.json` clean (dist/manifest.js carries `version: '1.0.0-rc.3'`)
- `npm pack` clean (143.2 kB / 622.5 kB unpacked / 14 files)

## Atomic commit trail

| # | Commit | Subject |
|---|--------|---------|
| 1 | `4f33c01` | fix(04.2-05): D1 — race-safe setEmployee in newTopic dispatch branch (chat/index.tsx) |
| 2 | `fde4390` | fix(04.2-05): D2 — wire seed dialog aria-labelledby to its visible heading (chat/index.tsx) |
| 3 | `59fb443` | fix(04.2-05): D7 — parent-title formatter avoids UUID-as-name fallback + thread employeeName from UI |
| 4 | `46a6441` | fix(04.2-05): D3 — RefChip + active-tasks rows + inline TASK CREATED cards link to /<prefix>/issues/<id> |
| 5 | `5d82a31` | fix(04.2-05): D4/D5 — composite stable keys in RuntimeNoiseRow sections/rows |
| 6 | `06ec6c5` | chore(04.2-05): version 1.0.0-rc.2 → 1.0.0-rc.3 — Plan 04.2-05 polish pass |

Task 7 (this SUMMARY + STATE + ROADMAP + MemPalace closure drawer) commits separately.

## Operator drill (deferred)

The orchestrator surfaced a verbatim copy-paste drill walkthrough in the terminal after Task 7. The walkthrough covers SCP / SSH / snapshot / install-helper / row-count COEXIST #6 / browser drill focused on:

- **Path 3 (newTopic dispatch / D1)** — CREATE TOPIC enables immediately on a cold task Continue.
- **Path 4 (chat-topic Reader / D7)** — Continue button absent (RCB-02 unchanged); the parent breadcrumb at the top of the Reader content area now reads `Chat — <name>` (when employeeName threaded) or `Chat thread` (when fallback). NO MORE "Chat — `<UUID>`".
- **D3** — the right-rail "Active Tasks Owned" rows + inline TASK CREATED cards are clickable; clicking lands on `/COU/issues/<identifier>` Reader.
- **D2 + D4 + D5** — browser console clean; no DialogContent warning; no React key warnings.

Eric will drill on his next sweep.

## What this does NOT change

- Phase 4.2 status: VERIFIED 2026-05-24 (per `04.2-VERIFICATION.md`). RCB-01..RCB-07 stay Implemented. Plan 04.2-05 is polish on top of the verified phase, not a closure plan.
- Existing chat-employee parent issues (e.g. Eric's `COU-989`) keep their current titles. Only NEW parent bootstraps get the D7 fix. Acceptable per drawer.
- The host's parent breadcrumb rendering is host-side; we changed our parent issue's TITLE so the breadcrumb reads better.

## References

- MemPalace `clarity_pack/decisions/drawer_clarity_pack_decisions_7435d91bc180b048de95e0ae` — D1-D6 spec
- MemPalace `clarity_pack/decisions/drawer_clarity_pack_decisions_50f2229a76335930ecfefd9d` — D7 spec
- MemPalace `clarity_pack/runbook/paperclip-issue-url-pattern` — `/COU/issues/<id>` URL pattern
- MemPalace `clarity_pack/runbook/chat-surface-render-conditional-on-employee` — D1 invariant
- MemPalace `clarity_pack/runbook/plugin-version-bump-two-sources` — version bump rule
- `.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md` — Phase 4.2 verified state
- `.planning/phases/04.2-reader-chat-bridge/04.2-05-PLAN.md` — task-level spec for this plan
