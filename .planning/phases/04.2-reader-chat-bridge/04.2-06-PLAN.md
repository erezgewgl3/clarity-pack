---
phase: 04.2-reader-chat-bridge
plan: 04.2-06
title: "Plan 04.2-06 — D9 + D10 UUID-leak fixes (v1.0.0-rc.4)"
wave: 1
gap_closure: true
autonomous: true
created: 2026-05-24
version_target: 1.0.0-rc.4
prior_version: 1.0.0-rc.3
status: CODE-COMPLETE-DRILL-DEFERRED
files_modified:
  - src/worker/handlers/chat-open-for-issue.ts
  - src/ui/surfaces/reader/continue-in-chat-button.tsx
  - src/worker/handlers/chat-topics.ts
  - src/ui/surfaces/chat/topic-strip.tsx
  - test/ui/continue-in-chat-button-d9.test.mjs
  - test/ui/topic-strip-d10.test.mjs
  - test/manifest/chat-capabilities.test.mjs
  - package.json
  - src/manifest.ts
---

<objective>
Close the two visible UUID-leak defects (D9 + D10) surfaced during the
2026-05-24 Plan 04.2-05 operator drill of clarity-pack-1.0.0-rc.3 on
Countermoves. Both leaks share the same root-cause family as D7 (missing
display-name resolution → fallback to raw UUID) but in different code paths
not covered by the D7 fix.

D4/D5 (drill-caught React key warnings still firing in rc.3) and D8 (Back-
after-deep-link drops chat state) are explicitly OUT of this plan's scope
and remain in the polish backlog.
</objective>

<defect_disposition>

| ID | Defect | Disposition | Commit |
|----|--------|-------------|--------|
| D9 | Reader Continue-in-chat button reads `Continue in chat with <UUID> →` | **Implemented** — chat-open-for-issue.ts resolves assignee display name via `ctx.agents.get(assigneeAgentId, companyId)` and threads `assigneeName: string \| null` into the result. continue-in-chat-button.tsx consumes `assigneeName ?? 'this employee'` (NEVER UUID). | `21008f6` |
| D10 | Topic-strip "About `<UUID>`" chip + click navigates to `/COU/issues/<UUID>` (404s) | **Implemented** — chat-topics.ts adds `resolveOriginIdentifiers` helper that resolves each distinct originIssueId UUID to its BEAAA-NNN identifier via `ctx.issues.get` (one roundtrip per distinct origin). TopicEntry carries `originIssueIdentifier: string \| null`. topic-strip.tsx derives `aboutIssueId` from the resolved identifier, hides the chip when null (no broken `/issues/<UUID>` ever renders). | `f9f9f6a` |

</defect_disposition>

<quality_gates>
- tsc --noEmit exit 0
- node --test "test/**/*.test.mjs" exit 0; suite 1360 → 1364 (+4: D9 +2, D10 +2)
- node scripts/check-css-scope.mjs exit 0 (105 selectors, all scoped)
- node scripts/check-a11y.mjs exit 0 (64 files, 0 violations)
- dist/manifest.js carries `version: '1.0.0-rc.4'`
- npm pack produces `clarity-pack-1.0.0-rc.4.tgz` — 143,583 bytes — sha256 `d015ebc665c58240e4ea06bf1ff44fde1e08166638293c1c27cb615c7e76789c`
</quality_gates>

<commit_trail>
| # | Commit | Subject |
|---|--------|---------|
| 1 | `21008f6` | fix(04.2-06): D9 — resolve assignee display name in chat.openForIssue + consume in Continue button |
| 2 | `f9f9f6a` | fix(04.2-06): D10 — resolve origin issue UUID to BEAAA-NNN identifier; topic-strip About chip uses it |
| 3 | `5d2644b` | chore(04.2-06): version 1.0.0-rc.3 → 1.0.0-rc.4 — Plan 04.2-06 D9 + D10 UUID-leak fixes |
</commit_trail>

<out_of_scope>
**Deferred to follow-up plans:**
- **D4/D5** — React key warnings on ContextRail / PersistedMessage / TrueTaskDialog / AnchoredToCards / ChatPageBody. Still firing in rc.3 drill console; the Plan 04.2-05 "Defensive-only" disposition was wrong. Real fix requires per-component key audits — separate plan.
- **D8** — Browser Back after deep-link consume drops chat state to empty. UX paper cut; side-effect of URL_HASH carrier's `nav.replace` design. Needs design thinking before fix.
</out_of_scope>

<drill>
Operator drill walkthrough delivered in the chat-window response that authorized this plan. Focused on D9 (Continue button label on a cold-task Reader reads `Continue in chat with <name> →`, NEVER UUID) + D10 (topic-strip chip reads `About COU-#### ↗`, click lands on the correct issue page). Snapshot-bookend per the standard CLAUDE.md rule; COEXIST #6 row counts byte-identical.
</drill>
</content>
