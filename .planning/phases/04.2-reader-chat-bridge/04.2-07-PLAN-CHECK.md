# Plan 04.2-07 Quality Check

**Verdict:** PASS

**Date:** 2026-05-25
**Plan reviewed:** `04.2-07-PLAN.md`
**Mode:** add-only addendum (Phase 4.2 closed at v1.0.0-rc.2; RCB-01..RCB-07 Implemented per `04.2-VERIFICATION.md`)
**Defects addressed:** D-5 (operator runbook), D-6 (README AC grammar), D-7 (load-bearing chat.openForIssue rewrite)
**Version target:** 1.0.0-rc.7 (from 1.0.0-rc.6)

---

## Standard gates

| # | Gate | Status | Evidence |
|---|------|--------|----------|
| 1 | Frontmatter validity — YAML parses, required fields present, version_target sensible | PASS | YAML parses cleanly. Fields present: `phase`, `plan`, `title`, `wave: 1`, `depends_on: []`, `created`, `version_target: 1.0.0-rc.7`, `prior_version: 1.0.0-rc.6`, `requirements: []` (deliberate — see gate 6), `defects_addressed: [D-7, D-5, D-6]`, `status: planned`, `files_modified` (13 entries), `must_haves` (12 truths, 6 artifacts, 4 key_links). Version bump rc.6 -> rc.7 is exactly one increment past the prior shipped tarball (`clarity-pack-1.0.0-rc.6.tgz`). |
| 2 | Every task has `<read_first>`, `<acceptance_criteria>`, `<action>`; no subjective language; no fenced code in `<action>` | PASS | All 8 tasks have populated `<read_first>` citing PATTERNS.md line ranges, CONTEXT.md D-IDs, and source/test analog line ranges. All have `<acceptance_criteria>` with grep counts / exit-0 commands / file-existence assertions. Action steps reference PATTERNS.md sections verbatim ("Mirror PATTERNS.md §3 lines 272-369 verbatim"; "Apply the diff in PATTERNS.md §4 lines 372-504 verbatim") instead of embedding code blocks. Task 8 has fully decomposed `<what-built>` with pre-flight / bookend / install / drill / rollback phases. |
| 3 | `<acceptance_criteria>` uses verifiable assertions | PASS | Every criterion is a runnable grep, exit-code assertion, count check, or operator-observable PASS/FAIL. Examples: Task 1 grep for `export async function listTopicsForIssueAndAssignee` returns exactly 1; Task 2 grep for `ctx.issues.update(` in chat-open-for-issue.ts returns 0 (CTT-07); Task 3 grep for `title=` with `assigneeAgentId` returns 0 (UUID never inside title attr). No subjective acceptance criteria. |
| 4 | Waves + dependencies make sense; no cycles | PASS | Single plan, Wave 1, `depends_on: []`. Intra-plan task ordering is implicit and sound: Task 1 (repo helper) -> Task 2 (handler uses helper) -> Tasks 3-5 (UI consumes new route) -> Tasks 6-7 (docs) -> Task 8 (version bump + drill, gated on ALL prior tasks). Task 8 `<read_first>` re-reads `runbook/operator-gotchas.md` AFTER Task 6 lands — only operator-attention ordering constraint. No cycles. |
| 5 | must_haves are testable goal-backward criteria, not vague | PASS | All 12 truths map to specific verifiable outcomes. Truth #2 (auto-opens popover pre-filtered for N>=2) is pinned by Task 2 RED test 4 + Task 8 drill scenario 7. Truth #5 (ctx.issues.update NEVER called) is pinned by Task 2 RED test 3 spy + Task 8 path 6 live DB probe. Truth #11 (version bump on BOTH package.json AND src/manifest.ts) is pinned by Task 8 grep acceptance for BOTH files. |

---

## Add-only special-case gates

| # | Gate | Status | Evidence |
|---|------|--------|----------|
| 6 | Empty `requirements: []` acceptable IF `<addresses>` block cites D-5/D-6/D-7 with MemPalace drawer paths + rc.6 drill SUMMARY | PASS | `<addresses>` block (plan lines 85-104) cites D-5/D-6/D-7 in a defect table with MemPalace drawer paths (`clarity_pack/decisions/260524-sm8-d7-reader-chat-continuation-gap`, `clarity_pack/runbook/d5-cou-2391-weak-test-case-mitigation`) and references the rc.6 drill SUMMARY at `.planning/quick/260524-sm8-drill-clarity-pack-1-0-0-rc-6-tgz-on-cou/260524-sm8-SUMMARY.md`. Block explicitly anticipates the plan-checker and explains why `requirements: []` is correct ("no new RCB IDs are coined"). |
| 7 | No other PLAN.md files modified; 04.2-01..06 byte-identical; ROADMAP / REQUIREMENTS / VERIFICATION untouched | PASS | `files_modified` enumerates only: 5 source files (handler + repo + 3 UI components), 4 NEW test files (`-d7` / `-ambiguous` / `-entry-point` / `-list-by-assignee` — no collision with closed-baseline tests), 2 docs (`runbook/operator-gotchas.md` append, `README.md` append), 2 version sources (`package.json`, `src/manifest.ts`). No PLAN/VERIFICATION/REQUIREMENTS/ROADMAP files appear. Task 8 explicitly says "DO NOT modify the existing 5; ADD a 6th" for drill fixtures (plan line 522). |
| 8 | No new chat-side parse path; `parseChatDeepLink` not extended (D-07) | PASS | must_have truth #7 states: "the canonical `existing-topic` URL_HASH deep-link via the unchanged `buildTopicDeepLink(companyPrefix, topicIssueId)` (D-07 deep-link contract — `parseChatDeepLink` is NOT extended)." Task 4 `<action>` step 6 verbatim: "DO NOT modify `src/ui/surfaces/chat/deep-link.mjs` (no `parseChatDeepLink` change; D-07 lock)." Task 4 source-grep T6 asserts `buildTopicDeepLink(companyPrefix, t.topicIssueId)` remains the row-click navigator; T7 forbids `buildChatDeepLink` being called with `route: 'existing-topics-ambiguous'`. Confirmed via repo grep: `existing-topics-ambiguous` has zero occurrences in `src/ui/surfaces/chat/deep-link.mjs` or `deep-link.d.mts` today, and the plan does not introduce any. |
| 9 | CTT-07 plugin-side archive preserved — D-04 uses `setChatTopicArchived`; `ctx.issues.update` NEVER appears in handler | PASS | must_have truth #5: "`setChatTopicArchived(ctx, companyId, topicIssueId, false)` is called, flipping `archived_at` to NULL — `ctx.issues.update` is NEVER called (CTT-07 plugin-side archive invariant)." Task 2 RED test 3 in `<behavior>` explicitly tests "`ctx.issues.update` spy received ZERO calls (CTT-07 invariant — mirror chat-topic-archive.test.mjs Test 6 pattern)." Task 2 acceptance: `grep -cE "ctx\.issues\.update\("` in chat-open-for-issue.ts returns 0 — file-level grep guarantee. Task 8 drill path 6 includes the live DB probe: `SELECT updated_at FROM public.issues WHERE id = '<X>';` UNCHANGED. |
| 10 | D-08 hygiene — every new payload field reuses server-resolved display names; UUIDs never reach UI text | PASS | must_have truth #6: "`existing-topics-ambiguous` result payload never carries a raw UUID in any operator-visible field — `assigneeName`, `candidate.topicId` (CHT-NN), and `sourceIssueIdentifier` (BEAAA-NNN) are server-resolved per D-08 (D9/D10 hygiene)." Task 2 `<action>`: "Reuse already-resolved `assigneeName` (lines 165-182) and `identifier` (lines 142-145); do NOT add new resolver calls." Task 2 RED test 5 asserts `assigneeName === 'CMO'`, `sourceIssueIdentifier === 'COU-2215'`, and `candidates[0].topicId` matches `/^CHT-\d+$/`. Task 3 source-grep T5 forbids `assigneeAgentId` inside any `title=` attribute. |
| 11 | 5 existing drill fixtures preserved; closure task adds 6th + 7th, doesn't modify them | PASS | Task 8 `<read_first>` says "DO NOT modify the existing 5; ADD a 6th." Drill walkthrough in `<what-built>` lists "Drill paths 1-5 — re-run the 5 existing Phase 4.2 closure fixtures verbatim from VERIFICATION.md (COU-2215 / COU-2361 / COU-2396 / COU-1115/CHT-1117 / COU-2399). All five MUST still PASS." NEW path 6 (cold-issue-with-existing-same-assignee-archived-topic) and NEW scenario 7 (ambiguous picker N=2) are additive. Acceptance #11: "RCB-01..RCB-07 not regressed (`04.2-VERIFICATION.md` baseline preserved)." |
| 12 | Closure task (Task 8) is snapshot-bookended per CLAUDE.md rule | PASS | Task 8 `<what-built>` covers all four phases: (a) **Bookend snapshot** via `scripts/safety/cli.mjs snapshot --db-url "$DB_URL"` with "Confirm the new snapshot ID is captured; bytes and sha256 of the snapshot tarball recorded." (b) **Rollback rehearsal BEFORE install** via `scripts/safety/cli.mjs verify` with `paperclip_restoring` pre-create per `§paperclip-restoring-db-precreate` runbook gotcha. (c) **Row-count baseline**: psql probe for `chat_topics`, `chat_messages`, `chat_topic_tasks` pre-uninstall AND post-install AND across rollback; byte-identical asserted (COEXIST #6). (d) **Final rollback rehearsal**: rc.7 -> rc.6 -> rc.7 with status=ready and row counts intact. Also: `clarity-pack-1.0.0-rc.7.tgz` bytes + sha256 captured (acceptance #6); plugin UUID `0d4fc40a-0541-4b67-8979-9d346cb9c07b` preserved (acceptance #4); canonical env re-export at start of VPS section per `vps-commands-self-contained` (plan line ~540); `cd ~/paperclip` before `pnpm paperclipai` calls per `paperclipai-workspace-script-gotcha` (plan lines 570, 572, 597, 599). |

---

## Goal-backward check (addendum-scope)

The "phase goal" for this addendum is the union of D-5 + D-6 + D-7 verifications, not Phase 4.2's already-achieved overall goal.

### D-7 (load-bearing — code change): cold-but-conversed-about issues open existing chat topic; picker on N>=2

| Verification | Where covered | Status |
|--------------|---------------|--------|
| N=1 silent resume (D-01 step-2) | Task 2 RED test 2 (unit) + Task 8 drill path 6 (live) | PASS |
| N=1 archived -> silent resume + auto-unarchive (D-04) | Task 2 RED test 3 (unit + spy on `setChatTopicArchived` + `ctx.issues.update` zero calls) + Task 8 drill path 6 (`SELECT archived_at` NULL probe + `SELECT updated_at FROM public.issues` UNCHANGED probe) | PASS |
| N>=2 ambiguous -> popover auto-opens pre-filtered (D-01 step-2 + D-02) | Task 2 RED test 4 (unit shape) + Task 3 source-grep T1+T3 (dispatch arm) + Task 4 source-grep T4+T5 (autoOpen useEffect + visibleTopics filter) + Task 8 drill scenario 7 (live N=2) | PASS |
| N=0 + 0 other-employee topics -> seeded New Topic dialog (D-11) | Task 2 RED test 1 (unit) + drill path 3 (COU-2396 cold) preserved | PASS |
| N=0 + N>0 cross-employee topics -> seeded New Topic dialog (D-03) | Task 2 RED test 6 (documentation pin via in-handler comment citing D-03; behaviour identical to Test 1 N=0). Acceptable because D-03 reduces to N=0 from step-2 same-assignee-filtered list. | PASS |
| chat-task lineage unchanged (RCB-02) | Task 2 RED test 7 (unit pins shape preserved + step-2 lookup NEVER called for CHAT_TASK_RE matches) + drill path 2 (COU-2361 chat-spawned) preserved | PASS |
| Tooltip differentiation per D-06 | Task 3 source-grep T4 ("Pick from ... conversations about") + Task 8 drill paths 1+2+6 read tooltip text verbatim | PASS |
| Deep-link contract unchanged (D-07) | Task 4 source-grep T6+T7 (`buildTopicDeepLink` is sole navigator; no `existing-topics-ambiguous` reaches chat surface) + Task 8 scenario 7 row-click navigates via canonical `existing-topic` URL_HASH | PASS |
| D-08 hygiene (no UUID in operator-visible text) | Task 2 RED test 5 + Task 3 source-grep T5 + Task 3 acceptance grep (`title=` with `assigneeAgentId` returns 0) | PASS |

### D-5 (operator runbook): `§ac-autostatus-drill-proof` next-drill proof rule

| Verification | Where covered | Status |
|--------------|---------------|--------|
| Section present with correct anchor | Task 6 acceptance: `grep -c '## §ac-autostatus-drill-proof' runbook/operator-gotchas.md` returns exactly 1 | PASS |
| Verbatim D-09 resolution rule present | Task 6 acceptance: `grep -c 'Step 9 MUST capture' runbook/operator-gotchas.md` returns exactly 1 | PASS |
| Discovered/source citation | Task 6 acceptance: `grep -c 'rc.6 drill'` returns >= 1 | PASS |
| Append-only convention honored | Task 6 acceptance: `git diff --stat` shows lines-added > 0 and lines-removed == 0 | PASS |

### D-6 (documentation): canonical AC marker grammar in README

| Verification | Where covered | Status |
|--------------|---------------|--------|
| Section present | Task 7 acceptance: `grep -c '## Acceptance criteria markers' README.md` returns exactly 1 | PASS |
| Canonical grammar `AC: <id>:` documented | Task 7 acceptance: `grep -c 'AC: <id>:' README.md` returns >= 1 | PASS |
| Bracket-alternate documented as tolerated-but-not-canonical | Task 7 acceptance: `grep -c 'AC\[<id>\]:' README.md` returns >= 1 | PASS |
| Scanner code unchanged | Task 7 `<action>`: "No code change to `src/worker/handlers/reader-ac-autostatus.ts` (scanner is unchanged)" | PASS |

**All three forward defects have verification coverage at both unit-test tier (where applicable) and operator-drill tier.** D-5 and D-6 are documentation-only and grep-pinned; D-7 has unit-test pins (Tasks 1-4) AND live-drill coverage (Task 8 paths 6 + 7).

---

## Findings

### BLOCKING
None.

### RECOMMENDED
None.

### OPTIONAL

1. **Task 2 RED test 6 is a comment-pin rather than a behavioural assertion.** The D-03 cross-employee fall-through case is documented via "a comment in the handler citing D-03" but does not have an independent spy assertion. This is acceptable because D-03 reduces to N=0 from step-2's already-same-assignee-filtered list (so RED test 1 is the behavioural pin), and PATTERNS.md confirms the helper's WHERE clause does the filtering. No revision needed — the plan correctly notes this is a deliberate choice ("RED via Test 1's N=0 case is sufficient"). Filed here only so a future reader does not flag it as missing coverage.

2. **Task 6 grep for `'rc.6 drill'`** uses a pipe-to-head fallback (`| head -1`) in the acceptance line; this is decorative since `grep -c` already returns the count. Not a functional issue.

3. **Task 8 `<resume-signal>`** asks for "three row-count triples" — the canonical shape is pre-uninstall, post-install, post-rollback, but the prose could spell out which is which for an operator running this drill for the first time. The drill walkthrough does enumerate them in `<what-built>`, so this is purely a UX nicety.

None of the above prevent execution.

---

## Recommendation

**PASS — execution-ready, no revisions required.**

Plan 04.2-07 is a textbook add-only forward-defects addendum:

- The empty `requirements: []` is properly accounted for by an `<addresses>` block citing D-5/D-6/D-7 with MemPalace drawer paths and the rc.6 drill SUMMARY reference (gate 6).
- D-7's load-bearing routing rewrite has unit-test coverage at three tiers (repo SQL pin in Task 1, handler shape pins in Task 2, UI source-grep in Tasks 3-5) plus live-drill coverage at Task 8 paths 6+7.
- The closed-baseline contracts are explicitly preserved: D-07 deep-link contract (`parseChatDeepLink` untouched, `buildTopicDeepLink` is the only navigator), CTT-07 plugin-side archive (handler grep forbids `ctx.issues.update`), the 5-path drill fixtures (additive 6th + 7th only), and the RCB-01..RCB-07 baseline.
- D-5 and D-6 fold-ins are documentation-only, grep-pinned, and explicitly do not touch the Plan 05-03 AC-autostatus scanner.
- Task 8's closure drill honors every CLAUDE.md hard rule (bookended snapshot + verified rollback rehearsal; plugin-version-bump-two-sources; COEXIST #6 row counts byte-identical across all transitions) and every operator-runbook gotcha cited in MemPalace (`vps-commands-self-contained`, `paperclipai-workspace-script-gotcha`, `§paperclip-restoring-db-precreate`).

The plan is ready for `/gsd:execute-plan 04.2-07`. Expected output on success: `clarity-pack-1.0.0-rc.7.tgz` live on Countermoves with all six drill paths + scenario 7 PASS, RCB-01..RCB-07 still Implemented, runbook + README documentation gaps closed.

---

*Checked: 2026-05-25 by gsd-plan-checker (opus-4.7-1m)*
*Verdict: PASS — execution-ready, no revisions required*
