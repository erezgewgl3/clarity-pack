---
phase: 04.2-reader-chat-bridge
plan: 04.2-07
title: "Plan 04.2-07 — D-7 routing rewrite (existing-topics-ambiguous) + D-5/D-6 polish fold-in"
wave: 1
depends_on: []
gap_closure: false
autonomous: false
created: 2026-05-25
version_target: 1.0.0-rc.7
prior_version: 1.0.0-rc.6
requirements: []
defects_addressed: [D-7, D-5, D-6]
status: planned
files_modified:
  - src/worker/handlers/chat-open-for-issue.ts
  - src/worker/db/chat-topics-repo.ts
  - src/ui/surfaces/reader/continue-in-chat-button.tsx
  - src/ui/surfaces/reader/reverse-topics-link.tsx
  - src/ui/surfaces/reader/index.tsx
  - runbook/operator-gotchas.md
  - README.md
  - test/worker/handlers/chat-open-for-issue-d7.test.mjs
  - test/worker/db/chat-topics-repo-list-by-assignee.test.mjs
  - test/ui/continue-in-chat-button-ambiguous.test.mjs
  - test/ui/reverse-topics-link-entry-point.test.mjs
  - package.json
  - src/manifest.ts

must_haves:
  truths:
    - "Reader Continue-in-chat button silently resumes an existing same-assignee chat topic when exactly one chat_topics row has origin_issue_id == issueId AND employee_agent_id == assigneeAgentId (D-01 step-2, N=1)."
    - "Reader Continue-in-chat button auto-opens the RCB-06 popover pre-filtered to same-assignee candidates when N=2+ same-assignee matches exist (D-01 step-2, N>=2; D-02 popover reuse — no new picker component, no chat-surface navigation until operator picks)."
    - "Reader Continue-in-chat button routes to the seeded New Topic dialog (today's `new-topic-needed`) when 0 same-assignee matches exist AND >0 other-employee topics about the same issue exist (D-03 cross-employee fall-through)."
    - "Reader Continue-in-chat button continues to route via `new-topic-needed` when 0 chat topics about the issue exist at all (D-11 unchanged cold-issue behaviour)."
    - "On the single-archived-match resume path (D-04), `setChatTopicArchived(ctx, companyId, topicIssueId, false)` is called, flipping `archived_at` to NULL — `ctx.issues.update` is NEVER called (CTT-07 plugin-side archive invariant)."
    - "`existing-topics-ambiguous` result payload never carries a raw UUID in any operator-visible field — `assigneeName`, `candidate.topicId` (CHT-NN), and `sourceIssueIdentifier` (BEAAA-NNN) are server-resolved per D-08 (D9/D10 hygiene)."
    - "Picker-resolved row click in `reverse-topics-link.tsx` emits the canonical `existing-topic` URL_HASH deep-link via the unchanged `buildTopicDeepLink(companyPrefix, topicIssueId)` (D-07 deep-link contract — `parseChatDeepLink` is NOT extended)."
    - "Reader header still mounts `reverse-topics-link.tsx` for the manual-click path; the popover toggles open on its own header-button click as before when no `autoOpen` request is active (RCB-06 unchanged)."
    - "`runbook/operator-gotchas.md` contains the new `§ac-autostatus-drill-proof` section with the verbatim D-09 Step-9 proof rule."
    - "`README.md` contains the new `## Acceptance criteria markers` section documenting canonical `AC: <id>: ✓` grammar and the bracket-alternate tolerance (D-10)."
    - "`package.json` and `src/manifest.ts` both read `version: '1.0.0-rc.7'` after the bump (plugin-version-bump-two-sources rule)."
    - "Countermoves snapshot-bookended operator drill: all 5 existing drill paths (COU-2215 / COU-2361 / COU-2396 / COU-1115/CHT-1117 / COU-2399) still PASS and the new 6th fixture (cold-issue-with-existing-same-assignee-archived-topic — N=1 silent resume + auto-unarchive) PASSES. RCB-01..RCB-07 unchanged."
  artifacts:
    - path: "src/worker/db/chat-topics-repo.ts"
      provides: "listTopicsForIssueAndAssignee(ctx, companyId, originIssueId, employeeAgentId) — D-05 sort, includes archived"
      exports: ["listTopicsForIssueAndAssignee"]
    - path: "src/worker/handlers/chat-open-for-issue.ts"
      provides: "extended 6-route resolver: existing-topic | existing-topics-ambiguous | new-topic-needed | topic-itself | NO_ASSIGNEE error"
      contains: "existing-topics-ambiguous"
    - path: "src/ui/surfaces/reader/continue-in-chat-button.tsx"
      provides: "dispatch arm for existing-topics-ambiguous → onRequestPickerOpen({ filterToAssignee })"
      contains: "existing-topics-ambiguous"
    - path: "src/ui/surfaces/reader/reverse-topics-link.tsx"
      provides: "entryPoint + filterToAssignee + autoOpen props; visibleTopics derivation"
      contains: "autoOpen"
    - path: "src/ui/surfaces/reader/index.tsx"
      provides: "parent-owned pickerRequest state plumbing Continue button → ReverseTopicsLink"
      contains: "pickerRequest"
    - path: "runbook/operator-gotchas.md"
      provides: "D-09 next-drill proof rule"
      contains: "§ac-autostatus-drill-proof"
    - path: "README.md"
      provides: "AC marker canonical grammar documentation"
      contains: "Acceptance criteria markers"
  key_links:
    - from: "src/worker/handlers/chat-open-for-issue.ts"
      to: "src/worker/db/chat-topics-repo.ts"
      via: "named import { listTopicsForIssueAndAssignee, setChatTopicArchived }"
      pattern: "listTopicsForIssueAndAssignee"
    - from: "src/ui/surfaces/reader/index.tsx"
      to: "src/ui/surfaces/reader/continue-in-chat-button.tsx"
      via: "onRequestPickerOpen prop wired to setPickerRequest"
      pattern: "onRequestPickerOpen"
    - from: "src/ui/surfaces/reader/index.tsx"
      to: "src/ui/surfaces/reader/reverse-topics-link.tsx"
      via: "autoOpen + filterToAssignee + entryPoint props plumbed from pickerRequest state"
      pattern: "autoOpen"
    - from: "src/ui/surfaces/reader/reverse-topics-link.tsx"
      to: "src/ui/surfaces/chat/deep-link.mjs"
      via: "buildTopicDeepLink(companyPrefix, topicIssueId) — unchanged contract (D-07 lock)"
      pattern: "buildTopicDeepLink"
---

<addresses>
This plan is a forward-defects addendum to Phase 4.2 (RCB-01..RCB-07 already
Implemented at v1.0.0-rc.2; closure baseline preserved in
`.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md`). The phase's
`requirements: []` field is empty by design — no new RCB IDs are coined; this
plan addresses three forward defects filed during the rc.6 drill `260524-sm8`
on Countermoves 2026-05-25.

| Defect | Description | MemPalace drawer | Origin SUMMARY |
|--------|-------------|------------------|----------------|
| D-7 (load-bearing) | `chat.openForIssue` opens NEW topic on cold-but-conversed-about issues; ignores `chat_topics.origin_issue_id` reverse-link | `clarity_pack/decisions/260524-sm8-d7-reader-chat-continuation-gap` | `.planning/quick/260524-sm8-drill-clarity-pack-1-0-0-rc-6-tgz-on-cou/260524-sm8-SUMMARY.md` |
| D-5 (operator-runbook) | rc.6 drill Step 9 had no documented proof artifact for the AC-toggle → Reader-refetch contract; closure depended on undocumented DevTools observation | `clarity_pack/runbook/d5-cou-2391-weak-test-case-mitigation` | (same SUMMARY) |
| D-6 (documentation) | Historical AC audit comment mixed canonical (`AC: 1: ✓`) and bracket-alternate (`AC[2]: done`) grammars in one comment body; canonical grammar undocumented | (filed in same drill SUMMARY) | (same SUMMARY) |

The plan-checker has been instructed (via the orchestrator's
`<planning_context>`) that empty `requirements: []` is permitted in this
specific add-only/forward-defects-addendum scenario. All locked decisions
(D-01..D-12) live in `04.2-CONTEXT.md`; all touchpoint analogs live in
`04.2-PATTERNS.md`. Both must be read before executing any task.
</addresses>

<objective>
Close three forward defects (D-7 load-bearing, D-5 + D-6 polish fold-in) filed
during the 2026-05-25 rc.6 operator drill of clarity-pack-1.0.0-rc.6.tgz on
Countermoves. Phase 4.2 RCB-01..RCB-07 stay Implemented and must not regress.

**D-7 (load-bearing — code change):** `chat.openForIssue` adds a step-2
reverse-lookup against `chat_topics.origin_issue_id + employee_agent_id` so
the Reader's "Continue in chat" button silently resumes an existing
same-assignee chat topic when one exists (N=1), auto-opens the RCB-06 popover
pre-filtered to same-assignee candidates when multiple exist (N>=2), and
falls through to the seeded New Topic dialog only when zero same-assignee
matches exist (N=0 + cross-employee fall-through per D-03). Single-archived
silent resume flips `chat_topics.archived_at` to NULL via the existing
`setChatTopicArchived` helper — `ctx.issues.update` is NEVER called.

**D-5 (operator runbook — documentation):** append a new
`§ac-autostatus-drill-proof` section to `runbook/operator-gotchas.md`
codifying the next drill's Step-9 proof rule.

**D-6 (README — documentation):** add an `## Acceptance criteria markers`
section to `README.md` documenting canonical `AC: <id>: ✓` grammar and the
bracket-alternate tolerance.

**Closure:** version bump to 1.0.0-rc.7 in `package.json` + `src/manifest.ts`,
`npm pack`, snapshot-bookended Countermoves drill covering 5 existing
fixtures + 1 new 6th fixture (cold-issue-with-existing-same-assignee-archived
-topic) + 1 ambiguous-fixture scenario (issue with N=2 same-assignee topics).

Output: `clarity-pack-1.0.0-rc.7.tgz` (operator captures bytes + sha256 at
drill time), Countermoves drill PASS for all six paths, RCB-01..RCB-07 still
Implemented.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md
@.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md
@.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md
@.planning/phases/04.2-reader-chat-bridge/04.2-06-SUMMARY.md
@CLAUDE.md

<!-- Source files modified by this plan -->
@src/worker/handlers/chat-open-for-issue.ts
@src/worker/db/chat-topics-repo.ts
@src/ui/surfaces/reader/continue-in-chat-button.tsx
@src/ui/surfaces/reader/reverse-topics-link.tsx
@src/ui/surfaces/reader/index.tsx
@runbook/operator-gotchas.md
@README.md

<!-- Test pin reference files -->
@test/ui/continue-in-chat-button-d9.test.mjs
@test/worker/chat-topics-repo.test.mjs
@test/worker/chat/chat-open-for-issue.test.mjs
@test/worker/chat/chat-topic-archive.test.mjs

<interfaces>
<!-- Server-side contract extensions the executor will implement. -->
<!-- See 04.2-PATTERNS.md §2 lines 60-88 for the exact ChatOpenForIssueResult diff. -->

ChatOpenForIssueResult (existing — see chat-open-for-issue.ts lines 55-69):
  - kind: 'chatOpenForIssue'
  - route: 'existing-topic' | 'new-topic-needed' | 'topic-itself'   ← add 'existing-topics-ambiguous'
  - topicIssueId?, sourceCommentId?, assigneeAgentId?, assigneeName?: string | null
  - seedTitle?, seedBody?, error?

ChatOpenForIssueResult addendum (NEW fields — both optional, present only on
the new 'existing-topics-ambiguous' route):
  - candidates?: Array<{
      topicIssueId: string;            // chat-topic UUID — internal only
      topicId: string;                  // CHT-NN — operator-visible (D-08)
      title: string;
      lastActivityAt: string;
      lastMessagePreview?: string;
      archived: boolean;
    }>
  - sourceIssueIdentifier?: string;    // BEAAA-NNN — already resolved from
                                       // issue.identifier at chat-open-for-issue.ts
                                       // lines 142-145 (D-08; reuse — no new resolver)

listTopicsForIssueAndAssignee (NEW repo helper — chat-topics-repo.ts):
  Signature:
    async function listTopicsForIssueAndAssignee(
      ctx: ChatTopicsRepoCtx,
      companyId: string,
      originIssueId: string,
      employeeAgentId: string,
    ): Promise<Array<{
      topicIssueId: string;
      topicId: string;
      title: string;
      lastActivityAt: string;
      archived: boolean;
    }>>

  Semantics:
    - INCLUDES archived rows (D-04 — no archived_at IS NULL filter)
    - ORDER BY GREATEST(chat_topics.last_activity_at, MAX(chat_messages.sent_at)) DESC
      (D-05 tiebreaker — note column is `sent_at` per CHAT_MESSAGE_COLS in this file,
       NOT `created_at` as CONTEXT.md sketched)
    - SELECTs from plugin_clarity_pack_cdd6bda4bd schema (no new migration)

ContinueInChatButtonProps addendum (continue-in-chat-button.tsx):
  - onRequestPickerOpen?: (req: { filterToAssignee: string | null }) => void;
    Called from the existing-topics-ambiguous dispatch arm; replaces nav.navigate.

ReverseTopicsLinkProps addendum (reverse-topics-link.tsx):
  - entryPoint?: 'continue-in-chat' | 'manual'    (default 'manual')
  - filterToAssignee?: string | null              (null = show all topics; today's behaviour)
  - autoOpen?: boolean                            (when true, popover opens via useEffect)

ReverseTopic addendum (reverse-topics-link.tsx):
  - employeeAgentId?: string                      (so visibleTopics filter can match)

Reader index parent-owned state (reader/index.tsx):
  - const [pickerRequest, setPickerRequest] = React.useState<{
      filterToAssignee: string | null;
    } | null>(null);
  - <ContinueInChatButton ... onRequestPickerOpen={setPickerRequest} />
  - <ReverseTopicsLink
      ...
      entryPoint={pickerRequest ? 'continue-in-chat' : 'manual'}
      filterToAssignee={pickerRequest?.filterToAssignee ?? null}
      autoOpen={pickerRequest !== null}
    />
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: listTopicsForIssueAndAssignee repo helper (D-04 + D-05)</name>
  <files>
    src/worker/db/chat-topics-repo.ts,
    test/worker/db/chat-topics-repo-list-by-assignee.test.mjs
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§2 lines 147-247 — analog `listChatTopicsByOriginIssue` shape + D-05 tiebreaker SQL),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-04, D-05),
    @src/worker/db/chat-topics-repo.ts (read once; existing helpers: `listChatTopicsByOriginIssue` line ~197, `setChatTopicArchived` line ~335, CHAT_TOPIC_COLS, CHAT_MESSAGE_COLS — note `sent_at` is the column name in chat_messages, NOT `created_at`),
    @test/worker/chat-topics-repo.test.mjs (R1 lines 561-582, R2 lines 584-594 — the SQL-shape pin idiom to mirror)
  </read_first>
  <behavior>
    - RED test 1 (SQL shape pin): the helper issues a SINGLE `ctx.db.query` call; the SQL string contains `WHERE company_id = $1 AND origin_issue_id = $2 AND employee_agent_id = $3` and `ORDER BY` clause includes both `chat_topics.last_activity_at` and `chat_messages.sent_at` (mirroring R1's regex idiom from chat-topics-repo.test.mjs).
    - RED test 2 (params positional order): `[companyId, originIssueId, employeeAgentId]` in that exact order.
    - RED test 3 (archived rows included): when the spy `db.query` returns two rows where one has `archived: true`, the helper returns BOTH rows; the `archived` flag is preserved on each returned entry (no client-side filter strips it).
    - RED test 4 (camelCase return shape): each entry has `topicIssueId`, `topicId`, `title`, `lastActivityAt`, `archived` (no UUID-named keys other than `topicIssueId` which is the internal id; `topicId` carries the CHT-NN sequence).
    - RED test 5 (empty result): when the spy returns an empty array, the helper returns `[]` (not null, no throw).
  </behavior>
  <action>
    Mirror `listChatTopicsByOriginIssue` (lines 197-215) plus an `employee_agent_id` predicate and the D-05 GREATEST(...) ORDER BY using a correlated subquery against `plugin_clarity_pack_cdd6bda4bd.chat_messages` keyed on `topic_issue_id = chat_topics.issue_id`. Use the actual column name `sent_at` per CHAT_MESSAGE_COLS. Do NOT add a `archived_at IS NULL` filter — D-04 includes archived rows. Map each row to the camelCase shape declared in the `<interfaces>` block; coerce `archived` to a boolean with `r.archived === true`. Tests live in a NEW file `test/worker/db/chat-topics-repo-list-by-assignee.test.mjs` mirroring the spy-based ctx pattern from `test/worker/chat-topics-repo.test.mjs` R1+R2. No new migration — schema already has both `origin_issue_id` (migration 0009) and `employee_agent_id` (migration 0006).
  </action>
  <verify>
    <automated>node --test test/worker/db/chat-topics-repo-list-by-assignee.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/worker/db/chat-topics-repo-list-by-assignee.test.mjs` exits 0 with all 5 tests PASS (file: test/worker/db/chat-topics-repo-list-by-assignee.test.mjs).
    - `grep -n "export async function listTopicsForIssueAndAssignee" src/worker/db/chat-topics-repo.ts` returns exactly 1 line.
    - `grep -cE "ORDER BY .*GREATEST" src/worker/db/chat-topics-repo.ts` returns >= 1 (D-05 tiebreaker present).
    - `grep -cE "archived_at IS NULL" src/worker/db/chat-topics-repo.ts | head -1` does NOT show a new occurrence in the new helper (D-04 — archived rows included). (Existing occurrences elsewhere in the file are permitted.)
    - tsc --noEmit exit 0.
    - No new migration file created (re-verify by `ls migrations/ | sort` — last file remains the highest-numbered one already present; new helper is read-only over existing schema).
  </acceptance_criteria>
  <done>
    Worker repo helper exposes `listTopicsForIssueAndAssignee`; SQL shape + ordering + param order pinned by tests; archived rows included; tsc clean.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: chat.openForIssue step-2 reverse-lookup + existing-topics-ambiguous route (D-01 + D-04 + D-08)</name>
  <files>
    src/worker/handlers/chat-open-for-issue.ts,
    test/worker/handlers/chat-open-for-issue-d7.test.mjs
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§1 lines 27-144 — step-2 insertion point, ctx-extension shape, return-shape diff, D9 hygiene reuse, error pattern; §Shared C lines 601-618 — CTT-07 spy idiom),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-01, D-04, D-08),
    @src/worker/handlers/chat-open-for-issue.ts (read once; existing CHAT_TASK_RE arm lines 184-195, D9 assigneeName resolve lines 165-182, issue.identifier capture lines 142-145),
    @test/worker/chat/chat-open-for-issue.test.mjs (Tests 1-4 lines 105-151 — handler ctx + opt-in-gate + existing-topic shape),
    @test/worker/chat/chat-topic-archive.test.mjs (Test 6 lines 45-94 — _issueUpdateCalls spy idiom to copy for D-04 invariant)
  </read_first>
  <behavior>
    - RED test 1 (N=0 fall-through): when `listTopicsForIssueAndAssignee` spy returns `[]` AND issue.originId is NOT a chat-task lineage match AND issue is not a chat-topic itself AND has an assignee, the result is `{ kind: 'chatOpenForIssue', route: 'new-topic-needed', ... }`.
    - RED test 2 (N=1 silent resume): spy returns one non-archived row → result is `{ route: 'existing-topic', topicIssueId: <row.topicIssueId>, assigneeAgentId, assigneeName, ... }` AND `sourceCommentId` is absent (resuming the thread, not a specific comment, per D-01).
    - RED test 3 (N=1 archived silent resume + auto-unarchive): spy returns one row with `archived: true` → result is `{ route: 'existing-topic', topicIssueId: ... }` AND `setChatTopicArchived` spy was called exactly once with `(ctx, companyId, row.topicIssueId, false)` AND `ctx.issues.update` spy received ZERO calls (CTT-07 invariant — mirror chat-topic-archive.test.mjs Test 6 pattern). The logger's `info` channel records the unarchive (assert at least one info-level call mentioning unarchive).
    - RED test 4 (N>=2 ambiguous): spy returns two rows → result is `{ route: 'existing-topics-ambiguous', assigneeAgentId, assigneeName, sourceIssueIdentifier, candidates: [...] }` AND `candidates.length === 2` AND every candidate entry has `topicId` (CHT-NN string) and a `archived` boolean, AND `sourceIssueIdentifier` matches the resolved BEAAA-NNN identifier (not the issue UUID).
    - RED test 5 (D-08 hygiene — no UUID in operator-visible fields): when spy resolves `assigneeName: 'CMO'` and `identifier: 'COU-2215'`, the ambiguous result's `assigneeName === 'CMO'`, `sourceIssueIdentifier === 'COU-2215'`, and NO field in candidates equals a UUID-shaped string. Assert via a regex check that `candidates[0].topicId` matches `/^CHT-\d+$/`.
    - RED test 6 (D-03 cross-employee fall-through is handled by step-2 returning 0 — same-assignee-filtered; do NOT add extra logic here): RED via Test 1's N=0 case is sufficient. Add a comment in the handler citing D-03.
    - RED test 7 (chat-task lineage UNCHANGED): when originId matches CHAT_TASK_RE, the result is still `{ route: 'existing-topic', topicIssueId: <chatTaskMatch[1]>, sourceCommentId: <chatTaskMatch[2]>, ... }` AND step-2 lookup is NEVER called (Test 4 shape preserved from the closure baseline).
    - RED test 8 (step-2 lookup throws → fail-open): when `listTopicsForIssueAndAssignee` spy throws, handler logs warn and falls through to `new-topic-needed` (does NOT crash; mirrors the existing ctx.issues.get pattern at lines 110-130).
  </behavior>
  <action>
    Extend `ChatOpenForIssueCtx` to include `db: PluginDatabaseClient` per PATTERNS.md §1 lines 46-57. Extend `ChatOpenForIssueResult.route` union with `'existing-topics-ambiguous'` and add optional `candidates` + `sourceIssueIdentifier` fields per PATTERNS.md §1 lines 60-88. Import `listTopicsForIssueAndAssignee` and `setChatTopicArchived` from `../db/chat-topics-repo.ts`. Insert step-2 logic AFTER the CHAT_TASK_RE arm (lines 184-195) and BEFORE the cold-task fallthrough; branch on cardinality (0 → fall through; 1 → return existing-topic shape without sourceCommentId AND if archived, call setChatTopicArchived + log info; >=2 → return existing-topics-ambiguous shape). Wrap the step-2 call in try/catch matching the issue-get pattern (lines 110-130) — on throw log warn and fall through. Reuse already-resolved `assigneeName` (lines 165-182) and `identifier` (lines 142-145); do NOT add new resolver calls. Use `ctx.logger?.info?.(...)` for the unarchive (per Claude's-discretion D-04 hint). Tests live in a NEW file `test/worker/handlers/chat-open-for-issue-d7.test.mjs` (parallel to the existing chat-open-for-issue.test.mjs); ctx fakes mirror Tests 1-4 of the closure-baseline test plus `db` and `listTopicsForIssueAndAssignee`/`setChatTopicArchived` spies plus the `_issueUpdateCalls` spy from chat-topic-archive.test.mjs Test 6.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/chat-open-for-issue-d7.test.mjs test/worker/chat/chat-open-for-issue.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/worker/handlers/chat-open-for-issue-d7.test.mjs` exits 0 with all 8 tests PASS.
    - `node --test test/worker/chat/chat-open-for-issue.test.mjs` STILL exits 0 (existing Tests 1-4 not regressed; chat-task lineage shape unchanged).
    - `grep -nE "existing-topics-ambiguous" src/worker/handlers/chat-open-for-issue.ts` returns >= 1 line (route is named).
    - `grep -cE "setChatTopicArchived\(" src/worker/handlers/chat-open-for-issue.ts` returns >= 1 (D-04 unarchive call site present).
    - `grep -cE "ctx\.issues\.update\(" src/worker/handlers/chat-open-for-issue.ts` returns 0 (CTT-07 invariant — handler never mutates host issue).
    - `grep -nE "listTopicsForIssueAndAssignee" src/worker/handlers/chat-open-for-issue.ts` returns >= 1 line.
    - tsc --noEmit exit 0.
  </acceptance_criteria>
  <done>
    chat.openForIssue's 5-case switch is now 6-case: existing-topic (originId lineage), existing-topic (reverse-lookup single-resume + auto-unarchive), existing-topics-ambiguous, topic-itself, NO_ASSIGNEE, new-topic-needed. CTT-07 invariant pinned by spy. D-08 hygiene: no UUID in operator-visible payload fields.
  </done>
</task>

<task type="auto">
  <name>Task 3: continue-in-chat-button dispatch arm + tooltip extension (D-02 + D-06 + D-08)</name>
  <files>
    src/ui/surfaces/reader/continue-in-chat-button.tsx,
    test/ui/continue-in-chat-button-ambiguous.test.mjs
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§3 lines 272-369 — type extension, employeeLabel pattern, tooltip-differentiation, dispatch arm; §Shared F lines 632-648 — source-grep idiom for .tsx),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-02, D-06, D-08),
    @src/ui/surfaces/reader/continue-in-chat-button.tsx (read once; existing ChatOpenForIssueResult mirror lines 64-77, employeeLabel line 171-172, tooltip lines 196-199, render branches lines 162-219),
    @test/ui/continue-in-chat-button-d9.test.mjs (lines 16-38 source-grep idiom; lines 49-63 UUID-leak guardrail to mirror)
  </read_first>
  <behavior>
    - Source-grep T1: file contains the literal route name `'existing-topics-ambiguous'` (single-quoted) — verifies the dispatch arm exists.
    - Source-grep T2: file contains `onRequestPickerOpen?` as a typed prop declaration — verifies the prop is present in `ContinueInChatButtonProps`.
    - Source-grep T3: file contains a call expression `onRequestPickerOpen?.({` — verifies the dispatch arm invokes the prop (not nav.navigate).
    - Source-grep T4: stripped source contains the literal tooltip phrase `Pick from ` near `conversations about` — verifies D-06 tooltip-differentiation extension.
    - Source-grep T5 (UUID-leak guardrail, mirror lines 49-63 of d9 test): for the new arm, no occurrence of `result.assigneeAgentId` inside a `title=` attribute or directly inside the button text (assert via a regex that scans for the substring `assigneeAgentId` within 200 chars before `</button>` and fails if found).
    - Source-grep T6: file contains `result.sourceIssueIdentifier` reference — verifies the BEAAA-NNN identifier is consumed (D-08).
    - Source-grep T7: the existing employeeLabel fallback `'this employee'` is still present (D9 baseline unchanged).
  </behavior>
  <action>
    Mirror PATTERNS.md §3 lines 272-369 verbatim:
    1. Extend the local `ChatOpenForIssueResult` mirror type to add `'existing-topics-ambiguous'` to the route union and add optional `candidates: Array<{ topicIssueId; topicId; title; lastActivityAt; lastMessagePreview?; archived; employeeAgentId? }>` + `sourceIssueIdentifier?: string` fields.
    2. Extend `ContinueInChatButtonProps` with `onRequestPickerOpen?: (req: { filterToAssignee: string | null }) => void`.
    3. Refactor the tooltip expression from the existing 2-way ternary into the 3-way structure per PATTERNS.md lines 308-316 (existing-topics-ambiguous → "Pick from N conversations about <identifier> →"; originId-lineage existing-topic → "Open source topic <id> →"; reverse-lookup existing-topic → "Resume conversation about <identifier> →"; default → "Continue in chat with <employeeLabel> →"). Button LABEL stays "Continue in chat with <employeeLabel> →" unchanged across all arms (D-06 lock).
    4. Add a dispatch branch BEFORE the existing buildChatNav path: `if (result.route === 'existing-topics-ambiguous')` → render the button with `onClick={() => onRequestPickerOpen?.({ filterToAssignee: result.assigneeAgentId ?? null })}` and `data-clarity-route="existing-topics-ambiguous"`. NO call to `nav.navigate`. Consume `result.sourceIssueIdentifier` in the tooltip text via the extended expression above; consume `employeeLabel` (the existing D9 fallback) for the button text.
    5. NEW test file `test/ui/continue-in-chat-button-ambiguous.test.mjs` mirrors the d9 source-grep idiom (lines 16-38) — read FILE with comment-strip, run the 7 regex assertions enumerated in `<behavior>`.
  </action>
  <verify>
    <automated>node --test test/ui/continue-in-chat-button-ambiguous.test.mjs test/ui/continue-in-chat-button-d9.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/ui/continue-in-chat-button-ambiguous.test.mjs` exits 0 with all 7 source-grep assertions PASS.
    - `node --test test/ui/continue-in-chat-button-d9.test.mjs` STILL exits 0 (existing D9 UUID-leak guardrail not regressed).
    - `grep -c "existing-topics-ambiguous" src/ui/surfaces/reader/continue-in-chat-button.tsx` returns >= 2 (route in the type union + dispatch branch + data-clarity-route attribute).
    - `grep -c "onRequestPickerOpen" src/ui/surfaces/reader/continue-in-chat-button.tsx` returns >= 2 (prop declaration + invocation).
    - `grep -c "Pick from " src/ui/surfaces/reader/continue-in-chat-button.tsx` returns >= 1 (D-06 tooltip).
    - `grep -cE "title=\\{[^}]*assigneeAgentId" src/ui/surfaces/reader/continue-in-chat-button.tsx` returns 0 (UUID never inside a title attribute expression).
    - tsc --noEmit exit 0.
  </acceptance_criteria>
  <done>
    Continue button has a new dispatch arm for existing-topics-ambiguous that auto-opens the popover via `onRequestPickerOpen`; tooltips differentiate the three existing-topic flavors per D-06; UUID never leaks into operator-visible text (D-08).
  </done>
</task>

<task type="auto">
  <name>Task 4: reverse-topics-link prop extension + visibleTopics filter + auto-open effect (D-02)</name>
  <files>
    src/ui/surfaces/reader/reverse-topics-link.tsx,
    test/ui/reverse-topics-link-entry-point.test.mjs
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§4 lines 372-504 — props extension, useEffect autoOpen, visibleTopics derivation, row-render unchanged; §Shared A lines 574-589 — URL_HASH carrier exclusively; §Shared F lines 632-648 — source-grep idiom),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-02, D-07),
    @src/ui/surfaces/reader/reverse-topics-link.tsx (read once; existing ReverseTopicsLinkProps lines 44-49, ReverseTopic type lines 36-42, state machine lines 62-126, row-render lines 97-122),
    @test/ui/reverse-topics-link.test.mjs (existing tests for context on the source-grep idiom used in this file)
  </read_first>
  <behavior>
    - Source-grep T1: file declares `entryPoint?: 'continue-in-chat' | 'manual'` in ReverseTopicsLinkProps (or equivalent union literal types).
    - Source-grep T2: file declares `filterToAssignee?: string | null` in ReverseTopicsLinkProps.
    - Source-grep T3: file declares `autoOpen?: boolean` in ReverseTopicsLinkProps.
    - Source-grep T4: file contains a `React.useEffect(()` block referencing `autoOpen` (verifies the controlled auto-open effect exists).
    - Source-grep T5: file contains a `visibleTopics` derivation that filters by `employeeAgentId === filterToAssignee` when `filterToAssignee` is a non-empty string.
    - Source-grep T6: file's row-render still calls `buildTopicDeepLink(companyPrefix, t.topicIssueId)` (D-07 deep-link contract unchanged).
    - Source-grep T7: file does NOT call `buildChatDeepLink` directly with `route: 'existing-topics-ambiguous'` (chat surface never sees the ambiguous route — D-07).
    - Source-grep T8: `ReverseTopic` type adds an optional `employeeAgentId?: string` field.
    - Source-grep T9: the `.map` over topics uses `visibleTopics` (not `topicsForIssue` directly) when the filter prop is present — assert via regex that `visibleTopics` appears in a `.map(` call site.
  </behavior>
  <action>
    Apply the diff in PATTERNS.md §4 lines 372-504 verbatim:
    1. Extend `ReverseTopicsLinkProps` with the three new optional props (`entryPoint`, `filterToAssignee`, `autoOpen`).
    2. Extend `ReverseTopic` with an optional `employeeAgentId?: string`.
    3. Inside the component, add `React.useEffect(() => { if (autoOpen) setOpen(true); }, [autoOpen]);` (parent owns the open-request; local state still owns close — PATTERNS.md lines 442-447).
    4. Add `const visibleTopics = typeof filterToAssignee === 'string' && filterToAssignee.length > 0 ? topicsForIssue.filter((t) => t.employeeAgentId === filterToAssignee) : topicsForIssue;` (PATTERNS.md lines 449-452).
    5. Replace the existing `topicsForIssue.map((t) => ...)` with `visibleTopics.map((t) => ...)` — keep the row-render body unchanged (`buildTopicDeepLink(companyPrefix, t.topicIssueId)` per D-07 lock).
    6. NEW test file `test/ui/reverse-topics-link-entry-point.test.mjs` mirrors the d9 source-grep idiom (comment-strip then regex), runs the 9 assertions in `<behavior>`.
    DO NOT modify `src/ui/surfaces/chat/deep-link.mjs` (no `parseChatDeepLink` change; D-07 lock).
  </action>
  <verify>
    <automated>node --test test/ui/reverse-topics-link-entry-point.test.mjs test/ui/reverse-topics-link.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/ui/reverse-topics-link-entry-point.test.mjs` exits 0 with all 9 source-grep assertions PASS.
    - `node --test test/ui/reverse-topics-link.test.mjs` STILL exits 0 (existing popover tests not regressed).
    - `grep -c "autoOpen" src/ui/surfaces/reader/reverse-topics-link.tsx` returns >= 3 (prop declaration + useEffect dep + useEffect body).
    - `grep -c "filterToAssignee" src/ui/surfaces/reader/reverse-topics-link.tsx` returns >= 2 (prop declaration + visibleTopics filter).
    - `grep -c "visibleTopics" src/ui/surfaces/reader/reverse-topics-link.tsx` returns >= 2 (derivation + .map site).
    - `grep -c "buildTopicDeepLink" src/ui/surfaces/reader/reverse-topics-link.tsx` returns >= 1 (D-07 lock — picker emits unchanged contract).
    - tsc --noEmit exit 0.
  </acceptance_criteria>
  <done>
    Popover lifts open-state from a parent prop (`autoOpen`), filters rows by assignee when `filterToAssignee` is set, and emits the unchanged `existing-topic` URL_HASH deep link on row click. Manual click path (entryPoint === 'manual') still works.
  </done>
</task>

<task type="auto">
  <name>Task 5: Reader index popover-state lift (parent-owned pickerRequest)</name>
  <files>
    src/ui/surfaces/reader/index.tsx
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§4 lines 479-501 — Reader-index integration block; the parent-owned state shape),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-02 popover lift),
    @src/ui/surfaces/reader/index.tsx (read once; existing ContinueInChatButton + ReverseTopicsLink mount sites — see PATTERNS.md citation lines 280-299; learn the existing data fetch / data flow)
  </read_first>
  <action>
    Add a parent-owned state slot at the top of the Reader index component:
      `const [pickerRequest, setPickerRequest] = React.useState<{ filterToAssignee: string | null } | null>(null);`
    Pass `onRequestPickerOpen={setPickerRequest}` to `<ContinueInChatButton ... />`.
    Pass `entryPoint={pickerRequest ? 'continue-in-chat' : 'manual'}`, `filterToAssignee={pickerRequest?.filterToAssignee ?? null}`, and `autoOpen={pickerRequest !== null}` to `<ReverseTopicsLink ... />`. Do NOT add a context or a custom hook — keep it parent-owned per PATTERNS.md "do not over-engineer." If `ReverseTopicsLink` receives `topicsForIssue` from a worker data hook, ensure each topic entry carries `employeeAgentId` (thread it through if missing — the worker already exposes it on `chat_topics`).
  </action>
  <verify>
    <automated>node --test test/ui/reverse-topics-link.test.mjs test/ui/reverse-topics-link-entry-point.test.mjs test/ui/continue-in-chat-button-ambiguous.test.mjs test/ui/continue-in-chat-button-d9.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pickerRequest" src/ui/surfaces/reader/index.tsx` returns >= 3 (state declaration + setter pass-through + reads on the two consumers).
    - `grep -c "onRequestPickerOpen" src/ui/surfaces/reader/index.tsx` returns >= 1 (button wiring).
    - `grep -c "autoOpen" src/ui/surfaces/reader/index.tsx` returns >= 1 (popover wiring).
    - `grep -c "filterToAssignee" src/ui/surfaces/reader/index.tsx` returns >= 1 (popover wiring).
    - tsc --noEmit exit 0.
    - All four affected UI test files still pass (no regression on existing assertions).
  </acceptance_criteria>
  <done>
    Reader index owns one piece of new state (`pickerRequest`) that plumbs the Continue button's click intent into the ReverseTopicsLink's auto-open + filter behavior. No React context introduced.
  </done>
</task>

<task type="auto">
  <name>Task 6: D-09 runbook fold-in — §ac-autostatus-drill-proof</name>
  <files>
    runbook/operator-gotchas.md
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§5 lines 507-541 — analog section shape from `§paperclip-restoring-db-precreate`),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-09),
    @runbook/operator-gotchas.md (read once; learn the file-wide convention — `§<slug>` anchors, Symptom / Discovered / Why it happens / Resolution sections; append-only)
  </read_first>
  <action>
    Append a new section `## §ac-autostatus-drill-proof` to `runbook/operator-gotchas.md`. Follow the verbatim structure of `§paperclip-restoring-db-precreate` (PATTERNS.md §5 lines 516-533):
    - **Symptom:** the next drill's Step 9 (AC-toggle → Reader-refetch contract) finishes without recoverable evidence that the contract fired — the test issue's cached TL;DR was already current, AC sources were decoupled from the auto-status derivation, or AC markers were frozen.
    - **Discovered:** 2026-05-25 rc.6 drill `260524-sm8` (Plan 04.2-07 / Phase 4.2 addendum D-5).
    - **Why it happens:** the visible UI signals the drill expects to see change (TL;DR refresh + AC checklist state shift) can ALL be decoupled from the underlying contract for a given test issue (cache-warm TL;DR, AC markers whose toggle does not affect derivation, etc.).
    - **Resolution:** Step 9 MUST capture either (a) a DevTools Network HAR or screenshot showing the rc.X contract firing OR (b) a test issue with a cached TL;DR + ≥2 AC sources whose manual toggle materially changes the auto-status derivation.
    Add a `---` separator before this new section (matching the file-wide pattern). Do NOT delete or edit any existing section.
  </action>
  <verify>
    <automated>grep -c '## §ac-autostatus-drill-proof' runbook/operator-gotchas.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## §ac-autostatus-drill-proof' runbook/operator-gotchas.md` returns exactly `1`.
    - `grep -c 'Step 9 MUST capture' runbook/operator-gotchas.md` returns exactly `1`.
    - `grep -c 'rc.6 drill' runbook/operator-gotchas.md | head -1` returns >= 1 (Discovered line cites the drill).
    - `grep -cE '^## §' runbook/operator-gotchas.md` returns a value at least 1 greater than the same grep on the pre-task baseline (operator confirms via `git diff --stat` that lines added > 0 and lines removed == 0 — append-only).
    - File ends with a final newline (`tail -c 1 runbook/operator-gotchas.md` is a newline byte).
  </acceptance_criteria>
  <done>
    Runbook has a new D-09 entry codifying the next-drill proof rule. Pre-existing sections are unchanged; append-only convention honored.
  </done>
</task>

<task type="auto">
  <name>Task 7: D-10 README fold-in — Acceptance criteria markers section</name>
  <files>
    README.md
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-PATTERNS.md (§6 lines 545-568 — tone analog + placement near AC-autostatus docs),
    @.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md (D-10),
    @README.md (read once; learn the existing H2 section structure to pick a placement consistent with the "Opt in" / "Install" / "What it adds" voice)
  </read_first>
  <action>
    Add a new H2 section `## Acceptance criteria markers` to `README.md` (place it near the Reader-view / AC-autostatus context — pick the spot that reads best given the existing section order; PATTERNS.md §6 suggests after "Opt in" or near where "acceptance criteria checklist" is first mentioned). Content (concise, operator-facing, plain prose):
    - State the canonical grammar: `AC: <id>: <state>` with `<state>` ∈ `{✓, done, complete, x}` (use the canonical glyph `✓` in examples). Example: `AC: 1: ✓`.
    - State the accepted bracket-alternate: `AC[<id>]: <state>` is tolerated by the Plan 05-03 scanner (`src/worker/handlers/reader-ac-autostatus.ts`) for robustness against historical audit comments, but is NOT canonical and should be avoided in new agent-authored audit comments.
    - Briefly explain the why (consistent grammar makes the Reader auto-status derivation predictable and human-readable; mixed grammar within one comment body is the audit-comment pathology that surfaced D-6 in the rc.6 drill).
    No code change to `src/worker/handlers/reader-ac-autostatus.ts` (scanner is unchanged). Do NOT edit other README sections.
  </action>
  <verify>
    <automated>grep -c '## Acceptance criteria markers' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Acceptance criteria markers' README.md` returns exactly `1`.
    - `grep -c 'AC: <id>:' README.md` returns >= 1 (canonical grammar literally documented).
    - `grep -c 'AC\[<id>\]:' README.md` returns >= 1 (bracket-alternate literally documented).
    - `git diff --stat README.md` shows lines added > 0 and lines removed == 0 (append-style addition; existing sections unchanged).
    - File ends with a final newline.
  </acceptance_criteria>
  <done>
    README has an operator-facing AC marker grammar section. Plan 05-03 scanner is unchanged.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Version bump + npm pack + Countermoves snapshot-bookended drill (CLOSURE)</name>
  <files>
    package.json,
    src/manifest.ts
  </files>
  <read_first>
    @.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md (5-path drill fixtures — DO NOT modify the existing 5; ADD a 6th),
    @CLAUDE.md (bookended-by-snapshots rule),
    @runbook/operator-gotchas.md (full file — re-read AFTER Task 6 lands so the new §ac-autostatus-drill-proof entry is visible during drill walkthrough),
    @package.json,
    @src/manifest.ts
  </read_first>
  <what-built>
    Automated portion of this task (done before the human-verify checkpoint fires):
    - Bump `version` in `package.json` from `1.0.0-rc.6` to `1.0.0-rc.7`.
    - Bump the hardcoded version literal in `src/manifest.ts` from `1.0.0-rc.6` to `1.0.0-rc.7` (per `plugin-version-bump-two-sources` rule — host reads `dist/manifest.js` built from `src/manifest.ts`, not `package.json`).
    - Update any test that pins the version literal (e.g. `test/manifest/chat-capabilities.test.mjs` if it asserts the version string).
    - Run `npm test` and confirm exit 0; run `npx tsc --noEmit` and confirm exit 0; run `node scripts/check-css-scope.mjs` and confirm exit 0; run `node scripts/check-a11y.mjs` and confirm exit 0.
    - Run `npm run build` to produce `dist/manifest.js` carrying `version: '1.0.0-rc.7'`.
    - Run `npm pack`, producing `clarity-pack-1.0.0-rc.7.tgz` in the repo root. Capture bytes (`wc -c clarity-pack-1.0.0-rc.7.tgz`) and sha256 (`sha256sum clarity-pack-1.0.0-rc.7.tgz` or PowerShell `Get-FileHash` equivalent on Windows). Record both values in the eventual SUMMARY.md.
    - Commit all changes with a single atomic commit `chore(04.2-07): version 1.0.0-rc.6 -> 1.0.0-rc.7 -- D-7 routing rewrite + D-5/D-6 fold-in`.

    Drill walkthrough (operator runs on Countermoves):

    **Pre-flight** (canonical env re-export per `vps-commands-self-contained`; first VPS message in this session MUST re-export):
    ```
    export DB_URL=$(sudo grep -m1 DATABASE_URL /etc/paperclip/db.env | cut -d= -f2-)
    export PAPERCLIP_API_URL=http://localhost:3100
    export PAPERCLIP_API_KEY=<from ~/.paperclip/auth.json>
    export COMPANY_ID=<COU>
    ```

    **Bookend snapshot** (CLAUDE.md rule):
    ```
    cd ~/clarity-pack
    node scripts/safety/cli.mjs snapshot --db-url "$DB_URL"
    ```
    Confirm the new snapshot ID is captured; bytes and sha256 of the snapshot tarball recorded.

    **Rollback rehearsal** (verify the path BEFORE installing the new version):
    ```
    sudo -u postgres psql -c 'CREATE DATABASE paperclip_restoring OWNER paperclip;' 2>/dev/null || true
    node scripts/safety/cli.mjs verify --db-url "$DB_URL" <new-snapshot-id>
    ```
    Verify exit 0 (rollback path rehearsed; staging DB pre-created per `§paperclip-restoring-db-precreate`).

    **Capture pre-uninstall row counts** (COEXIST #6 baseline):
    ```
    psql "$DB_URL" -c "SELECT 'chat_topics' AS t, count(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topics UNION ALL SELECT 'chat_messages', count(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_messages UNION ALL SELECT 'chat_topic_tasks', count(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topic_tasks ORDER BY t;"
    ```
    Save the three counts.

    **Uninstall → install** (the documented upgrade pattern — `pnpm paperclipai plugin install` rejects re-install with 400):
    ```
    cd ~/paperclip && pnpm paperclipai plugin uninstall clarity-pack
    cd ~/clarity-pack && scripts/install-helper.sh /home/eric/clarity-pack-1.0.0-rc.7.tgz
    cd ~/paperclip && pnpm paperclipai plugin list
    ```
    Confirm `clarity-pack status=ready version=1.0.0-rc.7 id=0d4fc40a-0541-4b67-8979-9d346cb9c07b` (plugin UUID preserved).

    **Capture post-install row counts** — assert byte-identical to pre-uninstall (COEXIST #6).

    **Drill paths 1-5** — re-run the 5 existing Phase 4.2 closure fixtures verbatim from VERIFICATION.md (COU-2215 assigned-with-source-topic, COU-2361 chat-spawned, COU-2396 cold, COU-1115/CHT-1117 topic-itself, COU-2399 unassigned). All five MUST still PASS. Capture screenshots of any visible regression.

    **NEW drill path 6 — same-assignee silent resume + auto-unarchive (D-04):**
    Pick a cold COU-#### issue X that has exactly ONE archived chat topic in `chat_topics` where `origin_issue_id = X` AND `employee_agent_id = X.assigneeAgentId`. If no such fixture exists, pre-seed one: create a chat topic from the Reader of issue X, then archive it via the chat-topic archive button (Plan 04.1-05); confirm `SELECT archived_at FROM chat_topics WHERE origin_issue_id = '<X>' AND employee_agent_id = '<assignee>';` returns a non-NULL timestamp.
    Now from the Reader of X, click **Continue in chat with <employeeName> →**. Expected outcomes:
    - Chat surface lands on the resumed topic (existing-topic URL_HASH deep link consumed). NOT the seeded New Topic dialog.
    - The button tooltip read `Resume conversation about <X-identifier> →` (D-06 reverse-lookup tooltip flavor).
    - Database probe AFTER the click: `SELECT archived_at FROM chat_topics WHERE origin_issue_id = '<X>' AND employee_agent_id = '<assignee>';` returns NULL (D-04 silent auto-unarchive flipped it; CTT-07: `SELECT updated_at FROM public.issues WHERE id = '<X>';` is UNCHANGED — host issue untouched).

    **NEW drill scenario 7 — ambiguous picker (D-02):**
    Pick (or pre-seed) an issue Y with TWO active same-assignee chat topics (`origin_issue_id = Y`, same `employee_agent_id`). From Y's Reader, click Continue. Expected:
    - The button tooltip reads `Pick from 2 conversations about <Y-identifier> →`.
    - The RCB-06 reverse-topics popover auto-opens, pre-filtered to same-assignee candidates only (other-employee topics about Y, if any, are not shown).
    - Operator clicks one of the two candidates. The chat surface navigates to that topic (canonical `existing-topic` URL_HASH; D-07 lock).

    **Console probe** during all six paths: open DevTools Console, filter for warnings and errors. Expected: NO React key warnings introduced by this plan; NO TypeScript errors in console; NO uncaught exceptions; the existing D4-style polish warnings from Plan 04.2-05 backlog are tolerated (not part of this plan's scope).

    **Final rollback rehearsal** (the CLAUDE.md rule's "verified rollback path"):
    ```
    cd ~/paperclip && pnpm paperclipai plugin uninstall clarity-pack
    cd ~/clarity-pack && scripts/install-helper.sh /home/eric/clarity-pack-1.0.0-rc.6.tgz
    cd ~/paperclip && pnpm paperclipai plugin list
    ```
    Confirm `version=1.0.0-rc.6 id=0d4fc40a-...`. From the Reader of a previously ambiguous issue Y, click Continue: expected behaviour reverts to rc.6's 5-case behaviour (opens NEW topic — the D-7 bug). Confirm chat data is intact (`SELECT count(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topics;` matches pre-uninstall count — COEXIST #6).
    Then reinstall rc.7 and re-confirm `status=ready`.
  </what-built>
  <how-to-verify>
    The operator (Eric) runs the walkthrough above on Countermoves. The checkpoint resumes when Eric reports back PASS or describes failures. The single composite PASS verdict requires ALL of:

    1. **Tarball produced:** `clarity-pack-1.0.0-rc.7.tgz` exists in repo root; bytes and sha256 captured and recorded in SUMMARY.md.
    2. **Bookend snapshot taken** before install; snapshot ID + bytes recorded.
    3. **Rollback verified** before install (verify exit 0 against the new snapshot).
    4. **Plugin UUID preserved** (`id=0d4fc40a-0541-4b67-8979-9d346cb9c07b`) across uninstall/install.
    5. **Row counts byte-identical** pre-uninstall vs post-install for `chat_topics`, `chat_messages`, `chat_topic_tasks` (COEXIST #6).
    6. **5 existing drill paths PASS** (paths 1-5 from VERIFICATION.md).
    7. **NEW path 6 PASS** (single-archived-match silent resume + auto-unarchive; `chat_topics.archived_at` flipped to NULL; `public.issues.updated_at` for the source issue unchanged — CTT-07).
    8. **NEW scenario 7 PASS** (popover auto-opens pre-filtered; row click navigates via existing-topic URL_HASH deep link).
    9. **Console clean** for any new React key warnings, TypeScript errors, or uncaught exceptions introduced by this plan (existing Plan 04.2-05 backlog warnings tolerated).
    10. **Final rollback rehearsal PASS** (uninstall rc.7, reinstall rc.6, observe rc.6's bug behaviour, then reinstall rc.7 — chat data intact, plugin UUID stable across all transitions).
    11. **RCB-01..RCB-07 not regressed** (`04.2-VERIFICATION.md` baseline behaviour preserved; the 5-path drill above is the live evidence).
  </how-to-verify>
  <resume-signal>
    Type **approved** + paste the captured tarball bytes/sha256 + snapshot ID + the three row-count triples to resume with SUMMARY.md write. OR describe failures (path number + symptom + console output + database probe results) so we can file gap-closure work as Plan 04.2-08 or revise.
  </resume-signal>
  <acceptance_criteria>
    - `grep -c '"version": "1.0.0-rc.7"' package.json` returns `1`.
    - `grep -c "version: '1.0.0-rc.7'" src/manifest.ts` returns `1` (or `"version": "1.0.0-rc.7"` depending on quote style — confirm with one of both grep variants).
    - `dist/manifest.js` (after `npm run build`) contains `version: '1.0.0-rc.7'` (assert via `grep -c "1.0.0-rc.7" dist/manifest.js` returns >= 1).
    - `npm test` exit 0; suite size is the prior baseline + new tests from Tasks 1-4 (operator records the new total in SUMMARY).
    - `npx tsc --noEmit` exit 0; `node scripts/check-css-scope.mjs` exit 0; `node scripts/check-a11y.mjs` exit 0.
    - `clarity-pack-1.0.0-rc.7.tgz` exists in repo root; `wc -c` and `sha256sum` outputs recorded in SUMMARY.
    - Snapshot bookend record present in SUMMARY: snapshot ID + bytes + verify exit 0 line.
    - All 6 drill paths (5 existing + new path 6) reported PASS by the operator; ambiguous scenario 7 reported PASS; final rollback rehearsal reported PASS.
    - COEXIST #6 row-count triples reported byte-identical pre-uninstall vs post-install AND across the rollback rehearsal.
    - SUMMARY.md links back to this plan and to MemPalace drawers for D-5/D-6/D-7 (the same drawer paths cited in `<addresses>` at the top of this plan).
  </acceptance_criteria>
  <done>
    clarity-pack 1.0.0-rc.7 is installed and healthy on Countermoves. The D-7 routing rewrite is operator-confirmed on three resolution shapes (silent resume, auto-unarchive, ambiguous picker). The D-5/D-6 documentation fold-ins ship with this version. RCB-01..RCB-07 still Implemented. Rollback path rehearsed and confirmed for the rc.7→rc.6 boundary.
  </done>
</task>

</tasks>

<verification>
End-of-plan verification — every must_have above is testable. The composite drill in Task 8 is the operator-confirmed evidence chain. Specifically:

- **Worker contract** (Tasks 1-2): `node --test test/worker/db/chat-topics-repo-list-by-assignee.test.mjs test/worker/handlers/chat-open-for-issue-d7.test.mjs test/worker/chat/chat-open-for-issue.test.mjs test/worker/chat/chat-topic-archive.test.mjs` all exit 0. The closure-baseline test pins (`chat-open-for-issue.test.mjs` Tests 1-4) MUST NOT regress.
- **UI contract** (Tasks 3-5): `node --test test/ui/continue-in-chat-button-ambiguous.test.mjs test/ui/continue-in-chat-button-d9.test.mjs test/ui/reverse-topics-link-entry-point.test.mjs test/ui/reverse-topics-link.test.mjs` all exit 0.
- **Documentation** (Tasks 6-7): grep -c assertions in each task's acceptance criteria.
- **Coexistence** (Task 8): plugin uninstall/install row counts byte-identical pre/post; plugin UUID preserved; rollback rehearsed end-to-end.
- **No regression to closed baseline** (Task 8 drill paths 1-5 + RCB-07 spot-check): the 5 Phase 4.2 closure fixtures from VERIFICATION.md must all still PASS at rc.7.
- **D-04 invariant** (Task 2 + Task 8 path 6): `ctx.issues.update` zero calls in handler; `public.issues.updated_at` unchanged for the source issue during the live drill; `chat_topics.archived_at` flipped to NULL — proves the plugin-side-only archive invariant (CTT-07).
- **D-07 deep-link contract unchanged** (Task 4): `buildTopicDeepLink` is the only navigation helper invoked from the popover row; `parseChatDeepLink` is NOT extended.

Quality gates that must all be exit 0 before the operator drill starts:
- tsc --noEmit
- node --test "test/**/*.test.mjs"
- node scripts/check-css-scope.mjs
- node scripts/check-a11y.mjs
- npm run build (worker + ui + manifest)
- npm pack
</verification>

<success_criteria>
Plan 04.2-07 is complete when:

1. clarity-pack 1.0.0-rc.7 is live on Countermoves; plugin UUID `0d4fc40a-0541-4b67-8979-9d346cb9c07b` preserved across the uninstall/install.
2. All 6 drill paths PASS (5 existing fixtures from VERIFICATION.md + NEW 6th fixture: cold-issue-with-existing-same-assignee-archived-topic).
3. NEW scenario 7 PASS: issue with N=2 same-assignee topics → button click auto-opens the popover pre-filtered to same-assignee candidates; operator pick navigates via canonical `existing-topic` URL_HASH deep link.
4. D-04 invariant proven live: `chat_topics.archived_at` flipped to NULL on the silent-resume path; source issue's `public.issues.updated_at` unchanged in the same transaction window — `ctx.issues.update` never called by the handler.
5. D-08 hygiene proven live: button tooltips and the popover render NO UUID-shaped strings for any path tested.
6. D-7 routing contract pinned by Tasks 1-3 tests; D-05 sort order pinned by Task 1 SQL-shape assertion; D-06 tooltip-differentiation pinned by Task 3 source-grep.
7. Runbook `§ac-autostatus-drill-proof` section present (D-09); README `## Acceptance criteria markers` section present (D-10).
8. Rollback rehearsed end-to-end: rc.7 → rc.6 → rc.7 with chat data intact and `status=ready` after each transition (COEXIST #6 row counts byte-identical at every step).
9. RCB-01..RCB-07 still Implemented; `04.2-VERIFICATION.md` baseline preserved.
10. SUMMARY.md `04.2-07-SUMMARY.md` written with: defect disposition table (D-5/D-6/D-7), atomic commit trail, suite delta, quality gates, tarball bytes + sha256, snapshot ID, drill PASS evidence for all six paths + scenario 7, final rollback rehearsal record.
</success_criteria>

<output>
Create `.planning/phases/04.2-reader-chat-bridge/04.2-07-SUMMARY.md` when done (drill PASS reported).
</output>
