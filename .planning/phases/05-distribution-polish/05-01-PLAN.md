---
phase: 05-distribution-polish
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [DIST-01, DIST-02]
version_target: "1.0.0-rc.1"
files_modified:
  - README.md
  - package.json
  - test/manifest/publish-readiness.test.mjs
tags: [npm-publish, readme, distribution]
---

<objective>
**Phase 5 Plan 01 — DIST-01 + DIST-02 closure: npm publish prep + README.**

`paperclipPlugin` field is already set in `package.json` (points at `dist/manifest.js`, `dist/worker.js`, `dist/ui/`). `files` is restricted to `dist/`, `migrations/`, `README.md` — publish blast radius scoped. What's missing for a clean `npm publish`:

1. `README.md` — declared in `files` but does not exist (`cat README.md` → `No such file`). Without it, the published tarball would be missing one of its three top-level files; npm would warn or fail.
2. A `prepublishOnly` build/verify guard so `npm publish` cannot ship a stale `dist/`.
3. A `test/manifest/publish-readiness.test.mjs` smoke check pinning the publish contract: `paperclipPlugin` keys present, `files` restricts publish, `engines.node >= 20`, peer deps shape stable.

DIST-02 says the README documents: install + opt-in toggle + rollback flow + runbook reference. The runbook reference is `scripts/safety/cli.mjs` (bookended-snapshot rule) and the install-helper at `scripts/install-helper.sh`.

Version target: `1.0.0-rc.1` is the natural milestone — the v0.x line shipped through 0.9.3 (Phase 4.2 code-complete-pending-drill). Phase 5 polish work justifies the 1.0 line. This plan bumps to `1.0.0-rc.1` so the README can document the install command using the rc tag.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: publish-readiness smoke test (RED)</name>
  <action>
Write `test/manifest/publish-readiness.test.mjs` that pins:
- `paperclipPlugin.{manifest, worker, ui}` all present and point at relative paths under `dist/`.
- `files` includes `dist/`, `migrations/`, `README.md`.
- `engines.node >= 20`.
- `peerDependencies` includes `@paperclipai/plugin-sdk`, `react`, `react-dom`.
- `scripts.prepublishOnly` is set (the build/verify guard).
- `README.md` exists at the repo root.
- `description` is non-empty.

Run: RED on `prepublishOnly` + README assertions. Commit: `test(05-01): RED -- publish-readiness contract (DIST-01)`.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write README.md</name>
  <action>
Repo-root `README.md` covering:
- 1-paragraph project description.
- Install: `pnpm paperclipai plugin install clarity-pack@1.0.0-rc.1`.
- Opt-in toggle: profile-settings path (per OPTIN-01).
- Rollback flow: `node scripts/safety/cli.mjs snapshot` + `verify` + `restore` (per CLAUDE.md bookended-snapshots rule).
- Runbook reference: `scripts/safety/` for the safety CLI, `scripts/install-helper.sh` for the `pnpm paperclipai plugin install` unpack helper (per MemPalace `paperclipai-workspace-script-gotcha`).
- Uninstall + `--purge`: data preservation (COEXIST-05); `--purge` is opt-in only — documented for Plan 05-02 to elaborate.
- License + author.

Commit: `docs(05-01): README -- install + opt-in + rollback + runbook (DIST-02)`.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 3: prepublishOnly guard + version 1.0.0-rc.1</name>
  <action>
1. `package.json` `scripts.prepublishOnly`: `pnpm build && pnpm typecheck && pnpm test && node scripts/check-css-scope.mjs`. This runs on `npm publish` and blocks stale-dist publishes.
2. Version `0.9.3` → `1.0.0-rc.1` in `package.json` + `src/manifest.ts`.
3. Update `test/manifest/chat-capabilities.test.mjs` version pin to `1.0.0-rc.1`.
4. `node scripts/build-worker.mjs && node scripts/build-ui.mjs && npx tsc --project tsconfig.manifest.json`. Confirm `dist/manifest.js` reports `version: '1.0.0-rc.1'`.
5. `npm pack --dry-run` — confirm the tarball contents are exactly `dist/**`, `migrations/**`, `README.md`, `package.json`.
6. Full suite GREEN; publish-readiness test now GREEN. `tsc --noEmit` clean.
7. Commit: `chore(05-01): prepublishOnly guard + version 1.0.0-rc.1 (DIST-01)`.
  </action>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Pack tarball + capture sha256</name>
  <action>
`npm pack`. Record byte size + sha256. Verify the tarball expands to the expected directory structure. Commit (if dist/ is rebuilt): rolls into Task 3's chore commit.
  </action>
</task>

</tasks>
