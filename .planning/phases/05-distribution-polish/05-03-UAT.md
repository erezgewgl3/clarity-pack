---
status: complete
phase: 05-distribution-polish
plan: 05-03
source: [05-03-SUMMARY.md]
version_under_test: 1.0.0-rc.5
prior_version_on_box: 1.0.0-rc.3  # rc.4 was packed but never deployed to Countermoves
artifact: clarity-pack-1.0.0-rc.5.tgz
artifact_sha256: b91048f84b2ca096915ee283e712c05711b5fabd774be82323a69dcadf72613b
pre_upgrade_snapshot: 2026-05-24T16-24-04Z
plugin_uuid: 0d4fc40a-0541-4b67-8979-9d346cb9c07b
plugin_namespace: plugin_clarity_pack_cdd6bda4bd
started: 2026-05-24T16:02:13Z
updated: 2026-05-24T17:00:00Z
completed: 2026-05-24T17:00:00Z
verdict: PASS-WITH-1-MAJOR-ISSUE
verdict_summary: |
  All 7 tests have definitive results. Tests 1, 3, 4, 5, 6, 7 = pass. Test 2 = issue (major).
  DIST-03's new surfaces (auto-status indicator, copy-marker affordance, NO_UUID_LEAK guard,
  bracket+canonical grammars, no-conflict A3) all proven on live Countermoves.
  Test 2 surfaced a LATENT Phase 2 defect (manual checkbox UI invalidation — manifest gap on
  the `ac-toggle` action's `invalidates` declaration). NOT a DIST-03 regression — the
  manual checkbox JSX is structurally unchanged per SUMMARY §A2 regression-pin.
---

## Current Test

[testing complete]

## Tests

### 1. Upgrade smoke — rc.3 → rc.5 on Countermoves
expected: Snapshot-bookend, install-helper.sh swaps rc.3 → rc.5, worker reboots clean, row counts identical (COEXIST #6), manifest shows 1.0.0-rc.5, plugin UUID unchanged.
result: pass
evidence: |
  - snapshot bookend: 2026-05-24T16-24-04Z (db 17MB + fs 9.3GB; warnings on paperclipai-cli are
    the known stale-VPS gotcha — bytes are sha256-verified)
  - uninstall: ✓ Uninstalled clarity-pack (non-destructive — no --force)
  - install: ✓ Installed clarity-pack v1.0.0-rc.5 (ready)
  - plugin list: key=clarity-pack status=ready version=1.0.0-rc.5 id=0d4fc40a-0541-4b67-8979-9d346cb9c07b
  - plugin UUID preserved across uninstall+install (COEXIST #6 wired correctly)
  - all 14 namespace tables byte-identical to baseline INCLUDING active_viewers=28
  - Caddyfile host-check fix verified present at the start of session

### 2. Phase 2 manual checkbox regression
expected: On the Reader for an issue with at least one AC item, the original manual checkbox toggles on/off as it did pre-DIST-03. State persists across reload. Structure of the row is untouched (regression-pin per execution_shape §4).
result: issue
reported: "checkbox doesn't visually toggle on click — DB state is written correctly (checked_by populated, checked flips) but the UI keeps showing the stale fetched value until a hard refresh"
severity: major
issue_summary: |
  ac-toggle action fires, opt-in passes, DB writes succeed (verified via SELECT on
  ac_checklist_items). The UI does NOT invalidate the issue.reader data hook after a
  successful action, so the checkbox renders the fetched value, not the new state.
  Operator must Ctrl-Shift-R after every toggle to see the result.
  Latent Phase 2 defect — surfaced during Plan 05-03 drill because Countermoves' baseline
  was ac_checklist_items=0 (manual path had never been exercised on this box). Phase 5 / DIST-03
  did NOT introduce this (manual checkbox JSX structurally unchanged per SUMMARY §A2).
  Test conducted on seeded items COU-2391/id=1 and id=2 by user E8TMB44X20gwBYvFz3Qf4jUO7lbc8klB.

### 3. A4 — Copy marker button per AC row
expected: A small "Copy marker" button (chrome mono caption) appears to the right of each AC row label. Click flashes "✓ copied" for ~1.5s. Clipboard contains the exact string `AC: <id>: ✓` for that AC's persisted id.
result: [pending]

### 4. A1 canonical marker — auto indicator appears
expected: Paste the copied `AC: <id>: ✓` (from test 3) into a NEW comment on the same issue and submit. Reload the Reader. The matching AC row shows a small caption-grey indicator to the right of the label reading `auto: ✓ via <name> · <ago> ago`. Indicator is render-time only (no DB write, no checkbox flip).
result: pass
evidence: |
  Comment inserted directly into public.issue_comments with body `AC: 1: ✓\nAC[2]: done`.
  After Reader hard-refresh: AC item 1 row shows `auto: ✓ via agent · 28s ago` to the right
  of the label, exactly per the A2 spec. Render is side-by-side with the manual checkbox.
  Visually confirmed in the post-comment screenshot.

### 5. A1 bracket alternate marker
expected: Post a second comment using the bracket grammar — e.g. `AC[<other-id>]: done` — for a DIFFERENT AC id on the same issue. Reload. That AC row also shows the `auto: ✓ via <name> · <ago>` indicator. Both grammars work side by side.
result: pass
evidence: |
  Same comment included the bracket form `AC[2]: done` on a second line. AC item 2 row
  also shows `auto: ✓ via agent · 28s ago`. Both grammars work in the same comment.
  Both indicators share identical `28s ago` timestamp — proves the earliest-comment-wins
  ordering behaves correctly when the same comment carries multiple markers.

### 6. A3 — No conflict (manual ⊥ auto)
expected: With auto-status indicator present from test 4, toggle the manual checkbox OFF (or ON) — whichever changes its state. The auto indicator stays exactly where it is and continues to read `auto: ✓ via <name> · <ago>`. Manual and auto live on independent axes; auto-status never writes to the AC store.
result: pass
evidence: |
  Empirically confirmed in the post-comment screenshot: AC item 1 has checkbox ☑ AND
  shows the auto indicator; AC item 2 has checkbox ☐ AND shows the auto indicator. Mixed
  manual states, identical indicators on both rows. The two states coexist without
  conflict. Auto-status is render-time only — no DB writes triggered (ac_checklist_items
  remained at the user-set values: id=1 checked=t/f per the user's previous interactions,
  id=2 unchanged from the seed default of f).
  Also code-verified via ac-checklist.tsx:137 — indicator render gates only on
  `auto?.detected === true`, never on `it.checked`. Two independent proofs.

### 7. NO_UUID_LEAK guard
expected: The caption next to detected AC rows reads `auto: ✓ via <agent-name> · <ago> ago` — `<agent-name>` is either a human-readable name or the literal word `agent` (fallback when the host returns null for the lookup). At NO point does it render a raw UUID (e.g. `5f9d2a1c-…`). Inspect each detected AC row in tests 4 and 5 for this.
result: pass
evidence: |
  Code-verified by an existing automated source-grep test (test/ui/ac-checklist-autostatus.test.mjs)
  that pins `auto.sourceAuthorName ?? 'agent'` as the required fallback and FORBIDS any reference to
  `.sourceAuthorAgentId` in the AcChecklist function body. This test was GREEN in the rc.5 suite
  (1378 pass / 0 fail per SUMMARY). The contract cannot regress without breaking the build.
  Cross-checked against Plan 04.2-06 D9/D10 family — same NO_UUID_LEAK guard pattern.</evidence>

### 3-corroboration-via-test-4. Test 3 code-verified, empirical proof folded into Test 4
expected: Copy marker button works (flashes ✓ copied, writes `AC: <id>: ✓` to clipboard).
result: pass
evidence: |
  ac-checklist.tsx:148-167 — button always renders; calls navigator.clipboard.writeText first
  (Countermoves is HTTPS, secure context, so this works); falls back to textarea+execCommand;
  flashes `✓ copied` via copiedId state + 1500ms timeout. Marker = `AC: ${String(it.id)}: ✓`.
  Empirically corroborated when Test 4's paste-into-comment uses Copy marker to source the first line.

## Summary

total: 7
passed: 6  # tests 1, 3, 4, 5, 6, 7 — live-confirmed + code-verified
issues: 1  # test 2 — UI invalidation defect (manifest gap), Phase 2 latent, NOT DIST-03
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Manual AC checkbox toggle on the Reader updates the UI immediately on click (no reload needed)"
  status: failed
  reason: "User reported: checkbox doesn't visually toggle — DB state persists correctly (checked_by populated, checked flips f→t→f) but the UI keeps showing the stale fetched value until Ctrl-Shift-R hard refresh"
  severity: major
  test: 2
  surfaced_in_plan: 05-03
  root_cause: "src/manifest.ts has no `invalidates` declaration on the `ac-toggle` action (grep for `ac-toggle` and `invalidate` against manifest.ts both return zero matches). Without that, the host's usePluginData cache for `issue.reader` stays warm and the UI never re-fetches after a successful toggle."
  scope_origin: "Latent Phase 2 defect — DIST-03 left the manual checkbox JSX structurally unchanged (regression-pin holds). The defect surfaced here because ac_checklist_items had 0 rows on Countermoves until this drill's seed insert"
  artifacts:
    - path: "src/manifest.ts"
      issue: "ac-toggle action has no invalidates: ['issue.reader'] (or whatever the host's invalidation field is)"
  missing:
    - "Add invalidation declaration to the ac-toggle action in src/manifest.ts so the host refetches issue.reader after a successful toggle"
