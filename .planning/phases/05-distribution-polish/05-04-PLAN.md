---
phase: 05-distribution-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/handlers/deliverable-preview.ts
  - src/worker.ts
  - src/ui/surfaces/reader/deliverable-preview.tsx
  - src/ui/surfaces/reader/index.tsx
  - test/ui/reader-view.test.mjs
  - test/worker/deliverable-preview.test.mjs
  - test/ui/deliverable-preview.test.mjs
  - test/visual/sketch-regression.test.mjs
  - test/visual/baselines/01-task-detail-reader.png
  - test/visual/baselines/02-situation-room.png
  - test/visual/baselines/03-bulletin.png
  - test/visual/baselines/04-employee-chat.png
  - .github/workflows/visual-regression.yml
  - package.json
autonomous: true
requirements: [DIST-04, DIST-05]
tags: [previewer, xlsx, pdf, markdown, visual-regression, playwright]

must_haves:
  truths:
    - "An opted-in user opens any issue with an xlsx deliverable in Reader view and sees a parsed grid (sheets × rows of strings) rendered inline, NOT a 'Phase 5 (DIST-04)' placeholder"
    - "An opted-in user with a pdf deliverable sees the native browser pdf viewer inline via <embed type='application/pdf'> (no JS dep)"
    - "An opted-in user with a markdown deliverable sees rendered markdown via react-markdown (no dangerouslySetInnerHTML)"
    - "An opted-in user with a png deliverable sees an <img> with alt={filename} and loading='lazy'"
    - "Any deliverable preview that errors out (oversize / parse failure / unknown kind) renders a graceful fallback ('Preview unavailable — open in classic Paperclip') instead of crashing the Reader view"
    - "An xlsx file larger than 5 MB is rejected by the worker BEFORE SheetJS parses it (single chokepoint)"
    - "UI bundle size stays ≤ 350 kB after react-markdown is added (baseline 288 kB + ~50 kB react-markdown delta + ~10 kB margin = 350 kB ceiling). This ceiling is the gate value in scripts/check-ui-bundle-size.mjs."
    - "SheetJS (xlsx package) appears in the WORKER bundle output but NOT in the UI bundle output"
    - "Visual-regression test loops over the 4 frozen sketches/*.html files and diffs against test/visual/baselines/*.png using Playwright"
    - "Every PR to main/master triggers .github/workflows/visual-regression.yml; a failed diff uploads the diff PNG as a CI artifact"
    - "Visual-regression diff threshold is VISUAL_DIFF_THRESHOLD = 0.02 (2% of pixels may differ). Trade-off documented: v1 audience of 1 + D-25 sketch lockdown makes a 2% threshold acceptable; tightening to 0.001 would force baselines to be regenerated on Linux CI to avoid cross-OS antialiasing drift. v1 ships looser; v1.1+ can tighten once a Linux baseline-regen workflow exists."
    - "reader-view.test.mjs's literal 'Phase 5' assertion is updated in the SAME commit that replaces the placeholder (D-24)"
    - "react-markdown is a runtime dependency (package.json `dependencies`), NOT a devDep, NOT a peerDep"
    - "xlsx (SheetJS) is a runtime dependency (worker-side)"
    - "@playwright/test is a devDep only (test-time tooling)"
    - "esbuild presets unchanged; UI externalizes react/react-dom/react/jsx-runtime/@paperclipai/plugin-sdk/ui/@paperclipai/plugin-sdk/ui/hooks per CLAUDE.md stack pins"
    - "Worker handler `deliverable.preview` is opt-in-guarded via wrapDataHandler; missing required params return { error: '<KEY>_REQUIRED' } structured errors (never throw)"
    - "No version bump in this plan. The phase-wide rc.7 → 1.0.0 bump lives ONLY in Plan 05-10 (closure). Plans 05-04..05-09 ship as code commits + SUMMARY.md without producing intermediate tgzs."
    - "pnpm audit --audit-level=high reports zero high-severity advisories after the three new deps (xlsx, react-markdown, @playwright/test) land"

  artifacts:
    - path: "src/worker/handlers/deliverable-preview.ts"
      provides: "Worker data handler that parses xlsx server-side and returns a discriminated-union result per deliverable kind"
      exports: ["registerDeliverablePreview", "DeliverablePreviewCtx", "DeliverablePreviewResult"]
      contains: "wrapDataHandler"
      min_lines: 80
    - path: "src/ui/surfaces/reader/deliverable-preview.tsx"
      provides: "UI dispatcher on { kind } discriminated union; mounts the xlsx-grid / pdf-embed / md / img / placeholder branches"
      exports: ["DeliverablePreview"]
      contains: "switch"
      min_lines: 40
    - path: "test/worker/deliverable-preview.test.mjs"
      provides: "Worker handler unit tests — opt-in-guard, file-size guard, dispatch by kind, error envelope"
      min_lines: 60
    - path: "test/ui/deliverable-preview.test.mjs"
      provides: "UI source-grep tests pinning the dispatcher switch and the absence of dangerouslySetInnerHTML"
      min_lines: 30
    - path: "test/visual/sketch-regression.test.mjs"
      provides: "Playwright-driven loop that renders each tracked sketches/*.html under file:// and compares against test/visual/baselines/<sketch>.png with VISUAL_DIFF_THRESHOLD = 0.02"
      contains: "VISUAL_DIFF_THRESHOLD"
      min_lines: 30
    - path: "test/visual/baselines/01-task-detail-reader.png"
      provides: "Baseline image for sketches/paperclip-fix-task-detail.html"
    - path: "test/visual/baselines/02-situation-room.png"
      provides: "Baseline image for sketches/paperclip-fix-situation-room.html"
    - path: "test/visual/baselines/03-bulletin.png"
      provides: "Baseline image for sketches/paperclip-fix-bulletin.html"
    - path: "test/visual/baselines/04-employee-chat.png"
      provides: "Baseline image for sketches/paperclip-fix-employee-chat.html"
    - path: ".github/workflows/visual-regression.yml"
      provides: "CI workflow that runs the Playwright sketch-regression test on every PR + push to main/master and uploads diffs on failure"

  key_links:
    - from: "src/worker/handlers/deliverable-preview.ts"
      to: "xlsx (SheetJS)"
      via: "module-top `import * as XLSX from 'xlsx'`"
      pattern: "from\\s+['\"]xlsx['\"]"
    - from: "src/ui/surfaces/reader/deliverable-preview.tsx"
      to: "deliverable.preview worker handler"
      via: "usePluginData('deliverable.preview', { companyId, userId, issueId, documentKey })"
      pattern: "usePluginData[\\s\\S]*deliverable\\.preview"
    - from: "src/ui/surfaces/reader/deliverable-preview.tsx"
      to: "react-markdown"
      via: "named or default import"
      pattern: "from\\s+['\"]react-markdown['\"]"
    - from: "src/worker.ts"
      to: "registerDeliverablePreview"
      via: "import + setup() call"
      pattern: "registerDeliverablePreview\\("
    - from: ".github/workflows/visual-regression.yml"
      to: "test/visual/sketch-regression.test.mjs"
      via: "node --test invocation"
      pattern: "test/visual/sketch-regression\\.test\\.mjs"
---

<objective>
Close DIST-04 (full-fidelity deliverable previewers) + the visual-regression half of DIST-05 by:

1. Adding a NEW opt-in-guarded worker data handler `deliverable.preview` that parses xlsx server-side via SheetJS (SheetJS lives in the worker bundle ONLY) and returns a discriminated-union result `{ kind: 'xlsx-grid' | 'pdf-embed' | 'md' | 'img' | 'placeholder' } | { error }` to the UI.
2. Rewriting `src/ui/surfaces/reader/deliverable-preview.tsx` to dispatch on the worker's `kind` and render four real previewers (xlsx grid, native `<embed type="application/pdf">`, react-markdown, lazy `<img>`) replacing the locked "Phase 5 (DIST-04)" placeholder.
3. Updating `test/ui/reader-view.test.mjs`'s "Phase 5" literal assertion in the SAME commit as the placeholder replacement (D-24 atomic-commit rule).
4. Adding a Playwright-driven static-sketch visual-regression test (`test/visual/sketch-regression.test.mjs`) with four PNG baselines under `test/visual/baselines/` — one per tracked frozen sketch (D-25).
5. Adding `.github/workflows/visual-regression.yml` mirroring the `a11y-check.yml` shape (D-05 — every PR / push to main, hard failure, diff PNG uploaded as artifact).

**No version bump in this plan.** Six other Wave-1 plans (05-04..05-09) all ship as code-only commits; the phase-wide `1.0.0-rc.7 → 1.0.0` bump lives EXCLUSIVELY in Plan 05-10 (closure). This avoids the rc.N collision where seven plans each declared the same rc.8 bump and the first-to-land would silently invalidate the rest via grep-gate.

Purpose: v1.0.0 final cannot ship while Reader view still says "coming in Phase 5 (DIST-04)" on every deliverable section, and DIST-05 has a held-back third sub-requirement (visual regression) blocking traceability sign-off in Plan 05-10's VERIFICATION.md write.

Output: a worker handler + UI dispatcher + four baselines + a CI workflow + an updated reader-view.test.mjs literal — all gated behind the existing opt-in / no-`dangerouslySetInnerHTML` / no-bundled-React / additive-only invariants.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-distribution-polish/05-CONTEXT.md
@.planning/phases/05-distribution-polish/05-PATTERNS.md
@.planning/phases/05-distribution-polish/05-01-SUMMARY.md
@.planning/phases/05-distribution-polish/05-02-SUMMARY.md
@CLAUDE.md

<!-- Existing files this plan touches; executor reads them whole, not as scattered greps. -->
@src/ui/surfaces/reader/deliverable-preview.tsx
@src/ui/surfaces/reader/index.tsx
@src/worker/handlers/chat-active-tasks.ts
@test/ui/reader-view.test.mjs
@.github/workflows/a11y-check.yml
@package.json

<interfaces>
<!-- Key types and contracts the executor MUST use. Extracted from codebase to avoid scavenger-hunt navigation. -->

From src/worker/handlers/chat-active-tasks.ts (verbatim — the exact Ctx-composition pattern this plan reuses):

```typescript
import { wrapDataHandler, type OptInGuardDataCtx } from '../opt-in-guard.ts';
import type { PluginIssuesClient, PluginLogger } from '@paperclipai/plugin-sdk';

export type ChatActiveTasksCtx = OptInGuardDataCtx & {
  issues: PluginIssuesClient;
  logger?: PluginLogger;
};

// Inside the handler body:
wrapDataHandler(ctx, 'chat.taskOwned', async (params) => {
  const companyId = typeof params?.companyId === 'string' && params.companyId ? params.companyId : null;
  if (!companyId) return { error: 'COMPANY_ID_REQUIRED' as const };
  // ...happy path returns the structured result with a `kind:` discriminator
});
```

From src/ui/surfaces/reader/deliverable-preview.tsx (CURRENT — the file being rewritten):

```typescript
export type DeliverableProps = {
  deliverable: { filename: string; last_write_at: string | null } | null | undefined;
};

export function DeliverablePreview({ deliverable }: DeliverableProps): React.ReactElement | null {
  if (!deliverable) return null;
  // ...renders the "Phase 5 (DIST-04)" placeholder
}
```

The CALLER in src/ui/surfaces/reader/index.tsx line 330 is:

```typescript
<DeliverablePreview deliverable={data.deliverable} />
```

The signature must remain back-compatible — `deliverable` is still the prop; `companyId` / `userId` / `issueId` / `entityId` are read from the parent React context exactly the way `<AcChecklist>` already does on the next line (line 331: `<AcChecklist issueId={entityId} ... userId={userId} ...>`). Either thread them through props (cleanest) or read via the same hooks the surrounding components use.

From .github/workflows/a11y-check.yml (verbatim — the shape this plan's new workflow mirrors):

```yaml
on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

jobs:
  a11y:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Static a11y check
        run: node scripts/check-a11y.mjs
      - name: Test pin (node --test test/ci/check-a11y.test.mjs)
        run: node --test test/ci/check-a11y.test.mjs
```

From test/ui/reader-view.test.mjs (lines 88–92 — D-24 atomic-commit target; ONE assertion to update):

```javascript
test('deliverable-preview.tsx exports DeliverablePreview and includes the literal "Phase 5" deferred-message marker (READER-05)', () => {
  const src = readSrc('deliverable-preview.tsx');
  assert.match(src, /export function DeliverablePreview/);
  assert.match(src, /Phase 5/, 'literal "Phase 5" appears (READER-05 deferred preview)');
});
```

The test's NAME and the second `assert.match` (for /Phase 5/) BOTH must be updated when the placeholder is removed. The first assertion (`export function DeliverablePreview`) STAYS — the export contract is preserved.

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Worker handler `deliverable.preview` + xlsx server-side parse + unit tests + package-legitimacy audit step</name>
  <files>src/worker/handlers/deliverable-preview.ts, src/worker.ts, test/worker/deliverable-preview.test.mjs, package.json</files>
  <read_first>
    - src/worker/handlers/chat-active-tasks.ts (analog — Ctx composition lines 33-44, wrapDataHandler usage lines 65-80, structured-error return pattern lines 78-81)
    - src/worker/handlers/chat-open-for-issue.ts (analog for param-guard cascade — first 30 lines of the handler body)
    - src/worker.ts (lines 100-150 — where new handler registrations land alongside Plan 05-03's reader.ac.autostatus)
    - package.json (current dependencies block — react-markdown + xlsx are NEW runtime deps; @playwright/test is a NEW devDep)
  </read_first>
  <behavior>
    - Test 1 (opt-in guard): handler returns { error: 'OPT_IN_REQUIRED' } when the caller is opted-out (wrapDataHandler fires first).
    - Test 2 (param guards): missing companyId → { error: 'COMPANY_ID_REQUIRED' }; missing userId → { error: 'USER_ID_REQUIRED' }; missing issueId → { error: 'ISSUE_ID_REQUIRED' }; missing documentKey → { error: 'DOCUMENT_KEY_REQUIRED' }.
    - Test 3 (kind detection): a documentKey ending in `.xlsx` (or `.xlsm` REJECTED — see Test 8) routes to xlsx-grid; `.pdf` routes to pdf-embed with the document url; `.md` / `.markdown` routes to md with the body text; `.png` / `.jpg` / `.jpeg` / `.gif` / `.webp` routes to img.
    - Test 4 (xlsx happy path): given a small fixture xlsx (≤5 MB), the handler returns `{ kind: 'xlsx-grid', sheets: [{ name, rows: string[][] }] }`. Every cell is coerced to a string (numbers, dates, booleans → string representation; null → empty string). Formula cells return the CACHED computed value, NEVER the formula text.
    - Test 5 (md happy path): given a markdown document, the handler returns `{ kind: 'md', body }` with the raw markdown body.
    - Test 6 (pdf / img happy path): the handler returns `{ kind: 'pdf-embed', url }` or `{ kind: 'img', url }` carrying the document's host-resolvable URL (whatever shape `ctx.issues.documents.get(...)` / equivalent surfaces — pick the smallest read that already exists in the codebase; mirror the readback pattern from src/worker/handlers/agent-task-delivery.ts if needed).
    - Test 7 (size-cap chokepoint): a documentKey reporting a size > 5_000_000 bytes returns `{ error: 'DELIVERABLE_TOO_LARGE', sizeBytes }`; SheetJS MUST NOT be called.
    - Test 8 (macro rejection): a documentKey ending in `.xlsm` returns `{ error: 'XLSM_REJECTED' }`; SheetJS MUST NOT be called.
    - Test 9 (parse failure): a corrupt xlsx returns `{ error: 'PARSE_FAILED' }` and the handler does NOT throw (graceful degrade — UI renders the placeholder fallback).
    - Test 10 (unknown kind): a documentKey with an unrecognised extension returns `{ kind: 'placeholder', reason: 'Preview not available for this file type' }` — never throws.
  </behavior>
  <action>
    Create `src/worker/handlers/deliverable-preview.ts`. Compose `DeliverablePreviewCtx = OptInGuardDataCtx & { issues: PluginIssuesClient; logger?: PluginLogger }` per `chat-active-tasks.ts` lines 33-44.

    Export `registerDeliverablePreview(ctx)` that wraps `wrapDataHandler(ctx, 'deliverable.preview', async (params) => { ... })`. Param cascade (per `chat-open-for-issue.ts`): companyId / userId / issueId / documentKey — each typed-string-guarded; first missing returns the matching `{ error: '<KEY>_REQUIRED' as const }`.

    Define module-top constants:
    `const XLSX_MAX_BYTES = 5_000_000;` (D-01 single chokepoint — RESEARCH file-size budget).
    `const IMAGE_EXTENSIONS = ['.png', '.jpg', '.jpeg', '.gif', '.webp'] as const;`

    Import SheetJS at module top: `import * as XLSX from 'xlsx';`. This is the FIRST and ONLY place in the codebase that imports `xlsx`; the UI bundle build script (`scripts/build-ui.mjs`) does not touch worker files, so no UI externals change.

    Detect kind by lowercased extension of `documentKey`:
    - `.xlsx` → branch A: size-check → parse → return `{ kind: 'xlsx-grid', sheets }`.
    - `.xlsm` → return `{ error: 'XLSM_REJECTED' as const }` (macro-enabled rejected per threat model T-05-04-02).
    - `.pdf` → branch B: return `{ kind: 'pdf-embed', url }`.
    - `.md` / `.markdown` → branch C: return `{ kind: 'md', body }`.
    - extension in IMAGE_EXTENSIONS → branch D: return `{ kind: 'img', url }`.
    - other → `{ kind: 'placeholder', reason: 'Preview not available for this file type' }`.

    For the document fetch (size, bytes, body, url): use whatever surface the existing codebase already exposes — most likely `ctx.issues.documents.list(issueId, companyId)` to locate the document by key, then `ctx.issues.documents.get(...)` for body/url. If the SDK surface diverges, mirror the read pattern Plan 03-08's `agent-task-delivery.ts` already uses (which the project knows works against this host). All reads are try/catch-wrapped — failure → `{ error: 'READ_FAILED' as const }` + `ctx.logger?.warn?.(...)`.

    XLSX branch A specifics (per D-01 threat-model — parse-only, never formula-eval):
    - Call `XLSX.read(buffer, { type: 'buffer', cellFormula: false, cellDates: true })` to disable formula re-evaluation (security: T-05-04-01).
    - Iterate `workbook.SheetNames` → for each sheet, `XLSX.utils.sheet_to_json(sheet, { header: 1, raw: false, defval: '' })` produces `Array<Array<string>>`.
    - Cap row count per sheet at 1000 (extra defense — operator-visible "(truncated)" footer row if exceeded).
    - Wrap parse in try/catch → corrupt files → `{ error: 'PARSE_FAILED' as const }` + warn-log.

    Register the handler in `src/worker.ts` AFTER `registerReaderAcAutostatus` (around line 163). Add the import next to the Plan 05-03 import block (lines 133-139). Update the trailing `ctx.logger?.info?.(...)` registration log (line 336) to include `+ deliverable.preview`.

    Update `package.json`:
    - `dependencies`: add `"xlsx": "^0.20.3"` (SheetJS community edition — pin to exact tested minor)
    - `dependencies`: add `"react-markdown": "^9.0.1"` (needed for Task 2; declared here because deps are a SINGLE atomic edit)
    - `devDependencies`: add `"@playwright/test": "^1.50.0"` (needed for Task 4)
    Use the exact version pins documented in RESEARCH (cross-reference live npmjs.com to confirm `xlsx` 0.20.x and `react-markdown` 9.x are current stable).

    **Package Legitimacy Gate (3-line operator step, NOT a sub-protocol):**
    Three new deps land in this task. Before running `pnpm install`, briefly verify each on npmjs.com:
    1. `xlsx` (SheetJS community edition) — publisher sheetjs.com; ≥1M weekly downloads; no recent supply-chain advisories.
    2. `react-markdown` — publisher remarkjs; ≥10M weekly downloads; well-known.
    3. `@playwright/test` — publisher Microsoft; ≥4M weekly downloads; well-known.
    After install, run `pnpm audit --audit-level=high` and confirm ZERO high-severity advisories. If any high-severity advisory appears, STOP and escalate to a `checkpoint:human-verify` gate before proceeding. Document the verification (and the audit exit code) in the commit message that adds the deps.

    Write tests at `test/worker/deliverable-preview.test.mjs` using `node:test` (mirror the shape of `test/worker/chat-active-tasks.test.mjs` if it exists, otherwise `test/worker/chat-open-for-issue.test.mjs`). Each behavior above maps to a discrete `test('...')` block. For the xlsx fixture, generate a small workbook IN-TEST via `XLSX.utils.book_new() + XLSX.write(..., { bookType: 'xlsx', type: 'buffer' })` so the test does not commit a binary fixture; the corrupt-file test passes a hand-crafted invalid buffer.

    NEVER use `dangerouslySetInnerHTML`. NEVER throw from inside the handler body — always return the structured `{ error: ... }` envelope.
  </action>
  <verify>
    <automated>node --test test/worker/deliverable-preview.test.mjs &amp;&amp; pnpm typecheck &amp;&amp; pnpm audit --audit-level=high &amp;&amp; node -e "const t = require('fs').readFileSync('src/worker/handlers/deliverable-preview.ts', 'utf8'); if (!t.includes(\"wrapDataHandler(ctx, 'deliverable.preview'\")) process.exit(1); if (!t.includes('XLSX_MAX_BYTES')) process.exit(1); if (t.includes('dangerouslySetInnerHTML')) process.exit(1);" &amp;&amp; node -e "const t = require('fs').readFileSync('src/worker.ts', 'utf8'); if (!t.includes('registerDeliverablePreview')) process.exit(1);" &amp;&amp; node -e "const p = require('./package.json'); if (!p.dependencies.xlsx) process.exit(1); if (!p.dependencies['react-markdown']) process.exit(1); if (!p.devDependencies['@playwright/test']) process.exit(1);"</automated>
  </verify>
  <done>
    All 10 behavior tests pass. Worker handler is registered in `src/worker.ts` between Plan 05-03's reader.ac.autostatus block and the Editor-Agent reconcile block. SheetJS imported at module top of `deliverable-preview.ts`; no UI file imports xlsx. `package.json` carries the three new entries (xlsx + react-markdown in deps; @playwright/test in devDeps). `pnpm typecheck` clean. `pnpm audit --audit-level=high` exits 0 (zero high-severity vulns across the three new packages). Grep `dangerouslySetInnerHTML` in the new handler file returns zero.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UI dispatcher `<DeliverablePreview>` (rewrite — replaces placeholder) + UI tests + reader-view.test.mjs literal update (D-24 atomic commit)</name>
  <files>src/ui/surfaces/reader/deliverable-preview.tsx, test/ui/deliverable-preview.test.mjs, test/ui/reader-view.test.mjs, src/ui/surfaces/reader/index.tsx</files>
  <read_first>
    - src/ui/surfaces/reader/deliverable-preview.tsx (current placeholder — the file being rewritten; export contract preserved, body replaced)
    - src/ui/surfaces/reader/index.tsx lines 320-340 (the call site at line 330; `companyId` / `userId` / `entityId` already resolved on the surrounding `<AcChecklist>` line 331 — thread the same values through)
    - test/ui/reader-view.test.mjs lines 88-92 (the literal "Phase 5" assertion — D-24 atomic-commit target)
    - src/ui/primitives/ref-chip.tsx (analog for `usePluginData` + nav usage; same hooks import path)
  </read_first>
  <behavior>
    - Test U1 (export shape): `src/ui/surfaces/reader/deliverable-preview.tsx` still `export function DeliverablePreview(...)`. The signature accepts the existing `deliverable` prop AND the new context props (`companyId`, `userId`, `issueId`) so `<DeliverablePreview deliverable=... companyId=... userId=... issueId=... />` at the call site compiles.
    - Test U2 (worker key wired): the file's source contains `usePluginData('deliverable.preview', ...)`.
    - Test U3 (react-markdown import): the file imports from `'react-markdown'` (default or named — pick what the package documents for v9).
    - Test U4 (NO dangerouslySetInnerHTML): grep `dangerouslySetInnerHTML` in the new file → ZERO occurrences (the existing check-a11y R3 stays green).
    - Test U5 (img alt): the `<img>` branch's JSX literally carries `alt=` (check-a11y R1).
    - Test U6 (pdf embed): the source contains `<embed type="application/pdf"` (literal substring).
    - Test U7 (kind dispatch): the source contains a `switch (data.kind)` with at least the five arms: `'xlsx-grid'`, `'pdf-embed'`, `'md'`, `'img'`, `'placeholder'`.
    - Test U8 (error fallback): the source handles `'error' in data` → renders a graceful fallback string `'Preview unavailable — open in classic Paperclip.'` (literal). This pins the load-bearing user-visible fallback string.
    - Test R1 (reader-view.test.mjs update — D-24): the existing "Phase 5" assertion in `test/ui/reader-view.test.mjs` is REPLACED with assertions that match the new contract (no more `/Phase 5/` match). The test must still exist; just verifies the post-rewrite shape (e.g., matches `/usePluginData/` + `/'deliverable\.preview'/` + `/switch.*data\.kind/s`).
    - Test R2 (index.tsx call site): `<DeliverablePreview` in reader/index.tsx still resolves; reader-view.test.mjs's earlier assertion `assert.match(src, new RegExp(\`<${name}\\\\b\`))` for `DeliverablePreview` (line 75) MUST stay green.
  </behavior>
  <action>
    REWRITE `src/ui/surfaces/reader/deliverable-preview.tsx`. The export name + the file path stay; the body is replaced.

    Imports (top of file):
    - `import * as React from 'react';`
    - `import { usePluginData } from '@paperclipai/plugin-sdk/ui/hooks';`
    - `import ReactMarkdown from 'react-markdown';` (per react-markdown v9 docs — default export).

    New prop shape — extend the existing `DeliverableProps`:
    ```
    export type DeliverableProps = {
      deliverable: { filename: string; last_write_at: string | null } | null | undefined;
      companyId?: string | null;
      userId?: string | null;
      issueId?: string | null;
    };
    ```
    Keep the existing `ago()` helper (lines 19-32 of the current file — load-bearing for the filename + last-write line that survives the rewrite).

    Define the result type LOCALLY in the file (mirrors the worker's `DeliverablePreviewResult` without importing from worker — UI/worker stay structurally typed across the JSON-RPC boundary):
    ```
    type DeliverablePreviewResult =
      | { kind: 'xlsx-grid'; sheets: Array<{ name: string; rows: string[][] }> }
      | { kind: 'pdf-embed'; url: string }
      | { kind: 'md'; body: string }
      | { kind: 'img'; url: string }
      | { kind: 'placeholder'; reason: string }
      | { error: string };
    ```

    Body:
    1. If `!deliverable` → return null (preserve existing behavior).
    2. Render the existing `<h3>The deliverable</h3>` + filename + `last write {ago(...)}` lines (load-bearing visual context preserved).
    3. Call `usePluginData<DeliverablePreviewResult>('deliverable.preview', companyId && userId && issueId ? { companyId, userId, issueId, documentKey: deliverable.filename } : null)` — pass `null` (skip-fetch) when any context piece is missing, so the placeholder still renders sensibly during boot races (mirror the `useResolvedUserId` skip-fetch idiom from Plan 02-09).
    4. While loading or data is null → render `<div className="clarity-deliverable-loading">Loading preview…</div>`.
    5. If `'error' in data` → render `<div className="clarity-deliverable-fallback">Preview unavailable — open in classic Paperclip.</div>`.
    6. Switch on `data.kind`:
       - `'xlsx-grid'`: render a local `<XlsxGrid sheets={data.sheets} />` component (define it in this file — a simple `<table>` per sheet wrapped in a `<section role="region" aria-label={sheet.name}>`; cells via `{row[i] ?? ''}`).
       - `'pdf-embed'`: `<embed type="application/pdf" src={data.url} title={deliverable.filename} style={{ width: '100%', height: '60vh' }} />` (the literal `type="application/pdf"` is grepped by U6).
       - `'md'`: `<ReactMarkdown>{data.body}</ReactMarkdown>` — verify react-markdown v9 default-export usage matches docs; if the package exports named, switch to the documented name.
       - `'img'`: `<img src={data.url} alt={deliverable.filename} loading="lazy" style={{ maxWidth: '100%' }} />` (the literal `alt=` is grepped by U5).
       - `'placeholder'`: render `<div className="clarity-deliverable-placeholder">{data.reason}</div>`.

    NEVER use `dangerouslySetInnerHTML`. ReactMarkdown by default renders without raw HTML (rehypeRaw NOT used). Preserve check-a11y R3 invariant.

    Update `src/ui/surfaces/reader/index.tsx` line 330 to pass the three new props:
    ```
    <DeliverablePreview
      deliverable={data.deliverable}
      companyId={companyId}
      userId={userId}
      issueId={entityId}
    />
    ```
    The variables `companyId`, `userId`, `entityId` are already in scope at that point (verify by reading the same `<AcChecklist>` line 331 which already uses `entityId` and `userId`; `companyId` is one of `useResolvedCompanyId`'s outputs already wired earlier in the file — confirm before editing).

    UPDATE `test/ui/reader-view.test.mjs` lines 88-92 in the SAME COMMIT (D-24 atomic rule):
    - Change the test name from `'…includes the literal "Phase 5" deferred-message marker (READER-05)'` to `'…dispatches on data.kind via usePluginData and renders four real previewers (DIST-04)'`.
    - REMOVE the `assert.match(src, /Phase 5/, ...)` line.
    - ADD three new assertions:
      - `assert.match(src, /usePluginData[\s\S]*['"]deliverable\.preview['"]/, 'wires the deliverable.preview worker handler');`
      - `assert.match(src, /switch\s*\(\s*data\.kind\s*\)/, 'dispatches on data.kind discriminator');`
      - `assert.match(src, /<embed[\s\S]*type=["']application\/pdf["']/, 'pdf branch uses native <embed>');`
    - KEEP the `assert.match(src, /export function DeliverablePreview/);` line (export contract).

    Write `test/ui/deliverable-preview.test.mjs` — source-grep tests (same pattern as reader-view.test.mjs) covering U1-U8 above. This file is NEW and exists alongside reader-view.test.mjs so the contract is pinned at the component level even if reader-view.test.mjs ever moves.

    NEVER place fenced code blocks in `<action>` (this prose is the directive).
  </action>
  <verify>
    <automated>node --test test/ui/deliverable-preview.test.mjs &amp;&amp; node --test test/ui/reader-view.test.mjs &amp;&amp; pnpm typecheck &amp;&amp; node scripts/check-a11y.mjs &amp;&amp; node -e "const t = require('fs').readFileSync('src/ui/surfaces/reader/deliverable-preview.tsx', 'utf8'); if (t.includes('dangerouslySetInnerHTML')) process.exit(1); if (!t.includes('deliverable.preview')) process.exit(1); if (!t.includes('react-markdown')) process.exit(1); if (!/switch\s*\(\s*data\.kind/.test(t)) process.exit(1);" &amp;&amp; node -e "const t = require('fs').readFileSync('test/ui/reader-view.test.mjs', 'utf8'); if (/Phase 5/.test(t)) { console.error('reader-view.test.mjs still contains the locked Phase 5 literal — D-24 atomic update missed'); process.exit(1); }"</automated>
  </verify>
  <done>
    DeliverablePreview component dispatches on `data.kind` for all five kinds + error fallback. ReactMarkdown imported. No `dangerouslySetInnerHTML`. `<img>` carries `alt=`. `<embed type="application/pdf">` literal present. `reader-view.test.mjs` no longer contains `/Phase 5/` and its replacement assertions are green. The reader index.tsx call site passes companyId/userId/issueId. `check-a11y.mjs` exit 0 (R1/R2/R3 all still green). The placeholder replacement + reader-view.test.mjs literal update + the index.tsx prop-threading are committed atomically (single commit per D-24).
  </done>
</task>

<task type="auto">
  <name>Task 3: UI bundle-size guardrail + react-markdown integration smoke + Reader build verification</name>
  <files>scripts/check-ui-bundle-size.mjs, package.json, test/ci/ui-bundle-size.test.mjs</files>
  <read_first>
    - scripts/build-ui.mjs (to learn the dist/ui output path + how externals are configured — confirm react-markdown is NOT externalized since it's a runtime dep)
    - scripts/check-a11y.mjs (analog for a "scan-and-assert" CI script structure)
    - package.json scripts block (where the new bundle-size script registers)
  </read_first>
  <action>
    Add a small CI guardrail to enforce the `must_haves.truths` bundle-size invariant. This is the safety net that fails CI if react-markdown ends up inflating the UI bundle past 350 kB OR if SheetJS accidentally leaks into the UI bundle.

    Create `scripts/check-ui-bundle-size.mjs` (Node ESM, mirrors `scripts/check-a11y.mjs` exit-code idiom):
    - Read `dist/ui/index.js` (the built UI bundle from `pnpm build:ui`). If the file does not exist, exit 0 with a "skipped — run pnpm build first" message (so local pre-build runs don't fail; CI always builds first).
    - Read the file's byte size. The hard ceiling is **350 * 1024 = 358_400 bytes**. Rationale (cite in a comment at top of the script): baseline 288 kB + ~50 kB react-markdown delta + ~10 kB margin = 350 kB. If the bundle exceeds this, print the size + the budget and exit 1.
    - Define the ceiling as a single named constant near the top of the file: `const UI_BUNDLE_BYTES_CEILING = 350 * 1024;` so future bumps are a one-line edit.
    - Read the file's text. Assert it does NOT contain the literal substring `'XLSX'` or `'SheetJS'` or the SheetJS sentinel string `'!ref'` (a substring SheetJS always emits when bundled — confirm by trial). If any sentinel appears, print which one and exit 1 ("SheetJS leaked into the UI bundle — must stay worker-only").
    - Print success line with the actual byte count and budget so the CI log carries the trend.

    Register the script in `package.json` scripts: `"check-ui-bundle-size": "node scripts/check-ui-bundle-size.mjs"`.

    Extend the existing `prepublishOnly` script chain to include the bundle-size check after `node scripts/check-css-scope.mjs`. The current `prepublishOnly` value reads `"npm run build && npm run typecheck && npm test && node scripts/check-css-scope.mjs"`; append ` && node scripts/check-ui-bundle-size.mjs`. (Edit the literal in package.json.)

    Write `test/ci/ui-bundle-size.test.mjs` (mirror `test/ci/check-a11y.test.mjs`) that:
    - Spawns `node scripts/check-ui-bundle-size.mjs` via `node:child_process` execSync.
    - Asserts exit code 0 (or that the script self-skips if dist/ui/index.js doesn't exist — same pattern as the reader-view.test.mjs dist-bundle test at line 136-144).
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; node scripts/check-ui-bundle-size.mjs &amp;&amp; node --test test/ci/ui-bundle-size.test.mjs &amp;&amp; node --test test/ui/reader-view.test.mjs &amp;&amp; node -e "const p = require('./package.json'); if (!p.scripts['check-ui-bundle-size']) process.exit(1); if (!p.scripts.prepublishOnly.includes('check-ui-bundle-size')) process.exit(1);" &amp;&amp; node -e "const t = require('fs').readFileSync('scripts/check-ui-bundle-size.mjs', 'utf8'); if (!t.includes('350')) { console.error('ceiling constant 350 not found'); process.exit(1); }"</automated>
  </verify>
  <done>
    `scripts/check-ui-bundle-size.mjs` exists, exits 0 on the current build (≤ 350 kB), exits 1 if SheetJS leaks. The 350 kB ceiling is declared as a named constant with a one-line rationale comment. The script is wired into `prepublishOnly`. Test pin exists. Building the project succeeds with the new react-markdown import. The actual on-disk UI bundle size after react-markdown is added is logged in the verify output for the SUMMARY.md trail.
  </done>
</task>

<task type="auto">
  <name>Task 4: Visual-regression infrastructure — Playwright test + 4 baselines + CI workflow (threshold 0.02)</name>
  <files>test/visual/sketch-regression.test.mjs, test/visual/baselines/01-task-detail-reader.png, test/visual/baselines/02-situation-room.png, test/visual/baselines/03-bulletin.png, test/visual/baselines/04-employee-chat.png, .github/workflows/visual-regression.yml, package.json, playwright.config.mjs, .gitattributes</files>
  <read_first>
    - .github/workflows/a11y-check.yml (entire file — verbatim shape this workflow mirrors per D-05)
    - .github/workflows/lockfile-audit.yml (cross-reference for a second example of the on:/jobs: shape used in this repo)
    - sketches/paperclip-fix-task-detail.html (top 50 lines, just to confirm it's a self-contained file with inline CSS — no network deps needed for file:// loading)
    - .planning/phases/05-distribution-polish/05-PATTERNS.md §"`.github/workflows/visual-regression.yml`" (verbatim YAML pattern)
  </read_first>
  <action>
    Create the visual-regression infrastructure under D-04 + D-05. Independent of Tasks 1-3 (no `files_modified` overlap → could run in parallel; this plan is single-wave + sequential commits per `paperclipai-workspace-script-gotcha` discipline, but the work itself is logically independent).

    Create a minimal `playwright.config.mjs` at the repo root (the simplest possible config — Playwright v1.50 supports `node --test` integration; we use the test-runner directly via Playwright's browser API, not the @playwright/test BDD style — that way we keep the single `node --test` invocation in CI):
    - Project: `chromium` only (matches the workflow YAML's `npx playwright install --with-deps chromium` line).
    - viewport: `{ width: 1280, height: 800 }` — single canonical viewport for v1 (mockups assume desktop; multi-viewport is out of scope per CONTEXT.md `<deferred>`).
    - Re-export config so the test file can import it if needed.

    Create `test/visual/sketch-regression.test.mjs` (Node `node:test`, NOT `@playwright/test` BDD):
    - Import `chromium` from `'playwright'` (the engine package, transitive dep of `@playwright/test`).
    - Define the four sketches list (filename → baseline name):
      - `paperclip-fix-task-detail.html` → `01-task-detail-reader.png`
      - `paperclip-fix-situation-room.html` → `02-situation-room.png`
      - `paperclip-fix-bulletin.html` → `03-bulletin.png`
      - `paperclip-fix-employee-chat.html` → `04-employee-chat.png`
    - For each sketch, register a `test('visual: ${baseline}', async () => {...})`:
      - Launch chromium, open page, set viewport 1280×800.
      - Navigate to `pathToFileURL(path.resolve('sketches', sketch)).href` (file:// load — no network).
      - Wait for `await page.waitForLoadState('networkidle')` (the sketches are self-contained but font CSS may resolve via local FS).
      - Disable animations: `await page.emulateMedia({ reducedMotion: 'reduce' });` (deterministic snapshots).
      - Capture `const actualBuffer = await page.screenshot({ fullPage: true, animations: 'disabled' });`.
      - Read the baseline from `test/visual/baselines/${baseline}`. If `process.env.UPDATE_BASELINES === '1'`, write the new buffer to the baseline path (used by `pnpm visual:update`) and skip the diff.
      - Otherwise, compare buffers via `pixelmatch` (transitive of @playwright/test) OR implement a simple `Buffer.equals` check on a downsampled or compressed PNG. **Recommendation**: use `pixelmatch` via `await import('pixelmatch')` + `await import('pngjs')` (both transitive deps of @playwright/test; verify on first run, otherwise add them to devDeps).
      - **Diff threshold**: declare a single module-top constant `const VISUAL_DIFF_THRESHOLD = 0.02;` (2% — allow up to 2% of pixels to differ). Rationale (cite in a comment): v1 audience of 1 + D-25 sketch lockdown makes a 2% threshold acceptable; tightening to 0.001 (0.1%) would require regenerating baselines on a Linux CI runner to avoid cross-OS antialiasing drift (Windows-generated baselines vs Linux CI diff > 0.001 on the first PR). v1 ships with the looser threshold; v1.1+ can tighten once a `workflow_dispatch` baseline-regen workflow exists.
      - If diff > VISUAL_DIFF_THRESHOLD * totalPixels, write the diff PNG to `test/visual/diffs/${baseline}` and `assert.fail()` with the pixel-count + the threshold + the actual ratio.
      - Tear down browser in `afterEach` / try-finally.
    - At top of file include guard: `if (process.env.SKIP_VISUAL === '1') { return; }` so contributors without a working chromium can opt out; CI never sets this.

    Add `package.json` scripts:
    - `"visual:update": "UPDATE_BASELINES=1 node --test test/visual/sketch-regression.test.mjs"` — regenerates all four baselines.
    - `"visual:check": "node --test test/visual/sketch-regression.test.mjs"` — the CI invocation.

    Generate the four baselines (one-time bootstrap):
    - Run `pnpm install` to materialize @playwright/test + transitive deps.
    - Run `npx playwright install chromium` to fetch the browser binary (local-dev only; CI runs `--with-deps`).
    - Run `pnpm visual:update`. Commit the four resulting PNGs under `test/visual/baselines/`.
    - **Cross-OS drift note (per WARNING #6):** Baselines may be generated on either Windows (dev) or Linux (CI). With `VISUAL_DIFF_THRESHOLD = 0.02` the antialiasing-drift envelope between Windows-generated baselines and Linux CI diffs sits comfortably under 2% for the four static sketches (verified empirically on prior visual-regression setups; if a baseline-vs-Linux-CI diff exceeds 2% on first PR, regenerate baselines via a one-off `workflow_dispatch` of `visual-regression.yml` on a Linux runner — see option (a) below; v1 ships option (b) — looser threshold — for simplicity).
    - Verify each baseline is plausible — open it visually to confirm it captures the corresponding sketch's main content (not a blank page from a load failure).

    Add `.gitattributes` entry (or create if absent):
    ```
    test/visual/baselines/*.png binary
    test/visual/diffs/* linguist-generated=true
    ```
    (Per D-25 + Pattern Map: baselines are committed binaries; diffs are ephemeral build artifacts.)

    Create `.github/workflows/visual-regression.yml` mirroring the a11y-check.yml shape (verbatim trigger block + Node 20 setup, with the Playwright-specific install + diff-upload-on-failure step). Per `05-PATTERNS.md` §`.github/workflows/visual-regression.yml`, the body is:
    ```
    on:
      pull_request:
        branches: [main, master]
      push:
        branches: [main, master]
      workflow_dispatch:           # allows one-off baseline regen on Linux if cross-OS drift ever exceeds 2%
        inputs:
          regen_baselines:
            description: 'Set to "1" to regenerate baselines instead of diffing'
            required: false
            default: '0'
    jobs:
      visual:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v4
          - name: Setup Node 20
            uses: actions/setup-node@v4
            with:
              node-version: 20
          - name: Install pnpm
            uses: pnpm/action-setup@v4
            with:
              version: 9.15.4
          - name: Install deps
            run: pnpm install --frozen-lockfile
          - name: Install Playwright Chromium with deps
            run: npx playwright install --with-deps chromium
          - name: Visual-regression
            run: |
              if [ "${{ github.event.inputs.regen_baselines }}" = "1" ]; then
                pnpm visual:update
              else
                pnpm visual:check
              fi
          - name: Upload diffs on failure
            if: failure()
            uses: actions/upload-artifact@v4
            with:
              name: visual-regression-diffs
              path: test/visual/diffs/
          - name: Upload regenerated baselines (workflow_dispatch only)
            if: github.event_name == 'workflow_dispatch' && github.event.inputs.regen_baselines == '1'
            uses: actions/upload-artifact@v4
            with:
              name: visual-regression-baselines-linux
              path: test/visual/baselines/
    ```

    The `workflow_dispatch` trigger preserves the option (a) escape hatch from WARNING #6 — if the looser 2% threshold ever isn't enough (operator changes a sketch significantly), one click in GitHub Actions UI regenerates baselines on a Linux runner and uploads them as an artifact for the operator to commit. v1 default flow stays simple (option b — looser threshold).

    Sanity-check: Run `pnpm visual:check` locally AFTER baselines are committed. Expect exit 0 (no diffs against just-captured baselines). Then deliberately tweak one sketch byte (revert immediately) to confirm the test fails — this validates the diff path is wired correctly. (Do NOT commit the deliberate tweak.)

    NEVER touch any `sketches/*.html` content (D-25 lockdown — sketches are frozen during v1.0.0; updating a sketch mid-cycle without re-baselining will hard-fail every subsequent PR).
  </action>
  <verify>
    <automated>pnpm visual:check &amp;&amp; node -e "const fs = require('fs'); const expected = ['01-task-detail-reader.png','02-situation-room.png','03-bulletin.png','04-employee-chat.png']; for (const b of expected) { const p = 'test/visual/baselines/' + b; if (!fs.existsSync(p)) { console.error('missing baseline:', p); process.exit(1); } const sz = fs.statSync(p).size; if (sz &lt; 10000) { console.error('baseline suspiciously small (likely blank):', p, sz); process.exit(1); } }" &amp;&amp; node -e "const t = require('fs').readFileSync('test/visual/sketch-regression.test.mjs', 'utf8'); if (!t.includes('VISUAL_DIFF_THRESHOLD')) { console.error('threshold constant missing'); process.exit(1); } if (!/VISUAL_DIFF_THRESHOLD\s*=\s*0\.02/.test(t)) { console.error('threshold value !== 0.02'); process.exit(1); }" &amp;&amp; node -e "const t = require('fs').readFileSync('.github/workflows/visual-regression.yml', 'utf8'); if (!t.includes('pnpm visual:check')) process.exit(1); if (!t.includes('upload-artifact@v4')) process.exit(1); if (!t.includes('pull_request')) process.exit(1); if (!t.includes('workflow_dispatch')) process.exit(1);" &amp;&amp; node -e "const p = require('./package.json'); if (!p.scripts['visual:check']) process.exit(1); if (!p.scripts['visual:update']) process.exit(1);"</automated>
  </verify>
  <done>
    Four baselines exist under `test/visual/baselines/` (one per tracked sketch; each PNG ≥ 10 kB indicating a real screenshot, not a blank page). `pnpm visual:check` exits 0 against the just-committed baselines. `VISUAL_DIFF_THRESHOLD = 0.02` declared as a single module-top constant with a one-paragraph rationale comment (cross-OS antialiasing drift, v1 audience of 1, D-25 sketch lockdown). The workflow YAML is in place with the upload-artifact failure path AND a `workflow_dispatch` baseline-regen escape hatch. The two new scripts (`visual:check` and `visual:update`) are in `package.json`. `.gitattributes` flags baselines as binary. No `sketches/*.html` file was modified.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → plugin UI bundle (same-origin) | Operator-controlled clicks; deliverable filenames originate from `data.deliverable.filename` returned by `issue.reader` (already trusted-by-design — Plan 02-03). |
| plugin UI → worker JSON-RPC (`deliverable.preview`) | Untrusted parameters cross here; worker enforces companyId/userId/issueId/documentKey guards + opt-in gate via `wrapDataHandler`. |
| worker → host RPC (`ctx.issues.documents.list/get`) | Documents are host-stored artifacts the operator has classic-Paperclip read access to (READER-04 contract); no privilege escalation. |
| worker → SheetJS (xlsx parser) | xlsx blob bytes from host doc storage flow into SheetJS. SheetJS has a history of prototype-pollution + formula-injection issues; chokepoint enforcement lives in this handler. |
| UI → react-markdown (md branch) | Markdown body from host doc storage flows into react-markdown. Default react-markdown config disables raw HTML (rehypeRaw NOT used); links rendered as plain anchors. |
| UI → native `<embed>` (pdf branch) | Browser-native pdf viewer; same-origin file URL only; no cross-origin pdf loads. |
| CI Playwright sandbox | sketches/*.html load via `file://` — zero network requests; baseline images committed to repo with `.gitattributes binary`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | `xlsx` (SheetJS) parse path in `src/worker/handlers/deliverable-preview.ts` | mitigate | Call `XLSX.read(buffer, { type: 'buffer', cellFormula: false, cellDates: true })` — `cellFormula: false` disables formula evaluation (no formula-injection eval). Parse-only path; never write/edit. Cap rows per sheet at 1000 to bound memory. Wrap entire parse in try/catch → corrupt file → `{ error: 'PARSE_FAILED' }`. |
| T-05-04-02 | Tampering | Macro-enabled xlsm files | mitigate | Reject `.xlsm` extensions BEFORE SheetJS is called → `{ error: 'XLSM_REJECTED' }`. Macros are outside the v1 trust envelope; explicit reject is safer than silent parse-and-strip. |
| T-05-04-03 | Denial-of-Service | Oversized xlsx blobs (memory / CPU exhaustion in worker) | mitigate | `XLSX_MAX_BYTES = 5_000_000` central constant; size-check via doc metadata BEFORE buffer load. Documents > 5 MB return `{ error: 'DELIVERABLE_TOO_LARGE', sizeBytes }`. Per-sheet row cap of 1000 as defense-in-depth against zip-bomb-style xlsx files that fit under 5 MB but expand to millions of rows. |
| T-05-04-04 | Information Disclosure | `deliverable.preview` returns document body to viewers without permission | mitigate | `wrapDataHandler` enforces opt-in gate (OPTIN-04). Documents fetched via `ctx.issues.documents.*` inherit the host's per-viewer ACL — the worker passes companyId + userId and the host filters. Worker NEVER attempts a privileged escalation. |
| T-05-04-05 | Tampering | react-markdown rendering operator-supplied markdown body | mitigate | Use react-markdown defaults — `rehypeRaw` NOT enabled (no raw HTML injection). Default link rewriting acceptable per CONTEXT.md D-03 (no auto-follow attack surface for v1 audience of 1). Confirm via `node scripts/check-a11y.mjs` (R3 `dangerouslySetInnerHTML` rule stays green). |
| T-05-04-06 | Spoofing | UI bundle accidentally bundles SheetJS, inflating + double-evaluating | mitigate | `scripts/check-ui-bundle-size.mjs` asserts the UI bundle does NOT contain SheetJS sentinels (`XLSX` / `SheetJS` / `!ref`). Hard CI gate via `prepublishOnly` chain. |
| T-05-04-07 | Denial-of-Service | UI bundle bloat blocks Reader view first-paint | mitigate | `scripts/check-ui-bundle-size.mjs` 350 kB hard ceiling; CI gate. Baseline 288 kB + react-markdown v9 ~50 kB delta + ~10 kB margin = 350 kB ceiling. The ceiling lives as a single named constant `UI_BUNDLE_BYTES_CEILING` so future bumps are a one-line edit. |
| T-05-04-08 | Tampering | Visual-regression baseline drift from sketch edits | mitigate | D-25 lockdown: `sketches/*.html` frozen during v1.0.0. Any sketch byte change without baseline regen fails the workflow on the next PR. Operator pattern: `pnpm visual:update` re-baselines AFTER intentional sketch changes. `VISUAL_DIFF_THRESHOLD = 0.02` absorbs cross-OS antialiasing drift (Windows-generated baselines vs Linux CI runner) without silently letting through real UI-token regressions; if needed, `workflow_dispatch` regenerates baselines on Linux. |
| T-05-04-09 | Tampering | Playwright browser fetch (network access during test) | mitigate | All sketches loaded via `file://` URLs (`pathToFileURL(...)`) — no network. CI runner's outbound network not exercised during visual regression. |
| T-05-04-SC | Tampering | npm installs (`xlsx`, `react-markdown`, `@playwright/test`) | mitigate | All three are well-known established packages (SheetJS by sheetjs.com; react-markdown by remarkjs; @playwright/test by Microsoft). Verify legitimacy at install time via `npmjs.com/package/<name>` (legitimate publisher, ≥1M weekly downloads, no recent supply-chain advisories) — 3-line operator step documented in Task 1's `<action>`. After install, `pnpm audit --audit-level=high` MUST exit 0 (gated in Task 1's `<verify>` automated chain). If any high-severity advisory appears, STOP and escalate to a `checkpoint:human-verify` gate before proceeding. |

</threat_model>

<verification>

After all four tasks complete, run the phase-wide checks:

1. `pnpm build` — worker + UI + manifest builds clean.
2. `pnpm typecheck` — tsc --noEmit clean.
3. `pnpm test` — full suite passes (current baseline ~1414 net; expect +20-40 new tests from Tasks 1+2+3+4 test pins).
4. `node scripts/check-css-scope.mjs` — exit 0 (108 selectors; no new selectors added by this plan).
5. `node scripts/check-a11y.mjs` — exit 0 (R1/R2/R3 all green; react-markdown stays inside R3).
6. `node scripts/check-ui-bundle-size.mjs` — exit 0; reported bundle size ≤ 350 kB.
7. `pnpm visual:check` — all four baselines diff-clean (within VISUAL_DIFF_THRESHOLD = 0.02).
8. `pnpm audit --audit-level=high` — zero high-severity advisories.
9. `node scripts/coexistence-checks/run-all.mjs` — 10/10 PASS (no migration in this plan; existing checks unaffected).

**No `npm pack` in this plan.** No tarball is produced. Plan 05-10 produces the single `clarity-pack-1.0.0.tgz`.

Manual spot-check (operator OPTIONAL — not a checkpoint gate):

- Load Reader view on a Countermoves issue with an xlsx deliverable → expect inline grid (NOT "Phase 5" placeholder).
- Load Reader view on an issue with a pdf deliverable → expect native pdf embed.
- Load Reader view on an issue with a `.md` deliverable → expect rendered markdown.

Operator drill is reserved for Plan 05-10's canonical ALL-paths drill; this plan ships code-only.

</verification>

<success_criteria>

Plan 05-04 is complete when:

- [ ] `src/worker/handlers/deliverable-preview.ts` exists with `registerDeliverablePreview` exported and the 5-kind discriminated-union result shape.
- [ ] `src/worker.ts` registers the new handler between Plan 05-03's `registerReaderAcAutostatus` and the Editor-Agent reconcile block.
- [ ] `src/ui/surfaces/reader/deliverable-preview.tsx` dispatches on `data.kind` for all five kinds + the error fallback string.
- [ ] `src/ui/surfaces/reader/index.tsx` line 330 passes `companyId` / `userId` / `issueId` props to `<DeliverablePreview>`.
- [ ] `test/ui/reader-view.test.mjs` no longer contains the `/Phase 5/` literal — replaced atomically in the same commit as the placeholder replacement (D-24).
- [ ] `package.json` adds `xlsx` + `react-markdown` to `dependencies` and `@playwright/test` to `devDependencies`. Three new scripts (`visual:check`, `visual:update`, `check-ui-bundle-size`); `prepublishOnly` chain includes `check-ui-bundle-size`.
- [ ] `pnpm audit --audit-level=high` exits 0 after the new deps land.
- [ ] `scripts/check-ui-bundle-size.mjs` enforces the **350 kB** ceiling (named constant `UI_BUNDLE_BYTES_CEILING`) AND fails on SheetJS leakage into the UI bundle.
- [ ] Four PNG baselines exist under `test/visual/baselines/` (each ≥ 10 kB).
- [ ] `test/visual/sketch-regression.test.mjs` declares `VISUAL_DIFF_THRESHOLD = 0.02` as a module-top constant with a rationale comment (cross-OS drift envelope + v1 audience of 1).
- [ ] `.github/workflows/visual-regression.yml` mirrors the a11y-check.yml shape, uploads diffs on failure, AND exposes a `workflow_dispatch` baseline-regen escape hatch on a Linux runner.
- [ ] `playwright.config.mjs` + `test/visual/sketch-regression.test.mjs` drive the loop deterministically (file:// loads, reduced-motion, 1280×800 viewport).
- [ ] **NO version bump in this plan.** `package.json` version stays at `1.0.0-rc.7`; `src/manifest.ts` version stays at `1.0.0-rc.7`. The phase-wide bump to `1.0.0` happens in Plan 05-10 ONLY.
- [ ] **NO `npm pack` / tarball produced** in this plan. Plan 05-10 produces the single `clarity-pack-1.0.0.tgz`.
- [ ] All threats T-05-04-01 through T-05-04-SC have implementing mitigations.
- [ ] Test suite delta tracked in the SUMMARY.md (expect ~+25 net tests; 0 fail; 2 pre-existing skip).
- [ ] DIST-04 + DIST-05 (visual-regression half) flip from `Pending` to `Implemented (Plan 05-04 ...)` in REQUIREMENTS.md — DEFERRED to Plan 05-10's canonical traceability flip; this plan ships the code, 05-10 closes the requirement rows.

</success_criteria>

<output>

Create `.planning/phases/05-distribution-polish/05-04-SUMMARY.md` when done, following `~/.claude/get-shit-done/templates/summary.md`. Include in the SUMMARY:

- Suite delta (before → after).
- Bundle size before/after react-markdown addition (the actual byte count from `scripts/check-ui-bundle-size.mjs`).
- `pnpm audit --audit-level=high` final exit code + advisory count.
- Visual-regression baseline file sizes + the empirically-measured diff ratio (should be ≪ 2%) on the first `pnpm visual:check` run against the just-committed baselines.
- A note that NO version bump shipped (rc.7 retained); the phase-wide `1.0.0` bump lives in Plan 05-10.
- A note that NO tarball was produced; Plan 05-10 produces the single `clarity-pack-1.0.0.tgz`.
- A note that DIST-04 + DIST-05 traceability flips wait for Plan 05-10's closure write.

</output>
</content>
</invoke>