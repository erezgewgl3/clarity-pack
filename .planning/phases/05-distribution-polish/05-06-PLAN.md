---
phase: 05-distribution-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/surfaces/chat/context-rail.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - src/ui/surfaces/chat/index.tsx
  - src/ui/surfaces/chat/true-task/inline-task-card.tsx
  - src/ui/styles/chat.css
  - test/ui/chat-pin-toggle-toast.test.mjs
  - test/ui/chat-pinned-chip-flash.test.mjs
  - test/ui/chat-css-live-sticky.test.mjs
  - test/ui/chat-toast-stripe.test.mjs
  - test/ui/chat-inline-task-card.test.mjs
  - test/ui/chat-context-rail.test.mjs
  - test/manifest/chat-capabilities.test.mjs
  - package.json
  - src/manifest.ts
autonomous: true
requirements: []
tags: [clarity-pack, phase-5, polish, chat-surface, pin-unpin, toast, flash-highlight]

must_haves:
  truths:
    - "Pin and Unpin a chat message both fire a clarity-pack toast (`Message pinned` / `Message unpinned`) with no modal and no confirmation, mirroring the chat.topic.archive toggle shape (D-11)"
    - "Right-rail Pinned-messages chip click scrolls to the source comment and adds `.flash-highlight` for 1500ms (D-12, reusing Plan 04.2-04's existing `.flash-highlight` keyframe — no duplicate keyframe is introduced)"
    - "After a Create-Task lands inline (setPendingTaskCard fires), the messages container scrolls the new card into view via the existing `endRef.scrollIntoView` path"
    - "The pause-heartbeat toast copy reads `Use ▶ Resume heartbeat below to restart` (NOT `Resume from the agent page`) — the inline Resume affordance shipped in Plan 04.1-10 is the canonical resume surface"
    - "The chat surface's `.auto-refresh` LIVE indicator resolves to `position: sticky` at runtime (computed-style test pins it), i.e. no ancestor in the `.messages` container chain breaks the sticky context"
    - "Clarity-pack toasts carry a left-edge 3px `--you` gold stripe AND a leading `↗ ` glyph prefix so they are visually distinct from Paperclip host toasts (which sit bottom-LEFT)"
    - "Inline task cards render an optimistic `Todo` pill instead of the loading `· — ·` placeholder until chat.taskOwned reconciles; the optimistic value is overridden once a real status arrives"
    - "Plugin actions never modify `public.issues.updated_at` (CTT-07 invariant preserved across all 7 items — no new ctx.issues.update call sites introduced by this plan)"
    - "No new migration is introduced; chat_messages.pinned column from Plan 04-04 still owns Pin/Unpin state (coexistence guarantee #3 holds by virtue of no schema change)"
    - "React 19 stays peer-only; no new bundled React import is added (CLAUDE.md FORCED constraint preserved)"
  artifacts:
    - path: "src/ui/surfaces/chat/message-thread.tsx"
      provides: "Pin/Unpin handler now calls `showToast` (silent toast) instead of inline `setFeedback` text; inline task card branch wires optimistic-Todo override; messages container is the scroll target for the auto-scroll-after-create-task fix"
    - path: "src/ui/surfaces/chat/context-rail.tsx"
      provides: "Pause toast copy updated to `Use ▶ Resume heartbeat below to restart`; Pinned-messages chip mounted with click → scroll + flash-highlight handler"
    - path: "src/ui/surfaces/chat/true-task/inline-task-card.tsx"
      provides: "Optimistic `status: 'todo'` default when status is null/undefined (reconciles to real status on next chat.taskOwned poll)"
    - path: "src/ui/styles/chat.css"
      provides: "`.clarity-toast` border-left `--you` stripe + leading `↗` glyph; LIVE indicator sticky-context fix (audit + repair of ancestor overflow chain — patch lives near the existing `.messages` block at lines 437-446)"
    - path: "src/ui/surfaces/chat/index.tsx"
      provides: "setPendingTaskCard onDialogSuccess path triggers a one-shot scrollIntoView on the messages-end ref so the new card lands in the viewport"
    - path: "test/ui/chat-pin-toggle-toast.test.mjs"
      provides: "Source-grep test pinning that the Pin/Unpin branch in message-thread.tsx calls `showToast({ message: 'Message pinned' })` / `'Message unpinned'` AND that the inline `setFeedback({ kind: 'ok', text: '⚑ Pinned' })` text is REMOVED (silent-toggle invariant)"
    - path: "test/ui/chat-pinned-chip-flash.test.mjs"
      provides: "Source-grep test pinning that context-rail.tsx imports/dispatches the flash-highlight handler and that NO duplicate `@keyframes clarity-flash` or `.flash-highlight` class is defined outside chat.css's existing block (lines 2253-2281)"
    - path: "test/ui/chat-css-live-sticky.test.mjs"
      provides: "Computed-style test using jsdom (or static-CSS parse) asserting `getComputedStyle(.auto-refresh).position === 'sticky'` AND that the `.messages` ancestor chain has no `overflow: hidden` interruption between the surface root and the sticky element"
    - path: "test/ui/chat-toast-stripe.test.mjs"
      provides: "CSS source-grep test pinning `border-left: 3px solid var(--you)` on `.clarity-toast` AND `content: '↗ '` on `.clarity-toast::before`"
    - path: "test/ui/chat-inline-task-card.test.mjs"
      provides: "Updated existing test to pin that ChatTaskStatusPill renders `Todo` (NOT `· — ·`) when InlineTaskCard receives `status: null`"
    - path: "test/ui/chat-context-rail.test.mjs"
      provides: "Updated existing test to pin the new pause-toast copy string AND the absence of the old `Resume from the agent page` string"
    - path: "test/manifest/chat-capabilities.test.mjs"
      provides: "Version pin bumped to v1.0.0-rc.8"
    - path: "package.json"
      provides: "Version bump 1.0.0-rc.7 → 1.0.0-rc.8 (per plugin-version-bump-two-sources rule)"
    - path: "src/manifest.ts"
      provides: "Version literal bump 1.0.0-rc.7 → 1.0.0-rc.8 (the host reads dist/manifest.js, NOT package.json — both must move together)"
  key_links:
    - from: "src/ui/surfaces/chat/message-thread.tsx (PromoteActions onPin)"
      to: "src/ui/primitives/toast.tsx (showToast)"
      via: "useToast() hook"
      pattern: "showToast\\(\\{\\s*message:\\s*['\"]Message (un)?pinned['\"]"
    - from: "src/ui/surfaces/chat/context-rail.tsx (pinned-chip click)"
      to: ".messages article#msg-<commentId>"
      via: "document.getElementById + scrollIntoView + classList.add('flash-highlight')"
      pattern: "flash-highlight"
    - from: "src/ui/surfaces/chat/index.tsx (onDialogSuccess promote arm)"
      to: ".messages endRef.scrollIntoView"
      via: "setTimeout-deferred scrollIntoView after setPendingTaskCard resolves"
      pattern: "scrollIntoView"
    - from: "src/ui/styles/chat.css .clarity-toast"
      to: "var(--you) gold accent token"
      via: "border-left + ::before glyph"
      pattern: "border-left:\\s*3px\\s+solid\\s+var\\(--you\\)"
---

<objective>
Bundle seven Phase-4.1 drill-deferred surface fixes (a-g per CONTEXT.md / ROADMAP §05-06 / project memory `phase-4.2-deferred-from-4.1`) into one polish commit cluster shipped at v1.0.0-rc.8 toward v1.0.0 final. Per D-11 + D-12 (locked in CONTEXT.md): Pin/Unpin becomes a silent toggle + clarity-pack toast mirroring the `chat.topic.archive` shape from Plan 04.1-05; the right-rail Pinned-messages chip click reuses Plan 04.2-04's existing `.flash-highlight` keyframe for a 1.5s highlight pass. Five additional surface polish items — auto-scroll thread after Create-Task, pause-toast copy correction, LIVE indicator sticky restore, clarity-pack toast `--you` stripe + `↗` icon disambiguation from host toasts, and inline-task-card optimistic Todo default — close the rest of the bundle.

Purpose: close out the Phase 4.1 polish backlog filed during the 0.8.3 / 0.8.4 drills (project memory `phase-4.2-deferred-from-4.1`) without introducing new schema, new actions, or any regression to CTT-07 (plugin actions never modify `public.issues.updated_at`). All seven items are pure surface-layer changes; no migration; no new worker handler. The existing `chat.pin` worker action (Plan 04-04) is sufficient for D-11 — only the UI's feedback path needs to swap inline `setFeedback` for `showToast`.

Output: 7 surface fixes shipped as one logical polish wave; the chat surface tests grow by ~6 new files / 2 updates; version bump to 1.0.0-rc.8 in both `package.json` and `src/manifest.ts`. No tarball pack or operator drill is performed here — Plan 05-10 owns the v1.0.0 final closure drill and consumes whatever rc is current at the time.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-distribution-polish/05-CONTEXT.md
@.planning/phases/05-distribution-polish/05-PATTERNS.md
@.planning/phases/05-distribution-polish/05-PLAN-OUTLINE.md
@CLAUDE.md

<!-- Source-of-truth for the seven items + locked design decisions -->
@.planning/phases/04.1-chat-true-task/04.1-05-SUMMARY.md
@.planning/phases/04.1-chat-true-task/04.1-10-SUMMARY.md
@.planning/phases/04.2-reader-chat-bridge/04.2-04-SUMMARY.md

<!-- Files this plan modifies, plus their analog references -->
@src/ui/surfaces/chat/message-thread.tsx
@src/ui/surfaces/chat/context-rail.tsx
@src/ui/surfaces/chat/index.tsx
@src/ui/surfaces/chat/true-task/inline-task-card.tsx
@src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx
@src/ui/primitives/toast.tsx
@src/ui/styles/chat.css
@src/worker/handlers/chat-pin.ts
@src/worker/handlers/chat-topic-archive.ts

<interfaces>
<!-- Contracts already in tree — executor uses them as-is, no codebase exploration needed. -->

From src/ui/primitives/toast.tsx (Plan 04.1-09 — ALREADY EXPORTS what D-11 needs):
```typescript
export type Toast = { id: string; message: string; duration?: number };
export function ToastProvider({ children }: { children: React.ReactNode }): React.ReactElement;
export function useToast(): {
  showToast: (toast: Omit<Toast, 'id'>) => void;
  dismissToast: (id: string) => void;
  toasts: Toast[];
};
export function ChatToast(...): React.ReactElement;
```
Pattern: `const { showToast } = useToast(); showToast({ message: 'Message pinned' });` — fires bottom-right transient. PromoteActions in message-thread.tsx is already mounted under <ToastProvider> via ChatPageBody (ToastProvider wraps the chat shell). useToast() is safe to call from PromoteActions.

From src/worker/handlers/chat-pin.ts (Plan 04-04 — UNCHANGED by this plan, already a toggle):
```typescript
// chat.pin action takes { commentId, topicIssueId, companyId, userId, pinned: boolean }
// Returns { ok: true, commentId, pinned } on success; { error: 'PIN_FAILED' } on repo failure;
// throws { error: 'OPT_IN_REQUIRED' } via wrapActionHandler for opted-out callers.
// PIN BEHAVIOR: chat_messages.pinned column flip via pinChatMessageByCommentId; UPSERTs a pin-only
// row when the comment is an agent reply with no operator-message row. CTT-07 invariant: no
// ctx.issues.update call exists in this file.
```
For D-11: the UI's PromoteActions.onPin (message-thread.tsx lines 798-824) keeps calling this same action; only the feedback path swaps from `setFeedback({ kind: 'ok', text: '⚑ Pinned' })` to `showToast({ message: pinned ? 'Message pinned' : 'Message unpinned' })`. The `setFeedback` inline UI is REMOVED for the pin path (the operator gets the toast instead). Promote keeps its `setFeedback` (promote is a navigation-affecting action, not a reversible silent toggle).

From src/ui/styles/chat.css lines 2253-2281 (Plan 04.2-01/04.2-04 — REUSE ONLY, do not duplicate):
```css
@keyframes clarity-flash {
  0%   { box-shadow: 0 0 0 2px var(--you); }
  100% { box-shadow: 0 0 0 2px transparent; }
}
[data-clarity-surface="chat"] .msg.flash-highlight .bubble {
  animation: clarity-flash 1.5s ease-out;
  border-radius: 8px;
}
@media (prefers-reduced-motion: reduce) {
  [data-clarity-surface="chat"] .msg.flash-highlight .bubble {
    animation: none;
    box-shadow: 0 0 0 2px var(--you);
  }
}
```
The handler in D-12 adds `.flash-highlight` to the `<article id="msg-<commentId>">` element (NOT the `.bubble` — CSS already selects `.msg.flash-highlight .bubble`) for 1500ms then removes it. Matches Plan 04.2-04's deep-link scroll-and-flash pattern exactly.

From src/ui/surfaces/chat/message-thread.tsx (article id="msg-<commentId>" — lines 630-635):
```typescript
// The stable DOM scroll target for any chat-surface scroll-and-flash idiom.
// Set by Plan 04.2-01 (RCB-03). REUSED unchanged here.
<article id={`msg-${msg.commentId}`} className={`msg${isMine ? ' me' : ''}`}>
```

From src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx (Plan 04.1-06 — REUSED unchanged):
```typescript
export function ChatTaskStatusPill({ status }: { status: string | null | undefined }): React.ReactElement;
// When status is null/undefined: renders `<span className="st todo" aria-label="Status: loading" style={{ opacity: 0.5 }}>· — ·</span>`
// When status === 'todo': renders `<span className="st todo" aria-label="Status: Todo">Todo</span>` (no opacity).
```
For item (g) optimistic Todo: InlineTaskCard's `status` prop default changes from raw passthrough to `status ?? 'todo'` (the pill renders the real Todo label, not the muted `· — ·` loader). Once chat.taskOwned reconciles, the real status replaces the optimistic value via the existing `matchedTask?.status ?? null` → `matchedTask?.status ?? 'todo'` pipeline in message-thread.tsx line 504.

From src/ui/surfaces/chat/context-rail.tsx line 99 (Plan 04.1-09 — REPLACED by item (d)):
```typescript
// CURRENT:
showToast({ message: `Heartbeat paused for ${name}. Resume from the agent page.` });
// TARGET:
showToast({ message: `Heartbeat paused for ${name}. Use ▶ Resume heartbeat below to restart.` });
```
The "▶ Resume heartbeat" button it points at lives in the same component at lines 215-224 (already mounted under the Quick actions header — the same component renders both halves).

From src/ui/surfaces/chat/index.tsx lines 624-672 (Plan 04.1-10 — the setPendingTaskCard call site item (c) extends):
```typescript
// After dialog success for PROMOTE mode (around line 646):
setPendingTaskCard({ issueId: result.issueId, title: titleForCard });
// Item (c) adds: schedule a scrollIntoView on the messages-end ref after this state lands.
// The end ref is owned by message-thread.tsx (line 395 endRef.current?.scrollIntoView({ block: 'end' })).
// index.tsx needs a way to trigger it. Options (planner picks one in <action>):
//   - Pass an onPendingResolved-style scroll-trigger prop down to MessageThread, OR
//   - Lift the endRef.scrollIntoView into a method MessageThread exposes via ref, OR
//   - Reuse the existing `ordered.length` effect in message-thread.tsx (line 393-396) which already
//     scrolls to end on length change — adding pendingTaskCard to the dep array auto-fires when the
//     optimistic card mounts.
// The third option is the cheapest and idiomatic (no new prop, no imperative handle); the dep-array
// extension is the minimal-touch path.
```

From src/ui/styles/chat.css lines 437-505 (LIVE indicator current sticky context):
```css
[data-clarity-surface="chat"] .messages {
  flex: 1;
  overflow-y: auto;  /* THIS is the scroll container — sticky context */
  padding: 24px 22px 16px;
  display: flex;
  flex-direction: column;
  gap: 18px;
  min-height: 0;
}
[data-clarity-surface="chat"] .auto-refresh {
  position: sticky;
  top: 0;
  z-index: 5;
  margin: -24px -22px 6px;  /* full-bleed cancel of .messages padding */
  /* ... */
}
```
The sticky context IS established (the `.messages` overflow-y: auto creates it). If the LIVE indicator is "floating mid-thread" (per the project memory + STATE.md historical note from the 0.8.4 drill), the breaker is most likely a side-effect of the recent Plan 04.1-08/04.1-10 wrapper changes (the `.thread` grid + `min-width: 0` retrofit). The audit traces upward from `.auto-refresh` through `.messages` → `.thread` → the surface root, flagging any ancestor with `overflow: hidden`, `transform`, `filter`, `backdrop-filter`, `contain`, or `will-change: transform` — all of which break sticky contexts per the CSS spec. Likely culprit: an `overflow: hidden` left on `.thread` from the responsive-layout fix.
</interfaces>

<grep_outputs_for_audit>
<!-- Pre-collected for the executor (avoids a fresh codebase scan). -->
<!-- All locations are 1-based line numbers as of the rc.7 working tree. -->

Pin/Unpin current UI feedback path:
- src/ui/surfaces/chat/message-thread.tsx:798-824 — onPin callback; `setFeedback({ kind: 'ok', text: '⚑ Pinned' })` at 816.
- src/ui/surfaces/chat/message-thread.tsx:831-833 — `<button className="pa" onClick={onPin}>⚑ Pin</button>` (button label stays; only the feedback channel changes).

Pause toast copy:
- src/ui/surfaces/chat/context-rail.tsx:99 — `Heartbeat paused for ${name}. Resume from the agent page.` (TARGET REPLACEMENT).

Inline-task-card status prop flow:
- src/ui/surfaces/chat/message-thread.tsx:504 — `status={matchedTask?.status ?? null}` (the source of the null pass-through).
- src/ui/surfaces/chat/true-task/inline-task-card.tsx:67 — `status: string | null | undefined` prop type.
- src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx:42-48 — null/undefined branch renders the muted `· — ·` form.

Right-rail Storage pin block (where the new Pinned-messages chip mounts — NOT the storage-pin card, which Plan 05-08 wires):
- src/ui/surfaces/chat/context-rail.tsx:238-249 — Storage pin block. The Pinned-messages chip is a NEW affordance; it does NOT replace storage-pin (which Plan 05-08 owns). Mount it above Storage pin OR under Active tasks owned (Claude's discretion).

setPendingTaskCard call site (auto-scroll trigger):
- src/ui/surfaces/chat/index.tsx:646 — `setPendingTaskCard({ issueId: result.issueId, title: titleForCard });` (PROMOTE arm of onDialogSuccess).
- src/ui/surfaces/chat/message-thread.tsx:393-396 — `React.useEffect(() => { endRef.current?.scrollIntoView({ block: 'end' }); }, [ordered.length, pendingOverlay.length]);` (the existing auto-scroll effect).

Toast CSS current shape:
- src/ui/styles/chat.css:2087-2099 — `.clarity-toast` block (TARGET: ADD `border-left: 3px solid var(--you);` + `padding-left` adjust + `::before { content: '↗ '; }`).

LIVE indicator sticky audit start point:
- src/ui/styles/chat.css:487-505 — `.auto-refresh` sticky declaration.
- src/ui/styles/chat.css:438-446 — `.messages` overflow-y: auto (the established scroll context).
- src/ui/styles/chat.css — Grep for `overflow: hidden` on any ancestor between `.messages` and the surface root; that's the breaker (most likely candidates: `.thread`, `.chat-shell`, `[data-clarity-surface="chat"]`).
</grep_outputs_for_audit>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin/Unpin silent toggle + clarity-pack toast (D-11) + Pinned-chip flash-highlight (D-12)</name>
  <files>
    src/ui/surfaces/chat/message-thread.tsx,
    src/ui/surfaces/chat/context-rail.tsx,
    src/ui/styles/chat.css,
    test/ui/chat-pin-toggle-toast.test.mjs,
    test/ui/chat-pinned-chip-flash.test.mjs
  </files>

  <read_first>
    @src/ui/surfaces/chat/message-thread.tsx (lines 686-833 — PromoteActions component, onPin callback)
    @src/ui/surfaces/chat/context-rail.tsx (entire file — for chip mount site)
    @src/ui/primitives/toast.tsx (useToast hook contract)
    @src/ui/styles/chat.css (lines 2253-2281 — existing .flash-highlight rules to REUSE)
    @src/worker/handlers/chat-pin.ts (the worker handler is unchanged; read for API shape only)
    @.planning/phases/04.1-chat-true-task/04.1-05-SUMMARY.md (the chat.topic.archive toggle pattern this mirrors)
    @.planning/phases/04.2-reader-chat-bridge/04.2-04-SUMMARY.md (the scroll-and-flash pattern this reuses)
  </read_first>

  <behavior>
    - D-11 Pin → calls `showToast({ message: 'Message pinned' })` and NO longer mounts the `setFeedback({ kind: 'ok', text: '⚑ Pinned' })` inline span on success.
    - D-11 Unpin → calls `showToast({ message: 'Message unpinned' })` (same code path; the `pinned: !(pinned || optimisticPinned)` calculation already produces the right truth value).
    - D-11 Pin/Unpin error path → still surfaces via `setFeedback({ kind: 'error', text: 'Could not pin (...)' })` (errors stay loud; only the success path goes silent). Acceptable to also pipe errors through `showToast` with a longer duration, but the inline error UI MUST remain so a toast that auto-dismisses does not leave the operator without recourse.
    - D-11 silent invariant: NO modal, NO confirmation prompt, NO `window.confirm` call, NO new dialog mount on the Pin/Unpin branches.
    - D-12 Pinned-chip mount site: a new affordance in context-rail.tsx — under `<h3>Pinned</h3>` (NEW heading) OR a chip inside an existing heading-anchored block. Claude's discretion on exact placement; constraint = it lives in the right rail, not the message thread, and it does NOT collide with the existing `<h3>Storage pin</h3>` block (lines 238-249) which Plan 05-08 owns.
    - D-12 chip data source: the chip lists pinned messages for the active topic. Source the list from the same `messages` array MessageThread already consumes; filter `msg.pinned === true`. The chip renders one row per pinned message with a short preview (first ~40 chars of `msg.body`) AND a click target.
    - D-12 click handler: on click, `document.getElementById('msg-' + commentId)` → if found, `scrollIntoView({ behavior: 'smooth', block: 'center' })` + `element.classList.add('flash-highlight')` + `setTimeout(() => element.classList.remove('flash-highlight'), 1500)`. NO new @keyframes rule; NO new `.flash-highlight` CSS block — chat.css lines 2253-2281 already own it.
    - Plumbing: context-rail.tsx needs the messages list to filter pinned. Either lift the pinned filter into a new prop ContextRail accepts (added at index.tsx line ~775 where ContextRail is mounted), OR fetch chat.messages independently from the rail (the host bridge deduplicates a parallel `usePluginData('chat.messages', ...)` call — the same pattern Plan 04.2-04 used for `chat.roster` deduplication). The prop-drill option is cheaper if MessageThread already exposes the array up; otherwise prefer the parallel fetch (the worker handler is read-only + cheap).
    - CTT-07 verification: `grep -nE "ctx\\.issues\\.update" src/worker/handlers/chat-pin.ts | wc -l == 0` MUST still hold (no change required — the handler isn't modified, but the test guards against accidental cross-plan regression).
  </behavior>

  <action>
    Items (a) + (b) bundled — D-11 + D-12. Five layers:

    Layer 1 — Pin/Unpin toast swap in message-thread.tsx PromoteActions.onPin (lines 798-824). Import `useToast` from `../../primitives/toast.tsx`; at the top of PromoteActions add `const { showToast } = useToast();`. In the success branch of onPin (currently `setFeedback({ kind: 'ok', text: '⚑ Pinned' })` at line 816), REPLACE with `showToast({ message: nextPinned ? 'Message pinned' : 'Message unpinned' })` where `nextPinned` is the local `!(pinned || optimisticPinned)` value already computed at line 807. Keep `setOptimisticPinned(true)` (the local UI marker for the next render — the bubble's `⚑ Pinned` resolved-class at line 665 still depends on msg.pinned and an onRefresh round-trip). Keep `onRefresh?.()`. REMOVE the success-only `setFeedback` line. Error path stays unchanged (setFeedback still fires `Could not pin (...)`). The success case now has NO setFeedback call.

    Layer 2 — Pinned-messages chip mount in context-rail.tsx. Add ContextRail props for the pinned-list source. Two acceptable shapes (per behavior):
      Shape A (prop-drill): add `pinnedMessages?: Array<{ commentId: string; body: string }>` to ContextRail's props type. Compute the list in ChatPageBody at index.tsx (the parent that already owns chat.messages — see usePluginData around lines 760-780). Pass it down.
      Shape B (parallel fetch): inside ContextRail, `const { data: msgData } = usePluginData('chat.messages', topic ? { companyId, userId, topicIssueId: topic.issueId } : {});` and derive `pinnedMessages = (msgData?.messages ?? []).filter(m => m.pinned === true)`.
    Pick Shape A if MessageThread already lifts the array to ChatPageBody (it does NOT — message-thread.tsx owns `messages` locally via its own usePluginData), so Shape B is the cheaper path and matches Plan 04.2-04's chat.roster dedup pattern (parallel usePluginData call; host bridge dedupes). Use Shape B.
    Render the chip inside a new `<h3>Pinned</h3>` block above `<h3>Storage pin</h3>` (line 240) only when `pinnedMessages.length > 0`. Each row: `<button type="button" className="pin-chip" onClick={() => handlePinnedClick(commentId)}>{previewText}</button>` where previewText is the first 40 chars of `body` (trim + ellipsis). Add minimal CSS in chat.css for `.pin-chip` (display inline-block, padding 4px 8px, border 1px solid var(--line), hover border-color var(--you), cursor pointer; the styling can match existing `.qa` row aesthetic so it visually belongs in the rail).

    Layer 3 — handlePinnedClick handler inside ContextRail:
    ```
    function handlePinnedClick(commentId: string): void {
      const el = typeof document !== 'undefined' ? document.getElementById(`msg-${commentId}`) : null;
      if (!el) return;
      el.scrollIntoView({ behavior: 'smooth', block: 'center' });
      el.classList.add('flash-highlight');
      window.setTimeout(() => el.classList.remove('flash-highlight'), 1500);
    }
    ```
    No new keyframe; chat.css lines 2253-2281 already own `.flash-highlight`'s animation rule (the existing selector `[data-clarity-surface="chat"] .msg.flash-highlight .bubble` matches when the article gets the class — same idiom as Plan 04.2-04's deep-link consume path).

    Layer 4 — Minimal `.pin-chip` CSS in chat.css (insert near the existing `.qa` block; rooted under `[data-clarity-surface="chat"]`):
    selector `[data-clarity-surface="chat"] .pin-chip` declares display, padding, border, border-radius, font-size 12px, color var(--ink-2), background transparent, cursor pointer; the `:hover` rule sets border-color var(--you) and color var(--ink). NO new @keyframes anywhere in this layer.

    Layer 5 — Two new contract tests:
      - test/ui/chat-pin-toggle-toast.test.mjs — source-grep style (matches existing test/ui/chat-url-params.test.mjs idiom). Asserts: (1) message-thread.tsx imports `useToast` from primitives/toast.tsx; (2) message-thread.tsx contains the literal `'Message pinned'` AND `'Message unpinned'` strings inside its `showToast` call site; (3) the `setFeedback` line for the Pin success path (`text: '⚑ Pinned'`) has been REMOVED — `grep -c "text: '⚑ Pinned'" src/ui/surfaces/chat/message-thread.tsx === 0`; (4) the error-path `setFeedback({ kind: 'error', text: 'Could not pin` line is still present (errors still loud).
      - test/ui/chat-pinned-chip-flash.test.mjs — source-grep. Asserts: (1) context-rail.tsx contains the literal `flash-highlight` class name string; (2) the call sequence `scrollIntoView` followed within ~200 chars by `.classList.add('flash-highlight')` followed by `setTimeout` and `.classList.remove('flash-highlight'`; (3) `grep -c "@keyframes clarity-flash" src/ui/styles/chat.css === 1` (DO NOT duplicate the keyframe); (4) `grep -c "\\.flash-highlight" src/ui/styles/chat.css === 2` (two existing matches in the keyframe block + selector + reduced-motion variant — confirm by reading the file; adjust the expected count if the existing chat.css has a different number of matches but the count MUST be unchanged by this plan).

    Use the existing `chat-url-params.test.mjs` file as the structural template for both new tests. No DOM/jsdom needed — pure source-grep on the .tsx and .css.

    Per D-11 + Wave 1 isolation note (CTT-07 reaffirmed in cross_cutting_truths_for_this_plan): there is NO worker change here. The plan does NOT touch `src/worker/handlers/chat-pin.ts` and does NOT introduce a new handler (mirroring the chat.topic.archive shape means matching the UI-toggle/toast UX, NOT cloning the handler — chat.pin is already a toggle).
  </action>

  <verify>
    <automated>node --test test/ui/chat-pin-toggle-toast.test.mjs test/ui/chat-pinned-chip-flash.test.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node scripts/check-css-scope.mjs</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/surfaces/chat/message-thread.tsx','utf8'); const stripped = c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,''); const setFb = (stripped.match(/setFeedback\(\{ kind: 'ok', text: '⚑ Pinned'/g) || []).length; if (setFb !== 0) { console.error('FAIL: success setFeedback for pin still present, count=' + setFb); process.exit(1); } const show = (stripped.match(/showToast\(\{[^}]*'Message (un)?pinned'/g) || []).length; if (show < 1) { console.error('FAIL: showToast Message (un)pinned not found'); process.exit(1); } console.log('OK: D-11 Pin/Unpin silent-toast invariant holds.');"</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/styles/chat.css','utf8'); const kf = (c.match(/@keyframes\s+clarity-flash/g) || []).length; if (kf !== 1) { console.error('FAIL: expected exactly 1 @keyframes clarity-flash, got ' + kf); process.exit(1); } console.log('OK: D-12 no duplicate keyframe; chat.css ownership preserved.');"</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/worker/handlers/chat-pin.ts','utf8'); const stripped = c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,''); const calls = (stripped.match(/ctx\.issues\.update\(/g) || []).length; if (calls !== 0) { console.error('FAIL: CTT-07 regression — chat-pin.ts calls ctx.issues.update'); process.exit(1); } console.log('OK: CTT-07 invariant holds.');"</automated>
  </verify>

  <acceptance_criteria>
    <source>The chat surface's PromoteActions.onPin callback fires `showToast` with `'Message pinned'` or `'Message unpinned'` based on the toggle direction (D-11 verbatim from CONTEXT.md).</source>
    <behavior>Right-rail Pinned-messages chip click → `scrollIntoView` + 1500ms `.flash-highlight` (D-12 verbatim from CONTEXT.md). NO new @keyframes is introduced.</behavior>
    <cli>All four automated verifies above MUST pass green. The grep gates `setFb === 0` and `show >= 1` MUST hold via the stripped-comments node script (raw file grep would false-positive on documentation strings; the stripping rule mirrors test/ui/chat-url-params.test.mjs's idiom).</cli>
    <cli>`node scripts/check-css-scope.mjs` exits 0 (no unscoped selectors added — the new `.pin-chip` rule is rooted at `[data-clarity-surface="chat"]`).</cli>
    <cli>`npx tsc --noEmit` exits 0 (the new useToast import + the new prop on ContextRail typecheck cleanly).</cli>
  </acceptance_criteria>

  <done>
    Pin/Unpin a chat message fires a transient bottom-right toast (`Message pinned` / `Message unpinned`) with no inline ⚑-text mounted on the bubble's success path; the right-rail Pinned-messages chip appears when any visible message in the active topic has `pinned: true`; clicking a chip row scrolls the source bubble into view and flashes it for 1.5s using the existing chat.css keyframe; chat-pin.ts still does not import or call ctx.issues.update.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Auto-scroll-after-create-task (c) + Pause-toast copy fix (d) + Inline-task-card optimistic Todo (g)</name>
  <files>
    src/ui/surfaces/chat/message-thread.tsx,
    src/ui/surfaces/chat/context-rail.tsx,
    src/ui/surfaces/chat/true-task/inline-task-card.tsx,
    test/ui/chat-inline-task-card.test.mjs,
    test/ui/chat-context-rail.test.mjs
  </files>

  <read_first>
    @src/ui/surfaces/chat/message-thread.tsx (lines 393-396 — endRef.scrollIntoView effect; lines 478-513 — inline-task-card render branch; line 504 — `status={matchedTask?.status ?? null}`)
    @src/ui/surfaces/chat/index.tsx (lines 624-672 — setPendingTaskCard onDialogSuccess promote arm)
    @src/ui/surfaces/chat/context-rail.tsx (lines 92-102 — onPauseHeartbeat toast call)
    @src/ui/surfaces/chat/true-task/inline-task-card.tsx (lines 44-75 — status prop type and ariaLabel)
    @src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx (entire file — null/undefined branch behavior to confirm `'todo'` will render `Todo` correctly)
    @test/ui/chat-inline-task-card.test.mjs (existing test file — for the update site)
    @test/ui/chat-context-rail.test.mjs (existing test file — for the pause-toast string update)
  </read_first>

  <behavior>
    - Item (c): when `setPendingTaskCard` lands a new optimistic card, the messages container scrolls so the new card is visible. The existing effect at message-thread.tsx lines 393-396 already fires on `ordered.length` / `pendingOverlay.length` changes — but the optimistic card from `pendingTaskCard` is NEITHER (the optimistic card is rendered as the trailing element in the JSX at lines 567-577, NOT inserted into `ordered` until the marker comment arrives via the next poll). Solution: add `pendingTaskCard` to the dep array of the lines-393 useEffect so a non-null transition fires the scroll. Same code, expanded deps.
    - Item (d): replace context-rail.tsx line 99 toast copy from `Resume from the agent page.` to `Use ▶ Resume heartbeat below to restart.` — matches Plan 04.1-10's inline Resume affordance (the same component renders the inline ▶ Resume button at lines 215-224).
    - Item (g): InlineTaskCard's `status` parameter now defaults to `'todo'` when null/undefined is passed (the optimistic render). The ariaLabel `${statusLabel}` line at message-thread.tsx ~75 of inline-task-card.tsx uses `statusLabel = status ?? 'pending'` — change `status ?? 'pending'` to `status ?? 'todo'` so the aria text reads `status todo` instead of `status pending`. The `<ChatTaskStatusPill status={status} />` call at line 127 of inline-task-card.tsx: status pill receives the same status the parent passed — to make the Todo render appear when status is null, EITHER (i) coerce inside InlineTaskCard via `const statusForPill = status ?? 'todo';` and pass `statusForPill` to the pill, OR (ii) change the pill's null branch to render `Todo` instead of `· — ·`. Option (i) is cleaner — it scopes the optimism to InlineTaskCard's context only; the pill stays a presentational primitive that can still render `· — ·` for other (non-inline-card) callers if any exist.
    - CTT-07 holds (no worker changes; UI-only).
    - No new keyframe; no new dependency.
  </behavior>

  <action>
    Three independent surface fixes bundled into one task because each is a few lines and they all live in the chat-surface UI tier. Order doesn't matter; commit can be split or kept as one — Claude's discretion.

    Fix (c) — auto-scroll thread after Create-Task. In src/ui/surfaces/chat/message-thread.tsx, locate the existing effect:
      `React.useEffect(() => { endRef.current?.scrollIntoView({ block: 'end' }); }, [ordered.length, pendingOverlay.length]);`
    Extend the dep array to `[ordered.length, pendingOverlay.length, pendingTaskCard?.issueId]`. The transition from `pendingTaskCard === null` to `pendingTaskCard !== null` re-runs the effect and the optimistic card lands in the viewport. Add a brief comment block above naming the fix as `Plan 05-06 item (c)` and that the issueId is used (not the object reference) because object identity changes on every parent re-render but issueId is stable per card.

    Fix (d) — pause toast copy. In src/ui/surfaces/chat/context-rail.tsx line 99, replace the string `\`Heartbeat paused for ${name}. Resume from the agent page.\`` with `\`Heartbeat paused for ${name}. Use ▶ Resume heartbeat below to restart.\``. One-line edit. The replacement matches Plan 04.1-10's inline Resume affordance which already lives at lines 215-224 of the same file (the message uses `below` — the Resume button renders later in the same component's JSX, which is below in DOM order, matching the user's eye sweep).

    Fix (g) — inline-task-card optimistic Todo. In src/ui/surfaces/chat/true-task/inline-task-card.tsx:
      (i) At line 69, change `const statusLabel = status ?? 'pending';` to `const statusLabel = status ?? 'todo';`.
      (ii) At line 127, replace `<ChatTaskStatusPill status={status} />` with `<ChatTaskStatusPill status={status ?? 'todo'} />` (the optimistic coercion).
      (iii) Add a doc comment block above the export naming the fix as `Plan 05-06 item (g) — optimistic Todo render` and noting that real status arrives via chat.taskOwned reconciliation (the `matchedTask?.status` lookup at message-thread.tsx line 504 still drives the real value once available).

    Update test/ui/chat-inline-task-card.test.mjs: add (or replace) an assertion that when InlineTaskCard receives `status: null`, the rendered output contains the literal string `Todo` (or, in source-grep form, that inline-task-card.tsx contains the literal `status ?? 'todo'` snippet at the pill call site AND in the statusLabel computation). Match the file's existing source-grep idiom. If the existing test exercises real React rendering via a jsdom harness, adapt to that style instead.

    Update test/ui/chat-context-rail.test.mjs: replace whatever assertion currently pins `Resume from the agent page` with one that pins the new string `Use ▶ Resume heartbeat below to restart`. Add a second assertion that the OLD string is NOT present (regression guard).
  </action>

  <verify>
    <automated>node --test test/ui/chat-inline-task-card.test.mjs test/ui/chat-context-rail.test.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/surfaces/chat/context-rail.tsx','utf8'); const stripped = c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,''); if (stripped.indexOf('Resume from the agent page') !== -1) { console.error('FAIL: old pause-toast copy still present'); process.exit(1); } if (stripped.indexOf('Use ▶ Resume heartbeat below to restart') === -1) { console.error('FAIL: new pause-toast copy missing'); process.exit(1); } console.log('OK: pause-toast copy updated.');"</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/surfaces/chat/true-task/inline-task-card.tsx','utf8'); const stripped = c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,''); if ((stripped.match(/status \?\? ['\"]todo['\"]/g) || []).length < 1) { console.error('FAIL: optimistic Todo coercion not found'); process.exit(1); } console.log('OK: item (g) optimistic Todo present.');"</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/surfaces/chat/message-thread.tsx','utf8'); const stripped = c.replace(/\/\*[\s\S]*?\*\//g,'').replace(/^\s*\/\/.*$/gm,''); if (!/pendingTaskCard\?\.issueId\s*\]/.test(stripped) && !/pendingTaskCard\.issueId\s*\]/.test(stripped)) { console.error('FAIL: pendingTaskCard.issueId not in scrollIntoView effect deps'); process.exit(1); } console.log('OK: item (c) dep added.');"</automated>
  </verify>

  <acceptance_criteria>
    <source>Plan 04.1-10 SUMMARY mandates the inline Resume affordance (the `▶ Resume heartbeat` button at context-rail.tsx lines 215-224); the toast copy MUST direct the operator at it, not at the agent page (which Plan 04.1-10's drill identified as the wrong target).</source>
    <behavior>The auto-scroll dep extension matches the existing `pendingOverlay.length` precedent (already in the dep array) — the optimistic card's issueId is the stable identity that transitions from null to set when setPendingTaskCard fires.</behavior>
    <behavior>InlineTaskCard's optimistic Todo coercion is scoped to the inline-task-card primitive only; ChatTaskStatusPill's null branch is UNCHANGED so any other call site that genuinely wants the `· — ·` loader still gets it.</behavior>
    <cli>All four automated verifies above MUST pass green.</cli>
    <cli>The existing chat-context-rail.test.mjs and chat-inline-task-card.test.mjs files must remain passing after the assertion updates land.</cli>
  </acceptance_criteria>

  <done>
    The chat thread auto-scrolls when a Create-Task completes (the optimistic card lands in viewport); the pause-toast points operators at the inline ▶ Resume button (not the agent page); the inline task card shows `Todo` immediately instead of `· — ·` while waiting for chat.taskOwned to reconcile; all three changes pass `tsc --noEmit` and the two updated tests.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: LIVE indicator sticky restore (e) + Clarity-pack toast `--you` stripe + ↗ icon (f) + version bump to rc.8</name>
  <files>
    src/ui/styles/chat.css,
    test/ui/chat-css-live-sticky.test.mjs,
    test/ui/chat-toast-stripe.test.mjs,
    package.json,
    src/manifest.ts,
    test/manifest/chat-capabilities.test.mjs
  </files>

  <read_first>
    @src/ui/styles/chat.css (lines 437-540 — `.messages` + `.auto-refresh` sticky declaration)
    @src/ui/styles/chat.css (lines 2087-2120 — `.clarity-toast` block; the target of item (f))
    @src/ui/styles/chat.css (full file — for the LIVE sticky ancestor audit; grep for any ancestor `overflow: hidden`, `transform:`, `filter:`, `contain:` between the chat surface root and `.messages`)
    @src/ui/primitives/clarity-surface-root.tsx (lines 1-50 — the surface root wrapper; confirms NO transform/filter/contain at the root)
    @package.json (line 3 — current version `1.0.0-rc.7`)
    @src/manifest.ts (line 337 — current version literal)
    @test/manifest/chat-capabilities.test.mjs (the version pin for the chat capabilities — gets updated together with the two version sources per memory `plugin-version-bump-two-sources`)
  </read_first>

  <behavior>
    - Item (e): the `.auto-refresh` LIVE indicator MUST stick to the top of the `.messages` scroll container when the operator scrolls down a multi-turn conversation. Current state per project memory `phase-4.2-deferred-from-4.1` §"LIVE indicator floating mid-thread" + STATE.md Plan 04.1-11 side-observation: it floats mid-thread, breaking the sticky context. Root cause is somewhere in the ancestor chain breaking sticky per CSS spec (overflow: hidden, transform, filter, contain, will-change: transform). Audit + fix.
    - Item (f): clarity-pack toast (the bottom-right `.clarity-toast` from Plan 04.1-09) carries a left-edge `--you` gold stripe AND a leading `↗ ` glyph prefix so it is visually unambiguous against Paperclip's host toasts (which sit bottom-LEFT). The `--you` token is already defined globally in the host CSS variables; the `↗` glyph matches the chat-actions-row `↗ Promote` and inline-task-card `↗ TASK CREATED` aesthetic family.
    - Version bump: per project memory `plugin-version-bump-two-sources`, version lives in package.json AND src/manifest.ts. The host reads dist/manifest.js (built from src/manifest.ts), not package.json — bumping only one ships v_new code with a v_old label. Both move together.
    - No new keyframe; no new dependency; no schema change.
  </behavior>

  <action>
    Three changes, ordered for safety:

    Step A — LIVE sticky ancestor audit (item e). In src/ui/styles/chat.css, search the full file for any rule that targets an ancestor of `.messages` (specifically: the chat surface root `[data-clarity-surface="chat"]`, `.chat-shell`, `.thread`, or any wrapper class that appears above `.messages` in JSX) and declares any of: `overflow: hidden`, `overflow-x: hidden`, `overflow-y: hidden`, `transform:`, `filter:`, `backdrop-filter:`, `contain:`, `will-change: transform`. Each of these breaks `position: sticky` per the CSS spec.
    Likely culprit per the rc.7 working tree: the Plan 04.1-10 viewport fix at chat.css introduced `.thread { ..., overflow-x: auto, min-width: 0 }` (line 366 — confirmed during the read_first). However `.thread` is the GRID ITEM container holding the messages column; if `.thread` has `overflow-x: auto` AND `.thread` is an ancestor of `.messages`, then `.thread` becomes the nearest scroll-creating ancestor and `.messages` inside it inherits the new scroll context — but `.auto-refresh` sticks within `.messages`'s scroll container (which still works). However if `.thread`'s overflow-x: auto creates a containing block that also clips top, sticky may misbehave on the cross-axis. The audit MUST trace the actual JSX ancestor chain (read `src/ui/surfaces/chat/index.tsx` to see what wraps `<MessageThread>`) and identify the breaker rule by line number.
    Apply the minimal fix:
      - If the breaker is `overflow-x: auto` on `.thread`, the fix is to either move the overflow-x to a sibling container that does NOT wrap `.messages`, OR scope the overflow-x to `.thread > .topics-strip` (the topic strip is what genuinely needs horizontal scroll), NOT the `.thread` container itself.
      - If the breaker is some other ancestor `overflow: hidden`, change to `overflow: visible` (or `overflow: clip` if hiding content overflow is genuinely needed — `overflow: clip` does NOT break sticky).
      - If no breaker is found in chat.css, the breaker is in the JSX tree's inline style or in clarity-surface-root.tsx — fix at the source.
    Document the audit findings in a code comment block above the fix (`Plan 05-06 item (e) — LIVE sticky restore. Breaker was: <selector> declared <property: value> at line <N>. Fix: <description>.`). The comment MUST name the breaker by selector + property + value so future drills can re-verify.

    Step B — Clarity-pack toast styling (item f). In src/ui/styles/chat.css at the `.clarity-toast` block (line 2087-2099):
      - ADD `border-left: 3px solid var(--you);` to the existing block.
      - ADJUST `padding-left` to compensate for the 3px stripe (existing `padding: ...`; bump left-padding by ~9px so the text indent matches the original visual).
      - ADD a new sibling rule:
        ```
        [data-clarity-surface="chat"] .clarity-toast::before {
          content: '↗ ';
          color: var(--you);
        }
        ```
    Keep all existing properties (z-index 300, animation, box-shadow, background, etc.) UNCHANGED. The result: every clarity-pack toast carries a gold left-edge stripe AND a leading gold `↗` glyph. Visually unambiguous vs host toasts.

    Step C — Version bump (per memory `plugin-version-bump-two-sources`). Both files in the same commit:
      - package.json line 3: `"version": "1.0.0-rc.7"` → `"version": "1.0.0-rc.8"`.
      - src/manifest.ts line 337: `version: '1.0.0-rc.7',` → `version: '1.0.0-rc.8',`.
      - test/manifest/chat-capabilities.test.mjs: update whatever pin expects the rc.7 string to expect rc.8. If the test reads `manifest.version` dynamically from dist/, it may need no change — read the test before editing.

    Step D — Two new contract tests (source-grep idiom matching test/ui/chat-url-params.test.mjs):
      - test/ui/chat-css-live-sticky.test.mjs — asserts: (1) chat.css contains `[data-clarity-surface="chat"] .auto-refresh` with `position: sticky` (already true; regression guard); (2) the breaker rule identified in Step A is REMOVED (or scoped). Encode the exact removed line as a NOT-present assertion — e.g., `assert(stripped.indexOf('.thread { ... overflow-x: auto ... }') === -1)` adapted to the actual fix shape. (3) `[data-clarity-surface="chat"] .messages` still has `overflow-y: auto` (the scroll container that establishes the sticky context).
      - test/ui/chat-toast-stripe.test.mjs — asserts: (1) `[data-clarity-surface="chat"] .clarity-toast` block contains `border-left: 3px solid var(--you)`; (2) `[data-clarity-surface="chat"] .clarity-toast::before` rule exists with `content: '↗ '` and `color: var(--you)`.

    Both tests use the same `readFileSync('src/ui/styles/chat.css', 'utf8')` + comment-stripping idiom as chat-url-params.test.mjs.
  </action>

  <verify>
    <automated>node --test test/ui/chat-css-live-sticky.test.mjs test/ui/chat-toast-stripe.test.mjs test/manifest/chat-capabilities.test.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node scripts/check-css-scope.mjs</automated>
    <automated>node scripts/check-a11y.mjs</automated>
    <automated>node -e "const pkg = require('./package.json'); const c = require('fs').readFileSync('src/manifest.ts','utf8'); if (pkg.version !== '1.0.0-rc.8') { console.error('FAIL: package.json version=' + pkg.version + ', expected 1.0.0-rc.8'); process.exit(1); } if (!/version:\s*['\"]1\.0\.0-rc\.8['\"]/.test(c)) { console.error('FAIL: src/manifest.ts version literal != 1.0.0-rc.8'); process.exit(1); } console.log('OK: dual-source version bump landed.');"</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/styles/chat.css','utf8'); if (!/\[data-clarity-surface=\"chat\"\]\s*\.clarity-toast\s*\{[^}]*border-left:\s*3px\s+solid\s+var\(--you\)/s.test(c)) { console.error('FAIL: .clarity-toast missing --you stripe'); process.exit(1); } if (!/\.clarity-toast::before\s*\{[^}]*content:\s*['\"]↗\s['\"]/s.test(c)) { console.error('FAIL: .clarity-toast::before missing ↗ glyph'); process.exit(1); } console.log('OK: item (f) toast styling landed.');"</automated>
    <automated>node -e "const c = require('fs').readFileSync('src/ui/styles/chat.css','utf8'); if (!/\[data-clarity-surface=\"chat\"\]\s*\.auto-refresh\s*\{[^}]*position:\s*sticky/s.test(c)) { console.error('FAIL: .auto-refresh position:sticky declaration missing or moved'); process.exit(1); } console.log('OK: item (e) sticky declaration preserved.');"</automated>
  </verify>

  <acceptance_criteria>
    <source>CSS spec rules on `position: sticky` context-breakers (the audit must trace the actual ancestor chain in the JSX; the fix must name the breaker by selector + property + value in a code comment so future drills can re-verify).</source>
    <source>Project memory `plugin-version-bump-two-sources` — version lives in BOTH package.json AND src/manifest.ts; bumping only one ships v_new code with a v_old label.</source>
    <behavior>Toast styling uses the existing `--you` gold accent token (already defined in host CSS variables, used by `.flash-highlight` keyframe at chat.css line 2266); no new color token introduced.</behavior>
    <cli>All six automated verifies above MUST pass green. The version-bump verify checks both sources atomically.</cli>
    <cli>check-css-scope and check-a11y both exit 0 (every new selector is rooted under `[data-clarity-surface="chat"]`; the `::before` pseudo-element adds no a11y violation — content text is decorative, not informational, but role-status toast text remains in the toast body).</cli>
  </acceptance_criteria>

  <done>
    The `.auto-refresh` LIVE indicator sticks to the top of `.messages` as the operator scrolls (sticky context restored — the breaker was identified, removed, and documented inline); clarity-pack toasts carry a gold `--you` left-edge stripe AND a leading `↗` glyph; version is bumped to 1.0.0-rc.8 in BOTH package.json and src/manifest.ts; all six gates green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat surface UI → chat.pin worker action | Already established in Plan 04-04; PromoteActions calls usePluginAction('chat.pin'). This plan does NOT introduce a new boundary; only the UI feedback channel changes (showToast vs setFeedback). |
| Right-rail Pinned-chip click → DOM scroll/flash | Pure client-side DOM manipulation. No worker call, no host RPC. Same trust posture as Plan 04.2-04's deep-link scroll-and-flash. |
| Toast text rendering | Pure React text rendering inside `<div className="clarity-toast">`. No dangerouslySetInnerHTML (toast.tsx line 103 is `{toast.message}` — pure text). check-a11y R3 stays green. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | message-thread.tsx PromoteActions.onPin success path | mitigate | Existing wrapActionHandler on chat.pin already enforces OPT_IN_REQUIRED + repo-failure → `{ error: 'PIN_FAILED' }`; the UI change here only swaps `setFeedback` (inline span text) for `showToast` (transient bottom-right notification). Both render as React text only — no innerHTML — so no new XSS surface. The toast text is a fixed literal (`Message pinned` / `Message unpinned`), not operator-controlled. |
| T-05-06-02 | Information Disclosure | Right-rail Pinned-messages chip preview text | mitigate | The chip renders the first 40 chars of `msg.body` (the comment body). The same body is already rendered in full in the message thread, gated by the same plugin opt-in and host viewer-permission check. No new disclosure surface — the chip is a derived view of data already in the operator's chat surface. |
| T-05-06-03 | Denial of Service | LIVE sticky audit changing ancestor overflow | accept | The fix is CSS-only; the worst-case regression is a visual layout glitch caught by check-css-scope.mjs + the new chat-css-live-sticky.test.mjs. Plan 05-04's visual-regression baseline (when it lands) provides a follow-up safety net at PR time. No security surface. |
| T-05-06-04 | Repudiation | None | accept | All Pin/Unpin actions are logged at the worker tier (chat-pin.ts line 75-79 via ctx.logger?.warn? on failure; success is implicit in the row mutation visible via `pnpm paperclipai db query`). Toast is a UI-only confirmation, not the audit trail. |
| T-05-06-05 | Elevation of Privilege | None new | accept | The plan introduces no new worker handlers, no new ctx capability use, no new database tables, no new manifest capabilities. The chat.pin opt-in guard from Plan 04-04 (wrapActionHandler → OPT_IN_REQUIRED for opted-out callers) is preserved. |
| T-05-06-06 | Spoofing | toast.tsx `↗` glyph and `--you` stripe | accept | The visual differentiation from host toasts is a UX safeguard, not a security claim. Host UI can in principle render anything inside the chat surface (same-origin trust model per PLUGIN_SPEC.md §19); the stripe + glyph make plugin toasts visually identifiable to the operator but do NOT constitute a security boundary. |
| T-05-06-SC | Tampering | npm installs | mitigate | This plan introduces ZERO new dependencies. No `npm install` runs. The version bump in package.json only changes the `version` field. Package Legitimacy Gate not applicable. |
</threat_model>

<verification>
**Plan-level gates (all from already-passing CI):**
1. `npx tsc --noEmit` — green.
2. `node scripts/check-css-scope.mjs` — exit 0 (every new selector rooted at `[data-clarity-surface="chat"]`).
3. `node scripts/check-a11y.mjs` — exit 0 (no new `img` without alt; no new form control without label; no new dangerouslySetInnerHTML).
4. `node --test` on the full suite — pass count grows by ~6 new files + a few asserts in updated files; 0 fail; 2 pre-existing skip preserved.
5. `node scripts/coexistence-checks/run-all.mjs` — 10/10 PASS (CTT-07 check via 09-true-task.mjs covers the no-host-mutation guarantee for chat-pin.ts).

**Plan-specific spot-checks (encoded as automated grep gates per task):**
- D-11: `setFeedback({ kind: 'ok', text: '⚑ Pinned' })` count === 0 (success path); `showToast({ message: 'Message (un)?pinned' })` count >= 1.
- D-12: `@keyframes clarity-flash` count === 1 (no duplicate); context-rail.tsx contains the literal `flash-highlight` + `scrollIntoView` + `setTimeout` sequence.
- Item (c): `pendingTaskCard?.issueId` or `pendingTaskCard.issueId` appears in the message-thread.tsx scrollIntoView effect dep array.
- Item (d): old `Resume from the agent page` string === 0 occurrences; new `Use ▶ Resume heartbeat below to restart` string >= 1.
- Item (e): `.auto-refresh { position: sticky` declaration preserved; the named breaker rule (identified during the audit) is removed or scoped.
- Item (f): `.clarity-toast { ... border-left: 3px solid var(--you) ... }` regex matches; `.clarity-toast::before { content: '↗ ' ... }` regex matches.
- Item (g): inline-task-card.tsx contains `status ?? 'todo'` at least once.
- Version: package.json.version === '1.0.0-rc.8' AND src/manifest.ts contains `version: '1.0.0-rc.8'`.

**Out of scope for THIS plan (handled by Plan 05-10):**
- Tarball pack + sha256 capture.
- Countermoves operator drill.
- VERIFICATION.md write.
- ROADMAP / REQUIREMENTS final flip.
</verification>

<success_criteria>
Plan 05-06 is complete when, in a single working tree:

1. Pin/Unpin in the chat message-thread (a) fires the clarity-pack toast (`Message pinned` / `Message unpinned`) with NO inline `⚑ Pinned` success-text on the bubble — the silent-toggle invariant (D-11) holds and is regression-guarded by test/ui/chat-pin-toggle-toast.test.mjs.

2. Right-rail Pinned-messages chip (b) is mounted in context-rail.tsx (above the existing `<h3>Storage pin</h3>` block); clicking a chip row scrolls to the source bubble and flashes for 1500ms using the EXISTING `.flash-highlight` keyframe at chat.css 2253-2281 — D-12 verbatim. No duplicate keyframe is introduced.

3. The auto-scroll-after-create-task fix (c) extends the existing message-thread.tsx scrollIntoView effect dep array to include `pendingTaskCard?.issueId`, so a non-null transition (the optimistic card mount) fires the scroll without any new prop or imperative handle.

4. The pause-toast copy fix (d) replaces the old `Resume from the agent page` literal with `Use ▶ Resume heartbeat below to restart` in context-rail.tsx line 99, matching Plan 04.1-10's inline Resume affordance which lives in the same component.

5. The LIVE indicator sticky restore (e) traces the actual JSX/CSS ancestor chain from `.messages` upward, identifies the breaker rule (most likely `overflow-x: auto` on `.thread` per the audit), removes or scopes it, and documents the finding in a code comment naming the breaker selector + property + value.

6. Clarity-pack toasts (f) carry a gold `--you` left-edge stripe AND a leading `↗ ` glyph via additions to `.clarity-toast` and a new `.clarity-toast::before` rule in chat.css — disambiguates them visually from Paperclip host toasts (which sit bottom-LEFT).

7. Inline task cards (g) render `Todo` immediately instead of the muted `· — ·` loader while waiting for chat.taskOwned to reconcile — the optimistic coercion `status ?? 'todo'` is scoped to InlineTaskCard's render path only.

8. Version bumps to `1.0.0-rc.8` in BOTH `package.json` and `src/manifest.ts` (per memory `plugin-version-bump-two-sources`); `test/manifest/chat-capabilities.test.mjs` updated.

9. All gates pass: `tsc --noEmit`, `check-css-scope`, `check-a11y`, `node --test` on the full suite, and `coexistence-checks/run-all.mjs` 10/10 PASS.

10. CTT-07 invariant preserved: `grep -nE "ctx\\.issues\\.update\\(" src/worker/handlers/chat-pin.ts | wc -l == 0` (no host issue mutation introduced or regressed).

11. No new migration (coexistence guarantee #3 holds trivially); no new dependency; no new worker handler.

12. SUMMARY drafted at `.planning/phases/05-distribution-polish/05-06-SUMMARY.md` capturing: which item maps to which commit(s), the audited breaker rule from item (e), any deviations recorded, and the final test suite delta.
</success_criteria>

<output>
Create `.planning/phases/05-distribution-polish/05-06-SUMMARY.md` when done.

The SUMMARY MUST include:
- Per-item commit lineage (a → commit X, b → commit Y, etc.).
- The exact CSS rule identified as the LIVE sticky breaker in item (e) — selector + property + value + line number — so future drills can verify the audit without re-running it.
- Suite delta: before/after pass counts; new test file names; any test updates.
- Version trail: `1.0.0-rc.7` → `1.0.0-rc.8`.
- Deviations recorded under the standard Rules 1/2/3 taxonomy.
- Carry-forward note: tarball pack + Countermoves drill DEFERRED to Plan 05-10 (the v1.0.0 final closure plan); 05-06 is code-complete-pending-drill on landing.
- Confirmation that CTT-07 invariant (chat-pin.ts contains zero `ctx.issues.update` call sites) holds.
- Confirmation that the chat.pin worker handler was NOT modified by this plan (this is a pure UI-tier polish bundle).
</output>
