---
phase: 05-distribution-polish
plan: 05-07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/handlers/chat-open-for-issue.ts
  - src/ui/surfaces/reader/continue-in-chat-button.tsx
  - src/ui/surfaces/reader/reverse-topics-link.tsx
  - src/ui/surfaces/chat/index.tsx
  - src/ui/surfaces/chat/topic-strip.tsx
  - src/ui/surfaces/chat/context-rail.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - src/ui/surfaces/chat/true-task/true-task-dialog.tsx
  - src/ui/surfaces/reader/ref-card.tsx
  - src/ui/styles/chat.css
  - test/worker/handlers/chat-open-for-issue-d7.test.mjs
  - test/ui/continue-in-chat-button-ambiguous.test.mjs
  - test/ui/reverse-topics-link-entry-point.test.mjs
  - test/ui/topic-strip-d10.test.mjs
  - test/ui/no-react-key-warnings.test.mjs
  - test/ui/chat-open-for-issue-d3-cross-employee.test.mjs
  - test/manifest/chat-capabilities.test.mjs
  - package.json
  - src/manifest.ts
autonomous: true
requirements: []
tags: [chat, deep-link, react-keys, d8-hygiene, rcb-05, d-03]

must_haves:
  truths:
    - "Reader Continue-in-chat tooltip on the originId-lineage path (GAP-D8-LINEAGE-TOOLTIP) renders CHT-NN identifier, not raw UUID"
    - "Reader Continue-in-chat tooltip on the reverse-lookup path (GAP-D8-REVERSE-TOOLTIP-FALLBACK) renders BEAAA-NNN identifier, not 'this issue' fallback"
    - "About COU-NNNN ↗ backlink chip in topic-strip renders as a rectangular chip (GAP-RCB-05-CHIP-STYLING fixed)"
    - "Browser-Back after a Reader→Chat deep-link consume returns to the previous Paperclip page with chat-state intact (D-13)"
    - "Forward after Back returns to chat surface with #h= hash intact and chat-state restored (D-13)"
    - "All 5 React-key warnings flagged by the operator drill (ContextRail / PersistedMessage / TrueTaskDialog / AnchoredToCards / ChatPageBody) are eliminated from console output"
    - "Cross-employee fall-through drill fixture proves N=0-same-assignee + N>=1-other-employee resolves to new-topic-needed (D-03)"
    - "No regression on RCB-01..RCB-07 closure-baseline (5 fixtures from 04.2-VERIFICATION.md)"
    - "CTT-07 invariant preserved: no plugin action calls ctx.issues.update on the chat-open-for-issue path"
    - "URL_HASH carrier contract (Plan 04.2-03) preserved — buildTopicDeepLink and parseChatDeepLink signatures unchanged"
  artifacts:
    - path: "src/worker/handlers/chat-open-for-issue.ts"
      provides: "topicIdentifier emission on existing-topic (both lineage and reverse-lookup branches); sourceIssueIdentifier emission on existing-topic reverse-lookup branch"
      contains: "topicIdentifier"
    - path: "src/ui/surfaces/reader/continue-in-chat-button.tsx"
      provides: "Tooltip consumes result.topicIdentifier when present (no UUID leak) and result.sourceIssueIdentifier for the reverse-lookup branch"
      contains: "topicIdentifier"
    - path: "src/ui/surfaces/chat/topic-strip.tsx"
      provides: "About-issue backlink chip with rectangular box-model"
    - path: "src/ui/styles/chat.css"
      provides: ".topic-about-chip rectangular border-radius + content alignment per RCB-05"
    - path: "src/ui/surfaces/chat/index.tsx"
      provides: "Deep-link consume effect no longer calls nav.navigate(pathname, { replace: true }) — URL_HASH preserved post-consume (D-13)"
    - path: "test/ui/chat-open-for-issue-d3-cross-employee.test.mjs"
      provides: "D-03 cross-employee fall-through drill fixture"
    - path: "package.json"
      provides: "version bumped to 1.0.0-rc.8 (rc.7 -> rc.8)"
    - path: "src/manifest.ts"
      provides: "manifest version literal bumped to 1.0.0-rc.8 (per plugin-version-bump-two-sources)"
  key_links:
    - from: "src/worker/handlers/chat-open-for-issue.ts"
      to: "src/ui/surfaces/reader/continue-in-chat-button.tsx"
      via: "ChatOpenForIssueResult.topicIdentifier + sourceIssueIdentifier payload fields"
      pattern: "topicIdentifier\\??:\\s*string"
    - from: "src/ui/surfaces/chat/index.tsx"
      to: "URL fragment carrier"
      via: "deep-link consume effect that REMOVES nav.navigate(pathname, { replace: true })"
      pattern: "parseChatDeepLink"
    - from: "src/ui/surfaces/chat/topic-strip.tsx"
      to: "src/ui/styles/chat.css"
      via: ".topic-about-chip selector"
      pattern: "topic-about-chip"
---

<objective>
Phase 4.2 polish bundle — closes three forward defects from the 1.0.0-rc.7 drill (GAP-D8-LINEAGE-TOOLTIP, GAP-D8-REVERSE-TOOLTIP-FALLBACK, GAP-RCB-05-CHIP-STYLING), fixes the D-13 Browser-Back behaviour so chat-state survives Back, eliminates the 5 React-key warnings flagged by the operator drill (ContextRail / PersistedMessage / TrueTaskDialog / AnchoredToCards / ChatPageBody), and adds the missing D-03 cross-employee fall-through drill fixture. Bumps the plugin version to 1.0.0-rc.8.

Purpose: Close the rc.7 forward defects and the React-key console noise inside v1.0.0 final — these polish items are operator-visible (tooltip UUID leakage, oval-instead-of-rectangle chip, Back-button drops chat-state, console warnings on every chat-surface mount) and were routed to this plan by the rc.7 drill rather than deferred to v1.1.

Output: Updated `chat-open-for-issue.ts` worker handler (additive payload fields only), updated Reader Continue button + reverse-topics-link tooltip consumption, RCB-05 chip CSS fix, removal of the `nav.replace`-on-consume side-effect in chat surface, React-key fixes across 5 chat-surface components, new D-03 cross-employee fall-through test fixture, version bump 1.0.0-rc.7 → 1.0.0-rc.8 in both `package.json` AND `src/manifest.ts` (per plugin-version-bump-two-sources memory).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-distribution-polish/05-CONTEXT.md
@.planning/phases/05-distribution-polish/05-PATTERNS.md
@.planning/phases/04.2-reader-chat-bridge/04.2-07-SUMMARY.md
@.planning/phases/04.2-reader-chat-bridge/04.2-CONTEXT.md
@.planning/phases/04.2-reader-chat-bridge/04.2-06-SUMMARY.md
@.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from the codebase. -->

From src/worker/handlers/chat-open-for-issue.ts (lines 60-101):
```typescript
export type ChatOpenForIssueCandidate = {
  topicIssueId: string;
  topicId: string;       // CHT-NN — operator-visible
  title: string;
  lastActivityAt: string;
  archived: boolean;
};

export type ChatOpenForIssueResult = {
  kind: 'chatOpenForIssue';
  route:
    | 'existing-topic'
    | 'existing-topics-ambiguous'
    | 'new-topic-needed'
    | 'topic-itself';
  topicIssueId?: string;
  sourceCommentId?: string;
  assigneeAgentId?: string;
  assigneeName?: string | null;
  candidates?: ChatOpenForIssueCandidate[];
  sourceIssueIdentifier?: string;   // present on 'existing-topics-ambiguous' today
  // TODO this plan adds: topicIdentifier?: string;   // CHT-NN of the resolved topic, both lineage + reverse-lookup branches
  // TODO this plan extends sourceIssueIdentifier emission to the reverse-lookup branch as well as the ambiguous branch
  seedTitle?: string;
  seedBody?: string;
  error?: string;
};

// Step 6 lineage branch (lines 217-227): chat-task originId match
// Step 6b reverse-lookup branch (lines 276-316): N=1 same-assignee match
// Step 6b ambiguous branch (lines 318-339): N>=2 same-assignee matches (already emits sourceIssueIdentifier)
// Step 7 cold/new-topic-needed (lines 341-350)
```

From src/ui/surfaces/reader/continue-in-chat-button.tsx (lines 256-269):
```typescript
const tooltip =
  result.route === 'existing-topic' && result.topicIssueId && result.sourceCommentId
    ? `Open source topic ${result.topicIssueId} →`                                 // <-- UUID leak (GAP-D8-LINEAGE-TOOLTIP)
    : result.route === 'existing-topic'
      ? `Resume conversation about ${result.sourceIssueIdentifier ?? 'this issue'} →`  // <-- 'this issue' fallback (GAP-D8-REVERSE-TOOLTIP-FALLBACK)
      : `Continue in chat with ${employeeLabel} →`;
```

From src/ui/surfaces/chat/index.tsx (line 441):
```typescript
// In the deep-link consume effect (lines 338-442):
nav.navigate(pathname, { replace: true });  // <-- REMOVE for D-13: drops URL_HASH and chat-state on Back
```

From src/ui/surfaces/chat/topic-strip.tsx (lines 229-254):
```typescript
{aboutIssueId && !aboutChipIsDismissed ? (
  <span className="topic-about-chip" data-clarity-region="about-issue-chip">
    <button type="button" className="topic-about-chip-link" ...>
      About {aboutIssueId} ↗
    </button>
    <button type="button" className="topic-about-chip-dismiss" ...>×</button>
  </span>
) : null}
```

From src/ui/styles/chat.css (lines 2349-2382):
```css
[data-clarity-surface="chat"] .topic-about-chip {
  display: inline-flex;
  align-items: center;
  gap: 2px;
  margin-right: 8px;
  border: 1px solid var(--you);
  border-radius: 999px;   /* <-- pill — wraps to circle/oval on long content (GAP-RCB-05) */
  background: var(--you-soft);
}
[data-clarity-surface="chat"] .topic-about-chip-link {
  padding: 2px 4px 2px 9px;     /* asymmetric padding for the pill curve */
  ...
}
[data-clarity-surface="chat"] .topic-about-chip-dismiss {
  padding: 2px 7px 2px 2px;     /* asymmetric padding for the pill curve */
  ...
}
```

From src/ui/surfaces/chat/index.tsx (lines 82-145):
```typescript
export function ChatPage(_props?: PluginPageProps): React.ReactElement { ... }    // outer opt-in gate
function ChatPageOptedIn(): React.ReactElement { ... }                              // companyId/userId resolve
function ChatPageBody({ companyId, userId }: { companyId: string; userId: string; }): React.ReactElement {
  // Plan 04.1-10 drill fix #1 — useToast must be called INSIDE the
  // <ToastProvider> tree; ChatPageBody is the first descendant guaranteed wrapped.
  ...
}
```

From src/ui/surfaces/chat/topic-strip.tsx (line 261, the "good" stable-key pattern):
```typescript
visible.map((topic) => {
  const isArchived = topic.archived;
  return (
    <button type="button" key={topic.issueId} ...>
```

From src/ui/surfaces/reader/reverse-topics-link.tsx (line 137, the "good" stable-key pattern):
```typescript
visibleTopics.map((t) => (
  <button key={t.topicIssueId} ...>
```

From src/ui/surfaces/chat/message-thread.tsx (line 946 + 954, composite stable keys):
```typescript
sections!.map((section, i) => (
  <div key={`section-${i}-${section.title ?? ''}`} ...>
    {(section.rows ?? []).map((row, j) => (
      <RuntimeNoiseStructuredRow key={`row-${i}-${j}-${(row as SectionRow).type ?? ''}`} row={row as SectionRow} />
    ))}
  </div>
))
```

From package.json (lines 2-3):
```json
"name": "clarity-pack",
"version": "1.0.0-rc.7",
```

From src/manifest.ts (line 337):
```typescript
version: '1.0.0-rc.7',
```

From test/ui/no-react-key-warnings.test.mjs (FILES array, lines 21-37):
```javascript
const FILES = [
  'src/ui/components/enable-clarity-cta.tsx',
  'src/ui/surfaces/situation-room/index.tsx',
  'src/ui/surfaces/situation-room/critical-path-strip.tsx',
  'src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx',
  // GAP 7 (Plan 04-05 round 3) — extended for chat surface
  'src/ui/surfaces/chat/index.tsx',
  'src/ui/surfaces/chat/roster-rail.tsx',
  'src/ui/surfaces/chat/topic-strip.tsx',
  'src/ui/surfaces/chat/context-rail.tsx',
  'src/ui/surfaces/chat/composer.tsx',
  'src/ui/surfaces/chat/message-thread.tsx',
];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D8 forward defects — worker `topicIdentifier` + `sourceIssueIdentifier` reverse-lookup, UI tooltip consumption, D-03 cross-employee fixture</name>
  <files>src/worker/handlers/chat-open-for-issue.ts, src/ui/surfaces/reader/continue-in-chat-button.tsx, test/worker/handlers/chat-open-for-issue-d7.test.mjs, test/ui/continue-in-chat-button-ambiguous.test.mjs, test/ui/chat-open-for-issue-d3-cross-employee.test.mjs</files>

  <read_first>
    @src/worker/handlers/chat-open-for-issue.ts (full file — particularly the step-6 chat-task lineage branch lines 216-227, step-6b reverse-lookup branch lines 229-316, step-6b ambiguous branch lines 318-339)
    @src/ui/surfaces/reader/continue-in-chat-button.tsx (lines 226-294 — tooltip dispatch arms + ambiguous-route branch)
    @test/worker/handlers/chat-open-for-issue-d7.test.mjs (existing D-7 routing tests — extend, do not replace)
    @test/ui/continue-in-chat-button-ambiguous.test.mjs (existing ambiguous-route UI tests — extend, do not replace)
    @.planning/phases/04.2-reader-chat-bridge/04.2-07-SUMMARY.md "Forward defects filed to Plan 04.2-08" section (gaps 1, 2, 4 — exact wording of defects)
    @.planning/phases/04.2-reader-chat-bridge/04.2-06-SUMMARY.md (D9/D10 hygiene precedent — `ctx.issues.get` server-side identifier resolution, NEVER UUID fallback)
  </read_first>

  <behavior>
    - Worker handler `chat.openForIssue` payload SHAPE changes (additive only):
      - Test 1A: Step-6 chat-task lineage branch result includes `topicIdentifier` (CHT-NN string) when the resolved topic-issue has an identifier. Resolution path: AFTER the `CHAT_TASK_RE` match, call `ctx.issues.get(topicIssueId, companyId)` and read `.identifier`. Best-effort: on throw / missing identifier, leave `topicIdentifier` undefined (NEVER fall back to UUID — D-08 hygiene, NO_UUID_LEAK cross-cutting truth). On any failure, warn-log per the existing pattern at lines 156-160 (`ctx.logger?.warn?.('chat.openForIssue: ctx.issues.get for topic identifier threw', { topicIssueId, ... })`).
      - Test 1B: Step-6b reverse-lookup branch (N=1 same-assignee, lines 276-316) ALSO includes `topicIdentifier` (CHT-NN). `match.topicId` is already CHT-NN per `ChatTopicByOriginEntry`; thread it directly as `topicIdentifier: match.topicId` — no second `ctx.issues.get` call needed.
      - Test 1C: Step-6b reverse-lookup branch (N=1) ALSO includes `sourceIssueIdentifier: identifier` (BEAAA-NNN — the input issue's identifier, already resolved at line 174-177). Today `sourceIssueIdentifier` only ships on the `existing-topics-ambiguous` branch.
      - Test 1D: Step-6 lineage branch already has `topicIssueId` + `sourceCommentId` — leaving those unchanged; `topicIdentifier` is purely additive.
      - Test 1E: Reverse-lookup auto-unarchive path STILL never calls `ctx.issues.update` (CTT-07 spy test — existing in `chat-open-for-issue-d7.test.mjs` per 04.2-07; add a CTT-07 invariant assertion on the new `topicIdentifier` resolution path to prove `ctx.issues.get` reads are non-mutating).
      - Test 1F (D-03 cross-employee fall-through fixture — NEW separate file `test/ui/chat-open-for-issue-d3-cross-employee.test.mjs`): given two distinct employees (employee-A and employee-B) and an issue assigned to employee-A with N=2 chat_topics rows that ALL have `employee_agent_id = employee-B`, `chat.openForIssue` for that issue with `assigneeAgentId = employee-A` returns `route: 'new-topic-needed'` (NOT `existing-topics-ambiguous`, NOT `existing-topic`). This proves the `listTopicsForIssueAndAssignee` employee filter scopes correctly. Mock the repo helper to return rows with mismatched `employee_agent_id` and assert the route. Per CONTEXT.md, this lives at `test/ui/chat-open-for-issue-d3-cross-employee.test.mjs` (planner picks per "either test/integration/ or as a seedable Countermoves drill fixture"; we pick the worker-shape unit test file location to match existing `chat-open-for-issue-d7.test.mjs` neighbourhood).
    - UI consumption — `src/ui/surfaces/reader/continue-in-chat-button.tsx`:
      - Test 2A: Type augmentation — `ChatOpenForIssueResult` adds optional `topicIdentifier?: string` (mirrors the worker shape).
      - Test 2B: Tooltip for the chat-task lineage branch (today `Open source topic ${result.topicIssueId} →` at line 266) becomes `Open source topic ${result.topicIdentifier ?? result.topicIssueId} →` — CHT-NN when available, raw topicIssueId ONLY as last-resort degrade (still preferable to literal string "this topic"). For the rc.7+ payload `result.topicIdentifier` will be present; the `??` fallback covers cached pre-bump payloads only.
      - Test 2C: Tooltip for the reverse-lookup branch (today `Resume conversation about ${result.sourceIssueIdentifier ?? 'this issue'} →` at line 268) — once the worker emits `sourceIssueIdentifier` on this branch, the fallback `'this issue'` is functionally unreachable on the rc.8+ path. Add a source-grep assertion that the tooltip expression EXPLICITLY uses `result.sourceIssueIdentifier ??` so the fallback contract stays intact for pre-bump cached payloads.
      - Test 2D: NO_UUID_LEAK source-grep assertion — `continue-in-chat-button.tsx` MUST NOT render `result.topicIssueId` raw in any tooltip OR button label EXCEPT inside the `?? result.topicIssueId` last-resort degrade. Verify with `grep -n 'topicIssueId' src/ui/surfaces/reader/continue-in-chat-button.tsx` returns at most ONE occurrence inside the tooltip expression (the degrade fallback). Existing data-attribute uses (e.g., `result.assigneeAgentId`) are unaffected — focus is on operator-visible text only.
    - Reverse-topics tooltip already PASSED D-08 hygiene on the ambiguous path per 04.2-07 (`Pick from N conversations about COU-2441 →`); no UI change needed in `reverse-topics-link.tsx` for Task 1 — its tooltip still reads from `result.sourceIssueIdentifier` which now ships on BOTH branches.
  </behavior>

  <action>
    Worker handler `chat-open-for-issue.ts` — three additive changes inside the existing 6-case route switch:

    1. Type extension on `ChatOpenForIssueResult` (lines 72-101) — add optional `topicIdentifier?: string` doc-comment block mirroring the existing `assigneeName?: string | null` D9-precedent shape. The comment cites GAP-D8-LINEAGE-TOOLTIP / GAP-D8-REVERSE-TOOLTIP-FALLBACK and references the 04.2-07-SUMMARY entries.

    2. Step-6 chat-task lineage branch (lines 217-227) — BEFORE the return, look up `topicIdentifier` via `ctx.issues.get(chatTaskMatch[1], companyId)`. Wrap in try/catch mirroring lines 153-162 / 200-214 (`ctx.logger?.warn?.('chat.openForIssue: ctx.issues.get for topic identifier threw', { topicIssueId: chatTaskMatch[1], companyId, err: ... })`). On success read `(topicIssue as { identifier?: string }).identifier` only if it is a non-empty string; otherwise leave undefined. Thread the resolved identifier into the return as `topicIdentifier`. Keep all existing fields (`topicIssueId`, `sourceCommentId`, `assigneeAgentId`, `assigneeName`).

    3. Step-6b reverse-lookup branch N=1 (lines 308-316) — extend the return to include `topicIdentifier: match.topicId` AND `sourceIssueIdentifier: identifier` (the BEAAA-NNN already resolved at line 174-177 from `issue.identifier`). `match.topicId` is CHT-NN per `ChatTopicByOriginEntry` from `chat-topics-repo.ts` listTopicsForIssueAndAssignee — already a server-resolved string, no extra round-trip.

    Existing step-6b ambiguous branch (lines 318-339) leaves `sourceIssueIdentifier` intact; topicIdentifier is irrelevant there (the route returns `candidates[]` instead of a single topic).

    UI `continue-in-chat-button.tsx`:

    1. Update local `ChatOpenForIssueResult` type (lines 79-105) — add `topicIdentifier?: string` after `topicIssueId?: string`.

    2. Rewrite the tooltip expression (lines 264-269):
       - For `result.route === 'existing-topic' && result.topicIssueId && result.sourceCommentId` (chat-task lineage), use `result.topicIdentifier ?? result.topicIssueId` inside the template literal — CHT-NN when present, raw UUID only as the cached-payload degrade.
       - For `result.route === 'existing-topic'` (reverse-lookup, no sourceCommentId), continue using `result.sourceIssueIdentifier ?? 'this issue'` — the `??` chain is preserved; the worker now ships `sourceIssueIdentifier` on this branch so the fallback string is unreachable on rc.8+ payloads but kept for cached-payload graceful-degrade.

    3. Header comment block — add a brief reference to the D8 hygiene closure citing Plan 05-07 + the two GAP- IDs, mirroring the D9-style precedent comments at lines 22-28.

    Tests:

    - Extend `test/worker/handlers/chat-open-for-issue-d7.test.mjs` (the existing D-7 test file at `test/worker/handlers/` per `find test -name "*chat-open-for-issue*"` — DO NOT create a parallel file at `test/worker/chat/`):
       - New test `chat.openForIssue lineage branch ships topicIdentifier resolved via ctx.issues.get`. Mock `ctx.issues.get` to return `{ identifier: 'CHT-1117' }` for the lineage `topicIssueId`. Assert `result.topicIdentifier === 'CHT-1117'`.
       - New test `chat.openForIssue lineage branch ships topicIdentifier undefined when ctx.issues.get throws` (warn-log call recorded, no throw bubbled up).
       - New test `chat.openForIssue lineage branch ships topicIdentifier undefined when target issue has no identifier`.
       - New test `chat.openForIssue reverse-lookup N=1 branch ships topicIdentifier from match.topicId`.
       - New test `chat.openForIssue reverse-lookup N=1 branch ships sourceIssueIdentifier from source issue identifier`.
       - CTT-07 anti-regression: existing spy on `ctx.issues.update` stays at zero across the new identifier-resolution code path.

    - Extend `test/ui/continue-in-chat-button-ambiguous.test.mjs` (per CONTEXT.md naming neighbourhood; either extend this OR add a sibling `continue-in-chat-button-d8.test.mjs` — planner picks the sibling-file approach to keep blast radius minimal and let the ambiguous-route file stay focused on its existing scenarios). New file: `test/ui/continue-in-chat-button-d8.test.mjs` with source-grep / source-text assertions:
       - The tooltip expression for the chat-task lineage branch reads `result.topicIdentifier ?? result.topicIssueId` (verifiable via fixed-string grep).
       - The tooltip expression for the reverse-lookup branch still reads `result.sourceIssueIdentifier ?? 'this issue'` (the fallback is preserved for graceful degrade of cached payloads).
       - NO_UUID_LEAK invariant: `topicIssueId` appears in this file's source code with a count of exactly the number of allowed degrade-fallback references — pin by `grep -nE` and an explicit allowed-count assertion (current allowed count after this change = 2: the existing data-attribute `data-clarity-route` adjacent reference + the new degrade fallback inside the tooltip expression; planner verifies the exact count by reading the post-edit source and pins it literally in the test).

    - New file `test/ui/chat-open-for-issue-d3-cross-employee.test.mjs` (D-03 fixture; planner picks this location over the suggested `test/integration/` because it stays inside the existing `node --test` glob and reuses the existing worker test helpers):
       - 1 test only — `chat.openForIssue with N>=2 OTHER-employee reverse matches falls through to new-topic-needed (D-03)`.
       - Mock `listTopicsForIssueAndAssignee` (the repo helper at `src/worker/db/chat-topics-repo.ts`) to return `[]` (which is the correct behaviour: the helper's WHERE clause filters by `employee_agent_id` at the SQL layer, so cross-employee topics never reach the handler — Plan 04.2-07-SUMMARY documents this as "D-03 fall-through is implicit"). Then assert the handler returns `route: 'new-topic-needed'`.
       - Add a SECOND assertion that DOCUMENTS this implicit behaviour: a comment block at the top of the test pinning the lesson per the existing rc.7 SUMMARY language.
       - This satisfies the D-03 drill-fixture requirement from CONTEXT.md without bloating worker-side test surface.

    Constraints:
    - NO `dangerouslySetInnerHTML` introduced anywhere (check-a11y R3 stays green).
    - All new payload fields are OPTIONAL (additive contract — pre-rc.8 cached payloads stay forward-compatible).
    - The new `ctx.issues.get` call in step-6 chat-task lineage adds ONE host RPC per Continue-button mount on chat-spawned issues; budget-conformant (server-side cache by host).
  </action>

  <verify>
    <automated>node --test test/worker/handlers/chat-open-for-issue-d7.test.mjs test/ui/continue-in-chat-button-d8.test.mjs test/ui/chat-open-for-issue-d3-cross-employee.test.mjs test/ui/continue-in-chat-button-ambiguous.test.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>grep -cE "topicIdentifier" src/worker/handlers/chat-open-for-issue.ts</automated>
    <automated>grep -cE "topicIdentifier \?\? result\.topicIssueId" src/ui/surfaces/reader/continue-in-chat-button.tsx</automated>
  </verify>

  <done>
    - `chat-open-for-issue.ts` ships `topicIdentifier` on BOTH chat-task lineage AND reverse-lookup N=1 branches; `sourceIssueIdentifier` ships on reverse-lookup N=1 (was: ambiguous-only).
    - Reader Continue button tooltip on path 1 (chat-task lineage) renders `Open source topic CHT-NNNN →` — no UUID.
    - Reader Continue button tooltip on path 6 (reverse-lookup) renders `Resume conversation about BEAAA-NNNN →` — no `this issue` fallback on rc.8+ payloads.
    - D-03 cross-employee fall-through fixture asserts the route degrades to `new-topic-needed` when `listTopicsForIssueAndAssignee` returns `[]` (the SQL employee filter scopes correctly).
    - CTT-07 spy test still green; `ctx.issues.update` zero calls.
    - `tsc --noEmit` clean; all new + existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-13 nav.replace removal, GAP-RCB-05 chip rectangular box-model, React-key audit fixes (5 components — one commit per component)</name>
  <files>src/ui/surfaces/chat/index.tsx, src/ui/styles/chat.css, src/ui/surfaces/chat/topic-strip.tsx, src/ui/surfaces/chat/context-rail.tsx, src/ui/surfaces/chat/message-thread.tsx, src/ui/surfaces/chat/true-task/true-task-dialog.tsx, src/ui/surfaces/reader/ref-card.tsx, test/ui/no-react-key-warnings.test.mjs, test/ui/topic-strip-d10.test.mjs, test/ui/chat-deeplink-back-preserves-hash.test.mjs</files>

  <read_first>
    @src/ui/surfaces/chat/index.tsx lines 290-450 — the deep-link consume effect; the `nav.navigate(pathname, { replace: true })` at line 441 is the D-13 target
    @src/ui/styles/chat.css lines 2345-2382 — the `.topic-about-chip` rule block (current `border-radius: 999px` pill shape causing RCB-05)
    @src/ui/surfaces/chat/topic-strip.tsx lines 225-260 — the about-chip JSX (operator-visible text + the wrapping `<span class="topic-about-chip">`)
    @src/ui/surfaces/chat/context-rail.tsx (full file — 252 lines; no current `.map()` returning JSX, but the operator drill 2026-05-25 still flagged React-key warnings attributed to this component; verify the attribution maps elsewhere)
    @src/ui/surfaces/chat/message-thread.tsx lines 456-590 (the main `ordered.map(...)` loop with three return arms — Fragment keys verified at lines 492, 525, 537) and lines 930-967 (RuntimeNoiseStructuredRow nested map with composite keys at 946+954)
    @src/ui/surfaces/chat/true-task/true-task-dialog.tsx lines 305-355 (roster + topics select-option .map()s at 327 + 346 — both already keyed)
    @src/ui/surfaces/reader/ref-card.tsx (full file — 72 lines; AnchoredToCards map at line 44 already keyed on c.id)
    @test/ui/no-react-key-warnings.test.mjs (the source-grep static analyser; extend FILES array if any new file lands in scope)
    @.planning/phases/04.2-reader-chat-bridge/04.2-07-SUMMARY.md "Console (DevTools)" section — the live React-key warning trail attributing to ToastProvider / ChatPageBody / RosterRail / ChatActionsRow / ContextRail / PersistedMessage / TrueTaskDialog / AnchoredToCards (component instance attribution; warnings may originate in a CHILD render path)
    @src/ui/primitives/toast.tsx (the ToastProvider) — line 72-73 `toasts.map((t) => <ChatToast key={t.id} ...>)` — already keyed
    @src/ui/surfaces/chat/roster-rail.tsx line 126-129 — already keyed on `emp.id`
    @src/ui/surfaces/chat/active-tasks-owned.tsx (mounted in ContextRail) line 111-112 — already keyed on `t.issueId`
    @src/ui/surfaces/chat/archive-panel.tsx line 161 — verify keying
  </read_first>

  <behavior>
    - **D-13 (browser-back preserves hash + chat-state):**
       - Test 3A: `src/ui/surfaces/chat/index.tsx` source NO LONGER contains the line `nav.navigate(pathname, { replace: true });` inside the deep-link consume effect. Verify via source-grep: `grep -cE "nav\.navigate\(pathname,\s*\{\s*replace:\s*true\s*\}\)" src/ui/surfaces/chat/index.tsx === 0`.
       - Test 3B: The deep-link consume effect's `consumedDeepLinkRef` guard is PRESERVED (it prevents the effect from re-firing within the same SPA session even if the hash is still present in the URL). Source-grep: `grep -cE "consumedDeepLinkRef" src/ui/surfaces/chat/index.tsx >= 2` (one declaration + at least one use).
       - Test 3C (new file `test/ui/chat-deeplink-back-preserves-hash.test.mjs`): source-grep assertion proving the `nav.navigate(pathname, { replace: true })` call has been replaced with an explanatory comment (or just removed cleanly). The test reads `src/ui/surfaces/chat/index.tsx` and asserts:
         - 0 occurrences of `nav.navigate(pathname, { replace: true })`.
         - At least 1 occurrence of an explanatory comment mentioning Plan 05-07 / D-13 / Browser-Back near the former call site (search for the literal string `D-13` within the file).
       - Test 3D: The URL_HASH carrier shape (Plan 04.2-03 lock) is unchanged. `buildTopicDeepLink` + `parseChatDeepLink` are NOT touched in this plan; source-grep on `src/ui/surfaces/chat/deep-link.mjs` MUST diff cleanly against `git show HEAD:src/ui/surfaces/chat/deep-link.mjs` (planner can't run git in test, so instead: assert the file has the same line count as before — pinning the byte-shape contract).
    - **GAP-RCB-05 chip rectangular box-model:**
       - Test 4A: `[data-clarity-surface="chat"] .topic-about-chip` selector in `chat.css` no longer uses `border-radius: 999px`. Replaced with a rectangular radius — pick `border-radius: 4px` (matches the existing `.btn` / `.topic-strip > .topic` chip radii in chat.css; planner picks the exact value by reading the existing rectangular-chip precedents in the same stylesheet).
       - Test 4B: The chip's inner padding is balanced — the asymmetric `padding: 2px 4px 2px 9px` (link) + `padding: 2px 7px 2px 2px` (dismiss) — currently designed for the pill curve — are replaced with symmetric padding that gives the chip a clean rectangular appearance. Planner picks values per visual consistency with existing rectangular chips (likely `padding: 2px 6px` on each child or a single `padding: 4px 8px` on the parent + inner children with 0 padding-x; whichever matches the closest existing rectangular-chip precedent in chat.css).
       - Test 4C (extend `test/ui/topic-strip-d10.test.mjs` OR new file `test/ui/topic-about-chip-rcb05.test.mjs`): source-grep `chat.css` line containing the `.topic-about-chip {` selector block — assert `border-radius` value does NOT match `999px` and DOES match a value ≤ `8px` (per the planner's rectangular pick). Add a literal-string assertion that pins the new chosen radius. Pick the new test file location to keep the existing `topic-strip-d10.test.mjs` focused on its archived-topic semantics.
       - Test 4D: NO change to the JSX in `topic-strip.tsx` — only CSS. The "About COU-NNNN ↗" text remains React text (T-04.2-01-03 hygiene), `nav.navigate(nav.resolveHref(...))` navigation pattern preserved.
    - **React-key audit (5 components per D-14):**
       - One commit per component (D-14 spec).
       - Test 5A: Audit each of the 5 components named by the operator (ContextRail / PersistedMessage / TrueTaskDialog / AnchoredToCards / ChatPageBody) for:
         - Any `Array.prototype.map(...)` callback returning JSX without a stable `key={...}` prop.
         - Any sibling `React.Fragment` shorthand `<>...</>` that should be `<React.Fragment key={...}>` because the parent list contains multiple siblings.
         - Any conditional render branch (`<>condA</> </>condB</>`) that lacks key disambiguation.
       - The audit MAY find that the keys are already correct — in which case the "fix" for that component is to add a defensive comment block citing Plan 05-07 + the operator-drill attribution, OR (preferred) to look one level deeper at any CHILD render path that THIS component renders (e.g., `ContextRail` renders `<ActiveTasksOwned>` which renders `tasks.map(...)`; an unstable key in the CHILD surfaces a warning ATTRIBUTED to the parent component instance in React's stack-trace output). The drill report at 04.2-07-SUMMARY "Console (DevTools)" section attributes warnings to component INSTANCES, not to the file that contains the broken key.
       - Per-component expected outcomes (planner reads each file and confirms):
         - **ContextRail (`src/ui/surfaces/chat/context-rail.tsx`):** the file itself has no `.map()`; the warning likely originates in `active-tasks-owned.tsx` (line 111 already keyed on `t.issueId`) OR in `archive-topic-button.tsx`. Audit both — if a list with potentially-non-unique IDs is found, swap to a composite key `${issueId}-${index}` per the message-thread.tsx precedent.
         - **PersistedMessage (`src/ui/surfaces/chat/message-thread.tsx` lines 595-end):** the function returns ONE `<article>` per call (called inside `ordered.map`, line 543 — keyed on the outer `msg.commentId`). Check the `PromoteActions` / `parseReasoning` / `parseRefChips` child render paths for any unstable keys.
         - **TrueTaskDialog (`src/ui/surfaces/chat/true-task/true-task-dialog.tsx` lines 327, 346):** roster + topics select-option maps; both keyed. Audit any deeper render path including the `chat-task-status-pill.tsx` + `inline-task-card.tsx` siblings.
         - **AnchoredToCards (`src/ui/surfaces/reader/ref-card.tsx` line 44):** map already keyed on `c.id`. Audit `RefCard` child for any `cards[].excerpt` or `cards[].refs` deep map.
         - **ChatPageBody (`src/ui/surfaces/chat/index.tsx` lines 145-end):** body composition renders `<RosterRail>`, `<TopicStrip>`, `<ChatActionsRow>`, `<ArchivePanel>`, `<MessageThread>`, `<Composer>`, `<ContextRail>`, `<TrueTaskDialog>`, `<ArchivedBanner>`. Audit each child's map paths.
       - Test 5B: Extend `test/ui/no-react-key-warnings.test.mjs` FILES array to include any deep-child files audited above (currently includes chat surface 6 files; add reader files if AnchoredToCards trace points there — `'src/ui/surfaces/reader/ref-card.tsx'`).
       - Test 5C: Commit-shape requirement — D-14 says "one commit per component (5 commits)". Document this in the SUMMARY: 5 atomic commits with subjects `fix(05-07): React-key warning -- <component> (D-14)`. The auditor may discover ZERO actual fixes are needed (the keys are all correct already, and the warnings are React 19 dev-only false-positives on a downstream library); in that case, each commit may be a documentation-only annotation (header comment block citing the audit). Each commit MUST still touch only its named component to honour D-14's per-component cadence.
       - If the audit finds the warnings originate in a HOST-provided component or a `@paperclipai/plugin-sdk/ui` component (out of plugin control), the SUMMARY records this as a documented "host-side React-key warning, plugin code clean" outcome — D-14's "fix ALL 5 components" requirement is then operationally satisfied by the audit + documentation; no production-code change is forced when the source is upstream.
    - **No regression on Phase 4.2 closure baseline (RCB-01..RCB-07):**
       - Existing tests in `test/ui/continue-in-chat-button.test.mjs`, `test/ui/reverse-topics-link.test.mjs`, `test/ui/topic-strip-backlink.test.mjs`, `test/ui/continue-in-chat-deeplink-contract.test.mjs` MUST continue to pass.
       - The static no-react-key-warnings.test.mjs MUST continue to pass over the extended FILES set.
  </behavior>

  <action>
    D-13 nav.replace removal (`src/ui/surfaces/chat/index.tsx`):

    1. Locate line 441 — `nav.navigate(pathname, { replace: true });`. DELETE this single line.

    2. Replace the surrounding comment block (lines 437-440) with an updated explanation: cite Plan 05-07 D-13, the operator desire ("URL is honest about app state"), and the Back-button behavioural contract. Reference the `consumedDeepLinkRef` guard as the load-bearing piece that PREVENTS re-fire on re-render — the hash is left in place; the guard owns the consume-once invariant now that the replace-nav no longer clears the hash. Mention that Forward-after-Back lands the operator back at the chat surface with the hash intact (the `useEffect` re-evaluates `link` from the still-present hash, the consume-ref blocks re-fire IF it has the same `JSON.stringify(link)` key — but on a fresh browser-history forward navigation that's still the same SPA session and the guard correctly holds).

    3. Keep `useHostNavigation` import — it's still consumed by the reverse-topics-link via `nav.navigate(deepLink.to)` in the Reader, AND it might still be needed for other consume-path navigations in the same file (e.g., the `handleSeededCreate` path). Audit and only remove the `nav` local destructure if no other consumer remains; otherwise leave as is.

    GAP-RCB-05 chip rectangular box-model (`src/ui/styles/chat.css`):

    1. Read the existing `[data-clarity-surface="chat"]` chip / button precedents in chat.css to pick a consistent rectangular `border-radius`. Likely candidates: `.topic` (the topic-strip pill — possibly also pill-shaped, in which case skip this comparison), `.btn`, `.btn.primary`, `.qa` (Quick Actions). Pick the radius value used by the closest visual analog. Likely value: `4px` or `6px`.

    2. Update lines 2349-2382 — the `.topic-about-chip` selector block:
       - Change `border-radius: 999px` to the chosen rectangular value.
       - Rebalance padding on `.topic-about-chip-link` and `.topic-about-chip-dismiss` to symmetrical / rectangular-friendly values that don't visually wrap on `About COU-NNNN ↗` (~12-14 chars) at the existing font-size 11px. Planner picks: likely move padding from the children to the parent `.topic-about-chip` (e.g., `padding: 4px 8px`) and zero out the children's padding-x so the box is one rectangle, NOT a parent-with-two-inset-pill children.
       - Preserve `border: 1px solid var(--you)`, `background: var(--you-soft)`, `display: inline-flex`, `align-items: center`, `gap: 2px`, `margin-right: 8px`.

    3. Add an inline comment block at the top of the modified rule: `/* Plan 05-07 RCB-05 — rectangular chip box-model. Pill (999px) wrapped on long text into a circle/oval per rc.7 drill 2026-05-25; rectangular radius matches existing .btn / .qa precedents. */`

    React-key audit + fix (5 components, ONE COMMIT EACH):

    1. **Commit 1 — ContextRail audit:** Read `src/ui/surfaces/chat/context-rail.tsx` + every component it renders (`ActiveTasksOwned`, `ArchiveTopicButton`, `ToastProvider` consumers reachable from here). For each `.map()` returning JSX, verify a stable key. If a list iterates over a payload where the id field may be non-unique (e.g., chat_topic_tasks rows where two tasks share an issueId in a degenerate edge case), swap to composite `${id}-${index}` keys. If the audit finds no actual unstable keys, add a header comment block under the existing one (lines 1-32) documenting the 05-07 audit pass + cite operator drill date. Either way, COMMIT with subject `fix(05-07): React-key audit -- ContextRail (D-14)`.

    2. **Commit 2 — PersistedMessage audit:** Read `src/ui/surfaces/chat/message-thread.tsx` lines 595-end (PersistedMessage function body). Audit every JSX-returning child: `PromoteActions`, `parseReasoning`, `parseRefChips`-rendered refs, ReasoningPanel rows. For each `.map()`, verify stable key. Likely findings: in PromoteActions if it renders pin-state options; in any inline reasoning bullet list. Fix unstable keys with composite `${stableField}-${index}`. COMMIT with subject `fix(05-07): React-key audit -- PersistedMessage (D-14)`.

    3. **Commit 3 — TrueTaskDialog audit:** Read `src/ui/surfaces/chat/true-task/true-task-dialog.tsx` lines 327 + 346 (the two `.map()`s are already keyed). Audit any sibling Fragment usage — particularly inside the conditional `mode === 'promote' && sourceMessage ? ... : null` and the `feedback ? ... : null` blocks. Audit the `chat-task-status-pill.tsx` + `inline-task-card.tsx` sibling files for unstable keys in their JSX render paths. COMMIT with subject `fix(05-07): React-key audit -- TrueTaskDialog (D-14)`.

    4. **Commit 4 — AnchoredToCards audit:** Read `src/ui/surfaces/reader/ref-card.tsx` (72 lines). Map at line 44 is keyed on `c.id` — the per-card `c.id` is a BEAAA-NNN string, stable. Audit `RefCard` (the child component) for any deep map. Audit the parent integration site at `src/ui/surfaces/reader/index.tsx:329`. COMMIT with subject `fix(05-07): React-key audit -- AnchoredToCards (D-14)`.

    5. **Commit 5 — ChatPageBody audit:** `src/ui/surfaces/chat/index.tsx` lines 145-end. Audit every conditional render branch (`{loading ? ... : ...}`, `{employee ? ... : null}`, etc.) for any sibling-list pattern. Audit the top-level `archivedRaw.topics.map((t) => ({...}))` at line 517 (data transformation — no JSX; not a key concern, but verify) and any rendering of the resulting array. COMMIT with subject `fix(05-07): React-key audit -- ChatPageBody (D-14)`.

    Test additions:

    1. New file `test/ui/chat-deeplink-back-preserves-hash.test.mjs`:
       - Test "D-13: deep-link consume effect no longer calls nav.navigate(pathname, { replace: true })" — read `src/ui/surfaces/chat/index.tsx` as text, assert 0 matches of the literal `nav.navigate(pathname, { replace: true })`.
       - Test "D-13: consumedDeepLinkRef guard remains as the consume-once invariant" — assert at least 2 occurrences of `consumedDeepLinkRef`.
       - Test "D-13: deep-link consume comment block cites Plan 05-07 / D-13" — assert at least 1 occurrence of literal `D-13` near the deep-link consume effect (roughly between the `useEffect` block opening and closing).

    2. New file `test/ui/topic-about-chip-rcb05.test.mjs`:
       - Test "GAP-RCB-05: .topic-about-chip no longer uses border-radius: 999px" — read `src/ui/styles/chat.css` as text, find the `.topic-about-chip {` rule block, assert it does NOT contain `border-radius: 999px`.
       - Test "GAP-RCB-05: .topic-about-chip uses a rectangular border-radius (<= 8px)" — assert the radius value (literal pin per the planner's pick).
       - Test "GAP-RCB-05: chat.css comment cites Plan 05-07 RCB-05" — assert at least 1 occurrence of `RCB-05` near the modified rule block.

    3. Extend `test/ui/no-react-key-warnings.test.mjs` FILES array if any new file lands in scope (e.g., `src/ui/surfaces/reader/ref-card.tsx`, `src/ui/surfaces/chat/active-tasks-owned.tsx`, `src/ui/surfaces/chat/true-task/inline-task-card.tsx`, `src/ui/surfaces/chat/true-task/chat-task-status-pill.tsx`, `src/ui/surfaces/chat/true-task/true-task-dialog.tsx`). Planner picks based on the audit findings — only files that contained an actual fix (or the parent of a fix) are added.

    Constraints:
    - `tsc --noEmit` clean.
    - `node scripts/check-css-scope.mjs` clean (all selectors scoped under `[data-clarity-surface="chat"]`).
    - `node scripts/check-a11y.mjs` clean (no dangerouslySetInnerHTML introduced; existing 65-file / 0-violation baseline preserved).
    - No JSX changes to `topic-strip.tsx` (CSS-only fix for RCB-05).
    - URL_HASH carrier contract (Plan 04.2-03 lock): `buildTopicDeepLink` + `parseChatDeepLink` + `deep-link.mjs` + `deep-link.d.mts` files are NOT modified by this plan. Pin via file-modification check in the SUMMARY: `git diff HEAD~10 -- src/ui/surfaces/chat/deep-link.mjs src/ui/surfaces/chat/deep-link.d.mts` shows zero changes from this plan's commits.
  </action>

  <verify>
    <automated>node --test test/ui/chat-deeplink-back-preserves-hash.test.mjs test/ui/topic-about-chip-rcb05.test.mjs test/ui/no-react-key-warnings.test.mjs</automated>
    <automated>npx tsc --noEmit</automated>
    <automated>node scripts/check-css-scope.mjs</automated>
    <automated>node scripts/check-a11y.mjs</automated>
    <automated>grep -cE "nav\.navigate\(pathname,\s*\{\s*replace:\s*true\s*\}\)" src/ui/surfaces/chat/index.tsx</automated>
    <automated>grep -cE "border-radius:\s*999px" src/ui/styles/chat.css</automated>
  </verify>

  <done>
    - D-13 `nav.replace` removed from chat surface deep-link consume effect; URL_HASH preserved post-consume; consumedDeepLinkRef guard owns the consume-once invariant.
    - `.topic-about-chip` renders as a rectangle (border-radius ≤ 8px, symmetric padding).
    - 5 React-key audit commits land (one per component): ContextRail, PersistedMessage, TrueTaskDialog, AnchoredToCards, ChatPageBody.
    - `no-react-key-warnings.test.mjs` still passes; extended FILES set covers any new audit-surface file.
    - `tsc --noEmit`, `check-css-scope`, `check-a11y` all clean.
    - `grep -cE "nav.navigate(pathname, { replace: true })"` returns 0; `grep -cE "border-radius:\s*999px"` in chat.css for the `.topic-about-chip` rule block returns 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Version bump 1.0.0-rc.7 → 1.0.0-rc.8 (two sources per plugin-version-bump-two-sources memory)</name>
  <files>package.json, src/manifest.ts, test/manifest/chat-capabilities.test.mjs</files>

  <read_first>
    @package.json (line 3 — version literal)
    @src/manifest.ts (line 337 — version literal)
    @test/manifest/chat-capabilities.test.mjs (the manifest-version pin — locate the version-assertion line and update)
    Memory: `plugin-version-bump-two-sources` — version lives in BOTH `package.json` AND `src/manifest.ts` (hardcoded literal). Host reads `dist/manifest.js` (built from `src/manifest.ts`), not `package.json`. Bumping only one source ships v_new code with a v_old label.
    Memory: `paperclipai-workspace-script-gotcha` — for any operator-side `npm pack` step, run from the workspace root, NOT from a subdirectory.
  </read_first>

  <action>
    1. Update `package.json` line 3: `"version": "1.0.0-rc.7"` → `"version": "1.0.0-rc.8"`.

    2. Update `src/manifest.ts` line 337 (or wherever the `version:` literal lives — verify via grep): `version: '1.0.0-rc.7',` → `version: '1.0.0-rc.8',`.

    3. Update `test/manifest/chat-capabilities.test.mjs` — find the assertion that pins the manifest version literal (typical pattern: `assert.equal(manifest.version, '1.0.0-rc.7')` or similar) and update to `'1.0.0-rc.8'`. If the test reads the version via `JSON.parse(readFileSync('package.json'))` instead of a hardcoded literal, no test change is needed — the dynamic read will pick up the new value. Planner verifies via grep first; updates the test only if it carries a hardcoded literal.

    4. Rebuild + pack (operator-side; the build is the deliverable):
       - `npx tsc --noEmit` — clean.
       - `node scripts/build-worker.mjs` — produces `dist/worker.js`.
       - `node scripts/build-ui.mjs` — produces `dist/ui/`.
       - `npx tsc --project tsconfig.manifest.json` — produces `dist/manifest.js` with the new version literal.
       - `npm pack` — produces `clarity-pack-1.0.0-rc.8.tgz`. Record the byte count and sha256 in the executor's commit-and-pack output for the SUMMARY.

    5. Per the plugin-version-bump-two-sources memory: the `dist/manifest.js` produced by step 4 above MUST carry `version: '1.0.0-rc.8'` (verify with `grep -E "version.*1\.0\.0-rc\.8" dist/manifest.js`). Without that grep pass, the plugin ships rc.8 code with an rc.7 label — the exact bug the memory warns against.
  </action>

  <verify>
    <automated>node --test test/manifest/chat-capabilities.test.mjs</automated>
    <automated>grep -cE "1\.0\.0-rc\.8" package.json</automated>
    <automated>grep -cE "1\.0\.0-rc\.8" src/manifest.ts</automated>
    <automated>npx tsc --noEmit</automated>
  </verify>

  <done>
    - `package.json` carries `"version": "1.0.0-rc.8"`.
    - `src/manifest.ts` carries `version: '1.0.0-rc.8',`.
    - `dist/manifest.js` rebuilt with the new version literal (validated by post-build grep).
    - `test/manifest/chat-capabilities.test.mjs` still green (assertion updated if it carries a hardcoded literal).
    - `clarity-pack-1.0.0-rc.8.tgz` produced by `npm pack`; bytes + sha256 captured for the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader UI → chat-open-for-issue worker | UI invokes `usePluginData('chat.openForIssue', ...)` with `{ companyId, userId, issueId }`. Worker reads via `ctx.issues.get` (host RPC). |
| Worker → host Paperclip API (`ctx.issues.get`) | Worker reads issue identifier (CHT-NN) for the topic-issue lookup. Identifiers are public-by-design in Paperclip (rendered everywhere in classic UI); no PII surface. |
| Chat surface URL_HASH carrier | `#h=<base64-JSON>` payload contains `topic` (UUID), `comment` (UUID), `employee` (UUID), `newTopic` (boolean), `seedTitle`/`seedBody` (operator-typed strings), `originIssueId` (UUID). All UUIDs are opaque host-internal; titles/bodies are operator-typed and rendered as React text only. |
| chat.css selectors | All selectors scoped under `[data-clarity-surface="chat"]` (check-css-scope gate). No host-style bleed-through. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Information Disclosure | `chat.openForIssue` topic identifier resolution path (Task 1) | accept | Identifiers (CHT-NN, BEAAA-NNN) are public-by-design in Paperclip — rendered in the issue list, the Reader breadcrumb, every classic-UI surface. Threading them through tooltips does not change the trust surface. The opt-in gate (line 124, `wrapDataHandler`) still gates the entire payload; an opted-out user receives `{ error: 'OPT_IN_REQUIRED' }` before any identifier resolution runs. |
| T-05-07-02 | Tampering | URL_HASH carrier with Browser-Back preserved (D-13) | mitigate | The hash payload remains client-side-only (RFC 3986 — never reaches the server). The `consumedDeepLinkRef` consume-once guard PREVENTS hash re-consumption within the same SPA session even if the hash sits in the URL (Task 2). A malicious operator typing a crafted `#h=` payload into the URL bar can only deep-link THEIR OWN session to a topic they already have permission to read (the chat-surface dispatch path runs through `chat.roster` + `chat.topics` opt-in-guarded fetches; an unauthorised topic UUID won't match anything in the roster and lands at the empty state — same behaviour as today). Verified via the existing chat-url-params.test.mjs. |
| T-05-07-03 | Denial of Service | Additional `ctx.issues.get` call for topic identifier (Task 1 lineage branch) | accept | One extra host RPC per Continue-button mount on chat-spawned issues. Reader-view mounts are operator-initiated, ~1 per minute peak. Worker-side host RPC is cached by Paperclip; budget impact negligible. Worst-case host outage degrades gracefully (try/catch warn-log; `topicIdentifier` undefined; tooltip falls back to the existing `result.topicIssueId` raw value — operator-visible but not crashing). |
| T-05-07-04 | Tampering | React-key audit changes (Task 2 commits 1-5) | accept | Pure UI rendering changes; no data-layer surface. React-key warnings are reconciliation hints — not security; correcting them improves correctness but doesn't introduce a new threat surface. |
| T-05-07-05 | Information Disclosure | RCB-05 chip CSS rectangular box-model (Task 2) | accept | CSS-only change to visual presentation of an existing operator-visible label. The label content (`About COU-NNNN ↗`) is unchanged; only the chip's box shape changes. No new data exposure. |
| T-05-07-06 | Repudiation | D-03 cross-employee fall-through fixture (Task 1) | accept | Test fixture only — proves an existing SQL-layer filter scopes correctly. No production-code data path change. |
| T-05-07-SC | Tampering | npm package installs (none in this plan) | n/a | This plan introduces NO new npm dependencies. `react-markdown`, `xlsx`, `playwright` are scoped to Plan 05-04. Package legitimacy gate not triggered. |

CTT-07 invariant (Plan 04.1-07): plugin actions never modify `public.issues.updated_at`. Preserved by construction in this plan — the new `ctx.issues.get` calls are READ-ONLY; the existing `setChatTopicArchived` plugin-side helper remains the sole archive mutator (it touches `chat_topics.archived_at` only); no `ctx.issues.update` is added or called.

NO_UUID_LEAK invariant (04.2-06 D9/D10 hygiene pattern): the new `topicIdentifier` field is sourced via `ctx.issues.get(...)?.identifier` (CHT-NN string). On failure / missing identifier, the field is left undefined; the UI tooltip falls back to `result.topicIssueId` as a documented degrade (still a UUID, but only on the cached-payload graceful-degrade path AND ONLY for the chat-task lineage branch where the operator-visible context is "the source topic of a chat-spawned task" — strictly improved over the rc.7 baseline that ALWAYS leaked the UUID).

</threat_model>

<verification>

## Cross-task verification

- `node --test "test/**/*.test.mjs"` — full suite. Baseline pre-plan: 1414 tests / 1412 pass / 0 fail / 2 skip (per rc.7 SUMMARY). Expected post-plan delta: +~10-15 net new tests (Task 1: 6-7 worker handler + UI source-grep + 1 D-03 fixture; Task 2: 3 + 3 + N react-key extensions). Final suite >= 1425; 0 fail; 2 pre-existing skip preserved.
- `npx tsc --noEmit` — clean.
- `node scripts/check-css-scope.mjs` — exit 0 (selector count may shift by 0-1 if a new rule-block annotation lands; baseline 108 selectors).
- `node scripts/check-a11y.mjs` — exit 0 (65 files / 0 violations baseline preserved; no `dangerouslySetInnerHTML` introduced).
- `node scripts/build-worker.mjs` — clean.
- `node scripts/build-ui.mjs` — clean.
- `npx tsc --project tsconfig.manifest.json` — produces `dist/manifest.js` carrying `version: '1.0.0-rc.8'`.
- `npm pack` — produces `clarity-pack-1.0.0-rc.8.tgz`; capture byte count + sha256.

## Closure baseline guard (RCB-01..RCB-07 — Phase 4.2 VERIFICATION.md)

Per CONTEXT.md: the existing 5 closure-fixture paths from 04.2-VERIFICATION.md MUST NOT regress.

- `test/ui/continue-in-chat-button.test.mjs` (RCB-01 baseline)
- `test/ui/reverse-topics-link.test.mjs` (RCB-06 baseline)
- `test/ui/topic-strip-backlink.test.mjs` (RCB-05 baseline — JSX shape; CSS is the only change here)
- `test/ui/continue-in-chat-deeplink-contract.test.mjs` (RCB-03 carrier baseline — Plan 04.2-03 lock)
- `test/worker/handlers/chat-open-for-issue-d7.test.mjs` (D-7 routing baseline; this plan extends, doesn't replace)

All four MUST stay green. Run as part of the cross-task suite above.

## React-key warning audit completeness check

For each of the 5 named components (ContextRail, PersistedMessage, TrueTaskDialog, AnchoredToCards, ChatPageBody):
- 1 commit lands touching ONLY that component's source file or a documented audit-comment annotation.
- Each commit's subject follows `fix(05-07): React-key audit -- <Component> (D-14)`.
- The audit may conclude "no actual broken keys; warnings originate in host-side React" — that conclusion is documented in the commit body AND the SUMMARY.

</verification>

<success_criteria>

1. The three rc.7 forward defects (GAP-D8-LINEAGE-TOOLTIP, GAP-D8-REVERSE-TOOLTIP-FALLBACK, GAP-RCB-05-CHIP-STYLING) are closed in code:
   - `chat.openForIssue` ships `topicIdentifier` on both chat-task lineage AND reverse-lookup N=1 branches; `sourceIssueIdentifier` ships on reverse-lookup N=1 (in addition to the existing ambiguous-branch emission).
   - Reader Continue button tooltip on path 1 reads `Open source topic CHT-NNNN →`; on path 6 reads `Resume conversation about BEAAA-NNNN →`.
   - `.topic-about-chip` renders as a rectangle.

2. D-13 Browser-Back behaviour:
   - `nav.navigate(pathname, { replace: true })` REMOVED from the chat surface deep-link consume effect.
   - `consumedDeepLinkRef` guard intact.
   - Hash sits in the URL post-consume; Back navigates to previous Paperclip page; Forward returns to chat with hash intact.

3. D-14 React-key fixes:
   - 5 commits land (one per component named in CONTEXT.md D-14).
   - `no-react-key-warnings.test.mjs` extended FILES array stays green.

4. D-03 cross-employee fall-through fixture:
   - New test `test/ui/chat-open-for-issue-d3-cross-employee.test.mjs` asserts route degrades to `new-topic-needed` when `listTopicsForIssueAndAssignee` returns `[]`.

5. Version bumped 1.0.0-rc.7 → 1.0.0-rc.8 in BOTH `package.json` AND `src/manifest.ts`; `dist/manifest.js` rebuilt with the new version literal; `clarity-pack-1.0.0-rc.8.tgz` packed.

6. Closure baseline preserved: RCB-01..RCB-07 tests + Plan 04.2-07 D-7 routing tests all stay green; CTT-07 invariant intact (zero `ctx.issues.update` calls in `chat.openForIssue`).

7. Quality gates: `tsc --noEmit`, `check-css-scope`, `check-a11y`, full `node --test` all clean.

</success_criteria>

<output>

Create `.planning/phases/05-distribution-polish/05-07-SUMMARY.md` when done, following @~/.claude/get-shit-done/templates/summary.md.

Required SUMMARY content:
- `defects_addressed: [GAP-D8-LINEAGE-TOOLTIP, GAP-D8-REVERSE-TOOLTIP-FALLBACK, GAP-RCB-05-CHIP-STYLING, D-13, D-14, D-03]`
- `version: 1.0.0-rc.8` / `prior_version: 1.0.0-rc.7`
- Suite delta from baseline 1414 to final (record exact numbers).
- 5 React-key audit commits documented per-component with the audit verdict (fix-applied OR no-fix-needed-with-documented-rationale).
- Tarball name + bytes + sha256.
- Forward-defects-routed list (anything that surfaces during this plan's execution that doesn't fit the rc.8 scope).
- Pin the closure-baseline test files (RCB-01..RCB-07) as green at rc.8.

</output>

---

*Phase: 05-distribution-polish*
*Plan: 05-07*
*Created: 2026-05-25*
*Closes: GAP-D8-LINEAGE-TOOLTIP, GAP-D8-REVERSE-TOOLTIP-FALLBACK, GAP-RCB-05-CHIP-STYLING, D-13, D-14, D-03*
*Version: 1.0.0-rc.7 → 1.0.0-rc.8*
