---
phase: 05-distribution-polish
plan: 08
type: execute
wave: 2
depends_on: ["05-05", "05-06"]
files_modified:
  - migrations/0010_chat_topics_pinned.sql
  - src/worker/db/chat-topics-repo.ts
  - src/worker/handlers/chat-topic-pin.ts
  - src/worker/handlers/chat-topic-archive.ts
  - src/worker/handlers/chat-topic-bulk-unarchive.ts
  - src/worker/handlers/chat-archived-topics.ts
  - src/worker.ts
  - src/manifest.ts
  - src/ui/primitives/clarity-surface-root.tsx
  - src/ui/primitives/clarity-surface-header.tsx
  - src/ui/surfaces/archive/archive-page.tsx
  - src/ui/surfaces/archive/archive-page.test.mjs
  - src/ui/surfaces/reader/index.tsx
  - src/ui/surfaces/situation-room/index.tsx
  - src/ui/surfaces/bulletin/index.tsx
  - src/ui/surfaces/chat/index.tsx
  - src/ui/surfaces/chat/archive-panel.tsx
  - src/ui/surfaces/chat/context-rail.tsx
  - src/ui/surfaces/chat/diagnostics-toggle.tsx
  - src/ui/surfaces/chat/composer.tsx
  - src/ui/surfaces/chat/shortcuts-popover.tsx
  - src/ui/styles/chat.css
  - test/worker/handlers/chat-topic-pin.test.mjs
  - test/worker/handlers/chat-topic-bulk-unarchive.test.mjs
  - test/worker/handlers/chat-topic-archive-pin-exempt.test.mjs
  - test/ui/composer-shortcuts-popover.test.mjs
  - test/ui/diagnostics-toggle-persistence.test.mjs
  - test/manifest/archive-page-slot.test.mjs
  - package.json
autonomous: true
requirements: []
tags:
  - chat
  - archive
  - storage-pin
  - power-features
  - migration
  - manifest
must_haves:
  truths:
    - "Operator navigates to `/<companyPrefix>/archive` and sees a full-screen list of every archived chat topic in the company (per D-15)"
    - "Operator multi-selects archived topics with row checkboxes and clicks Unarchive — every selected row's chat_topics.archived flips false in one round-trip; a single toast reads 'N topics unarchived' (per D-16)"
    - "Operator presses + Create task in the top-right corner of Reader, Situation Room, Bulletin, OR Chat — the same TrueTaskDialog opens in cold mode (per D-17)"
    - "Operator toggles Diagnostics ON for Topic A, reloads the page, navigates back to Topic A — diagnostics remain ON; Topic B's diagnostics state is independent (per D-18)"
    - "Operator presses ? inside the chat composer with an empty textarea — a popover anchored to the composer lists keyboard shortcuts; typing ? in a non-empty composer inserts a literal ? character (per D-19)"
    - "Operator clicks the Storage pin card in the chat right rail — chat_topics.pinned_at flips to now(); a subsequent chat.topic.archive request for that topic returns { error: 'PIN_EXEMPT' } and the row stays archived=false (per D-20)"
    - "Plugin disable preserves the new pinned_at column; row data survives a disable/enable cycle (coexistence guarantee #3 + #6)"
    - "No plugin action modifies public.issues.updated_at across any of the new D-15/D-16/D-20 paths (CTT-07)"
  artifacts:
    - path: "migrations/0010_chat_topics_pinned.sql"
      provides: "Additive plugin-namespace pinned_at column on chat_topics"
      contains: "ADD COLUMN IF NOT EXISTS pinned_at"
    - path: "src/worker/handlers/chat-topic-pin.ts"
      provides: "chat.topic.pin action handler — Storage-pin toggle"
      exports: ["registerChatTopicPin", "ChatTopicPinCtx"]
    - path: "src/worker/handlers/chat-topic-bulk-unarchive.ts"
      provides: "chat.topic.bulkUnarchive action handler — bulk operation for archive full-view"
      exports: ["registerChatTopicBulkUnarchive", "ChatTopicBulkUnarchiveCtx"]
    - path: "src/ui/surfaces/archive/archive-page.tsx"
      provides: "ArchivePage page-slot component for `/<companyPrefix>/archive`"
      exports: ["ArchivePage"]
    - path: "src/ui/primitives/clarity-surface-header.tsx"
      provides: "Shared top-right header with + Create task button mounted on all four clarity-pack surfaces"
      exports: ["ClaritySurfaceHeader"]
    - path: "src/ui/surfaces/chat/shortcuts-popover.tsx"
      provides: "ComposerShortcutsPopover anchored to composer; opens on bare-? when textarea empty"
      exports: ["ComposerShortcutsPopover"]
  key_links:
    - from: "src/manifest.ts"
      to: "src/ui/surfaces/archive/archive-page.tsx"
      via: "page-slot exportName: 'ArchivePage', routePath: 'archive'"
      pattern: "exportName:\\s*['\"]ArchivePage['\"]"
    - from: "src/worker/handlers/chat-topic-archive.ts"
      to: "chat_topics.pinned_at"
      via: "PIN_EXEMPT guard before setChatTopicArchived"
      pattern: "PIN_EXEMPT|pinned_at"
    - from: "src/ui/surfaces/chat/context-rail.tsx"
      to: "chat.topic.pin"
      via: "Storage pin click → usePluginAction('chat.topic.pin')"
      pattern: "chat\\.topic\\.pin"
    - from: "src/ui/surfaces/chat/composer.tsx"
      to: "src/ui/surfaces/chat/shortcuts-popover.tsx"
      via: "handleKeyDown ? detection → setShortcutsPopoverOpen(true)"
      pattern: "ComposerShortcutsPopover|shortcutsPopoverOpen"
---

<objective>
Ship the five Phase 4.1 power features locked in CONTEXT.md D-15..D-20 (task templates + smart-prefill explicitly DROPPED per `feedback_trust-the-clarification-loop`):

1. **D-15 Archive full-view** — NEW page-slot route at `/<companyPrefix>/archive` (NOT `/clarity-pack/archive` — the CONTEXT.md `/clarity-pack/` segment is a slip per memory `clarity-pack-plugin-page-routes`; PATTERNS.md flagged this; align with existing `bulletin` / `situation-room` / `chat` route shape). Full-screen list, bulk-select checkboxes, search + filter by employee, bookmarkable.
2. **D-16 Bulk-unarchive** — silent + single count toast `N topics unarchived`. NO confirmation regardless of N (reversible per CTT-07).
3. **D-17 Cold-task-from-global** — shared `+ Create task` button in a top-right header mounted on Reader / Situation Room / Bulletin / Chat. Reuses Plan 04.1-02's TrueTaskDialog cold-mode entry.
4. **D-18 Per-topic diagnostics persistence** — diagnostics toggle state persisted in `localStorage` keyed `clarity:diagnostics:<topic-id>`. Survives page reload. Per-topic independence.
5. **D-19 Composer shortcuts popover** [OPERATOR DEVIATION] — `?` keypress inside an EMPTY composer textarea opens an inline popover listing chat shortcuts. Typing `?` in a non-empty composer inserts the literal character (must not interfere).
6. **D-20 Storage-pin = exempt from archive** — wires the existing static right-rail "Storage pin" card to a real `chat.topic.pin` toggle. Pinned topics return `{ error: 'PIN_EXEMPT' }` from `chat.topic.archive` (mirrors `chat.topic.archive`'s CTT-07 invariant — host issue untouched). NEW additive migration `migrations/0010_chat_topics_pinned.sql` adds `pinned_at timestamptz NULL`.

Purpose: Delivers the operator's locked power features for v1.0.0 final. Plan 05-08 is Wave 2 and depends on Plans 05-05 (shared chat-header banner) and 05-06 (shared right-rail Pin/Unpin pattern) settling shared surfaces first.

Output: 6 new files (migration, two worker handlers, archive page + test, shortcuts popover, shared header), modifications to 11 existing files (manifest, worker.ts, repo, archive handler, four surface index.tsx, archive-panel, context-rail, diagnostics-toggle, composer, chat.css), 5 new test files, version bump.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-distribution-polish/05-CONTEXT.md
@.planning/phases/05-distribution-polish/05-PATTERNS.md
@CLAUDE.md
@migrations/0008_chat_topics_archived_at.sql
@migrations/0009_chat_topics_origin_issue.sql
@src/worker/db/chat-topics-repo.ts
@src/worker/handlers/chat-topic-archive.ts
@src/worker/handlers/chat-archived-topics.ts
@src/worker.ts
@src/manifest.ts
@src/ui/primitives/clarity-surface-root.tsx
@src/ui/surfaces/chat/index.tsx
@src/ui/surfaces/chat/archive-panel.tsx
@src/ui/surfaces/chat/context-rail.tsx
@src/ui/surfaces/chat/topic-strip.tsx
@src/ui/surfaces/chat/diagnostics-toggle.tsx
@src/ui/surfaces/chat/composer.tsx
@src/ui/surfaces/chat/actions-row.tsx
@src/ui/surfaces/bulletin/index.tsx
@src/ui/surfaces/situation-room/index.tsx
@src/ui/surfaces/reader/index.tsx

<interfaces>
<!-- Key contracts the executor needs — extracted from the codebase so no exploration is required. -->

From `src/worker/db/chat-topics-repo.ts`:
- `export type ChatTopicsRepoCtx = { db: PluginDatabaseClient }`
- `export type ChatTopicRow` — snake_case row shape including optional `archived_at?: string | null`, `origin_issue_id?: string | null`. **EXTEND** with optional `pinned_at?: string | null`.
- `export async function setChatTopicArchived(ctx, companyId, topicIssueId, archived: boolean): Promise<void>` — current implementation runs `UPDATE … SET archived = $1, archived_at = CASE WHEN $1 = true THEN now() ELSE NULL END WHERE issue_id = $2 AND company_id = $3`.
- `export async function listArchivedChatTopicsForEmployee(ctx, companyId, employeeAgentId): Promise<ChatTopicRow[]>` — already returns archived rows newest-archived-first.
- Add `setChatTopicPinned(ctx, companyId, topicIssueId, pinned: boolean): Promise<void>` mirroring setChatTopicArchived (flips `pinned_at` between `now()` and `NULL`).
- Add `bulkSetChatTopicArchived(ctx, companyId, topicIssueIds: string[], archived: boolean): Promise<{updated: number}>` — single round-trip `UPDATE … WHERE issue_id = ANY($2) AND company_id = $1`; returns the host's `rowCount`.
- Add `listAllArchivedChatTopics(ctx, companyId): Promise<ChatTopicRow[]>` — company-scoped (NOT employee-scoped) variant of `listArchivedChatTopicsForEmployee`; ORDER BY archived_at DESC NULLS LAST, last_activity_at DESC.
- Add `isChatTopicPinned(ctx, companyId, topicIssueId): Promise<boolean>` — SELECT 1 round-trip; returns `pinned_at IS NOT NULL`. Used by `chat.topic.archive` pin-exempt guard.

From `src/worker/handlers/chat-topic-archive.ts`:
- `export type ChatTopicArchiveCtx = OptInGuardActionCtx & ChatTopicsRepoCtx & { logger?: PluginLogger }`
- Current return on success: `{ ok: true, topicIssueId, archived }`. Current error: `{ error: 'ARCHIVE_FAILED' }`.
- **EXTEND** to return `{ error: 'PIN_EXEMPT' }` when caller tries to archive a pinned topic (D-20 invariant).

From `src/worker/handlers/chat-archived-topics.ts`:
- `export type ArchivedTopicEntry = { topicIssueId, topicId, title, employeeAgentId, messageCount: number, lastActiveAt, archivedAt: string | null }`
- Handler key: `chat.archivedTopics` — currently REQUIRES `employeeAgentId`. Archive full-view needs an `all-employees` variant.
- **EXTEND** to accept omitted/empty `employeeAgentId` → company-scoped listing via `listAllArchivedChatTopics`. Existing `employeeAgentId` path stays unchanged for backwards compat with chat surface archive panel.

From `src/manifest.ts` ui.slots (lines 441-476):
- Page-slot shape (verbatim): `{ type: 'page', id, displayName, exportName, routePath }`. Host route resolves to `/<companyPrefix>/<routePath>`.
- Existing exportNames: `ReaderView`, `SituationRoom`, `BulletinPage`, `ChatPage`. **ADD** `{ type: 'page', id: 'clarity-archive', displayName: 'Archive', exportName: 'ArchivePage', routePath: 'archive' }`.
- UI bundle entry point: ArchivePage must be exported from the same module the host loads for clarity-pack UI (mirror `BulletinPage`/`ChatPage` export pattern).

From `src/ui/primitives/clarity-surface-root.tsx`:
- `export type ClaritySurfaceName = 'reader' | 'situation-room' | 'bulletin' | 'chat' | 'settings'`. **EXTEND** with `'archive'`.

From `src/ui/surfaces/chat/index.tsx`:
- `ChatPage` three-gate composition: `useOptIn()` → `useResolvedCompanyId()` + `useResolvedUserId()` → body. Identical shape MUST be used by `ArchivePage`.
- Toast surface is the `useToast()` hook inside a `<ToastProvider>` tree.

From `src/ui/surfaces/chat/topic-strip.tsx` lines 199-225:
- localStorage persistence pattern: key shape `clarity-about-chip-dismissed:<issueId>`; read on mount via `window.localStorage.getItem(key) === '1'`; write on change via `window.localStorage.setItem(key, '1')` inside try/catch; Set mirror drives re-render without re-reading localStorage. For D-18 diagnostics, key shape is `clarity:diagnostics:<topic-id>` (CTT-cross-cutting-truths constraint).

From `src/ui/surfaces/chat/actions-row.tsx` lines 49-72:
- Single-key shortcut pattern with editable-element bail — uses a `window` listener with `e.ctrlKey || e.metaKey || e.altKey || e.shiftKey` bail. For D-19, the composer popover must DEVIATE: scope to the textarea's own `onKeyDown` (NOT a window listener) so the `?` key only triggers when the composer is the active surface.

From `src/ui/surfaces/chat/composer.tsx` lines 191-200:
- `handleKeyDown` callback receives `React.KeyboardEvent<HTMLTextAreaElement>`. Currently handles only Enter / Shift+Enter. D-19 extends this callback with the `?` detection.

From `src/ui/surfaces/chat/context-rail.tsx` lines 238-249:
- Storage pin card is STATIC (no click handler) — renders `📁 <b>{topic.title}</b>` with the byline "all messages persist as issue comments · single source of truth". D-20 wires this to a real toggle.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + repo extensions for pinned_at + bulk-unarchive + all-archived listing</name>
  <files>migrations/0010_chat_topics_pinned.sql, src/worker/db/chat-topics-repo.ts, test/worker/db/chat-topics-repo-pinned.test.mjs</files>
  <read_first>
    @migrations/0009_chat_topics_origin_issue.sql
    @migrations/0008_chat_topics_archived_at.sql
    @src/worker/db/chat-topics-repo.ts (lines 1-100, 395-440 — type shape + setChatTopicArchived precedent)
    @test/migrations/ddl-prefix-validator.test.mjs (validator constraints — NO standalone CREATE INDEX, NO DO blocks, apostrophe-free comments, ends on semicolon)
  </read_first>
  <action>
    Implement D-20 schema + supporting repo helpers consumed by Tasks 2-4.

    **(a) Create `migrations/0010_chat_topics_pinned.sql`** — additive plugin-namespace column. Single `ALTER TABLE plugin_clarity_pack_cdd6bda4bd.chat_topics ADD COLUMN IF NOT EXISTS pinned_at timestamptz DEFAULT NULL;` statement. File header comments must mirror 0009's apostrophe-free style and explain that pinned_at IS NOT NULL means the topic is exempt from archive (D-20 invariant). No standalone CREATE INDEX. No DO blocks. Last byte must be a semicolon-terminated statement (validator requirement per ddl-prefix-validator). Idempotent via IF NOT EXISTS.

    **(b) Extend `ChatTopicRow` in `src/worker/db/chat-topics-repo.ts`** with `pinned_at?: string | null` (mirror the `archived_at?` and `origin_issue_id?` field placement). Add JSDoc citing Plan 05-08 D-20 and migration 0010.

    **(c) Add `setChatTopicPinned(ctx, companyId, topicIssueId, pinned: boolean): Promise<void>`** — mirror `setChatTopicArchived` (lines 398-411) byte-for-byte except: replace `archived = $1, archived_at = CASE WHEN $1 = true THEN now() ELSE NULL END` with `pinned_at = CASE WHEN $1 = true THEN now() ELSE NULL END`. Uses `ctx.db.execute`.

    **(d) Add `isChatTopicPinned(ctx, companyId, topicIssueId): Promise<boolean>`** — `ctx.db.query<{pinned_at: string | null}>(SELECT pinned_at FROM ... WHERE issue_id = $1 AND company_id = $2)` returning `rows[0]?.pinned_at != null`. Used by chat.topic.archive PIN_EXEMPT guard (Task 3). Empty result returns `false`.

    **(e) Add `bulkSetChatTopicArchived(ctx, companyId, topicIssueIds: string[], archived: boolean): Promise<{ updated: number }>`** — single-round-trip `UPDATE plugin_clarity_pack_cdd6bda4bd.chat_topics SET archived = $1, archived_at = CASE WHEN $1 = true THEN now() ELSE NULL END WHERE company_id = $2 AND issue_id = ANY($3::text[]) AND (pinned_at IS NULL OR $1 = false)`. The `pinned_at IS NULL OR $1 = false` clause enforces D-20 invariant inside the SQL itself: pinned topics can be UN-archived (rare race) but never archived. Returns `{ updated: result.rowCount ?? 0 }`. Empty input array → return `{ updated: 0 }` without a DB round-trip.

    **(f) Add `listAllArchivedChatTopics(ctx, companyId): Promise<ChatTopicRow[]>`** — mirror `listArchivedChatTopicsForEmployee` (lines 423-440) but drop the `employee_agent_id` filter. Same ORDER BY (archived_at DESC NULLS LAST, last_activity_at DESC). SELECT list includes the new `pinned_at` column. Company-scoped only.

    **(g) Create `test/worker/db/chat-topics-repo-pinned.test.mjs`** — `node --test` style mirroring `test/worker/db/chat-topics-repo.test.mjs` shape. Required cases:
    - R1: setChatTopicPinned(true) issues UPDATE with `pinned_at = ... now() ...` (regex match on captured SQL); WHERE clause uses company_id + issue_id.
    - R2: setChatTopicPinned(false) flips pinned_at to NULL.
    - R3: isChatTopicPinned returns true when row's pinned_at is non-null; false when null; false when row absent.
    - R4: bulkSetChatTopicArchived issues single UPDATE with `= ANY($` array binding; rowCount surfaces as `updated`.
    - R5: bulkSetChatTopicArchived(archived=true) SQL contains `pinned_at IS NULL OR $1 = false` guard (regex match — pinned topics excluded from archive batch).
    - R6: bulkSetChatTopicArchived with empty topicIssueIds returns `{updated: 0}` and does NOT call ctx.db.execute.
    - R7: listAllArchivedChatTopics is company-scoped (no employee_agent_id parameter), ORDER BY archived_at DESC NULLS LAST.

    Use the same fake-db harness pattern as `test/worker/db/chat-topics-repo.test.mjs` (capture executed SQL strings and parameter arrays). NO docstring-derived fakes — fakes mimic the live PluginDatabaseClient surface only.

    **CTT-07 invariant note:** None of these helpers issue any `public.issues.*` write. All UPDATEs target `plugin_clarity_pack_cdd6bda4bd.chat_topics` literally. Migration is additive-only per coexistence guarantee #3.
  </action>
  <acceptance_criteria>
    <source>migrations/0010_chat_topics_pinned.sql + src/worker/db/chat-topics-repo.ts + test/worker/db/chat-topics-repo-pinned.test.mjs</source>
    <behavior>
      - Migration 0010 contains exactly one DDL statement matching `/^ALTER TABLE plugin_clarity_pack_cdd6bda4bd\.chat_topics\s+ADD COLUMN IF NOT EXISTS pinned_at timestamptz DEFAULT NULL;/m`.
      - No apostrophes in 0010 comments (validator constraint); file ends with `;\n` or `;`.
      - chat-topics-repo.ts exports `setChatTopicPinned`, `isChatTopicPinned`, `bulkSetChatTopicArchived`, `listAllArchivedChatTopics`.
      - ChatTopicRow type includes `pinned_at?: string | null` (grep confirms).
      - All 7 R-cases in chat-topics-repo-pinned.test.mjs pass.
      - Existing chat-topics-repo.test.mjs continues to pass unchanged (no regression on existing helpers).
    </behavior>
    <cli>node --test test/worker/db/chat-topics-repo-pinned.test.mjs && node --test test/worker/db/chat-topics-repo.test.mjs && node --test test/migrations/ddl-prefix-validator.test.mjs</cli>
  </acceptance_criteria>
  <verify>
    <automated>node --test test/worker/db/chat-topics-repo-pinned.test.mjs test/worker/db/chat-topics-repo.test.mjs test/migrations/ddl-prefix-validator.test.mjs</automated>
  </verify>
  <done>
    Migration 0010 lands per validator rules; four new repo helpers exported and tested; ChatTopicRow type carries pinned_at; CTT-07 unchanged (no host issue mutation in any helper); coexistence guarantee #3 preserved (additive only).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chat.topic.pin action handler + chat.topic.bulkUnarchive action handler + chat.archivedTopics all-employee extension + worker.ts wiring</name>
  <files>src/worker/handlers/chat-topic-pin.ts, src/worker/handlers/chat-topic-bulk-unarchive.ts, src/worker/handlers/chat-archived-topics.ts, src/worker.ts, test/worker/handlers/chat-topic-pin.test.mjs, test/worker/handlers/chat-topic-bulk-unarchive.test.mjs, test/worker/handlers/chat-archived-topics-all.test.mjs</files>
  <read_first>
    @src/worker/handlers/chat-topic-archive.ts (entire file — the byte-for-byte analog per D-20)
    @src/worker/handlers/chat-archived-topics.ts (current employee-scoped handler)
    @src/worker/opt-in-guard.ts (wrapActionHandler / wrapDataHandler signatures + OPT_IN_REQUIRED contract)
    @src/worker.ts lines 110-250 (existing chat handler registration pattern, especially the `registerChatTopicArchive(ctx as unknown as ChatTopicArchiveCtx)` line for the pattern of the unknown-cast)
    @test/worker/handlers/chat-topic-archive.test.mjs (especially the Test 6 spy on ctx.issues.update — D-10/CTT-07 regression guard)
  </read_first>
  <behavior>
    - Test 1: chat.topic.pin happy path — params `{ companyId, userId, topicIssueId, pinned: true }` → wrapActionHandler routes through opt-in check → calls setChatTopicPinned(true) → returns `{ ok: true, topicIssueId, pinned: true }`.
    - Test 2: chat.topic.pin missing companyId throws `Error('chat.topic.pin: companyId required')`.
    - Test 3: chat.topic.pin missing topicIssueId throws `Error('chat.topic.pin: topicIssueId required')`.
    - Test 4: chat.topic.pin wrong-typed pinned throws `Error('chat.topic.pin: pinned (boolean) required')`.
    - Test 5: chat.topic.pin opted-out caller → wrapActionHandler returns `{ error: 'OPT_IN_REQUIRED' }`.
    - Test 6 [CTT-07 PIN-LOAD-BEARING]: across pinned=true and pinned=false paths, `ctx.issues.update` is NEVER called — spy on ctx.issues.update across both paths must record zero invocations.
    - Test 7: chat.topic.pin repo failure → returns `{ error: 'PIN_FAILED' }` and emits ctx.logger?.warn?.
    - Test 8: chat.topic.bulkUnarchive happy path — params `{ companyId, userId, topicIssueIds: [a, b, c] }` → calls bulkSetChatTopicArchived(companyId, [a,b,c], false) → returns `{ ok: true, updated: 3 }`.
    - Test 9: chat.topic.bulkUnarchive empty array → returns `{ ok: true, updated: 0 }` without DB call.
    - Test 10: chat.topic.bulkUnarchive wrong-typed topicIssueIds (non-array) throws `Error('chat.topic.bulkUnarchive: topicIssueIds (string[]) required')`.
    - Test 11: chat.topic.bulkUnarchive opted-out caller → `{ error: 'OPT_IN_REQUIRED' }`.
    - Test 12 [CTT-07 BULK-LOAD-BEARING]: `ctx.issues.update` never called during bulkUnarchive.
    - Test 13: chat.archivedTopics with `employeeAgentId: 'a-1'` (existing path) calls `listArchivedChatTopicsForEmployee` (unchanged from rc.7).
    - Test 14: chat.archivedTopics with omitted/empty `employeeAgentId` calls `listAllArchivedChatTopics` (new company-scoped path); response shape grows `topics[].pinnedAt: string | null` for the archive full-view UI to render a pin indicator.
  </behavior>
  <action>
    **(a) Create `src/worker/handlers/chat-topic-pin.ts`** — VERBATIM copy of `chat-topic-archive.ts` with these substitutions:
    - rename `registerChatTopicArchive` → `registerChatTopicPin`, `ChatTopicArchiveCtx` → `ChatTopicPinCtx`.
    - replace action key `'chat.topic.archive'` → `'chat.topic.pin'`.
    - replace `params?.archived` → `params?.pinned`, `archived` → `pinned` throughout body.
    - replace `setChatTopicArchived` import → `setChatTopicPinned`.
    - replace error key `'ARCHIVE_FAILED'` → `'PIN_FAILED'`.
    - header docstring: cite Plan 05-08 D-20; document the CTT-07 invariant exactly as chat-topic-archive.ts does (regression-guarded by Test 6); note that storage-pin makes the topic EXEMPT from archive (the read side of the invariant lives in chat-topic-archive.ts PIN_EXEMPT guard from Task 3, not here).

    **(b) Create `src/worker/handlers/chat-topic-bulk-unarchive.ts`** — action handler for the archive full-view's bulk-select Unarchive button. Shape:
    - `export type ChatTopicBulkUnarchiveCtx = OptInGuardActionCtx & ChatTopicsRepoCtx & { logger?: PluginLogger }`.
    - `export function registerChatTopicBulkUnarchive(ctx): void` calling `wrapActionHandler(ctx, 'chat.topic.bulkUnarchive', async (params) => { ... })`.
    - Param validation: `companyId` (string, required → throw), `topicIssueIds` (string[], required → throw `'chat.topic.bulkUnarchive: topicIssueIds (string[]) required'` when not array; rejects non-string elements as well).
    - Body: `await bulkSetChatTopicArchived(ctx, companyId, topicIssueIds, false)` → return `{ ok: true, updated: result.updated }`. Empty array short-circuit (returns `{ ok: true, updated: 0 }` without DB call). Repo failure → `{ error: 'BULK_UNARCHIVE_FAILED' }` with warn log.
    - DOES NOT bulk-ARCHIVE — only un-archives. The archive direction stays single-row via the existing `chat.topic.archive` handler. (Symmetry can ship in v1.1 if operator demand emerges.)
    - Note in header: bulkSetChatTopicArchived's SQL guard `pinned_at IS NULL OR $1 = false` is moot here because we pass archived=false (un-archive), but the guard is load-bearing for any future bulk-archive variant; documentation cites Task 1.

    **(c) Modify `src/worker/handlers/chat-archived-topics.ts`** to support omitted `employeeAgentId`:
    - Remove the `if (!employeeAgentId) return { error: 'EMPLOYEE_AGENT_ID_REQUIRED' };` line (line 80).
    - Branch: when `employeeAgentId` is null/empty → `await listAllArchivedChatTopics(ctx, companyId)`. Otherwise the existing employee-scoped path.
    - Extend `ArchivedTopicEntry` with `pinnedAt: string | null` field; populate from `row.pinned_at ?? null` in `mapArchivedTopic`. Existing employee-scoped callers receive the new field with `null` for pre-0010 rows (additive on payload — non-breaking).
    - Existing `EMPLOYEE_AGENT_ID_REQUIRED` error code is RETIRED; downstream callers that previously hit it now succeed with the all-archived listing. Note in header: chat-archive-panel.tsx in the existing chat right rail still passes employeeAgentId → unchanged path; only archive-page.tsx (Task 4) omits it.

    **(d) Modify `src/worker.ts`** to register both new handlers. Mirror the existing `registerChatTopicArchive(ctx as unknown as ChatTopicArchiveCtx)` import + call pattern (lines 113-116 + 226-229). Add:
    - import block: `import { registerChatTopicPin, type ChatTopicPinCtx } from './worker/handlers/chat-topic-pin.ts';`
    - import block: `import { registerChatTopicBulkUnarchive, type ChatTopicBulkUnarchiveCtx } from './worker/handlers/chat-topic-bulk-unarchive.ts';`
    - registration block: place new calls IMMEDIATELY AFTER the existing `registerChatTopicArchive(ctx as unknown as ChatTopicArchiveCtx);` so the chat topic handlers cluster together. Each new line follows the same `ctx as unknown as` cast pattern used at line 229.

    **(e) Create 3 test files** under `test/worker/handlers/`:
    - `chat-topic-pin.test.mjs` — Tests 1-7 from <behavior>. Reuse the harness from `test/worker/handlers/chat-topic-archive.test.mjs` byte-for-byte and substitute keys. Test 6 (CTT-07) creates a spy on `ctx.issues.update` and asserts `spy.calls.length === 0` after both pinned=true and pinned=false runs.
    - `chat-topic-bulk-unarchive.test.mjs` — Tests 8-12. Tests 8 and 12 spy on `ctx.issues.update` to confirm zero calls. Test 9 spies on `ctx.db.execute` and asserts zero calls.
    - `chat-archived-topics-all.test.mjs` — Tests 13-14. Verifies that omitting employeeAgentId hits the new listAllArchivedChatTopics path; verifies the new `pinnedAt` field appears in the response (null when row.pinned_at is null, ISO string when set).

    Test harness reminder: use `wrapHostFaithfulDb` from existing repo test harness so test fakes match the host PluginDatabaseClient surface (NOT docstring-derived fakes — per memory `router-fake-vs-production-host` lesson).
  </action>
  <acceptance_criteria>
    <source>src/worker/handlers/chat-topic-pin.ts + chat-topic-bulk-unarchive.ts + chat-archived-topics.ts diff + src/worker.ts diff + 3 new test files</source>
    <behavior>
      - chat-topic-pin.ts exports registerChatTopicPin + ChatTopicPinCtx; file contains zero call sites of ctx.issues.update or ctx.issues.delete* (CTT-07 by construction).
      - chat-topic-bulk-unarchive.ts exports registerChatTopicBulkUnarchive + ChatTopicBulkUnarchiveCtx; same CTT-07 zero-host-issue-mutation guarantee.
      - chat-archived-topics.ts no longer returns EMPLOYEE_AGENT_ID_REQUIRED; payload grows `pinnedAt` field.
      - worker.ts contains both `registerChatTopicPin(ctx as unknown as ChatTopicPinCtx)` and `registerChatTopicBulkUnarchive(ctx as unknown as ChatTopicBulkUnarchiveCtx)` registration lines.
      - All 14 behavior tests pass.
      - Existing chat-topic-archive.test.mjs continues to pass unchanged.
    </behavior>
    <cli>node --test test/worker/handlers/chat-topic-pin.test.mjs test/worker/handlers/chat-topic-bulk-unarchive.test.mjs test/worker/handlers/chat-archived-topics-all.test.mjs test/worker/handlers/chat-topic-archive.test.mjs test/worker/handlers/chat-archived-topics.test.mjs</cli>
    <grep-gates>
      - grep -v '^//' src/worker/handlers/chat-topic-pin.ts | grep -c 'ctx\.issues\.\(update\|delete\)' == 0 (CTT-07 by construction)
      - grep -v '^//' src/worker/handlers/chat-topic-bulk-unarchive.ts | grep -c 'ctx\.issues\.\(update\|delete\)' == 0 (CTT-07 by construction)
      - grep -c 'registerChatTopicPin' src/worker.ts >= 2 (import + call)
      - grep -c 'registerChatTopicBulkUnarchive' src/worker.ts >= 2 (import + call)
    </grep-gates>
  </acceptance_criteria>
  <verify>
    <automated>node --test test/worker/handlers/chat-topic-pin.test.mjs test/worker/handlers/chat-topic-bulk-unarchive.test.mjs test/worker/handlers/chat-archived-topics-all.test.mjs test/worker/handlers/chat-topic-archive.test.mjs test/worker/handlers/chat-archived-topics.test.mjs && pnpm build:worker</automated>
  </verify>
  <done>
    Two new action handlers shipped with full opt-in + CTT-07 + param-validation contracts; chat.archivedTopics learns the company-scoped variant for the archive full-view; worker.ts registers both; pnpm build:worker is clean; existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: chat.topic.archive PIN_EXEMPT guard (D-20 reverse invariant)</name>
  <files>src/worker/handlers/chat-topic-archive.ts, test/worker/handlers/chat-topic-archive-pin-exempt.test.mjs</files>
  <read_first>
    @src/worker/handlers/chat-topic-archive.ts (entire file — modifying body)
    @test/worker/handlers/chat-topic-archive.test.mjs (existing test surface — must NOT regress; specifically Test 6 CTT-07 spy must stay green)
    @src/worker/db/chat-topics-repo.ts (isChatTopicPinned helper from Task 1)
  </read_first>
  <behavior>
    - Test PE1: when caller requests archive=true on a topic whose `pinned_at IS NOT NULL` → handler returns `{ error: 'PIN_EXEMPT', topicIssueId }` and DOES NOT call setChatTopicArchived (DB spy asserts zero update calls).
    - Test PE2: when caller requests archive=false on a pinned topic → handler proceeds normally (un-archive on a pinned-but-already-unarchived row is a no-op upstream; PIN_EXEMPT applies ONLY to the archive direction). setChatTopicArchived IS called.
    - Test PE3: when caller requests archive=true on an UNPINNED topic → unchanged behavior; happy path returns `{ ok: true, topicIssueId, archived: true }`.
    - Test PE4: isChatTopicPinned throws → handler logs warn + still returns `{ error: 'ARCHIVE_FAILED' }` (defensive — better to deny than archive against an unknown pin state).
    - Test PE5 [CTT-07 REGRESSION]: across PE1/PE2/PE3 paths, ctx.issues.update is never called (preserves the existing Test 6 invariant from chat-topic-archive.test.mjs).
  </behavior>
  <action>
    Extend the existing `chat.topic.archive` handler with a PIN_EXEMPT short-circuit:

    **(a) Modify `src/worker/handlers/chat-topic-archive.ts`** — INSIDE the existing try block (after the param validation, BEFORE `await setChatTopicArchived(...)`):
    - When `archived === true`: call `const pinned = await isChatTopicPinned(ctx, companyId, topicIssueId);` (import added at top alongside setChatTopicArchived).
    - If `pinned === true` → return `{ error: 'PIN_EXEMPT' as const, topicIssueId };` BEFORE the setChatTopicArchived call.
    - If `pinned === false` or the un-archive direction → proceed to existing setChatTopicArchived call unchanged.
    - The isChatTopicPinned read goes inside the existing try/catch so a repo read failure still routes to ARCHIVE_FAILED (PE4 behavior).

    **(b) Update the header docstring** to document the PIN_EXEMPT contract:
    - Add a bullet to the existing comment block explaining that archive on a pinned topic returns `{ error: 'PIN_EXEMPT' }`.
    - Cite Plan 05-08 D-20 and the migration 0010 pinned_at column.
    - Preserve the existing D-10/CTT-07 documentation verbatim.

    **(c) Create `test/worker/handlers/chat-topic-archive-pin-exempt.test.mjs`** — five PE-cases from <behavior>. Reuse the existing chat-topic-archive.test.mjs harness:
    - Mock `isChatTopicPinned` via the same ctx.db fake the harness already wires (since isChatTopicPinned uses ctx.db.query under the hood — the fake captures the SELECT and returns the desired pinned_at).
    - PE5 spies on ctx.issues.update across all three earlier paths (PE1, PE2, PE3) and asserts zero calls.

    **(d) DO NOT regress existing tests.** The existing chat-topic-archive.test.mjs harness assumes isChatTopicPinned returns false (default fake state) — the existing tests' fakes will return empty result sets, which makes isChatTopicPinned() return false, so the existing happy-path tests keep passing. Re-run the existing suite to confirm.
  </action>
  <acceptance_criteria>
    <source>src/worker/handlers/chat-topic-archive.ts diff + test/worker/handlers/chat-topic-archive-pin-exempt.test.mjs</source>
    <behavior>
      - All 5 PE-cases pass.
      - All existing chat-topic-archive.test.mjs cases continue to pass (no regression).
      - PIN_EXEMPT short-circuit happens BEFORE setChatTopicArchived (verified by DB spy in PE1).
      - PE5 spy confirms ctx.issues.update is never called across any branch.
    </behavior>
    <cli>node --test test/worker/handlers/chat-topic-archive.test.mjs test/worker/handlers/chat-topic-archive-pin-exempt.test.mjs</cli>
    <grep-gates>
      - grep -c "PIN_EXEMPT" src/worker/handlers/chat-topic-archive.ts >= 1
      - grep -v '^//' src/worker/handlers/chat-topic-archive.ts | grep -c 'ctx\.issues\.\(update\|delete\)' == 0 (CTT-07 unchanged)
    </grep-gates>
  </acceptance_criteria>
  <verify>
    <automated>node --test test/worker/handlers/chat-topic-archive.test.mjs test/worker/handlers/chat-topic-archive-pin-exempt.test.mjs && pnpm build:worker</automated>
  </verify>
  <done>
    chat.topic.archive learns PIN_EXEMPT for pinned topics; archive in the unpinned direction unchanged; un-archive on a pinned topic still proceeds; ctx.issues.update zero-call invariant preserved; existing test suite green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Archive full-view page-slot — ArchivePage component + manifest entry + ClaritySurfaceName extension</name>
  <files>src/manifest.ts, src/ui/primitives/clarity-surface-root.tsx, src/ui/surfaces/archive/archive-page.tsx, src/ui/surfaces/archive/archive-page.test.mjs, src/ui/surfaces/chat/archive-panel.tsx, test/manifest/archive-page-slot.test.mjs</files>
  <read_first>
    @src/manifest.ts lines 440-477 (slot array)
    @src/ui/primitives/clarity-surface-root.tsx (ClaritySurfaceName union)
    @src/ui/surfaces/bulletin/index.tsx lines 1-100 (three-gate composition + page-slot shape)
    @src/ui/surfaces/chat/index.tsx lines 82-143 (three-gate composition reference)
    @src/ui/surfaces/chat/archive-panel.tsx lines 200-230 (the existing "View all archived →" no-op stub that this task wires up)
    @src/worker/handlers/chat-archived-topics.ts (Task 2 extension — payload includes pinnedAt)
  </read_first>
  <action>
    Ship the bookmarkable archive full-view per D-15/D-16.

    **(a) Modify `src/manifest.ts`** — INSIDE the existing `ui.slots` array (lines 441-476). Add a fifth entry IMMEDIATELY AFTER the `clarity-chat` page entry (line 469):
    ```
    {
      type: 'page',
      id: 'clarity-archive',
      displayName: 'Archive',
      exportName: 'ArchivePage',
      routePath: 'archive',
    },
    ```
    The resulting route resolves to `/<companyPrefix>/archive` (the host concatenates `companyPrefix + '/' + routePath`; this is the established pattern per memory `clarity-pack-plugin-page-routes` — NOT `/clarity-pack/archive`, which is the slip in CONTEXT.md D-15).

    **(b) Modify `src/ui/primitives/clarity-surface-root.tsx`** — extend `ClaritySurfaceName` union with `'archive'`:
    ```
    export type ClaritySurfaceName =
      | 'reader'
      | 'situation-room'
      | 'bulletin'
      | 'chat'
      | 'settings'
      | 'archive';
    ```

    **(c) Create `src/ui/surfaces/archive/archive-page.tsx`** exporting `ArchivePage`:
    - Three-gate composition byte-identical to `bulletin/index.tsx` lines 78-150 and `chat/index.tsx` lines 82-143: `useOptIn` → `useResolvedCompanyId` + `useResolvedUserId`. Each gate renders a `ClaritySurfaceRoot name="archive"` wrapper with the same loading / opt-in / error fallbacks. Use `<EnableClarityCta surfaceName="Archive" />` for the opt-in path.
    - Once gated through, the body component `ArchivePageBody` is wrapped in a `<ToastProvider>` so bulk-unarchive can showToast.
    - ArchivePageBody fetches `chat.archivedTopics` via `usePluginData` passing `{ companyId, userId }` (no employeeAgentId — Task 2's company-scoped variant). Also fetches `chat.roster` to resolve employee display names (mirror archive-panel.tsx's needs).
    - UI:
      - Page header `<h1>` "Archive" + topic count.
      - Search input (case-insensitive substring filter on title), filter dropdown by employee (uses roster names; "All employees" default).
      - Bulk-select header checkbox + per-row checkboxes (`<input type="checkbox">`). When ≥1 row selected, show a sticky action bar with `Selected (N) — Unarchive` button.
      - Each row: checkbox, title, employee name (server-resolved via roster, NEVER UUID per NO_UUID_LEAK CTT), message count placeholder, archived timestamp (relative time), `📌` indicator when `pinnedAt !== null`, Open button (navigates to `/<companyPrefix>/chat#h=<base64-payload>` via existing `buildTopicDeepLink` from deep-link.mjs — same nav pattern as archive-panel.tsx but full-page, not popover).
      - On bulk-unarchive: dispatch `chat.topic.bulkUnarchive` with selected `topicIssueIds`. On success, show toast `"${result.updated} topics unarchived"`; clear selection; the usePluginData refetch will pull the post-update state. NO confirmation modal regardless of N (D-16).
    - All CSS rules scoped under `[data-clarity-surface="archive"]`. Add an `archive.css` import via the existing centralized style-injection in `src/ui/index.tsx` (the same surface-style mechanism Plan 02-02 uses for chat.css / bulletin.css). For v1 minimal style: reuse chat-archive-panel CSS tokens but on a full-page layout. Inline minimal styling via the existing chat.css `chat-archive-panel__*` selectors where applicable — extend chat.css with new `archive-page__*` selectors (still under chat surface? NO — under `[data-clarity-surface="archive"]` per scope-check rule). Create new `src/ui/styles/archive.css` and inject in `src/ui/index.tsx`.
    - The ArchivePage MUST be exported from the same UI bundle entry that ships ChatPage / BulletinPage / SituationRoom — wire the export in `src/ui/index.tsx` (or wherever the four existing page surfaces are re-exported for the host loader).

    **(d) Create `src/ui/surfaces/archive/archive-page.test.mjs`** — `node --test` style mirroring `test/ui/chat-url-params.test.mjs`:
    - AP1: ArchivePage three-gate composition renders Loading / EnableClarityCta / Resolving / no-company-context / no-user-context / body correctly across the four gate states.
    - AP2: ArchivePageBody dispatches `chat.archivedTopics` with `{companyId, userId}` (no employeeAgentId).
    - AP3: Checkbox selection toggles produce correct `selected` Set; sticky bar appears when N≥1.
    - AP4: Bulk-unarchive click dispatches `chat.topic.bulkUnarchive` with selected ids; on success, toast fires with `"${updated} topics unarchived"` copy (regex match — NOT a confirmation modal call).
    - AP5: Search input filters by title case-insensitively.
    - AP6: Employee filter dropdown is populated from chat.roster.
    - AP7 [NO_UUID_LEAK]: No rendered row shows a raw UUID for the employee — the test passes a roster with `name: 'CEO'` and asserts that the `data-clarity-archive-row` cell does NOT contain the bare UUID (regex match on the agent's UUID returns zero matches in the row's textContent).

    **(e) Modify `src/ui/surfaces/chat/archive-panel.tsx`** — wire the existing "View all archived →" button (line 211-223) from console-warn-stub to a real route navigation:
    - Replace the `onClick` body's `console.warn(...)` with `nav.navigate('/' + companyPrefix + '/archive')` — use `useHostNavigation` per SCAF-09 + no-raw-anchor ESLint rule.
    - Thread `companyPrefix` into ArchivePanel as a prop (mirroring how it gets `archivedTopics`).
    - Remove the TODO(4.2) comment block.

    **(f) Create `test/manifest/archive-page-slot.test.mjs`** — assert the new manifest slot is well-formed:
    - MS1: dist/manifest.js (post-build) contains a slot with `id: 'clarity-archive'`, `routePath: 'archive'`, `exportName: 'ArchivePage'`, `type: 'page'`.
    - MS2: the UI bundle entry exports `ArchivePage` (grep dist/ui/index.js for the symbol).
    - MS3: the slot list length increases by exactly 1 vs the rc.7 baseline (5 → 6).
  </action>
  <acceptance_criteria>
    <source>src/manifest.ts diff + src/ui/primitives/clarity-surface-root.tsx diff + src/ui/surfaces/archive/archive-page.tsx + archive-page.test.mjs + chat/archive-panel.tsx diff + test/manifest/archive-page-slot.test.mjs</source>
    <behavior>
      - dist/manifest.js post-build exposes the new clarity-archive page slot with routePath: 'archive'.
      - Visiting `/<companyPrefix>/archive` in a host with the plugin installed routes to ArchivePage (verified at the manifest level; full live verification deferred to Plan 05-10 closure drill).
      - All AP1-AP7 archive-page tests pass; all 3 MS-cases pass.
      - chat-archive-panel View-all link is no longer a no-op stub.
      - check-css-scope.mjs reports 0 selectors leaked outside `[data-clarity-surface=...]` after archive.css is added.
      - check-a11y.mjs passes on archive-page.tsx (every <input> has an associated label; no dangerouslySetInnerHTML).
    </behavior>
    <cli>pnpm build && node --test test/ui/archive-page.test.mjs test/manifest/archive-page-slot.test.mjs && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs</cli>
    <grep-gates>
      - grep -c "ArchivePage" src/manifest.ts == 1 (exportName entry)
      - grep -c "routePath: 'archive'" src/manifest.ts == 1
      - grep -c "archive" src/ui/primitives/clarity-surface-root.tsx >= 1 (added to union)
      - grep -c "console.warn" src/ui/surfaces/chat/archive-panel.tsx == 0 (no-op stub removed)
    </grep-gates>
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && node --test test/ui/archive-page.test.mjs test/manifest/archive-page-slot.test.mjs && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs</automated>
  </verify>
  <done>
    ArchivePage page-slot registered in manifest; route resolves to `/<companyPrefix>/archive`; three-gate composition preserves opt-in + companyId + userId invariants; bulk-unarchive wired; archive-panel View-all link no longer a stub; check-css-scope + check-a11y clean.
  </done>
</task>

<task type="auto">
  <name>Task 5: Shared clarity-surface-header (cold-task button) + mount on all four surfaces (D-17)</name>
  <files>src/ui/primitives/clarity-surface-header.tsx, src/ui/surfaces/reader/index.tsx, src/ui/surfaces/situation-room/index.tsx, src/ui/surfaces/bulletin/index.tsx, src/ui/surfaces/chat/index.tsx, src/ui/styles/chat.css, test/ui/clarity-surface-header.test.mjs</files>
  <read_first>
    @src/ui/surfaces/chat/actions-row.tsx (the existing T-key + Create task button — mirror its dialog-open semantics, NOT its keyboard handler)
    @src/ui/surfaces/chat/index.tsx lines 165-180 (TrueTaskDialog mount + dialogState shape — same dialog must work cold-mode from non-chat surfaces)
    @src/ui/surfaces/chat/true-task/true-task-dialog.tsx (or wherever TrueTaskDialog lives — identify cold-mode entry contract)
    @src/ui/surfaces/reader/index.tsx (mount point for header)
    @src/ui/surfaces/bulletin/index.tsx (mount point for header)
    @src/ui/surfaces/situation-room/index.tsx (mount point for header)
  </read_first>
  <action>
    Introduce the shared `+ Create task` top-right header per D-17.

    **(a) Create `src/ui/primitives/clarity-surface-header.tsx`** exporting `ClaritySurfaceHeader`:
    - Props: `{ companyId: string; userId: string; surface: ClaritySurfaceName }`.
    - Renders a `<header className="clarity-surface-header" data-clarity-region="surface-header">` containing a right-aligned `<button type="button" className="btn primary">+ Create task</button>`.
    - On click: opens TrueTaskDialog in cold mode (`mode='cold'`, no sourceMessage, no sourceTopic). The dialog component must be importable + invocable from outside the chat surface.
    - Local React state `[dialogOpen, setDialogOpen]`; the existing TrueTaskDialog already covers the cold flow per Plan 04.1-02. The header component owns its own dialog mount so cold-task creation works on Reader / Situation Room / Bulletin too.
    - DO NOT bind a window keyboard listener for the `T` key — that lives only on the chat actions-row (existing Plan 04.1-09 contract). The shared header is click-only on the three non-chat surfaces to avoid collision; on chat the same header is mounted but the actions-row's T-listener remains the keyboard path.
    - On task creation success: showToast `'Task created'` if a ToastProvider is in scope; otherwise no-op (Reader/SituationRoom/Bulletin don't wrap a ToastProvider — wrap the header in a minimal ToastProvider OR detect missing context). SIMPLER: mount header WITHOUT depending on useToast; use a callback prop `onTaskCreated?: (taskIssueId: string) => void` that the surface can wire if it has a Toast surface. Reader/SituationRoom/Bulletin pass undefined → no toast (acceptable v1 ergonomics; Plan 04.2-04 toast is chat-only anyway). Chat continues to use its own ToastProvider for the existing T-key path.

    **(b) Mount on all four surfaces:**
    - `src/ui/surfaces/reader/index.tsx` — add `<ClaritySurfaceHeader companyId={companyId} userId={userId} surface="reader" />` at the top of the opted-in / resolved body, BEFORE the TLDR strip. Pass companyId/userId from the existing resolver gates.
    - `src/ui/surfaces/situation-room/index.tsx` — same shape, surface="situation-room", BEFORE the agent grid.
    - `src/ui/surfaces/bulletin/index.tsx` — same shape, surface="bulletin", BEFORE the Masthead.
    - `src/ui/surfaces/chat/index.tsx` — same shape, surface="chat", at the top of ChatPageBody BEFORE the topic strip / actions row. The chat surface already has its own `+ Create task` in `actions-row.tsx`; the shared header is the GLOBAL affordance (always reachable from the header), and the actions-row in-thread button stays for in-thread access. Both call into the same TrueTaskDialog cold mode.

    **(c) Add CSS** — extend `src/ui/styles/chat.css` (or a new shared header CSS file injected by index.tsx) with `[data-clarity-surface="reader"] .clarity-surface-header`, `[data-clarity-surface="situation-room"] .clarity-surface-header`, etc. Header CSS:
    - `display: flex; justify-content: flex-end; padding: 8px 16px;`
    - Inherits the surface's font + color tokens.
    - The `.btn.primary` styles already exist in chat.css scoped under chat — duplicate the rule into each surface scope OR (cleaner) refactor `.btn.primary` to a shared selector. CLEANER: write the `+ Create task` button with a NEW class `.clarity-cold-task-btn` scoped to `[data-clarity-region="surface-header"]` to avoid duplicating the chat `.btn.primary` token table.

    **(d) Create `test/ui/clarity-surface-header.test.mjs`** — `node --test` style:
    - CSH1: ClaritySurfaceHeader renders a button with text matching `/\+ Create task/`.
    - CSH2: Clicking the button calls `setDialogOpen(true)` (assert via React.act + queried button click; the dialog mount is rendered).
    - CSH3: surface="reader" prop produces `data-clarity-surface-header-context="reader"` (or equivalent) so we can grep that mount-point works on the non-chat surfaces.
    - CSH4: ClaritySurfaceHeader does NOT add a window keydown listener (verify by spying on addEventListener; assert no `keydown` registration).
    - CSH5: On TrueTaskDialog success callback, if `onTaskCreated` prop is provided, it fires; otherwise no error.
  </action>
  <acceptance_criteria>
    <source>src/ui/primitives/clarity-surface-header.tsx + 4 surface index.tsx diffs + clarity-surface-header.test.mjs</source>
    <behavior>
      - All 4 surfaces render `<ClaritySurfaceHeader>` at the top.
      - Clicking `+ Create task` on any surface opens the TrueTaskDialog cold-mode.
      - Chat surface's existing actions-row `+ Create task` continues to work (T-keyboard shortcut preserved).
      - No window-level keyboard listener registered by ClaritySurfaceHeader.
      - All 5 CSH tests pass.
      - check-css-scope.mjs reports 0 selectors leaked.
      - check-a11y.mjs passes (button has accessible name).
    </behavior>
    <cli>pnpm build && node --test test/ui/clarity-surface-header.test.mjs && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs</cli>
    <grep-gates>
      - grep -c "ClaritySurfaceHeader" src/ui/surfaces/reader/index.tsx >= 1
      - grep -c "ClaritySurfaceHeader" src/ui/surfaces/situation-room/index.tsx >= 1
      - grep -c "ClaritySurfaceHeader" src/ui/surfaces/bulletin/index.tsx >= 1
      - grep -c "ClaritySurfaceHeader" src/ui/surfaces/chat/index.tsx >= 1
      - grep -v '^//' src/ui/primitives/clarity-surface-header.tsx | grep -c "window\.addEventListener" == 0 (no global keydown)
    </grep-gates>
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && node --test test/ui/clarity-surface-header.test.mjs && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs</automated>
  </verify>
  <done>
    Shared cold-task affordance lands on all four surfaces; TrueTaskDialog reusable cold-mode entry preserved; chat's T-key + in-thread Create-task continues to work; no keyboard-shortcut collision; check-css-scope + check-a11y clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: Per-topic diagnostics persistence (D-18) + composer shortcuts popover (D-19) + storage-pin UI wiring (D-20)</name>
  <files>src/ui/surfaces/chat/diagnostics-toggle.tsx, src/ui/surfaces/chat/index.tsx, src/ui/surfaces/chat/shortcuts-popover.tsx, src/ui/surfaces/chat/composer.tsx, src/ui/surfaces/chat/context-rail.tsx, src/ui/styles/chat.css, test/ui/diagnostics-toggle-persistence.test.mjs, test/ui/composer-shortcuts-popover.test.mjs, test/ui/context-rail-storage-pin.test.mjs</files>
  <read_first>
    @src/ui/surfaces/chat/topic-strip.tsx lines 199-225 (localStorage+Set mirror pattern — verbatim shape to replicate for D-18)
    @src/ui/surfaces/chat/diagnostics-toggle.tsx (current session-only React state — extend with persistence)
    @src/ui/surfaces/chat/composer.tsx lines 191-200 (handleKeyDown extension point)
    @src/ui/surfaces/chat/context-rail.tsx lines 238-249 (Storage pin static card — wire to action)
    @src/ui/surfaces/chat/index.tsx lines 165-180 (diagnostics React state + ChatPageBody composition)
  </read_first>
  <behavior>
    **D-18 diagnostics persistence:**
    - DP1: DiagnosticsToggle accepts a topicId prop; on mount, reads `clarity:diagnostics:<topicId>` from localStorage and sets initial `armed` from `value === '1'`.
    - DP2: Toggling the button writes `'1'` or `'0'` to localStorage under the same key.
    - DP3: Topic A's localStorage state is independent of Topic B's (operator changes Topic A from OFF→ON; switching to Topic B preserves Topic B's OFF state).
    - DP4: localStorage failure (privacy mode → setItem throws) does not throw out of DiagnosticsToggle — try/catch swallows; the in-memory Set mirror still drives this-session state.
    - DP5: When topicId is null/undefined (no active topic), the toggle behaves as the rc.7 session-only state (graceful degrade).

    **D-19 composer shortcuts popover:**
    - SP1: Pressing `?` (Shift+/) inside an EMPTY textarea opens ComposerShortcutsPopover; the `?` character is NOT inserted (preventDefault).
    - SP2: Pressing `?` inside a NON-EMPTY textarea (length > 0) inserts the literal `?` character; popover does NOT open.
    - SP3: Pressing Escape while the popover is open closes it (focus returns to textarea).
    - SP4: Pressing any printable key while the popover is open closes it (textarea resumes focus; the key event is NOT consumed).
    - SP5: Popover lists shortcuts including at minimum `T` (open Create task) and `Enter / Shift+Enter` (send / newline). Item rows render as untrusted React text (no dangerouslySetInnerHTML).
    - SP6: Clicking outside the popover closes it (mousedown listener with the click-that-opened deferred via setTimeout(0), mirroring archive-panel.tsx open/close).

    **D-20 storage-pin UI wiring:**
    - SP-A: The context-rail Storage pin card becomes a `<button type="button">` with onClick dispatching `chat.topic.pin` with `pinned: !currentlyPinned`.
    - SP-B: Card visual state reflects pinned status — when `topic.pinnedAt !== null`, the card shows `📌 Pinned — exempt from archive` byline; otherwise the rc.7 copy.
    - SP-C: After pin toggle success, refetch `chat.topics` so the strip + rail re-read updated pinned_at.
    - SP-D: A toast fires `'Topic pinned'` / `'Topic unpinned'` (uses the existing showToast from ChatPageBody).
  </behavior>
  <action>
    Three composable UI surfaces.

    **(a) D-18 — Modify `src/ui/surfaces/chat/diagnostics-toggle.tsx`** to accept `topicId?: string | null` prop. Inside the component, mirror the localStorage pattern from topic-strip.tsx lines 199-225 verbatim with key shape `clarity:diagnostics:${topicId}`:
    - Initial state via lazy `React.useState(() => { try { return window.localStorage.getItem(key) === '1'; } catch { return false; } })`.
    - `onClick` callback wraps the parent's `onToggle` AND writes the new value to localStorage inside try/catch.
    - When `topicId` is null/undefined, skip the localStorage read/write entirely (graceful degrade per DP5).
    - Modify `src/ui/surfaces/chat/index.tsx` lines 165-180 — replace the single `const [diagnostics, setDiagnostics] = React.useState(false);` with a per-topic mapping. Simplest implementation: lift the localStorage state INTO the toggle itself (already done above) and remove the `diagnostics` React state from ChatPageBody. Pass `topicId={topic?.issueId ?? null}` to DiagnosticsToggle. The MessageThread's `diagnostics` prop is now derived from the toggle's internal armed state — easier: the toggle EMITS its state via `onToggle(nextArmed)` so the parent can still pipe `diagnostics={armedFromToggle}` into MessageThread. Keep the existing parent-controlled API; the toggle reads localStorage on mount to seed `armed` and writes on toggle.

    **(b) D-19 — Create `src/ui/surfaces/chat/shortcuts-popover.tsx`** exporting `ComposerShortcutsPopover`:
    - Props `{ open: boolean; onClose: () => void; anchorRef: React.RefObject<HTMLElement> }`.
    - Pure positioning: a small CSS-positioned div anchored above the composer textarea (mirror archive-panel positioning idiom — absolute + transform).
    - Renders a list of shortcuts:
      - `T` — Open Create task dialog
      - `Enter` — Send message
      - `Shift+Enter` — Insert newline
      - `?` (empty composer) — Open this popover
      - `Esc` — Close popover
    - All entries rendered as React text (NO dangerouslySetInnerHTML).
    - Click-outside + Escape handlers mirror archive-panel.tsx escape + outside-click pattern lines 91-112.

    **(c) Modify `src/ui/surfaces/chat/composer.tsx`** — extend `handleKeyDown` (lines 191-200):
    - Add `?` detection BEFORE the existing Enter check:
      - `if (e.key === '?' && !e.ctrlKey && !e.metaKey && !e.altKey) { const textarea = e.currentTarget; if (textarea.value.length === 0) { e.preventDefault(); setShortcutsPopoverOpen(true); return; } /* non-empty: fall through, native ? inserts */ }`
    - Add `[shortcutsPopoverOpen, setShortcutsPopoverOpen] = React.useState(false)`.
    - Add a ref `composerWrapperRef` on the composer container `<div className="composer">`.
    - Render `<ComposerShortcutsPopover open={shortcutsPopoverOpen} onClose={() => setShortcutsPopoverOpen(false)} anchorRef={composerWrapperRef} />` next to the textarea.
    - Add a window keydown listener (only while popover is open) that closes on Escape AND on any printable key (lets the operator immediately type — the printable-key path does NOT preventDefault, so the keystroke reaches the textarea).
    - Bind to the textarea's `onKeyDown`, NOT a global window listener (per cross_cutting_truths_for_this_plan — must not fire when operator literally types `?` in composer body unless body is empty).

    **(d) D-20 — Modify `src/ui/surfaces/chat/context-rail.tsx`** lines 238-249:
    - Replace the static `<div className="pin-row">📁 <b>{topic.title}</b>...</div>` block with a `<button type="button" onClick={handleTogglePin}>` containing the same icon/text but reflecting pinned state.
    - When `topic.pinnedAt` (a new optional field on the ChatTopic UI type — extend the local ChatTopic to include `pinnedAt?: string | null`) is non-null, render `📌 Pinned — exempt from archive` plus an Unpin affordance.
    - `handleTogglePin` calls `usePluginAction('chat.topic.pin')` with `{ companyId, userId, topicIssueId: topic.issueId, pinned: !currentlyPinned }`. On success, fire `showToast('Topic pinned')` / `('Topic unpinned')` AND bump a refresh key so chat.topics refetches (mirror Plan 04.1-08's `refreshKey` bump in ChatPageBody).
    - Thread `companyId`, `userId`, `showToast`, and the refresh-bumper as props from ChatPageBody to ContextRail (some are already threaded; add only what's missing).

    **(e) Wire pinned_at into ChatTopic UI type** — the chat surface's `chat.topics` handler payload needs `pinnedAt` added to its returned shape. Check `src/worker/handlers/chat-topics.ts` for the response mapping; if the topic row maps to a UI type, surface `pinned_at` as `pinnedAt: row.pinned_at ?? null`. Update any consumer type defs (`ChatTopic` in `src/ui/surfaces/chat/topic-strip.tsx` or shared types) to include `pinnedAt?: string | null`.

    **(f) Create 3 test files** under `test/ui/`:
    - `diagnostics-toggle-persistence.test.mjs` — Tests DP1-DP5 from <behavior>. Use a fake localStorage stub via `globalThis.localStorage` mock (or jsdom equivalent already in chat-url-params.test.mjs).
    - `composer-shortcuts-popover.test.mjs` — Tests SP1-SP6.
    - `context-rail-storage-pin.test.mjs` — Tests SP-A..SP-D. Mock `usePluginAction` to assert the dispatched action key + params.
  </action>
  <acceptance_criteria>
    <source>diagnostics-toggle.tsx + shortcuts-popover.tsx + composer.tsx + context-rail.tsx + 3 test files</source>
    <behavior>
      - All DP1-DP5 + SP1-SP6 + SP-A..SP-D tests pass.
      - Typing `?` in non-empty composer inserts literal character (SP2).
      - Typing `?` in empty composer opens popover and prevents default insertion (SP1).
      - Toggling diagnostics on Topic A and switching to Topic B preserves independent state across reload (simulated via localStorage stub).
      - Storage pin click dispatches chat.topic.pin with the inverted boolean.
      - check-css-scope + check-a11y stay green; no dangerouslySetInnerHTML introduced.
    </behavior>
    <cli>pnpm build && node --test test/ui/diagnostics-toggle-persistence.test.mjs test/ui/composer-shortcuts-popover.test.mjs test/ui/context-rail-storage-pin.test.mjs && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs</cli>
    <grep-gates>
      - grep -c "clarity:diagnostics:" src/ui/surfaces/chat/diagnostics-toggle.tsx >= 1
      - grep -c "ComposerShortcutsPopover" src/ui/surfaces/chat/composer.tsx >= 1
      - grep -c "chat.topic.pin" src/ui/surfaces/chat/context-rail.tsx >= 1
      - grep -v '^//' src/ui/surfaces/chat/shortcuts-popover.tsx | grep -c "dangerouslySetInnerHTML" == 0
      - grep -v '^//' src/ui/surfaces/chat/composer.tsx | grep -c "window\.addEventListener.*keydown" <= 1 (popover-window listener is conditional/scoped)
    </grep-gates>
  </acceptance_criteria>
  <verify>
    <automated>pnpm build && node --test test/ui/diagnostics-toggle-persistence.test.mjs test/ui/composer-shortcuts-popover.test.mjs test/ui/context-rail-storage-pin.test.mjs && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs</automated>
  </verify>
  <done>
    Diagnostics state survives reload per-topic; composer `?` popover triggers ONLY on empty textarea; storage pin card is live; ChatTopic UI type carries pinnedAt; no a11y / css-scope regression; no dangerouslySetInnerHTML introduced.
  </done>
</task>

<task type="auto">
  <name>Task 7: Version bump + full suite + manifest build smoke</name>
  <files>package.json, src/manifest.ts</files>
  <read_first>
    @package.json
    @src/manifest.ts lines 330-340 (the literal version field)
  </read_first>
  <action>
    Bump the plugin version per memory `plugin-version-bump-two-sources` — BOTH `package.json` AND `src/manifest.ts` MUST change in the SAME commit.

    **(a) Bump `package.json` "version"** from current `1.0.0-rc.7` → `1.0.0-rc.8`. (Plan 05-10 will do the final `1.0.0-rc.N` → `1.0.0` bump; this plan emits the next rc.)

    **(b) Bump `src/manifest.ts` `version: '1.0.0-rc.7'` literal (line 337)** → `version: '1.0.0-rc.8'`. The host reads dist/manifest.js (built from src/manifest.ts); bumping only one ships v_new code with a v_old label per the memory.

    **(c) Run the full test suite** to catch any cross-test regression:
    - `pnpm typecheck` clean.
    - `pnpm test` (node --test on every test file) — net suite count grows by approximately +30 tests across Tasks 1-6 (R1-R7 + Tests 1-14 + PE1-PE5 + AP1-AP7 + MS1-MS3 + CSH1-CSH5 + DP1-DP5 + SP1-SP6 + SP-A..SP-D ≈ 50; allow ±10 variance).
    - `pnpm build` (worker + UI + manifest) — all three clean.
    - `npm pack` produces `clarity-pack-1.0.0-rc.8.tgz`; capture the sha256.
    - `node scripts/check-css-scope.mjs` exit 0.
    - `node scripts/check-a11y.mjs` exit 0.

    NO operator drill on Countermoves in this plan — Plan 05-10 owns the closure drill per Plan-Outline. The rc.8 tarball stays on disk for the Wave-3 closure drill to pick up.
  </action>
  <acceptance_criteria>
    <source>package.json + src/manifest.ts + npm pack output + check-css-scope + check-a11y</source>
    <behavior>
      - package.json `"version"` field is `1.0.0-rc.8`.
      - src/manifest.ts `version:` literal is `'1.0.0-rc.8'`.
      - dist/manifest.js post-build reports `version: '1.0.0-rc.8'`.
      - Suite count grows by ~30-60 tests; 0 failures; pre-existing 2 skips preserved.
      - npm pack produces `clarity-pack-1.0.0-rc.8.tgz`.
      - check-css-scope + check-a11y both exit 0.
    </behavior>
    <cli>pnpm typecheck && pnpm test && pnpm build && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs && npm pack</cli>
    <grep-gates>
      - grep -c "1.0.0-rc.8" package.json == 1
      - grep -c "1.0.0-rc.8" src/manifest.ts == 1
      - grep -c "1.0.0-rc.7" package.json == 0 (old version fully replaced)
      - grep -c "1.0.0-rc.7" src/manifest.ts == 0 (in the version literal — comments referencing rc.7 lineage are fine; gate is scoped via `grep -n "version:.*1.0.0-rc" src/manifest.ts` showing only one line)
    </grep-gates>
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm test && pnpm build && node scripts/check-css-scope.mjs && node scripts/check-a11y.mjs && npm pack</automated>
  </verify>
  <done>
    Version bumped in both sources per memory; full suite green; tarball `clarity-pack-1.0.0-rc.8.tgz` packed and ready for the Wave-3 closure drill (Plan 05-10).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → plugin UI bundle | Same-origin trusted JS; plugin UI shares React tree with host (PLUGIN_SPEC.md §19). |
| Plugin UI → plugin worker (JSON-RPC over stdio via host) | All RPCs go through `usePluginAction` / `usePluginData`; no raw fetch from UI. |
| Plugin worker → plugin-namespace Postgres (`plugin_clarity_pack_cdd6bda4bd.*`) | DML only inside namespace via `ctx.db.execute`; reads via `ctx.db.query`. Host issue rows (public.issues) are read-only from plugin worker. |
| Plugin worker → host issues (`public.issues`) | READ-ONLY per CTT-07 invariant; no `ctx.issues.update` / `ctx.issues.delete*` allowed for D-15/D-16/D-20 paths. |
| Browser localStorage (D-18 diagnostics key) | Client-side only; topic-id is not sensitive (UUIDs are not secrets in this trust model per CONTEXT.md). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Spoofing | `chat.topic.pin` action | mitigate | wrapActionHandler enforces opt-in + companyId + userId checks (returns OPT_IN_REQUIRED on opted-out caller); same surface as `chat.topic.archive`. |
| T-05-08-02 | Tampering | Bulk-unarchive (`chat.topic.bulkUnarchive`) | mitigate | Plugin-namespace UPDATE only; `bulkSetChatTopicArchived` parameterised with `$1 = false` AND `issue_id = ANY($2::text[])` AND `company_id = $3` — operator cannot affect another company's topics; CTT-07 invariant unchanged (no host-issue mutation). |
| T-05-08-03 | Tampering | `chat.topic.archive` PIN_EXEMPT bypass | mitigate | Guard runs INSIDE the existing try block AFTER param validation; `isChatTopicPinned` is parameterised SELECT against plugin namespace; race window (operator pins then archives concurrently) is operator-only, not a multi-tenant concern; v1 audience = Eric. |
| T-05-08-04 | Repudiation | Bulk-unarchive audit | accept | v1 audience = Eric (single operator); plugin log emits `ctx.logger?.warn?.` on failure but no per-success audit trail. Acceptable trade for v1; revisit if multi-operator deployments emerge. |
| T-05-08-05 | Information disclosure | ArchivePage (company-scoped list) | mitigate | Three-gate composition (opt-in → companyId → userId) before any chat.archivedTopics fetch; worker handler additionally enforces opt-in via wrapDataHandler. NEVER renders raw UUIDs in operator-visible text (NO_UUID_LEAK CTT — display names resolved via chat.roster). |
| T-05-08-06 | Information disclosure | localStorage `clarity:diagnostics:<topic-id>` | accept | topic-id (UUID) is not sensitive in this trust model (CONTEXT.md D-18 rationale). Survives across sessions on shared machines — flag, but v1 audience of 1 ≠ shared machine concern. |
| T-05-08-07 | Denial of service | Bulk-unarchive size cap | accept | `topicIssueIds: string[]` size is bounded by the archive full-view's selection UI (operator can pick at most one screen of rows at a time); no explicit length cap in v1. Postgres handles `ANY($::text[])` arrays in the thousands without issue. Revisit if abuse vector emerges. |
| T-05-08-08 | Denial of service | Diagnostics localStorage spam | accept | Per-topic key shape with topic-id bound; no growth beyond `O(# of distinct topics the operator visits)`. Browser localStorage quota (~5MB) handles thousands of keys. |
| T-05-08-09 | Elevation of privilege | Cold-task button on non-chat surfaces | mitigate | TrueTaskDialog already enforces opt-in + user identity per Plan 04.1-02; the shared header doesn't bypass any gates — it just opens the same dialog. No new privilege surface introduced. |
| T-05-08-10 | Information disclosure | Composer `?` popover content | accept | Shortcut list is static text; no untrusted input rendered. No dangerouslySetInnerHTML (gate-checked). |
| T-05-08-11 | Tampering | Migration 0010 additive guarantee | mitigate | `ADD COLUMN IF NOT EXISTS pinned_at timestamptz DEFAULT NULL` only; no DROP / ALTER / TRUNCATE on `public.*`; idempotent; ddl-prefix-validator test ensures validator-conformant shape; coexistence guarantee #3 satisfied. |
| T-05-08-SC | Tampering | npm / pnpm installs | accept | No new external runtime dependencies introduced by this plan (Plan 05-04 owns react-markdown / SheetJS; this plan reuses existing primitives). devDeps unchanged. Plan 05-10 will run the final package-legitimacy audit at v1.0.0 ship. |
</threat_model>

<verification>
Phase-level checks across all 7 tasks:

- `pnpm typecheck` clean (TypeScript strict — all new types thread correctly through generic Ctx composition).
- `pnpm test` (node --test) — full suite passes; net +30..+60 new tests.
- `pnpm build:worker && pnpm build:ui && pnpm build:manifest` — all three clean.
- `node scripts/check-css-scope.mjs` exit 0 (every new selector scoped under `[data-clarity-surface="<name>"]`).
- `node scripts/check-a11y.mjs` exit 0 (all new <input>, <button>, <textarea> elements have accessible names; no dangerouslySetInnerHTML introduced).
- `node scripts/coexistence-checks/run-all.mjs` PASS — additive migration 0010 satisfies coexistence-09 (true-task) and any other CTT-07 / additive-schema gates.
- `npm pack` produces `clarity-pack-1.0.0-rc.8.tgz`; sha256 captured for the Plan 05-10 closure drill.
- Migration 0010 passes the validator (no standalone CREATE INDEX, no DO blocks, apostrophe-free comments, ends on semicolon).

Plan-level invariants enforced:

- **CTT-07** — `grep -v '^//' src/worker/handlers/chat-topic-pin.ts | grep -c 'ctx\.issues\.\(update\|delete\)' == 0`; same for chat-topic-bulk-unarchive.ts. Existing chat-topic-archive.ts gate stays at 0 (PIN_EXEMPT guard does NOT introduce host-issue mutation).
- **NO_UUID_LEAK** — ArchivePage tests assert that rendered rows show employee names (resolved server-side via chat.roster), never raw UUIDs (AP7).
- **Additive-only schema** — migration 0010 contains exactly one ALTER TABLE ADD COLUMN IF NOT EXISTS statement; no DROP/ALTER/TRUNCATE on public.*.
- **Plugin route pattern** — manifest entry uses `routePath: 'archive'`, NOT `routePath: 'clarity-pack/archive'` (correcting CONTEXT.md D-15 slip per memory `clarity-pack-plugin-page-routes`).
- **localStorage key shape** — `grep -c "clarity:diagnostics:" src/ui/surfaces/chat/diagnostics-toggle.tsx >= 1`.
- **React 19 peer-only** — package.json unchanged on React deps; no bundled React introduced.

Coexistence guarantees re-verified:

- #3 (additive schema) — migration 0010 ADD COLUMN IF NOT EXISTS only.
- #6 (clean uninstall preserves data) — pinned_at column lives in plugin namespace; survives disable; --purge is opt-in only.
</verification>

<success_criteria>
1. `migrations/0010_chat_topics_pinned.sql` lands; validator-conformant; idempotent; additive.
2. `chat_topics.pinned_at` column populated by `setChatTopicPinned(true)`, NULL after `setChatTopicPinned(false)`.
3. `chat.topic.pin` action handler ships with CTT-07 zero-host-issue-mutation invariant (Test 6 of chat-topic-pin.test.mjs proves zero `ctx.issues.update` invocations across both directions).
4. `chat.topic.archive` returns `{ error: 'PIN_EXEMPT' }` when caller tries to archive a pinned topic; un-archive direction unchanged.
5. `chat.topic.bulkUnarchive` accepts an array of topicIssueIds and flips them in a single round-trip; empty array → no DB call.
6. `chat.archivedTopics` accepts omitted `employeeAgentId` and returns the company-scoped listing; response shape grows `pinnedAt` field.
7. ArchivePage page-slot registered in manifest at `routePath: 'archive'`; route resolves to `/<companyPrefix>/archive`; three-gate composition gates the body.
8. Bulk-select checkboxes + `Selected (N) — Unarchive` button + single count toast `"${N} topics unarchived"` (no confirmation modal).
9. `+ Create task` button mounted in shared `ClaritySurfaceHeader` on Reader / Situation Room / Bulletin / Chat; clicking opens TrueTaskDialog cold-mode; no window keydown listener (no T-shortcut collision with actions-row).
10. DiagnosticsToggle persists per-topic in localStorage `clarity:diagnostics:<topic-id>`; survives reload; topic-independent; graceful degrade on null topicId.
11. ComposerShortcutsPopover opens on `?` keypress in EMPTY composer textarea; typing `?` in non-empty composer inserts literal character; ESC closes; click-outside closes.
12. Storage pin card in context-rail wires to `chat.topic.pin`; visual state reflects pinned status; refetches chat.topics on success.
13. Version bumped to `1.0.0-rc.8` in BOTH `package.json` AND `src/manifest.ts`.
14. Full suite green; pnpm build clean; check-css-scope + check-a11y green; `clarity-pack-1.0.0-rc.8.tgz` packed.
</success_criteria>

<output>
Create `.planning/phases/05-distribution-polish/05-08-SUMMARY.md` when done, recording:
- Atomic-commit lineage (one commit per task, 7 commits expected: migration+repo, two handlers + wiring, PIN_EXEMPT, ArchivePage+manifest, ClaritySurfaceHeader, D-18+D-19+D-20 UI, version bump).
- Suite delta (pre-test count → post-test count; failure count).
- Tarball sha256 for `clarity-pack-1.0.0-rc.8.tgz`.
- Any deviations from this plan (with [Rule N] tag per execute-plan conventions).
- Pointer to the next plan (Plan 05-10 closure if 05-04/05/06/07/09 are complete; otherwise the next Wave-1 / Wave-2 sibling).
</output>
