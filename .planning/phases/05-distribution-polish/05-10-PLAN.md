---
phase: 05-distribution-polish
plan: 10
type: execute
wave: 3
depends_on: ["05-04", "05-05", "05-06", "05-07", "05-08", "05-09"]
files_modified:
  - package.json
  - src/manifest.ts
  - pnpm-lock.yaml
  - .planning/phases/05-distribution-polish/05-VERIFICATION.md
  - .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: ["COEXIST-05", "DIST-01", "DIST-02", "DIST-03", "DIST-04", "DIST-05"]
user_setup:
  - service: npm
    why: "Publish clarity-pack@1.0.0 to public npm registry (DIST-01 final gate)"
    env_vars: []
    dashboard_config:
      - task: "Run `npm login` and verify `npm whoami` returns the operator account that owns the `clarity-pack` package name"
        location: "Local terminal in Clarity Pack repo root"
      - task: "Approve the publish (Task 9 gates on operator running `npm publish` after `npm publish --dry-run` is reviewed)"
        location: "Local terminal in Clarity Pack repo root"

must_haves:
  truths:
    - "package.json `version` and `src/manifest.ts` `version` literal both read `1.0.0` (byte-equal across the two source files); built `dist/manifest.js` reports `version: '1.0.0'`."
    - "`clarity-pack-1.0.0.tgz` exists at repo root; sha256 recorded in 05-VERIFICATION.md; `npm pack --dry-run` lists `dist/manifest.js`, `dist/worker.js`, `dist/ui/`, `migrations/`, `README.md` and nothing under `src/` or `test/`."
    - "Forward install `rc.7 → 1.0.0` on live Countermoves PASSES: `pnpm paperclipai plugin install` (via `~/clarity-pack/scripts/install-helper.sh /home/eric/clarity-pack-1.0.0.tgz` per memory `paperclipai-workspace-script-gotcha`) returns `status=ready version=1.0.0` and the plugin UUID `0d4fc40a-0541-4b67-8979-9d346cb9c07b` is preserved across the rc.7→1.0.0 transition."
    - "Coexistence guarantee #6 byte-identical re-verification PASSES: row counts of `plugin_clarity_pack_cdd6bda4bd.{chat_topics, chat_messages, chat_topic_tasks}` (and `tldrs`, `bulletins`, `chat_topic_tasks` per current Phase 5 schema state) before `pnpm paperclipai plugin disable` and after `pnpm paperclipai plugin enable` differ by ZERO rows (`diff` produces no output)."
    - "All five Reader↔Chat bridge closure-baseline paths from Phase 4.2 (RCB-01..RCB-07) PASS on Countermoves at 1.0.0 (no regression from rc.2 baseline)."
    - "All Phase 5 polish surfaces ship intact: Pin/Unpin toggle + pinned-chip flash-highlight (05-06); ref-chip hover-peek with description excerpt (05-05); paused-agent banner on chat header + Reader (05-05); archive full-view route `/<companyPrefix>/archive` renders (05-08); composer `?` shortcuts popover (05-08); previewer dispatch on xlsx / pdf / md / png (05-04)."
    - "`05-VERIFICATION.md` exists with: drill PASS/FAIL per requirement (DIST-01..DIST-05 + COEXIST-05 + every D-NN from 05-CONTEXT.md), tarball sha256 + bytes, suite count delta vs rc.7, COEXIST #6 byte-identical evidence (row-count diff stdout), explicit D-23 deviation note (`Rollback rehearsal SKIPPED — operator decision per CONTEXT.md D-23; Phase 1 bookend snapshot/restore loop is sole recovery path`)."
    - "REQUIREMENTS.md DIST-01..DIST-05 + COEXIST-05 all read `Implemented (Plan 05-NN — <one-liner>)`; traceability test `test/phases/05-traceability.test.mjs` PASSES against the flipped rows."
    - "ROADMAP.md Phase 5 entry marked `COMPLETE & VERIFIED 2026-05-NN` with date; STATE.md `milestone` flipped to `v1.0.0` (no -rc.N suffix) with `completed_phases` count incremented."
    - "MemPalace `clarity_pack/decisions/v1.0.0-shipped` drawer filed recording: tarball sha256, drill PASS verdict per path, D-23 deviation rationale, suite count, plugin UUID preservation evidence."
    - "`npm publish` gate is a clean operator handoff (Task 9): the gate task displays exact `npm login`, `npm publish --dry-run`, `npm publish`, and `npm view clarity-pack@1.0.0` invocations; the rest of the plan executes autonomously up to that gate."
  artifacts:
    - path: "package.json"
      provides: "version 1.0.0 source-of-truth for npm"
      contains: '"version": "1.0.0"'
    - path: "src/manifest.ts"
      provides: "version 1.0.0 source-of-truth for the host (read from dist/manifest.js at runtime)"
      contains: "version: '1.0.0'"
    - path: "clarity-pack-1.0.0.tgz"
      provides: "publishable tarball + drill input"
      min_lines: 0
    - path: ".planning/phases/05-distribution-polish/05-VERIFICATION.md"
      provides: "Phase 5 final closure record"
      min_lines: 60
    - path: ".planning/phases/05-distribution-polish/05-10-DRILL-LOG.md"
      provides: "Operator drill transcript with timestamps + raw command output excerpts"
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "DIST-01..DIST-05 + COEXIST-05 flipped to Implemented"
      contains: "Implemented (Plan 05-"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 marked COMPLETE & VERIFIED"
      contains: "Phase 5"
  key_links:
    - from: "package.json"
      to: "src/manifest.ts"
      via: "version literal — MUST agree byte-equal in the same commit"
      pattern: '"version": "1.0.0"'
    - from: "src/manifest.ts"
      to: "dist/manifest.js"
      via: "esbuild via `pnpm build` — the host reads dist/manifest.js, NOT package.json"
      pattern: "version: '1.0.0'"
    - from: ".planning/phases/05-distribution-polish/05-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "every DIST-NN + COEXIST-05 line in VERIFICATION cites the requirement ID + closing plan"
      pattern: "DIST-0[1-5]|COEXIST-05"
    - from: ".planning/phases/01-pre-install-safety/01-VERIFICATION.md"
      to: ".planning/phases/05-distribution-polish/05-VERIFICATION.md"
      via: "D-23 deviation note — Phase 1 bookend snapshot/restore loop is SOLE v1.0.0 recovery path"
      pattern: "D-23|bookend snapshot|Phase 1"
---

<objective>
This is the v1.0.0 final closure plan — the single gate that flips clarity-pack from `1.0.0-rc.N` to a published, drilled, and verified npm package. Plans 05-04..05-09 ship the last seven plans' worth of features, polish, and forward-defect fixes; this plan ships the version bump, the canonical Countermoves ALL-paths drill, the VERIFICATION.md, the REQUIREMENTS/ROADMAP flips, the MemPalace drawer, and the npm publish gate.

Purpose: Lock all Phase 5 requirements (DIST-01..DIST-05 + COEXIST-05) at the production tier, ship clarity-pack@1.0.0 to public npm, and record the closure with explicit acknowledgement of the D-23 operator deviation (rollback rehearsal SKIPPED — Phase 1 bookend snapshot/restore loop is the sole recovery path for the v1.0.0 ship).

Output:
- `clarity-pack-1.0.0.tgz` (published to npm by operator at the Task 9 gate)
- `package.json` + `src/manifest.ts` both at `1.0.0`
- `.planning/phases/05-distribution-polish/05-VERIFICATION.md` (Phase 5 closure record)
- `.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` (raw drill transcript)
- `.planning/REQUIREMENTS.md` DIST-01..DIST-05 + COEXIST-05 → Implemented
- `.planning/ROADMAP.md` Phase 5 → COMPLETE & VERIFIED
- `.planning/STATE.md` milestone → v1.0.0
- MemPalace `clarity_pack/decisions/v1.0.0-shipped` drawer
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

@.planning/phases/05-distribution-polish/05-CONTEXT.md
@.planning/phases/05-distribution-polish/05-PATTERNS.md
@.planning/phases/05-distribution-polish/05-PLAN-OUTLINE.md
@.planning/phases/05-distribution-polish/05-01-SUMMARY.md
@.planning/phases/05-distribution-polish/05-02-SUMMARY.md
@.planning/phases/05-distribution-polish/05-03-SUMMARY.md

@.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md
@.planning/phases/01-pre-install-safety/01-VERIFICATION.md

@CLAUDE.md
@runbook/operator-gotchas.md

<load_bearing_memories>
The executing agent MUST internalise these MemPalace drawers before proceeding:

- `plugin-version-bump-two-sources` — version lives in BOTH `package.json` AND `src/manifest.ts` (hardcoded literal). The host reads `dist/manifest.js` (built from `src/manifest.ts`), NOT `package.json`. Bumping only one ships v1.0.0 code with an rc.N label. Both MUST be bumped in the same commit.
- `paperclipai-workspace-script-gotcha` — `pnpm paperclipai ...` MUST be run from `~/paperclip` (cd first). `install` / `uninstall` / `list` / `--version` all subject to this. Only `~/clarity-pack/scripts/install-helper.sh` runs from outside `~/paperclip`.
- `countermoves-safety-cli-invocation` — `~/clarity-pack` on the VPS is a partial copy (no root package.json). Safety CLI runs via `node scripts/safety/cli.mjs`. Snapshot needs `--db-url` flag from `/etc/paperclip/db.env`.
- `countermoves-verify-install-drill` — verify also needs `--db-url`; pre-create `paperclip_restoring`; the `--gate-bypass` two-factor pattern; sibling config edits (port + the safety-critical connectionString swap).
- `feedback_vps-commands-self-contained` — every VPS bash block MUST start with the canonical env re-export. Never assume env vars survive across messages, SSH windows, or reconnects.
- `feedback_vps-drill-walkthrough-completeness` — build a complete pre-flight from operator-gotchas + memories + safety-CLI source BEFORE sending a VPS walkthrough; don't ship a happy-path and patch gaps reactively on a live box.
- `clarity-pack-plugin-page-routes` — plugin pages are `/<companyPrefix>/<routePath>` (e.g. `/COU/bulletin`, `/COU/situation-room`), NOT `/plugins/clarity-pack/...`. Used in drill URL construction.
- `paperclip-auth-pattern` — `auth login --instance-admin` + browser approval; token at `~/.paperclip/auth.json`; safety CLI smoke needs `--api-key=$TOKEN`. Don't set `PAPERCLIP_API_URL` env when running snapshot (breaks auth.json profile lookup).
- `paperclip-issue-url-pattern` — issue detail page is `/COU/issues/<identifier>` (NOT `/COU/<identifier>` — 404s).
</load_bearing_memories>

<interfaces>
<!-- Key contracts the executor needs. The version-bump invariant is the most load-bearing. -->

Version literal in `src/manifest.ts` (around line 337 per 05-PATTERNS.md):
```typescript
// CURRENT (rc.7):
version: '1.0.0-rc.7',

// TARGET (1.0.0 final):
version: '1.0.0',
```

Version literal in `package.json` (around line 3):
```json
{
  "name": "clarity-pack",
  "version": "1.0.0-rc.7",  // → "1.0.0"
  ...
}
```

Test pin in `test/manifest/chat-capabilities.test.mjs` may also contain a version-string assertion (it was bumped in Plan 04.2-04 0.9.3, Plan 05-02 rc.2, etc.). Search for the existing rc.7 literal — if found, bump it in the same commit. If absent, no test change needed beyond the source bumps.

Plugin namespace (Countermoves): `plugin_clarity_pack_cdd6bda4bd`
Plugin UUID (preserved across installs): `0d4fc40a-0541-4b67-8979-9d346cb9c07b`
Database (Countermoves): `paperclip_countermoves` (NOT `paperclip`)
SSH: `ssh -i $HOME/.ssh/countermoves_vps_ed25519 eric@82.29.197.74`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification — all sub-plans CODE-COMPLETE, suite GREEN, gates clean</name>
  <files>(read-only — no source edits in this task)</files>
  <read_first>
    - `.planning/phases/05-distribution-polish/05-04-SUMMARY.md` (must exist)
    - `.planning/phases/05-distribution-polish/05-05-SUMMARY.md` (must exist)
    - `.planning/phases/05-distribution-polish/05-06-SUMMARY.md` (must exist)
    - `.planning/phases/05-distribution-polish/05-07-SUMMARY.md` (must exist)
    - `.planning/phases/05-distribution-polish/05-08-SUMMARY.md` (must exist)
    - `.planning/phases/05-distribution-polish/05-09-SUMMARY.md` (must exist)
    - `package.json` (capture current version string for the rc.N → 1.0.0 delta)
    - `src/manifest.ts` (capture current version literal)
  </read_first>
  <acceptance_criteria>
    <source>05-CONTEXT.md D-26 ("Plans 05-01 / 05-02 / 05-03 stay closed"); 05-PLAN-OUTLINE.md depends_on row for 05-10.</source>
    <behavior>
      - All six SUMMARY.md files for Plans 05-04..05-09 exist on disk (file presence check).
      - `pnpm test` (full suite) exit code 0; record `(passing | failing | skipped)` triplet.
      - `pnpm tsc --noEmit` exit code 0.
      - `node scripts/check-css-scope.mjs` exit code 0.
      - `node scripts/check-a11y.mjs` exit code 0.
      - `node scripts/coexistence-checks/run-all.mjs` returns "10/10 PASS" (or N/N PASS if Plan 05-08 added an 11th check for storage-pin).
      - Current version in `package.json` and `src/manifest.ts` agree (byte-equal rc.N string).
      - Captured the rc.N value (`1.0.0-rc.X` for some X) for the Task 2 delta.
    </behavior>
    <cli>
      - `test -f .planning/phases/05-distribution-polish/05-04-SUMMARY.md && test -f .planning/phases/05-distribution-polish/05-05-SUMMARY.md && test -f .planning/phases/05-distribution-polish/05-06-SUMMARY.md && test -f .planning/phases/05-distribution-polish/05-07-SUMMARY.md && test -f .planning/phases/05-distribution-polish/05-08-SUMMARY.md && test -f .planning/phases/05-distribution-polish/05-09-SUMMARY.md && echo "ALL SIX SUMMARYS PRESENT"`
      - `pnpm test 2>&1 | tail -20 | grep -E "(pass|fail|skip)"`
      - `pnpm tsc --noEmit && echo "TSC CLEAN"`
      - `node scripts/check-css-scope.mjs && echo "CSS-SCOPE CLEAN"`
      - `node scripts/check-a11y.mjs && echo "A11Y CLEAN"`
      - `node scripts/coexistence-checks/run-all.mjs 2>&1 | tail -5`
      - `node -e "console.log(require('./package.json').version)"`
      - `grep -E "version:\\s*'1\\.0\\.0-rc\\.[0-9]+'" src/manifest.ts`
    </cli>
  </acceptance_criteria>
  <action>
    Before any source edits, confirm Plans 05-04..05-09 are all CODE-COMPLETE and the trunk is clean. Run the seven CLI checks above and record the output in working notes (will be folded into 05-10-DRILL-LOG.md at Task 7). If ANY check fails — missing SUMMARY, suite red, gate exit non-zero, or the two version sources disagree at the rc.N tier — STOP immediately and surface to the operator. Do NOT proceed to Task 2 with a dirty trunk; the v1.0.0 ship requires a green starting state. Record the rc.N value (likely `1.0.0-rc.7` if no addendum bumps shipped between rc.7 and now, but read the actual file) for the Task 2 delta.
  </action>
  <verify>
    <automated>node -e "const v = require('./package.json').version; const fs = require('fs'); const m = fs.readFileSync('src/manifest.ts', 'utf8'); const match = m.match(/version:\s*'([^']+)'/); if (!match) { console.error('manifest.ts version literal not found'); process.exit(1); } if (match[1] !== v) { console.error('mismatch: package.json=' + v + ' manifest.ts=' + match[1]); process.exit(1); } if (!/^1\.0\.0-rc\.\d+$/.test(v)) { console.error('not at rc.N starting state: ' + v); process.exit(1); } console.log('PRE-FLIGHT OK — starting at ' + v);"</automated>
  </verify>
  <done>All six SUMMARYs present; pnpm test green; tsc clean; check-css-scope clean; check-a11y clean; coexistence-checks N/N PASS; `package.json` and `src/manifest.ts` agree at a 1.0.0-rc.N value; rc.N value captured for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Version bump — single commit touching BOTH package.json AND src/manifest.ts (per memory plugin-version-bump-two-sources)</name>
  <files>package.json, src/manifest.ts, pnpm-lock.yaml, test/manifest/chat-capabilities.test.mjs (conditional — only if it contains the rc.N literal)</files>
  <read_first>
    - `package.json` (line 1-10 for the version field)
    - `src/manifest.ts` (full file — version literal is around line 337 per 05-PATTERNS.md)
    - `test/manifest/chat-capabilities.test.mjs` (grep for the current rc.N literal; if found, bump in same commit)
  </read_first>
  <acceptance_criteria>
    <source>Memory `plugin-version-bump-two-sources` (load-bearing); 05-CONTEXT.md D-27 (npm credentials gating); 05-PATTERNS.md §"Plan 05-10 — v1.0.0 final closure" §"Version bump".</source>
    <behavior>
      - `package.json` `version` field reads exactly `1.0.0` (no `-rc.N` suffix, no `-beta`, no pre-release tag).
      - `src/manifest.ts` `version:` literal reads exactly `'1.0.0'` (single-quoted, matching existing literal style).
      - `pnpm install` regenerated `pnpm-lock.yaml` to reflect the new `clarity-pack@1.0.0` self-reference (workspace metadata).
      - If `test/manifest/chat-capabilities.test.mjs` contained the rc.N literal, it now contains `1.0.0`. If it did NOT contain a version literal, leave it unchanged.
      - All four files are staged in a single commit with message `chore(05-10): version 1.0.0-rc.N -> 1.0.0 (v1.0.0 final)`.
      - `pnpm test` still passes (the version-string bump must not regress any pinned-version test).
      - `pnpm tsc --noEmit` still clean.
    </behavior>
    <cli>
      - `node -e "const v = require('./package.json').version; if (v !== '1.0.0') { console.error('package.json version is ' + v + ' not 1.0.0'); process.exit(1); } console.log('package.json OK');"`
      - `grep -cE "version:\\s*'1\\.0\\.0'" src/manifest.ts` (must output a number > 0)
      - `grep -cE "version:\\s*'1\\.0\\.0-rc\\." src/manifest.ts` (must output `0` — no residual rc.N literals)
      - `grep -cE "1\\.0\\.0-rc\\." test/manifest/chat-capabilities.test.mjs || true` (must output `0` — no residual rc.N in the capability test pin)
      - `pnpm test 2>&1 | tail -10 | grep -E "(pass|fail)"`
      - `git status --short` (must show only the four files: package.json, src/manifest.ts, pnpm-lock.yaml, and OPTIONALLY test/manifest/chat-capabilities.test.mjs)
    </cli>
  </acceptance_criteria>
  <action>
    Bump the version literal in both source files. Use the Edit tool on each file — do NOT regenerate either file from scratch. (1) In `package.json`, change `"version": "1.0.0-rc.N"` to `"version": "1.0.0"`. (2) In `src/manifest.ts`, find the line `version: '1.0.0-rc.N',` (around line 337) and change to `version: '1.0.0',`. (3) Run `pnpm install` to regenerate `pnpm-lock.yaml` with the new self-reference. (4) Run `grep -nE "1\\.0\\.0-rc\\." test/manifest/chat-capabilities.test.mjs` — if it matches, Edit that test file to replace the rc.N literal with `1.0.0`; if no match, leave it unchanged. (5) Run `pnpm test` and `pnpm tsc --noEmit` to confirm nothing regressed. (6) `git add` the modified files (specific paths — DO NOT use `git add .` or `git add -A`) and commit with the message above. This is ONE commit, not two — the two-source bump must be atomic so a future `git bisect` never lands on a half-bumped state.

    Memory `plugin-version-bump-two-sources` is the load-bearing rule: the host reads `dist/manifest.js` (built from `src/manifest.ts`), NOT `package.json`. Shipping with mismatched values means the install reports the wrong version. The grep gates above check both sources AND confirm zero residual `1.0.0-rc.` literals anywhere in those three files.
  </action>
  <verify>
    <automated>node -e "const v1 = require('./package.json').version; const fs = require('fs'); const m = fs.readFileSync('src/manifest.ts', 'utf8'); const match = m.match(/version:\s*'([^']+)'/); if (v1 !== '1.0.0') { console.error('package.json: ' + v1); process.exit(1); } if (!match || match[1] !== '1.0.0') { console.error('manifest.ts: ' + (match && match[1])); process.exit(1); } const residual = fs.readFileSync('src/manifest.ts', 'utf8').match(/1\.0\.0-rc\./); if (residual) { console.error('residual rc literal in manifest.ts'); process.exit(1); } console.log('VERSION BUMP OK — both sources at 1.0.0');"</automated>
  </verify>
  <done>Single commit on master flips both `package.json` and `src/manifest.ts` to `1.0.0` (no rc.N residuals); `pnpm-lock.yaml` regenerated; tests still pass; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Build + pack — produce clarity-pack-1.0.0.tgz; record sha256 + byte count</name>
  <files>dist/ (regenerated), clarity-pack-1.0.0.tgz (new)</files>
  <read_first>
    - `package.json` `scripts` section (confirm `build` + `pack` script shape)
    - `05-01-SUMMARY.md` (`prepublishOnly` guard runs `npm run build && npm run typecheck && npm test && node scripts/check-css-scope.mjs`)
    - `05-02-SUMMARY.md` (rc.2 tarball was 142,544 bytes for size-baseline)
    - `04.2-07-SUMMARY.md` (rc.7 tarball was 147,533 bytes — Phase 5 size baseline)
  </read_first>
  <acceptance_criteria>
    <source>05-CONTEXT.md (Plan 05-10 scope); 05-PATTERNS.md §"Plan 05-10 — v1.0.0 final closure".</source>
    <behavior>
      - `pnpm build` exits 0 and produces `dist/manifest.js`, `dist/worker.js`, `dist/ui/` (UI bundle).
      - `node -e "console.log(require('./dist/manifest.js').default?.version || 'NO_VERSION')"` reports `1.0.0` (the built manifest agrees with the source).
      - `npm pack` (or `pnpm pack`) produces `clarity-pack-1.0.0.tgz` at repo root.
      - Tarball size is in the range 140 kB to 160 kB inclusive (sanity check against rc.7 baseline of 147,533 bytes; large deltas indicate the previewer libs or react-markdown may have bloated the UI bundle beyond expectation OR something was missed).
      - Tarball SHA-256 is captured for the VERIFICATION.md write.
      - `npm pack --dry-run 2>&1` (or `tar -tzf clarity-pack-1.0.0.tgz`) lists `package/dist/manifest.js`, `package/dist/worker.js`, `package/dist/ui/`, `package/migrations/`, `package/README.md` — and contains NO files under `package/src/`, `package/test/`, `package/.planning/`, `package/sketches/`.
    </behavior>
    <cli>
      - `pnpm build 2>&1 | tail -10`
      - `node -e "const m = require('./dist/manifest.js'); const v = m.default?.version || m.version; if (v !== '1.0.0') { console.error('dist/manifest.js version is ' + v); process.exit(1); } console.log('dist/manifest.js OK');"`
      - `pnpm pack 2>&1 | tail -3` (or `npm pack 2>&1 | tail -3`)
      - `test -f clarity-pack-1.0.0.tgz && echo TARBALL_PRESENT`
      - `node -e "const fs = require('fs'); const st = fs.statSync('clarity-pack-1.0.0.tgz'); if (st.size < 140000 || st.size > 160000) { console.error('tarball size out of range: ' + st.size); process.exit(1); } console.log('SIZE OK ' + st.size);"`
      - `node -e "const crypto = require('crypto'); const fs = require('fs'); const h = crypto.createHash('sha256'); h.update(fs.readFileSync('clarity-pack-1.0.0.tgz')); console.log('SHA256 ' + h.digest('hex'));"`
      - `tar -tzf clarity-pack-1.0.0.tgz | grep -c "^package/src/"` (must output `0`)
      - `tar -tzf clarity-pack-1.0.0.tgz | grep -c "^package/test/"` (must output `0`)
      - `tar -tzf clarity-pack-1.0.0.tgz | grep -cE "^package/dist/(manifest\\.js|worker\\.js)$"` (must output `2`)
      - `tar -tzf clarity-pack-1.0.0.tgz | grep -c "^package/README.md$"` (must output `1`)
    </cli>
  </acceptance_criteria>
  <action>
    Build the production artifact: `pnpm build` regenerates `dist/`. Confirm the built `dist/manifest.js` reports `version: '1.0.0'` (this is what the Paperclip host actually reads at install time — Task 2's source bump alone is insufficient without a build). Then `pnpm pack` to produce `clarity-pack-1.0.0.tgz` at the repo root. Capture the SHA-256 and byte count via the `node -e "..."` one-liner above and stash them for Task 7 (VERIFICATION.md write). Run the four `tar -tzf` greps to confirm package contents — `src/` and `test/` must NOT be in the tarball (the `files` field in `package.json` + `.npmignore` should already enforce this from Plan 05-01); `dist/manifest.js` + `dist/worker.js` + `README.md` MUST be present. The `prepublishOnly` hook from Plan 05-01 will run `npm run build && npm run typecheck && npm test && node scripts/check-css-scope.mjs` as a side-effect of `pnpm pack` (if pack triggers it) or implicitly via the build step; if either gate fails, the pack does not happen and the operator is alerted. DO NOT use `--ignore-scripts` to bypass the gate; the gate is the point.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const crypto = require('crypto'); const st = fs.statSync('clarity-pack-1.0.0.tgz'); if (st.size < 140000 || st.size > 160000) { console.error('tarball size ' + st.size + ' out of [140000, 160000]'); process.exit(1); } const h = crypto.createHash('sha256'); h.update(fs.readFileSync('clarity-pack-1.0.0.tgz')); console.log('TGZ OK size=' + st.size + ' sha256=' + h.digest('hex'));"</automated>
  </verify>
  <done>`clarity-pack-1.0.0.tgz` exists at repo root with size in [140000, 160000] bytes; `dist/manifest.js` reports `version: '1.0.0'`; tarball contains no `src/` or `test/` entries; sha256 + byte count recorded for Task 7.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator drill on Countermoves — bookend snapshot, forward install, ALL-paths fixture sweep, COEXIST #6 byte-identical check (D-23 deviation applies: NO rollback rehearsal)</name>
  <files>(no source edits; operator runs VPS commands and reports outcome)</files>
  <read_first>
    - `runbook/operator-gotchas.md` (full file — bookend pattern; VPS gotchas; D-21 new `§vps-clarity-pack-scripts-sync` if Plan 05-09 shipped it)
    - `.planning/phases/01-pre-install-safety/01-VERIFICATION.md` (the loop D-23 is trusting)
    - `.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md` (RCB-01..RCB-07 closure-baseline drill template + COEXIST #6 row-count evidence pattern)
    - `.planning/phases/04.2-reader-chat-bridge/04.2-07-SUMMARY.md` (rc.7 drill outcome — 5 paths + Path 6 + Scenario 7; closure-baseline fixtures to re-drill)
    - `05-CONTEXT.md` D-23 (rollback rehearsal SKIPPED — explicit operator deviation)
  </read_first>
  <acceptance_criteria>
    <source>05-CONTEXT.md D-23 (rollback rehearsal SKIPPED); CLAUDE.md "Bookended-by-snapshots rule"; 04.2-07-SUMMARY.md drill template.</source>
    <behavior>
      - Bookend Postgres + filesystem snapshot taken on Countermoves BEFORE the rc.7 → 1.0.0 install (via `node scripts/safety/cli.mjs snapshot --db-url=$DB_URL ...` from `~/clarity-pack` per memory `countermoves-safety-cli-invocation`).
      - Pre-install plugin-namespace row counts captured: `chat_topics`, `chat_messages`, `chat_topic_tasks`, `tldrs`, `bulletins` (any Phase 5 table additions from 05-08 also included if `0010_chat_topics_pinned.sql` shipped — counts at the rc.7 tier may not yet have the column populated).
      - Plugin uninstall (`pnpm paperclipai plugin uninstall clarity-pack` from `~/paperclip` per memory `paperclipai-workspace-script-gotcha`); post-uninstall row counts re-captured. Pre-vs-post-uninstall: BYTE-IDENTICAL (`diff` produces no output). This is COEXIST #6.
      - Forward install: `~/clarity-pack/scripts/install-helper.sh /home/eric/clarity-pack-1.0.0.tgz` (the helper unpacks the npm tarball into a directory the host can install; `pnpm paperclipai plugin install <npm-tarball-path>` directly rejects with `Missing package.json` per memory `paperclips-issue-url-pattern` companion memory observed during rc.5 / rc.7 drills).
      - Post-install state confirmed: `pnpm paperclipai plugin list` from `~/paperclip` shows `version=1.0.0 id=0d4fc40a-0541-4b67-8979-9d346cb9c07b` (plugin UUID preserved across uninstall/install — historical evidence of this UUID surviving every transition since Plan 04.1-07).
      - Post-install plugin-namespace row counts captured. Pre-vs-post-install: BYTE-IDENTICAL. This is COEXIST #6's second half.
      - Full ALL-paths fixture sweep PASS:
        - **Phase 4.2 closure-baseline paths (5):** Paths 1-5 (assigned existing-topic + chat-spawned existing-topic + cold new-topic-needed + topic-itself + no-assignee — fixtures COU-2215 / COU-2361 / COU-2396 / COU-1115 / COU-2399 per 04.2-07-SUMMARY) — all PASS. Path 6 (silent resume + auto-unarchive on the COU-2441 fresh fixture or equivalent) — PASS. RCB-07 (pre-0009-topic spot-check on the existing CHT-NN topic; renders cleanly, no console error) — PASS.
        - **Phase 5 polish smoke:** Plan 05-04 previewer dispatch — open one Reader with an xlsx attachment, one with pdf, one with md, one with png; all four render via the new dispatcher, NOT the "Phase 5 (DIST-04)" placeholder. Plan 05-05 ref-chip hover-peek — hover over a ref-chip in Reader; peek card shows title + status + owner-name + description excerpt (NOT a UUID). Plan 05-05 paused-agent banner — pause a non-editor agent; banner appears on its chat header AND Reader top-of-tab. Plan 05-06 Pin/Unpin — click Pin on a chat message; clarity-pack toast "Message pinned"; click again; toast "Message unpinned"; both actions reversible without modal. Plan 05-06 pinned-chip flash — click a right-rail pinned-chip; thread scrolls + `.flash-highlight` animates for ~1.5s. Plan 05-08 archive route — navigate to `/COU/archive`; full-screen list renders; bulk-select + unarchive 2+ topics; toast `"N topics unarchived"`. Plan 05-08 composer `?` popover — empty composer + press `?` (Shift-/); inline shortcuts popover opens; ESC closes; literal `?` in mid-message body does NOT trigger.
        - **Plan 05-07 D8 Browser-Back:** click a Reader → Continue in chat (URL_HASH-carrier deep-link); chat surface lands correctly; press Browser Back; returns to Reader (NOT to a bare `/COU/chat`); press Browser Forward; returns to chat with topic still active (URL_HASH preserved).
      - **D-23 deviation explicitly NOT executed:** the `1.0.0 → rc.7 → 1.0.0` round-trip rollback rehearsal is NOT performed. The drill log notes this explicitly.
      - Operator records per-path verdict + console (DevTools) verdict + any new defects in the drill log; submits to the planning agent for VERIFICATION.md write.
    </behavior>
    <cli>
      <!-- All VPS commands MUST be self-contained per memory feedback_vps-commands-self-contained. -->
      <!-- The full canonical env re-export goes at the head of every block. -->

      Step 1 — SSH + canonical env re-export (RUN ONCE per SSH session; re-export if reconnected):
      ```
      ssh -i $HOME/.ssh/countermoves_vps_ed25519 eric@82.29.197.74
      # Inside the VPS:
      export DB_URL="$(sudo grep DATABASE_URL /etc/paperclip/db.env | cut -d= -f2-)"
      export API_URL="http://localhost:3100"
      export API_KEY="$(cat ~/.paperclip/auth.json | node -e "let s='';process.stdin.on('data',c=>s+=c).on('end',()=>console.log(JSON.parse(s).profiles[0].token))")"
      export COMPANY_ID="$(cd ~/paperclip && pnpm paperclipai company list 2>/dev/null | grep -iE 'COU|countermoves' | awk '{print $1}' | head -1)"
      echo "ENV: DB_URL=$(echo $DB_URL | sed 's/:.*@/:***@/') API_URL=$API_URL COMPANY_ID=$COMPANY_ID"
      ```

      Step 2 — Pre-flight: VPS clarity-pack scripts sync (D-21 from Plan 05-09):
      ```
      cd ~/clarity-pack && git pull
      ```

      Step 3 — Bookend snapshot:
      ```
      cd ~/clarity-pack && node scripts/safety/cli.mjs snapshot --db-url="$DB_URL" --api-key="$API_KEY"
      # Capture the snapshot ID and verify the bytes via the standard postgres + FS tar hash.
      ```

      Step 4 — Pre-install row counts:
      ```
      psql "$DB_URL" -c "SELECT 'chat_topics' as table_name, COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topics UNION ALL SELECT 'chat_messages', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_messages UNION ALL SELECT 'chat_topic_tasks', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topic_tasks UNION ALL SELECT 'tldrs', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.tldrs UNION ALL SELECT 'bulletins', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.bulletins;" > /tmp/pre-uninstall-counts.txt
      cat /tmp/pre-uninstall-counts.txt
      ```

      Step 5 — Plugin uninstall (data-preserving by default per coexistence #6 — DO NOT pass --purge):
      ```
      cd ~/paperclip && pnpm paperclipai plugin uninstall clarity-pack
      psql "$DB_URL" -c "SELECT 'chat_topics' as table_name, COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topics UNION ALL SELECT 'chat_messages', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_messages UNION ALL SELECT 'chat_topic_tasks', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topic_tasks UNION ALL SELECT 'tldrs', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.tldrs UNION ALL SELECT 'bulletins', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.bulletins;" > /tmp/post-uninstall-counts.txt
      diff /tmp/pre-uninstall-counts.txt /tmp/post-uninstall-counts.txt && echo "COEXIST #6 PASS (uninstall byte-identical)"
      ```

      Step 6 — SCP the 1.0.0 tarball to the VPS (run from local laptop, not the VPS):
      ```
      scp -i $HOME/.ssh/countermoves_vps_ed25519 clarity-pack-1.0.0.tgz eric@82.29.197.74:/home/eric/clarity-pack-1.0.0.tgz
      ```

      Step 7 — Forward install (via the install-helper, NOT raw `pnpm paperclipai plugin install <tgz>` which rejects with `Missing package.json`):
      ```
      ~/clarity-pack/scripts/install-helper.sh /home/eric/clarity-pack-1.0.0.tgz
      cd ~/paperclip && pnpm paperclipai plugin list | grep clarity-pack
      # Expect: version=1.0.0 id=0d4fc40a-0541-4b67-8979-9d346cb9c07b status=ready
      ```

      Step 8 — Post-install row counts + COEXIST #6 second half:
      ```
      psql "$DB_URL" -c "SELECT 'chat_topics' as table_name, COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topics UNION ALL SELECT 'chat_messages', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_messages UNION ALL SELECT 'chat_topic_tasks', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.chat_topic_tasks UNION ALL SELECT 'tldrs', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.tldrs UNION ALL SELECT 'bulletins', COUNT(*) FROM plugin_clarity_pack_cdd6bda4bd.bulletins;" > /tmp/post-install-counts.txt
      diff /tmp/pre-uninstall-counts.txt /tmp/post-install-counts.txt && echo "COEXIST #6 PASS (install byte-identical vs pre-uninstall baseline)"
      ```

      Step 9 — Browser drill (operator at the keyboard, https://countermoves.gl3group.com/COU/...):
      - Open each of the 5 closure-baseline fixtures (paths 1-5) per 04.2-07-SUMMARY drill template.
      - Open Path 6 (silent resume + auto-unarchive — fresh fixture).
      - Open Phase 5 polish surfaces (previewers / hover-peek / banner / Pin / pinned-chip flash / archive route / composer `?`).
      - Open RCB-07 pre-0009-topic (existing CHT-NN that pre-dates migration 0009).
      - Press Browser Back from a chat-after-deep-link to confirm D-13 (hash preserved; Back returns to Reader, not bare /COU/chat).
      - Record verdict per path (PASS / PARTIAL / FAIL); console (DevTools) verdict (tolerated noise from Vite HMR + chat-surface React-key warnings if 05-07 didn't ship the fixes, otherwise expect clean).

      Step 10 — Report verdicts to the planning agent (paste row-count outputs + per-path verdicts; the agent writes Task 5 + Task 7).

      **D-23 explicitly NOT EXECUTED:** Do NOT perform a `1.0.0 → rc.7 → 1.0.0` rollback rehearsal. Operator's call per CONTEXT.md.
    </cli>
  </acceptance_criteria>
  <what-built>
    Plans 05-04..05-09 polish + the v1.0.0 version bump + the production `clarity-pack-1.0.0.tgz`. The drill confirms all sub-plans land correctly on live Countermoves under the bookended-snapshots rule, that coexistence guarantee #6 (clean disable/uninstall preserves data byte-identical) holds for the rc.7 → 1.0.0 transition, and that no Phase 4.2 path regresses.
  </what-built>
  <how-to-verify>
    1. **Run the 10-step CLI sequence above on Countermoves** (canonical env re-export at the top of every fresh SSH session per memory `feedback_vps-commands-self-contained`).
    2. **Confirm the two `diff` invocations produce no output** (COEXIST #6 byte-identical PASS).
    3. **Confirm `plugin list` shows `version=1.0.0 id=0d4fc40a-...`** (forward install PASS + UUID preserved).
    4. **Visually drill all 5 closure-baseline paths + Path 6 + RCB-07 + Phase 5 polish surfaces in a browser** (paste verdict per path).
    5. **DevTools console verdict** (tolerated: Vite HMR WebSocket noise; pre-existing React-key warnings if 05-07 did not address every component. NEW warnings = report. NEW errors = FAIL.).
    6. **Browser Back from a deep-linked chat returns to Reader** (D-13 PASS — hash preserved per Plan 05-07).
    7. **`D-23 SKIPPED — rollback rehearsal not performed (operator decision per CONTEXT.md D-23)`** explicitly recorded in the drill log.
    8. **Submit verdicts** to the planning agent in the format:
       ```
       DRILL VERDICT: PASS / PARTIAL / FAIL
       Snapshot: <snapshot-id> bytes verified
       Pre-uninstall counts: <paste>
       Post-uninstall counts: <paste>  (matches pre-uninstall: yes/no)
       Post-install counts:   <paste>  (matches pre-uninstall: yes/no)
       Plugin list:           <paste>  (version=1.0.0, UUID preserved: yes/no)
       Path 1 (COU-2215):     PASS / PARTIAL / FAIL  <one-liner>
       Path 2 (COU-2361):     PASS / PARTIAL / FAIL  <one-liner>
       Path 3 (COU-2396):     PASS / PARTIAL / FAIL  <one-liner>
       Path 4 (COU-1115):     PASS / PARTIAL / FAIL  <one-liner>
       Path 5 (COU-2399):     PASS / PARTIAL / FAIL  <one-liner>
       Path 6 (silent resume): PASS / PARTIAL / FAIL  <one-liner>
       RCB-07 (pre-0009):     PASS / PARTIAL / FAIL  <one-liner>
       05-04 previewers:      PASS / PARTIAL / FAIL  <one-liner per kind: xlsx / pdf / md / png>
       05-05 hover-peek:      PASS / PARTIAL / FAIL  <one-liner>
       05-05 pause banner:    PASS / PARTIAL / FAIL  <one-liner>
       05-06 Pin/Unpin:       PASS / PARTIAL / FAIL  <one-liner>
       05-06 pinned-chip:     PASS / PARTIAL / FAIL  <one-liner>
       05-08 archive route:   PASS / PARTIAL / FAIL  <one-liner>
       05-08 composer ?:      PASS / PARTIAL / FAIL  <one-liner>
       05-07 D-13 Back:       PASS / PARTIAL / FAIL  <one-liner>
       Console:               CLEAN / TOLERATED-KNOWN-NOISE / NEW-WARNINGS-OR-ERRORS
       D-23 rollback rehearsal: SKIPPED (operator decision per CONTEXT.md D-23)
       New defects:           <list — empty if FULL PASS>
       ```
  </how-to-verify>
  <resume-signal>
    Type `drill-pass` if every path is PASS with no new defects (full closure can proceed); `drill-partial: <list>` if SOME paths PARTIAL/FAIL but the overall ship is acceptable (a Phase 5 gap-closure addendum may be needed); `drill-fail: <reason>` if the install or COEXIST #6 itself fails (closure HALTS — restore from bookend snapshot per Phase 1 loop per D-23, then planning re-opens to triage).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 5: Write 05-10-DRILL-LOG.md — raw drill transcript (executor folds operator-reported verdicts + command output into a permanent record)</name>
  <files>.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md</files>
  <read_first>
    - Task 4 operator output (the verdict block + raw row-count diff outputs)
    - `.planning/phases/04.2-reader-chat-bridge/04.2-07-SUMMARY.md` (drill log shape reference — version trail, paths, defects, console verdict pattern)
  </read_first>
  <acceptance_criteria>
    <source>Task 4 operator output; 04.2-07-SUMMARY.md as shape template.</source>
    <behavior>
      - File `.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` exists.
      - Frontmatter records: drill date (yyyy-mm-dd), pre-install version (1.0.0-rc.N), post-install version (1.0.0), tarball sha256, tarball byte count, snapshot ID, COEXIST #6 verdict, overall drill verdict (PASS / PARTIAL / FAIL).
      - Body contains: full env re-export block (with `***` masked secrets); pre-vs-post-uninstall row-count tables (paste of psql output + the `diff` outcome); pre-vs-post-install row-count tables + `diff` outcome; `plugin list` output line; per-path verdict table for all 8 closure-baseline paths + RCB-07 + the 7 Phase 5 polish smoke surfaces + D-13 D8 Browser-Back; console (DevTools) verdict with NEW/TOLERATED breakdown; explicit `D-23 SKIPPED` line with rationale; new-defects list (empty if PASS); operator-reported timestamps.
      - File ends with a `## Closure verdict` section recording the drill verdict + the recovery posture (`Recovery path: Phase 1 bookend snapshot/restore loop (D-23: rollback rehearsal SKIPPED for v1.0.0)`).
    </behavior>
    <cli>
      - `test -f .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md && echo PRESENT`
      - `grep -c "D-23" .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` (must output > 0 — the rehearsal-skip note is present)
      - `grep -c "COEXIST #6" .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` (must output > 0)
      - `grep -cE "Path [1-6]|RCB-07|previewers|hover-peek|pause banner|Pin/Unpin|archive route|composer|D-13" .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` (must output >= 10 — at least 10 distinct surfaces drilled)
      - `wc -l .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md | awk '{ if ($1 < 40) { print "TOO SHORT " $1; exit 1; } else { print "LEN OK " $1; } }'`
    </cli>
  </acceptance_criteria>
  <action>
    Take the operator's verdict block from Task 4 and fold it into a permanent drill log under `.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md`. Use the Write tool. Structure:

    1. **Frontmatter** — drill date, version trail (1.0.0-rc.N → 1.0.0), tarball sha256 + byte count from Task 3, snapshot ID from Task 4 Step 3, COEXIST #6 verdict from Task 4 Steps 5+8 (BYTE-IDENTICAL / NOT BYTE-IDENTICAL), overall drill verdict.
    2. **Environment** — paste the env re-export block from Task 4 Step 1 with secrets masked to `***`.
    3. **Snapshot** — snapshot ID + `node scripts/safety/cli.mjs snapshot` output excerpt (bytes verified).
    4. **VPS scripts sync** — record that the D-21 git pull ran clean (or note if it pulled new commits).
    5. **COEXIST #6 evidence** — paste pre-uninstall counts, post-uninstall counts, `diff` output (empty = PASS); same for post-install.
    6. **Plugin list** — paste the `plugin list` line confirming `version=1.0.0 id=0d4fc40a-...`.
    7. **Per-path drill table** — markdown table with columns Path / Fixture / Outcome / Notes; populate from operator verdicts.
    8. **Console (DevTools) verdict** — TOLERATED-KNOWN-NOISE (Vite HMR + any pre-existing warnings 05-07 didn't address) vs NEW warnings/errors.
    9. **D-23 deviation note** — verbatim block:
       ```
       ## D-23 Deviation — Rollback rehearsal SKIPPED for v1.0.0

       Per CONTEXT.md D-23 (operator's call): the `1.0.0 → rc.7 → 1.0.0` round-trip rollback rehearsal is NOT performed for the v1.0.0 ship. Rationale:
       - Dozens of drills (Phases 4.1, 4.2-01..04.2-04) have validated the Phase 1 bookend snapshot/restore loop.
       - The rollback rehearsal is incremental safety on top of an already-PASSED safety net.
       - v1 audience = Eric on BEAAA + Countermoves; speed > redundant verification.
       - Forward install (rc.7 → 1.0.0) IS in the drill; only the reverse rehearsal is dropped.

       Recovery path for v1.0.0 production incident: Phase 1 bookend snapshot/restore loop (see `.planning/phases/01-pre-install-safety/01-VERIFICATION.md`). MemPalace drawer `clarity_pack/decisions/v1.0.0-shipped` files this as a memorable operator call so v1.1+ ships don't accidentally re-introduce rehearsal as default.
       ```
    10. **New defects** — bulleted list (empty if FULL PASS).
    11. **Closure verdict** — explicit `PASS / PARTIAL / FAIL` + recovery posture.

    The log is verbatim raw evidence; the next task (Task 6) writes the structured VERIFICATION.md from it.
  </action>
  <verify>
    <automated>grep -c "D-23" .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md && grep -c "COEXIST #6" .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md && wc -l .planning/phases/05-distribution-polish/05-10-DRILL-LOG.md | awk '{ if ($1 >= 40) exit 0; else exit 1; }' && echo DRILL_LOG_OK</automated>
  </verify>
  <done>`05-10-DRILL-LOG.md` exists, ≥40 lines, contains the D-23 deviation block and COEXIST #6 evidence and per-path verdicts ≥10 surfaces.</done>
</task>

<task type="auto">
  <name>Task 6: Write 05-VERIFICATION.md — structured Phase 5 closure record (per-requirement verdict, traceability, tarball pin)</name>
  <files>.planning/phases/05-distribution-polish/05-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` (from Task 5)
    - `.planning/phases/04.2-reader-chat-bridge/04.2-VERIFICATION.md` (shape template — frontmatter + per-requirement section + version trail)
    - `.planning/REQUIREMENTS.md` (lines 156-167 + 294-300 — the requirement IDs to flip + their current Pending status)
    - `.planning/phases/05-distribution-polish/05-CONTEXT.md` (D-01..D-27 — every decision must be cited)
    - All six 05-NN-SUMMARY.md files (one-liner closure evidence per plan)
  </read_first>
  <acceptance_criteria>
    <source>04.2-VERIFICATION.md shape; 05-CONTEXT.md D-01..D-27; REQUIREMENTS.md Phase 5 rows; Task 5 drill log.</source>
    <behavior>
      - File `.planning/phases/05-distribution-polish/05-VERIFICATION.md` exists.
      - Frontmatter contains: `phase: 05-distribution-polish`, `verified: <yyyy-mm-dd>`, `status: verified` (or `gaps_found` if Task 4 returned PARTIAL/FAIL), `version_pin: 1.0.0`, `artifact: clarity-pack-1.0.0.tgz`, `artifact_bytes: <N>`, `artifact_sha256: <hash>`, `plans_delivered: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08", "05-09", "05-10"]`, `prior_version: 1.0.0-rc.N`, `coexist_6_verdict: byte-identical-pass`.
      - Body §"Phase Goal Verdict" cites the verbatim Phase 5 goal from ROADMAP.md and records the verdict.
      - Body §"Requirement-by-Requirement Verdict" lists every Phase 5 requirement:
        - DIST-01 (npm publish) — Implemented at Plan 05-01 (rc.1 prep) + Plan 05-10 (publish gate); evidence: published `clarity-pack@1.0.0` per Task 9 output OR `pending operator npm publish` if gate not yet executed.
        - DIST-02 (README install/opt-in/rollback/runbook) — Implemented at Plan 05-01; evidence: README.md sections.
        - DIST-03 (AC auto-status) — Implemented at Plan 05-03; evidence: `reader.ac.autostatus` handler + UI integration.
        - DIST-04 (previewers xlsx/pdf/md/png + visual-regression) — Implemented at Plan 05-04; evidence: `deliverable.preview` worker handler + Playwright sketch-regression.
        - DIST-05 (lockfile + a11y + visual-regression CI) — Implemented at Plan 05-02 (lockfile + a11y) + Plan 05-04 (visual-regression).
        - COEXIST-05 (clean uninstall preserves data + `--purge` opt-in) — Implemented at Plan 05-02 (CI gate `10-uninstall-runbook.mjs`) + re-verified by Task 4 drill COEXIST #6 byte-identical evidence.
      - Body §"D-NN decision audit" lists every D-NN from CONTEXT.md (D-01..D-27) and the plan + closure evidence (D-23 explicitly notes `SKIPPED — operator decision; Phase 1 bookend loop sole recovery path`).
      - Body §"Drill Evidence" cross-references 05-10-DRILL-LOG.md per path.
      - Body §"Tarball Pin" records the published clarity-pack-1.0.0.tgz sha256 + byte count.
      - Body §"Version Trail" recapitulates rc.1 → rc.2 → rc.5 → rc.6 → rc.7 → 1.0.0.
      - File ≥60 lines.
    </behavior>
    <cli>
      - `test -f .planning/phases/05-distribution-polish/05-VERIFICATION.md && echo PRESENT`
      - `grep -cE "^- \\*\\*(DIST-0[1-5]|COEXIST-05)" .planning/phases/05-distribution-polish/05-VERIFICATION.md` (must output 6 — every Phase 5 REQ has a verdict bullet)
      - `grep -cE "D-(0[1-9]|1[0-9]|2[0-7])" .planning/phases/05-distribution-polish/05-VERIFICATION.md` (must output >= 27 — every D-NN cited at least once; OK if multiple)
      - `grep -c "D-23" .planning/phases/05-distribution-polish/05-VERIFICATION.md` (must output >= 1 — explicit deviation note)
      - `grep -cE "1\\.0\\.0\\.tgz" .planning/phases/05-distribution-polish/05-VERIFICATION.md` (must output >= 1)
      - `wc -l .planning/phases/05-distribution-polish/05-VERIFICATION.md | awk '{ if ($1 < 60) exit 1 }' && echo LEN_OK`
    </cli>
  </acceptance_criteria>
  <action>
    Write `.planning/phases/05-distribution-polish/05-VERIFICATION.md` using the Write tool. Use `04.2-VERIFICATION.md` as the shape template — frontmatter block, narrative Phase Goal Verdict, per-requirement verdict bullets, then a D-NN audit section, drill evidence cross-reference, tarball pin, and version trail.

    For each of the 6 Phase 5 requirements (DIST-01..DIST-05 + COEXIST-05), write a `### <REQ-ID>: <name>` section with:
    - **Verdict:** ✅ DELIVERED / ⚠ PARTIAL / ❌ FAILED
    - **Closed by:** Plan(s) and version pin
    - **Worker / UI evidence:** specific files + line numbers
    - **Operator-tier evidence:** drill section ID from 05-10-DRILL-LOG.md
    - **Test evidence:** specific test files added/extended

    For the D-NN audit section, write a markdown table:
    | Decision | Plan | Evidence | Verdict |
    |----------|------|----------|---------|
    | D-01 xlsx server-side | 05-04 | `src/worker/handlers/deliverable-preview.ts` + drill xlsx PASS | ✅ |
    | ... | ... | ... | ... |
    | D-23 rollback rehearsal SKIPPED | 05-10 | Task 4 explicitly did NOT execute reverse rehearsal; Phase 1 bookend loop sole recovery path | ⊘ SKIPPED (operator decision) |
    | ... | ... | ... | ... |

    Fill in every D-NN row from D-01 through D-27. D-24 (Reader's deferred-message text) closure is the test update in Plan 05-04. D-25 (sketches frozen) closure is implicit — visual-regression baselines exist. D-26 (05-01/02/03 stay closed) closure is the unchanged SUMMARYs. D-27 (npm publish gating) closure is Task 9.

    Final §"Tarball Pin" block: sha256 + bytes from Task 3 (and the publish receipt from Task 9 if it ran in this session). §"Version Trail" lists every rc plus 1.0.0 final.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const c = fs.readFileSync('.planning/phases/05-distribution-polish/05-VERIFICATION.md', 'utf8'); const reqs = ['DIST-01', 'DIST-02', 'DIST-03', 'DIST-04', 'DIST-05', 'COEXIST-05']; for (const r of reqs) { if (!c.includes(r)) { console.error('missing ' + r); process.exit(1); } } if (!c.includes('D-23')) { console.error('missing D-23 deviation'); process.exit(1); } if (c.length < 3000) { console.error('too short ' + c.length); process.exit(1); } console.log('VERIFICATION OK');"</automated>
  </verify>
  <done>`05-VERIFICATION.md` exists ≥60 lines; all 6 Phase 5 REQ IDs have verdict sections; D-23 SKIPPED deviation explicitly noted; tarball sha256 + byte count recorded; D-NN audit table populated for D-01..D-27.</done>
</task>

<task type="auto">
  <name>Task 7: Flip REQUIREMENTS.md DIST-01..DIST-05 + COEXIST-05 to Implemented + add Phase 5 traceability test (RED→GREEN)</name>
  <files>.planning/REQUIREMENTS.md, test/phases/05-traceability.test.mjs</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` (current Pending rows at lines 294-300; coexistence-guarantee block lines 156-167)
    - `test/phases/04.1-traceability.test.mjs` (Plan 04.1-07 closure produced this — shape template for the new Phase 5 test)
    - `test/phases/04-traceability.test.mjs` (additional shape reference)
  </read_first>
  <acceptance_criteria>
    <source>04.1-07 closure pattern (test/phases/04.1-traceability.test.mjs); REQUIREMENTS.md Pending rows; 05-VERIFICATION.md per-requirement verdicts.</source>
    <behavior>
      - New file `test/phases/05-traceability.test.mjs` mirrors `04.1-traceability.test.mjs` shape: asserts every Phase 5 requirement (DIST-01..DIST-05 + COEXIST-05) row exists in REQUIREMENTS.md, is tagged `Phase 5`, status is `Implemented`, and the Implemented line references at least one Plan `05-NN` ID.
      - The test starts RED (pre-flip): run it once before editing REQUIREMENTS.md — confirm at least 5 of 6 fail (DIST-03 already Implemented per current state).
      - REQUIREMENTS.md updated: lines 296-300 (DIST-01, DIST-02, DIST-04, DIST-05, COEXIST-05) Pending → `Implemented (Plan 05-NN — <one-liner>)`. DIST-03 (line 298) is already Implemented — leave unchanged.
      - REQUIREMENTS.md lines 156-167 (coexistence-guarantee checklist) update DIST-01..DIST-05 + COEXIST-05 boxes from `[ ]` to `[x]`. (DIST-03 line 165 already shows `[x]` — verify.)
      - Test goes GREEN post-flip: `node --test test/phases/05-traceability.test.mjs` exits 0.
      - One commit: `feat(05-10): Phase 5 traceability — DIST-01..05 + COEXIST-05 → Implemented (RED→GREEN)` containing both the new test file AND the REQUIREMENTS.md edits.
    </behavior>
    <cli>
      - `test -f test/phases/05-traceability.test.mjs && echo NEW_TEST_PRESENT`
      - `node --test test/phases/05-traceability.test.mjs 2>&1 | tail -5` (must show all PASS after flip)
      - `grep -cE "Implemented \\(Plan 05-" .planning/REQUIREMENTS.md` (must output >= 6 — every Phase 5 REQ now has Implemented + 05-NN cite)
      - `grep -cE "^- \\[x\\] \\*\\*(DIST-0[1-5]|COEXIST-05)" .planning/REQUIREMENTS.md` (must output 6 — every REQ box checked)
      - `grep -cE "^\\| (DIST-0[1-5]|COEXIST-05) \\| Phase 5 \\| Pending" .planning/REQUIREMENTS.md` (must output 0 — no residual Pending Phase 5 rows)
    </cli>
  </acceptance_criteria>
  <action>
    Two-step RED→GREEN flip per the 04.1-07 closure pattern.

    **Step A — Write the RED test first** (`test/phases/05-traceability.test.mjs`). Mirror `test/phases/04.1-traceability.test.mjs`: use `node:test` + `node:assert/strict`; one `describe('Phase 5 traceability')` block; one `test('row exists for <REQ>')` per requirement (6 tests); the body reads REQUIREMENTS.md, finds the row, asserts the status column reads `Implemented`, asserts the Implemented line references at least one `Plan 05-NN`, asserts the row tags as `Phase 5`. Run `node --test test/phases/05-traceability.test.mjs` — expect 5 of 6 to FAIL (DIST-03 will pass; the other 5 will fail because Pending != Implemented).

    **Step B — Flip REQUIREMENTS.md**. Edit `.planning/REQUIREMENTS.md`:

    1. Lines 296-300, table rows for DIST-01 / DIST-02 / DIST-04 / DIST-05 / COEXIST-05 (line 294 for COEXIST-05 specifically): replace `Pending` with the format `Implemented (Plan 05-NN — <one-liner>)`. One-liners per the SUMMARY closures:
       - DIST-01: `npm-published clarity-pack@1.0.0; package.json paperclipPlugin points at dist/manifest.js / worker.js / ui/`
       - DIST-02: `README documents install + opt-in + rollback + runbook; landed Plan 05-01`
       - DIST-04: `previewers xlsx/pdf/md/png via deliverable.preview worker handler + native embed/img + react-markdown; visual-regression baseline locked; landed Plan 05-04`
       - DIST-05: `lockfile-audit + a11y workflows landed Plan 05-02; visual-regression workflow landed Plan 05-04; all three CI-gated`
       - COEXIST-05: `clean uninstall preserves data byte-identical; --purge opt-in documented; CI gate scripts/coexistence-checks/10-uninstall-runbook.mjs; re-verified Task 4 drill 2026-05-NN`

    2. Lines 156-167, the checklist block. Flip the `[ ]` boxes for DIST-01, DIST-02, DIST-04, DIST-05, COEXIST-05 to `[x]`. (DIST-03 is already `[x]`.)

    3. Run the test again — expect GREEN.

    Commit both files in a single commit: `feat(05-10): Phase 5 traceability — DIST-01..05 + COEXIST-05 → Implemented (RED→GREEN)`. Specific `git add` paths, NOT `git add .`.
  </action>
  <verify>
    <automated>node --test test/phases/05-traceability.test.mjs 2>&1 | tail -3 | grep -E "(pass|ok)" && grep -cE "^\\| (DIST-0[1-5]|COEXIST-05) \\| Phase 5 \\| Pending" .planning/REQUIREMENTS.md | awk '{ if ($1 == 0) print "NO_PENDING_PHASE_5"; else exit 1 }'</automated>
  </verify>
  <done>`test/phases/05-traceability.test.mjs` PASSES; all 6 Phase 5 REQ rows show `Implemented (Plan 05-NN — ...)` in REQUIREMENTS.md; coexistence-guarantee checklist shows `[x]` for all 6; commit landed.</done>
</task>

<task type="auto">
  <name>Task 8: Flip ROADMAP.md Phase 5 → COMPLETE & VERIFIED + update STATE.md milestone to v1.0.0 + file MemPalace closure drawer</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/ROADMAP.md` lines 163-188 (Phase 5 entry)
    - `.planning/STATE.md` lines 1-15 (frontmatter — milestone, milestone_name, progress)
    - `.planning/phases/05-distribution-polish/05-VERIFICATION.md` (from Task 6 — closure date + verdict)
    - `.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` (from Task 5 — closure verdict)
  </read_first>
  <acceptance_criteria>
    <source>04.1-07 / 04.2-04 closure pattern (ROADMAP + STATE flip); Task 4 drill verdict; Task 6 VERIFICATION.md.</source>
    <behavior>
      - ROADMAP.md Phase 5 entry annotated: `**Status:** COMPLETE & VERIFIED 2026-05-NN — clarity-pack@1.0.0 published to npm; Countermoves drill PASS; 6/6 requirements Implemented; see `.planning/phases/05-distribution-polish/05-VERIFICATION.md`.` (or PARTIAL with note if Task 4 came back PARTIAL).
      - ROADMAP.md Phase 5 success-criteria checklist items flipped `[ ]` → `[x]` where the Task 4 drill confirmed each.
      - STATE.md frontmatter: `milestone: v1.0.0` (no `-rc.N` suffix); `milestone_name: phase-5-shipped-v1.0.0-final`; `status: shipped` (or `gaps_found` if PARTIAL); `progress.completed_phases: 5` (was 4); `progress.completed_plans` incremented to reflect the 7 newly-closed plans (37 + 7 = 44 if all 7 closed; check current value first); `progress.percent` updated.
      - STATE.md body: new top-section block recording the v1.0.0 ship with tarball sha256, drill outcome, COEXIST #6 verdict, and `next_action: All Phase 5 requirements Implemented. clarity-pack@1.0.0 live on Countermoves and published to npm (Task 9 gate). Phase 6 (if planned) or wind-down.`
      - MemPalace drawer `clarity_pack/decisions/v1.0.0-shipped` filed via `mempalace_add_drawer` (wing=`clarity_pack`, room=`decisions`, silent_save=true per project config) recording: tarball sha256, drill PASS per path, D-23 deviation rationale, plugin UUID preservation evidence, suite count delta.
    </behavior>
    <cli>
      - `grep -c "COMPLETE & VERIFIED" .planning/ROADMAP.md` (must output >= 1 — Phase 5 closure annotation present; existing earlier-phase closures may also match, OK)
      - `grep -cE "Phase 5.*COMPLETE & VERIFIED" .planning/ROADMAP.md` (specifically Phase 5; must output >= 1; multiline OK via -P or by checking the section content)
      - `node -e "const fs = require('fs'); const yaml = require('yaml'); const c = fs.readFileSync('.planning/STATE.md', 'utf8'); const m = c.match(/^---\\n([\\s\\S]+?)\\n---/); const fm = yaml.parse(m[1]); if (fm.milestone !== 'v1.0.0') { console.error('milestone ' + fm.milestone); process.exit(1); } console.log('STATE milestone OK');" 2>&1` (or, if `yaml` not installed, a plain grep: `grep -E "^milestone: v1\\.0\\.0$" .planning/STATE.md`)
      - `grep -E "^milestone: v1\\.0\\.0$" .planning/STATE.md` (must output one line)
    </cli>
  </acceptance_criteria>
  <action>
    Three docs-edits.

    **A) ROADMAP.md** — Use the Edit tool to find Phase 5's `### Phase 5: Distribution & Polish` section (line 163). Add a `**Status:** COMPLETE & VERIFIED 2026-05-NN — ...` line under the **Goal** line. Flip the success-criteria checklist boxes that map to Task 4 drill outcomes. Reference `.planning/phases/05-distribution-polish/05-VERIFICATION.md` for evidence.

    **B) STATE.md** — Use the Edit tool on the frontmatter (lines 1-15): change `milestone:` from the current `v1.0.0-rc.7` to `v1.0.0`; update `milestone_name`, `status`, and bump `completed_phases` to 5 + `completed_plans` to the new count (current 37 + 7 newly closed plans this session = 44 if all 7 closed cleanly — verify against actual SUMMARY files). Update `last_updated` to today's ISO date. Then add a new top-section paragraph (after line 19 or wherever the most-recent paragraph is) recording the v1.0.0 ship with: tarball sha256, byte count, drill outcome from Task 4 (`PASS` / `PARTIAL` / `FAIL` plus key evidence — COEXIST #6 byte-identical, plugin UUID preserved across rc.7→1.0.0), D-23 deviation note, and `next_action` line.

    **C) MemPalace drawer** — Call the `mempalace_add_drawer` MCP tool with `wing=clarity_pack`, `room=decisions`, `title=v1.0.0-shipped`, and a body containing:
    - Tarball: `clarity-pack-1.0.0.tgz` <bytes> sha256 `<hash>`
    - Drill date: 2026-05-NN
    - Drill verdict: PASS (or PARTIAL, with caveats)
    - Coexistence #6: byte-identical PASS
    - Plugin UUID: `0d4fc40a-0541-4b67-8979-9d346cb9c07b` preserved across rc.7→1.0.0
    - D-23 deviation rationale: rollback rehearsal SKIPPED for v1.0.0; operator's call; Phase 1 bookend snapshot/restore loop is sole recovery path; default for v1.1+ to be re-evaluated
    - Suite count: <pre-Task-1> → <post-Task-7>
    - Plans delivered in v1.0.0 final: 05-04, 05-05, 05-06, 05-07, 05-08, 05-09, 05-10 (plus 05-01, 05-02, 05-03 already shipped at earlier rcs)

    Commit A + B together: `docs(05-10): Phase 5 COMPLETE & VERIFIED — v1.0.0 final closure (ROADMAP + STATE flip)`. The MemPalace drawer is filed via MCP, not via git.
  </action>
  <verify>
    <automated>grep -E "^milestone: v1\\.0\\.0$" .planning/STATE.md && grep -E "Phase 5.*COMPLETE & VERIFIED" .planning/ROADMAP.md && echo CLOSURE_DOCS_OK</automated>
  </verify>
  <done>ROADMAP.md Phase 5 shows COMPLETE & VERIFIED with date; STATE.md milestone is `v1.0.0`; STATE.md body has the closure paragraph; MemPalace `clarity_pack/decisions/v1.0.0-shipped` drawer filed.</done>
</task>

<task type="checkpoint:human-action" gate="blocking" flagged_for="eric">
  <name>Task 9: Operator npm publish gate — `npm login`, `npm publish --dry-run`, `npm publish`, `npm view clarity-pack@1.0.0` (D-27)</name>
  <files>(no source edits; operator runs npm CLI; publish receipt folded back into 05-VERIFICATION.md tarball-pin section)</files>
  <read_first>
    - `package.json` (confirm `"name": "clarity-pack"`, `"version": "1.0.0"`, `"publishConfig"` if present)
    - `clarity-pack-1.0.0.tgz` (the artifact to be published)
    - `.planning/phases/05-distribution-polish/05-01-SUMMARY.md` (Plan 05-01 set up `prepublishOnly` hook + `paperclipPlugin` field)
    - `runbook/operator-gotchas.md` §"npm publish" if it exists
  </read_first>
  <acceptance_criteria>
    <source>05-CONTEXT.md D-27 (npm publish requires Eric's npm credentials); DIST-01 final gate.</source>
    <behavior>
      - Operator runs `npm whoami` and confirms the authenticated account owns (or can publish to) the `clarity-pack` npm package name. If `npm whoami` errors (not logged in), operator runs `npm login` and re-runs.
      - Operator runs `npm publish --dry-run ./clarity-pack-1.0.0.tgz` (or `npm publish --dry-run` from repo root) and reviews the package contents preview. Confirms: only `dist/`, `migrations/`, `README.md`, and `package.json` are listed; NO `src/` or `test/` or `.planning/`.
      - Operator runs `npm publish ./clarity-pack-1.0.0.tgz` (or `npm publish` from repo root if the tarball is at root).
      - Operator runs `npm view clarity-pack@1.0.0` and confirms the publish receipt: `name`, `version: 1.0.0`, `dist.tarball` URL, `dist.shasum` matches the local tarball sha256 (the npm registry-side hash is sha1 of the tarball BYTES — confirm Eric understands this — but the body of the package matches).
      - Operator confirms via the browser: `https://www.npmjs.com/package/clarity-pack` shows version `1.0.0`.
      - Operator pastes the `npm view clarity-pack@1.0.0` JSON output back to the planning agent for the VERIFICATION.md tarball-pin update.
    </behavior>
    <cli>
      ```
      # Local terminal in repo root (NOT VPS — npm publish is local)
      npm whoami
      # If not logged in:
      # npm login
      # (browser-based; complete the flow)

      npm publish --dry-run ./clarity-pack-1.0.0.tgz
      # Review the file list. Confirm:
      #   - dist/manifest.js, dist/worker.js, dist/ui/* are listed
      #   - migrations/*.sql are listed
      #   - README.md is listed
      #   - NO src/, NO test/, NO .planning/, NO sketches/

      npm publish ./clarity-pack-1.0.0.tgz
      # Expect: + clarity-pack@1.0.0

      npm view clarity-pack@1.0.0
      # Expect: JSON with name, version: 1.0.0, dist.tarball URL, dist.shasum

      # Optional in-browser confirmation:
      # open https://www.npmjs.com/package/clarity-pack
      ```
    </cli>
  </acceptance_criteria>
  <what-built>
    `clarity-pack-1.0.0.tgz` produced by Task 3 + verified by Task 4. The publish gate is the LAST step — gating on operator npm credentials per D-27. Once `npm publish` completes, the package is live on the public npm registry and `pnpm paperclipai plugin install clarity-pack` (from anywhere on the internet, not just Countermoves) installs the same artifact that the drill validated.
  </what-built>
  <how-to-verify>
    1. **Run `npm whoami`** — confirm authenticated. If not, `npm login` (browser-based OAuth).
    2. **Run `npm publish --dry-run ./clarity-pack-1.0.0.tgz`** — review the file list against the acceptance bullets. If the list contains `src/`, `test/`, or `.planning/`, STOP — the tarball's `files` field needs a fix (Plan 05-01 territory; halt and surface).
    3. **Run `npm publish ./clarity-pack-1.0.0.tgz`** — confirm `+ clarity-pack@1.0.0` output.
    4. **Run `npm view clarity-pack@1.0.0`** — paste the JSON output back to the planning agent. The agent will fold the `dist.tarball` URL + `dist.shasum` into 05-VERIFICATION.md §"Tarball Pin".
    5. **(Optional) Browser-confirm** `https://www.npmjs.com/package/clarity-pack` shows 1.0.0.
  </how-to-verify>
  <resume-signal>
    Type `publish-done` followed by the `npm view clarity-pack@1.0.0` JSON output (or relevant excerpt — `dist.tarball`, `dist.shasum`, `time.1.0.0` timestamp). Planning agent folds the publish receipt into VERIFICATION.md and completes Phase 5 closure. If `publish-failed: <reason>`, halt; restore is not needed (publishing is idempotent up to the version being claimed; if the publish failed mid-flight, simply re-run).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local CI → npm public registry | Eric's npm credentials authorise package publish; published bytes are world-readable forever |
| local laptop → VPS (Countermoves) | SCP transfer of `clarity-pack-1.0.0.tgz` over SSH; integrity by ssh transport |
| VPS install-helper → Paperclip host plugin runtime | `install-helper.sh` unpacks the tarball and hands it to the host's plugin install pipeline; host writes to filesystem + runs migrations 0001..0010+ |
| Paperclip host → plugin namespace `plugin_clarity_pack_cdd6bda4bd` | Worker (clarity-pack@1.0.0) reads/writes plugin tables; never touches `public.*` mutate APIs (CTT-07 invariant) |
| MemPalace `clarity_pack` wing | Closure decisions stored as drawers for v1.1+ retrieval |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10-01 | Tampering | npm publish — wrong package, wrong version, or stale tarball | mitigate | Task 9 mandates `npm publish --dry-run` review of file list BEFORE `npm publish`; Task 3 confirms `dist/manifest.js` version matches `package.json` post-build; tarball sha256 captured Task 3 and re-confirmed in Task 9's `npm view` receipt |
| T-05-10-02 | Spoofing | npm publish — wrong account publishes `clarity-pack` | mitigate | Task 9 mandates `npm whoami` confirmation before publish; operator-driven gate; `flagged_for: eric` ensures the operator (not the agent) holds the credentials |
| T-05-10-03 | Information disclosure | Source / planning notes / VPS env leaked in npm package | mitigate | Task 3 acceptance criteria explicitly check `tar -tzf` output for absence of `src/`, `test/`, `.planning/`, `sketches/`; Task 9 dry-run is the final human review before publish |
| T-05-10-04 | Tampering | Version-bump mismatch — host reports rc.N while package.json says 1.0.0 | mitigate | Task 2 single-commit atomic bump of BOTH sources; Task 3 confirms built `dist/manifest.js` version; Task 1 pre-flight confirms the two sources start in agreement |
| T-05-10-05 | Repudiation | Forward install fails on Countermoves; operator cannot recover | mitigate | Task 4 mandates Phase 1 bookend snapshot BEFORE the rc.7 → 1.0.0 install; D-23 deviation explicitly acknowledged; recovery path = `node scripts/safety/cli.mjs restore` per Phase 1 loop |
| T-05-10-06 | Tampering | COEXIST #6 violation — uninstall destroys data | mitigate | Task 4 mandates `pnpm paperclipai plugin uninstall` WITHOUT `--purge` flag; pre/post row counts diff'd byte-identical; FAIL = drill halts |
| T-05-10-07 | Denial of service | npm publish takes hostile dependency, blocks future publishes | accept | clarity-pack has no transitive untrusted deps (audit covered Plan 05-02); npm package-name squatting is not a v1 threat; v1.1+ may add npm provenance signing |
| T-05-10-08 | Elevation of privilege | Same-origin trust model — published plugin gains host privileges | accept | Documented in PLUGIN_SPEC.md §19; out of clarity-pack's scope to tighten; coexistence guarantees + CTT-07 invariants are the defense-in-depth Eric relies on |
| T-05-10-SC | Tampering | Supply-chain — npm install pulls compromised dependency at the consumer side | mitigate | Plan 05-02 shipped `lockfile-audit.yml` GitHub workflow (`npm audit --audit-level=high --omit=dev`); RESEARCH.md Package Legitimacy Gate (if present) applies; no NEW packages added by Plan 05-10 itself — this task only ships the version bump + publish + verification, not new deps |

## D-23 acknowledgement (operator deviation)

The decision to SKIP the `1.0.0 → rc.7 → 1.0.0` rollback rehearsal removes an incremental verification on top of an already-PASSED Phase 1 safety net. The threat is operator confidence — has Phase 1's loop actually been exercised enough? Evidence: Phase 4.1, 4.2-01, 4.2-02, 4.2-03, 4.2-04 drills (≥5 instances) all used the bookend pattern; one of them (Plan 04.2-01 `verify` step) explicitly rehearsed the rollback path against the same VPS and confirmed it works. The decision is operator-bounded and recorded as a memorable decision in MemPalace so v1.1+ doesn't accidentally adopt skip-by-default.
</threat_model>

<verification>

## Plan-level verification (executed at task completion, not in lieu of operator drill)

1. **Pre-flight gates clean (Task 1):** `pnpm test`, `pnpm tsc --noEmit`, `check-css-scope`, `check-a11y`, `coexistence-checks/run-all.mjs` all return zero exit.
2. **Atomic version bump (Task 2):** `package.json` + `src/manifest.ts` both at `1.0.0`, single commit; `grep -cE "1\\.0\\.0-rc\\." src/manifest.ts package.json` returns `0`.
3. **Tarball gates (Task 3):** `clarity-pack-1.0.0.tgz` exists, size in [140000, 160000] bytes, `dist/manifest.js` reports `1.0.0`, no `src/` or `test/` in tarball.
4. **Operator drill PASS (Task 4):** COEXIST #6 byte-identical pre/post uninstall + pre/post install; all 5 Phase 4.2 closure-baseline paths + RCB-07 + Phase 5 polish surfaces PASS; D-23 explicitly NOT executed.
5. **Drill log + VERIFICATION present (Tasks 5+6):** `05-10-DRILL-LOG.md` ≥40 lines, `05-VERIFICATION.md` ≥60 lines with all 6 REQ verdicts + D-NN audit + D-23 deviation note.
6. **REQUIREMENTS flipped (Task 7):** `test/phases/05-traceability.test.mjs` PASSES; all 6 Phase 5 rows read `Implemented (Plan 05-NN — ...)`.
7. **ROADMAP + STATE flipped (Task 8):** ROADMAP Phase 5 marked COMPLETE & VERIFIED; STATE milestone `v1.0.0`; MemPalace drawer filed.
8. **npm publish gate (Task 9):** `npm view clarity-pack@1.0.0` returns the published package; receipt folded into VERIFICATION.md tarball-pin section.

## Closure verdict

This plan PASSES when:
- Every task acceptance criterion above is met.
- Task 4 operator drill returns `drill-pass` (full PASS) OR `drill-partial` with operator-acknowledged caveats explicitly recorded in 05-VERIFICATION.md.
- Task 9 returns `publish-done` with `npm view clarity-pack@1.0.0` receipt.

This plan PARTIAL-PASSES when:
- Task 4 returns `drill-partial` with non-blocking defects.
- A gap-closure addendum (Plan 05-11 or similar) is filed for the defects but v1.0.0 ships anyway.

This plan FAILS when:
- Task 1 pre-flight is dirty → halt, Phase 5 not ready to close.
- Task 4 drill returns `drill-fail` → restore Countermoves via Phase 1 bookend; halt; planning re-opens to triage.
- Task 9 publish fails → version remains in source (1.0.0) but not published; operator can re-run `npm publish` after fixing the npm-side issue (Task 9 is idempotent on the version literal).

</verification>

<success_criteria>

Phase 5 is COMPLETE when:

- [ ] `package.json` + `src/manifest.ts` both at `1.0.0` (atomic commit; built `dist/manifest.js` agrees).
- [ ] `clarity-pack-1.0.0.tgz` exists at repo root; sha256 + byte count captured.
- [ ] `clarity-pack@1.0.0` published to public npm registry (Task 9 receipt).
- [ ] Countermoves drill PASS at 1.0.0: forward install `rc.7 → 1.0.0` clean; COEXIST #6 byte-identical; plugin UUID preserved; all 5 Phase 4.2 closure-baseline paths + Path 6 + RCB-07 + all Phase 5 polish surfaces PASS in the browser.
- [ ] `.planning/phases/05-distribution-polish/05-VERIFICATION.md` written; per-requirement verdicts; D-NN audit; D-23 deviation note explicit.
- [ ] `.planning/phases/05-distribution-polish/05-10-DRILL-LOG.md` written; raw drill transcript.
- [ ] `.planning/REQUIREMENTS.md` DIST-01..DIST-05 + COEXIST-05 = Implemented; `test/phases/05-traceability.test.mjs` PASSES.
- [ ] `.planning/ROADMAP.md` Phase 5 = COMPLETE & VERIFIED 2026-05-NN.
- [ ] `.planning/STATE.md` milestone = `v1.0.0` (no -rc.N suffix); `completed_phases` incremented.
- [ ] MemPalace `clarity_pack/decisions/v1.0.0-shipped` drawer filed (tarball sha256, drill verdict, D-23 deviation rationale).
- [ ] All operator gates resumed (`drill-pass` + `publish-done`).

</success_criteria>

<output>
Create `.planning/phases/05-distribution-polish/05-10-SUMMARY.md` when done. Use the standard SUMMARY template — frontmatter with `phase`, `plan: 10`, `status: closed`, `version_shipped: 1.0.0`, `requirements_closed: [DIST-01, DIST-02, DIST-04, DIST-05, COEXIST-05]` (note: DIST-03 already closed at Plan 05-03 rc.5; this plan re-verifies it but does not close it), tarball sha256 + bytes; body sections "What was built" / "Commits" / "Quality gates" / "Operator drill" / "D-23 deviation" / "Tarball" / "MemPalace drawer" / "Phase 5 closure".
</output>
