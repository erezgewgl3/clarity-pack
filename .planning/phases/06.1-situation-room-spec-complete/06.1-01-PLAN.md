---
phase: 06.1-situation-room-spec-complete
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0013_clarity_agent_owners.sql
  - src/worker/db/clarity-agent-owners-repo.ts
  - src/worker/handlers/agent-take-ownership.ts
  - src/worker/jobs/situation-snapshot.ts
  - src/worker.ts
  - test/worker/handlers/agent-take-ownership.test.mjs
  - test/ctt07/agent-take-ownership-no-issue-update.test.mjs
autonomous: true
requirements: [ROOM-09]
tags: [worker, migration, owner-resolution, situation-room]
must_haves:
  truths:
    - "D-01: clarity_agent_owners schema = flat upsert (agent_id PK, ON CONFLICT (agent_id) DO UPDATE — last write wins). Migration 0013 plugin-namespace-only additive."
    - "D-08: agent.takeOwnership viewer-authority follows Plan 04-04 T-04-16 (server-side re-verify viewer is opted-in AND agent belongs to caller's company). Side-table includes company_id discriminator (4 columns total: agent_id PK, owner_user_id, company_id, set_at)."
    - "Migration 0013 creates plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners with agent_id PK + owner_user_id + company_id + set_at"
    - "agent.takeOwnership handler re-verifies viewer authority server-side (ownerUserId === userId AND ctx.agents.get returns non-null in caller's company)"
    - "situation-snapshot owner-resolution consults clarity_agent_owners FIRST and falls back to host agent owner_user_id"
    - "Chain leaves that previously resolved to __unowned__ now resolve to the side-table owner_user_id when a row exists"
    - "Zero ctx.issues.update calls in agent-take-ownership.ts (runtime spy + source-grep both pin this)"
  artifacts:
    - path: "migrations/0013_clarity_agent_owners.sql"
      provides: "Additive plugin-namespace clarity_agent_owners table (4 columns: agent_id PK, owner_user_id, company_id, set_at)"
      contains: "CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners"
    - path: "src/worker/db/clarity-agent-owners-repo.ts"
      provides: "Typed CRUD wrapper: upsertClarityAgentOwner + listClarityAgentOwnersForCompany"
      exports: ["ClarityAgentOwnersRepoCtx", "upsertClarityAgentOwner", "listClarityAgentOwnersForCompany"]
    - path: "src/worker/handlers/agent-take-ownership.ts"
      provides: "agent.takeOwnership action handler wrapped by wrapActionHandler"
      exports: ["AgentTakeOwnershipCtx", "registerAgentTakeOwnership"]
    - path: "test/worker/handlers/agent-take-ownership.test.mjs"
      provides: "10 tests including CTT-07 runtime spy at Test 10"
      contains: "issueUpdateCalls.length === 0"
    - path: "test/ctt07/agent-take-ownership-no-issue-update.test.mjs"
      provides: "CTT-07 source-grep companion (defense-in-depth)"
  key_links:
    - from: "src/worker.ts"
      to: "agent-take-ownership.ts"
      via: "registerAgentTakeOwnership(ctx) call in setup"
      pattern: "registerAgentTakeOwnership"
    - from: "src/worker/jobs/situation-snapshot.ts"
      to: "clarity-agent-owners-repo.ts"
      via: "listClarityAgentOwnersForCompany call before buildIdLookup at line 253-260"
      pattern: "listClarityAgentOwnersForCompany"
    - from: "src/worker/handlers/agent-take-ownership.ts"
      to: "clarity-agent-owners-repo.ts"
      via: "upsertClarityAgentOwner call"
      pattern: "upsertClarityAgentOwner"
---

<objective>
Land the worker-tier owner-resolution path for ROOM-09: additive plugin-namespace migration 0013 (`clarity_agent_owners`), typed repo wrapper, `agent.takeOwnership` action handler with server-side viewer-authority re-check (T-04-16 pattern), and the side-table-wins owner merge into `situation-snapshot.ts`'s lookup-map construction. After this plan ships, a snapshot recompute that previously emitted `HUMAN_ACTION_ON(__unowned__)` for a claimed agent now emits `HUMAN_ACTION_ON(<real_user_id>)` automatically — `humanize-snapshot.ts` becomes a no-op rewrite for those rows. CTT-07 invariant pinned by construction (runtime spy + source-grep).

Purpose: Close the load-bearing half of the rc.8 Phase B Playwright drill finding — "every row reads no owner assigned" because the leaf hop has no owner. The chain walk in `blocker-chain.ts` and the 4-terminal classification are correct already; the fix is owner resolution at the leaf, NOT in the chain logic.

Output: Migration + repo + handler + snapshot integration + 10-test runtime spy + CTT-07 source-grep companion test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md
@CLAUDE.md

<interfaces>
Key contracts the executor needs (do not explore the codebase to find these — they are reproduced verbatim from RESEARCH.md):

From `node_modules/@paperclipai/plugin-sdk/dist/types.d.ts` (PluginAgentsClient at lines 1110-1137):
- `ctx.agents.get(agentId: string, companyId: string): Promise<Agent | null>` — returns null when agentId is not in this company (the T-04-16 viewer-authority gate).
- `ctx.agents.list({ companyId }): Promise<Agent[]>` — already used in situation-snapshot.ts:247.

From `src/worker/opt-in-guard.ts:108-124` (existing):
- `wrapActionHandler(ctx, key: string, fn: (params) => Promise<unknown>): void` — wraps action handler with opt-in gate. Opted-out callers receive `{ error: 'OPT_IN_REQUIRED' }` before body runs.

From `src/worker/handlers/chat-promote.ts` (T-04-16 viewer-authority pattern, Plan 04-04):
- Pattern: server-side re-verification that the caller belongs to the company AND owns the target entity before the write.
- Convention: action handlers RETURN `{ error: '<CODE>' as const, ...details }` envelope for recoverable errors; THROW for missing-param validation (opt-in-guard's wrapActionHandler catches throws and routes through JSON-RPC error channel).

From `src/worker/db/chat-topics-repo.ts:1-29` + `insertChatTopic` at lines 154-183:
- `ctx.db.execute(sql, params): Promise<{ rowCount }>` returns ONLY rowCount — after INSERT/UPDATE, do a SELECT to read back.
- `ctx.db.query(sql, params): Promise<Row[]>` for reads.

From `src/worker/jobs/situation-snapshot.ts` lines 253-260 (the load-bearing modify point):
```ts
const lookup: IdLookup = buildIdLookup({
  agents: employees as unknown as Array<{ user_id?: string; id?: string; role?: string }>,
  users: [],
});
```
This is where the side-table-wins owner merge inserts. Per RESEARCH.md §Pattern 5, the recommended approach is BEFORE-the-walk: build a `Map<agentId, ownerUserId>` from `(sideTableOwners ∪ employees.map(e => [e.id, e.owner_user_id]))` (side-table wins), then in `buildEmployeeRow` after the relations walk populates `nodeMeta`, do one final pass setting any null `nodeMeta[id].ownerUserId` from the map. The blocker-chain walk then sees populated owners and never emits `__unowned__` for claimed agents.

From `src/shared/blocker-chain.ts:178` (LOCKED INVARIANT — DO NOT MODIFY):
- The `__unowned__` literal string is written by the walk when `meta.ownerUserId` is null at a `HUMAN_ACTION_ON` leaf. The fix lives in pre-population of `meta.ownerUserId`, NOT in modifying this fallback.

From `migrations/0011_chat_message_attachments.sql` (canonical CREATE TABLE template):
- Fully-qualified schema name `plugin_clarity_pack_cdd6bda4bd.<table>` literal in every non-COMMENT statement.
- DDL-only statements (no UPDATE/DELETE — host validator rejects).
- No standalone `CREATE INDEX ... ON schema.table` (host extractQualifiedRefs has no pattern for this; rely on PRIMARY KEY).
- Apostrophe-free in `--` comments (greedy string-literal stripper hazard).
- File ends on a semicolon-terminated statement (no trailing comment).
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → worker action dispatch | Untrusted: UI can pass any `ownerUserId`; handler MUST re-verify it equals `userId` from the request context. |
| UI → worker → host SDK | `ctx.agents.get(agentId, companyId)` is the viewer-authority gate; companyId is supplied by host bridge per request. |
| Worker → plugin-namespace SQL | Trusted at this boundary IFF parameterized; all `clarity_agent_owners` writes use `$1, $2, ...` bind parameters. |
| Migration → host validator | Untrusted at install time: validator rejects `UPDATE`/`DELETE` statements; only CREATE/ALTER/COMMENT allowed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Spoofing | `agent.takeOwnership` ownerUserId param | mitigate | Handler refuses any request where `ownerUserId !== userId` (resolved server-side via opt-in-guard); returns `{ error: 'OWNER_MISMATCH' }`. Test 3 pins this. |
| T-06.1-02 | Elevation of Privilege | Cross-company agent claim | mitigate | `ctx.agents.get(agentId, companyId)` returns null when agent belongs to a different company; handler returns `{ error: 'NOT_FOUND' }`. Test 4 pins this. |
| T-06.1-03 | Information Disclosure | clarity_agent_owners cross-company leak | mitigate | Every SELECT filters by `company_id = $1`; the situation-snapshot per-company loop scopes reads. Side-table includes `company_id` column (D-08). |
| T-06.1-04 | Tampering | SQL injection via agentId / ownerUserId | mitigate | All queries parameterized via `$1, $2, ...` binding; no string concatenation; matches every existing plugin-namespace repo. |
| T-06.1-05 | Tampering | Migration 0013 contains DDL-rejected statements | mitigate | Use only CREATE TABLE + COMMENT ON TABLE; no UPDATE/DELETE/INSERT. Apostrophe-free comments. Existing `ddl-prefix-validator.test.mjs` auto-validates on every test run. |
| T-06.1-06 | Repudiation | Opt-in bypass on takeOwnership | mitigate | Handler wrapped by `wrapActionHandler` (existing opt-in-guard); opted-out callers receive `{ error: 'OPT_IN_REQUIRED' }` before body runs. Test 1 pins this. |
| T-06.1-07 | Tampering | CTT-07 invariant — clarity-pack handler calling ctx.issues.update | mitigate | (1) Runtime spy in Test 10 asserts `issueUpdateCalls.length === 0` across all 9 prior tests' code paths; (2) source-grep companion test scans agent-take-ownership.ts for the literal `ctx.issues.update` and fails the build if present. Defense-in-depth. |
| T-06.1-08 | Denial of Service | Unbounded clarity_agent_owners growth | accept | v1.0 has bounded N (≤ ~50 agents/company). PK-only index sufficient. v1.1+ may add ownership-history table as additive migration; bulk-claim pattern available if scale demands. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 6.1 introduces ZERO new runtime dependencies (CONTEXT.md §code_context constraint). No package-install tasks; supply-chain surface unchanged from prior phase. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration 0013 + typed repo wrapper</name>
  <files>migrations/0013_clarity_agent_owners.sql, src/worker/db/clarity-agent-owners-repo.ts</files>
  <read_first>
    - migrations/0011_chat_message_attachments.sql (entire file — byte-for-byte CREATE TABLE template per RESEARCH.md §Pattern 3)
    - migrations/0010_chat_topics_pinned.sql (additive SQL shape sanity-check)
    - src/worker/db/chat-topics-repo.ts (entire file — repo wrapper template per RESEARCH.md §Pattern 4; lines 1-29 for type shape; lines 154-183 for upsert-then-read-back pattern)
    - test/migrations/ddl-prefix-validator.test.mjs (entire file — confirms migration constraints; the validator will run against 0013 automatically)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Pattern 3 + §Pattern 4 + §Gotchas item 2 (the migration 0012 UPDATE-rejected lesson)
  </read_first>
  <behavior>
    - Test (existing ddl-prefix-validator): 0013_clarity_agent_owners.sql passes DDL-prefix gate (CREATE/ALTER/COMMENT only).
    - Test (existing ddl-prefix-validator): No standalone `CREATE INDEX` statements; no apostrophes in `--` comments; fully-qualified schema names.
    - Test (new in agent-take-ownership.test.mjs): upsertClarityAgentOwner produces a single INSERT ... ON CONFLICT (agent_id) DO UPDATE statement, then a SELECT to read back.
    - Test: listClarityAgentOwnersForCompany returns Array<{ agent_id, owner_user_id }> filtered by company_id.
    - Test: Repo helpers parameterize all values via $1..$N bind parameters (no string concatenation).
  </behavior>
  <action>
    Create `migrations/0013_clarity_agent_owners.sql` per the canonical skeleton in RESEARCH.md §Pattern 3. Four columns: `agent_id text PRIMARY KEY`, `owner_user_id text NOT NULL`, `company_id text NOT NULL`, `set_at timestamptz NOT NULL DEFAULT now()`. Fully-qualified schema name `plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners`. Apostrophe-free `--` comments referencing Phase 6.1 ROOM-09 + the side-table-wins resolution rule. End the COMMENT ON TABLE statement with a semicolon (no trailing prose). Honor T-06.1-05: NO UPDATE/DELETE/INSERT statements (those live in the worker repo per D-01 + RESEARCH.md §Gotchas item 2).

    Create `src/worker/db/clarity-agent-owners-repo.ts` following the `chat-topics-repo.ts` header + type shape. Export `ClarityAgentOwnersRepoCtx` (requires `db: PluginDb` with `query` + `execute`). Export `upsertClarityAgentOwner(ctx, { agent_id, owner_user_id, company_id, set_at })` returning `{ agent_id, owner_user_id, company_id, set_at }`; implementation = parameterized `INSERT INTO ... VALUES ($1, $2, $3, $4) ON CONFLICT (agent_id) DO UPDATE SET owner_user_id = EXCLUDED.owner_user_id, set_at = EXCLUDED.set_at, company_id = EXCLUDED.company_id` then a separate `SELECT agent_id, owner_user_id, company_id, set_at FROM ... WHERE agent_id = $1 LIMIT 1`. Export `listClarityAgentOwnersForCompany(ctx, companyId)` returning `Array<{ agent_id: string, owner_user_id: string }>` via parameterized `SELECT agent_id, owner_user_id FROM ... WHERE company_id = $1`. All SQL strings literal-concat the fully-qualified schema name (no template substitution).
  </action>
  <verify>
    <automated>node --test test/migrations/ddl-prefix-validator.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `migrations/0013_clarity_agent_owners.sql` exists.
    - Behavior assertion: `grep -E '^(CREATE TABLE|COMMENT ON|--)' migrations/0013_clarity_agent_owners.sql` covers every non-blank, non-whitespace line (no `INSERT`/`UPDATE`/`DELETE`/`DROP` statements present).
    - Behavior assertion: `grep -c "plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners" migrations/0013_clarity_agent_owners.sql` ≥ 2 (CREATE + COMMENT).
    - Behavior assertion: `grep -v "^--" migrations/0013_clarity_agent_owners.sql | grep "'"` returns empty (no apostrophes outside comments; and `grep "'" migrations/0013_clarity_agent_owners.sql` returns empty overall since comments are also apostrophe-free).
    - File `src/worker/db/clarity-agent-owners-repo.ts` exists and exports `ClarityAgentOwnersRepoCtx`, `upsertClarityAgentOwner`, `listClarityAgentOwnersForCompany`.
    - Source assertion: `grep -c "ON CONFLICT (agent_id) DO UPDATE" src/worker/db/clarity-agent-owners-repo.ts` ≥ 1.
    - Source assertion: `grep -c "ctx.issues.update" src/worker/db/clarity-agent-owners-repo.ts` = 0 (CTT-07).
    - Test command: `node --test test/migrations/ddl-prefix-validator.test.mjs` exits 0 (0013 passes every constraint).
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Migration 0013 ships; repo wrapper compiles; ddl-prefix-validator green against 0013; no CTT-07 violations in repo source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: agent.takeOwnership handler + 10-test runtime spy + CTT-07 source-grep companion</name>
  <files>src/worker/handlers/agent-take-ownership.ts, src/worker.ts, test/worker/handlers/agent-take-ownership.test.mjs, test/ctt07/agent-take-ownership-no-issue-update.test.mjs</files>
  <read_first>
    - src/worker/handlers/chat-promote.ts (entire file — T-04-16 viewer-authority pattern per RESEARCH.md §Pattern 1; lines 60-119 are the load-bearing chunk)
    - src/worker/handlers/chat-topic-archive.ts (entire file — minimal action-handler shape)
    - src/worker/opt-in-guard.ts (entire file — wrapActionHandler signature at lines 108-124)
    - src/worker.ts (entire file — register-call wiring pattern; Plan 05-11 chat-attachment-upload registration at the analogous lines 166-173 + 213 area)
    - test/worker/handlers/chat-attachment-upload.test.mjs (entire 638-line file — canonical action-handler test template per RESEARCH.md §Test 1; ctx-mock template at lines 67-214)
    - test/ctt07/chat-attachment-handlers-no-issue-update.test.mjs (entire 50-line file — source-grep companion template per RESEARCH.md §Test 3)
    - src/worker/db/clarity-agent-owners-repo.ts (just-created in Task 1 — for the upsert signature)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Pattern 1 (canonical skeleton verbatim) + §SDK API Confirmations + §Test Shape Blueprint §Test 1
  </read_first>
  <behavior>
    - Test 1 (opt-in gate): opted-out caller → `{ error: 'OPT_IN_REQUIRED' }`, no `ctx.agents.get` call.
    - Test 2 (param validation): missing `companyId` / `agentId` / `ownerUserId` / `userId` each THROW with canonical message `agent.takeOwnership: <key> required`.
    - Test 3 (OWNER_MISMATCH): `ownerUserId !== userId` → `{ error: 'OWNER_MISMATCH' }`, no upsert call.
    - Test 4 (NOT_FOUND): `ctx.agents.get` returns null → `{ error: 'NOT_FOUND' }`, no upsert call.
    - Test 5 (agents.get throws): `ctx.agents.get` rejects → `{ error: 'OWNERSHIP_FAILED' }`, no upsert call.
    - Test 6 (happy path): returns `{ ok: true, agentId, ownerUserId, setAt }`; one INSERT into `clarity_agent_owners`.
    - Test 7 (idempotent re-claim): same `(agentId, ownerUserId)` twice succeeds twice via ON CONFLICT DO UPDATE; final SELECT returns one row.
    - Test 8 (reassign): same `agentId` + different `ownerUserId` returns updated row; `set_at` advances (compare second result.setAt > first result.setAt).
    - Test 9 (repo write fails): `ctx.db.execute` rejects → `{ error: 'OWNERSHIP_FAILED' }`.
    - Test 10 (CTT-07 runtime spy): across every code path in Tests 1-9, `ctx._issueUpdateCalls.length === 0`. **Load-bearing.**
    - CTT-07 source-grep companion test: agent-take-ownership.ts source contains zero `ctx.issues.update(` matches; build fails if present.
  </behavior>
  <action>
    Create `src/worker/handlers/agent-take-ownership.ts` per the canonical skeleton in RESEARCH.md §Pattern 1 (reproduced in `<interfaces>` above). Export `AgentTakeOwnershipCtx` (intersection of `OptInGuardActionCtx` + `ClarityAgentOwnersRepoCtx` + `{ agents: PluginAgentsClient; logger?: PluginLogger }`). Export `registerAgentTakeOwnership(ctx)` which wraps a single `wrapActionHandler(ctx, 'agent.takeOwnership', async (params) => ...)` call. Inside the handler: extract `companyId`, `agentId`, `ownerUserId`, `userId` via `reqStr(p, k)` (throws with literal `agent.takeOwnership: <k> required`). Apply T-06.1-01 mitigation: if `ownerUserId !== userId` return `{ error: 'OWNER_MISMATCH' as const }`. Apply T-06.1-02 mitigation: `ctx.agents.get(agentId, companyId)` wrapped in try/catch — null returns `{ error: 'NOT_FOUND' }`, throw returns `{ error: 'OWNERSHIP_FAILED' }`. Then call `upsertClarityAgentOwner(ctx, { agent_id, owner_user_id, company_id, set_at: new Date().toISOString() })` wrapped in try/catch (rejection → `{ error: 'OWNERSHIP_FAILED' }`). Success returns `{ ok: true, agentId, ownerUserId, setAt: row.set_at }`. **NEVER call `ctx.issues.update` anywhere in this file.**

    Wire into `src/worker.ts`: add the import `import { registerAgentTakeOwnership } from './worker/handlers/agent-take-ownership.ts'` near the existing handler imports; add the `registerAgentTakeOwnership(ctx)` call in the analogous registration block where `registerChatAttachmentUpload` / `registerChatTopicArchive` are wired (matches the chat-* registration pattern shipped in Plan 05-11).

    Create `test/worker/handlers/agent-take-ownership.test.mjs` forking the chat-attachment-upload.test.mjs ctx-mock template (lines 67-214). Tests 1-10 per the §behavior block. Ctx-mock MUST track `_issueUpdateCalls` array; Test 10 asserts `ctx._issueUpdateCalls.length === 0` after every prior test scenario.

    Create `test/ctt07/agent-take-ownership-no-issue-update.test.mjs` forking `test/ctt07/chat-attachment-handlers-no-issue-update.test.mjs` verbatim. Replace the `HANDLERS` array with `[path.join(REPO_ROOT, 'src', 'worker', 'handlers', 'agent-take-ownership.ts')]`. (situation-artifacts.ts is added in Plan 06.1-02 — this test references only the file shipped in this plan.)
  </action>
  <verify>
    <automated>node --test test/worker/handlers/agent-take-ownership.test.mjs test/ctt07/agent-take-ownership-no-issue-update.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `src/worker/handlers/agent-take-ownership.ts` exists and exports `AgentTakeOwnershipCtx`, `registerAgentTakeOwnership`.
    - Source assertion: `grep -c "ctx.issues.update" src/worker/handlers/agent-take-ownership.ts` = 0 (CTT-07 by construction).
    - Source assertion: `grep -c "wrapActionHandler" src/worker/handlers/agent-take-ownership.ts` ≥ 1.
    - Source assertion: `grep -c "OWNER_MISMATCH" src/worker/handlers/agent-take-ownership.ts` ≥ 1.
    - Source assertion: `grep -c "registerAgentTakeOwnership" src/worker.ts` ≥ 2 (import + call).
    - Test command: `node --test test/worker/handlers/agent-take-ownership.test.mjs` exits 0 — all 10 tests pass.
    - Test command: `node --test test/ctt07/agent-take-ownership-no-issue-update.test.mjs` exits 0.
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Handler shipped, registered in worker.ts, 10 tests green, CTT-07 invariant double-pinned (runtime spy at Test 10 + source-grep companion).</done>
</task>

<task type="auto">
  <name>Task 3: Wire clarity_agent_owners into situation-snapshot owner-resolution (the integration moment)</name>
  <files>src/worker/jobs/situation-snapshot.ts</files>
  <read_first>
    - src/worker/jobs/situation-snapshot.ts (entire file — modify point at lines 253-260 per RESEARCH.md §Pattern 5; also lines 106-115 for the snake_case Agent cast, lines 117 for the userId resolution pattern, lines 155-160 for where nodeMeta[toId] is currently populated, lines 217-231 for the active-viewer gate, line 247 for ctx.agents.list usage)
    - src/worker/jobs/humanize-snapshot.ts (entire file — confirms NO modification needed; the __unowned__ rewrite pass at lines 98-103 becomes a no-op when owners are pre-populated)
    - src/shared/blocker-chain.ts (lines 130-189 — confirms the __unowned__ sentinel write at line 178 and the meta.ownerUserId read site; this file is LOCKED — do NOT modify)
    - src/worker/db/clarity-agent-owners-repo.ts (just-created in Task 1 — for the listClarityAgentOwnersForCompany signature)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Pattern 5 (the load-bearing integration recipe) + §Gotchas item 7 (the __unowned__ string literal) + §No-Go Zones (blocker-chain.ts + humanize-snapshot.ts are off-limits)
  </read_first>
  <action>
    In `src/worker/jobs/situation-snapshot.ts`, inside the per-company loop (where `employees = await ctx.agents.list({ companyId })` is followed by `buildIdLookup(...)`):

    Step 1 — Before the `buildIdLookup` call at line 253-260, INSERT a side-table fetch wrapped in try/catch (graceful degrade on error per RESEARCH.md §Pattern 5):
    - Call `listClarityAgentOwnersForCompany(ctx, companyId)` (newly imported from `../db/clarity-agent-owners-repo.ts`); on throw, log a warn via `ctx.logger?.warn?.('situation-snapshot: clarity_agent_owners SELECT failed', { companyId, err })` and fall through with an empty array — host-only resolution then applies.

    Step 2 — Build a `Map<agentId, ownerUserId>` (variable name `ownerMap`) seeded from `employees.map(e => [e.id ?? e.user_id, e.owner_user_id ?? null])` (host fallback), then OVERWRITE with side-table entries (`for (const row of sideTableOwners) ownerMap.set(row.agent_id, row.owner_user_id)`). Side-table wins per D-01.

    Step 3 — After the existing relations walk that populates `nodeMeta[toId]` (around lines 155-160 — the per-blocker `nodeMeta[toId] = { kind, label, ownerUserId: blocker.assigneeUserId ?? blocker.ownerUserId ?? null, ... }` write), add a final reconciliation pass: `for (const [nodeId, meta] of Object.entries(nodeMeta)) { if (meta.ownerUserId == null) { const resolved = ownerMap.get(nodeId); if (resolved) meta.ownerUserId = resolved; } }`. This pass ensures the blocker-chain walk (which runs AFTER this in `flattenBlockerChain`) sees populated `ownerUserId` values for any agent in `clarity_agent_owners`.

    **CRITICAL:** Per RESEARCH.md §No-Go Zones, do NOT modify `src/shared/blocker-chain.ts` or `src/worker/jobs/humanize-snapshot.ts`. The pre-population is the entire fix.

    Add the import `import { listClarityAgentOwnersForCompany } from '../db/clarity-agent-owners-repo.ts'` at the top of the file (near other repo imports).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "listClarityAgentOwnersForCompany" src/worker/jobs/situation-snapshot.ts` ≥ 2 (import + call).
    - Source assertion: `grep -c "ownerMap" src/worker/jobs/situation-snapshot.ts` ≥ 3 (Map construction + side-table overwrite loop + reconciliation pass).
    - Source assertion: `grep -c "ctx.issues.update" src/worker/jobs/situation-snapshot.ts` is unchanged from pre-edit baseline (the file is a job, not a handler — should remain 0 for CTT-07 hygiene).
    - Source assertion: `git diff src/shared/blocker-chain.ts` returns empty (LOCKED INVARIANT — no modification).
    - Source assertion: `git diff src/worker/jobs/humanize-snapshot.ts` returns empty (the rewrite pass becomes a no-op when owners are pre-populated; no source change needed).
    - Source assertion: the order of operations in the per-company loop is: (1) fetch side-table owners, (2) build ownerMap with side-table-wins, (3) run existing relations walk that writes nodeMeta, (4) reconciliation pass that backfills null nodeMeta[].ownerUserId from ownerMap, (5) buildIdLookup + flattenBlockerChain.
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
    - Existing situation-snapshot test suite continues to pass: `node --test test/worker/jobs/situation-snapshot.test.mjs` (if the test file exists; if it does not, this acceptance criterion is satisfied by the broader `pnpm test` gate run in Plan 06.1-04).
  </acceptance_criteria>
  <done>Snapshot owner-resolution consults the side table first; blocker-chain.ts and humanize-snapshot.ts unchanged (LOCKED); typecheck green.</done>
</task>

</tasks>

<verification>
**Worker-tier owner-resolution path closure:**

1. `node --test test/worker/handlers/agent-take-ownership.test.mjs` → 10 tests pass.
2. `node --test test/ctt07/agent-take-ownership-no-issue-update.test.mjs` → exits 0.
3. `node --test test/migrations/ddl-prefix-validator.test.mjs` → 0013 passes every constraint.
4. `pnpm exec tsc --noEmit` → clean (no new type errors).
5. `grep -c "ctx.issues.update" src/worker/handlers/agent-take-ownership.ts src/worker/db/clarity-agent-owners-repo.ts` → both zero (CTT-07 by construction).
6. `git diff src/shared/blocker-chain.ts src/worker/jobs/humanize-snapshot.ts` → empty (LOCKED invariants preserved).
7. `pnpm build:worker` (or equivalent worker bundler) → succeeds without errors.

**Integration sanity (manual, executed against a fresh dev install in Plan 06.1-04's drill):**
- A claimed agent's chain terminal renders `HUMAN_ACTION_ON(<real_user_id>)` not `HUMAN_ACTION_ON(__unowned__)`.
- Removing a row from `clarity_agent_owners` reverts to the host fallback in ≤ 60s (next snapshot tick).
</verification>

<success_criteria>
- Migration 0013 ships, additive plugin-namespace only, validates against host DDL-prefix gate.
- `clarity-agent-owners-repo.ts` exports `upsertClarityAgentOwner` + `listClarityAgentOwnersForCompany` with parameterized SQL.
- `agent-take-ownership.ts` ships with all 10 tests green, including T-10 CTT-07 runtime spy + CTT-07 source-grep companion.
- `situation-snapshot.ts` consults the side table FIRST with side-table-wins semantics; blocker-chain.ts and humanize-snapshot.ts untouched.
- Handler registered in `worker.ts`; typecheck clean.
- Zero `ctx.issues.update` calls in any file shipped by this plan.
</success_criteria>

<output>
Create `.planning/phases/06.1-situation-room-spec-complete/06.1-01-SUMMARY.md` when done — record the actual diff, the 10 tests landed, any deviations (none expected — every pattern is locked).
</output>
