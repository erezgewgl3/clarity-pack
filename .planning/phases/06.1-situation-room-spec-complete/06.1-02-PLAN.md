---
phase: 06.1-situation-room-spec-complete
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/handlers/situation-artifacts.ts
  - src/manifest.ts
  - src/worker.ts
  - test/worker/handlers/situation-artifacts.test.mjs
  - test/ctt07/situation-artifacts-no-issue-update.test.mjs
autonomous: true
requirements: [ROOM-10]
tags: [worker, data-handler, artifacts, situation-room]
must_haves:
  truths:
    - "D-05: artifact window default = 24h, configurable via instanceConfigSchema.situationArtifactsWindow (allowed values: 24h / 7d / 30d). Mirrors Phase 2 D-03 cadence pattern."
    - "D-10: situation.artifacts SQL union = single bulk query for chat-attachments half (PRIM-01 no N+1) + per-agent ctx.issues.documents.list for deliverables half (bounded N≈20). Default limit 5/agent, clamp 100."
    - "situation.artifacts data handler returns per-agent union of ctx.issues.documents.list (deliverables) ∪ plugin-namespace chat_message_attachments (chat-attachments)"
    - "Window defaults to 24h, configurable via instanceConfigSchema.situationArtifactsWindow with allowed values 24h/7d/30d"
    - "Single bulk SQL query for the chat-attachments half (PRIM-01); per-agent ctx.issues.documents.list for the deliverables half (bounded N≈20)"
    - "Default limit 5/agent, clamp at 100"
    - "Empty window → artifacts: {} (graceful, no error)"
    - "Zero ctx.issues.update calls in situation-artifacts.ts (runtime spy + source-grep both pin this)"
  artifacts:
    - path: "src/worker/handlers/situation-artifacts.ts"
      provides: "situation.artifacts data handler wrapped by wrapDataHandler"
      exports: ["SituationArtifactsCtx", "registerSituationArtifacts"]
    - path: "src/manifest.ts"
      provides: "instanceConfigSchema.situationArtifactsWindow key (enum: 24h/7d/30d, default 24h)"
      contains: "situationArtifactsWindow"
    - path: "test/worker/handlers/situation-artifacts.test.mjs"
      provides: "10 tests including PRIM-01 single-query assertion + CTT-07 runtime spy"
    - path: "test/ctt07/situation-artifacts-no-issue-update.test.mjs"
      provides: "CTT-07 source-grep companion (defense-in-depth)"
  key_links:
    - from: "src/worker.ts"
      to: "situation-artifacts.ts"
      via: "registerSituationArtifacts(ctx) call in setup"
      pattern: "registerSituationArtifacts"
    - from: "src/manifest.ts"
      to: "instanceConfigSchema"
      via: "situationArtifactsWindow enum property"
      pattern: "situationArtifactsWindow"
---

<objective>
Land the worker-tier artifact union path for ROOM-10: `situation.artifacts` data handler returning, per agent, the union of (a) host deliverables via `ctx.issues.documents.list` and (b) plugin-namespace `chat_message_attachments` joined to `chat_topics` for agent attribution. Window is 24h-sliding by default (configurable via new `instanceConfigSchema.situationArtifactsWindow` accepting `24h`/`7d`/`30d`). The chat-attachments half is ONE bulk SQL query (PRIM-01 single-round-trip); the deliverables half is a bounded per-agent loop (N≈20 agents per RESEARCH.md §A1).

Purpose: Power the new per-agent inline `<ArtifactChipRow>` in the Situation Room (Plan 06.1-03 consumes this handler). Replaces the bottom artifact shelf as the single source for ROOM-04 + ROOM-10. CTT-07 invariant by construction.

Output: Data handler + manifest config key + 10-test suite (including PRIM-01 single-query assertion) + CTT-07 source-grep companion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md
@CLAUDE.md

<interfaces>
Key contracts the executor needs (do not explore the codebase to find these — they are reproduced verbatim from RESEARCH.md):

From `src/worker/opt-in-guard.ts:108-124` (existing):
- `wrapDataHandler(ctx, key: string, fn: (params) => Promise<unknown>): void` — opted-out callers receive `{ error: 'OPT_IN_REQUIRED' }` before body runs.

From `src/worker/handlers/chat-attachment-list.ts` (canonical data-handler template, Plan 05-11):
- `DEFAULT_LIST_LIMIT = 5` and `MAX_LIST_LIMIT = 100` (lines 32-33) — reuse these constants for situation.artifacts.
- Param-validation pattern (lines 67-69): RETURN `{ error: 'COMPANY_ID_REQUIRED' }` etc — never throw in a data handler. (Action handlers throw; data handlers return.)
- Return shape: discriminated-union with `kind: 'situation-artifacts'`.

From `ctx.issues.documents.list` (PluginIssuesClient — per RESEARCH.md §SDK API + situation-snapshot existing usage):
- Returns Document[] with fields including `id, documentKey, title, mimeType, byteSize, createdAt`. The exact field names are structurally accessed (cast through `unknown` per the chat-attachment-upload.ts existing pattern). Bounded N agents → bounded N calls per dispatch (acceptable per A1 in RESEARCH.md).

From `instanceConfigSchema` shape in `src/manifest.ts:511-540` (Plan 02-04 `situationRefreshIntervalMs` precedent):
- Add new property `situationArtifactsWindow` with `type: 'string'`, `enum: ['24h', '7d', '30d']`, `default: '24h'`. Mirror the existing config-key shape verbatim.

From RESEARCH.md §Pattern 2 — SQL union shape (the bulk query for chat-attachments half):
```sql
SELECT
  cma.id, cma.chat_message_id, cma.topic_issue_id,
  cma.document_key, cma.mime_type, cma.original_filename,
  cma.byte_size, cma.created_at,
  ct.employee_agent_id AS agent_id
FROM plugin_clarity_pack_cdd6bda4bd.chat_message_attachments cma
JOIN plugin_clarity_pack_cdd6bda4bd.chat_topics ct
  ON ct.issue_id = cma.topic_issue_id
  AND ct.company_id = cma.company_id
WHERE cma.company_id = $1
  AND cma.created_at > now() - $2::interval
ORDER BY cma.created_at DESC;
```
The `$2::interval` parameter is the window converted from `'24h'` / `'7d'` / `'30d'` to a Postgres interval literal at handler entry (never pass the raw config string into SQL).

Return shape (per RESEARCH.md §Pattern 2 §Handler return shape):
```ts
{
  kind: 'situation-artifacts',
  windowDuration: '24h' | '7d' | '30d',
  artifacts: {
    [agentId: string]: Array<{
      id: string;
      kind: 'deliverable' | 'chat-attachment';
      documentKey: string;
      mimeType: string;
      originalFilename: string;
      byteSize: number;
      createdAt: string; // ISO
      topicIssueId?: string;  // chat-attachment only
      sourceIssueId?: string; // deliverable only
    }>;
  };
}
```
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → worker data dispatch | Untrusted: UI can request any companyId; handler must scope by request context. |
| Worker → instanceConfigSchema | Trusted at read, but config strings are user-supplied — must validate enum at handler entry and fall back to `24h` on unknown input (T-06.1-12 below). |
| Worker → plugin-namespace SQL | Trusted IFF parameterized; all values via `$1, $2, ...` binding. |
| Worker → host SDK `ctx.issues.documents.list` | Trusted; host scopes by companyId implicitly via context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-09 | Information Disclosure | situation.artifacts cross-company leak | mitigate | SQL WHERE clause filters by `cma.company_id = $1` AND `ct.company_id = cma.company_id`; both halves of the JOIN are company-scoped. |
| T-06.1-10 | Tampering | SQL injection via window string | mitigate | Window value validated at handler entry against enum `['24h', '7d', '30d']`; coerced to `'24h'` on unknown input. Postgres interval cast uses `$2::interval` bind param, not string concatenation. |
| T-06.1-11 | Information Disclosure | Deliverables from issues the viewer cannot see | mitigate | `ctx.issues.documents.list(issueId, companyId)` respects host viewer-permissions automatically (PRIM-02 inheritance pattern). Plugin does not bypass host's per-issue ACL. |
| T-06.1-12 | Denial of Service | Unbounded per-agent results | mitigate | Default limit 5/agent (DEFAULT_LIST_LIMIT); clamp at 100 (MAX_LIST_LIMIT). Bounded N agents per company (≤ ~50). PRIM-01 single-query for chat-attachments half. |
| T-06.1-13 | Repudiation | Opt-in bypass on data read | mitigate | Handler wrapped by `wrapDataHandler` (existing opt-in-guard); opted-out callers receive `{ error: 'OPT_IN_REQUIRED' }` before body. Test 1 pins this. |
| T-06.1-14 | Tampering | CTT-07 invariant — clarity-pack handler calling ctx.issues.update | mitigate | (1) Runtime spy in Test 10 asserts `issueUpdateCalls.length === 0`; (2) source-grep companion test scans situation-artifacts.ts for the literal `ctx.issues.update`. Defense-in-depth. |
| T-06.1-15 | Denial of Service | `ctx.issues.documents.list` per-agent latency | accept | RESEARCH.md §A1 — N≈20 agents, 60s tick budget. If N grows beyond ~50 a plugin-namespace cache becomes the v1.1+ additive migration; v1.0 has bounded N. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 6.1 introduces ZERO new runtime dependencies (CONTEXT.md §code_context constraint). No package-install tasks. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: instanceConfigSchema.situationArtifactsWindow key in manifest</name>
  <files>src/manifest.ts</files>
  <read_first>
    - src/manifest.ts (entire file — focus on lines 343-440 capability list + lines 511-540 instanceConfigSchema where the Plan 02-04 `situationRefreshIntervalMs` precedent lives)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Pattern 7 (Manifest registration) + §Gotchas item 12 (instanceConfigSchema shape)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md §decisions D-05
  </read_first>
  <action>
    In `src/manifest.ts`, locate the existing `instanceConfigSchema` block (around lines 511-540). Add ONE new property `situationArtifactsWindow` mirroring the shape of the existing `situationRefreshIntervalMs` (or whichever sibling config key uses the same JSON-Schema convention shipped at Plan 02-04). Property definition:
    - `type: 'string'`
    - `enum: ['24h', '7d', '30d']`
    - `default: '24h'`
    - `description: 'Window for the Situation Room per-agent artifact chip row (ROOM-10). Default 24h matches the Surface 2 verbatim copy.'`

    Honor the atomic version-bump constraint: **DO NOT bump the version literal in `src/manifest.ts:337` or in `package.json`** (Plan 05-10 owns the rc → 1.0.0 transition per project memory `plugin-version-bump-two-sources`).

    No new capabilities needed — `agents.read` + `database.namespace.read` + `database.namespace.write` already cover the Phase 6.1 surface (RESEARCH.md §Gotchas item 10).
  </action>
  <verify>
    <automated>node -e "const m = require('./src/manifest.ts'); console.log(JSON.stringify(m, null, 2));" 2>&1 | grep -q situationArtifactsWindow || (echo 'situationArtifactsWindow missing'; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "situationArtifactsWindow" src/manifest.ts` ≥ 1.
    - Source assertion: `grep -c "'24h'" src/manifest.ts` ≥ 1 (default value present).
    - Source assertion: `grep -c "'30d'" src/manifest.ts` ≥ 1 (enum upper bound present).
    - Source assertion: `git diff src/manifest.ts | grep -E "^\+.*version"` is empty (no version-literal change — Plan 05-10 owns it).
    - Source assertion: `git diff package.json | grep -E "^\+.*\"version\""` is empty.
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>New config key shipped; no version bump; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: situation.artifacts data handler + 10-test suite + CTT-07 source-grep companion</name>
  <files>src/worker/handlers/situation-artifacts.ts, src/worker.ts, test/worker/handlers/situation-artifacts.test.mjs, test/ctt07/situation-artifacts-no-issue-update.test.mjs</files>
  <read_first>
    - src/worker/handlers/chat-attachment-list.ts (entire 115-line file — canonical data-handler template per RESEARCH.md §Pattern 2; lines 32-33 for limit constants; lines 53 for wrapDataHandler; lines 67-69 for return-don't-throw param validation; entire file as the shape template)
    - src/worker/handlers/chat-attachment-upload.ts (lines around the bulk-query shape that joins chat_message_attachments to chat_topics — for the JOIN clause reference)
    - src/worker/jobs/situation-snapshot.ts (line 247 area — for the `ctx.agents.list({ companyId })` usage pattern that situation.artifacts will replicate to enumerate agents)
    - src/worker.ts (entire file — registration pattern; analogous to where registerChatAttachmentList is wired)
    - test/worker/handlers/chat-attachment-list.test.mjs (data-handler test template per RESEARCH.md §Test 2)
    - test/ctt07/chat-attachment-handlers-no-issue-update.test.mjs (50-line source-grep template per RESEARCH.md §Test 3)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Pattern 2 (canonical SQL + return shape verbatim) + §Test Shape Blueprint §Test 2 + §Gotchas items 3 + 12
  </read_first>
  <behavior>
    - Test 1 (opt-in gate): opted-out caller → `{ error: 'OPT_IN_REQUIRED' }`, no SQL query, no `ctx.agents.list` call.
    - Test 2 (missing companyId): returns `{ error: 'COMPANY_ID_REQUIRED' }` (NOT throw — data handler convention per chat-attachment-list.ts:67-69).
    - Test 3 (missing userId): returns `{ error: 'USER_ID_REQUIRED' }`.
    - Test 4 (default 24h window): handler reads instance config; falls back to `'24h'` when key absent; SQL parameter bound as Postgres `24 hours` interval.
    - Test 5 (configurable 7d / 30d): handler honors instance config value; SQL parameter bound accordingly.
    - Test 6 (unknown window value): handler coerces to `'24h'` default; T-06.1-10 mitigation.
    - Test 7 (empty result): `artifacts: {}` returned; no error.
    - Test 8 (PRIM-01 single bulk query for chat-attachments half): `ctx.db.query` fires AT MOST TWICE per dispatch — once for `clarity_user_prefs` opt-in lookup, once for the artifact union. NO per-agent loop with multiple DB queries. (The deliverables half uses `ctx.issues.documents.list` per agent — that is a host SDK call, not a DB query, and is bounded by N≈20 per RESEARCH.md §A1.)
    - Test 9 (newest-first ordering preserved through the map()): rows returned sorted DESC by created_at.
    - Test 10 (limit clamp at 100/agent): when a synthetic agent has 150 chat-attachments in window, returned array length ≤ 100.
    - Test 11 (CTT-07 runtime spy): across every code path in Tests 1-10, `ctx._issueUpdateCalls.length === 0`. **Load-bearing.**
    - CTT-07 source-grep companion test: situation-artifacts.ts source contains zero `ctx.issues.update(` matches.
  </behavior>
  <action>
    Create `src/worker/handlers/situation-artifacts.ts` following the `chat-attachment-list.ts` shape:

    - Import `wrapDataHandler`, `OptInGuardDataCtx`. Define `SituationArtifactsCtx = OptInGuardDataCtx & { agents: PluginAgentsClient; issues: PluginIssuesClient; config?: { get(key: string): unknown }; logger?: PluginLogger }`.
    - Constants `DEFAULT_LIST_LIMIT = 5`, `MAX_LIST_LIMIT = 100`, `ALLOWED_WINDOWS = ['24h', '7d', '30d'] as const`, `WINDOW_TO_INTERVAL = { '24h': '24 hours', '7d': '7 days', '30d': '30 days' }`.
    - Helper `resolveWindow(ctx): '24h' | '7d' | '30d'` reads `ctx.config?.get?.('situationArtifactsWindow')` and returns the value if it is in `ALLOWED_WINDOWS`, else `'24h'` (T-06.1-10 coercion).
    - Export `registerSituationArtifacts(ctx: SituationArtifactsCtx)` calling `wrapDataHandler(ctx, 'situation.artifacts', async (params) => ...)`.

    Inside the handler:
    1. Validate `companyId` and `userId` params — return `{ error: 'COMPANY_ID_REQUIRED' as const }` / `{ error: 'USER_ID_REQUIRED' as const }` (do NOT throw — data-handler convention).
    2. Resolve the window via the helper; compute `intervalLiteral = WINDOW_TO_INTERVAL[window]`.
    3. Enumerate agents via `ctx.agents.list({ companyId })`.
    4. **PRIM-01 bulk query for chat-attachments half:** ONE `ctx.db.query` call using the canonical SQL from RESEARCH.md §Pattern 2 (reproduced in `<interfaces>` above). Parameters `[companyId, intervalLiteral]` — interval passed as a string that Postgres casts via `$2::interval`. Group rows by `agent_id` into a `Map<string, Array<...>>`.
    5. **Deliverables half (per-agent, bounded loop):** for each agent in the list, look up the agent's source issue ID (the host's agent record carries this — read via the existing structural cast pattern in situation-snapshot.ts:106-115), then call `ctx.issues.documents.list(sourceIssueId, companyId)` filtered to `createdAt > now - window`. Wrap each per-agent call in try/catch (single-agent failure must not break the whole response — log warn and continue with empty array for that agent).
    6. Merge the two halves into the return-shape `artifacts: { [agentId]: Array<{ kind: 'deliverable' | 'chat-attachment', ... }> }`. Sort each agent's array DESC by `createdAt`. Clamp each agent's array length at `MAX_LIST_LIMIT` (100).
    7. Return `{ kind: 'situation-artifacts' as const, windowDuration: window, artifacts }`.

    **NEVER call `ctx.issues.update` anywhere in this file.**

    Wire into `src/worker.ts`: add import `import { registerSituationArtifacts } from './worker/handlers/situation-artifacts.ts'` and call `registerSituationArtifacts(ctx)` in the registration block (analogous placement to `registerChatAttachmentList`).

    Create `test/worker/handlers/situation-artifacts.test.mjs` forking the chat-attachment-list.test.mjs shape. 11 tests per §behavior. The ctx-mock MUST:
    - Track `ctx._dbQueryCalls` so Test 8 can assert at-most-twice call-count.
    - Track `ctx._issueUpdateCalls` for Test 11 CTT-07 spy.
    - Mock `ctx.issues.documents.list` to return synthetic Document arrays.
    - Mock `ctx.agents.list` to return a 2-agent synthetic company.

    Create `test/ctt07/situation-artifacts-no-issue-update.test.mjs` forking `test/ctt07/chat-attachment-handlers-no-issue-update.test.mjs`. Replace HANDLERS array with `[path.join(REPO_ROOT, 'src', 'worker', 'handlers', 'situation-artifacts.ts')]`.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/situation-artifacts.test.mjs test/ctt07/situation-artifacts-no-issue-update.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `src/worker/handlers/situation-artifacts.ts` exists and exports `SituationArtifactsCtx`, `registerSituationArtifacts`.
    - Source assertion: `grep -c "ctx.issues.update" src/worker/handlers/situation-artifacts.ts` = 0 (CTT-07).
    - Source assertion: `grep -c "wrapDataHandler" src/worker/handlers/situation-artifacts.ts` ≥ 1.
    - Source assertion: `grep -c "DEFAULT_LIST_LIMIT\|MAX_LIST_LIMIT" src/worker/handlers/situation-artifacts.ts` ≥ 2.
    - Source assertion: `grep -c "ALLOWED_WINDOWS" src/worker/handlers/situation-artifacts.ts` ≥ 1.
    - Source assertion: `grep -E "FROM plugin_clarity_pack_cdd6bda4bd.chat_message_attachments" src/worker/handlers/situation-artifacts.ts` matches at least once (the canonical bulk SQL).
    - Source assertion: `grep -c "registerSituationArtifacts" src/worker.ts` ≥ 2 (import + call).
    - Test command: `node --test test/worker/handlers/situation-artifacts.test.mjs` exits 0 — all 11 tests pass.
    - Test command: `node --test test/ctt07/situation-artifacts-no-issue-update.test.mjs` exits 0.
    - Test 8 specifically asserts: `ctx._dbQueryCalls.filter(c => /chat_message_attachments/i.test(c.sql)).length === 1` (PRIM-01 — single bulk query for the chat-attachments half, regardless of agent count).
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Handler shipped, registered in worker.ts, 11 tests green including PRIM-01 single-query assertion at Test 8 and CTT-07 runtime spy at Test 11; CTT-07 source-grep companion green.</done>
</task>

</tasks>

<verification>
**Worker-tier artifact union path closure:**

1. `node --test test/worker/handlers/situation-artifacts.test.mjs` → 11 tests pass.
2. `node --test test/ctt07/situation-artifacts-no-issue-update.test.mjs` → exits 0.
3. `pnpm exec tsc --noEmit` → clean.
4. `grep -c "ctx.issues.update" src/worker/handlers/situation-artifacts.ts` → zero.
5. `grep -c "situationArtifactsWindow" src/manifest.ts` → ≥ 1; no version-literal change.
6. PRIM-01 invariant: the chat-attachments half fires ONE `ctx.db.query` call regardless of agent count (Test 8 pin).
7. Window coercion (T-06.1-10): unknown config value defaults to `'24h'` (Test 6 pin).
</verification>

<success_criteria>
- `situation-artifacts.ts` ships with 11 tests green, including PRIM-01 (Test 8), window-coercion (Test 6), and CTT-07 runtime spy (Test 11).
- `instanceConfigSchema.situationArtifactsWindow` declared in manifest with enum `[24h, 7d, 30d]` and default `24h`.
- Handler registered in `worker.ts`; typecheck clean.
- Zero `ctx.issues.update` calls in the new handler (runtime + source-grep both pin).
- No version bump (Plan 05-10 owns the rc → 1.0.0 transition).
</success_criteria>

<output>
Create `.planning/phases/06.1-situation-room-spec-complete/06.1-02-SUMMARY.md` when done — record the actual SQL shipped, the 11 tests landed, any deviations.
</output>
