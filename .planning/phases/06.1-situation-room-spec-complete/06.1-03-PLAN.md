---
phase: 06.1-situation-room-spec-complete
plan: 03
type: execute
wave: 2
depends_on: [06.1-01, 06.1-02]
files_modified:
  - src/ui/surfaces/situation-room/artifact-chip-row.tsx
  - src/ui/surfaces/situation-room/index.tsx
  - src/ui/surfaces/situation-room/agent-card.tsx
  - src/ui/surfaces/situation-room/critical-path-strip.tsx
  - src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx
  - src/ui/primitives/clarity-surface-header.tsx
  - src/ui/styles/theme.css
  - test/ui/surfaces/situation-room/critical-path-affordances.test.mjs
  - test/ui/surfaces/situation-room/artifact-chip-row.test.mjs
autonomous: true
requirements: [ROOM-09, ROOM-10, ROOM-11]
tags: [ui, situation-room, critical-path, artifact-chips]
must_haves:
  truths:
    - "D-02: per-agent inline ArtifactChipRow REPLACES the Phase 2 bottom shelf (artifacts-shipped-shelf.tsx file DELETED; index.tsx mount REMOVED)."
    - "D-03: + Create task entry point = BOTH (standalone in ClaritySurfaceHeader via Plan 05-08 D-17 mount registry + per-blocker Convert-to-task button on each Critical Path row)."
    - "D-04: CYCLE terminal renders label as-is in v1.0 (humanize-snapshot already rewrites agent UUIDs to names; no auto-suggest cycle-break — deferred to v1.1+)."
    - "D-09: UI Take-Ownership uses useResolvedUserId hook (Plan 02-09); disables button with 'Sign in to claim ownership' tooltip when viewerUserId is null/error."
    - "D-11: artifact chip click → DeliverablePreview popover via AttachmentChipWithPreview wrapper (Plan 05-11 shell); canonical preview surface across Reader/Chat/Situation Room."
    - "D-12: per-blocker Convert-to-task dialog NEVER pre-fills title/description/assignee (load-bearing operator constraint per project memory feedback_trust-the-clarification-loop). Real-DOM test pins title-input value === '' on dialog open."
    - "D-13: CYCLE-terminal Critical Path rows render `+ Create task to break this cycle` ONLY — NO Take-Ownership button (Take-Ownership presupposes a single leaf agent; CYCLE terminals don't have one)."
    - "Every Critical Path row whose terminal is HUMAN_ACTION_ON.__unowned__ renders a working Take-Ownership button (gold accent border + label)"
    - "Every Critical Path row whose terminal is non-CYCLE renders a Convert-to-task button (neutral chrome, no gold)"
    - "Every CYCLE-terminal Critical Path row renders ONLY a `+ Create task to break this cycle` button (NO Take-Ownership per D-13)"
    - "Each agent row in the grid renders an inline ArtifactChipRow when situation.artifacts returns ≥1 chip for that agent; empty windows render no DOM"
    - "ArtifactsShippedShelf component file is DELETED; index.tsx no longer mounts it"
    - "Situation Room surface mounts the standalone `+ Create task` button via clarity-surface-header (verified visible OR registry-included)"
    - "useResolvedUserId returning null/error disables Take-Ownership with `Sign in to claim ownership` tooltip"
    - "Take-Ownership dispatch fires direct (no confirmation modal); success → toast `Ownership claimed` + force-refetch; failure → toast `Could not claim ownership — try again`"
    - "Per-blocker Convert-to-task dialog NEVER pre-fills title/description/assignee (D-12 — load-bearing operator constraint)"
  artifacts:
    - path: "src/ui/surfaces/situation-room/artifact-chip-row.tsx"
      provides: "Inline per-agent chip row consuming situation.artifacts payload"
      min_lines: 40
    - path: "src/ui/surfaces/situation-room/critical-path-strip.tsx"
      provides: "Per-row Take-Ownership + Convert-to-task button cluster"
      contains: "clarity-take-ownership-btn"
    - path: "src/ui/styles/theme.css"
      provides: "NEW selectors: .clarity-artifact-chip-row, .clarity-take-ownership-btn, .clarity-convert-to-task-btn, .clarity-row-actions; DELETED orphaned .clarity-artifacts-shelf rules"
  key_links:
    - from: "src/ui/surfaces/situation-room/index.tsx"
      to: "situation.artifacts handler"
      via: "usePluginData('situation.artifacts')"
      pattern: "situation.artifacts"
    - from: "src/ui/surfaces/situation-room/critical-path-strip.tsx"
      to: "agent.takeOwnership handler"
      via: "usePluginAction('agent.takeOwnership')"
      pattern: "agent.takeOwnership"
    - from: "src/ui/surfaces/situation-room/critical-path-strip.tsx"
      to: "useResolvedUserId hook"
      via: "Take-Ownership button gating"
      pattern: "useResolvedUserId"
    - from: "src/ui/surfaces/situation-room/agent-card.tsx"
      to: "artifact-chip-row.tsx"
      via: "props passthrough from index.tsx"
      pattern: "ArtifactChipRow"
---

<objective>
Land the UI delta for the Situation Room: ONE new component (`ArtifactChipRow`), TWO modifications (`index.tsx` drops the bottom-shelf mount + threads artifact payload + viewer userId into the grid; `agent-card.tsx` renders the chip-row slot; `critical-path-strip.tsx` adds Take-Ownership + Convert-to-task buttons per terminal kind), ONE deletion (`artifacts-shipped-shelf.tsx` + its orphaned CSS), ONE one-line surface-header registry verification.

Every visual primitive is inherited from prior phases — no new design tokens, no new dialog shells, no new chip shapes, no new dismissal patterns. CSS additions are < 60 net lines, all scoped under `[data-clarity-surface="situation-room"]` (SCAF-06; check-css-scope CI gate).

Purpose: Make ROOM-09's owner-resolution actionable from the UI; surface ROOM-10's artifact union per-agent inline; render ROOM-11's per-row affordances over the unchanged (LOCKED) chain walk.

Output: One new component file + four modified UI files + CSS additions/deletions + 2 new test files (real-DOM render tests + UI invariant pins).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-UI-SPEC.md
@CLAUDE.md

<project_skills>
Apply `Skill("sketch-findings-clarity-pack")`:
- warm-dark palette inherited (existing tokens in theme.css:13-58)
- chip-styling convention from chat.css:2604 (AttachmentChip) — reuse verbatim
- zero-rabbit-holes principle: chip click = inline peek via canonical popover, never navigate
</project_skills>

<interfaces>
Key contracts the executor needs (do not explore the codebase to find these — reproduced verbatim from UI-SPEC.md + RESEARCH.md):

From `src/ui/surfaces/chat/attachment-chip-with-preview.tsx` (Plan 05-11 hotfix-2 — INHERITED, do not modify):
- Component prop shape: `{ documentKey, mimeType, originalFilename, byteSize, sourceContext, onPreviewOpen?, onPreviewClose? }`. (Param shape may use `topicIssueId` for chat-attachments; planner authorizes a `sourceContext` polymorphic shape if necessary — default: import as-is.)
- Three dismissal affordances on the popover: Escape / backdrop click / close-X button. Fixed-inset backdrop + centered body shell.

From `src/ui/primitives/use-resolved-user-id.ts` (Plan 02-09 — INHERITED, do not modify):
- `useResolvedUserId(): { userId: string | null, loading: boolean, error: null | 'no-user-context' }`. Production host-bridge gap returns `{ userId: null, loading: false, error: 'no-user-context' }`.

From `src/ui/primitives/clarity-surface-header.tsx` (Plan 05-08 D-17 — INHERITED):
- Per UI-SPEC.md §Flow 5 + verified read 2026-05-26: the `+ Create task` button is **already rendered unconditionally** for every surface that mounts the header. There is NO `SURFACES_WITH_CREATE_TASK` allowlist in the current code. The Situation Room already mounts `<ClaritySurfaceHeader surface="situation-room" />` at index.tsx:131-136. Therefore: ROOM-11 D-03's standalone-header requirement is ALREADY SATISFIED structurally. This plan's verification step on the surface header is a Playwright/real-DOM check, not a code change.
- IF a follow-up branch introduces a registry filter (the planner has not surfaced one), this plan authorizes a one-line registry inclusion of `'situation-room'`.

From `src/ui/surfaces/chat/true-task/true-task-dialog.tsx` (Plan 04.1-09 + rc.8 commit `1b11649` — INHERITED, do not modify):
- Cold-mode dialog props: opens with empty editable fields. CSS scope broadened to bare `[data-clarity-surface]` in rc.8 (chat.css:1343 ff.). Convert-to-task button opens this dialog in cold mode with NO pre-fill (D-12).

From `src/ui/primitives/toast.tsx` (Plan 05-08 — INHERITED):
- `useToast()` hook returns `{ success(message), error(message) }`. Already mounted on every surface via ToastProvider at ClaritySurfaceRoot.

From `usePluginAction` + `usePluginData` (host SDK hooks — INHERITED):
- `usePluginAction('agent.takeOwnership')` returns `(params) => Promise<{ ok, agentId, ownerUserId, setAt } | { error: '<CODE>' }>`.
- `usePluginData('situation.artifacts', { companyId, userId })` returns the discriminated-union shape from Plan 06.1-02; supports query-key invalidation via the existing pattern (Plan 04.2-04 race-safe dispatch precedent).
- `usePluginData('situation.snapshot', ...)` already exists; force-refetch via the same query-key invalidation that other phases use.

From UI-SPEC.md §Visual Hierarchy Lock (agent-card composition, top-to-bottom):
1. `clarity-agent-card-header` (UNCHANGED)
2. `clarity-now-doing` (UNCHANGED)
3. `clarity-artifact-chip-row` (**NEW Phase 6.1**; rendered only when non-empty)
4. `clarity-agent-terminal` (UNCHANGED)
5. `clarity-agent-artifact` (latest-artifact placeholder) — DEPRECATED (orphaned by chip row; UI-SPEC authorizes deletion or retention; recommend deletion for clarity)
6. `clarity-sparkline` (UNCHANGED)

Critical Path row composition (UI-SPEC §Visual Hierarchy Lock):
- `clarity-critical-path-index` (UNCHANGED)
- `clarity-critical-path-text` (UNCHANGED)
- **NEW: `clarity-row-actions`** button cluster with Take-Ownership + Convert-to-task per terminal kind
- `clarity-critical-path-narrative` (UNCHANGED)

Locked literal strings (UI-SPEC §Copywriting Contract — DO NOT paraphrase):
- Take-Ownership button label: `Take ownership`
- Convert-to-task button label (non-CYCLE): `Convert to task →`
- Convert-to-task button label (CYCLE rows): `+ Create task to break this cycle`
- Take-Ownership disabled tooltip: `Sign in to claim ownership`
- Take-Ownership aria-busy label: `Claiming ownership…`
- Success toast: `Ownership claimed`
- Failure toast: `Could not claim ownership — try again`
- Empty artifact chip row: (no DOM emitted — empty windows render NOTHING per D-02)
- "+N more" affordance label when artifacts > 5: `+{N-5} more`

Color accent reservation (UI-SPEC §Color §Accent reservation list — gold `--clarity-you` / `--clarity-you-soft` reserved):
1. Critical Path HUMAN_ACTION_ON terminal border-left + background tint (existing — unchanged)
2. Awaiting-You pill (existing — unchanged)
3. `+ Create task` header button (existing — unchanged)
4. **NEW: Take-Ownership button border + label**
5. Reader's Continue-in-Chat (existing on Reader, not Situation Room)

**Convert-to-task button MUST NOT use gold accent.** Neutral border (`--clarity-line`) + body-color text (`--clarity-fg`) + transparent background.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → worker action dispatch (agent.takeOwnership) | Untrusted: UI passes `ownerUserId`; handler re-verifies server-side (Plan 06.1-01 T-06.1-01). |
| UI → ToastProvider | Trusted via React context; no external surface. |
| CSS scope → host UI | Trusted IFF every new selector is scoped under `[data-clarity-surface]` (SCAF-06; check-css-scope CI gate runs in Plan 06.1-04 verification). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-16 | Spoofing | UI submitting Take-Ownership with forged ownerUserId | mitigate | UI populates `ownerUserId` from `useResolvedUserId()`; worker re-verifies `ownerUserId === userId` server-side (Plan 06.1-01 already pins this). UI button is also disabled when `useResolvedUserId` returns null/error. |
| T-06.1-17 | Information Disclosure | Artifact chip popover leaking deliverable for issue user cannot see | mitigate | `ctx.issues.documents.list` respects host viewer-permissions (PRIM-02 inheritance). Plugin does not bypass. |
| T-06.1-18 | CSS bleed-through | New selectors leaking into host UI | mitigate | Every new CSS rule scoped under `[data-clarity-surface="situation-room"]`. SCAF-06 check-css-scope CI gate enforces (re-runs in Plan 06.1-04 verification). |
| T-06.1-19 | Accessibility regression | Take-Ownership disabled state not communicated to AT | mitigate | Native HTML `disabled` attribute (removes from tab order); `aria-label` + `title` tooltip; `aria-busy="true"` during in-flight. UI-SPEC §Accessibility Notes locks the pattern. |
| T-06.1-20 | Operator scope-creep | Per-blocker dialog pre-filling against operator constraint | mitigate | UI-SPEC §Copywriting Contract §"Dialog (per-blocker)" + D-12 + `feedback_trust-the-clarification-loop` — the dialog opens in cold mode with empty fields; blocker context renders OUTSIDE the dialog body (static context block). Test in Task 3 pins this via real-DOM assertion. |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new runtime dependencies in this plan (CONTEXT.md constraint). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New ArtifactChipRow component + delete ArtifactsShippedShelf + parent wiring</name>
  <files>src/ui/surfaces/situation-room/artifact-chip-row.tsx, src/ui/surfaces/situation-room/agent-card.tsx, src/ui/surfaces/situation-room/index.tsx, src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx, src/ui/styles/theme.css, test/ui/surfaces/situation-room/artifact-chip-row.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/index.tsx (entire file — focus on lines 131-136 for the ClaritySurfaceHeader mount, lines 176 area for usePollWithLeader, lines 181 for pauseOnHidden, lines 189 for opt-in error handler, lines 192 for plugin-disabled handler; also locate the existing ArtifactsShippedShelf import + JSX mount to delete)
    - src/ui/surfaces/situation-room/agent-card.tsx (entire file — focus on the existing composition order: header → now-doing → terminal → latest-artifact placeholder → sparkline; insert chip row between now-doing and terminal per UI-SPEC §Visual Hierarchy Lock)
    - src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx (entire file — being DELETED; read once to confirm what its prop shape was so any callers are unwound)
    - src/ui/surfaces/chat/attachment-chip-with-preview.tsx (entire file — INHERITED chip popover shell from Plan 05-11; the artifact chips inside ArtifactChipRow render this component directly)
    - src/ui/surfaces/chat/attachment-chip.tsx (entire file — the inner chip primitive; understand prop shape for direct invocation if not going through AttachmentChipWithPreview)
    - src/ui/styles/theme.css (lines 13-58 token list + lines 504-902 situation-room-specific block + lines 828-894 the .clarity-artifacts-shelf rule cluster being DELETED + lines 1024 .clarity-cold-task-btn padding shape for inherited 6px exception)
    - src/ui/styles/chat.css (lines 2604-2620 — AttachmentChip chip-styling for the inherited 6px chip-gap exception)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-UI-SPEC.md §Component Inventory + §CSS Additions Required + §Visual Hierarchy Lock + §Edge Cases (all four rows about empty artifact windows)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md §decisions D-02 + D-10 + D-11
  </read_first>
  <behavior>
    - Behavior: `<ArtifactChipRow artifacts={...} agentRole={...} />` with empty `artifacts` array → returns `null` (no DOM emitted; UI-SPEC §Edge Cases row 1).
    - Behavior: `<ArtifactChipRow>` with 1-5 chips → renders horizontal flex-wrap of `<AttachmentChipWithPreview>` per chip; each chip clickable; click opens the canonical DeliverablePreview popover (existing 3-dismissal-affordance shell).
    - Behavior: `<ArtifactChipRow>` with > 5 chips → renders first 5 chips + a `+{N-5} more` mono-styled chip; click on more-chip opens a per-agent drawer reusing the same popover shell (D-10).
    - Behavior: index.tsx no longer imports ArtifactsShippedShelf; the bottom `<section className="clarity-artifacts-shelf">` mount is removed.
    - Behavior: index.tsx threads `situation.artifacts` data into the agent grid via prop passthrough to each `<AgentCard>` (matching the existing prop-shape conventions for the snapshot data).
    - Behavior: agent-card.tsx accepts the new `artifacts` prop and renders `<ArtifactChipRow>` between `.clarity-now-doing` and `.clarity-agent-terminal` (UI-SPEC §Visual Hierarchy Lock #3).
    - File `artifacts-shipped-shelf.tsx` is DELETED.
    - Theme.css selectors `.clarity-artifacts-shelf`, `.clarity-artifacts-heading`, `.clarity-artifacts-list`, `.clarity-artifact-item`, `.clarity-artifact-title`, `.clarity-artifact-author`, `.clarity-artifact-preview` (lines 828-894) are DELETED.
    - NEW theme.css selectors `.clarity-artifact-chip-row`, `.clarity-artifact-chip-row:empty`, `.clarity-artifact-chip-row .attachment-chip` added per UI-SPEC §CSS Additions Required.
    - Real-DOM test: empty input → renders nothing (queryByTestId('clarity-artifact-chip-row') returns null).
    - Real-DOM test: 3 chips → 3 button-like chips rendered, all keyboard-focusable.
    - Real-DOM test: 7 chips → 5 chips + 1 more-chip rendered (labeled `+2 more`).
  </behavior>
  <action>
    Create `src/ui/surfaces/situation-room/artifact-chip-row.tsx`:
    - Default export `ArtifactChipRow` accepting `{ artifacts: Array<Artifact>, agentRole: string, companyId: string, userId: string | null }`. The `Artifact` type matches the worker handler's return shape (`{ id, kind: 'deliverable' | 'chat-attachment', documentKey, mimeType, originalFilename, byteSize, createdAt, topicIssueId?, sourceIssueId? }`).
    - If `artifacts.length === 0` → return `null`.
    - Otherwise: render `<div className="clarity-artifact-chip-row" data-testid="clarity-artifact-chip-row">` containing the first 5 chips via `<AttachmentChipWithPreview ... />` (one per artifact). If `artifacts.length > 5`, append a `<button className="clarity-artifact-chip-row-more">{`+${artifacts.length - 5} more`}</button>` whose click opens the per-agent drawer (reuses the AttachmentChipWithPreview popover shell scoped to the full agent artifact array).
    - Compose the `documentKey` + `mimeType` + `originalFilename` + `byteSize` props from the artifact object verbatim; pass `sourceContext` (or whichever polymorphic prop name AttachmentChipWithPreview accepts — verify from the read; if the existing shape requires a refactor to accept both topicIssueId and sourceIssueId, the planner authorizes adding a single optional `sourceContext` discriminated-union prop on AttachmentChipWithPreview WITHOUT touching its internal popover logic).

    Modify `src/ui/surfaces/situation-room/index.tsx`:
    - Remove the `import ArtifactsShippedShelf from './artifacts-shipped-shelf'` line.
    - Remove the JSX `<ArtifactsShippedShelf .../>` mount near the bottom of the component (the section currently sitting between agent-grid and pause-banner per UI-SPEC §Visual Hierarchy Lock §Situation Room body composition).
    - Add a `usePluginData('situation.artifacts', { companyId, userId })` call (matching the existing usePluginData call shape used for the snapshot) — RIGHT AFTER the existing snapshot fetch.
    - Thread the returned `artifacts` map into the agent grid by passing `artifacts={data.artifacts[employee.id] ?? []}` (or whichever ID field matches `agent_id` from Plan 06.1-02's return shape — match what `situation-artifacts.test.mjs` produces) to each `<AgentCard>`.
    - Also thread `viewerUserId` (from `useResolvedUserId()`) + `companyId` into the `<CriticalPathStrip>` mount — these are consumed by Task 2's Take-Ownership button.

    Modify `src/ui/surfaces/situation-room/agent-card.tsx`:
    - Accept new prop `artifacts?: Array<Artifact>` (default empty array).
    - Import `ArtifactChipRow` from `./artifact-chip-row`.
    - Insert `<ArtifactChipRow artifacts={artifacts ?? []} agentRole={role} companyId={companyId} userId={userId} />` between the existing `<div className="clarity-now-doing">` block and the `<div className="clarity-agent-terminal">` block (UI-SPEC §Visual Hierarchy Lock #3).
    - **Authorized deletion (UI-SPEC §Visual Hierarchy Lock #5 — recommend deletion for visual clarity):** remove the existing `.clarity-agent-artifact` latest-artifact placeholder block (the dashed-border slot). The new chip row supersedes it as the "what shipped" signal. If the existing `latest_artifact` prop is consumed elsewhere, leave the data threading intact but remove only the JSX mount.

    DELETE `src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx`. Remove any orphaned imports. Use `git rm` semantics (the executor's Write tool with a deletion-equivalent step; or a shell `rm` then commit).

    In `src/ui/styles/theme.css`:
    - DELETE the `.clarity-artifacts-shelf`, `.clarity-artifacts-heading`, `.clarity-artifacts-list`, `.clarity-artifact-item`, `.clarity-artifact-title`, `.clarity-artifact-author`, `.clarity-artifact-preview` rules at lines 828-894. (Pre-flight: `grep -rn "clarity-artifacts-shelf" src/` to confirm no other file references these classes; UI-SPEC authorizes deletion only after this rg check is clean.)
    - ADD new rules per UI-SPEC §CSS Additions Required §rows 1-3:
      - `[data-clarity-surface="situation-room"] .clarity-artifact-chip-row { padding: 8px 18px 12px; display: flex; flex-wrap: wrap; gap: 6px; }` (6px inherited exception per UI-SPEC §Inherited spacing exceptions — chat.css:2607)
      - `[data-clarity-surface="situation-room"] .clarity-artifact-chip-row:empty { display: none; }`
      - `[data-clarity-surface="situation-room"] .clarity-artifact-chip-row-more { font-family: var(--clarity-mono); font-size: 11px; padding: 4px 8px; border: 1px solid var(--clarity-line); border-radius: 6px; background: transparent; color: var(--clarity-ink-2); cursor: pointer; }`

    Create `test/ui/surfaces/situation-room/artifact-chip-row.test.mjs` — real-DOM render tests using the existing test-DOM harness (look for the closest existing real-DOM test in `test/ui/` — Plan 04.2-04 era added these for chat surface; if none exists, use the source-grep + jsdom harness used in `test/ui/primitives/`). Tests:
    1. Empty input → no DOM emitted.
    2. 3 chips → 3 chip elements rendered with `role="button"` (inherited from AttachmentChip).
    3. 7 chips → 5 + 1 more-chip labeled `+2 more`.
    4. Chip click → AttachmentChipWithPreview popover opens (verifiable via DOM presence of the backdrop element).
    5. **No-warning render:** assert React-key warnings count === 0 after rendering the component (this is the rc.8 "real-DOM tests matter" lesson from STATE.md §Phase B Closure Record §Session lessons).
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/artifact-chip-row.test.mjs && grep -c "clarity-artifacts-shelf" src/ui/styles/theme.css</automated>
  </verify>
  <acceptance_criteria>
    - File `src/ui/surfaces/situation-room/artifact-chip-row.tsx` exists and default-exports `ArtifactChipRow`.
    - File `src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx` is DELETED (not just emptied — `test -f src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx` returns non-zero).
    - Source assertion: `grep -c "ArtifactsShippedShelf" src/ui/surfaces/situation-room/index.tsx` = 0 (import + mount both removed).
    - Source assertion: `grep -c "ArtifactChipRow" src/ui/surfaces/situation-room/agent-card.tsx` ≥ 2 (import + JSX mount).
    - Source assertion: `grep -c "usePluginData.*situation.artifacts" src/ui/surfaces/situation-room/index.tsx` ≥ 1.
    - Source assertion: `grep -c "clarity-artifacts-shelf\|clarity-artifacts-heading\|clarity-artifacts-list\|clarity-artifact-item\|clarity-artifact-title\|clarity-artifact-author\|clarity-artifact-preview" src/ui/styles/theme.css` = 0 (all 7 orphaned selectors deleted).
    - Source assertion: `grep -c "clarity-artifact-chip-row" src/ui/styles/theme.css` ≥ 2 (new selectors added).
    - Source assertion: `grep -c '\[data-clarity-surface="situation-room"\]' src/ui/styles/theme.css` strictly increases vs pre-edit baseline (CSS scope intact per SCAF-06).
    - Test command: `node --test test/ui/surfaces/situation-room/artifact-chip-row.test.mjs` exits 0 — all 5 tests pass; Test 5 specifically asserts React-key-warning count = 0 (rc.8 lesson).
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>ArtifactChipRow component shipped, mounted in AgentCard, bottom shelf deleted (both file + CSS); index.tsx threads artifact data; real-DOM tests green with zero React-key warnings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Critical Path per-row Take-Ownership + Convert-to-task buttons (with CYCLE special-case)</name>
  <files>src/ui/surfaces/situation-room/critical-path-strip.tsx, src/ui/styles/theme.css, test/ui/surfaces/situation-room/critical-path-affordances.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/critical-path-strip.tsx (entire file — focus on line 44 NO_OWNER_ASSIGNED_RE filter; line 60 the `actionable.length === 0 → null` short-circuit; the rc.8 final shape of the row composition)
    - src/ui/primitives/use-resolved-user-id.ts (entire file — INHERITED hook; understand return shape `{ userId, loading, error }`)
    - src/ui/primitives/toast.tsx (entire file — INHERITED toast hook; understand the success/error/info channel names)
    - src/ui/primitives/clarity-surface-header.tsx (entire file — INHERITED; verify the `+ Create task` button render conditions per UI-SPEC §Flow 5 note; if a SURFACES_WITH_CREATE_TASK registry exists, locate the array and add 'situation-room' to it; if no registry exists, the button renders unconditionally and no code change is needed)
    - src/ui/surfaces/chat/true-task/true-task-dialog.tsx (entire file — INHERITED cold-mode dialog; the Convert-to-task button opens THIS dialog with NO pre-fill per D-12)
    - src/ui/styles/theme.css (lines 1024 area — .clarity-cold-task-btn padding shape for inherited 6px exception)
    - src/shared/blocker-chain.ts (lines 130-189 — READ ONLY; confirms terminal taxonomy `HUMAN_ACTION_ON | SELF_RESOLVING | EXTERNAL | CYCLE` and the `__unowned__` string sentinel. This file is LOCKED — do NOT modify.)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-UI-SPEC.md §Copywriting Contract (locked literal strings) + §Flow 1 + §Flow 2 + §Flow 3 + §Edge Cases (cycle row + resolved-owner row) + §CSS Additions Required (rows 4-8) + §Accessibility Notes
    - .planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md §decisions D-03 + D-04 + D-09 + D-12 + D-13
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Gotchas item 7 (the __unowned__ string literal — tests must compare against the literal `'__unowned__'`)
  </read_first>
  <behavior>
    - Real-DOM test 1 (HUMAN_ACTION_ON.__unowned__ terminal): row renders BOTH `Take ownership` + `Convert to task →` buttons.
    - Real-DOM test 2 (HUMAN_ACTION_ON with resolved owner): row renders ONLY `Convert to task →` (no Take-Ownership — ownership already exists).
    - Real-DOM test 3 (SELF_RESOLVING terminal): row renders ONLY `Convert to task →`.
    - Real-DOM test 4 (EXTERNAL terminal): row renders ONLY `Convert to task →`.
    - Real-DOM test 5 (CYCLE terminal): row renders ONLY `+ Create task to break this cycle` (NO Take-Ownership — D-13).
    - Real-DOM test 6 (useResolvedUserId returns null/error): Take-Ownership button has `disabled` attribute AND `title="Sign in to claim ownership"`.
    - Real-DOM test 7 (Take-Ownership click on __unowned__ row): `usePluginAction('agent.takeOwnership')` dispatched with `{ agentId, ownerUserId: viewerUserId, companyId }`; on success a toast `Ownership claimed` fires; on failure toast `Could not claim ownership — try again`.
    - Real-DOM test 8 (Convert-to-task click): opens TrueTaskDialog in cold mode; dialog editable fields are EMPTY (D-12). Blocker context (chain narration) appears OUTSIDE the dialog body — assert via DOM that the title input value === '' and that a `.clarity-critical-path-blocker-context` element exists OUTSIDE the dialog tree.
    - Real-DOM test 9 (CYCLE Convert-to-task click): button label literal === `+ Create task to break this cycle`; dialog opens with empty fields; blocker context shows the cycle path.
    - **No-warning render:** assert React-key-warnings count === 0 after rendering all variants.
  </behavior>
  <action>
    In `src/ui/surfaces/situation-room/critical-path-strip.tsx`:

    1. Add imports: `useResolvedUserId` (from `../../primitives/use-resolved-user-id`), `useToast` (from `../../primitives/toast`), `usePluginAction` (host SDK), `usePluginData`'s query-key invalidation handle (matching the pattern used elsewhere — typically `useQueryClient` or a re-exported invalidation helper from the existing surface hooks; locate via the precedent in chat surface or critical-path-strip's existing data wiring), and `TrueTaskDialog` (the cold-mode dialog from `../chat/true-task/true-task-dialog`).

    2. Accept new props on the component: `viewerUserId: string | null`, `companyId: string` (both threaded from index.tsx per Task 1).

    3. For each Critical Path row inside the existing render loop:
       - Compute `isCycleRow = terminal.kind === 'CYCLE'`.
       - Compute `isUnownedHumanAction = terminal.kind === 'HUMAN_ACTION_ON' && terminal.userId === '__unowned__'`.
       - Compute `showTakeOwnership = isUnownedHumanAction` (per UI-SPEC §Edge Cases + D-13).
       - Compute `convertToTaskLabel = isCycleRow ? '+ Create task to break this cycle' : 'Convert to task →'` (literal strings from UI-SPEC §Copywriting Contract).
       - Render a new `<div className="clarity-row-actions">` cluster AFTER the existing `.clarity-critical-path-text` block:
         - IF `showTakeOwnership`: render `<button className="clarity-take-ownership-btn" ...>Take ownership</button>` with the dispatch + disabled-state logic detailed below.
         - Always render `<button className="clarity-convert-to-task-btn" onClick={() => setDialogOpen(true)}>{convertToTaskLabel}</button>`.

    4. Take-Ownership button logic:
       - Disabled state: `disabled={!viewerUserId}`; `title={viewerUserId ? undefined : 'Sign in to claim ownership'}`; `aria-label={`Take ownership of ${humanizeAgent(terminal.agentId)}`}` (use the existing humanize-snapshot label or pass the row's display label via props).
       - In-flight state: local `useState<boolean>(false)` for `claiming`; while claiming: `aria-busy="true"`, `aria-label` swaps to `Claiming ownership of ${role}…` (per UI-SPEC §Copywriting Contract); button text stays literal `Take ownership`.
       - On click (when enabled): call `takeOwnership({ companyId, agentId: terminal.agentId, ownerUserId: viewerUserId, userId: viewerUserId })`. On `{ ok: true }`: `toast.success('Ownership claimed')`, invalidate the snapshot query key to force-refetch. On `{ error }`: `toast.error('Could not claim ownership — try again')`. Reset `claiming = false` in finally.

    5. Convert-to-task / cycle-break button logic:
       - Local state for dialog open + blocker-context payload.
       - On click: set `dialogOpen = true`, set `blockerContext = { chainNarration: row.narrative, terminalAgentId: terminal.agentId, blockerReason: ... }`.
       - Mount `<TrueTaskDialog mode="cold" open={dialogOpen} onClose={() => setDialogOpen(false)} onTaskCreated={() => { setDialogOpen(false); /* existing snapshot refetch */ }} />`. Pass NO initial values — D-12 + load-bearing operator constraint (`feedback_trust-the-clarification-loop`).
       - Render `<aside className="clarity-critical-path-blocker-context">...chain narration + terminal agent ID + blocker reason verbatim...</aside>` OUTSIDE the dialog body but adjacent to it (planner picks: either render alongside the dialog as a side-panel within the same portal, OR render as a static block that appears next to the dialog backdrop). The blocker-context block MUST be readable while the dialog is open; it MUST NOT be inside any editable field.

    In `src/ui/styles/theme.css`, ADD the following new selectors per UI-SPEC §CSS Additions Required (all scoped under `[data-clarity-surface="situation-room"]`):

    - `.clarity-critical-path-item .clarity-row-actions { display: inline-flex; gap: 8px; margin-top: 8px; }`
    - `.clarity-take-ownership-btn { border: 1px solid var(--clarity-you); color: var(--clarity-you); background: transparent; padding: 6px 12px; border-radius: 6px; font-weight: 500; cursor: pointer; }`
    - `.clarity-take-ownership-btn:hover { background: var(--clarity-you-soft); }`
    - `.clarity-take-ownership-btn:disabled, .clarity-take-ownership-btn[aria-busy="true"] { opacity: 0.6; cursor: not-allowed; }`
    - `.clarity-convert-to-task-btn { border: 1px solid var(--clarity-line); color: var(--clarity-fg); background: transparent; padding: 6px 12px; border-radius: 6px; font-weight: 500; cursor: pointer; }`
    - `.clarity-convert-to-task-btn:hover { background: var(--clarity-bg-3); border-color: var(--clarity-line-bright); }`
    - `.clarity-critical-path-blocker-context { ...reuse .clarity-ref-card or .true-task-dialog-source shape per UI-SPEC §CSS Additions Required row 10... }`

    Per UI-SPEC §Color §Accent reservation list: gold `--clarity-you` MUST appear ONLY on `.clarity-take-ownership-btn` (border + label) and `.clarity-take-ownership-btn:hover` (background). Convert-to-task button MUST use neutral chrome — `--clarity-line` border + `--clarity-fg` text + transparent background. Do NOT use gold on the convert button anywhere.

    Verify the surface-header `+ Create task` button: read `src/ui/primitives/clarity-surface-header.tsx`. If the button is rendered unconditionally (per UI-SPEC §Flow 5 note from 2026-05-26 read), no code change is needed — verification falls to the Plan 06.1-04 closure drill. If a `SURFACES_WITH_CREATE_TASK` registry exists, add the literal `'situation-room'` to the array (one-line change).

    Create `test/ui/surfaces/situation-room/critical-path-affordances.test.mjs` — real-DOM render tests covering all 9 behaviors above. Use the same test-DOM harness as Task 1's artifact-chip-row test. Each test renders `<CriticalPathStrip>` with a synthetic terminal payload covering one of the 5 terminal kinds + a viewerUserId variant.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/critical-path-affordances.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "clarity-take-ownership-btn" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1.
    - Source assertion: `grep -c "clarity-convert-to-task-btn" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1.
    - Source assertion: `grep -c "Take ownership" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1 (literal label from UI-SPEC).
    - Source assertion: `grep -c "Convert to task" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1.
    - Source assertion: `grep -c "Create task to break this cycle" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1 (CYCLE-specific literal).
    - Source assertion: `grep -c "Sign in to claim ownership" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1 (disabled tooltip literal).
    - Source assertion: `grep -c "Ownership claimed" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1 (success toast literal).
    - Source assertion: `grep -c "Could not claim ownership" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1 (failure toast literal).
    - Source assertion: `grep -c "__unowned__" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1 (string-sentinel comparison per RESEARCH.md §Gotchas #7).
    - Source assertion: `grep -c "useResolvedUserId" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1.
    - Source assertion: `grep -c "usePluginAction.*agent.takeOwnership" src/ui/surfaces/situation-room/critical-path-strip.tsx` ≥ 1.
    - Source assertion: `grep -c "clarity-take-ownership-btn\|clarity-convert-to-task-btn\|clarity-row-actions\|clarity-critical-path-blocker-context" src/ui/styles/theme.css` ≥ 4.
    - Source assertion: `grep -c "var(--clarity-you)" src/ui/styles/theme.css` lines added include the Take-Ownership rules only — NOT the Convert-to-task rules. (Verify by inspecting the diff: convert-to-task CSS uses `var(--clarity-line)` + `var(--clarity-fg)`.)
    - Source assertion: `git diff src/shared/blocker-chain.ts` is empty (LOCKED invariant).
    - Test command: `node --test test/ui/surfaces/situation-room/critical-path-affordances.test.mjs` exits 0 — all 9 tests pass, including no-warning render assertion.
    - Typecheck: `pnpm exec tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Per-row Take-Ownership (gold accent) + Convert-to-task (neutral chrome) shipped on Critical Path strip; CYCLE special-case + no-prefill + viewerUserId-null disabled state all pinned by real-DOM tests; blocker-chain.ts unchanged.</done>
</task>

</tasks>

<verification>
**UI-tier closure:**

1. `node --test test/ui/surfaces/situation-room/artifact-chip-row.test.mjs` → 5 tests pass; no React-key warnings.
2. `node --test test/ui/surfaces/situation-room/critical-path-affordances.test.mjs` → 9 tests pass; no React-key warnings.
3. `pnpm exec tsc --noEmit` → clean.
4. `git diff src/shared/blocker-chain.ts src/worker/jobs/humanize-snapshot.ts` → empty (LOCKED invariants).
5. CSS scope check (will re-run in Plan 06.1-04's coexistence run-all): every new selector scoped under `[data-clarity-surface="situation-room"]`.
6. Color reservation check: `--clarity-you` appears in `.clarity-take-ownership-btn*` rules and NOT in `.clarity-convert-to-task-btn*` rules.
7. `test ! -f src/ui/surfaces/situation-room/artifacts-shipped-shelf.tsx` (file deleted).
8. Literal-string lock: every locked string from UI-SPEC §Copywriting Contract appears verbatim in the source.

**Playwright/real-DOM sanity (manual, executed in Plan 06.1-04 drill on Countermoves):**
- Each Critical Path row renders the correct affordance set per terminal kind.
- Take-Ownership click on a `__unowned__` row results in: (1) toast `Ownership claimed`, (2) row no longer shows Take-Ownership after refetch.
- Convert-to-task dialog has empty editable fields and shows blocker context outside the body.
- ArtifactChipRow appears under each agent card with ≥ 1 chip; chip click opens preview popover.
- Bottom artifact shelf is gone.
</verification>

<success_criteria>
- ArtifactChipRow component shipped; bottom shelf (component + CSS) deleted.
- Critical Path rows render Take-Ownership (when applicable per terminal + D-13) + Convert-to-task (always non-CYCLE) + cycle-specific button (CYCLE only).
- Locked literal strings present verbatim (Take ownership / Convert to task → / + Create task to break this cycle / Sign in to claim ownership / Ownership claimed / Could not claim ownership — try again).
- Gold accent (--clarity-you) applied ONLY to Take-Ownership button per UI-SPEC §Color accent reservation list.
- Per-blocker dialog opens with NO pre-fill (D-12 / `feedback_trust-the-clarification-loop`); blocker context outside editable fields.
- blocker-chain.ts and humanize-snapshot.ts unchanged (LOCKED).
- Real-DOM tests green with zero React-key warnings (rc.8 lesson).
- CSS scope intact under `[data-clarity-surface="situation-room"]`.
- Total net-new CSS lines ≤ 60 (UI-SPEC §CSS Additions Required ceiling).
- Typecheck clean; no new runtime deps.
</success_criteria>

<output>
Create `.planning/phases/06.1-situation-room-spec-complete/06.1-03-SUMMARY.md` when done — record the actual UI diff, the test count, the deleted shelf file, and any deviations from the UI-SPEC.
</output>
