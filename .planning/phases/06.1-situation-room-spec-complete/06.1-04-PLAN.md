---
phase: 06.1-situation-room-spec-complete
plan: 04
type: execute
wave: 3
depends_on: [06.1-03]
files_modified:
  - scripts/coexistence-checks/11-take-ownership.mjs
  - scripts/coexistence-checks/run-all.mjs
  - test/phases/06.1-traceability.test.mjs
  - .planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md
autonomous: false
requirements: [ROOM-09, ROOM-10, ROOM-11]
tags: [coexistence, closure, traceability, drill]
must_haves:
  truths:
    - "scripts/coexistence-checks/11-take-ownership.mjs exists, runs, and asserts COEXIST-11 (clarity_agent_owners survives disable/enable with byte-identical rowcount; agent-take-ownership.ts contains zero ctx.issues.update)"
    - "run-all.mjs registers COEXIST-11 → 11-take-ownership.mjs at the next available registry slot"
    - "test/phases/06.1-traceability.test.mjs pins ROOM-09/10/11 each appear in at least one plan's `requirements` frontmatter"
    - "clarity-pack-1.0.0.tgz REPACKED (no version bump — version literal stays 1.0.0 per Plan 05-10 invariant + project memory plugin-version-bump-two-sources)"
    - "Eric's live Countermoves drill verifies all 5 Phase 6.1 success criteria + COEXIST-11 PASS"
  artifacts:
    - path: "scripts/coexistence-checks/11-take-ownership.mjs"
      provides: "COEXIST-11 coexistence check"
      contains: "clarity_agent_owners"
    - path: "scripts/coexistence-checks/run-all.mjs"
      provides: "Registry entry { id: 'COEXIST-11', script: '11-take-ownership.mjs' }"
      contains: "COEXIST-11"
    - path: "test/phases/06.1-traceability.test.mjs"
      provides: "RED→GREEN traceability assertion for ROOM-09/10/11"
    - path: ".planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md"
      provides: "Closure record with operator drill PASS verdict"
  key_links:
    - from: "scripts/coexistence-checks/run-all.mjs"
      to: "11-take-ownership.mjs"
      via: "registry entry COEXIST-11"
      pattern: "11-take-ownership"
    - from: "test/phases/06.1-traceability.test.mjs"
      to: "06.1-01-PLAN.md + 06.1-02-PLAN.md + 06.1-03-PLAN.md frontmatter"
      via: "requirements field grep across .planning/phases/06.1-*/*-PLAN.md"
      pattern: "ROOM-09|ROOM-10|ROOM-11"
---

<objective>
Land the closure tier of Phase 6.1: coexistence check #11 (`11-take-ownership.mjs`), its registry entry in `run-all.mjs`, the Phase 6.1 traceability test, the repacked `clarity-pack-1.0.0.tgz` (no version bump — Plan 05-10 owns the rc → 1.0.0 transition), and Eric's live Countermoves drill that verifies the five Phase 6.1 success criteria + COEXIST-11.

This plan has TWO autonomous sub-tasks (the coexistence check + the traceability test + the repack) followed by ONE blocking human-verify checkpoint (Eric's Playwright + safety-CLI drill). The post-drill traceability flip in REQUIREMENTS.md (ROOM-09/10/11 → Implemented) is OUT OF SCOPE for this plan — it lives in the closure commit that the planner agent writes AFTER a passing drill verdict (matches Plan 05-10 / 05-11 pattern of "traceability flip lives in post-drill closure commit").

Purpose: Close Phase 6.1 with verified coexistence guarantees #3 + #6 (additive schema; disable preserves data) and a live operator-driven drill against Countermoves that confirms the rc.8 Phase B Playwright finding is closed.

Output: New coexistence check + registry update + traceability test + repacked tarball + 06.1-VERIFICATION.md with operator PASS verdict.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-CONTEXT.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md
@.planning/phases/06.1-situation-room-spec-complete/06.1-UI-SPEC.md
@CLAUDE.md

<interfaces>
Key contracts (reproduced verbatim from RESEARCH.md):

From `scripts/coexistence-checks/09-true-task.mjs` (canonical coexistence-check template, Plan 04.1-07 — 284 lines):
- Asserts plugin-namespace tables remain after disable/enable (rowcount byte-identical).
- Source-grep asserts `ctx.issues.update` count = 0 across a list of clarity-pack handler paths.
- Worker-tree scan for `ctx.issues.delete` / `ctx.issues.deleteComment` (none allowed).
- Manifest declaration scan for destructive uninstall hooks (none allowed).
- Defensive existence checks on the new phase source files.

From `scripts/coexistence-checks/run-all.mjs` (registry of all coexistence checks; lines 17-28):
- Pattern: `{ id: 'COEXIST-NN', script: 'NN-name.mjs' }` entries; the script runs and reports PASS/FAIL to a stdout summary.
- Existing entries (verified 2026-05-26): 01-original-ui-unchanged through 10-uninstall-runbook. Next slot is **11**.

From `test/phases/04.1-traceability.test.mjs` (closure-phase traceability template):
- Pattern: grep across `.planning/phases/<phase>/*-PLAN.md` frontmatter `requirements` field; assert each requirement ID appears in at least one plan.
- Uses `fs.readFileSync` + a YAML/regex parse of the frontmatter.

From CLAUDE.md "bookended-by-snapshots" rule (Phase 1 SAFE-01..05):
- Every clarity-pack action against the live BEAAA Paperclip instance MUST be bookended by a verified Postgres + filesystem snapshot taken immediately before AND a working rollback path verified at least once before any feature work ships.
- Eric's drill against Countermoves uses the safety-CLI; the snapshot bookend is operator-driven via `pnpm clarity-safety snapshot ...`.

From project memory `plugin-version-bump-two-sources`:
- Version literal lives in BOTH `package.json` AND `src/manifest.ts` (currently `'1.0.0'`).
- Phase 6.1 ships NO version bump — the tarball is a BINARY REPACK at `1.0.0`. Matches Plan 05-10 hotfix + Plan 05-11 HOTFIX-1/2 precedent.

From project memory `gsd-execute-plan-not-real`:
- Addendum execution is `/gsd:execute-phase <N> --gaps`; never `/gsd:execute-plan <id>`. STATE.md `next_action` must be updated post-planning to reflect the correct invocation.

From STATE.md §Phase B Closure Record §Deferred to Plan 06-01:
- The Plan 06-01 / Phase 6.1 success drill MUST verify these specific symptoms are closed:
  - Every Critical Path row that previously showed "Agent has no owner assigned" now shows either a resolved owner OR a working Take-Ownership button.
  - Each agent card shows an inline ArtifactChipRow (or nothing if 24h window is empty).
  - Bottom artifact shelf is gone.
  - `+ Create task` button visible on Situation Room surface header (UI-SPEC §Flow 5 verification).
  - Convert-to-task per-blocker button opens TrueTaskDialog with empty fields.
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Coexistence check → plugin disable/enable cycle | Trusted: pnpm paperclipai plugin disable + enable is host-controlled; the check observes pre/post DB state. |
| Drill → live Countermoves | Untrusted at install time: the safety-CLI snapshot bookend is the mitigation per CLAUDE.md bookended-by-snapshots rule. Eric authenticates personally; no transcript credentials. |
| Traceability test → planning frontmatter | Trusted: planner-only writes; YAML parse is purely defensive. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-21 | Tampering | Drill against live Countermoves without snapshot | mitigate | Drill step 1 is `pnpm clarity-safety snapshot ...` per CLAUDE.md bookended-by-snapshots rule. Drill MUST NOT proceed without verified snapshot ≤ 15 minutes old (SAFE-05 pre-flight gate). Eric is the authenticated operator; no Claude credential surface. |
| T-06.1-22 | Information Disclosure | Production credentials in transcript | mitigate | Per STATE.md §Session lessons: NEVER paste production credentials in transcript. Eric authenticates personally; drill steps reference env vars + safety-CLI invocations only. Pending-operator-item #1 (rotate password) is acknowledged. |
| T-06.1-23 | Repudiation | Drill PASS claim without verifiable evidence | mitigate | Drill produces specific assertions: (a) `SELECT count(*) FROM clarity_agent_owners` byte-identical before vs after disable/enable; (b) Playwright DOM-anchor screenshots of the 4 affordance variants; (c) `pnpm test` suite green. All recorded in 06.1-VERIFICATION.md. |
| T-06.1-24 | Tampering | Coexistence-check regression silently passes | mitigate | The check itself has a regression test pattern (matches 09-true-task.mjs structure: every assertion has a deterministic expected output). If a future plan adds a destructive uninstall hook, the check fails noisily. |
| T-06.1-25 | Elevation of Privilege | Repacked tarball pulls in unexpected code | mitigate | Repack is `pnpm pack` from a clean working tree on master HEAD; sha256 recorded in 06.1-VERIFICATION.md; matches existing tarball-trail discipline (STATE.md). |
| T-06.1-SC | Tampering | npm/pip/cargo installs | mitigate | ZERO new runtime dependencies (CONTEXT.md constraint). Repack consumes only files already on disk. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Coexistence check #11 (11-take-ownership.mjs) + registry entry + traceability test</name>
  <files>scripts/coexistence-checks/11-take-ownership.mjs, scripts/coexistence-checks/run-all.mjs, test/phases/06.1-traceability.test.mjs</files>
  <read_first>
    - scripts/coexistence-checks/09-true-task.mjs (entire 284-line file — byte-for-byte template per RESEARCH.md §Pattern 6)
    - scripts/coexistence-checks/run-all.mjs (entire file — focus on the existing registry entries at lines 17-28; confirm next slot is 11 since 10-uninstall-runbook.mjs already exists per RESEARCH.md §Gotchas #1)
    - scripts/coexistence-checks/10-uninstall-runbook.mjs (existence check — confirms slot 10 is taken; the new check is **11**, not 10)
    - test/phases/04.1-traceability.test.mjs (or whichever closure-phase traceability file ships on master — fork as template)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-RESEARCH.md §Pattern 6 + §Test Shape Blueprint §Test 5
    - .planning/phases/06.1-situation-room-spec-complete/06.1-01-PLAN.md frontmatter (verify ROOM-09 is listed under `requirements`)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-02-PLAN.md frontmatter (verify ROOM-10 is listed)
    - .planning/phases/06.1-situation-room-spec-complete/06.1-03-PLAN.md frontmatter (verify ROOM-09/10/11 are listed)
  </read_first>
  <action>
    Create `scripts/coexistence-checks/11-take-ownership.mjs` forking `09-true-task.mjs` verbatim. Replacements per RESEARCH.md §Pattern 6:
    - `TRUE_TASK_TABLES` → `[ 'clarity_agent_owners' ]`
    - `ARCHIVE_HANDLER_PATH` → `src/worker/handlers/agent-take-ownership.ts`
    - `PHASE_4_1_SOURCES` → `[ 'src/worker/handlers/agent-take-ownership.ts', 'src/worker/handlers/situation-artifacts.ts', 'src/worker/db/clarity-agent-owners-repo.ts' ]`
    - Headline assertion message updated to COEXIST-11 / Phase 6.1 / ROOM-09 wording.

    The check asserts (verbatim from RESEARCH.md §Pattern 6 §What it asserts):
    1. Migrations are additive-only on Phase 6.1 surfaces (no `DROP TABLE clarity_agent_owners`, no `DROP COLUMN owner_user_id`, no `DROP SCHEMA plugin_clarity_pack_cdd6bda4bd` anywhere in `migrations/`).
    2. `agent-take-ownership.ts` contains zero `ctx.issues.update` call sites (CTT-07 source-grep — defense-in-depth with Plan 06.1-01 Task 2's runtime spy + Task 2's source-grep companion test).
    3. Worker tree has no `ctx.issues.delete` / `ctx.issues.deleteComment` calls anywhere (verified via grep over `src/worker/**/*.ts`).
    4. Manifest declares no destructive uninstall hook (grep `src/manifest.ts` for any `uninstall` or `destructive` literal — should return 0).
    5. Phase 6.1 source files exist (defensive — the check is scanning the current generation).

    Modify `scripts/coexistence-checks/run-all.mjs`: locate the existing registry array (lines 17-28); append a new entry `{ id: 'COEXIST-11', script: '11-take-ownership.mjs' }` (matches RESEARCH.md §Pattern 6 §Register at line 28).

    Create `test/phases/06.1-traceability.test.mjs` forking `test/phases/04.1-traceability.test.mjs` (or whichever closure-phase template ships on master). Test asserts: each of `ROOM-09`, `ROOM-10`, `ROOM-11` appears in the `requirements` frontmatter field of at least one plan file under `.planning/phases/06.1-situation-room-spec-complete/*-PLAN.md`. Implementation: read each plan file, regex-extract the `requirements: [...]` line from the YAML frontmatter, parse the IDs, accumulate into a Set, then assert the Set contains all three IDs.

    **Do NOT modify REQUIREMENTS.md** — the ROOM-09/10/11 → Implemented flip lives in the post-drill closure commit (NOT in this plan execution; matches Plan 05-10 / 05-11 pattern).
  </action>
  <verify>
    <automated>node scripts/coexistence-checks/run-all.mjs && node --test test/phases/06.1-traceability.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - File `scripts/coexistence-checks/11-take-ownership.mjs` exists.
    - Source assertion: `grep -c "clarity_agent_owners" scripts/coexistence-checks/11-take-ownership.mjs` ≥ 2 (table name + assertion).
    - Source assertion: `grep -c "COEXIST-11" scripts/coexistence-checks/11-take-ownership.mjs` ≥ 1 (headline literal).
    - Source assertion: `grep -c "COEXIST-11" scripts/coexistence-checks/run-all.mjs` ≥ 1 (registry entry).
    - Source assertion: `grep -c "11-take-ownership" scripts/coexistence-checks/run-all.mjs` ≥ 1.
    - File `test/phases/06.1-traceability.test.mjs` exists.
    - Source assertion: `grep -c "ROOM-09\|ROOM-10\|ROOM-11" test/phases/06.1-traceability.test.mjs` ≥ 3 (one match per ID).
    - Test command: `node scripts/coexistence-checks/run-all.mjs` exits 0 — all 11 COEXIST-* checks PASS (no regression on the 10 existing checks; COEXIST-11 green).
    - Test command: `node --test test/phases/06.1-traceability.test.mjs` exits 0.
    - Source assertion: `git diff .planning/REQUIREMENTS.md` is empty (the Implemented-flip is a post-drill commit, NOT in this plan).
  </acceptance_criteria>
  <done>COEXIST-11 check + registry entry + Phase 6.1 traceability test all shipped; all coexistence checks green (11/11); REQUIREMENTS.md untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Build + repack clarity-pack-1.0.0.tgz (no version bump)</name>
  <files>(repack only — no source changes; produces clarity-pack-1.0.0.tgz binary)</files>
  <read_first>
    - .planning/STATE.md §Plan 05-11 HOTFIX-2 record (the repack workflow + the version-literal-unchanged invariant)
    - package.json (verify `version` field still reads `1.0.0`)
    - src/manifest.ts (verify line ~337 still reads `version: '1.0.0'`)
    - project memory note: `plugin-version-bump-two-sources` (version lives in BOTH places; Phase 6.1 ships NEITHER)
    - scripts/build-worker.mjs + scripts/build-ui.mjs + scripts/build-manifest.mjs (or equivalent — the existing build pipeline; located via package.json scripts)
    - .planning/phases/05-distribution-polish/05-11-HOTFIX-COMMENT-ID-AND-CHIP-CLICK.md (precedent for the repack-without-bump pattern)
  </read_first>
  <action>
    Pre-flight checks:
    1. `grep "^\"version\":" package.json` returns `1.0.0`.
    2. `grep "version:" src/manifest.ts | head -3` confirms `'1.0.0'` literal (matches project memory `plugin-version-bump-two-sources`).
    3. `pnpm exec tsc --noEmit` exits 0.
    4. All Phase 6.1 source files from Plans 06.1-01, 06.1-02, 06.1-03 are committed (`git status -s | grep -v "^.. \\.planning" | grep -v "^.. scripts/coexistence-checks/11-" | grep -v "^.. test/phases/06.1-"` should be empty before the build runs — i.e., the only uncommitted files are this Plan 06.1-04's own outputs).

    Build:
    1. Run the worker build (whatever script package.json's `build:worker` invokes — typically `node scripts/build-worker.mjs`). Confirm `dist/worker.js` produced.
    2. Run the UI build (`pnpm build:ui` or equivalent). Confirm `dist/ui/` populated.
    3. Run the manifest build (`pnpm build:manifest` or equivalent). Confirm `dist/manifest.js` produced AND its `version` literal reads `'1.0.0'`.
    4. Run the full bundle build (`pnpm build` or whichever orchestrator).

    Smoke-load test (per Plan 05-10 worker-bundler hotfix lesson):
    - `node --check dist/worker.js` exits 0 (basic syntax validity).
    - `node -e "import('./dist/worker.js').then(() => process.exit(0), e => { console.error(e); process.exit(1); })"` exits 0 (ESM eval).

    Pack:
    1. `pnpm pack` (produces `clarity-pack-1.0.0.tgz` in the repo root).
    2. Compute and record sha256: `sha256sum clarity-pack-1.0.0.tgz` (Linux/WSL) OR `Get-FileHash clarity-pack-1.0.0.tgz -Algorithm SHA256` (PowerShell).
    3. Confirm tarball byte size + sha256 in STATE.md format (matches the existing tarball-trail entries).

    **NO version bump anywhere.** `git diff package.json src/manifest.ts` MUST be empty for the version field after the build (the `dist/manifest.js` is generated from `src/manifest.ts`; only `dist/*` should differ from the pre-build tree, and `dist/*` is gitignored).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit && pnpm build && node --check dist/worker.js && test -f clarity-pack-1.0.0.tgz</automated>
  </verify>
  <acceptance_criteria>
    - File `clarity-pack-1.0.0.tgz` exists in the repo root.
    - Source assertion: `grep "\"version\":" package.json` returns the literal `"1.0.0"` (unchanged from pre-build).
    - Source assertion: `grep "version:" src/manifest.ts | head -3` matches `'1.0.0'` literal (unchanged).
    - Test command: `pnpm exec tsc --noEmit` exits 0.
    - Test command: `pnpm test` exits 0 (full suite green — including the new Phase 6.1 tests from Plans 06.1-01, 06.1-02, 06.1-03 + the COEXIST-11 + traceability test from this plan's Task 1).
    - Test command: `node --check dist/worker.js` exits 0.
    - Test command: `node scripts/coexistence-checks/run-all.mjs` exits 0 (all 11 checks green).
    - CLI output: `sha256sum clarity-pack-1.0.0.tgz` (or PowerShell equivalent) emits a sha256 string; this hash is recorded in 06.1-VERIFICATION.md (Task 3 below).
  </acceptance_criteria>
  <done>Tarball repacked at version 1.0.0 (no bump); all gates green; sha256 captured for the closure record.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Eric's live Countermoves closure drill (Playwright + safety-CLI) + 06.1-VERIFICATION.md</name>
  <files>.planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/06.1-situation-room-spec-complete/06.1-UI-SPEC.md §Flow 1 + §Flow 2 + §Flow 3 + §Flow 4 + §Flow 5 (the 5 interaction flows that must be drilled)
    - CLAUDE.md (bookended-by-snapshots rule — drill step 1 is always a verified snapshot)
    - MemPalace: clarity_pack/runbook drawer for "Countermoves safety CLI invocation" (project memory `countermoves-safety-cli-invocation` + `countermoves-verify-install-drill`) — operator-tested invocation patterns
    - MemPalace: clarity_pack/runbook drawer for "feedback_vps-commands-self-contained" — every VPS bash block MUST start with the canonical env re-export (DB_URL via sudo grep, API_URL/KEY, COMPANY_ID); never assume env vars survive across messages
    - .planning/STATE.md §Phase B Closure Record §Deferred to Plan 06-01 (the symptom list this drill must close)
  </read_first>
  <what-built>
    Plans 06.1-01..03 shipped:
    - Migration 0013 `clarity_agent_owners` plugin-namespace side table (4 columns: agent_id PK, owner_user_id, company_id, set_at)
    - `agent.takeOwnership` worker action handler with server-side viewer-authority re-check (CTT-07 by construction)
    - `situation.artifacts` worker data handler unioning ctx.issues.documents.list ∪ chat_message_attachments (24h-sliding default, configurable via instanceConfigSchema.situationArtifactsWindow)
    - `clarity-agent-owners-repo.ts` typed CRUD wrapper
    - `situation-snapshot.ts` integration: side-table-wins owner-resolution merged into the per-company lookup-map construction
    - New UI component `ArtifactChipRow` rendered inline per agent card
    - Critical Path strip now renders Take-Ownership (gold accent) + Convert-to-task (neutral chrome) buttons per terminal kind
    - CYCLE terminals render ONLY `+ Create task to break this cycle` (D-13)
    - Bottom `<ArtifactsShippedShelf>` DELETED (file + CSS)
    - COEXIST-11 coexistence check + run-all.mjs registry entry
    - Traceability test pinning ROOM-09/10/11 in plan frontmatter
    - Tarball repacked as `clarity-pack-1.0.0.tgz` (sha256 captured in Task 2; NO version bump — Plan 05-10 owns rc → 1.0.0)
    - blocker-chain.ts + humanize-snapshot.ts UNCHANGED (LOCKED INVARIANTS preserved)
    - Zero new runtime dependencies
    - Suite delta: ~26 new tests (Plan 06.1-01 +10 + CTT-07 source-grep, Plan 06.1-02 +11 + CTT-07 source-grep, Plan 06.1-03 +5+9 real-DOM tests, Plan 06.1-04 +1 traceability)
  </what-built>
  <how-to-verify>
    **Pre-flight (bookended-by-snapshots rule — MANDATORY before any Countermoves action):**

    1. SSH into Countermoves: `ssh -i $HOME/.ssh/countermoves_vps_ed25519 eric@82.29.197.74` (per MemPalace SSH note — never default to root@<ip>).
    2. Re-export the canonical env vars (per MemPalace `feedback_vps-commands-self-contained`):
       ```bash
       export PAPERCLIP_DB_URL=$(sudo grep "^PAPERCLIP_DB_URL=" /etc/paperclip/db.env | cut -d= -f2-)
       export PAPERCLIP_API_URL=http://localhost:3100
       export PAPERCLIP_API_KEY=$(cat ~/.paperclip/auth.json | jq -r .accessToken)
       export PAPERCLIP_COMPANY_ID=$(curl -s -H "Authorization: Bearer $PAPERCLIP_API_KEY" $PAPERCLIP_API_URL/api/companies | jq -r '.[0].id')
       ```
    3. Take a verified snapshot:
       ```bash
       cd ~/clarity-pack
       node scripts/safety/cli.mjs snapshot --db-url=$PAPERCLIP_DB_URL
       ```
       Confirm output reports `verifiedAt: <iso>`; verify the snapshot is ≤ 15 minutes old before proceeding.

    **Install the new tarball:**

    4. SCP the repacked tarball from local to Countermoves: `scp -i $HOME/.ssh/countermoves_vps_ed25519 clarity-pack-1.0.0.tgz eric@82.29.197.74:~/`
    5. Verify sha256 on the VPS matches the local sha256 captured in Task 2: `sha256sum ~/clarity-pack-1.0.0.tgz`.
    6. Uninstall the current clarity-pack: `cd ~/paperclip && pnpm paperclipai plugin uninstall clarity-pack` (per MemPalace `paperclipai-workspace-script-gotcha` — MUST be from `~/paperclip`).
    7. Install the new tarball: `cd ~/paperclip && pnpm paperclipai plugin install ~/clarity-pack-1.0.0.tgz`.

    **Drill — verify the 5 success criteria + COEXIST-11 (Playwright via the user's authenticated browser session against `https://countermoves.gl3group.com`):**

    8. **Critical Path affordance variants (UI-SPEC §Flow 1, 2, 3 + ROOM-11):**
       - Navigate to the Situation Room route (`/COU/situation-room` per MemPalace `clarity-pack-plugin-page-routes`).
       - Find a Critical Path row whose terminal is HUMAN_ACTION_ON with `__unowned__` (these are the rc.8 Phase B "no owner assigned" rows). Verify the row renders BOTH a `Take ownership` button (gold accent) AND a `Convert to task →` button.
       - Find a Critical Path row whose terminal is SELF_RESOLVING or EXTERNAL. Verify the row renders ONLY `Convert to task →` (no Take-Ownership).
       - If a CYCLE terminal exists in the current snapshot, verify the row renders ONLY `+ Create task to break this cycle`.
       - If `useResolvedUserId` returns null in the current host-bridge state, verify the Take-Ownership button is disabled with the tooltip `Sign in to claim ownership`.
       - Take Playwright screenshots: each affordance variant captured to `.playwright-mcp/06.1-drill-<timestamp>-<variant>.png`.

    9. **Take-Ownership dispatch (UI-SPEC §Flow 1 happy path + ROOM-09):**
       - Click `Take ownership` on a __unowned__ row.
       - Verify toast `Ownership claimed` fires.
       - Verify the row's terminal changes to a resolved owner label within ≤ 60s (snapshot refetch).
       - Verify the Take-Ownership button is no longer present on that row.
       - Verify via psql: `SELECT agent_id, owner_user_id, set_at FROM plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners WHERE owner_user_id = '<eric's userId>';` returns the new row.

    10. **Artifact chip row + bottom shelf deletion (UI-SPEC §Flow 4 + ROOM-10 + D-02):**
        - Verify the bottom `<section className="clarity-artifacts-shelf">` is GONE from the DOM (rg the rendered page for `clarity-artifacts-shelf` — should return 0).
        - Verify each agent card with ≥ 1 chat-attachment or deliverable in the 24h window renders an inline `<div className="clarity-artifact-chip-row">` with ≥ 1 chip.
        - Click a chip. Verify the canonical `AttachmentChipWithPreview` popover opens (fixed-inset backdrop + centered body + 3 dismissal affordances).

    11. **Surface-header `+ Create task` button (UI-SPEC §Flow 5 + ROOM-11 D-03):**
        - Verify the standalone `+ Create task` button is visible in the surface-header bar at the top of the Situation Room.
        - Click it; verify TrueTaskDialog opens in cold mode with empty fields.

    12. **Convert-to-task no-prefill (UI-SPEC §Flow 2 + D-12):**
        - Click `Convert to task →` on any non-CYCLE Critical Path row.
        - Verify the TrueTaskDialog opens with EMPTY title + EMPTY description + UNSELECTED assignee.
        - Verify the blocker context (chain narration + terminal agent ID + blocker reason) appears OUTSIDE the dialog body — NOT inside any editable field.

    13. **COEXIST-11 (additive + disable preserves data — ROOM-09 + coexistence #3 + #6):**
        - On the VPS, capture the rowcount: `psql $PAPERCLIP_DB_URL -c "SELECT count(*) FROM plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners;"` → record value N1.
        - `cd ~/paperclip && pnpm paperclipai plugin disable clarity-pack`.
        - Wait ≥ 5 seconds.
        - Capture rowcount again: should still equal N1 (the table is plugin-namespace; disable does NOT drop).
        - `pnpm paperclipai plugin enable clarity-pack`.
        - Capture rowcount again: N3. Assert N1 == N3 byte-identical.
        - Verify on the Situation Room UI that the claimed row from Step 9 still renders with the resolved owner (not back to `__unowned__`).

    **Closure:**

    14. Run the full coexistence suite from the VPS: `cd ~/clarity-pack && node scripts/coexistence-checks/run-all.mjs` — confirm all 11 checks PASS (no regression).
    15. Write `.planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md` with:
        - Drill date + sha256 of the tarball installed
        - Snapshot ID (pre-drill bookend)
        - One row per drill step (8-13) with PASS/FAIL verdict + Playwright screenshot path
        - Rowcount sequence N1 == N3 for COEXIST-11
        - Final verdict: `APPROVED — Phase 6.1 closed YYYY-MM-DD` OR `FAILED at step <N>: <reason>` (in which case route to a gap-closure plan via `/gsd:plan-phase 6.1 --gaps`)
  </how-to-verify>
  <resume-signal>
    Eric resumes the session with one of:
    - `approved — phase 6.1 closed YYYY-MM-DD` (PASS verdict; triggers the planning agent to write the post-drill closure commit flipping ROOM-09/10/11 → Implemented in REQUIREMENTS.md, marking Phase 6.1 COMPLETE in ROADMAP.md, updating STATE.md milestone, and queueing Plan 05-10 rc → 1.0.0 next-action)
    - `gaps at step <N>: <reason>` (FAIL verdict; triggers `/gsd:plan-phase 6.1 --gaps` per project memory `gsd-execute-plan-not-real` — addendum execution is via the gaps flag, NOT `/gsd:execute-plan <id>`)
    - `defer <reason>` (operator chose to pause; STATE.md `next_action` updated accordingly)
  </resume-signal>
  <acceptance_criteria>
    - File `.planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md` exists with the drill record per §how-to-verify Step 15.
    - Pre-flight assertion: snapshot bookend captured with `verifiedAt` timestamp ≤ 15 minutes before any install action (CLAUDE.md bookended-by-snapshots rule).
    - Behavior assertion (psql): `SELECT count(*)` on `clarity_agent_owners` is byte-identical before vs after disable/enable cycle (N1 == N3).
    - Behavior assertion (Playwright DOM): zero elements with class `clarity-artifacts-shelf` rendered on the Situation Room route after install.
    - Behavior assertion (Playwright DOM): at least one `clarity-take-ownership-btn` element exists OR the operator confirms no `__unowned__` rows are present in the current snapshot (degenerate case acceptable).
    - Behavior assertion (Playwright DOM): the `+ Create task` button is visible on the Situation Room surface-header.
    - Behavior assertion (Playwright DOM): Convert-to-task dialog opens with empty editable fields (asserted via DOM inspection: input value === '').
    - CLI output: `node scripts/coexistence-checks/run-all.mjs` exits 0 on the VPS — all 11 COEXIST-* checks green.
    - Operator verdict recorded verbatim in 06.1-VERIFICATION.md.
  </acceptance_criteria>
  <done>Eric's drill produces a PASS verdict (or a clear FAIL routed to gap closure); 06.1-VERIFICATION.md records the result; COEXIST-11 + 10 prior checks all green on the live VPS.</done>
</task>

</tasks>

<verification>
**Phase 6.1 closure tier:**

1. `node --test test/phases/06.1-traceability.test.mjs` → exits 0.
2. `node scripts/coexistence-checks/run-all.mjs` → all 11 COEXIST-* checks green (10 existing + COEXIST-11 new).
3. `pnpm test` → full suite green (Plan 06.1-01 +10 tests + Plan 06.1-02 +11 + Plan 06.1-03 +14 + this plan's traceability test).
4. `pnpm exec tsc --noEmit` → clean.
5. `clarity-pack-1.0.0.tgz` exists; sha256 captured; version literal `1.0.0` unchanged in BOTH `package.json` and `src/manifest.ts`.
6. Operator drill against Countermoves: pre-snapshot, install, 5-affordance variant verification, take-ownership round-trip with psql confirmation, artifact chip rendering, surface-header button visible, dialog no-prefill, disable/enable rowcount byte-identical. Recorded in 06.1-VERIFICATION.md with APPROVED verdict.

**Post-drill follow-up (separate closure commit; NOT in this plan's execution):**
- Flip REQUIREMENTS.md ROOM-09/10/11 status → Implemented.
- Append a row to ROOM-04 note: "UI shell relocated to per-agent inline chips in Phase 6.1 (ROOM-10); bottom `<section className='clarity-artifacts-shelf'>` deleted."
- Mark Phase 6.1 COMPLETE in ROADMAP.md.
- Update STATE.md milestone: rc.8 final → Phase 6.1 closed; next_action: `/gsd:execute-phase 5` for Plan 05-10 (rc → 1.0.0 + npm publish + Hostinger wipe-and-rebuild).
- File MemPalace `clarity_pack/decisions/phase-6.1-closed` drawer.
</verification>

<success_criteria>
- COEXIST-11 coexistence check shipped + registered + all 11 checks green.
- Phase 6.1 traceability test pins ROOM-09/10/11 in plan frontmatter.
- `clarity-pack-1.0.0.tgz` REPACKED with no version bump; sha256 captured.
- Operator drill against live Countermoves produces APPROVED verdict, recorded in 06.1-VERIFICATION.md.
- Coexistence guarantees #3 + #6 verified at the database layer (rowcount byte-identical before/after disable/enable).
- CTT-07 invariant pinned by THREE layers (runtime spy in Plan 06.1-01 Test 10 + source-grep companion in Plan 06.1-01 Task 2 + COEXIST-11 source-grep in this plan).
- No version bump anywhere (Plan 05-10 owns the rc → 1.0.0 transition).
- REQUIREMENTS.md untouched in this plan (the Implemented-flip lives in the post-drill closure commit).
</success_criteria>

<output>
The post-drill closure commit lives OUTSIDE this plan's execution — the planning agent writes it AFTER Eric's `approved — phase 6.1 closed` resume signal. That commit:
- Updates REQUIREMENTS.md (ROOM-09/10/11 → Implemented; ROOM-04 augmentation note)
- Updates ROADMAP.md (Phase 6.1 → [x] CLOSED & VERIFIED)
- Updates STATE.md (milestone v1.0.0-rc.8 → next_action = Plan 05-10 rc → 1.0.0 ship)
- Writes 06.1-SUMMARY.md (synthesizes the four plan summaries + the operator drill verdict)
- Files MemPalace drawer `clarity_pack/decisions/phase-6.1-closed`

This plan's own outputs are: 11-take-ownership.mjs + run-all.mjs registry entry + 06.1-traceability.test.mjs + repacked tarball + 06.1-VERIFICATION.md.
</output>
