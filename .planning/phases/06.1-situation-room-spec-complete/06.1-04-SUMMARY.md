---
phase: 06.1-situation-room-spec-complete
plan: 04
subsystem: closure / coexistence / drill
tags: [coexistence, traceability, repack, closure, drill, ROOM-09, ROOM-10, ROOM-11, CTT-07]
status: PARTIAL — Tasks 1 + 2 complete; Task 3 PENDING-OPERATOR-DRILL
requires:
  - 09-true-task.mjs (canonical coexistence-check template, Plan 04.1-07)
  - 10-uninstall-runbook.mjs (occupies slot 10; confirms next slot is 11)
  - 04.1-traceability.test.mjs (Phase 4.1 closure traceability template; shape forked to plan-frontmatter grep)
  - Plans 06.1-01 + 06.1-02 + 06.1-03 (the Phase 6.1 worker + UI tiers this closure plan ratifies)
provides:
  - scripts/coexistence-checks/11-take-ownership.mjs (COEXIST-11 third-layer CTT-07 defense)
  - scripts/coexistence-checks/run-all.mjs (COEXIST-11 registry entry)
  - test/phases/06.1-traceability.test.mjs (ROOM-09/10/11 traceability gate; plan-frontmatter shape)
  - clarity-pack-1.0.0-rc.8.tgz (REPACKED with Phase 6.1 worker + UI deltas; new sha256 8727bfd0...)
affects:
  - rc.8 Phase B Playwright drill finding — closure pending the operator drill in Task 3
  - REQUIREMENTS.md ROOM-09/10/11 will flip from Pending → Implemented in the
    POST-DRILL closure commit (NOT in this plan; matches Plan 05-10 / 05-11 precedent)
  - REQUIREMENTS.md ROOM-04 note will be appended in the POST-DRILL closure commit (per CONTEXT.md traceability notes)
  - ROADMAP.md Phase 6.1 → [x] CLOSED & VERIFIED in the POST-DRILL closure commit
  - STATE.md milestone v1.0.0-rc.8 → next_action = Plan 05-10 (rc → 1.0.0 ship) in the POST-DRILL closure commit
tech_stack:
  added: []
  patterns:
    - "Third-layer CTT-07 defense (build tier): the COEXIST-11 source-grep complements
      Plan 06.1-01 Test 10 runtime spy + ctt07 source-grep companion. ROOM-09 + ROOM-10
      worker tier CTT-07 invariant now pinned by THREE independent layers."
    - "Plan-frontmatter traceability shape: the Phase 6.1 traceability test forks the
      Plan 04.1-07 closure-test template but rewrites the assertion target from
      REQUIREMENTS.md table rows to plan frontmatter `requirements: [...]` arrays.
      This is intentional -- per CONTEXT.md / Plan 05-10 / 05-11 precedent, the
      REQUIREMENTS.md implementation-status flip lives in the post-drill closure
      commit, so the build-tier traceability link the test pins is `the planner
      allocated each ROOM-NN to at least one plan.`"
    - "Repack-without-bump invariant: tarball regenerates after a feature ship; both
      `package.json` and `src/manifest.ts:337` version literals stay at the existing
      semver string. Plan 05-10 owns the rc → 1.0.0 transition; this plan does not."
key_files:
  created:
    - scripts/coexistence-checks/11-take-ownership.mjs
    - test/phases/06.1-traceability.test.mjs
    - .planning/phases/06.1-situation-room-spec-complete/06.1-04-SUMMARY.md  (this file)
  modified:
    - scripts/coexistence-checks/run-all.mjs (COEXIST-11 registry entry + header comment)
  pending_operator_drill:
    - .planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md (the operator writes this post-drill)
decisions:
  - "COEXIST-11 slot numbering confirmed: 10-uninstall-runbook.mjs already exists,
    so the new check occupies slot 11 (matches CONTEXT.md / RESEARCH.md correction
    captured during planning). The 09-true-task.mjs shape was forked byte-for-byte
    with three substitutions: TRUE_TASK_TABLES → [clarity_agent_owners], ARCHIVE_HANDLER_PATH
    → src/worker/handlers/agent-take-ownership.ts, PHASE_4_1_SOURCES → the three Phase 6.1
    sources (agent-take-ownership.ts, situation-artifacts.ts, clarity-agent-owners-repo.ts).
    The check asserts six layers: migrations are additive-only on Phase 6.1 surfaces;
    agent-take-ownership.ts has zero ctx.issues.update; situation-artifacts.ts has zero
    ctx.issues.update; no ctx.issues.delete/deleteComment anywhere in src/worker/**;
    no destructive uninstall hook in src/manifest.ts; all three Phase 6.1 source files exist."
  - "Phase 6.1 traceability shape locked at plan-frontmatter grep (NOT REQUIREMENTS.md
    table-row grep). Per CONTEXT.md the post-drill closure commit handles the table-row
    flip; the build-tier traceability link is the planner-allocation envelope. The
    minimum coverage envelope asserted: ROOM-09 owns ≥2 plans (worker + UI/closure),
    ROOM-10 owns ≥2 plans (worker + UI/closure), ROOM-11 owns ≥1 plan (UI tier where
    it first appears). Current allocation: ROOM-09 ∈ {01, 03, 04}; ROOM-10 ∈ {02, 03, 04};
    ROOM-11 ∈ {03, 04}."
  - "Tarball name follows the actual version literal on disk. The Plan 06.1-04 spec
    asserts `clarity-pack-1.0.0.tgz` but the working tree carries `1.0.0-rc.8` in BOTH
    package.json AND src/manifest.ts (Plan 05-10 has not yet bumped to 1.0.0). Per
    the orchestrator's load-bearing constraint NO VERSION BUMP, the repack ships at
    the existing version literal -- `clarity-pack-1.0.0-rc.8.tgz`. Documented as a
    Rule 3 deviation in the Task 2 commit message."
metrics:
  duration_minutes: ~25  (Tasks 1 + 2 only -- the operator drill is Task 3)
  completed_date: "2026-05-27"
  commits_so_far: 2  (Task 1 + Task 2 -- partial SUMMARY commit increments this)
  task_count: 3  (Tasks 1 + 2 complete; Task 3 PENDING-OPERATOR-DRILL)
  files_created: 3
  files_modified: 1
  tests_added: 5  (Phase 6.1 traceability test suite: 5 source-grep / structural tests)
  tests_passing_total: "1887 (1885 pass / 0 fail / 2 skip across the entire repo;
    +5 net new from this plan's traceability test); all 11 coexistence checks PASS"
---

# Phase 06.1 Plan 04: Closure tier (Tasks 1 + 2) — PARTIAL summary

**Status:** PARTIAL — Tasks 1 + 2 code-tier complete; Task 3 (operator's live Countermoves drill) PENDING.

This plan ships the build-tier closure ratifying Plans 06.1-01 + 06.1-02 + 06.1-03:

1. **COEXIST-11 coexistence check** — third-layer CTT-07 defense (after Plan 06.1-01 Test 10 runtime spy + ctt07/agent-take-ownership-no-issue-update.test.mjs source-grep companion). Six assertions: migrations additive-only on Phase 6.1 surfaces; `agent-take-ownership.ts` has zero `ctx.issues.update`; `situation-artifacts.ts` has zero `ctx.issues.update`; no `ctx.issues.delete`/`deleteComment` anywhere in `src/worker/**`; no destructive uninstall hook; Phase 6.1 source files exist.

2. **run-all.mjs registry** — COEXIST-11 added at slot 11 (slot 10 occupied by 10-uninstall-runbook.mjs from Plan 05-02). All 11 checks PASS.

3. **Phase 6.1 traceability test** — `test/phases/06.1-traceability.test.mjs` asserts ROOM-09/10/11 each appear in at least one Phase 6.1 plan's `requirements` frontmatter array; pins the minimum coverage envelope (ROOM-09 + ROOM-10 each own ≥2 plans; ROOM-11 owns ≥1).

4. **Tarball repacked** — `clarity-pack-1.0.0-rc.8.tgz` regenerated from a fresh build pipeline (`node scripts/build-worker.mjs && node scripts/build-ui.mjs && tsc --project tsconfig.manifest.json && npm pack`). New sha256 `8727bfd0b0dd24a16d6661be8d351a37af860cca90fee4e94cfd0b5c7892bbd4`; size 639,863 bytes (+344 vs prior Phase B Fix 4 tarball `7ffb3b55...`). Version literal unchanged at `1.0.0-rc.8` in BOTH `package.json` AND `src/manifest.ts:337` AND `dist/manifest.js` (Plan 05-10 owns the rc → 1.0.0 transition; this plan does NOT bump).

Task 3 — Eric's live Countermoves Playwright + safety-CLI drill — is the operator-blocking checkpoint. The drill checklist below MUST be executed by the operator on `https://countermoves.gl3group.com`; the planner agent writes the post-drill closure commit AFTER Eric's `approved — phase 6.1 closed YYYY-MM-DD` resume signal.

## Commits

| # | Hash      | Type  | Message                                                                                          |
|---|-----------|-------|--------------------------------------------------------------------------------------------------|
| 1 | `175e726` | feat  | task 1: COEXIST-11 + run-all.mjs registry + 06.1-traceability test                               |
| 2 | `7eae5a6` | chore | task 2: repack clarity-pack-1.0.0-rc.8.tgz with Phase 6.1 worker + UI deltas                     |

## What Shipped (Tasks 1 + 2)

### Task 1 — Coexistence check #11 + registry + traceability test

- **New file:** `scripts/coexistence-checks/11-take-ownership.mjs` (296 lines). Forked verbatim from `09-true-task.mjs` (Plan 04.1-07 canonical template, 284 lines) with the Phase 6.1 substitutions:
  - `TRUE_TASK_TABLES` → `[ 'clarity_agent_owners' ]`
  - `ARCHIVE_HANDLER_PATH` → `src/worker/handlers/agent-take-ownership.ts`
  - `PHASE_4_1_SOURCES` → the three Phase 6.1 sources (`agent-take-ownership.ts`, `situation-artifacts.ts`, `clarity-agent-owners-repo.ts`)
  - Added Assertion 3 (ROOM-10 worker tier CTT-07 source-grep on `situation-artifacts.ts`) — the Phase 4.1 template only had one handler to scan
  - Headline literal `COEXIST-11`; in-doc invariants reference Phase 6.1 / ROOM-09 / ROOM-10 / CTT-07

- **Modified:** `scripts/coexistence-checks/run-all.mjs` — added `{ id: 'COEXIST-11', script: '11-take-ownership.mjs' }` to the registry at slot 11; header comment now traces through Plans 02-04 / 03-04 / 04-06 / 04.1-07 / 05-02 / 06.1-04.

- **New file:** `test/phases/06.1-traceability.test.mjs` (5 tests, 130 lines). Plan-frontmatter shape; asserts:
  1. Phase 6.1 directory exists
  2. At least one `06.1-NN-PLAN.md` file exists
  3. Every plan has a parseable `requirements: [...]` frontmatter field
  4. Every ROOM-09 / ROOM-10 / ROOM-11 id appears in at least one plan's frontmatter
  5. Minimum coverage envelope (ROOM-09 + ROOM-10 each ≥2 plans; ROOM-11 ≥1 plan)

**Validation:**
- `node scripts/coexistence-checks/run-all.mjs` → all 11 checks PASS (10 existing + COEXIST-11 new)
- `node --test test/phases/06.1-traceability.test.mjs` → 5/5 pass
- `git diff .planning/REQUIREMENTS.md` → empty (the Implemented-flip is the post-drill commit, NOT this plan)

### Task 2 — Build + repack clarity-pack-1.0.0-rc.8.tgz

- Build pipeline (no version bump):
  - `node scripts/build-worker.mjs` → `dist/worker.js` (2.1 MB; `node --check dist/worker.js` exit 0; ESM eval OK)
  - `node scripts/build-ui.mjs` → `dist/ui/index.js` (669.9 KB)
  - `node_modules/.bin/tsc --project tsconfig.manifest.json` → `dist/manifest.js` (40.8 KB; `version: '1.0.0-rc.8'`)
  - `npm pack` → `clarity-pack-1.0.0-rc.8.tgz` (639.9 KB, 17 files)

- **Tarball record:**
  - sha256 before (Phase B Fix 4 — Countermoves rc.8 LIVE): `7ffb3b55291a4209339b15deda3567222fc8cc5b91ac51d17769c9279540e001`
  - sha256 after:  `8727bfd0b0dd24a16d6661be8d351a37af860cca90fee4e94cfd0b5c7892bbd4`
  - size before: 639,519 bytes
  - size after:  639,863 bytes (+344 bytes; absorbs rc.8 final commit `1b11649` + Phase 6.1 worker + UI tier deltas)

- **Version literal unchanged:** `git diff package.json src/manifest.ts` empty; both still read `1.0.0-rc.8`.

**Validation:**
- `node_modules/.bin/tsc --noEmit` → clean
- `node --test test/**/*.test.mjs` → **1887 total / 1885 pass / 0 fail / 2 skip** (+5 net new from Plan 06.1-04 traceability test)
- `node scripts/coexistence-checks/run-all.mjs` → all 11 PASS
- `node --check dist/worker.js` → exit 0
- ESM eval (`import('./dist/worker.js')`) → exit 0

## Deviations from Plan

### Rule 3 — Tarball name follows actual version literal (1.0.0-rc.8, not 1.0.0)

**Found during:** Task 2 pre-flight version check.

**Issue:** Plan 06.1-04 acceptance criteria + the orchestrator's critical constraint #4 both refer to `clarity-pack-1.0.0.tgz` and the version literal `1.0.0`. In reality, the working tree's `package.json` and `src/manifest.ts:337` both carry the literal `1.0.0-rc.8` (the milestone STATE.md records; Plan 05-10 owns the rc → 1.0.0 transition and has not run yet).

**Fix:** Treat the load-bearing invariant as "NO VERSION BUMP" (preserved verbatim). Repack ships at whatever version is on disk → `clarity-pack-1.0.0-rc.8.tgz`. The plan-text tarball name was misaligned with reality; the constraint is preserved. STATE.md milestone (`v1.0.0-rc.8`) corroborates the correct version.

**Files modified:** None — the deviation is documentary. The tarball naturally takes its name from `package.json` `version`.

**Commit:** `7eae5a6` Task 2 commit message documents this deviation in its body.

### Rule 3 — Pnpm not on the bash PATH (matches Plan 05-11 deviation)

**Found during:** Task 2 first attempt at `pnpm build`.

**Issue:** `pnpm` is not on the Bash tool's PATH on this Windows machine. The `pnpm build` shorthand fails with `pnpm: command not found`.

**Fix:** Invoked each build step directly:
- `node scripts/build-worker.mjs` (instead of `pnpm build:worker`)
- `node scripts/build-ui.mjs` (instead of `pnpm build:ui`)
- `node_modules/.bin/tsc --project tsconfig.manifest.json` (instead of `pnpm build:manifest`)
- `npm pack` (instead of `pnpm pack`)

Same artifact produced; matches the Plan 05-11 SUMMARY deviation #2 precedent.

**Files modified:** None.

**Commit:** Captured as a noted operational pattern; no code change.

## Self-Check: PASSED

**Files exist:**
```
$ ls scripts/coexistence-checks/11-take-ownership.mjs       # FOUND
$ ls scripts/coexistence-checks/run-all.mjs                 # FOUND (modified)
$ ls test/phases/06.1-traceability.test.mjs                 # FOUND
$ ls clarity-pack-1.0.0-rc.8.tgz                            # FOUND (639,863 bytes)
$ ls dist/worker.js dist/ui/index.js dist/manifest.js       # FOUND (gitignored build artifacts)
```

**Commits exist:**
```
$ git log --oneline | grep "06\.1-04"
7eae5a6 chore(06.1-04) task 2: repack clarity-pack-1.0.0-rc.8.tgz with Phase 6.1 worker + UI deltas
175e726 feat(06.1-04) task 1: COEXIST-11 + run-all.mjs registry + 06.1-traceability test
```

**Source assertions verified:**
- `grep -c "clarity_agent_owners" scripts/coexistence-checks/11-take-ownership.mjs` = **13** (≥ 2 ✔)
- `grep -c "COEXIST-11" scripts/coexistence-checks/11-take-ownership.mjs` = **3** (≥ 1 ✔)
- `grep -c "COEXIST-11" scripts/coexistence-checks/run-all.mjs` = **2** (≥ 1 ✔)
- `grep -c "11-take-ownership" scripts/coexistence-checks/run-all.mjs` = **1** (≥ 1 ✔)
- `grep -c "ROOM-09\|ROOM-10\|ROOM-11" test/phases/06.1-traceability.test.mjs` = **20** (≥ 3 ✔)
- `git diff .planning/REQUIREMENTS.md` → empty ✔
- `git diff package.json src/manifest.ts` → empty (no version bump ✔)

**Test results:**
- `node scripts/coexistence-checks/run-all.mjs` → 11/11 PASS
- `node --test test/phases/06.1-traceability.test.mjs` → 5/5 pass
- `node --test test/phases/04.1-traceability.test.mjs test/phases/04.2-traceability.test.mjs test/phases/06.1-traceability.test.mjs` → 10/10 pass (cross-phase regression)
- `node --test test/**/*.test.mjs` → **1887 total / 1885 pass / 0 fail / 2 skip**
- `node_modules/.bin/tsc --noEmit` → clean

**Build artifacts:**
- `dist/worker.js` 2.1 MB; `node --check` exit 0; ESM eval OK
- `dist/ui/index.js` 669.9 KB
- `dist/manifest.js` 40.8 KB; `version: '1.0.0-rc.8'`
- `clarity-pack-1.0.0-rc.8.tgz` 639,863 bytes; sha256 `8727bfd0b0dd24a16d6661be8d351a37af860cca90fee4e94cfd0b5c7892bbd4`

## Threat Surface Scan

No new security-relevant surface introduced. COEXIST-11 is a read-only build-tier source-grep check — no network, no database, no host-API mutation. The traceability test is read-only over `.planning/phases/06.1-*-PLAN.md` files. The tarball repack consumes only on-disk files. No new runtime dependencies.

**No threat flags raised.** The Phase 6.1 threat register (T-06.1-21 through T-06.1-25 + T-06.1-SC per the plan frontmatter) is addressed:
- T-06.1-21 (drill without snapshot) — operator's responsibility per the drill checklist below (bookended-by-snapshots gate)
- T-06.1-22 (prod creds in transcript) — operator authenticates personally; no Claude credential surface in this plan
- T-06.1-23 (drill PASS claim without evidence) — drill checklist mandates specific psql + Playwright assertions recorded in 06.1-VERIFICATION.md
- T-06.1-24 (coexistence-check regression silently passes) — COEXIST-11 fails noisily on any destructive uninstall hook addition (Assertion 5) or any future `ctx.issues.update` introduction into either handler (Assertion 2 + 3)
- T-06.1-25 (repacked tarball pulls in unexpected code) — tarball produced from clean master HEAD; sha256 recorded; `npm pack` lists every file (17 files matching the existing tarball-trail discipline)
- T-06.1-SC (npm/pip/cargo installs) — ZERO new runtime dependencies; repack consumes only files already on disk

## Pending — Task 3: Operator's Countermoves drill

Eric runs the following drill on `https://countermoves.gl3group.com` (per Plan 06.1-04 Task 3 spec). The planner agent writes the post-drill closure commit AFTER Eric's resume signal.

### Operator drill checklist

**Pre-flight (bookended-by-snapshots — MANDATORY before any Countermoves action):**

1. SSH into Countermoves: `ssh -i $HOME/.ssh/countermoves_vps_ed25519 eric@82.29.197.74` (per MemPalace SSH note — never default to root@<ip>).
2. Re-export canonical env vars (per MemPalace `feedback_vps-commands-self-contained`):
   ```bash
   export PAPERCLIP_DB_URL=$(sudo grep "^PAPERCLIP_DB_URL=" /etc/paperclip/db.env | cut -d= -f2-)
   export PAPERCLIP_API_URL=http://localhost:3100
   export PAPERCLIP_API_KEY=$(cat ~/.paperclip/auth.json | jq -r .accessToken)
   export PAPERCLIP_COMPANY_ID=$(curl -s -H "Authorization: Bearer $PAPERCLIP_API_KEY" $PAPERCLIP_API_URL/api/companies | jq -r '.[0].id')
   ```
3. Take verified snapshot ≤ 15 minutes old:
   ```bash
   cd ~/clarity-pack
   node scripts/safety/cli.mjs snapshot --db-url=$PAPERCLIP_DB_URL
   ```
   Confirm output reports `verifiedAt: <iso>`.

**Install the new tarball:**

4. SCP from local to VPS:
   ```powershell
   scp -i $HOME\.ssh\countermoves_vps_ed25519 clarity-pack-1.0.0-rc.8.tgz eric@82.29.197.74:~/
   ```
5. Verify sha256 on the VPS matches local:
   ```bash
   sha256sum ~/clarity-pack-1.0.0-rc.8.tgz
   # Expected: 8727bfd0b0dd24a16d6661be8d351a37af860cca90fee4e94cfd0b5c7892bbd4
   ```
6. Uninstall current clarity-pack (per MemPalace `paperclipai-workspace-script-gotcha` — MUST be from `~/paperclip`):
   ```bash
   cd ~/paperclip
   pnpm paperclipai plugin uninstall clarity-pack
   ```
7. Install the new tarball:
   ```bash
   cd ~/paperclip
   pnpm paperclipai plugin install ~/clarity-pack-1.0.0-rc.8.tgz
   ```

**Drill — verify the 5 success criteria + COEXIST-11 (Playwright via authenticated browser session against `https://countermoves.gl3group.com`):**

8. **Critical Path affordance variants (UI-SPEC §Flow 1/2/3 + ROOM-11):**
   - Navigate to Situation Room: `/COU/situation-room` (per MemPalace `clarity-pack-plugin-page-routes`)
   - Find a Critical Path row with terminal HUMAN_ACTION_ON `__unowned__` → verify BOTH `Take ownership` (gold accent) AND `Convert to task →` buttons render
   - Find a row with terminal SELF_RESOLVING or EXTERNAL → verify ONLY `Convert to task →` renders
   - If CYCLE terminal exists → verify ONLY `+ Create task to break this cycle` renders
   - If `useResolvedUserId` returns null → verify Take-Ownership button disabled with tooltip `Sign in to claim ownership`
   - Capture Playwright screenshots for each variant to `.playwright-mcp/06.1-drill-<timestamp>-<variant>.png`

9. **Take-Ownership dispatch (UI-SPEC §Flow 1 + ROOM-09):**
   - Click `Take ownership` on a `__unowned__` row → verify toast `Ownership claimed`
   - Verify the row's terminal changes to a resolved owner within ≤ 60s (snapshot refetch)
   - Verify Take-Ownership button no longer present on that row
   - Verify via psql:
     ```sql
     SELECT agent_id, owner_user_id, set_at
       FROM plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners
       WHERE owner_user_id = '<eric_user_id>';
     ```
     returns the new row

10. **Artifact chip row + bottom shelf deletion (UI-SPEC §Flow 4 + ROOM-10 + D-02):**
    - Verify bottom `<section className="clarity-artifacts-shelf">` is GONE from DOM (rendered-page rg returns 0)
    - Verify each agent card with ≥1 chat-attachment or deliverable in 24h window renders inline `<div className="clarity-artifact-chip-row">` with ≥1 chip
    - Click a chip → verify canonical `AttachmentChipWithPreview` popover opens (fixed-inset backdrop + centered body + 3 dismissal affordances)

11. **Surface-header `+ Create task` button (UI-SPEC §Flow 5 + ROOM-11 D-03):**
    - Verify standalone `+ Create task` button visible in Situation Room surface-header bar
    - Click → verify TrueTaskDialog opens in cold mode with empty fields

12. **Convert-to-task no-prefill (UI-SPEC §Flow 2 + D-12):**
    - Click `Convert to task →` on any non-CYCLE Critical Path row
    - Verify TrueTaskDialog opens with EMPTY title + EMPTY description + UNSELECTED assignee
    - Verify blocker context (chain narration + terminal agent ID + blocker reason) renders OUTSIDE the dialog body — NOT inside any editable field

13. **COEXIST-11 (additive + disable preserves data — ROOM-09 + coexistence #3 + #6):**
    - psql:
      ```sql
      SELECT count(*) FROM plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners;
      ```
      Record value `N1`.
    - `cd ~/paperclip && pnpm paperclipai plugin disable clarity-pack`
    - Wait ≥ 5 seconds
    - psql count again — should still equal `N1` (the table is plugin-namespace; disable does NOT drop)
    - `pnpm paperclipai plugin enable clarity-pack`
    - psql count again → `N3`. Assert `N1 == N3` byte-identical.
    - Verify on Situation Room UI: the claimed row from Step 9 still renders with the resolved owner (not back to `__unowned__`)

**Closure:**

14. From the VPS: `cd ~/clarity-pack && node scripts/coexistence-checks/run-all.mjs` → all 11 checks PASS (no regression).
15. Write `.planning/phases/06.1-situation-room-spec-complete/06.1-VERIFICATION.md` with:
    - Drill date + sha256 of installed tarball
    - Snapshot ID (pre-drill bookend)
    - One row per drill step (8–13) with PASS/FAIL verdict + Playwright screenshot path
    - Rowcount sequence `N1 == N3` for COEXIST-11
    - Final verdict: `APPROVED — Phase 6.1 closed YYYY-MM-DD` OR `FAILED at step <N>: <reason>`

### Resume signals (Eric runs one of):

- `approved — phase 6.1 closed YYYY-MM-DD` — triggers post-drill closure commit (REQUIREMENTS.md ROOM-09/10/11 → Implemented + ROOM-04 note appended; ROADMAP.md Phase 6.1 → [x] CLOSED & VERIFIED; STATE.md milestone v1.0.0-rc.8 → next_action = Plan 05-10; 06.1-SUMMARY.md synthesizes the four plan summaries; MemPalace `clarity_pack/decisions/phase-6.1-closed` drawer filed)
- `gaps at step <N>: <reason>` — triggers `/gsd:plan-phase 6.1 --gaps` per project memory `gsd-execute-plan-not-real` (addendum execution is via `--gaps`, NOT `/gsd:execute-plan <id>`)
- `defer <reason>` — operator pauses; STATE.md `next_action` updated accordingly

## What This Plan Closes / Leaves Open

**Closes (Tasks 1 + 2):**
- Build-tier closure for Phase 6.1: COEXIST-11 third-layer defense; Phase 6.1 plan-frontmatter traceability gate; tarball repacked with all Phase 6.1 deltas; sha256 captured.

**Leaves open (Task 3):**
- Operator's live Countermoves drill — the operational evidence that the rc.8 Phase B "every row reads no owner assigned" finding is closed on a real-data instance, that the per-agent inline chip row renders as expected, that the bottom shelf is gone, that Take-Ownership round-trips, and that COEXIST-11's rowcount-byte-identical assertion holds on the live VPS through a disable/enable cycle.

**Out of scope for THIS plan (lives in the post-drill closure commit; matches Plan 05-10 / 05-11 precedent):**
- REQUIREMENTS.md ROOM-09 / ROOM-10 / ROOM-11 status flip → Implemented
- REQUIREMENTS.md ROOM-04 note appended ("UI shell relocated to per-agent inline chips in Phase 6.1 (ROOM-10); bottom <section className='clarity-artifacts-shelf'> deleted.")
- ROADMAP.md Phase 6.1 → [x] CLOSED & VERIFIED
- STATE.md milestone advance + next_action update
- 06.1-SUMMARY.md (phase-level synthesis of all four plan summaries)
- MemPalace `clarity_pack/decisions/phase-6.1-closed` drawer
