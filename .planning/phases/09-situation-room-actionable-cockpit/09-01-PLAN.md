---
phase: 09-situation-room-actionable-cockpit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/handlers/situation-assign-owner.ts
  - src/worker/situation/group-employee-state.ts
  - src/worker/situation/build-employees-rollup.ts
  - src/worker/handlers/situation-room.ts
  - src/worker.ts
  - src/manifest.ts
  - src/worker/jobs/situation-snapshot.ts
  - test/worker/situation/group-employee-state.test.mjs
  - test/worker/handlers/situation-assign-owner.test.mjs
  - test/worker/situation/build-employees-rollup-needsyou.test.mjs
# NOTE (revision, BLOCKER 1): the situation.artifacts WORKER handler removal moved
# OUT of this plan and INTO 09-02 so the worker handler and its UI caller are
# deleted atomically in the SAME commit. This plan removes ONLY the recompute-
# situation cron job (a job with NO synchronous UI caller — safe to remove mid-wave).
autonomous: true
requirements: [R1, R2, R3, R5, R8]
must_haves:
  truths:
    - "situation.assignOwner reassigns the real core issue via ctx.issues.update with operator actor attribution"
    - "Every employee row carries a worker-computed group bucket (needs_you | working | idle)"
    - "needsYou.count includes unowned blocked rows (no longer frozen at viewer-owned only)"
    - "needsYou.topAction for the unowned case carries the oldest-unowned row's agentId + non-null leafIssueId (enough to drive the picker, NOT a chat deep-link) so 09-02's [Assign first] is never a dead button (R4)"
    - "The recompute-situation cron job is removed at the worker tier (situation.artifacts handler removal is atomic-with-its-UI-caller in 09-02)"
    - "situation-room.ts no longer DEPENDS on the situation_snapshots materialized read-path (the dead-job row-exists branch is removed or annotated dead-safe; the TABLE itself is NOT dropped — R9 additive-only)"
    - "manifest capabilities[] includes issues.update"
  artifacts:
    - path: "src/worker/handlers/situation-assign-owner.ts"
      provides: "situation.assignOwner action: viewer-authority gate + ctx.issues.update(leafIssueId,{assigneeAgentId|assigneeUserId},companyId,actor)"
      contains: "ctx.issues.update("
    - path: "src/worker/situation/group-employee-state.ts"
      provides: "pure groupForState(state) -> 'needs_you'|'working'|'idle' classifier"
      exports: ["groupForState", "EmployeeGroup"]
    - path: "src/worker/situation/build-employees-rollup.ts"
      provides: "group field on each SituationEmployeeRow + un-frozen needsYou compute (topAction carries unowned row agentId + leafIssueId)"
      contains: "group:"
  key_links:
    - from: "src/worker/handlers/situation-assign-owner.ts"
      to: "ctx.issues.update"
      via: "typed core-issue mutation with actor"
      pattern: "ctx\\.issues\\.update\\("
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "src/worker/situation/group-employee-state.ts"
      via: "groupForState(row.state) called at row build"
      pattern: "groupForState\\("
    - from: "src/manifest.ts"
      to: "capabilities"
      via: "issues.update string present, recompute-situation jobs[] entry absent"
      pattern: "'issues\\.update'"
---

<objective>
Worker tier for the actionable cockpit. Adds the hero `situation.assignOwner` action (the first plugin core-issue mutation), worker-tier group bucketing, an un-frozen needs-you count, and removes the dead `recompute-situation` cron job (a job with no synchronous UI caller — safe to remove this wave). Also adds the `issues.update` capability to the manifest.

NOTE (revision, BLOCKER 1): the `situation.artifacts` worker-handler removal was MOVED OUT of this plan and INTO 09-02 so the worker handler and its `usePluginData('situation.artifacts')` UI caller are deleted atomically in the same commit (otherwise the UI calls a handler that no longer exists between waves → runtime error). This plan still removes the `recompute-situation` cron job, which has no synchronous UI consumer.

Purpose: Give the UI tier (Plan 09-02) a verbatim-renderable rollup where each row already knows its group and the banner count already reflects unowned blockers, and give it a real assign-owner action to wire buttons to.
Output: `situation-assign-owner.ts` (new), `group-employee-state.ts` (new), extended `build-employees-rollup.ts`, extended `situation.snapshot` ctx (with the dead materialized read-path removed/annotated), worker.ts registration delta, manifest capability + dead-cron-job removal, and tests for the three pure/business-logic pieces.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-situation-room-actionable-cockpit/09-SPEC.md
@.planning/phases/09-situation-room-actionable-cockpit/09-CONTEXT.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Extracted from files already read this session. Executor uses these directly. -->

From src/worker/situation/classify-employee-state.ts (the 5-state classifier — DO NOT MODIFY):
```
export type EmployeeState = 'running' | 'reviewing' | 'blocked' | 'idle' | 'stale' | 'unknown';
export function classifyEmployeeState(input: ClassifyInput): EmployeeState
```

From src/worker/situation/build-employees-rollup.ts (the builder to extend):
```
export type SituationEmployeeRow = {
  agentId, name, role, state, focusIssueId, focusLine, lastActivityAt, ageBucket,
  blockerChain: { rootIssueId, leafIssueId, humanAction, ownerName, ownerAgentId } | null,
  doneTodayCount
}
export type NeedsYou = { count: number; topAction: {agentId, humanAction, leafIssueId}|null }
export async function buildEmployeesRollup(ctx, companyId, viewerUserId): Promise<{employees, needsYou}>
// needsYou today: targeting = rows.filter(r => r.__targetsViewer && r.blockerChain) — VIEWER-OWNED ONLY (frozen).
// InternalRow already carries __targetsViewer + __activityMs (stripped before return).
```

From src/worker/handlers/situation-room.ts (the snapshot handler — VALID HTTP scope; KEEP):
```
export type SituationRoomCtx = OptInGuardDataCtx & {
  issues: Pick<PluginIssuesClient,'list'|'get'|'relations'>;
  agents?: Pick<PluginAgentsClient,'list'|'get'>;
  logger?: PluginLogger;
};
// returns { ...payload?, org_blocked_backlog, situation_employees, needsYou, taken_at }
// KEEP situation_employees + needsYou keys. The dead `...payload` spread + `employees`(AgentEmployee[]) grid feed go in Plan 09-02's UI delete; the handler keeps serving situation_employees.
// The `ctx.db.query<SnapshotRow>(... situation_snapshots ...)` SELECT at ~128 + the `if (row)` branch + the `...payload` spread are the DEAD-JOB READ-PATH (WARNING 5) — addressed in Task 3.
```

From src/worker/handlers/agent-take-ownership.ts (the authority gate to MIRROR — it does NOT call ctx.issues.update; situation.assignOwner is the first that does):
```
wrapActionHandler(ctx, 'agent.takeOwnership', async (params) => {
  const companyId = reqStr(params,'companyId'); const agentId = reqStr(params,'agentId');
  const ownerUserId = reqStr(params,'ownerUserId'); const userId = reqStr(params,'userId');
  if (ownerUserId !== userId) return { error: 'OWNER_MISMATCH' };   // viewer-authority
  const agent = await ctx.agents.get(agentId, companyId);            // company-scope gate
  if (!agent) return { error: 'NOT_FOUND' };
  ...
});
// reqStr throws "<handler>: <key> required" on missing string param.
```

ctx.issues.update signature (CLAUDE.md + editor.ts:663 + bulletin.action.approve call sites):
`ctx.issues.update(issueId, patch, companyId, actor)` — patch includes assigneeAgentId | assigneeUserId | status. `actor` = the operator (carries audit attribution). Capability string = `issues.update`.
</interfaces>

<read_first>
Executor MUST read these before writing any code:
- src/worker/handlers/agent-take-ownership.ts (the gate to mirror, byte-for-byte structure)
- src/worker/situation/build-employees-rollup.ts (the builder to extend — note __targetsViewer/__activityMs internals)
- src/worker/situation/classify-employee-state.ts (the state source; group derives from state)
- src/worker/handlers/situation-room.ts (the snapshot ctx you widen; the dead-job SnapshotRow read-path at ~128-156)
- src/worker.ts (registration site; find registerAgentTakeOwnership + registerSituationRoomHandlers + the recompute-situation job registration. DO NOT touch the situation.artifacts registration — that is 09-02's job per BLOCKER 1)
- src/worker/agents/editor.ts around line 663 (a proven ctx.issues.update call site — copy its exact arg order + actor shape)
- src/manifest.ts (capabilities[] + jobs[] — find the recompute-situation jobs[] entry)
- Find the existing situation.assignOwner-adjacent test conventions: test/worker/handlers/agent-take-ownership.test.mjs (mirror its fake-ctx + spy pattern, esp. ctx._issueUpdateCalls if present)
</read_first>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure group classifier + group field on the rollup</name>
  <files>src/worker/situation/group-employee-state.ts, src/worker/situation/build-employees-rollup.ts, test/worker/situation/group-employee-state.test.mjs</files>
  <read_first>
    - src/worker/situation/classify-employee-state.ts (the EmployeeState union — all six values incl. 'unknown')
    - src/worker/situation/build-employees-rollup.ts (the SituationEmployeeRow type + buildOneEmployeeRow return + the public-shape map at the end)
    - 09-SPEC.md R2 (the exact mapping: needs_you = blocked; working = running|reviewing; idle = idle|stale)
  </read_first>
  <behavior>
    - groupForState('blocked') === 'needs_you'
    - groupForState('running') === 'working'
    - groupForState('reviewing') === 'working'
    - groupForState('idle') === 'idle'
    - groupForState('stale') === 'idle'
    - groupForState('unknown') === 'idle'  (degrade-safe fallback — unknown never lands in needs_you/working)
    - Every member of EmployeeState maps to exactly one EmployeeGroup (exhaustiveness test iterates all six)
  </behavior>
  <action>
    Create `src/worker/situation/group-employee-state.ts` exporting `type EmployeeGroup = 'needs_you' | 'working' | 'idle'` and a pure function `groupForState(state: EmployeeState): EmployeeGroup` implementing the R2 mapping above. Import `EmployeeState` from `./classify-employee-state.ts`. No SDK import, no I/O — pure and synchronous (same discipline as classify-employee-state.ts). In `build-employees-rollup.ts`: add `group: EmployeeGroup` to the `SituationEmployeeRow` type; import `groupForState`; set `group: groupForState(state)` in the `buildOneEmployeeRow` return object AND in `degradeSafeRow` (which currently returns state:'unknown' → group:'idle'). Do NOT change the existing `ORDER` sort, the deterministic blocked→stale→idle→reviewing→running sort, or the focusLine/blockerChain logic — group is additive metadata the UI groups BY, the worker sort stays the verbatim render order WITHIN each group.
    Write `test/worker/situation/group-employee-state.test.mjs` as a node:test file asserting all six behaviors above plus an exhaustiveness loop over `['running','reviewing','blocked','idle','stale','unknown']`.
  </action>
  <verify>
    <automated>node --test test/worker/situation/group-employee-state.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/worker/situation/group-employee-state.test.mjs` passes with ≥7 assertions
    - grep shows `export function groupForState` and `export type EmployeeGroup` in src/worker/situation/group-employee-state.ts
    - grep shows `group: groupForState(` in src/worker/situation/build-employees-rollup.ts (in buildOneEmployeeRow)
    - grep shows `group:` appears in degradeSafeRow's returned object
    - `node --test test/worker/situation/build-employees-rollup*.test.mjs` still passes (no regression to existing rollup tests)
  </acceptance_criteria>
  <done>EmployeeGroup classifier is pure + fully tested; every rollup row carries a worker-assigned group; existing rollup tests stay green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: situation.assignOwner action (first core-issue mutation) + un-freeze needsYou</name>
  <files>src/worker/handlers/situation-assign-owner.ts, src/worker/situation/build-employees-rollup.ts, src/worker.ts, test/worker/handlers/situation-assign-owner.test.mjs, test/worker/situation/build-employees-rollup-needsyou.test.mjs</files>
  <read_first>
    - src/worker/handlers/agent-take-ownership.ts (mirror the OWNER_MISMATCH / NOT_FOUND / company-scope structure + reqStr + wrapActionHandler convention)
    - src/worker/agents/editor.ts near line 663 (the exact ctx.issues.update(issueId, patch, companyId, actor) call shape + how `actor` is constructed)
    - src/worker/handlers/bulletin-action-approve.ts (the second proven ctx.issues.update call site)
    - src/worker/opt-in-guard.ts (wrapActionHandler + OptInGuardActionCtx — assignOwner is opt-in-guarded like every action)
    - src/worker/situation/build-employees-rollup.ts (the needsYou compute block + __targetsViewer/__activityMs internals — you un-freeze the count here)
    - 09-SPEC.md R3 + R5; 09-CONTEXT.md D-01 (roster picker), D-02 ("Take it myself" = assigneeUserId, the ONE assigneeUserId use)
  </read_first>
  <behavior>
    situation.assignOwner:
    - Missing companyId / leafIssueId / userId param → throws "situation.assignOwner: <key> required" (reqStr convention)
    - Opted-out caller → returns { error: 'OPT_IN_REQUIRED' } (via wrapActionHandler)
    - Agent-assign branch (params.assigneeAgentId present): calls ctx.agents.get(assigneeAgentId, companyId); null → { error: 'NOT_FOUND' }; then ctx.issues.update(leafIssueId, { assigneeAgentId }, companyId, actor) where actor encodes the operator userId
    - Self-assign branch (params.takeItMyself === true, per D-02): calls ctx.issues.update(leafIssueId, { assigneeUserId: userId }, companyId, actor) — assigneeUserId, NOT assigneeAgentId; this is the only assigneeUserId path
    - Exactly one of {assigneeAgentId, takeItMyself} required; neither / both → { error: 'BAD_REQUEST' }
    - Success → { ok: true, leafIssueId, assignedTo } so the UI can toast without a second round-trip
    - ctx.issues.update throws → { error: 'ASSIGN_FAILED' } (logged via ctx.logger?.warn)
    - actor attribution: the update's 4th arg carries the operator userId so the Paperclip audit trail attributes the change to the operator, NOT the plugin worker
    needsYou (un-freeze, R5):
    - count = (rows where group==='needs_you' AND blockerChain.ownerName === 'Unassigned')  +  (rows where __targetsViewer && blockerChain)
    - the two sets are de-duplicated (a viewer-owned row that is also unowned cannot exist — ownerName 'Unassigned' ⇔ unowned ⇔ not viewer-targeted — but compute as a Set of agentId to be safe)
    - topAction = the OLDEST unowned blocked row (smallest __activityMs) when any unowned exists, else the oldest viewer-targeted row (preserve current behavior when zero unowned)
    - topAction shape carries enough to DRIVE THE UI for BOTH cases (WARNING 1 / R4 — the [Assign first ▾] button must never render disabled when count > 0):
      - UNOWNED case → topAction = { agentId: <oldest-unowned row's agentId>, humanAction, leafIssueId: <that row's blockerChain.leafIssueId> }. This is enough for the UI to scroll to that row and OPEN ITS OWNER PICKER (NOT a chat deep-link — there is no ownerAgentId to chat with).
      - OWNED-needs-you case (zero unowned, ≥1 viewer-targeted) → topAction = { agentId: <oldest viewer-targeted row's agentId>, humanAction, leafIssueId } exactly as today (the UI resolves ownerAgentId from that row for the chat deep-link — the existing Phase 8 path).
      - leafIssueId MUST be non-null for the unowned case (the picker assigns the leaf); it may be null only in the legacy owned path where the banner deep-links to chat instead.
  </behavior>
  <action>
    Create `src/worker/handlers/situation-assign-owner.ts` exporting `registerSituationAssignOwner(ctx)`. Mirror agent-take-ownership.ts's structure: `wrapActionHandler(ctx, 'situation.assignOwner', async (params) => {...})`, `reqStr` for required params, the OWNER/company-scope gate. Construct `actor` exactly as editor.ts:663 does (read that line — copy the actor object shape; it carries the operator userId). Implement the agent-assign and self-assign branches per the behavior block. NEVER use ctx.db for this — it is public.issues and MUST go through ctx.issues.update (CLAUDE.md hard rule). The ctx type extends OptInGuardActionCtx with `issues: Pick<PluginIssuesClient,'update'>` and `agents: Pick<PluginAgentsClient,'get'>` and `logger?`.
    In `build-employees-rollup.ts`: rewrite the `needsYou` compute block per the behavior above so unowned blocked rows count. Keep `__targetsViewer`/`__activityMs` internals. The unowned predicate is `r.group === 'needs_you' && r.blockerChain && r.blockerChain.ownerName === 'Unassigned'` (NO_UUID_LEAK: ownerName is already scrubbed to 'Unassigned' for __unowned__). Build a Set of agentIds across both sets for the count; pick topAction = oldest-by-__activityMs unowned row, falling back to oldest viewer-targeted row. CRITICAL (WARNING 1): for the unowned case, populate `topAction.leafIssueId` from that row's `blockerChain.leafIssueId` (non-null) so the UI can drive the owner picker from the banner's [Assign first ▾]; do NOT leave it null for the unowned case (a null leafIssueId there would force the UI to render a disabled button — an R4 violation).
    In `src/worker.ts`: import + call `registerSituationAssignOwner(...)` alongside the existing `registerAgentTakeOwnership` registration, passing the same ctx clients (issues, agents, logger, db for opt-in-guard).
    Write `test/worker/handlers/situation-assign-owner.test.mjs` (node:test, fake ctx with spy `_issueUpdateCalls` mirroring agent-take-ownership's test) covering: param-missing throw; opt-out OPT_IN_REQUIRED; agent-assign NOT_FOUND when agents.get null; agent-assign success calls ctx.issues.update with ({assigneeAgentId}) + 4th-arg actor carrying operator userId; self-assign success calls ctx.issues.update with ({assigneeUserId: userId}); neither/both → BAD_REQUEST; update-throws → ASSIGN_FAILED.
    Write `test/worker/situation/build-employees-rollup-needsyou.test.mjs` asserting: a fixture with ≥1 unowned blocked row + 0 viewer-owned yields needsYou.count ≥ 1 (the un-freeze proof, the exact bug R5 names); topAction points to the oldest unowned row AND carries that row's agentId + a non-null leafIssueId (WARNING 1 — enough to drive the picker, not a chat deep-link); an all-owned-by-others fixture with 0 unowned + 0 viewer-targeted yields count 0.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/situation-assign-owner.test.mjs test/worker/situation/build-employees-rollup-needsyou.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass
    - grep shows exactly one `ctx.issues.update(` in src/worker/handlers/situation-assign-owner.ts (the leafIssueId mutation); zero `ctx.db` calls in that file
    - The agent-assign-success test asserts the update's 4th argument (actor) contains the operator userId (audit attribution)
    - The self-assign-success test asserts the patch is `{ assigneeUserId: <userId> }` (NOT assigneeAgentId) — the D-02 path
    - build-employees-rollup-needsyou test proves needsYou.count > 0 when ≥1 unowned blocked row exists and 0 viewer-owned (the frozen-banner bug fixed)
    - (WARNING 1 / R4) build-employees-rollup-needsyou test asserts that for the all-unowned fixture, `needsYou.topAction` carries the oldest-unowned row's `agentId` AND a non-null `leafIssueId` (enough to drive the owner picker — NOT a chat deep-link). The unowned case must NOT yield a null/empty topAction when count > 0, or 09-02's [Assign first ▾] would render disabled (an R4 dead button).
    - grep shows `registerSituationAssignOwner` imported and called in src/worker.ts
  </acceptance_criteria>
  <done>The first plugin core-issue mutation exists, gated + actor-attributed + tested; the needs-you count counts unowned blockers; topAction can drive the unowned picker.</done>
</task>

<task type="auto">
  <name>Task 3: Add issues.update capability; remove recompute-situation cron job + dead materialized read-path (worker tier)</name>
  <files>src/manifest.ts, src/worker.ts, src/worker/jobs/situation-snapshot.ts, src/worker/handlers/situation-room.ts</files>
  <read_first>
    - src/manifest.ts (capabilities[] array — confirm `issues.update` is NOT present; jobs[] array — find the recompute-situation entry around line 895)
    - src/worker.ts (find the recompute-situation job registration — the job handler is registerSituationSnapshotJob or similar in src/worker/jobs/situation-snapshot.ts)
    - src/worker/jobs/situation-snapshot.ts (the dead cron writer — its host calls fail every tick on 2026.525.0 PR #6547; safe to delete; it has NO synchronous UI caller)
    - src/worker/handlers/situation-room.ts (lines 128-156 — the materialized `situation_snapshots` SELECT + the `row` branch + the `...payload` spread. This is the dead-job READ-PATH. The job is being deleted, so `row` is never written post-Phase-9.)
    - 09-SPEC.md R1 (worker side) + R8 (capability) + R9 (no migration / table not dropped); 09-CONTEXT.md integration-points section
    - CLAUDE.md "version-in-two-places" rule (NOTE: version bump itself is Plan 09-03's job — do NOT bump version here; only add the capability + remove the dead surfaces)
  </read_first>
  <action>
    In `src/manifest.ts` capabilities[]: add the string `'issues.update'` (the comment block near the chat-capability note explicitly said it was deliberately NOT added in Phase 4 — supersede that with a Phase 9 note: "Plan 09-01 — situation.assignOwner is the first plugin core-issue mutation; ctx.issues.update now needs its capability string"). Remove the `recompute-situation` entry from the jobs[] array (the `{ jobKey: 'recompute-situation', schedule: '*/1 * * * *', ... }` object). Leave the `compile-bulletin` job entry intact. Leave `jobs.schedule` capability in place (compile-bulletin still uses it). DO NOT remove the situation.artifacts handler here — that removal is in 09-02 (BLOCKER 1: atomic with its UI caller).
    In `src/worker.ts`: remove the import + registration of the recompute-situation job handler (registerSituationSnapshotJob). Delete the now-orphaned file `src/worker/jobs/situation-snapshot.ts` (verify via grep that nothing else imports it — the situation.snapshot DATA HANDLER in situation-room.ts is a DIFFERENT file and STAYS). DO NOT touch the situation.artifacts handler registration here (09-02 owns it).
    (WARNING 5) In `src/worker/handlers/situation-room.ts`: the materialized read-path (the `ctx.db.query<SnapshotRow>(... situation_snapshots ...)` at ~128 + the `if (row) { return { ...payload, ... } }` branch) is now PERMANENTLY DEAD — the writer cron is deleted, so a row is never written post-Phase-9. Resolve the dependency one of two ways: (a) PREFERRED — delete the SnapshotRow query + the row-exists branch entirely and always return the freshly computed `{ org_blocked_backlog, situation_employees, needsYou, taken_at: new Date().toISOString() }` (the no-row path becomes the only path); OR (b) retain the read branch but add an explicit comment `// dead-safe fallback — situation_snapshots row never written post-Phase-9 (recompute-situation cron deleted in 09-01)`. Either way the handler must no longer DEPEND on a materialized row to render. DO NOT drop the `situation_snapshots` TABLE or add a migration (R9 additive-only — leaving the empty table is correct).
    Run typecheck after the deletes to catch any dangling import.
  </action>
  <verify>
    <automated>cd "C:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep shows `'issues.update'` present in src/manifest.ts capabilities[]
    - grep for `recompute-situation` returns ZERO matches in src/manifest.ts and src/worker.ts
    - src/worker/jobs/situation-snapshot.ts no longer exists (deleted)
    - grep for the situation-snapshot job import returns zero matches in src/worker.ts
    - (WARNING 5) src/worker/handlers/situation-room.ts no longer depends on the situation_snapshots materialized read-path: EITHER the SnapshotRow query + row-exists branch is deleted (grep for `situation_snapshots` returns zero in this file), OR the branch is retained WITH the explicit `// dead-safe fallback — ... never written post-Phase-9` comment present (grep finds the comment). The handler renders from the fresh compute regardless.
    - (WARNING 5) NO `migrations/` file is added and no `DROP TABLE`/`ALTER TABLE situation_snapshots` appears anywhere (R9 — the empty table stays)
    - grep confirms `situation.snapshot` data handler (registerSituationRoomHandlers) and the `situation_employees` key still present in src/worker/handlers/situation-room.ts (NOT removed)
    - (BLOCKER 1) grep confirms the situation.artifacts handler file + its src/worker.ts registration are STILL PRESENT after this plan (their removal is 09-02, atomic with the UI caller)
    - `npx tsc --noEmit` reports no new errors attributable to the deletes
    - package.json + src/manifest.ts version literal still read 1.2.2 (NOT bumped — that is Plan 09-03)
  </acceptance_criteria>
  <done>issues.update capability is declared; the dead recompute-situation cron job + its dead materialized read-path are removed at the worker tier with no dangling imports; the situation_snapshots table is preserved (R9); the situation.artifacts handler stays for 09-02's atomic removal; the live snapshot handler renders from the fresh compute.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → situation.assignOwner worker action | operator-supplied leafIssueId / assigneeAgentId / takeItMyself cross into a core-issue mutation |
| situation.assignOwner → public.issues | first plugin write to a core Paperclip table (assignee reassignment) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Elevation of Privilege | situation.assignOwner | mitigate | viewer-authority gate mirrors agent.takeOwnership (userId required); ctx.agents.get(assigneeAgentId, companyId) company-scope gate rejects cross-company agentIds (NOT_FOUND) |
| T-09-02 | Spoofing/Repudiation | ctx.issues.update actor arg | mitigate | the 4th-arg actor carries the operator userId so the Paperclip audit trail attributes the reassignment to the operator, never the plugin worker (asserted in test) |
| T-09-03 | Tampering | leafIssueId param | mitigate | mutation goes only through typed ctx.issues.update (never ctx.db, which cannot touch public.*); host validates the issue belongs to companyId |
| T-09-04 | Information Disclosure | needsYou unowned predicate | accept | ownerName is already scrubbed to 'Unassigned' (NO_UUID_LEAK) before the count predicate reads it; no raw UUID enters the count path |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | no new package-manager installs in this plan (zero new runtime deps); no install task → audit table N/A |
</threat_model>

<verification>
- `node --test test/worker/situation/group-employee-state.test.mjs` passes
- `node --test test/worker/handlers/situation-assign-owner.test.mjs test/worker/situation/build-employees-rollup-needsyou.test.mjs` passes
- `npx tsc --noEmit` clean (no dangling imports after the deletes)
- grep: `'issues.update'` present; `recompute-situation` absent; one `ctx.issues.update(` in situation-assign-owner.ts; zero `ctx.db` there
- (WARNING 5) situation-room.ts no longer depends on the situation_snapshots materialized read-path (branch deleted OR annotated dead-safe); the situation_snapshots TABLE is NOT dropped (no migration) — R9 preserved
- (BLOCKER 1) The situation.artifacts handler is INTACT after this plan — its removal is 09-02 (atomic with the UI caller). Do NOT expect a red situation-artifacts test from this plan; the existing situation-artifacts test stays green here and is swept in 09-02 with the handler+caller deletion.
</verification>

<success_criteria>
- situation.assignOwner reassigns a core issue via ctx.issues.update with operator actor attribution (unit-proven; live-proven in 09-03)
- Each rollup row carries a worker-computed group; UI will render verbatim
- needsYou.count includes unowned blockers; topAction carries the unowned row's agentId + non-null leafIssueId so the banner's [Assign first ▾] can drive the picker (R4, no dead button)
- issues.update capability declared; recompute-situation cron job + its dead materialized read-path removed at the worker tier; situation_snapshots table preserved (R9)
- situation.artifacts handler left in place for 09-02's atomic removal (BLOCKER 1)
- No version bump (deferred to 09-03)
</success_criteria>

<output>
Create `.planning/phases/09-situation-room-actionable-cockpit/09-01-SUMMARY.md` when done. Note for Plan 09-02: the situation.artifacts worker handler + its registration are STILL PRESENT — 09-02 deletes them atomically with the `usePluginData('situation.artifacts')` UI caller (BLOCKER 1).
</output>
