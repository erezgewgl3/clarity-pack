---
phase: 09-situation-room-actionable-cockpit
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  # --- UI surface (edit / restructure) ---
  - src/ui/surfaces/situation-room/index.tsx
  - src/ui/surfaces/situation-room/employee-row.tsx
  - src/ui/surfaces/situation-room/employee-row-strip.tsx
  - src/ui/surfaces/situation-room/needs-you-banner.tsx
  - src/ui/surfaces/situation-room/owner-picker-popover.tsx
  - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx
  - src/ui/primitives/theme.css
  # --- UI surface (DELETE — dead grid + merged components) ---
  - src/ui/surfaces/situation-room/agent-card.tsx              # DELETE
  - src/ui/surfaces/situation-room/artifact-chip-row.tsx       # DELETE (imported only by agent-card)
  - src/ui/surfaces/situation-room/org-blocked-backlog-banner.tsx  # DELETE (merged into expander)
  - src/ui/surfaces/situation-room/critical-path-strip.tsx     # DELETE (merged into expander)
  - src/ui/surfaces/situation-room/awaiting-you-pill.tsx       # DELETE
  # --- WORKER (BLOCKER 1: situation.artifacts removal — atomic with its UI caller above) ---
  - src/worker/handlers/situation-artifacts.ts                 # DELETE (worker handler)
  - src/worker.ts                                              # remove situation.artifacts import + registration
  - src/manifest.ts                                            # remove any situation.artifacts capability/registration entry (if present)
  # --- TESTS (explicit per-file disposition; WARNING 3 — all paths verified to exist) ---
  - test/ui/situation-room.test.mjs                            # REWRITE (grid -> group assertions)
  - test/ui/clarity-pack-css-rules.test.mjs                    # REWRITE (grid CSS -> group CSS rule set)
  - test/ui/no-react-key-warnings.test.mjs                     # REWRITE (extend source-grep to new components)
  - test/build/runtime-css-injection.test.mjs                  # REWRITE (grid CSS -> group CSS injected set)
  - test/ui/agent-card-now-doing-fallback.test.mjs            # DELETE (tests deleted AgentCard)
  - test/ui/surfaces/situation-room/agent-card-open-chat.test.mjs   # DELETE (tests deleted AgentCard)
  - test/ui/surfaces/situation-room/artifact-chip-row.test.mjs      # DELETE (tests deleted artifact-chip-row)
  - test/worker/handlers/situation-artifacts.test.mjs          # DELETE (tests deleted worker handler — BLOCKER 1)
  - test/ctt07/situation-artifacts-no-issue-update.test.mjs    # DELETE (asserts the now-deleted handler does not call issues.update — moot once handler is gone)
  - test/ui/surfaces/situation-room/no-dead-buttons.test.mjs   # NEW (R4 gate)
autonomous: true
requirements: [R1, R2, R4, R5, R6, R7, R9]
# NOTE (revision): R1 here now ALSO explicitly spans the situation.artifacts WORKER
# handler removal moved in from 09-01 (BLOCKER 1) — the worker handler and its
# usePluginData('situation.artifacts') UI caller are deleted in the SAME plan/commit
# (atomic), so the UI never calls a handler that no longer exists between waves.
must_haves:
  truths:
    - "D-01: owner picker = full roster role-ordered, Editor-Agent excluded (sourced from chat.roster), no smart/least-busy sort. D-02: 'Take it myself' = assigneeUserId, row reads 'with you — handling manually'. D-03: all three group headers always render even when empty ('— none —'). D-04: paused agents (row.isPaused from 09-01) show a 'paused' marker + one-click Resume via agents.resumeHeartbeat."
    - "The Situation Room renders exactly three groups (Needs you / Working / Idle), always all three with counts even when empty"
    - "Every surfaced row action button performs a real action or is absent — none disabled, none bound to a no-op"
    - "Assign owner opens a roster popover (incl. Take it myself) wired to situation.assignOwner; on success the snapshot force-refetches and the row re-groups"
    - "The owner picker is sourced from chat.roster (Editor-Agent already excluded), NOT ctx.agents.list — the Editor-Agent never appears as an assignable owner"
    - "The needs-you banner's [Assign first] is never disabled when needsYou.count > 0: for the unowned case it scrolls to the oldest-unowned row and opens its OWNER PICKER; for the owned-needs-you case it keeps the chat deep-link"
    - "The dead AgentCard grid, org-backlog banner, critical-path strip, awaiting-you pill, the situation.artifacts UI fetch AND the situation.artifacts WORKER handler are all gone (atomic — BLOCKER 1)"
    - "Org-backlog + critical-path appear only as one '+N more blocked issues' expander at the end of Needs-you"
    - "Stand down shows a confirm dialog before pausing; Assign owner/Assign work apply immediately"
  artifacts:
    - path: "src/ui/surfaces/situation-room/index.tsx"
      provides: "three grouped sections fed by situation_employees+group; force-refetch on assign; no dead-grid/artifacts fetch"
      contains: "needs_you"
    - path: "src/ui/surfaces/situation-room/owner-picker-popover.tsx"
      provides: "role-ordered roster popover (chat.roster) + Take it myself, dispatching situation.assignOwner"
      contains: "situation.assignOwner"
    - path: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      provides: "single '+N more blocked issues' expander merging org-backlog + critical-path, at end of Needs-you"
      contains: "more blocked issues"
  key_links:
    - from: "src/ui/surfaces/situation-room/owner-picker-popover.tsx"
      to: "situation.assignOwner"
      via: "usePluginAction dispatch"
      pattern: "situation\\.assignOwner"
    - from: "src/ui/surfaces/situation-room/owner-picker-popover.tsx"
      to: "chat.roster"
      via: "usePluginData('chat.roster') — Editor-Agent excluded server-side (CHAT-01); NOT ctx.agents.list"
      pattern: "chat\\.roster"
    - from: "src/ui/surfaces/situation-room/index.tsx"
      to: "forceRefetch"
      via: "onAssignSuccess bumps refreshKey -> re-reads snapshot -> row re-groups"
      pattern: "refreshKey|forceRefetch"
    - from: "src/ui/surfaces/situation-room/employee-row.tsx"
      to: "per-state action clusters"
      via: "no disabled/no-op buttons; absent when not applicable"
      pattern: "row\\.group|row\\.state"
    - from: "src/worker.ts"
      to: "situation.artifacts handler (DELETED — BLOCKER 1)"
      via: "the worker handler + its UI caller are removed in the same commit (atomic)"
      pattern: "situation\\.artifacts"
---

<objective>
UI tier for the actionable cockpit. Restructures `index.tsx` into the three grouped sections (always all three, D-03), wires every per-state action button to a real action (R4 — the operator's headline rule), adds the owner-picker popover (D-01) with "Take it myself" (D-02), un-freezes the banner with `[Assign first]` (R5), merges org-backlog + critical-path into a single expander (R6), adds the Stand-down confirm + Resume (R7 + D-04), force-refetches so a row visibly re-groups after assign (the mockup's live behavior), and deletes the dead grid + dead pipelines + dead CSS (R1). Builds to match `sketches/clarity-situation-room-redesign.html`.

NOTE (revision, BLOCKER 1): this plan now ALSO removes the `situation.artifacts` WORKER handler (handler file + worker.ts registration + any manifest entry) in the SAME commit as deleting its `usePluginData('situation.artifacts')` UI caller. The two MUST be atomic — if the worker handler were removed a wave earlier (09-01), the still-deployed UI would call a handler that no longer exists and throw on the live surface. Removing both together is the only safe ordering.

Purpose: Make the cockpit actionable — zero dead buttons, the hero assign-owner round-trip working in place.
Output: restructured `index.tsx` + `employee-row.tsx`, two new components (owner-picker-popover, blocked-backlog-expander), deletion of agent-card/artifact-chip-row/org-blocked-backlog-banner/critical-path-strip/awaiting-you-pill (UI) + the situation.artifacts WORKER handler (atomic), scoped CSS, and the dead-grid test files rewritten/deleted per explicit disposition + a new no-dead-buttons test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-situation-room-actionable-cockpit/09-SPEC.md
@.planning/phases/09-situation-room-actionable-cockpit/09-CONTEXT.md
@.planning/phases/09-situation-room-actionable-cockpit/09-01-SUMMARY.md
@sketches/clarity-situation-room-redesign.html
@CLAUDE.md
@.claude/skills/sketch-findings-clarity-pack/SKILL.md

<interfaces>
<!-- From files already read this session. -->

From src/ui/surfaces/situation-room/index.tsx (the body to restructure — current mount order):
```
SituationData = { employees: AgentEmployee[] (DEAD GRID), critical_path, artifacts_shipped_today,
  awaiting_you_count, awaiting_you_oldest_age, narrative, org_blocked_backlog?, situation_employees?, needsYou? }
// SituationRoomBody currently renders, in order:
//   <NeedsYouBanner ... /> <EmployeeRowStrip ... /> <OrgBlockedBacklogBanner ... />
//   <header><AwaitingYouPill .../></header> <CriticalPathStrip .../>
//   <div className="clarity-agent-grid">{payload.employees.map(emp => <AgentCard key={emp.userId} .../>)}</div>
// refreshKey/forceRefetch pattern already exists (lines 207-215): setRefreshKey(k=>k+1) -> both usePluginData refetch.
// usePluginData('situation.snapshot', { userId, companyId, _refreshKey: refreshKey })
// usePluginData('situation.artifacts', ...)  <- DELETE this fetch + delete the worker handler in the SAME commit (BLOCKER 1)
// useHostLocation/useHostNavigation + extractCompanyPrefixFromPathname already wired (companyPrefix + navigate).
```

From src/ui/surfaces/situation-room/employee-row.tsx (Plan 08-02 — the dead-button source):
```
export type SituationEmployeeRow = { agentId, name, role, state, focusIssueId, focusLine,
  lastActivityAt, ageBucket, blockerChain:{rootIssueId,leafIssueId,humanAction,ownerName,ownerAgentId}|null, doneTodayCount }
// THE DEAD BUTTONS to fix (R4):
//   - blocked row: <button disabled={!deepLink} ...>Open chat with {ownerName}</button>  <- disabled when unowned
//   - idle/stale: <button ...onClick={()=>{ /* deferred — affordance only */ }}>Assign work / Stand down</button>  <- no-op
// buildChatDeepLink({ route:'employee-only', companyPrefix, assigneeAgentId }) -> { to } ; navigate(to).
// formatAge from ../../primitives/state-pill-format.ts ; ageMsFromISO helper present.
// NOTE: add `group: EmployeeGroup` to this type to mirror the 09-01 worker field.
```

From src/worker/handlers/chat-roster.ts (WARNING 2 — the owner-picker roster source):
```
registerChatRoster: wrapDataHandler(ctx, 'chat.roster', ...) returns { kind:'roster', employees:[{id,name,role,status}] }
// The Editor-Agent is excluded server-side by AGENT ID (resolved via ctx.agents.managed.get(EDITOR_AGENT_KEY)) — CHAT-01/D-03.
// The owner picker MUST source options from chat.roster (NOT ctx.agents.list) so the Editor-Agent never appears as an assignable owner.
```

The mockup (sketches/clarity-situation-room-redesign.html) is the interaction contract. Key behaviors encoded there:
- Three sections via `section(cls,icon,title,count,metaText,rowsHtml,emptyText)` — ALWAYS rendered; empty -> `<p class="empty-note">… — none —</p>`.
- needsRow: unowned -> [Assign owner (gold)] + [Open <leaf>]; owned -> [Open chat: <owner> (gold)] + [Wake] + [Open].
- workingRow: focus line + "moving · no action needed" (no buttons).
- idleRow: idle -> [Assign work (gold)]; stale -> [Assign work] + [Stand down (danger)].
- rosterPop: role-listed items + a `.self` "Take it myself (Eric)" item.
- banner urgent: "N stuck · M unowned -> assign owners" + [Assign first]; clear: "0 need you — M working · K idle".
- Assign first scrolls oldest unowned row into view + opens its picker (NOT a chat deep-link for the unowned case — WARNING 1).
- assign(): on success the row re-groups (jumps into Working) + flashes + toast. In the real plugin this is the situation.assignOwner round-trip + forceRefetch.
- orphan expander: "+ N more blocked issues across the org (no active agent)" toggling a list; each orphan row has its own [Assign] + [Open].

From the 09-01 worker output (this plan's dependency):
- situation.snapshot now returns `situation_employees: SituationEmployeeRow[]` where each row has `group: 'needs_you'|'working'|'idle'`, sorted blocked->stale->idle->reviewing->running.
- `needsYou` count includes unowned blockers; `needsYou.topAction` = oldest unowned (or viewer-owned) row, carrying agentId + (for the unowned case) a non-null leafIssueId so the banner picker has what it needs (WARNING 1).
- New action: `situation.assignOwner` with params { companyId, leafIssueId, userId, assigneeAgentId? , takeItMyself? } -> { ok, leafIssueId, assignedTo } | { error }.
- The situation.artifacts WORKER handler is STILL PRESENT after 09-01 — this plan deletes it atomically with the UI fetch (BLOCKER 1).

Existing reusable assets (read before reuse):
- chat.roster handler (Editor-Agent already excluded) — the owner-picker roster source. Dispatch via usePluginData('chat.roster', {companyId, userId}). WARNING 2: NOT ctx.agents.list.
- agents.resumeHeartbeat action — Resume for paused agents (D-04). usePluginAction('agents.resumeHeartbeat').
- ctx.agents.pause via the existing action used for Stand down — find the existing pause action key (the chat surface's Pause heartbeat used usePluginAction; confirm the bound key, likely 'agents.pause' or the chat Pause path). Wake via 'issues.wakeup' requestWakeup action (find the bound action key used elsewhere).
- buildChatDeepLink (src/ui/surfaces/chat/deep-link.mjs) — employee-only carrier for Open chat / Assign work.
</interfaces>

<read_first>
Executor MUST read these before writing any UI code:
- sketches/clarity-situation-room-redesign.html (the FULL interaction contract — group structure, row anatomy, action clusters, popover, banner variants, expander, gold-reserved accent)
- src/ui/surfaces/situation-room/index.tsx (the body to restructure + the refreshKey pattern to reuse; the usePluginData('situation.artifacts') fetch to delete)
- src/ui/surfaces/situation-room/employee-row.tsx (the dead buttons to fix; the buildChatDeepLink usage)
- src/ui/surfaces/situation-room/employee-row-strip.tsx (current flat strip — replaced by grouped rendering)
- src/ui/surfaces/situation-room/needs-you-banner.tsx (current banner — un-freeze + Assign first; the `disabled={!deepLink}` dead-button pattern to REMOVE)
- src/ui/surfaces/situation-room/org-blocked-backlog-banner.tsx + critical-path-strip.tsx + awaiting-you-pill.tsx + agent-card.tsx (the components being merged/deleted — read to know what data they consumed so the expander preserves the org-backlog + critical-path content)
- src/worker/handlers/chat-roster.ts (WARNING 2 — the roster source for the owner picker; Editor-Agent excluded server-side)
- src/worker/handlers/situation-artifacts.ts + src/worker.ts (BLOCKER 1 — the worker handler + registration to delete atomically with the UI fetch)
- src/ui/surfaces/chat/deep-link.mjs (buildChatDeepLink signature)
- src/ui/surfaces/chat (find the existing Pause-heartbeat + Resume-heartbeat dispatch to copy the action keys + confirm/toast pattern; toast primitive at src/ui/primitives/toast.tsx; how a surface dispatches usePluginData('chat.roster'))
- src/ui/primitives/theme.css (the Situation Room block ~565-826 + state tokens ~600-606 + DEAD grid CSS ~1033-1066, 1242 — scope everything under [data-clarity-surface='situation-room'])
- The dead-grid test files listed in files_modified (read each to know what it asserts before rewriting OR deleting per its disposition)
- src/ui/surfaces/chat/shortcuts-popover.tsx (a popover pattern to reuse for the owner picker)
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Owner-picker popover (chat.roster source) + blocked-backlog expander + per-state action clusters (no dead buttons)</name>
  <files>src/ui/surfaces/situation-room/owner-picker-popover.tsx, src/ui/surfaces/situation-room/blocked-backlog-expander.tsx, src/ui/surfaces/situation-room/employee-row.tsx, src/ui/primitives/theme.css</files>
  <read_first>
    - sketches/clarity-situation-room-redesign.html (rosterPop, needsRow, idleRow, orphan expander, .btn.gold/.btn.danger, .pop/.pop-item.self)
    - src/ui/surfaces/situation-room/employee-row.tsx (the type + dead buttons)
    - src/worker/handlers/chat-roster.ts (WARNING 2 — the roster SOURCE for the owner picker. It already excludes the Editor-Agent by id per CHAT-01/D-03. The picker MUST source options from chat.roster — NOT ctx.agents.list — so the Editor-Agent is never an assignable owner. Returns { kind:'roster', employees:[{id,name,role,status}] }.)
    - src/ui/surfaces/chat/shortcuts-popover.tsx (popover open/close-on-outside-click pattern)
    - src/ui/surfaces/chat (the Pause + Resume + toast dispatch — exact action keys; how an existing surface dispatches usePluginData('chat.roster'))
    - src/ui/primitives/theme.css (gold token --clarity-you / --you reservation; state tokens; the dead-grid CSS to remove)
    - 09-SPEC.md R4 + R7; 09-CONTEXT.md D-01/D-02/D-04
  </read_first>
  <action>
    Create `owner-picker-popover.tsx`: a normal typed React component (NOT a plugin slot-root) `{ leafIssueId, companyId, userId, onAssigned }`. It fetches the roster via usePluginData('chat.roster', {companyId, userId}) — NOT ctx.agents.list and NOT a fresh agents fetch — because chat.roster already excludes the Editor-Agent server-side (CHAT-01/D-03; WARNING 2). The picker options are exactly `roster.employees`; the Editor-Agent MUST NOT appear. Render a popover (mirror shortcuts-popover.tsx open/close + outside-click) listing each agent role-ordered (preserve roster order — NO smart/least-busy sort, D-01), plus a trailing `.self` "Take it myself" item (D-02). Selecting an agent dispatches usePluginAction('situation.assignOwner')({ companyId, leafIssueId, userId, assigneeAgentId }); selecting Take-it-myself dispatches the same with { takeItMyself: true }. On { ok } it calls onAssigned(result) (the parent fires the toast + forceRefetch). Apply immediately, NO intermediate confirm (R7). Gold accent on the trigger + .self item (--clarity-you).
    Create `blocked-backlog-expander.tsx`: `{ backlog, criticalPath, companyId }` — a single collapsible "+ N more blocked issues across the org" toggle (mockup's orphan block) that, when expanded, lists the residual org-backlog blocked issues AND folds the critical-path narrative into the same panel (R6). Each residual row reuses owner-picker-popover for its own [Assign] + an [Open] link. Default collapsed. This REPLACES both OrgBlockedBacklogBanner and CriticalPathStrip.
    Rewrite `employee-row.tsx` per-state action clusters (R4 — every button performs or is ABSENT, never disabled/no-op):
      - group needs_you, unowned (blockerChain.ownerName === 'Unassigned'): [Assign owner] (owner-picker-popover) + [Open <leaf>] (navigate to issue). NO disabled Open-chat.
      - group needs_you, owned: [Open chat: <owner>] (buildChatDeepLink employee-only, ownerAgentId) + [Wake] (dispatch the requestWakeup action) + [Open].
      - group working (running/reviewing): focus line + "moving · no action needed" — NO buttons.
      - group idle, state idle: [Assign work] -> buildChatDeepLink to brief the agent (navigate). NO no-op.
      - group idle, state stale (not paused): [Assign work] + [Stand down] -> opens a confirm dialog (R7); confirm -> dispatch the pause action; cancel -> no pause.
      - paused agent (D-04): stays in Idle with a "paused" marker + [Resume] -> dispatch agents.resumeHeartbeat. Detect paused via `row.isPaused` (the boolean 09-01 sets on every row from agent.status) — NOT a separate per-row fetch, NOT a 6th state. A row with isPaused===true renders the marker + Resume regardless of its idle/stale state.
    Add `group: EmployeeGroup` and `isPaused: boolean` to the SituationEmployeeRow type here (mirror the 09-01 worker fields). Remove the `disabled` attribute and the empty-onClick no-op handlers entirely. Every visible string stays a React text node (T-08-UI / R9) — no dangerouslySetInnerHTML; ownerAgentId consumed only as a buildChatDeepLink arg, never rendered.
    In `theme.css`: add the three group-section styles (.sec needs/working/idle, .empty-note, .pop/.pop-item, .btn.gold/.btn.danger, .orphans/.orphan-toggle, .toast/.flash if needed) scoped under `[data-clarity-surface='situation-room']`. Remove the dead grid CSS (~1033-1066, 1242 — .clarity-agent-grid / .clarity-agent-card and artifact-chip-row rules). Recalibrate the bundle ceiling if needed per Phase 5/7/8 precedent.
  </action>
  <verify>
    <automated>cd "C:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node scripts/check-css-scope.mjs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep shows `situation.assignOwner` dispatched in owner-picker-popover.tsx; roster sourced from `chat.roster`; a "Take it myself" item present
    - (WARNING 2) owner-picker-popover.tsx grep shows `chat.roster` as the option source and ZERO `ctx.agents.list` / `agents.list` / `'agents'` data-fetch calls; a unit/source test asserts the Editor-Agent does NOT appear in picker options (the picker maps roster.employees verbatim, which already excludes the Editor-Agent per CHAT-01)
    - grep for `disabled` in src/ui/surfaces/situation-room/employee-row.tsx returns ZERO matches on action buttons (no disabled action affordances)
    - grep for an empty/no-op onClick (`onClick={() => {` with only a comment body) in employee-row.tsx returns ZERO matches
    - blocked-backlog-expander.tsx renders one expander; grep shows it consumes both org-backlog rows and the critical-path narrative
    - employee-row.tsx Stand-down path opens a confirm before dispatching pause; Resume dispatches agents.resumeHeartbeat
    - rows with `row.isPaused === true` render a 'paused' marker + a [Resume] button (D-04); a test asserts the marker+Resume appear for a paused row and not for a non-paused idle row
    - `node scripts/check-css-scope.mjs` passes (all new CSS under [data-clarity-surface='situation-room'])
    - grep for `dangerouslySetInnerHTML` in the three new/edited UI files returns ZERO matches
  </acceptance_criteria>
  <done>Owner picker (chat.roster-sourced, Editor-Agent excluded) + expander + per-state clusters exist with zero dead buttons; CSS scoped; confirm-on-pause, immediate-on-assign.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure index.tsx into 3 groups + un-frozen banner (Assign-first picker) + force-refetch; delete dead grid + components + situation.artifacts UI fetch AND worker handler (atomic)</name>
  <files>src/ui/surfaces/situation-room/index.tsx, src/ui/surfaces/situation-room/needs-you-banner.tsx, src/ui/surfaces/situation-room/employee-row-strip.tsx, src/ui/surfaces/situation-room/agent-card.tsx, src/ui/surfaces/situation-room/artifact-chip-row.tsx, src/ui/surfaces/situation-room/org-blocked-backlog-banner.tsx, src/ui/surfaces/situation-room/critical-path-strip.tsx, src/ui/surfaces/situation-room/awaiting-you-pill.tsx, src/worker/handlers/situation-artifacts.ts, src/worker.ts, src/manifest.ts</files>
  <read_first>
    - src/ui/surfaces/situation-room/index.tsx (the full body + the existing refreshKey/forceRefetch + companyPrefix/navigate wiring; find the usePluginData('situation.artifacts') fetch to delete)
    - sketches/clarity-situation-room-redesign.html (render() group order: Needs you -> expander -> Working -> Idle; banner variants; #assign-first handler)
    - src/ui/surfaces/situation-room/needs-you-banner.tsx (current banner to un-freeze + add Assign first; note the existing `disabled={!deepLink}` dead-button pattern to REMOVE)
    - the 09-01-SUMMARY.md (confirm situation_employees rows carry `group`; needsYou counts unowned; topAction carries the unowned row's agentId + non-null leafIssueId for the picker)
    - src/worker/handlers/situation-artifacts.ts + src/worker.ts (BLOCKER 1 — the worker handler + its registration to delete atomically with the UI fetch)
    - src/manifest.ts (check for any situation.artifacts capability/registration entry to remove)
    - 09-SPEC.md R1/R2/R5/R6
  </read_first>
  <action>
    Rewrite `employee-row-strip.tsx` into a grouped renderer (or replace its usage in index.tsx with inline grouping that consumes the worker `group` field — R2: NO client-side re-sort, only partition by row.group preserving worker order). Render exactly three sections ALWAYS (D-03): "Needs you" (group==='needs_you'), "Working" (group==='working'), "Idle" (group==='idle'), each with a header + count; an empty group renders its header + count + a muted "— none —" line. Within each section map rows to <EmployeeRow>. At the END of the Needs-you section, render <BlockedBacklogExpander> (Task 1) merging org_blocked_backlog + critical_path.
    Rewrite `needs-you-banner.tsx` to consume the un-frozen needsYou (R5): urgent variant when stuck>0 — "N stuck · M unowned -> assign owners" with [Assign first]; when stuck>0 all-owned — chase variant (no Assign first); neutral "0 need you — M working · K idle" only when count is genuinely 0. (WARNING 1 / R4) [Assign first] is NEVER rendered disabled when needsYou.count > 0. Its behavior branches on whether the topAction row is unowned:
      - UNOWNED topAction (the row at needsYou.topAction.agentId has blockerChain.ownerName === 'Unassigned'): [Assign first] scrolls that oldest-unowned row into view (ref/id + scrollIntoView, mirroring the mockup's #assign-first handler) and OPENS ITS OWNER PICKER. It does NOT build a chat deep-link (there is no ownerAgentId to chat with — that was the old dead-button seam). 09-01 guarantees topAction.leafIssueId is non-null for this case, so the picker has what it needs.
      - OWNED-needs-you topAction (zero unowned, the topAction row resolves to a viewer-owned chain): retain the existing Phase 8 behavior — resolve ownerRow.blockerChain.ownerAgentId and build the chat deep-link; the banner button opens chat with the owner.
      Remove the old `disabled={!deepLink}` pattern from this banner entirely (that was the frozen-banner dead-button) — when count > 0 the button always performs (picker for unowned, chat for owned).
    In `index.tsx`: thread the existing forceRefetch into the grouped strip + banner + expander as `onAssignSuccess` so a successful situation.assignOwner bumps refreshKey -> snapshot re-reads -> the row re-groups (the mockup's live "jumps into Working" behavior). DELETE: the `usePluginData('situation.artifacts')` fetch + artifactsByAgent map; the `<div className="clarity-agent-grid">…AgentCard…</div>` block; the `<OrgBlockedBacklogBanner>`, `<CriticalPathStrip>`, and `<AwaitingYouPill>` mounts + imports; the `employees: AgentEmployee[]` field + `artifacts_shipped_today` + `critical_path` (now consumed only inside the expander via a passed prop) handling that fed the dead grid. Keep `situation_employees`, `needsYou`, `org_blocked_backlog`. Delete the now-orphaned files: agent-card.tsx, org-blocked-backlog-banner.tsx, critical-path-strip.tsx, awaiting-you-pill.tsx (grep first to confirm no other importer; artifact-chip-row.tsx is only imported by agent-card — delete it too if so).
    (BLOCKER 1 — ATOMIC situation.artifacts WORKER removal in this same commit) Now that the `usePluginData('situation.artifacts')` UI caller is gone, delete the worker handler too in the SAME commit so no wave-gap exists where the UI calls a removed handler: delete `src/worker/handlers/situation-artifacts.ts`; remove its import + registration call from `src/worker.ts`; remove any `situation.artifacts` capability/registration entry from `src/manifest.ts` if present. Grep `usePluginData('situation.artifacts')` across src/ to confirm zero remaining callers BEFORE deleting the handler. (The recompute-situation cron job + the dead materialized read-path were already removed in 09-01 — do NOT touch those again here.)
    Run typecheck to catch dangling imports.
  </action>
  <verify>
    <automated>cd "C:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep in index.tsx: zero `clarity-agent-grid`, zero `AgentCard`, zero `situation.artifacts`, zero `OrgBlockedBacklogBanner`, zero `CriticalPathStrip`, zero `AwaitingYouPill`
    - agent-card.tsx, artifact-chip-row.tsx, org-blocked-backlog-banner.tsx, critical-path-strip.tsx, awaiting-you-pill.tsx no longer exist (deleted)
    - (BLOCKER 1 — ATOMIC) grep of index.tsx shows ZERO `usePluginData('situation.artifacts')` references AND `src/worker/handlers/situation-artifacts.ts` no longer exists AND `src/worker.ts` has no `registerSituationArtifacts` (or equivalent situation.artifacts registration) call AND `src/manifest.ts` has no situation.artifacts entry — the worker handler and its UI caller are gone in the SAME commit
    - grep confirms three group sections (needs_you / working / idle) rendered, each with a count, with a "— none —" empty branch (D-03)
    - needs-you-banner.tsx grep shows the urgent `unowned` variant + an `Assign first` affordance; neutral variant gated on count===0
    - (WARNING 1 / R4) needs-you-banner.tsx grep shows NO `disabled={` on the [Assign first] button; the unowned branch opens the owner picker (references the owner-picker-popover / scrollIntoView), the owned branch builds the chat deep-link — and the button is rendered whenever needsYou.count > 0 (never absent/disabled in the urgent variant)
    - index.tsx threads onAssignSuccess -> forceRefetch (refreshKey bump) into the grouped strip
    - BlockedBacklogExpander mounted at the end of the Needs-you section; no standalone org-backlog banner or critical-path strip remains
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The cockpit is one three-group people view with an un-frozen banner (picker for unowned, chat for owned), a single backlog expander, and a live re-group on assign; all dead components/fetches deleted AND the situation.artifacts worker handler removed atomically (BLOCKER 1).</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the dead-grid test files per explicit disposition (delete vs rewrite) + sweep the situation.artifacts worker tests + add the no-dead-buttons test; full gates green</name>
  <files>test/ui/situation-room.test.mjs, test/ui/clarity-pack-css-rules.test.mjs, test/ui/no-react-key-warnings.test.mjs, test/build/runtime-css-injection.test.mjs, test/ui/agent-card-now-doing-fallback.test.mjs, test/ui/surfaces/situation-room/agent-card-open-chat.test.mjs, test/ui/surfaces/situation-room/artifact-chip-row.test.mjs, test/worker/handlers/situation-artifacts.test.mjs, test/ctt07/situation-artifacts-no-issue-update.test.mjs, test/ui/surfaces/situation-room/no-dead-buttons.test.mjs</files>
  <read_first>
    - each listed test file (know what each asserts before rewriting/deleting)
    - the rewritten index.tsx + employee-row.tsx + owner-picker-popover.tsx + needs-you-banner.tsx (the new render to assert against)
    - 09-SPEC.md acceptance checks (the falsifiable list); R4 ("no rendered action button is disabled or bound to a no-op")
    - NOTE (WARNING 3): all listed paths were VERIFIED to exist this session. If the executor finds a path differs, it should grep the closest actual file and note the substitution in the SUMMARY rather than skipping the disposition.
  </read_first>
  <action>
    Apply the explicit per-file disposition (WARNING 3 — some files test DELETED components and must be DELETED, not rewritten; the executor must first re-confirm each path exists, as the planner did):

    REWRITE (component still exists; assert the new grouped render / group CSS):
      - test/ui/situation-room.test.mjs -> assert exactly three group sections render with counts; assert NO `.clarity-agent-grid` in the output; assert situation_employees rows appear under their worker-assigned group (R2 — no client re-sort).
      - test/ui/clarity-pack-css-rules.test.mjs -> update expected rule set: remove .clarity-agent-grid/.clarity-agent-card/artifact-chip rules; add the new group-section + popover + expander rules; all scoped under [data-clarity-surface='situation-room'].
      - test/build/runtime-css-injection.test.mjs -> update the injected-rule set the same way (grid rules out, group/popover/expander rules in, all scoped).
      - test/ui/no-react-key-warnings.test.mjs -> extend the source-grep coverage to the new components (owner-picker-popover, blocked-backlog-expander, grouped strip) — every .map() has a key.

    DELETE (the file tests a component/handler that no longer exists after 09-02; a rewrite would have nothing to assert):
      - test/ui/agent-card-now-doing-fallback.test.mjs -> tests deleted AgentCard. DELETE the file (and remove from any run-all manifest if listed).
      - test/ui/surfaces/situation-room/agent-card-open-chat.test.mjs -> tests deleted AgentCard. DELETE.
      - test/ui/surfaces/situation-room/artifact-chip-row.test.mjs -> tests deleted artifact-chip-row. DELETE.
      - test/worker/handlers/situation-artifacts.test.mjs -> tests the deleted situation.artifacts WORKER handler (BLOCKER 1). DELETE.
      - test/ctt07/situation-artifacts-no-issue-update.test.mjs -> asserts the situation.artifacts handler does not call issues.update; moot once the handler is gone. DELETE (or, if it also guards other handlers, narrow it to drop the situation.artifacts assertion only — prefer DELETE if it is situation.artifacts-specific).
    OPTIONAL coverage replacement for the deleted UI component tests: if the project convention prefers a positive guard, add ONE source-grep guard (it can live in no-dead-buttons.test.mjs or situation-room.test.mjs) asserting the deleted components/handler have no importer anywhere in src/ (agent-card / artifact-chip-row / org-blocked-backlog-banner / critical-path-strip / awaiting-you-pill / situation-artifacts).

    ADD test/ui/surfaces/situation-room/no-dead-buttons.test.mjs (R4 — the operator's headline check, MANDATORY): a source-grep test over employee-row.tsx + owner-picker-popover.tsx + blocked-backlog-expander.tsx + needs-you-banner.tsx asserting (a) zero `disabled` attributes on action buttons (incl. the [Assign first] button — WARNING 1), (b) zero onClick handlers whose body is only a comment / empty block (no-op).
    Run the full gate set.
  </action>
  <verify>
    <automated>cd "C:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test "test/**/*.test.mjs" 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "test/**/*.test.mjs"` reports 0 failures
    - test/ui/surfaces/situation-room/no-dead-buttons.test.mjs exists and passes (asserts zero disabled + zero no-op action buttons, incl. the [Assign first] button)
    - (WARNING 3 — DELETE disposition) these files no longer exist: test/ui/agent-card-now-doing-fallback.test.mjs, test/ui/surfaces/situation-room/agent-card-open-chat.test.mjs, test/ui/surfaces/situation-room/artifact-chip-row.test.mjs, test/worker/handlers/situation-artifacts.test.mjs, test/ctt07/situation-artifacts-no-issue-update.test.mjs
    - (WARNING 3 — REWRITE disposition) these files exist and assert the grouped render / group CSS (not the dead grid): test/ui/situation-room.test.mjs, test/ui/clarity-pack-css-rules.test.mjs, test/build/runtime-css-injection.test.mjs, test/ui/no-react-key-warnings.test.mjs
    - no test references a deleted component (agent-card / artifact-chip-row / org-blocked-backlog-banner / critical-path-strip / awaiting-you-pill / situation-artifacts handler) as live
    - `node scripts/check-css-scope.mjs` passes; `node scripts/check-ui-bundle-size.mjs` passes (ceiling recalibrated if needed)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Suite is green; the dead-grid tests assert the grouped render or are deleted per disposition; the situation.artifacts worker tests are swept; the no-dead-buttons gate enforces R4 in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Situation Room UI -> situation.assignOwner / pause / wake / resume actions | operator clicks cross into worker mutations |
| Worker-supplied row strings -> React render | agent/user names + human-action strings rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Information Disclosure | employee-row + owner-picker render | mitigate | React text nodes only; no dangerouslySetInnerHTML (grep-gated); ownerAgentId consumed only as buildChatDeepLink arg, never rendered (NO_UUID_LEAK preserved by construction) |
| T-09-06 | Tampering | assignOwner dispatch from UI | accept | the UI passes leafIssueId/userId; the worker (09-01) re-gates authority server-side (viewer + company-scope) — UI is trusted same-origin per PLUGIN_SPEC, server-side gate is the real boundary |
| T-09-07 | Denial of Service | force-refetch on every assign | accept | refreshKey bump triggers one snapshot re-read per assign (operator-paced clicks); same pattern already shipped in Chat/06.1 |
| T-09-11 | Elevation of Privilege | owner-picker option source | mitigate | picker sources options from chat.roster (Editor-Agent excluded server-side per CHAT-01), NOT ctx.agents.list — the infra agent can never be assigned as an owner (WARNING 2; test-asserted) |
| T-09-SC | Tampering | npm installs | mitigate | zero new package-manager installs in this plan; audit table N/A |
</threat_model>

<verification>
- `node --test "test/**/*.test.mjs"` 0 failures
- `node scripts/check-css-scope.mjs` passes; `node scripts/check-ui-bundle-size.mjs` passes
- `npx tsc --noEmit` clean
- grep gates: zero `disabled`/no-op action buttons in employee-row.tsx AND needs-you-banner.tsx ([Assign first]); zero dead-grid references in index.tsx; zero `dangerouslySetInnerHTML` in new components; deleted components/handler have no importer
- (BLOCKER 1 — ATOMIC) zero `usePluginData('situation.artifacts')` in src/; src/worker/handlers/situation-artifacts.ts deleted; src/worker.ts has no situation.artifacts registration — worker handler + UI caller removed in one commit
- (WARNING 2) owner-picker sources options from chat.roster (Editor-Agent excluded), not ctx.agents.list — test asserts the Editor-Agent is absent from picker options
- Visual structure matches sketches/clarity-situation-room-redesign.html (three groups, action clusters, gold-reserved accent)
</verification>

<success_criteria>
- One three-group people view (always all three, D-03), fed verbatim by situation_employees+group
- Every surfaced action performs or is absent (R4) — enforced by a CI grep gate; [Assign first] is never disabled when count > 0 (picker for unowned, chat for owned)
- Assign owner popover (D-01, sourced from chat.roster -> Editor-Agent excluded) + Take it myself (D-02) -> situation.assignOwner -> force-refetch re-group
- Un-frozen banner + Assign first (R5); single backlog/critical-path expander (R6); Stand-down confirm + Resume (R7 + D-04)
- Dead grid + dead pipelines + dead CSS removed (R1); the situation.artifacts WORKER handler + its UI caller removed ATOMICALLY (BLOCKER 1); NO_UUID_LEAK + scoped CSS + no migration preserved (R9)
</success_criteria>

<output>
Create `.planning/phases/09-situation-room-actionable-cockpit/09-02-SUMMARY.md` when done. Record the recalibrated bundle ceiling if changed. Confirm in the SUMMARY that the situation.artifacts WORKER handler + UI caller were removed in the SAME commit (BLOCKER 1 atomicity) and list each test file's actual disposition (deleted vs rewritten) with the verified paths.
</output>
