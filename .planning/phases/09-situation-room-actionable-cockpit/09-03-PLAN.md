---
phase: 09-situation-room-actionable-cockpit
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - package.json
  - src/manifest.ts
  - .planning/phases/09-situation-room-actionable-cockpit/09-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [R1, R2, R3, R4, R5, R6, R7, R8, R9]
must_haves:
  truths:
    - "Both version literals (package.json + src/manifest.ts) read 1.3.0"
    - "The host install validator on 2026.525.0 accepts the new issues.update capability"
    - "A verified pre-snapshot AND a rehearsed-and-VERIFIED restore/rollback path precede the BEAAA reinstall (bookended-by-snapshots — a snapshot file existing is NOT sufficient; the restore path must be confirmed working)"
    - "The live drill clicks Assign owner / Open chat / Wake / Assign work / Stand down and each performs its real effect; zero disabled/no-op buttons"
    - "Clicking Assign owner -> pick agent reassigns the real core issue (post-action re-read shows new assigneeAgentId), attributed to the operator"
    - "At drill time >=1 blocked-and-UNOWNED issue exists on BEAAA (found or seeded), the banner shows a non-zero UNOWNED count, and the Assign-owner flow reassigns a real issue (verified by re-read); no .clarity-agent-grid in the DOM"
  artifacts:
    - path: ".planning/phases/09-situation-room-actionable-cockpit/09-VERIFICATION.md"
      provides: "live-drill evidence mapping R1-R9 + 11 acceptance checks to PASS/N-A with screenshots"
      contains: "assigneeAgentId"
    - path: "src/manifest.ts"
      provides: "version 1.3.0 literal + release-history note + issues.update capability"
      contains: "1.3.0"
  key_links:
    - from: "package.json"
      to: "src/manifest.ts"
      via: "both version literals 1.3.0 (host reads dist/manifest.js)"
      pattern: "1\\.3\\.0"
    - from: "live BEAAA issue"
      to: "situation.assignOwner"
      via: "post-action re-read shows new assigneeAgentId attributed to operator"
      pattern: "assigneeAgentId"
---

<objective>
Ship v1.3.0 to BEAAA and prove the actionable cockpit live. Bumps the version in both required places, runs the full gate set, takes the mandatory pre-deploy snapshot bookend WITH a verified-working restore path (R8 + BLOCKER 2), deploys via DEPLOY-RUNBOOK Path A, and runs the Playwright live drill proving the 11 SPEC acceptance checks — critically R4 (no dead buttons; every action performs), R3 (assign-owner mutates the real issue, operator-attributed), R1 (no agent grid), R5 (non-zero banner with unowned blockers). Files VERIFICATION.md, flips R1-R9 to Implemented, updates STATE + ROADMAP, closure commit.

Purpose: Close Phase 9 on a passing live drill — the operator's "every button works when I click it" acceptance spine.
Output: v1.3.0 deployed + verified on BEAAA; 09-VERIFICATION.md; requirements + state + roadmap updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-situation-room-actionable-cockpit/09-SPEC.md
@.planning/phases/09-situation-room-actionable-cockpit/09-CONTEXT.md
@.planning/phases/09-situation-room-actionable-cockpit/09-01-SUMMARY.md
@.planning/phases/09-situation-room-actionable-cockpit/09-02-SUMMARY.md
@.planning/STATE.md
@CLAUDE.md

<deploy_context>
- Current version is 1.2.2 (a 1.2.2 Reader-TL;DR hotfix shipped after Phase 8's 1.2.0 — SPEC said 1.2.1 but the live literal is 1.2.2). Bump 1.2.2 -> 1.3.0.
- BEAAA is the live instance. Phase 8 deployed v1.2.0 via DEPLOY-RUNBOOK Path A (local-tarball install); plugin UUID a763176a-2f4d-4986-b190-b5151e42cc00 preserved across upgrades.
- Eric runs fully autonomous and is authorized to deploy + live-drill BEAAA WITHOUT pausing (project memory autonomous-deploy-authorization) — BUT the snapshot bookend is mandatory (CLAUDE.md bookended-by-snapshots rule + project memory mempalace-before-vps-commands).
- Operator gotchas to honor (project memory): query the runbook BEFORE proposing snapshot/restore/install commands; every VPS bash block self-contained with the canonical env re-export; safety CLI invocation specifics live in runbook/operator-gotchas.md + the DEPLOY runbook.
- This is the FIRST plugin core-issue mutation — the snapshot is the rehearsed-restore guarantee that the assignee write is reversible.
- (BLOCKER 2) A snapshot file alone does NOT satisfy the bookended-by-snapshots rule — CLAUDE.md requires "a working rollback path verified at least once." The restore/rollback path MUST be verified working before deploy: either a rehearsed restore-to-staging smoke (`node scripts/safety/cli.mjs verify --db-url <from /etc/paperclip/db.env>`; pre-create the `paperclip_restoring` staging DB first per project memory countermoves-verify-install-drill) OR an explicit re-confirmation that the Phase 1 restore drill (rehearsal-drill-findings, PASS 2026-05-13) is still valid for this host state. Query MemPalace clarity_pack/runbook + decisions for the exact safety-CLI invocation before composing VPS commands (mempalace-before-vps-commands). Auth note (paperclip-auth-pattern): don't set PAPERCLIP_API_URL env when running snapshot (breaks auth.json profile lookup).
- (WARNING 4) Phase 8 VERIFICATION found the live BEAAA roster had ZERO null-owner chains (M3 was N/A). The drill MUST ensure >=1 blocked-and-unowned issue exists (find one or seed a throwaway, cleaned up after) so the hero Assign-owner flow + un-frozen banner are actually exercised, not vacuously passed.
</deploy_context>

<read_first>
Executor MUST read before any deploy/version step:
- CLAUDE.md (version-in-two-places rule; bookended-by-snapshots rule)
- The DEPLOY runbook (DEPLOY-RUNBOOK / runbook/ — Phase 8 Path A local-tarball install; the snapshot -> pack -> upgrade -> smoke -> verify sequence)
- runbook/operator-gotchas.md (staging DB pre-create, instance-id-dot rename, safety CLI --db-url, env re-export)
- src/manifest.ts (the `version:` literal — currently 1.2.2; the release-history comment block to append to)
- package.json (the `version` field — 1.2.2)
- 09-SPEC.md Acceptance Criteria (the 11-check list the drill must satisfy)
- The MemPalace clarity_pack/runbook + decisions wings (query for the latest BEAAA deploy pattern + safety CLI invocation + the rehearsed-restore drill outcome BEFORE composing VPS commands). Relevant memories: countermoves-verify-install-drill (verify needs --db-url; pre-create paperclip_restoring), rehearsal-drill-findings (Phase 1 restore PASS 2026-05-13), paperclip-auth-pattern (don't set PAPERCLIP_API_URL env when running snapshot).
</read_first>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version bump to 1.3.0 (two sources) + release-history note + full gate set + pack</name>
  <files>package.json, src/manifest.ts</files>
  <read_first>
    - package.json (version field 1.2.2)
    - src/manifest.ts (version literal 1.2.2 + the release-history comment block above it)
    - 09-01-SUMMARY.md + 09-02-SUMMARY.md (what shipped; the recalibrated bundle ceiling if changed)
    - CLAUDE.md version-in-two-places rule + project memory plugin-version-bump-two-sources
  </read_first>
  <action>
    Set `"version": "1.3.0"` in package.json. Set `version: '1.3.0'` in src/manifest.ts (the host reads dist/manifest.js built from this literal — both MUST match or you ship v_new code with a v_old label). Prepend a 1.3.0 release-history note to the manifest comment block summarizing Phase 9: "Situation Room actionable cockpit — three-group people view replaces the dead AgentCard grid; situation.assignOwner is the first plugin core-issue mutation (ctx.issues.update, operator actor-attributed); issues.update capability added; recompute-situation cron job + situation.artifacts handler removed; un-frozen needs-you banner; org-backlog+critical-path merged into one expander; no dead buttons (R4)." Then run the full gate set: tsc, check-css-scope, check-ui-bundle-size, full test suite, and the three builds (worker/ui/manifest). Pack the tarball (the build -> pack step the DEPLOY runbook prescribes) and record its sha256 + byte size.
  </action>
  <verify>
    <automated>cd "C:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit && node scripts/check-css-scope.mjs && node scripts/check-ui-bundle-size.mjs && node --test "test/**/*.test.mjs" 2>&1 | tail -8 && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep shows `"version": "1.3.0"` in package.json AND `version: '1.3.0'` in src/manifest.ts (both literals 1.3.0)
    - `npx tsc --noEmit` clean
    - `node scripts/check-css-scope.mjs` passes; `node scripts/check-ui-bundle-size.mjs` passes
    - `node --test "test/**/*.test.mjs"` reports 0 failures (incl. the no-dead-buttons gate from 09-02)
    - `pnpm build` produces dist/worker.js, dist/ui, dist/manifest.js with no errors
    - dist/manifest.js (built) carries version 1.3.0 AND `issues.update` in capabilities[] (grep the built file)
    - the packed tarball exists; sha256 + byte size recorded in the SUMMARY
  </acceptance_criteria>
  <done>v1.3.0 stamped in both sources; all gates green; tarball packed + hashed; built manifest carries the version + issues.update capability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Snapshot + VERIFIED-restore bookend + BEAAA Path A deploy + Playwright live drill of the 11 acceptance checks</name>
  <files>package.json, src/manifest.ts, BEAAA live instance (deploy target), .planning/phases/09-situation-room-actionable-cockpit/09-VERIFICATION.md (evidence)</files>
  <read_first>
    - the DEPLOY runbook + runbook/operator-gotchas.md (snapshot/restore/install sequence; env re-export; --db-url; the `paperclip_restoring` pre-create + the port + connectionString swap)
    - 09-SPEC.md Acceptance Criteria (the 11-check list)
    - MemPalace clarity_pack/runbook + decisions (latest deploy pattern, safety CLI invocation, the rehearsed-restore drill outcome) — query BEFORE composing any VPS command. Relevant memories: countermoves-verify-install-drill (verify needs --db-url; pre-create paperclip_restoring), rehearsal-drill-findings (Phase 1 restore PASS 2026-05-13), paperclip-auth-pattern (don't set PAPERCLIP_API_URL env when running snapshot).
  </read_first>
  <what-built>
    The full v1.3.0 actionable cockpit (09-01 worker + 09-02 UI), gated green and packed in Task 1. This checkpoint deploys it to live BEAAA behind the mandatory snapshot + VERIFIED-restore bookend and drives the live acceptance drill. It is `blocking-human` because (a) it deploys to a live instance and (b) it is the FIRST plugin core-issue mutation — the snapshot bookend + a confirmed-working restore path + assign-owner reversibility must be confirmed by the operator.
  </what-built>
  <how-to-verify>
    PRE-DEPLOY (R8 snapshot bookend — MANDATORY, do FIRST):
    1. Take a verified pre-snapshot of BEAAA (Postgres dump + filesystem archive + version + plugin list) via the safety CLI per the runbook (self-contained env re-export; --db-url from the box's db.env). Confirm the snapshot age < 15 min BEFORE reinstall.
    1b. (BLOCKER 2 — REHEARSED RESTORE, MANDATORY — a snapshot file existing is NOT sufficient; CLAUDE.md requires "a working rollback path verified at least once"): verify the restore/rollback path actually works before deploy, via EITHER:
        (a) a rehearsed restore-to-staging smoke per the Phase 1 drill pattern — run `node scripts/safety/cli.mjs verify --db-url <from /etc/paperclip/db.env>` (verify needs --db-url; pre-create the `paperclip_restoring` staging DB per project memory countermoves-verify-install-drill BEFORE running verify), and confirm the verify gate returns PASS; OR
        (b) explicit re-confirmation that the already-rehearsed Phase 1 restore drill (project memory rehearsal-drill-findings — PASS 2026-05-13, SAFE-01..05 green) remains valid for THIS host state, citing the safety CLI invocation (`node scripts/safety/cli.mjs verify --db-url <from db.env>`, pre-create `paperclip_restoring`) and confirming the box's snapshot/restore config (port + connectionString swap per the runbook) is unchanged since that drill.
        Query MemPalace clarity_pack/runbook + decisions for the latest safety-CLI invocation + restore pattern BEFORE composing the VPS commands (project memory mempalace-before-vps-commands). The snapshot-without-a-verified-restore state is INSUFFICIENT to pass this task — do NOT deploy on a snapshot whose restore path has not been confirmed this session (path a) or re-confirmed-valid (path b).
    DEPLOY (Path A local-tarball, plugin UUID preserved):
    2. Upload the v1.3.0 tarball; `pnpm paperclipai plugin install` (run from ~/paperclip, cd first). Confirm host install VALIDATOR accepts the new `issues.update` capability (no "Missing required capabilities" / no capability-rejection). Confirm `status=ready version=1.3.0 id=a763176a`.
    DRILL PRECONDITION (WARNING 4 — MANDATORY, do BEFORE the drill; Phase 8 VERIFICATION found the live BEAAA roster had ZERO null-owner chains so M3 was N/A — if the roster again has zero unowned blockers the hero Assign-owner flow and the un-frozen banner cannot be exercised and the drill would vacuously pass):
    2b. Ensure >=1 blocked-AND-unowned issue exists on BEAAA at drill time. First look for one (a blocked issue whose assignee is null / chain owner is `__unowned__`). If none exists, CREATE a throwaway blocked, unassigned issue to exercise the flow. Record its issue identifier. NOTE: any throwaway issue MUST be cleaned up after the drill (delete/close it) OR explicitly noted as a leftover in the VERIFICATION + SUMMARY. Confirm the situation.snapshot for the viewer now reports needsYou.count > 0 with a non-zero unowned component before proceeding.
    LIVE DRILL at /BEAAA/situation-room (the 11 SPEC acceptance checks):
    3. R1: the page renders exactly three group sections (Needs you / Working / Idle); inspect DOM — NO `.clarity-agent-grid` / `.clarity-agent-card` present.
    4. R5: with the >=1 unowned blocked agent from the precondition, the banner shows a NON-ZERO count + urgent styling (not "0 need you"); click [Assign first] -> the oldest unowned row scrolls into view and its OWNER PICKER opens (not a chat deep-link — the unowned case has no owner to chat with; WARNING 1).
    5. R3 + R4 (the hero): on a blocked-unowned row click [Assign owner] -> pick an agent from the roster -> a toast confirms + the row re-groups (jumps to Working). Then RE-READ that issue (classic UI or API) and confirm `assigneeAgentId` == the picked agent AND the Paperclip audit trail attributes the change to the OPERATOR (not the plugin worker). Also exercise "Take it myself" on another unowned row -> issue shows assigneeUserId + the row reads "with you — handling manually". (The picker MUST NOT list the Editor-Agent — WARNING 2.)
    6. R4: click EACH remaining distinct action once and observe the real effect — [Open chat: <owner>] opens chat scoped to that agent; [Wake] fires requestWakeup; [Assign work] on an idle row opens chat to brief the agent; [Stand down] on a stale row shows a CONFIRM dialog (cancel = no pause; confirm = the agent pauses) and the paused agent stays visible in Idle with a [Resume] that resumes it. Confirm ZERO disabled / no-op buttons anywhere on the surface.
    7. R6: confirm org-backlog + critical-path appear ONLY as a single "+N more blocked issues" expander at the end of Needs-you — no standalone banner, no standalone critical-path strip.
    8. R9: confirm zero raw-UUID leaks in any human-facing string across the drill (agent/owner names, not UUIDs).
    POST-DRILL:
    9. Capture screenshots for each check. If any check FAILS, do NOT close — record the gap and route to a 09-04 gap-closure plan (and execute the now-verified restore if the deploy itself regressed BEAAA). Clean up any throwaway unowned issue seeded in step 2b (delete/close it) OR note it as a leftover in VERIFICATION + SUMMARY (WARNING 4).
    ACCEPTANCE (must ALL hold to pass):
    - (BLOCKER 2) the restore/rollback path was verified working BEFORE deploy (rehearsed restore-to-staging this session OR re-confirmed Phase 1 drill validity) — a snapshot file existing is NOT sufficient.
    - (WARNING 4) during the drill the needs-you banner showed a non-zero UNOWNED count (>=1 real blocked-and-unowned issue present) AND the Assign-owner flow reassigned a real issue, verified by re-reading the issue (new assigneeAgentId == picked agent). The drill must NOT vacuously pass on a zero-unowned roster.
  </how-to-verify>
  <action>Take the mandatory R8 pre-snapshot of BEAAA (safety CLI per the runbook; verify age < 15 min BEFORE reinstall) AND verify the restore/rollback path works (BLOCKER 2 — either a rehearsed restore-to-staging via `node scripts/safety/cli.mjs verify --db-url <from db.env>` with `paperclip_restoring` pre-created, OR re-confirm the Phase 1 restore drill remains valid for this host state; a snapshot file alone is insufficient). Ensure >=1 blocked-and-unowned issue exists on BEAAA before the drill (find or seed a throwaway; WARNING 4). Then deploy the v1.3.0 tarball via DEPLOY-RUNBOOK Path A (pnpm paperclipai plugin install from ~/paperclip; plugin UUID a763176a preserved), confirm the host validator accepts the issues.update capability, then run the Playwright live drill at /BEAAA/situation-room covering the 11 SPEC acceptance checks per the how-to-verify steps. Capture a screenshot per check. Clean up (or note) any seeded throwaway issue post-drill. This is a blocking-human checkpoint: deploy + first-core-issue-mutation reversibility require operator confirmation.</action>
  <verify>Operator confirms 11/11 acceptance checks PASS — including: (BLOCKER 2) a verified-working restore path BEFORE deploy (not just a snapshot file); the R3 re-read (issue assigneeAgentId == picked agent, operator-attributed in the audit trail); the R4 every-distinct-button-performs sweep with zero disabled/no-op buttons; the R1 no-.clarity-agent-grid DOM check; (WARNING 4) the R5 non-zero UNOWNED banner with >=1 real unowned blocker present at drill time AND the Assign-owner flow having reassigned a real issue (re-read confirmed).</verify>
  <done>v1.3.0 live on BEAAA behind a verified snapshot bookend WITH a confirmed-working restore path (BLOCKER 2); >=1 unowned blocker exercised the hero flow (WARNING 4); all 11 acceptance checks PASS (or a gap list captured for a 09-04 gap-closure plan).</done>
  <resume-signal>Type "approved — phase 9 closed" when all 11 checks PASS, or describe the failing check(s).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: File 09-VERIFICATION.md, flip R1-R9 Implemented, update STATE + ROADMAP, closure commit</name>
  <files>.planning/phases/09-situation-room-actionable-cockpit/09-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - the Task 2 drill outcome (the operator's approval + screenshots)
    - .planning/REQUIREMENTS.md (find where R1-R9 status is tracked — note Phase 9 uses SPEC R-IDs, not ROOM-*; record their status there or in the SPEC)
    - .planning/ROADMAP.md Phase 9 block (mark the phase + plans complete)
    - .planning/STATE.md (the latest-shipped section to update to v1.3.0)
    - @~/.claude/get-shit-done/templates/summary.md (the VERIFICATION shape, mirror 08-VERIFICATION.md)
  </read_first>
  <action>
    Write `09-VERIFICATION.md` mapping every R1-R9 + the 11 SPEC acceptance checks to PASS / N-A with the drill evidence (the re-read assigneeAgentId proof for R3, the each-button-performs proof for R4, the no-grid DOM proof for R1, the non-zero banner proof for R5, the BLOCKER-2 verified-restore proof for R8, the WARNING-4 real-unowned-blocker proof, screenshots referenced by filename). Mark R1-R9 Implemented in REQUIREMENTS.md (or the SPEC's acceptance-check list if that is where Phase 9 R-IDs live — match how Phase 8 recorded ROOM-13..18). Update STATE.md: new "Phase 9 CLOSED & VERIFIED LIVE on BEAAA — v1.3.0" section (tarball sha256 + byte size + plugin UUID preserved + 11/11 checks PASS + the verified-restore note + any throwaway-issue cleanup note), bump the milestone progress counters, set next_action. Update ROADMAP.md: flip Phase 9 to `[x]` with the closure note + mark all three plans `[x]`. Make the closure commit (`docs(phase-9): close situation room actionable cockpit (v1.3.0)`) per the gsd commit convention.
  </action>
  <verify>
    <automated>cd "C:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && test -f .planning/phases/09-situation-room-actionable-cockpit/09-VERIFICATION.md && grep -c "assigneeAgentId" .planning/phases/09-situation-room-actionable-cockpit/09-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 09-VERIFICATION.md exists and maps all R1-R9 + the 11 acceptance checks to PASS/N-A with evidence
    - 09-VERIFICATION.md cites the R3 proof (re-read assigneeAgentId == picked agent, operator-attributed), the R4 proof (each distinct button performed), the BLOCKER-2 verified-restore proof, and the WARNING-4 real-unowned-blocker proof
    - REQUIREMENTS.md (or SPEC) shows R1-R9 Implemented
    - STATE.md has a Phase-9-closed v1.3.0 section with the tarball hash + 11/11 PASS + verified-restore note + next_action
    - ROADMAP.md Phase 9 is `[x]` and all three plans are `[x]`
    - closure commit made
  </acceptance_criteria>
  <done>Phase 9 closed on a passing live drill; v1.3.0 recorded; requirements + state + roadmap reflect the close; closure commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build -> live BEAAA install | the v1.3.0 tarball crosses into the live single-tenant instance |
| operator -> situation.assignOwner (live) | the first real core-issue mutation runs against production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Tampering/Availability | BEAAA reinstall | mitigate | bookended-by-snapshots: verified pre-snapshot (age < 15 min) AND a verified-working restore/rollback path (rehearsed restore-to-staging via safety CLI verify with paperclip_restoring pre-created, OR Phase 1 drill re-confirmed valid) confirmed BEFORE reinstall — a snapshot file alone is insufficient (BLOCKER 2); pre-flight gate refuses a stale/unverified snapshot (R8) |
| T-09-09 | Tampering | first live core-issue mutation (assignOwner) | mitigate | the snapshot bookend WITH its rehearsed-and-verified restore makes the assignee write reversible; the worker gate (09-01) re-checks viewer authority + company scope server-side |
| T-09-10 | Repudiation | live assign-owner audit | mitigate | drill RE-READS the issue and confirms the audit trail attributes the change to the operator (actor), not the plugin worker — the R3 acceptance proof |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | no new package-manager runtime installs in this plan (the tarball is the existing build); `pnpm paperclipai plugin install` is the host's own validated install path, not an npm dependency add |
</threat_model>

<verification>
- Both version literals read 1.3.0; full gate set green; tarball packed + hashed
- Host install validator accepts `issues.update`; status=ready version=1.3.0
- Snapshot bookend verified (age < 15 min) AND the restore/rollback path verified working (rehearsed restore-to-staging via `safety cli verify --db-url` with `paperclip_restoring` pre-created, OR Phase 1 drill re-confirmed valid) BEFORE reinstall — a snapshot file alone is insufficient (BLOCKER 2)
- (WARNING 4) >=1 blocked-and-unowned issue present at drill time (found or seeded + cleaned up/noted); the un-frozen banner showed a non-zero UNOWNED count and the Assign-owner flow reassigned a real issue (re-read confirmed) — not a vacuous pass
- Live drill: 11/11 acceptance checks PASS — R3 re-read assigneeAgentId proof, R4 every-button-performs proof, R1 no-grid DOM proof, R5 non-zero banner proof, R9 zero-UUID-leak
- 09-VERIFICATION.md filed; R1-R9 Implemented; STATE + ROADMAP updated; closure commit
</verification>

<success_criteria>
- v1.3.0 deployed to BEAAA behind the mandatory snapshot bookend WITH a verified-working restore path (R8 + BLOCKER 2 — not just a snapshot file)
- The first plugin core-issue mutation proven live: assign owner reassigns the real issue, operator-attributed (R3), exercised against a real unowned blocker present at drill time (WARNING 4)
- Every surfaced button performs; zero dead buttons (R4 — the operator's acceptance spine)
- No agent grid (R1); non-zero banner with unowned blockers (R5); single expander (R6); confirm-on-pause (R7); no UUID leaks (R9)
- Phase 9 closed with VERIFICATION + requirements + state + roadmap updated
</success_criteria>

<output>
Create `.planning/phases/09-situation-room-actionable-cockpit/09-03-SUMMARY.md` when done (or after the checkpoint resolves). If the drill fails, return the gap list for a 09-04 gap-closure plan instead of closing.
</output>
