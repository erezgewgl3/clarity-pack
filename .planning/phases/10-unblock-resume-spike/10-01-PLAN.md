---
phase: 10-unblock-resume-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/spike/unblock-resume-spike-probe.mjs
autonomous: false
requirements: [DO-03]
user_setup:
  - service: BEAAA (AriClaw DO droplet — live Paperclip host)
    why: "D-01 requires the spike target be the real BEAAA host; the dry-confirm and bookend run against it."
    env_vars:
      - name: PAPERCLIP_API_URL
        source: "Tunnel: ssh -L 3100:localhost:3100 ariclaw → http://localhost:3100"
      - name: PAPERCLIP_API_KEY
        source: "Bearer token from auth login --instance-admin; lives at ~/.paperclip/auth.json on the box (redacted from all probe output)"
      - name: PAPERCLIP_COMPANY_ID
        source: "BEAAA company id 59f8876e-e729-4dda-98f9-1317c2b50492"
    dashboard_config:
      - task: "Take a DO-droplet backup immediately before any live action (D-03 bookend; no safety-CLI on BEAAA)"
        location: "DigitalOcean droplet → Backups/Snapshots (operator-run on a LOCAL tunnel+SSH window)"
      - task: "Confirm the plugin-reinstall rollback path once (reinstall prior clarity-pack tarball restores known-good state)"
        location: "On BEAAA: sudo -u beai-agent bash -lc 'cd ~ && npx paperclipai plugin install <prior-tarball>'"

must_haves:
  truths:
    - "D-01: The spike runs against live BEAAA (company 59f8876e-e729-4dda-98f9-1317c2b50492) through the operator tunnel — the real Paperclip host/version, NOT a Countermoves proxy — so every dry-confirm and read-only observation reflects the literal live model."
    - "A committed unblock-resume probe script exists that copies the chat-true-task-spike-probe harness verbatim (REST client, redaction, poll loop, structured JSON) and is adapted to the DO-backup bookend (NOT safety-CLI)."
    - "It is empirically known (A1) whether status:'blocked' is settable on a probe issue via PATCH /api/issues/{id} within the declared issues.update capability — or whether blocked requires a non-empty blockedByIssueIds (which would merge Shape B into Shape C)."
    - "It is empirically known (A3) whether the probe can mint AND delete a dedicated sacrificial agent via REST, or whether the operator must mint one manually and pin it via SPIKE_PROBE_AGENT_ID."
    - "At least one REAL BEAAA blocked item per shape has been observed READ-ONLY (D-02) and the probe-shape construction confirmed to match what BEAAA actually produces (status string, relation shape, awaiting-answer vs status-blocked vs relation-blocked)."
    - "A verified DO-droplet backup was taken and the plugin-reinstall rollback path confirmed once before any write (D-03)."
  artifacts:
    - path: "scripts/spike/unblock-resume-spike-probe.mjs"
      provides: "REST-direct throwaway probe harness for the three blocked shapes + D-08 ladder + three-signal observation"
      contains: "redactedError"
      min_lines: 200
  key_links:
    - from: "scripts/spike/unblock-resume-spike-probe.mjs"
      to: "/api/issues/{id}/comments"
      via: "createComment REST helper (mirrors ctx.issues.createComment, the path Phase 14 Send takes)"
      pattern: "api/issues.*comments"
    - from: "scripts/spike/unblock-resume-spike-probe.mjs"
      to: "/api/issues/{id} (PATCH)"
      via: "updateIssue REST helper (mirrors ctx.issues.update — the A1 status-flip lever)"
      pattern: "PATCH.*api/issues"
---

<objective>
Build the throwaway probe harness for the unblock-resume spike and resolve the two cheap Wave-0 open questions (A1: can status:'blocked' be set; A3: can a sacrificial agent be minted/deleted via REST) plus the D-02 read-only fidelity check, all bookended by the BEAAA DO-droplet backup. This is the foundation for the three-shape run in Plan 10-02 — it produces NO production code and ships nothing.

Purpose: De-risk the full three-shape run before it executes — confirm the probe construction matches real BEAAA blocked items, confirm the blocked-status lever works (or learn it must merge with Shape C), and confirm the agent teardown path. Cheap round-trips resolve the assumptions most likely to reshape Plan 10-02.
Output: `scripts/spike/unblock-resume-spike-probe.mjs` (committed in Plan 10-03) + a recorded dry-confirm result (A1/A3 + D-02 read-only evidence) the operator pastes back into the GSD session.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unblock-resume-spike/10-CONTEXT.md
@.planning/phases/10-unblock-resume-spike/10-RESEARCH.md
@scripts/spike/chat-true-task-spike-probe.mjs
@src/worker/handlers/chat-send.ts
@src/worker/chat/topic-watchdog.ts

<interfaces>
<!-- Reuse these verbatim from chat-true-task-spike-probe.mjs — DO NOT re-derive. -->
<!-- Route shapes VERIFIED LIVE (04-01): issue/agent COLLECTION routes are companyId-scoped; per-issue SUB-routes are FLAT. -->

REST helpers to copy verbatim (chat-true-task-spike-probe.mjs lines 124-256):
  redactedError(err, apiKey)        // scrub bearer token from every error line
  call(method, pathname, body)      // returns { status, body }; only network failures throw
  ok(status), sleep(ms), now(), log(msg, extra)
  asArray(body)                     // normalize array-or-wrapped REST bodies

Endpoint helpers to copy verbatim (lines 198-244):
  listAgents()                      // GET  /api/companies/{C}/agents
  createIssue(payload)              // POST /api/companies/{C}/issues   (companyId-scoped collection)
  getIssue(id)                      // GET  /api/issues/{id}            (flat sub-route)
  updateIssue(id, patch)            // PATCH /api/issues/{id}           (the A1 status lever — ctx.issues.update)
  listComments(id)                  // GET  /api/issues/{id}/comments   (behavioral signal source)
  createComment(id, body)           // POST /api/issues/{id}/comments   (the resume trigger; = ctx.issues.createComment)
  requestWakeup(id, opts)           // POST /api/issues/{id}/wakeup     (D-08 rung 2; EXPECTED 404 on BEAAA)
  listIssues(query)                 // GET  /api/companies/{C}/issues?... (for D-02 read-only scan)
  pollForNewComment(id, seenIds, windowMs)  // bounded new-comment waiter

Env config (copy lines 77-92):
  PAPERCLIP_API_URL / PAPERCLIP_API_KEY / PAPERCLIP_COMPANY_ID
  SPIKE_PROBE_AGENT_ID (NEW — pins the sacrificial agent if A3 says operator must mint manually)

Declared-capability boundary (D-09 — confirmed against src/manifest.ts lines 649-720):
  DECLARED: issue.comments.create, issues.wakeup, issues.update, agents.pause,
            agents.resume, agents.managed, agents.read, issue.relations.read
  NOT DECLARED: issue.relations.write   ← D-10: do NOT use; document the edge-clear path as spec'd-not-proven
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the unblock-resume probe harness skeleton (copy chat-true-task harness verbatim, adapt header to BEAAA bookend)</name>
  <files>scripts/spike/unblock-resume-spike-probe.mjs</files>
  <read_first>
    - scripts/spike/chat-true-task-spike-probe.mjs (the analog harness to copy — REST client lines 124-256, env config lines 77-92, main() structure lines 1110-1191)
    - .planning/phases/10-unblock-resume-spike/10-RESEARCH.md (Pattern 1 REST-direct harness; "Don't Hand-Roll" table; Wave 0 Gaps; State of the Art — the safety-CLI→DO-backup bookend swap)
    - src/worker/chat/topic-watchdog.ts (TERMINAL_OR_BLOCKED_STATUSES = {done,cancelled,blocked}; the CTT-07 hotfix — the spike must determine whether blocked needs an explicit flip)
    - MEMORY.md → beaaa-deploy-mechanics.md (BEAAA reach: ssh ariclaw→root, runs as beai-agent, DO-backup bookend NOT safety-CLI, fail2ban, company id 59f8876e-…)
  </read_first>
  <action>
    Create scripts/spike/unblock-resume-spike-probe.mjs. Copy the REST client block (redactedError, call, ok, sleep, now, log, asArray) and the endpoint helpers (listAgents, createIssue, getIssue, updateIssue, listComments, createComment, requestWakeup, listIssues, pollForNewComment) VERBATIM from chat-true-task-spike-probe.mjs — point to <read_first> for the bodies; do not re-invent them. Add a SPIKE_PROBE_AGENT_ID env pin alongside the existing PAPERCLIP_API_URL/API_KEY/COMPANY_ID. Set the SPIKE_TAG constant to '[SPIKE 10]'. Replace the USAGE header comment block: it MUST reference the BEAAA DO-droplet backup bookend (operator takes a DO snapshot + confirms plugin-reinstall rollback once on a LOCAL tunnel+SSH window) and MUST NOT reference scripts/safety/cli.mjs snapshot/gate (safety-CLI is a Countermoves-only inheritance, absent on BEAAA per the deploy mechanics). State in the header that the probe runs OUTSIDE the worker (no ctx), uses REST-direct bearer auth, redacts the token from all output, and that issue/agent COLLECTION routes are companyId-scoped while per-issue SUB-routes are flat. Leave the three-shape probe functions and main() orchestration as named stubs (probeShapeA/probeShapeB/probeShapeC + dryConfirmA1A3 + observeRealBlockedItems) returning a finding object shape — they are filled in by Plan 10-02's tasks; this task only lands the harness + stubs so the file lints clean as an .mjs.
    No fenced code in this plan — the harness body is in <read_first>.
  </action>
  <acceptance_criteria>
    - File scripts/spike/unblock-resume-spike-probe.mjs exists and is a syntactically valid ESM module (parses under `node --check`).
    - redactedError, call, asArray, pollForNewComment, and all nine endpoint helpers are present and identical in behavior to the chat-true-task analog (the bearer token is never echoed un-redacted).
    - SPIKE_TAG === '[SPIKE 10]'; SPIKE_PROBE_AGENT_ID env var is read.
    - The USAGE header references the DO-droplet backup bookend and contains NO reference to scripts/safety/cli.mjs, `snapshot`, or `gate` (grep for 'safety/cli' returns zero non-comment hits; grep for 'DO-droplet' or 'DO backup' returns ≥1).
    - The file imports no plugin code, adds no npm dependency, and uses only Node built-in fetch.
  </acceptance_criteria>
  <verify>
    <automated>node --check "scripts/spike/unblock-resume-spike-probe.mjs"</automated>
  </verify>
  <done>The probe harness file exists, parses clean as ESM, carries the [SPIKE 10] tag + BEAAA DO-backup bookend header, copies the proven REST helpers verbatim, and stubs the three-shape + dry-confirm functions for Plan 10-02 to fill.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Operator bookend — DO-droplet backup + rollback verify before any live write (D-03)</name>
  <read_first>
    - MEMORY.md → beaaa-deploy-mechanics.md (DO-backup is the bookend; NO safety-CLI on BEAAA; Path A scp vs Path B push-master-flip-public; fail2ban banner-exchange guard)
    - .planning/phases/10-unblock-resume-spike/10-RESEARCH.md (Environment Availability table — DO-droplet backup + plugin-reinstall rollback; no fallback gap)
    - CLAUDE.md (bookended-by-snapshots rule — every live BEAAA action bookended by a verified backup + a rollback path verified at least once)
  </read_first>
  <what-built>The probe harness (Task 1) is ready to run against live BEAAA. Before ANY write to the live host, the standard BEAAA bookend must be in place: a DO-droplet backup taken immediately before, and the plugin-reinstall rollback path verified once. This checkpoint gates every subsequent live action in this phase.</what-built>
  <how-to-verify>
    1. On a LOCAL window, open the tunnel: `ssh -L 3100:localhost:3100 ariclaw` (key beaaa_ariclaw_ed25519; minimize repeat SSH to avoid fail2ban banner timeouts — wait 15-30 min or use the DO Web Console if banned).
    2. Take a DO-droplet backup/snapshot of the AriClaw droplet via the DigitalOcean console and record its id/timestamp.
    3. Verify the rollback path ONCE: confirm the prior clarity-pack tarball is reachable and that `sudo -u beai-agent bash -lc 'cd ~ && npx paperclipai plugin list'` shows the current install (so a reinstall would restore known-good). Do NOT actually roll back now — just confirm the path is real.
    4. Mint a bearer token: `auth login --instance-admin` (token lands at ~/.paperclip/auth.json). Do NOT paste the token into the session.
    5. Record: DO-backup id + timestamp, rollback-path confirmation, and that PAPERCLIP_API_URL=http://localhost:3100 / COMPANY_ID=59f8876e-e729-4dda-98f9-1317c2b50492 are set for the run.
  </how-to-verify>
  <acceptance_criteria>
    - A DO-droplet backup id + timestamp is recorded, taken immediately before the dry-confirm run.
    - The plugin-reinstall rollback path is confirmed reachable once (prior tarball present; plugin list returns the current install via the beai-agent user, NOT cd ~/paperclip as root which short-circuits to an empty list).
    - A bearer token exists at ~/.paperclip/auth.json and is NOT pasted into the GSD session.
    - The tunnel is open and reachable at http://localhost:3100 for the BEAAA company.
  </acceptance_criteria>
  <resume-signal>Type "bookend verified — DO-backup id <id> @ <ts>, rollback path confirmed" or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Dry-confirm A1 + A3 and D-02 read-only fidelity against live BEAAA</name>
  <files>scripts/spike/unblock-resume-spike-probe.mjs</files>
  <read_first>
    - scripts/spike/unblock-resume-spike-probe.mjs (the harness from Task 1 — fill the dryConfirmA1A3 + observeRealBlockedItems stubs)
    - .planning/phases/10-unblock-resume-spike/10-RESEARCH.md (Assumptions Log A1/A3/A5; Open Questions 1 & 2; Pitfall 6 wrong-terminal; Pattern 2 shape-construction table)
    - .planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md (the JSON summary shape + per-probe finding/verdictHint pattern to mirror)
  </read_first>
  <action>
    Fill the dryConfirmA1A3 stub: (A1) create one [SPIKE 10]-tagged throwaway issue at status:'in_progress' (createIssue), re-GET it (CAS guard — Pitfall 4), then PATCH status:'blocked' via updateIssue and re-GET to confirm whether the host accepts the bare status flip (record httpStatus + the re-read status). If the flip is rejected because blocked requires a non-empty blockedByIssueIds, record that the host rule merges Shape B into Shape C (Assumption A1 mitigation). (A3) attempt to mint a sacrificial agent via the REST agent-create route and then delete it; record whether create+delete succeed via bearer-token REST, or whether the operator must mint manually (then the probe pins SPIKE_PROBE_AGENT_ID). Fill observeRealBlockedItems: via listIssues, scan for REAL BEAAA blocked items READ-ONLY (D-02 — never write to them) and record, per shape, one real example's status string + relation shape so Plan 10-02's construction is confirmed to match (Pitfall 6 / Open Question 2). Tear down the A1 throwaway issue and any A3 test agent at the end of this dry run (delete by id, or note for the bookend rollback). main() runs ONLY dryConfirmA1A3 + observeRealBlockedItems for this plan — the three-shape probes are not yet wired. Print the single structured JSON summary on stdout; the operator pastes it back into the session. Run via the tunnel: `PAPERCLIP_API_URL=http://localhost:3100 PAPERCLIP_API_KEY=<token> PAPERCLIP_COMPANY_ID=59f8876e-e729-4dda-98f9-1317c2b50492 node scripts/spike/unblock-resume-spike-probe.mjs > 10-01-dryconfirm-output.txt 2>&1`.
  </action>
  <acceptance_criteria>
    - A1 RESULT recorded: whether PATCH /api/issues/{id} {status:'blocked'} is accepted on BEAAA via the declared issues.update capability, read back from a fresh GET (not the PATCH response alone — Pitfall 4 CAS guard). If rejected, the exact host rule (e.g. "blocked requires non-empty blockedByIssueIds") is captured verbatim and the Shape-B-merges-into-C implication noted.
    - A3 RESULT recorded: whether the probe can mint AND delete a sacrificial agent via REST; if not, the manual-mint fallback (operator mints, probe pins SPIKE_PROBE_AGENT_ID) is documented.
    - D-02 read-only evidence recorded: for each of Shape A (awaiting-answer), Shape B (status='blocked'), Shape C (blockedByIssueIds relation), either one REAL BEAAA blocked item matching that shape is captured (status string + relation shape, READ ONLY — no write), OR it is explicitly recorded that BEAAA produces no real items of that shape (e.g. real blocks are all in_progress-awaiting, making Shape B academic — Open Question 2).
    - NO real in-flight blocked item was written to (every write targets only a [SPIKE 10]-tagged probe issue / sacrificial agent — D-02).
    - The A1 throwaway issue and any A3 test agent were torn down (deleted) OR explicitly listed for the bookend rollback; zero un-tagged residue.
    - The structured JSON summary printed to stdout redacts the bearer token (no token substring appears in 10-01-dryconfirm-output.txt).
  </acceptance_criteria>
  <verify>
    <automated>node --check "scripts/spike/unblock-resume-spike-probe.mjs"</automated>
    <human-check>Operator confirms the dry-confirm JSON shows the A1 result, the A3 result, and per-shape D-02 read-only evidence (or explicit "shape not present on BEAAA"), with zero residue left and the token redacted.</human-check>
  </verify>
  <done>A1 (blocked settable?), A3 (agent mint/delete?), and the D-02 per-shape read-only fidelity are all answered against live BEAAA, recorded in a redacted JSON summary, with the probe construction for Plan 10-02 confirmed to match real BEAAA items and zero untagged residue left behind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local probe → live BEAAA REST (bearer) | Untrusted-from-host writes cross here; the probe is operator-run trusted code but acts on a LIVE production board. |
| probe writes → public.issues / public.issue_comments | Mutations land on real host tables; must be confined to [SPIKE 10]-tagged throwaway rows only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | dry-confirm writes to a REAL in-flight blocked item | mitigate | D-02 absolute — every write targets a [SPIKE 10]-tagged throwaway issue / sacrificial agent; real items observed READ-ONLY via listIssues only. Acceptance criteria assert zero real-item writes. |
| T-10-02 | Information disclosure | bearer token leaks into 10-01-dryconfirm-output.txt or commit | mitigate | redactedError scrubs the token from every line; output file reviewed before commit; token never pasted into the session, never committed. |
| T-10-03 | (operational) un-rolled-back mutation / orphan residue on the live board | mitigate | D-03 DO-droplet backup taken before any write (Task 2); A1 throwaway + A3 test agent torn down at end of Task 3; [SPIKE 10] tags make residue greppable + deletable; rollback path verified once. |
| T-10-04 | Elevation of privilege | sacrificial agent minted with special privileges | mitigate | Governance parity — the probe agent is a standard hire under normal caps/pause/terminate (Success Criterion 3); no plugin-private mechanism. |
| T-10-05 | (governance) capability-boundary creep | mitigate | D-09 — only declared caps used (issue.comments.create, issues.update, issues.wakeup, agents.*); issue.relations.write NOT used (D-10). The harness never calls a relations-write route. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this phase installs ZERO packages (Node built-in fetch only; no npm install). No package legitimacy gate required. |
</threat_model>

<verification>
- `node --check scripts/spike/unblock-resume-spike-probe.mjs` passes (valid ESM, no plugin imports, no deps).
- The dry-confirm JSON summary records A1, A3, and per-shape D-02 read-only evidence, token redacted.
- DO-droplet backup id + timestamp recorded; rollback path verified once before any write.
- Zero untagged residue on the live board after the dry run.
</verification>

<success_criteria>
- Probe harness committed-ready (copies the proven REST helpers verbatim; BEAAA DO-backup bookend header; [SPIKE 10] tag).
- A1 + A3 resolved against live BEAAA; the Shape-B-vs-Shape-C construction question for Plan 10-02 is settled.
- D-02 read-only fidelity confirmed per shape (or shape explicitly recorded as absent on BEAAA).
- No real in-flight item written to; no schema touched (Success Criterion 3); only declared caps used (D-09/D-10).
</success_criteria>

<output>
Create `.planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md` when done, recording the A1/A3/D-02 dry-confirm results (these feed Plan 10-02's shape construction) and the DO-backup bookend id.
</output>
</output>
