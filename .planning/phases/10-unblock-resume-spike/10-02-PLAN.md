---
phase: 10-unblock-resume-spike
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - scripts/spike/unblock-resume-spike-probe.mjs
autonomous: false
requirements: [DO-03]
user_setup:
  - service: BEAAA (AriClaw DO droplet — live Paperclip host)
    why: "D-01 — the three-shape run executes against the real BEAAA host through the operator's tunnel; the same DO-backup bookend from Plan 10-01 governs it."
    env_vars:
      - name: PAPERCLIP_API_URL
        source: "Tunnel: ssh -L 3100:localhost:3100 ariclaw → http://localhost:3100"
      - name: PAPERCLIP_API_KEY
        source: "Bearer token from ~/.paperclip/auth.json (redacted from all probe output)"
      - name: PAPERCLIP_COMPANY_ID
        source: "BEAAA company id 59f8876e-e729-4dda-98f9-1317c2b50492"
      - name: SPIKE_PROBE_AGENT_ID
        source: "The sacrificial probe agent id (REST-minted in Plan 10-01, or operator-minted per A3)"

must_haves:
  truths:
    - "Shape A (awaiting-answer): a comment-alone resume was attempted and judged by the three-signal rule (behavioral + consumption + state); the minimal working recipe (or a FAIL with evidence) is recorded."
    - "Shape B (status='blocked'): comment-alone was attempted first; if it failed the three-signal test, the D-08 ladder was climbed one rung at a time (issues.update({status:'in_progress'}) → requestWakeup → agents.resume/resumeHeartbeat) until the MINIMAL passing recipe was locked, with the exact required transition + ordering (flip-before-comment vs flip-after) recorded."
    - "Shape C (blockedByIssueIds): the cascade-on-answer hypothesis was tested within declared caps (answer/resolve the blocker issue X, observe whether Y resumes WITHOUT touching Y's relation); if Y only resumes after edge-clear, that path is recorded as spec'd-not-proven (the exact transition + the issue.relations.write capability it needs + its CTT-07/governance cost) — NOT exercised (D-10)."
    - "Every PASS is corroborated by all three D-07 signals; a state-only flip with no fresh agent run is recorded as PARTIAL (host disposition-recovery, not agent resume), never PASS."
    - "Landmine guards are observed: requestWakeup 404 from REST recorded as EXPECTED (not a FAIL); each status PATCH preceded by a fresh GET (CAS guard); FAIL declared only after the realistic window (8-min first reply / 4-min re-wake), not after 60s."
    - "All probe agents + probe issues were torn down after the run (zero [SPIKE 10] residue); no real in-flight item written to; no schema touched."
  artifacts:
    - path: "scripts/spike/unblock-resume-spike-probe.mjs"
      provides: "Filled probeShapeA/B/C functions + the D-08 ladder + three-signal observation; one structured JSON summary on stdout"
      contains: "[SPIKE 10]"
      min_lines: 400
  key_links:
    - from: "probeShapeB"
      to: "updateIssue(id, {status:'in_progress'})"
      via: "D-08 ladder rung 1 — un-terminal a blocked issue (ctx.issues.update, declared)"
      pattern: "status.*in_progress"
    - from: "probeShapeC"
      to: "blockedByIssueIds at createIssue"
      via: "establishes the dependency edge legally at create time (no relations.write)"
      pattern: "blockedByIssueIds"
    - from: "three-signal observation"
      to: "createdByRunId on the agent reply comment"
      via: "consumption-signal proxy — a FRESH run id after the reply proves a new run picked up the issue"
      pattern: "createdByRunId"
---

<objective>
Execute the three-shape live run against BEAAA: drive a dedicated sacrificial probe agent into each blocked shape (A awaiting-answer, B status='blocked', C blockedByIssueIds relation), attempt comment-alone resume first, and climb the D-08 fallback ladder one minimal rung at a time where comment-alone fails — judging every shape by the three-signal PASS (behavioral + consumption + state). Produce one structured JSON summary that Plan 10-03 turns into the findings contract. A clean negative recorded honestly is a SUCCESSFUL outcome — do NOT strain toward a manufactured PASS.

Purpose: Answer the make-or-break DO-03 question per shape — does answering an agent actually unblock and resume it, or is a status/blocker transition also required, and (for Shape B) exactly which transition and in what order? Phase 14 implements the locked recipe; it must receive an executable recipe, not a yes/no.
Output: A redacted JSON probe summary (pasted back into the session) capturing, per shape, the comment-write path, any required accompanying transition, the heartbeat-consumption timing, the three-signal evidence, and a PASS/PARTIAL/FAIL verdict.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unblock-resume-spike/10-CONTEXT.md
@.planning/phases/10-unblock-resume-spike/10-RESEARCH.md
@.planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md
@scripts/spike/unblock-resume-spike-probe.mjs
@.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
@src/worker/chat/topic-watchdog.ts

<interfaces>
<!-- The D-08 transition ladder (RESEARCH Pattern 3) — climb minimal-first, re-observe after each rung, lock the FIRST rung where all three signals fire. All rungs use DECLARED caps. -->
  Rung 0: comment alone                          (issue.comments.create)  — Shape A proven baseline
  Rung 1: + issues.update({status:'in_progress'})(issues.update)          — un-terminal blocked (Shape B likely fix)
  Rung 2: + requestWakeup(reason, idempotencyKey)(issues.wakeup)          — EXPECTED 404 from REST; fire-and-forget, never block on it
  Rung 3: + agents.resume / resumeHeartbeat       (agents.resume)         — ONLY if the AGENT itself is paused (check agents.get(id).status first)

<!-- The three D-07 signals — ALL THREE required for PASS (RESEARCH Pattern 4) -->
  Behavioral  = NEW comment with authorType:'agent' (or authorAgentId===probeAgentId) not in seenIds   [GET /api/issues/{id}/comments]
  Consumption = a FRESH createdByRunId on that reply (differs from the run that produced the question)  [createdByRunId on the new comment]
  State       = issue status off blocked/awaiting                                                       [GET /api/issues/{id}]
  Windows: FIRST_REPLY_WINDOW_MS = 8*60_000 ; RE_WAKE_WINDOW_MS = 4*60_000 ; POLL_INTERVAL_MS = 30_000

<!-- Shape construction (RESEARCH Pattern 2) — confirmed against the Plan 10-01 D-02 read-only evidence -->
  Shape A: createIssue status:'in_progress', assigneeAgentId=PROBE; comment poses a question + REPLY_CHANNEL_INSTRUCTION; let it run + park.
  Shape B: createIssue + assign; updateIssue {status:'blocked'}; re-GET (CAS); then comment-alone; ladder if it fails.
  Shape C: createIssue blockerX; createIssue blockedY with blockedByIssueIds:[X] AT CREATE (legal, no relations.write) + assign Y to PROBE; answer/resolve X; observe Y.

<!-- D-09/D-10 boundary — issue.relations.write is NOT declared; the edge-clear path for Shape C is documented spec'd-not-proven, NEVER exercised. -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Re-confirm the bookend is current for the three-shape run (D-03)</name>
  <read_first>
    - .planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md (the DO-backup id + rollback confirmation from Plan 10-01)
    - MEMORY.md → beaaa-deploy-mechanics.md (DO-backup bookend; fail2ban; beai-agent user)
    - CLAUDE.md (bookended-by-snapshots rule)
  </read_first>
  <what-built>Plan 10-01 took a DO-droplet backup and verified the rollback path. The three-shape run is a fresh batch of live writes (more probe issues + agent runs); the operator confirms a current DO-backup is in place and the tunnel is open before this run begins.</what-built>
  <how-to-verify>
    1. Confirm a DO-droplet backup is current (taken since/for this run window); record id + timestamp.
    2. Confirm the tunnel `ssh -L 3100:localhost:3100 ariclaw` is open and http://localhost:3100 reaches the BEAAA company.
    3. Confirm SPIKE_PROBE_AGENT_ID resolves to the sacrificial agent from Plan 10-01 (NOT the Editor-Agent, NOT a real hire).
    4. Confirm the bearer token is fresh at ~/.paperclip/auth.json (not pasted into the session).
  </how-to-verify>
  <acceptance_criteria>
    - A current DO-droplet backup id + timestamp is recorded for this run window.
    - The tunnel reaches the live BEAAA company at http://localhost:3100.
    - SPIKE_PROBE_AGENT_ID is the sacrificial probe agent (confirmed NOT Editor-Agent, NOT a real hire).
  </acceptance_criteria>
  <resume-signal>Type "bookend current — DO-backup id <id> @ <ts>, probe agent <id>, tunnel up" or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Implement Shape A + Shape B probes with the D-08 ladder and three-signal observation</name>
  <files>scripts/spike/unblock-resume-spike-probe.mjs</files>
  <read_first>
    - scripts/spike/unblock-resume-spike-probe.mjs (fill the probeShapeA + probeShapeB stubs landed in Plan 10-01)
    - .planning/phases/10-unblock-resume-spike/10-RESEARCH.md (Pattern 2 shape construction; Pattern 3 D-08 ladder; Pattern 4 three-signal map; Pitfalls 1/2/3/4/5; "Construct Shape B" code example)
    - .planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md (A1 result — whether status:'blocked' is settable bare or merges into Shape C; D-02 Shape A/B real-item evidence)
    - .planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md (the proven Shape-A baseline: native multi-turn wake on in_progress, no requestWakeup; the captured disposition-recovery system_notice — the false-pass case D-07 guards against)
    - src/worker/chat/topic-watchdog.ts (CTT-07: plugin avoids mutating public.issues, relies on host disposition-recovery — the spike must determine empirically if blocked needs the explicit flip)
  </read_first>
  <action>
    Fill probeShapeA: create a [SPIKE 10] issue status:'in_progress' assigned to SPIKE_PROBE_AGENT_ID; post a comment that poses a question/decision plus the REPLY_CHANNEL_INSTRUCTION; let the agent run and ask, then post the answer comment and run the three-signal observation (pollForNewComment for behavioral within FIRST_REPLY_WINDOW_MS=8min; createdByRunId freshness for consumption; getIssue status poll for state). Record PASS only if all three fire; record PARTIAL if state flipped but no fresh agent run (disposition-recovery false-pass — Pitfall 2); FAIL only after the full window. Fill probeShapeB per the A1 result from 10-01: create + assign, then drive into blocked (updateIssue {status:'blocked'} if A1 said bare flip works; else set a blocker edge too and record the merge). Re-GET before AND after every transition (Pitfall 4 CAS guard — record pre/post from re-reads, not the PATCH response). Attempt comment-alone first; if not all three signals within the window, climb the D-08 ladder one rung at a time: rung 1 updateIssue {status:'in_progress'} (test BOTH orderings — flip-before-comment and flip-after — and record which works); rung 2 requestWakeup (record the HTTP status verbatim; a 404 from REST is EXPECTED and is NOT a FAIL — Pitfall 3, fire-and-forget); rung 3 agents.resume/resumeHeartbeat ONLY if agents.get(PROBE).status shows the agent is paused (check first — Pitfall 6/A6; an issue being blocked ≠ agent paused). Lock the MINIMAL passing rung as the recipe and record the full ladder (which rungs failed) so Plan 14 understands why. If the agent never runs at all, check for a budget/invocation-block incident before declaring FAIL (Pitfall 5). Each probe returns a finding object with: shape, construction steps, the rung locked, the three-signal evidence (behavioral commentId, consumption createdByRunId, state transition list), and a PASS/PARTIAL/FAIL verdictHint. Do NOT inline implementation as fenced code — mirror the 04.1 probe function structure referenced in <read_first>.
  </action>
  <acceptance_criteria>
    - probeShapeA judges resume by all three D-07 signals; a state-only flip with no fresh createdByRunId is recorded as PARTIAL (disposition-recovery), never PASS.
    - probeShapeB attempts comment-alone first, then climbs the D-08 ladder minimal-first; the LOCKED recipe is the first rung where all three signals fire; the full ladder (failed rungs) is recorded.
    - For Shape B rung 1, BOTH transition orderings (flip-before-comment, flip-after-comment) are exercised and the working ordering is recorded explicitly (Phase 14's recipe depends on it).
    - Every status PATCH is preceded by a fresh GET and the pre/post status is read back from a re-read, not the PATCH response (Pitfall 4 CAS guard).
    - A requestWakeup 404 from REST is recorded verbatim as EXPECTED and does NOT flip the shape to FAIL (Pitfall 3).
    - Rung 3 (agents.resume) is attempted ONLY when agents.get(PROBE).status shows the agent paused; otherwise it is recorded as not-applicable (Pitfall 6/A6).
    - FAIL is declared only after the full FIRST_REPLY_WINDOW_MS / RE_WAKE_WINDOW_MS, not after a 60s poll; a no-run case checks for a budget/invocation-block incident first (Pitfall 5).
    - `node --check` passes on the modified probe.
  </acceptance_criteria>
  <verify>
    <automated>node --check "scripts/spike/unblock-resume-spike-probe.mjs"</automated>
  </verify>
  <done>probeShapeA and probeShapeB are implemented with three-signal judging, the D-08 minimal-first ladder, CAS-guarded transitions, the requestWakeup-404-is-expected guard, the paused-vs-blocked distinction, and the realistic windows — each returning a structured finding with a PASS/PARTIAL/FAIL verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Shape C (cascade-on-answer, D-10 bounded) and run all three shapes live; capture the summary; tear down</name>
  <files>scripts/spike/unblock-resume-spike-probe.mjs</files>
  <read_first>
    - scripts/spike/unblock-resume-spike-probe.mjs (fill probeShapeC + wire main() to run A/B/C; add teardown)
    - .planning/phases/10-unblock-resume-spike/10-RESEARCH.md (Shape C row in Pattern 2; "Establish Shape C's blockedByIssueIds edge at create time" code example; D-10 spec'd-not-proven; Open Question 3 cascade hypothesis; Runtime State Inventory teardown)
    - .planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md (A3 — REST agent delete or operator-manual teardown; D-02 Shape C real-item relation shape)
    - src/manifest.ts lines 649-720 (declared caps — confirm issue.relations.write is ABSENT; the edge-clear path is documented, never called)
  </read_first>
  <action>
    Fill probeShapeC: createIssue a blocker X [SPIKE 10]; createIssue a blocked Y [SPIKE 10] with blockedByIssueIds:[X] set AT CREATE (legal via issues.create — no relations.write) and assigneeAgentId=PROBE. Test the cascade hypothesis (D-10, Open Question 3): answer/resolve X — post a resolving comment on X and/or flip X to 'done' via updateIssue — then run the three-signal observation on Y to see whether Y resumes WITHOUT touching Y's relation. If Y resumes via cascade, lock that as the Shape C recipe. If Y stays blocked, record the edge-clear path as SPEC'D-NOT-PROVEN: the exact transition (relations.removeBlockers / setBlockedBy([])), the capability it needs (issue.relations.write — NOT declared), and its CTT-07/governance cost — and do NOT call any relations-write route (D-10). Wire main() to run probeShapeA, probeShapeB, probeShapeC in order, thread shared state, and print ONE structured JSON summary (mirror the 04.1 summary shape: { probe, phase:'10-unblock-resume-spike', plan:'10-02', startedAt, findings:{shapeA,shapeB,shapeC}, spikeIssues, finishedAt }) with the bearer token redacted. Add an explicit TEARDOWN step at the end: delete every [SPIKE 10] probe issue created (X, Y, Shape A/B issues) and terminate/delete the sacrificial probe agent (per the A3 path from 10-01 — REST delete or note for operator/rollback). Run the full pass through the tunnel: `PAPERCLIP_API_URL=http://localhost:3100 PAPERCLIP_API_KEY=<token> PAPERCLIP_COMPANY_ID=59f8876e-e729-4dda-98f9-1317c2b50492 SPIKE_PROBE_AGENT_ID=<id> node scripts/spike/unblock-resume-spike-probe.mjs > 10-02-probe-output.txt 2>&1`. The operator pastes the JSON back into the session for Plan 10-03.
  </action>
  <acceptance_criteria>
    - probeShapeC establishes the blockedByIssueIds edge AT CREATE (issues.create only — grep confirms no relations-write route is called anywhere in the file).
    - The cascade-on-answer hypothesis is tested within declared caps: X is answered/resolved and Y is observed; the result (cascade resumes Y, or Y stays blocked) is recorded with three-signal evidence.
    - If Y stays blocked, the edge-clear path is recorded as SPEC'D-NOT-PROVEN with the exact transition + the issue.relations.write capability + the CTT-07/governance cost — and issue.relations.write is NEVER called (D-10).
    - main() runs all three shapes and prints ONE structured JSON summary with the token redacted (no token substring in 10-02-probe-output.txt).
    - TEARDOWN deleted every [SPIKE 10] probe issue and terminated/deleted the sacrificial agent (or the residue is explicitly listed for the bookend rollback); a post-run listIssues scan for [SPIKE 10] returns zero (or the rollback was executed).
    - No real in-flight blocked item was written to (D-02); no migration/schema touched (Success Criterion 3); only declared caps used (D-09/D-10).
    - `node --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>node --check "scripts/spike/unblock-resume-spike-probe.mjs"</automated>
    <human-check>Operator confirms the full-run JSON summary contains shapeA/shapeB/shapeC findings each with a three-signal verdict, the Shape C cascade result (or spec'd-not-proven edge-clear note), token redacted, and that the post-run [SPIKE 10] scan is empty (or rollback executed).</human-check>
  </verify>
  <done>All three shapes are implemented and run live; Shape C tests the cascade hypothesis within the declared boundary and documents (never exercises) the edge-clear path; one redacted JSON summary captures the per-shape three-signal verdicts; all probe residue is torn down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local probe → live BEAAA REST (bearer) | Live production writes cross here; confined to [SPIKE 10] throwaways. |
| probe status PATCH → host CAS-guarded lifecycle | A stale flip can lose the CAS (PR #4738); transitions must be computed from a fresh read. |
| probe → host dispatcher (agent resume) | The host owns whether/when a parked agent runs; the plugin can only nudge + observe — false-pass risk if state flips without a real run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | probe writes to a REAL in-flight blocked item during the three-shape run | mitigate | D-02 absolute — only [SPIKE 10]-tagged probe issues + the sacrificial agent are written; real items observed read-only in Plan 10-01. Acceptance asserts zero real-item writes. |
| T-10-07 | (false verdict) state flips without the agent resuming → false PASS | mitigate | D-07 three-signal rule — behavioral + consumption (fresh createdByRunId) required alongside state; a state-only flip is recorded PARTIAL (host disposition-recovery), never PASS (Pitfall 2). |
| T-10-08 | Tampering | a CAS-lost status flip silently no-ops, corrupting the verdict | mitigate | Re-GET immediately before each transition; read pre/post status from a re-read, not the PATCH response; retry once with current status if a flip no-ops (Pitfall 4). |
| T-10-09 | (governance) Shape B status flip violates CTT-07 (plugin mutating public.issues) | accept-with-flag | The issues.update flip is operator-attributed, bounded to [SPIKE 10] probe issues, and host-audit-logged. If Shape B REQUIRES the flip, the findings doc (10-03) flags it as a deliberate CTT-07 exception Phase 14 must own — not a silent default. |
| T-10-10 | Elevation of privilege | Shape C edge-clear via undeclared issue.relations.write | mitigate | D-10 — issue.relations.write is NOT called; the edge-clear path is documented spec'd-not-proven only. Acceptance greps the file for any relations-write route (must be zero). |
| T-10-11 | Information disclosure | bearer token leaks into 10-02-probe-output.txt or commit | mitigate | redactedError scrubs the token from every line; output reviewed before commit; token never committed. |
| T-10-12 | (operational) probe residue (orphan agent consuming budget/heartbeats, orphan issues) | mitigate | Explicit teardown deletes all [SPIKE 10] issues + the sacrificial agent; post-run [SPIKE 10] scan must be empty; DO-backup bookend is the rollback net (D-03). |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (Node built-in fetch only). No package legitimacy gate required. |
</threat_model>

<verification>
- `node --check scripts/spike/unblock-resume-spike-probe.mjs` passes.
- The full-run JSON summary records shapeA/shapeB/shapeC findings, each with a three-signal PASS/PARTIAL/FAIL verdict and the locked recipe (or honest FAIL/spec'd-not-proven).
- requestWakeup 404 recorded as expected; every PATCH CAS-guarded; FAIL only after the realistic window.
- Post-run [SPIKE 10] scan empty (or rollback executed); no real-item writes; no schema touched; no relations-write call.
</verification>

<success_criteria>
- Per shape, it is proven whether comment-alone resumes the agent or a transition is required, with the exact required transition + ordering documented (Success Criterion 1).
- The three-signal rule prevented any false PASS; a clean negative/PARTIAL is recorded honestly (Success Criterion 4).
- Governance parity preserved; no non-additive schema; declared caps only; Shape C edge-clear documented not exercised (Success Criterion 3, D-10).
</success_criteria>

<output>
Create `.planning/phases/10-unblock-resume-spike/10-02-SUMMARY.md` when done, embedding the redacted three-shape JSON summary (the raw input Plan 10-03 writes the findings contract from) and noting teardown completion.
</output>
