---
phase: 10-unblock-resume-spike
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - .planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md
  - scripts/spike/unblock-resume-spike-probe.mjs
autonomous: true
requirements: [DO-03]

must_haves:
  truths:
    - "A 10-03-SPIKE-FINDINGS.md exists in the phase dir capturing, per shape (A/B/C): the comment-write path used, any required accompanying state transition, when/whether the agent consumes it on heartbeat, the observed three-signal evidence, and a PASS/PARTIAL/FAIL verdict with its DO-03 scope implication."
    - "The findings doc states a single explicit DO-03 gate verdict (GO / GO-WITH-CONSTRAINT / NO-GO-FOR-SHAPE) and the locked turnkey recipe(s) Phase 14 implements against — or, for any shape that failed/partialed, the honest negative and its scope implication (which is itself a SUCCESSFUL spike outcome per Success Criterion 4)."
    - "The doc distinguishes REST-direct probe results from the in-worker ctx path Phase 14 will use, flagging any transition (e.g. requestWakeup, or a Shape B status flip) that Phase 14 must re-confirm in-worker."
    - "For Shape C, the doc records either the proven cascade-on-answer recipe (within declared caps) or the spec'd-not-proven edge-clear path (exact transition + issue.relations.write capability + CTT-07/governance cost) as the deliberate deploy decision left to Phase 14 planning (D-10)."
    - "The committed probe script (scripts/spike/unblock-resume-spike-probe.mjs) is the auditable artifact behind every claim; the raw redacted JSON summary is preserved verbatim in the doc's appendix; no bearer token appears anywhere in the committed files."
  artifacts:
    - path: ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md"
      provides: "The written unblock-resume contract Phase 14 (DO-01/02/04/05) implements against — the phase's canonical deliverable"
      contains: "DO-03"
      min_lines: 80
    - path: "scripts/spike/unblock-resume-spike-probe.mjs"
      provides: "The committed throwaway probe behind the findings"
      contains: "[SPIKE 10]"
  key_links:
    - from: ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md"
      to: "Phase 14 (DO-01/02/04/05)"
      via: "the locked per-shape recipe + the spec'd-not-proven edge-clear decision"
      pattern: "Phase 14|DO-01|recipe"
    - from: ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md"
      to: "10-02 probe JSON summary"
      via: "raw evidence appendix (verbatim, redacted)"
      pattern: "Raw evidence|probe JSON"
---

<objective>
Turn the three-shape live-run evidence (Plan 10-02's JSON summary) into the written unblock-resume contract — `10-03-SPIKE-FINDINGS.md` — the canonical input Phase 14 implements against, and commit it alongside the throwaway probe script. The contract records, per shape, the working recipe (or honest negative) judged by the three-signal rule, with the DO-03 scope implication. This is the phase's must_have: a complete, honest contract covering all three shapes within the capability boundary — NOT a manufactured PASS.

Purpose: Phase 14's reply-in-place Send (DO-01/02/04/05) must receive an executable recipe per shape — comment-write path + any required transition + heartbeat-consumption timing — or, where a shape failed, an honest negative with its scope implication so the action loop is not built on an unproven assumption (Success Criterion 4). DO-03 is gated here.
Output: A committed `10-03-SPIKE-FINDINGS.md` + the committed `scripts/spike/unblock-resume-spike-probe.mjs`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-unblock-resume-spike/10-CONTEXT.md
@.planning/phases/10-unblock-resume-spike/10-RESEARCH.md
@.planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md
@.planning/phases/10-unblock-resume-spike/10-02-SUMMARY.md
@.planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md
@scripts/spike/unblock-resume-spike-probe.mjs

<interfaces>
<!-- Mirror the 04.1-01-SPIKE-FINDINGS.md structure (the proven findings-doc template): frontmatter (status/verdict/phase/plan/date/operator/target/probe/probe_commit/spike_issues) → TL;DR Verdict → Per-Shape PASS/FAIL table → per-shape sections (Question / Evidence / Locked finding / Design input for Phase 14) → DO-03 Gate Verdict → Raw evidence (probe JSON, verbatim, redacted). -->

<!-- Required per-shape fields (D-11) for each of A/B/C: -->
  - comment-write path used (REST POST /api/issues/{id}/comments = ctx.issues.createComment)
  - any required accompanying state transition (and its ordering — flip-before vs flip-after, for Shape B)
  - when/whether the agent consumes it on heartbeat (the consumption-signal timing)
  - the observed three-signal evidence (behavioral commentId, consumption createdByRunId, state transition list)
  - PASS / PARTIAL / FAIL verdict + DO-03 scope implication
  - REST-direct vs in-worker ctx fidelity note (what Phase 14 must re-confirm in-worker)

<!-- Shape C extra (D-10): cascade-on-answer recipe (proven) OR edge-clear path documented spec'd-not-proven (transition + issue.relations.write cap + CTT-07/governance cost). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the 10-03-SPIKE-FINDINGS.md unblock-resume contract</name>
  <files>.planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md</files>
  <read_first>
    - .planning/phases/10-unblock-resume-spike/10-02-SUMMARY.md (the three-shape JSON summary — the evidence this doc is written from)
    - .planning/phases/10-unblock-resume-spike/10-01-SUMMARY.md (A1/A3/D-02 dry-confirm results — fold the Shape-B-vs-C construction reality + the real-item fidelity evidence in)
    - .planning/phases/04.1-chat-true-task/04.1-01-SPIKE-FINDINGS.md (the findings-doc template + frontmatter shape + raw-evidence-appendix pattern to mirror)
    - .planning/phases/10-unblock-resume-spike/10-CONTEXT.md (D-07 three-signal, D-08 ladder, D-10 boundary, D-11 output spec — the contract's required contents)
    - .planning/ROADMAP.md (Phase 10's 4 success criteria — the doc must answer all four; and Phase 14 DO-01/02/04/05 — the consumer)
  </read_first>
  <action>
    Write .planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md mirroring the 04.1-01-SPIKE-FINDINGS.md structure. Frontmatter: status (locked), verdict (the DO-03 gate: GO / GO-WITH-CONSTRAINT / NO-GO-FOR-SHAPE), phase, plan:03, date, operator:Eric, target (BEAAA + company id 59f8876e-…), probe path, probe_commit (the commit from Task 2), spike_issues (the [SPIKE 10] ids from 10-02, marked torn-down). Body sections: (1) TL;DR Verdict — one-paragraph honest summary including any clean negative; (2) Per-Shape PASS/PARTIAL/FAIL table; (3) one section per shape A/B/C with Question, Evidence (the three-signal data: behavioral commentId, consumption createdByRunId, state transition list, the locked rung), Locked finding (the turnkey recipe: comment-write path + required transition + ordering + consumption timing — OR the honest negative + its DO-03 scope implication), and Design input for Phase 14 (exactly what DO-01/02/04/05 implements); (4) the REST-direct vs in-worker ctx fidelity note flagging what Phase 14 must re-confirm in-worker (requestWakeup scope per Pitfall 3; any Shape B status flip as a deliberate CTT-07 exception); (5) Shape C's cascade recipe OR the spec'd-not-proven edge-clear path (exact transition + issue.relations.write cap + CTT-07/governance cost — the Phase 14 deploy decision, D-10); (6) DO-03 Gate Verdict answering all four ROADMAP success criteria explicitly, with an honest-negative being a valid gate-pass (Success Criterion 4); (7) Raw evidence appendix — the verbatim redacted 10-02 JSON summary. Verify no bearer token substring appears anywhere in the doc. Do NOT manufacture a PASS — if a shape partialed or failed, record it plainly with its scope implication.
  </action>
  <acceptance_criteria>
    - The doc exists at .planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md with the mirrored frontmatter (status, verdict, phase, plan, date, operator, target, probe, probe_commit, spike_issues).
    - Each of Shape A, B, C has a section recording all six D-11 fields: comment-write path, required accompanying transition (with ordering for B), consumption timing, three-signal evidence, PASS/PARTIAL/FAIL verdict, and DO-03 scope implication.
    - A single explicit DO-03 Gate Verdict answers all four ROADMAP Phase-10 success criteria; an honest negative/partial is recorded as such (not strained into a PASS).
    - The REST-direct vs in-worker ctx fidelity gap is documented, naming what Phase 14 must re-confirm in-worker.
    - Shape C records either the proven cascade recipe OR the spec'd-not-proven edge-clear path with its capability + CTT-07/governance cost (D-10).
    - The raw redacted 10-02 JSON summary is preserved verbatim in an appendix; NO bearer token substring appears anywhere in the doc (grep for 'Bearer ' and any token fragment returns zero).
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md" && grep -q "DO-03" ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md" && grep -qi "Gate Verdict" ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md" && ! grep -qi "Bearer [A-Za-z0-9]" ".planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md"</automated>
  </verify>
  <done>The unblock-resume contract exists, covers all three shapes with the six D-11 fields each, states a single DO-03 gate verdict answering all four success criteria, documents the REST-vs-worker fidelity gap and Shape C's D-10 boundary, embeds the verbatim redacted evidence, and contains no token.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the probe script + findings contract; update ROADMAP/STATE traceability</name>
  <files>scripts/spike/unblock-resume-spike-probe.mjs</files>
  <read_first>
    - scripts/spike/unblock-resume-spike-probe.mjs (final committed probe — confirm no token, [SPIKE 10] tag present, node --check clean)
    - .planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md (the contract being committed alongside)
    - .planning/REQUIREMENTS.md (DO-03 traceability row — flip Phase 10 status to reflect the spike outcome)
    - CLAUDE.md (GSD workflow enforcement; MemPalace protocol — file the locked recipe drawer at end of substantive work)
  </read_first>
  <action>
    Final-check the probe: run node --check, confirm no bearer token substring is committed, confirm the [SPIKE 10] tag + DO-backup-bookend header are present. Commit scripts/spike/unblock-resume-spike-probe.mjs together with .planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md (and the 10-01/10-02 SUMMARY docs) using message `docs(10): unblock-resume spike findings + probe (DO-03 gate)`. Backfill the probe_commit field in the findings-doc frontmatter with the resulting commit short-sha (amend or follow-up commit). Update .planning/REQUIREMENTS.md DO-03 traceability row to reflect the spike verdict (Proven / Proven-with-constraint / Negative-recorded — per the gate verdict). Per CLAUDE.md MemPalace protocol, note for the operator to file a clarity_pack/decisions drawer with the locked recipe + DO-03 gate verdict (do not block the commit on it). Do NOT propose npm publish (clarity-pack is internal-only).
  </action>
  <acceptance_criteria>
    - scripts/spike/unblock-resume-spike-probe.mjs and 10-03-SPIKE-FINDINGS.md are committed in one commit with the message `docs(10): unblock-resume spike findings + probe (DO-03 gate)`.
    - The committed probe passes node --check and contains no bearer token substring.
    - The findings-doc probe_commit frontmatter holds the actual commit short-sha.
    - .planning/REQUIREMENTS.md DO-03 traceability row reflects the spike outcome (no longer bare "Pending").
    - No npm publish / Clipmart action proposed (internal-only).
  </acceptance_criteria>
  <verify>
    <automated>node --check "scripts/spike/unblock-resume-spike-probe.mjs" && git log --oneline -1 | grep -qi "unblock-resume spike"</automated>
  </verify>
  <done>The probe + findings contract are committed with the DO-03 gate message, probe_commit backfilled, REQUIREMENTS.md DO-03 row updated to the spike outcome, and the MemPalace drawer flagged for filing — no npm publish proposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artifact → repo history | A leaked bearer token in the committed probe/findings would be permanent in git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Information disclosure | bearer token committed in the probe output or findings appendix | mitigate | redactedError scrubbed the token at run time; Task 1 + Task 2 acceptance grep the committed files for any token/`Bearer ` substring (must be zero) before commit. |
| T-10-14 | Repudiation | findings claims not traceable to evidence | mitigate | The verbatim redacted JSON summary is preserved in the doc appendix; probe_commit pins the exact script; every per-shape claim is auditable against the raw evidence. |
| T-10-15 | (integrity) a manufactured PASS papers over an honest negative | mitigate | D-07 three-signal evidence is recorded per shape; the gate verdict must answer Success Criterion 4 (an honest negative is a valid gate-pass); acceptance forbids straining a partial/fail into a PASS. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no packages; documentation + commit only. No package legitimacy gate required. |
</threat_model>

<verification>
- 10-03-SPIKE-FINDINGS.md exists, contains DO-03 + a Gate Verdict, covers all three shapes with the six D-11 fields, and contains no token.
- The probe + findings are committed with the DO-03 gate message; probe_commit backfilled; REQUIREMENTS.md DO-03 row updated.
- `node --check` clean on the committed probe.
</verification>

<success_criteria>
- The written unblock-resume contract exists and is the canonical Phase 14 input (Success Criterion 2).
- It records honestly per shape, including any negative/partial with its DO-03 scope implication (Success Criterion 4).
- Governance parity + no-schema + declared-caps facts are reflected in the contract (Success Criterion 3).
- The probe is committed as the auditable artifact (D-11); no token leaked; internal-only (no publish).
</success_criteria>

<output>
Create `.planning/phases/10-unblock-resume-spike/10-03-SUMMARY.md` when done, recording the DO-03 gate verdict, the locked recipe(s), the spec'd-not-proven Shape C decision handed to Phase 14, and the commit sha.
</output>
