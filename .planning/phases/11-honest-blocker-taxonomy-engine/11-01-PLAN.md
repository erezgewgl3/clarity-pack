---
phase: 11-honest-blocker-taxonomy-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/types.ts
  - src/shared/blocker-chain.ts
  - src/shared/scrub-human-action.ts
  - test/shared/blocker-chain.test.mjs
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03]

must_haves:
  truths:
    - "The Terminal union has exactly 8 honest kinds — 7 honest + UNCLASSIFIED, HUMAN_ACTION_ON renamed to AWAITING_HUMAN, no dead alias (SC1, D-05)"
    - "flattenBlockerChain classifies a leaf into one honest kind using user owner, agent owner, agent liveness, ETA, or genuine-unowned — awaiting-human beats agent ownership (SC1, D-07)"
    - "A chain waiting on another agent still walks through the agent to the human-actionable leaf; no mid-chain agent is a terminal (SC2)"
    - "classifyVerdict maps every one of the 8 kinds to a {tier, actionAffordance, needsYou} triple exactly per the design-seed Section 1 table (SC1, SC5)"
    - "The __unowned__ sentinel no longer exists anywhere; UNOWNED is a real kind with no userId (SC3, D-11)"
    - "blocker-chain.ts stays pure — the determinism test (100x JSON.stringify) and the AI-token grep guard pass (SC4, D-08)"
    - "The verdict carries a human-readable awaitedPartyLabel and separate mutation-only targetAgentUuid/targetIssueUuid (NO_UUID_LEAK, D-15)"
    - "The engine stays read-only classification — it adds no leverage/unblocks-impact count; the leverage ranking is deferred to Phase 12 (D-16)"
  artifacts:
    - path: "src/shared/types.ts"
      provides: "8-variant Terminal union + enriched BlockerChainResult verdict"
      contains: "AWAITING_AGENT_STUCK"
    - path: "src/shared/blocker-chain.ts"
      provides: "D-07 awaiting-first cascade + exported classifyVerdict() pure mapping"
      contains: "export function classifyVerdict"
    - path: "src/shared/scrub-human-action.ts"
      provides: "NO_UUID_LEAK scrub for all 8 kinds; sentinel removed"
    - path: "test/shared/blocker-chain.test.mjs"
      provides: "per-kind cases for the 4 new kinds + classifyVerdict table + determinism + grep guard"
  key_links:
    - from: "src/shared/blocker-chain.ts"
      to: "src/shared/types.ts"
      via: "import type { BlockerChainResult, Terminal }"
      pattern: "import type .* from './types"
    - from: "src/shared/blocker-chain.ts classifyVerdict"
      to: "design-seed Section 1 table"
      via: "kind -> {tier, affordance, needsYou} 1:1 mapping"
      pattern: "classifyVerdict"
---

<objective>
Land the pure shared contract for the honest blocker taxonomy: the 8-variant `Terminal` union, the enriched `BlockerChainResult` verdict (D-13/D-15), the D-07 awaiting-first leaf cascade with agent-ownership/liveness branches, the new exported `classifyVerdict()` pure mapping (D-14), and the NO_UUID_LEAK scrub for all 8 kinds. This is the Wave-0 contract: landing the union first makes `tsc` fail at every downstream `switch`/string-match, turning the compile errors into the live migration checklist (RESEARCH Pitfall 1).

Purpose: SC1 (8 honest kinds), SC2 (transitive flatten through agents), SC4 (engine stays pure/deterministic), and the structural half of SC5 (one verdict shape). TAX-01/02/03 are all rooted here.
Output: `types.ts` + `blocker-chain.ts` + `scrub-human-action.ts` updated; engine test extended and green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-CONTEXT.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-RESEARCH.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-PATTERNS.md
@docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md

<interfaces>
<!-- Current contract being extended. Source: src/shared/types.ts:38-49 (read verbatim). -->
Terminal (4 variants today) → 8 variants:
  KEEP/RENAME: 'HUMAN_ACTION_ON' → 'AWAITING_HUMAN' { userId; label }
  KEEP: 'SELF_RESOLVING' { etaIso; label }, 'EXTERNAL' { label }, 'CYCLE' { cycleNodes; label }
  ADD: 'AWAITING_AGENT_WORKING' { agentId; label }, 'AWAITING_AGENT_STUCK' { agentId; label },
       'UNOWNED' { label } (no userId), 'UNCLASSIFIED' { label } (degrade kind)

BlockerChainResult today: { startId; pathIds; terminal; isStale }
  ADD (D-13/D-15): needsYou: boolean; tier: 'needs-you'|'in-motion'|'watch';
    actionAffordance: 'reply'|'nudge'|'assign'|'open'|'none'; degradeReason?: string;
    awaitedPartyLabel: string;  targetAgentUuid?: string | null; targetIssueUuid?: string | null

BlockerChainInput.nodeMeta value gains (D-01): assigneeAgentId?: string | null; agentState?: 'working'|'stuck'|null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the Terminal union and BlockerChainResult verdict (the Wave-0 contract)</name>
  <files>src/shared/types.ts, src/shared/blocker-chain.ts</files>
  <read_first>
    - src/shared/types.ts:38-49 (the current 4-variant union + result shape being self-extended)
    - 11-PATTERNS.md "src/shared/types.ts" section (the LineageThread.ownerAgentId split-identity comment idiom at L127-129 to copy verbatim onto the *Uuid fields)
    - docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md §3 Section 1 (the literal taxonomy table)
  </read_first>
  <behavior>
    - The exported `Terminal` type union contains exactly these 8 kind literals: AWAITING_HUMAN, AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, SELF_RESOLVING, EXTERNAL, CYCLE, UNOWNED, UNCLASSIFIED.
    - `BlockerChainResult` gains needsYou, tier, actionAffordance, optional degradeReason, awaitedPartyLabel, and optional targetAgentUuid/targetIssueUuid.
    - `BlockerChainInput.nodeMeta` value type gains optional assigneeAgentId and agentState.
  </behavior>
  <action>
    In src/shared/types.ts: rename the `HUMAN_ACTION_ON` variant to `AWAITING_HUMAN` (keep `userId: string; label: string`). Add four new variants per D-05: `AWAITING_AGENT_WORKING` and `AWAITING_AGENT_STUCK` (each `{ kind; agentId: string; label: string }`), `UNOWNED` (`{ kind; label: string }` — NO userId, per D-11), and `UNCLASSIFIED` (`{ kind; label: string }`). Enrich `BlockerChainResult` per D-13 with `needsYou: boolean`, `tier: 'needs-you' | 'in-motion' | 'watch'`, `actionAffordance: 'reply' | 'nudge' | 'assign' | 'open' | 'none'`, `degradeReason?: string`, and per D-15 `awaitedPartyLabel: string`, `targetAgentUuid?: string | null`, `targetIssueUuid?: string | null`. Add the split-identity provenance comment from `LineageThread.ownerAgentId` verbatim onto `targetAgentUuid`/`targetIssueUuid` (mutation-only, NEVER rendered — NO_UUID_LEAK). Update the `BlockerChainInput.nodeMeta` value type in src/shared/blocker-chain.ts to add `assigneeAgentId?: string | null` and `agentState?: 'working' | 'stuck' | null` (D-01). Add a `// Plan 11-01` provenance comment on each new field. Do not yet update the cascade — that is Task 2; this task just lands the types so `tsc` lights up the downstream checklist.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit 2>&1 | Select-String -Pattern "is not assignable to type 'never'|HUMAN_ACTION_ON" | Select-Object -First 20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -v '^//' src/shared/types.ts | grep -c "AWAITING_AGENT_STUCK\|AWAITING_AGENT_WORKING\|UNOWNED\|UNCLASSIFIED\|AWAITING_HUMAN"` reports all 5 kind tokens present.
    - Source assertion: `targetAgentUuid` and `targetIssueUuid` appear in BlockerChainResult; the string `HUMAN_ACTION_ON` no longer appears in types.ts.
    - Behavior assertion: `npx tsc --noEmit` now reports `never`-exhaustiveness errors in humanize-snapshot.ts (expected — that is the live migration checklist; not a failure of this task).
  </acceptance_criteria>
  <done>The 8-variant union and enriched verdict compile; downstream `tsc` failures enumerate the consumers Plans 11-02/03/04 must migrate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the leaf cascade (D-07) and add the pure classifyVerdict() mapping (D-14)</name>
  <files>src/shared/blocker-chain.ts</files>
  <read_first>
    - src/shared/blocker-chain.ts:1-31 (PRIM-03 header comment — preserve exactly, the grep guard scans it), :147-181 (the leaf cascade skeleton), :211-230 (pickTopChains priority switch)
    - 11-PATTERNS.md "src/shared/blocker-chain.ts" + "NEW classifyVerdict()" sections (exhaustive switch + const _exhaustive: never idiom)
    - src/worker/jobs/humanize-snapshot.ts:155-172 (the never-guard idiom to copy)
    - docs/superpowers/specs/...truthful-cockpit-design.md §3 Section 1 (the verdict table classifyVerdict must encode 1:1)
  </read_first>
  <behavior>
    - Leaf precedence (D-07): EXTERNAL/CYCLE pre-empt → status==='awaiting' ⇒ AWAITING_HUMAN → else ownerUserId!=null ⇒ AWAITING_HUMAN → else assigneeAgentId!=null ⇒ AWAITING_AGENT_WORKING (agentState==='working') | AWAITING_AGENT_STUCK (agentState==='stuck' OR missing, per D-04) → else etaIso && no owner ⇒ SELF_RESOLVING → else ⇒ UNOWNED.
    - classifyVerdict(terminal) returns the {tier, actionAffordance, needsYou} triple for all 8 kinds exactly per the design-seed table; a 9th kind would fail tsc via the never-guard.
    - flattenBlockerChain populates needsYou/tier/actionAffordance/awaitedPartyLabel on the returned BlockerChainResult from classifyVerdict; targetAgentUuid/targetIssueUuid are set from agent/issue ids on the terminal (mutation-only).
    - The maxSteps-exceeded internal fallthrough emits UNCLASSIFIED (RESEARCH Open Question 2, D-10 text favors this); real cycle detection still emits CYCLE.
  </behavior>
  <action>
    In src/shared/blocker-chain.ts: replace the leaf cascade at L147-181 with the D-07 awaiting-first order above. Insert the agent branch BETWEEN the user-owner branch and the SELF_RESOLVING branch: when `meta.assigneeAgentId != null`, emit `AWAITING_AGENT_WORKING` if `meta.agentState === 'working'` else `AWAITING_AGENT_STUCK` (D-04: missing/null agentState ⇒ STUCK conservatively). Widen the user-owner branch so `ownerUserId != null` alone ⇒ `AWAITING_HUMAN` (was gated on `status==='awaiting'`); keep the explicit `status==='awaiting'` branch FIRST so awaiting beats agent ownership. Replace the `__unowned__` fallback (L176-181) with a real `UNOWNED` terminal carrying NO userId (D-11). Add a new exported pure function `classifyVerdict(terminal: Terminal): { tier; actionAffordance; needsYou }` using the exhaustive `switch (terminal.kind)` + `const _exhaustive: never = terminal` idiom; encode the 8-row table: AWAITING_HUMAN→{needs-you, reply, true}, AWAITING_AGENT_WORKING→{in-motion, none, false}, AWAITING_AGENT_STUCK→{watch, nudge, false}, SELF_RESOLVING→{watch, none, false}, EXTERNAL→{watch, open, false}, CYCLE→{watch, open, false}, UNOWNED→{needs-you, assign, true}, UNCLASSIFIED→{watch, open, false}. In `flattenBlockerChain`'s return sites, call `classifyVerdict` and spread `needsYou/tier/actionAffordance` onto the result; set `awaitedPartyLabel` to the terminal's display label (raw scrub happens in scrub-human-action.ts — here just pass the label through), and set `targetAgentUuid` (from the AWAITING_AGENT_* agentId) / `targetIssueUuid` (the leaf node id) where applicable. Change the `maxSteps`-exceeded fallthrough (L190-196) to emit `UNCLASSIFIED` with `degradeReason: 'max-depth-exceeded'`. Update the `pickTopChains` priority switch (L215-228) so needs-you kinds lead: AWAITING_HUMAN=0, UNOWNED=1, then SELF_RESOLVING=2, AWAITING_AGENT_WORKING=3, AWAITING_AGENT_STUCK=4, EXTERNAL=5, CYCLE=6, UNCLASSIFIED=7; keep `[...chains].sort(...)` copy-then-sort. Use ONLY grep-safe vocabulary (working/stuck/tier/affordance/reply/nudge/assign/open) — no openai/anthropic/claude_local/llm/gpt/completion tokens anywhere in this file.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "export function classifyVerdict" src/shared/blocker-chain.ts` ≥ 1.
    - Source assertion: `grep -c "__unowned__" src/shared/blocker-chain.ts` == 0 (sentinel gone from the engine).
    - Source assertion: AI-token guard — `grep -iE '\b(openai|anthropic|claude_local|llm|gpt|completion)\b' src/shared/blocker-chain.ts` finds nothing.
    - Behavior + test assertion: `node --test test/shared/blocker-chain.test.mjs` exits 0 (after Task 3 adds the new cases); the determinism case and grep-guard case stay green (SC4).
  </acceptance_criteria>
  <done>The cascade emits all 8 kinds deterministically; classifyVerdict is exported and total; pickTopChains ranks needs-you kinds first; the engine reads no clock and contains no AI token.</done>
</task>

<task type="auto">
  <name>Task 3: Remove the __unowned__ sentinel from the scrub + add engine test cases for the 4 new kinds</name>
  <files>src/shared/scrub-human-action.ts, test/shared/blocker-chain.test.mjs</files>
  <read_first>
    - src/shared/scrub-human-action.ts:19 (UNOWNED_SENTINEL='__unowned__'), :46-76 (the 4-step scrub)
    - test/shared/blocker-chain.test.mjs:19-38 (per-kind case shape), :92-114 (determinism), :181-190 (pickTopChains purity), :192-199 (AI-token grep guard)
    - 11-PATTERNS.md "scrub-human-action.ts" + "test/shared/blocker-chain.test.mjs" sections
  </read_first>
  <behavior>
    - scrubHumanAction produces an awaitedPartyLabel with zero raw UUIDs for all 8 terminal kinds; the renamed AWAITING_HUMAN path replaces the old HUMAN_ACTION_ON path; UNOWNED produces an "assign an owner first"-class line without referencing the dead sentinel; UNCLASSIFIED produces the honest "can't determine blocker — open <issue>" line with no assign verb.
    - The engine test has one passing case per new kind (AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, UNOWNED, UNCLASSIFIED), a classifyVerdict table-coverage case, and the renamed AWAITING_HUMAN case; determinism + grep-guard + pickTopChains purity stay green.
  </behavior>
  <action>
    In src/shared/scrub-human-action.ts: remove `export const UNOWNED_SENTINEL = '__unowned__'` (D-11) and the `terminal.userId === UNOWNED_SENTINEL` special-case branch. Update the `switch`/branches to handle `AWAITING_HUMAN` (was HUMAN_ACTION_ON), `UNOWNED` (no userId → "Owner unknown — assign an owner first" class label), `AWAITING_AGENT_WORKING`/`AWAITING_AGENT_STUCK` (scrub the agentId to a name via the nameByUuid map or `agent#<8>` fallback — never the raw UUID), and `UNCLASSIFIED` (planner-discretion copy per D-12, e.g. "Can't determine blocker — open to investigate", NO assign verb). Keep the existing UUID regex scrub + viewer→"You" + belt-and-suspenders final pass intact. In test/shared/blocker-chain.test.mjs: rename the existing HUMAN_ACTION_ON case to assert `AWAITING_HUMAN`; add per-kind cases copying the L19-38 shape — AWAITING_AGENT_WORKING (leaf with `assigneeAgentId` set + `agentState:'working'`), AWAITING_AGENT_STUCK (leaf with `assigneeAgentId` set + `agentState` missing, asserting D-04 conservative STUCK), UNOWNED (leaf with no owner/agent/eta), and a UNCLASSIFIED case (assert classifyVerdict('UNCLASSIFIED') triple). Add a classifyVerdict table-coverage test that asserts the {tier, affordance, needsYou} triple for all 8 kinds against the design-seed table. Leave the determinism (100×), pickTopChains purity, and AI-token grep-guard tests unmodified — they MUST stay green.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "__unowned__\|UNOWNED_SENTINEL" src/shared/scrub-human-action.ts` == 0.
    - Test assertion: `node --test test/shared/blocker-chain.test.mjs` exits 0; output shows passing cases named for AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, UNOWNED, UNCLASSIFIED, and a classifyVerdict-table case.
    - Behavior assertion: the determinism test and the PRIM-03 AI-token grep-guard test both report pass (SC4 / D-08).
  </acceptance_criteria>
  <done>The sentinel is gone repo-wide in shared/; the scrub yields UUID-free labels for all 8 kinds; the engine test is green with new-kind coverage and the purity guards intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → consuming surfaces | The verdict (incl. *Uuid fields) crosses from the pure engine to worker/UI; UUIDs must never become rendered text. |
| engine input (nodeMeta) | agentState is a pre-resolved enum string injected by the worker; the engine must not read a clock to derive it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information Disclosure | BlockerChainResult.targetAgentUuid / targetIssueUuid (D-15) | mitigate | Split identity: awaitedPartyLabel is the only display string (scrubbed in scrub-human-action.ts to zero raw UUIDs for all 8 kinds); *Uuid fields carry the LineageThread provenance comment "NEVER rendered as visible text (NO_UUID_LEAK)". Render-side enforcement is Plan 11-04. |
| T-11-02 | Tampering | src/shared/blocker-chain.ts purity | mitigate | AI-token grep guard + 100× determinism test in test/shared/blocker-chain.test.mjs stay green; liveness injected as a string (D-01), no Date.now()/new Date()/cadence math in the engine. |
| T-11-03 | Spoofing (honesty defect) | UNOWNED vs UNCLASSIFIED affordance | mitigate | classifyVerdict gives UNOWNED `assign` and UNCLASSIFIED `open` (no assign) — a walk-failure can never surface a false "assign owner" (TAX-03, D-09/D-12). |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (RESEARCH "Package Legitimacy Audit: Not applicable"); change set is first-party src/ + test/ only. No T-11-SC checkpoint required. |
</threat_model>

<verification>
- `node --test test/shared/blocker-chain.test.mjs` exits 0 (engine purity + new-kind coverage).
- `npx tsc --noEmit` produces ONLY the expected downstream `never`-exhaustiveness/migration errors (in humanize-snapshot.ts and other unmigrated consumers) — types.ts/blocker-chain.ts/scrub-human-action.ts themselves compile clean. (Full-repo green is reached after Plans 11-02/03/04.)
- The string `__unowned__` is absent from all of `src/shared/`.
</verification>

<success_criteria>
- 8-variant Terminal union compiles; classifyVerdict exported and total over the union (SC1).
- The cascade walks through agents to the human leaf; no mid-chain agent terminal (SC2).
- UNOWNED (assign) and UNCLASSIFIED (open, no assign) are distinct (SC3/TAX-03).
- Determinism test + AI-token grep guard green; no clock in the engine (SC4).
- One verdict shape (BlockerChainResult) exported for every surface to read (SC5 structural half).
</success_criteria>

<output>
Create `.planning/phases/11-honest-blocker-taxonomy-engine/11-01-SUMMARY.md` when done.
</output>
