---
phase: 11-honest-blocker-taxonomy-engine
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/worker/handlers/org-blocked-backlog.ts
  - src/worker/handlers/flatten-blocker-chain.ts
  - src/worker/situation/agent-liveness.ts
  - test/worker/org-blocked-backlog.test.mjs
autonomous: true
requirements: [TAX-01, TAX-02, TAX-03]

must_haves:
  truths:
    - "buildEdges captures assigneeAgentId + worker-computed agentState per blocker node, so an agent-owned leaf classifies as AWAITING_AGENT_WORKING/STUCK instead of unowned (SC1, TAX-01, D-01)"
    - "Liveness (working vs stuck) is computed in the worker — stale heartbeat AND nothing queued, missing⇒stuck — never in the engine (SC4, D-02/D-03/D-04)"
    - "The Reader handler's BFS and the shared buildEdges produce the identical nodeMeta field set, so the same chain classifies identically across surfaces (SC5)"
    - "graceful() on walk failure emits UNCLASSIFIED with degradeReason, not a false EXTERNAL (SC3, TAX-03, D-10)"
    - "A blocker issue that throws during edge build surfaces an honest UNCLASSIFIED row instead of being silently dropped (SC3, TAX-03)"
  artifacts:
    - path: "src/worker/situation/agent-liveness.ts"
      provides: "pure worker helper projecting heartbeat/run-state to agentState 'working'|'stuck'|null"
      contains: "export function resolveAgentState"
    - path: "src/worker/handlers/org-blocked-backlog.ts"
      provides: "buildEdges capturing assigneeAgentId+agentState; UNCLASSIFIED on skip; need_you off verdict.needsYou"
    - path: "src/worker/handlers/flatten-blocker-chain.ts"
      provides: "nodeMeta shape parity with buildEdges; graceful()→UNCLASSIFIED on walk failure"
  key_links:
    - from: "src/worker/handlers/org-blocked-backlog.ts buildEdges"
      to: "blocker.assigneeAgentId + resolveAgentState"
      via: "nodeMeta[toId].assigneeAgentId / .agentState"
      pattern: "assigneeAgentId"
    - from: "src/worker/handlers/flatten-blocker-chain.ts graceful()"
      to: "Terminal kind UNCLASSIFIED"
      via: "degrade emission on walk failure"
      pattern: "UNCLASSIFIED"
---

<objective>
Wire worker-side agent ownership + liveness into the engine boundary and make walk failures honest. `buildEdges` (the shared builder used by the Situation Room and rollup) and `walkBlockerChain` (the Reader handler's private BFS) both gain `assigneeAgentId` + worker-computed `agentState` in `nodeMeta`, so agent-owned leaves classify as `AWAITING_AGENT_WORKING/STUCK` (D-01). A small pure helper computes the liveness projection (D-02/03/04) reusing the `classify-employee-state.ts` heartbeat-age precedent. The Reader handler's `graceful()` stops lying (EXTERNAL→UNCLASSIFIED on walk failure, D-10), and the skip-drop in `buildEdges` becomes an honest UNCLASSIFIED row.

Purpose: TAX-01 (agent ownership), TAX-03 (degrade-safe), and SC5 (the two BFS builders must agree). RESEARCH Pitfall 2 (divergent builders) and Pitfall 3 (graceful EXTERNAL lie) are closed here.
Output: agent-ownership/liveness capture in both worker BFS builders + a pure liveness helper + honest UNCLASSIFIED degrade; worker tests updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-CONTEXT.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-RESEARCH.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-PATTERNS.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-01-SUMMARY.md

<interfaces>
<!-- The contract from Plan 11-01 (already landed). Source: src/shared/types.ts + blocker-chain.ts. -->
BlockerChainInput.nodeMeta value: { ownerUserId; etaIso; status; assigneeAgentId?: string|null; agentState?: 'working'|'stuck'|null }
Terminal kinds available: AWAITING_HUMAN | AWAITING_AGENT_WORKING | AWAITING_AGENT_STUCK | SELF_RESOLVING | EXTERNAL | CYCLE | UNOWNED | UNCLASSIFIED
classifyVerdict(terminal) → { tier, actionAffordance, needsYou } (import from ../../shared/blocker-chain.ts)

<!-- Liveness precedent. Source: src/worker/situation/classify-employee-state.ts:18-62 -->
classifyEmployeeState({ lastHeartbeatMs, topOpenIssueStatus, lastActivityMs, nowMs }) → 'running'|'reviewing'|'blocked'|'idle'|'stale'|'unknown'
  RUNNING_WINDOW_MS = 5*60*1000 ; STALE_WINDOW_MS = 24*60*60*1000  (the established fixed-window fallback, Assumption A2)

<!-- The exact buildEdges injection point. Source: src/worker/handlers/org-blocked-backlog.ts:211-219 (read verbatim) -->
nodeMeta[toId] = { ownerUserId: blocker.assigneeUserId ?? blocker.ownerUserId ?? null, etaIso: ..., status: ... }
  // root IssueLike already declares assigneeAgentId (org-blocked-backlog.ts:122)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the pure worker liveness helper (D-02/D-03/D-04)</name>
  <files>src/worker/situation/agent-liveness.ts</files>
  <read_first>
    - src/worker/situation/classify-employee-state.ts:1-62 (the pure injected-nowMs precedent + the 5min/24h fixed windows)
    - 11-PATTERNS.md "Shared Pattern: Caller-injects-the-impure-bits" section
    - 11-RESEARCH.md Assumption A2 (cadence sourcing fallback)
  </read_first>
  <behavior>
    - resolveAgentState projects an agent's heartbeat/run-state to 'working' (fresh heartbeat AND progressing) | 'stuck' (stale heartbeat AND nothing queued) | null.
    - Missing/unknown liveness signal ⇒ 'stuck' (D-04 conservative), NOT null, when the node has a known assigneeAgentId; null is reserved for "no agent on this node."
    - The helper is pure: nowMs injected, no SDK import, no wall-clock read (mirrors classify-employee-state.ts).
  </behavior>
  <action>
    Create src/worker/situation/agent-liveness.ts exporting a pure `resolveAgentState(input: { lastHeartbeatMs: number | null; hasQueuedWork: boolean; nowMs: number; expectedCadenceMs?: number }): 'working' | 'stuck' | null`. Compute `heartbeatAge = nowMs - lastHeartbeatMs` (Infinity when null). Stale window = `2 * (expectedCadenceMs ?? RUNNING_WINDOW_MS)` per D-03 (self-tuning when a host cadence is passed, else the established 5-min fallback constant from classify-employee-state.ts per Assumption A2). Return 'working' when heartbeatAge < stale window; return 'stuck' when heartbeatAge ≥ stale window AND `hasQueuedWork === false` (D-02); per D-04 a node with a known agent but no heartbeat signal (heartbeatAge === Infinity) ⇒ 'stuck' (conservative). Mirror the `// Pure: no SDK import, no I/O, no wall-clock read — nowMs is injected` header comment. Keep grep-safe vocabulary. This helper is the SINGLE liveness source both buildEdges (Task 2) and the rollup (Plan 11-03) import — no liveness math in the engine.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit src/worker/situation/agent-liveness.ts 2>&1 | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "export function resolveAgentState" src/worker/situation/agent-liveness.ts` == 1.
    - Source assertion: no `Date.now()`/`new Date()` in agent-liveness.ts (nowMs injected) — `grep -c "Date.now\|new Date" src/worker/situation/agent-liveness.ts` == 0.
    - Behavior assertion: a call with heartbeatAge=Infinity (null lastHeartbeatMs) returns 'stuck' (D-04), and a fresh heartbeat returns 'working'.
  </acceptance_criteria>
  <done>A pure, injected-clock liveness helper exists as the single source of agentState; no time math leaks toward the engine.</done>
</task>

<task type="auto">
  <name>Task 2: Capture assigneeAgentId + agentState in buildEdges; emit UNCLASSIFIED on skip; re-triage need_you off the verdict</name>
  <files>src/worker/handlers/org-blocked-backlog.ts, test/worker/org-blocked-backlog.test.mjs</files>
  <read_first>
    - src/worker/handlers/org-blocked-backlog.ts:122 (IssueLike.assigneeAgentId), :202-219 (blockedBy cast + nodeMeta build — the injection point), :271-291 (try/catch skip-drop), :300 (pickTopChains), :332,:370-374 (UNOWNED_SENTINEL viewer-match / need_you)
    - 11-PATTERNS.md "org-blocked-backlog.ts" + "Defensive-cast field reads" sections
    - 11-RESEARCH.md Pitfall 7 (loose-cast convention) + Assumption A1
    - src/worker/situation/agent-liveness.ts (resolveAgentState from Task 1)
  </read_first>
  <behavior>
    - Each blocker node's nodeMeta carries assigneeAgentId (loose-cast `?? null`) and a worker-resolved agentState; an agent-owned leaf then classifies as AWAITING_AGENT_WORKING/STUCK via the engine cascade.
    - A blocker issue whose edge build throws produces an UNCLASSIFIED OrgBlockedRow with degradeReason (honest), not a silent `continue`.
    - need_you membership keys on the engine verdict (verdict.needsYou / tier==='needs-you'), not the HUMAN_ACTION_ON + UNOWNED_SENTINEL viewer string-match.
    - viewer-scoping (V4) is preserved: need_you still keys on the UI-supplied viewerUserId.
  </behavior>
  <action>
    In src/worker/handlers/org-blocked-backlog.ts: extend the inline `blockedBy` cast type (L202-210) to include `assigneeAgentId?: string | null`; in the `nodeMeta[toId]` literal (L211-219) add `assigneeAgentId: blocker.assigneeAgentId ?? null` and `agentState: <resolved>` where the resolved value comes from `resolveAgentState(...)` (Task 1) using the agent's heartbeat/queue data already available to the handler, or `null` when there is no `assigneeAgentId`. Keep the defensive `?? null` posture for every new read (Pitfall 7 / V5). In the try/catch at L271-291: instead of `continue` (silent drop), push an `UNCLASSIFIED` OrgBlockedRow with `degradeReason` set and keep the existing `ctx.logger?.warn?.(...)` (TAX-03/D-09). Update `OrgBlockedRow.terminalKind` consumption and the need_you computation (L332/L370-374) to key off the engine verdict's `needsYou`/`tier` instead of `terminal.kind === 'HUMAN_ACTION_ON' && userId === UNOWNED_SENTINEL`; remove the now-dead UNOWNED_SENTINEL import. Preserve viewer-scoping: need_you still gates on the UI-supplied `viewerUserId`. Update test/worker/org-blocked-backlog.test.mjs stubs to provide `assigneeAgentId` + agentState fixtures for the new agent terminals, and add a case asserting a thrown edge-build yields an UNCLASSIFIED row (not a dropped issue).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/org-blocked-backlog.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "assigneeAgentId" src/worker/handlers/org-blocked-backlog.ts` ≥ 2 (cast + nodeMeta read).
    - Source assertion: `grep -c "UNOWNED_SENTINEL" src/worker/handlers/org-blocked-backlog.ts` == 0.
    - Test assertion: `node --test test/worker/org-blocked-backlog.test.mjs` exits 0, including a case where an agent-owned leaf classifies AWAITING_AGENT_* and a thrown edge-build yields a UNCLASSIFIED row.
    - Behavior assertion: need_you membership is computed from verdict.needsYou (not ownerName/sentinel string-match).
  </acceptance_criteria>
  <done>Agent ownership + liveness ride into the engine via nodeMeta; skips become honest UNCLASSIFIED rows; need_you keys on the verdict; viewer-scoping intact.</done>
</task>

<task type="auto">
  <name>Task 3: Bring the Reader BFS to nodeMeta-shape parity and fix the graceful() EXTERNAL lie</name>
  <files>src/worker/handlers/flatten-blocker-chain.ts</files>
  <read_first>
    - src/worker/handlers/flatten-blocker-chain.ts:48-72 (handler + graceful call sites), :74-127 (the private walkBlockerChain BFS), :129-141 (graceful() EXTERNAL synthesis)
    - 11-PATTERNS.md "flatten-blocker-chain.ts" section + Pitfall 2 / Pitfall 3
    - 11-RESEARCH.md Open Question 1 (collapse vs keep-in-sync decision)
    - src/worker/handlers/org-blocked-backlog.ts buildEdges nodeMeta shape (the parity target)
  </read_first>
  <behavior>
    - walkBlockerChain produces nodeMeta with the SAME field set as buildEdges (incl. assigneeAgentId + agentState), so the Reader panel classifies an agent-owned leaf identically to the Situation Room (SC5).
    - graceful() on a walk FAILURE (missing params at L49, relations-walk throw at L57) emits an UNCLASSIFIED terminal with degradeReason — never EXTERNAL (D-10/TAX-03).
    - The empty-graph case (L61 "No active blockers" — genuinely no blockers) stays distinct from a walk failure: it does NOT become UNCLASSIFIED (Pitfall 3 — distinguish "no blockers" from "walk failed").
  </behavior>
  <action>
    DECISION (RESEARCH Open Question 1): keep-in-sync rather than full collapse this phase — the Reader handler's ctx typing (PluginIssuesClient) differs structurally from OrgBlockedBacklogCtx, so a full collapse exceeds this wave's budget; instead thread the identical nodeMeta field set into walkBlockerChain and pin it with a same-shape test (the cheaper of the two RESEARCH options, justified by blast radius). In src/worker/handlers/flatten-blocker-chain.ts: extend walkBlockerChain's `blockedBy` loose-cast (L101-119) and `nodeMeta[toId]` literal (L110-119) to add `assigneeAgentId: blocker.assigneeAgentId ?? null` and `agentState` resolved via `resolveAgentState(...)` (Task 1) — exactly matching the buildEdges field set. Rewrite `graceful()` (L129-141): for the walk-FAILURE call sites (L49 missing-params, L57 relations-walk throw) emit `{ kind: 'UNCLASSIFIED', label }` with the result carrying `degradeReason` and the classifyVerdict-derived verdict fields (tier='watch', actionAffordance='open', needsYou=false). LEAVE the empty-graph path (L61 "No active blockers") emitting whatever non-UNCLASSIFIED result indicates "render no panel" — do NOT relabel a genuinely-blocker-free issue as a degrade. Add a test (in test/worker or alongside the Reader-panel tests) asserting walkBlockerChain's nodeMeta has the identical key set as buildEdges for a fixture with an agent-owned blocker, and that a thrown relations.get yields UNCLASSIFIED (not EXTERNAL).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit 2>&1 | Select-String -Pattern "flatten-blocker-chain" | Select-Object -First 10</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "assigneeAgentId" src/worker/handlers/flatten-blocker-chain.ts` ≥ 2 (parity with buildEdges).
    - Source assertion: graceful() walk-failure path emits UNCLASSIFIED — `grep -c "UNCLASSIFIED" src/worker/handlers/flatten-blocker-chain.ts` ≥ 1; the EXTERNAL synthesis for walk failure is gone.
    - Behavior assertion: a same-shape test confirms walkBlockerChain nodeMeta keys === buildEdges nodeMeta keys (incl. assigneeAgentId + agentState); a thrown relations.get yields UNCLASSIFIED.
    - Behavior assertion: the genuinely-empty-graph "No active blockers" case is NOT classified UNCLASSIFIED.
  </acceptance_criteria>
  <done>Both worker BFS builders emit the identical nodeMeta shape (SC5 protected); walk failures degrade to UNCLASSIFIED while truly-blocker-free issues stay distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host relations.get → worker nodeMeta | assigneeAgentId rides on the relation summary via a loose cast; may be absent at runtime (Assumption A1). |
| worker liveness compute → engine | agentState is resolved in the worker (clock here is legitimate) and injected as a string; the engine never reads a clock. |
| company scoping | every ctx.issues.* / ctx.agents.* call is companyId-scoped (V4, inherited). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Tampering | resolveAgentState (clock in worker, not engine) | mitigate | Liveness math lives in the worker helper with nowMs injected (D-01); the engine only branches on the pre-resolved string, preserving SC4 determinism. |
| T-11-05 | Information Disclosure | cross-company blocker disclosure | mitigate | All relations.get / agent reads stay companyId-scoped (V4, inherited; unchanged by this phase). |
| T-11-06 | Spoofing (honesty defect) | graceful() walk-failure terminal | mitigate | Walk failure ⇒ UNCLASSIFIED (open affordance, no assign), never a false EXTERNAL chase-action (TAX-03/D-10). |
| T-11-07 | Input Validation | assigneeAgentId loose-cast read | mitigate | Defensive `?? null` on every new node-field read (Pitfall 7 / V5); a missing field falls through to UNOWNED/SELF_RESOLVING (conservative-correct), never a crash. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH "Package Legitimacy Audit: Not applicable"); first-party src/test edits only. |
</threat_model>

<verification>
- `node --test test/worker/org-blocked-backlog.test.mjs` exits 0 with agent-terminal + UNCLASSIFIED-row coverage.
- Same-shape test confirms the two BFS builders' nodeMeta key sets match (SC5).
- No `Date.now()`/`new Date()` reaches `src/shared/blocker-chain.ts` (the engine stays clock-free).
- A1 reminder for the live drill: confirm `assigneeAgentId` is populated on a real BEAAA `relations.get` (a miss is conservative per D-04, but coverage degrades).
</verification>

<success_criteria>
- Agent-owned leaves classify as AWAITING_AGENT_WORKING/STUCK via injected nodeMeta (SC1/TAX-01).
- Liveness computed in the worker, injected as a string; engine clock-free (SC4).
- Both BFS builders agree on nodeMeta shape (SC5).
- Walk failures emit UNCLASSIFIED, not EXTERNAL; skips become honest rows (SC3/TAX-03).
</success_criteria>

<output>
Create `.planning/phases/11-honest-blocker-taxonomy-engine/11-02-SUMMARY.md` when done.
</output>
