---
phase: 11-honest-blocker-taxonomy-engine
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/worker/situation/build-employees-rollup.ts
  - src/worker/jobs/humanize-snapshot.ts
  - test/worker/situation/build-employees-rollup.test.mjs
  - test/worker/situation/build-employees-rollup-needsyou.test.mjs
autonomous: true
requirements: [TAX-01, TAX-03]

must_haves:
  truths:
    - "build-employees-rollup re-triages Needs-you off verdict.needsYou / tier==='needs-you', never ownerName==='Unassigned' (SC5, D-13/D-14)"
    - "The rollup carries the verdict's split identity — human awaitedPartyLabel rendered, targetAgentUuid/targetIssueUuid mutation-only (NO_UUID_LEAK, D-15)"
    - "A chain-build throw in the rollup emits an UNCLASSIFIED verdict (honest fallback line), not a silent null (SC3, TAX-03, D-09)"
    - "Every exhaustive switch over Terminal handles all 8 kinds so the typecheck compile-gate is satisfied (SC1)"
  artifacts:
    - path: "src/worker/situation/build-employees-rollup.ts"
      provides: "verdict-driven Needs-you re-triage + UNCLASSIFIED-on-throw + split-identity passthrough"
    - path: "src/worker/jobs/humanize-snapshot.ts"
      provides: "exhaustive switch resolved for all 8 kinds (or file deleted if zero imports)"
  key_links:
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "BlockerChainResult.verdict.needsYou"
      via: "Needs-you re-triage filter"
      pattern: "needsYou|tier"
    - from: "src/worker/jobs/humanize-snapshot.ts"
      to: "Terminal 8-kind union"
      via: "exhaustive switch + const _exhaustive: never"
      pattern: "_exhaustive"
---

<objective>
Migrate the worker rollup off the `ownerName === 'Unassigned'` string-match to the engine verdict (D-13/D-14), thread the split-identity fields (D-15), make a chain-build throw emit an honest UNCLASSIFIED verdict (D-09/TAX-03), and resolve the `humanize-snapshot.ts` exhaustive-switch compile gate (the `never`-guard that has been failing `tsc` since Plan 11-01 landed the union). This is the last worker-tier consumer; after it, the worker half of the big-bang reads the new verdict (SC5 worker side).

Purpose: SC5 (single source of truth — no view-layer re-derivation), SC3/TAX-03 (honest degrade), SC1 (compile gate satisfied). RESEARCH Pitfall 1 (the compile gate) and Pitfall 5 (NO_UUID_LEAK regression via split identity) are closed here.
Output: rollup re-triages off the verdict + degrades to UNCLASSIFIED; humanize-snapshot resolved; worker rollup tests updated.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-CONTEXT.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-RESEARCH.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-PATTERNS.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-01-SUMMARY.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-02-SUMMARY.md

<interfaces>
<!-- From Plan 11-01 + 11-02 (already landed). -->
BlockerChainResult: { startId; pathIds; terminal; isStale; needsYou; tier; actionAffordance;
  degradeReason?; awaitedPartyLabel; targetAgentUuid?; targetIssueUuid? }
classifyVerdict(terminal) → { tier, actionAffordance, needsYou }  (../../shared/blocker-chain.ts)

<!-- The string-match to kill. Source: src/worker/situation/build-employees-rollup.ts:463-465 (verbatim) -->
const unowned = rows.filter((r) => r.group === 'needs_you' && r.blockerChain && r.blockerChain.ownerName === 'Unassigned');

<!-- The split-identity precedent to mirror. Source: build-employees-rollup.ts:329-353 (leafIssueUuid carried separately, human leafIssueId rendered) -->

<!-- The compile-gate idiom. Source: src/worker/jobs/humanize-snapshot.ts:155-172 (switch (t.kind) + const _exhaustive: never = t) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-triage Needs-you off the verdict + thread split identity + emit UNCLASSIFIED on chain-build throw</name>
  <files>src/worker/situation/build-employees-rollup.ts, test/worker/situation/build-employees-rollup.test.mjs, test/worker/situation/build-employees-rollup-needsyou.test.mjs</files>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts:274-374 (chain pipeline + the HUMAN_ACTION_ON/UNOWNED_SENTINEL sites at L291,L321,L358), :329-353 (leafIssueUuid split-identity precedent), :365-373 (chain-build try/catch → null), :463-465 (the ownerName==='Unassigned' string-match)
    - 11-PATTERNS.md "build-employees-rollup.ts" section + "NO_UUID_LEAK scrub" + Pitfall 5
    - src/worker/situation/agent-liveness.ts (resolveAgentState, if liveness for rollup nodes is computed here)
  </read_first>
  <behavior>
    - Needs-you membership is computed from the engine verdict (needsYou true / tier==='needs-you'), not the ownerName string.
    - The row carries awaitedPartyLabel (rendered, scrubbed of UUIDs) and the targetAgentUuid/targetIssueUuid (mutation-only) per the leafIssueUuid split precedent — no raw UUID enters a rendered field.
    - A chain-build throw produces an UNCLASSIFIED verdict on the row (honest fallback line), not blockerChain = null.
    - Viewer-scoping (V4) preserved: need_you keys on viewerUserId.
  </behavior>
  <action>
    In src/worker/situation/build-employees-rollup.ts: replace the L463-465 `ownerName === 'Unassigned'` filter with a filter on `r.blockerChain.needsYou === true` (or `r.blockerChain.tier === 'needs-you'`) per D-13/D-14. Replace the `terminal.kind === 'HUMAN_ACTION_ON'` + `UNOWNED_SENTINEL` branches at L291/L321/L358 with verdict-field reads (e.g. `actionAffordance === 'assign'` for the genuinely-unowned assign path; `tier`/`needsYou` for grouping); remove the dead UNOWNED_SENTINEL import. Extend the blockerChain row object to carry the verdict's `awaitedPartyLabel` (the rendered string), `targetAgentUuid`, and `targetIssueUuid`, mirroring the existing `leafIssueUuid` split-identity pattern at L329-353 (human id rendered; UUID carried mutation-only — D-15/Pitfall 5). In the chain-build try/catch at L365-373: instead of `blockerChain = null`, build an UNCLASSIFIED verdict (`classifyVerdict({kind:'UNCLASSIFIED', label})` for tier/affordance/needsYou) with a `degradeReason`, so the row shows the honest fallback line (D-09). Keep the `ctx.logger?.warn?.(...)`. Preserve viewer-scoping. Update test/worker/situation/build-employees-rollup.test.mjs and -needsyou.test.mjs: replace the `ownerName === 'Unassigned'` assertions (L146 / L79) with assertions on `verdict.tier`/`needsYou`; add a case asserting a chain-build throw yields an UNCLASSIFIED verdict (not null) and a case asserting no raw UUID appears in the rendered awaitedPartyLabel while targetAgentUuid/targetIssueUuid carry the UUID.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/situation/build-employees-rollup.test.mjs test/worker/situation/build-employees-rollup-needsyou.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "ownerName === 'Unassigned'\|UNOWNED_SENTINEL" src/worker/situation/build-employees-rollup.ts` == 0.
    - Source assertion: `grep -c "needsYou\|tier" src/worker/situation/build-employees-rollup.ts` ≥ 1 (re-triage keys on the verdict).
    - Test assertion: both rollup test files exit 0; a UUID source-scan / render-scan case confirms awaitedPartyLabel has no raw UUID while targetAgentUuid/targetIssueUuid carry it (Pitfall 5 / D-15).
    - Behavior assertion: a chain-build throw yields an UNCLASSIFIED verdict row, not a null blockerChain.
  </acceptance_criteria>
  <done>The rollup reads ownership exclusively from the engine verdict; split identity holds (no UUID leak); throws degrade honestly to UNCLASSIFIED.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the humanize-snapshot.ts exhaustive-switch compile gate</name>
  <files>src/worker/jobs/humanize-snapshot.ts</files>
  <read_first>
    - src/worker/jobs/humanize-snapshot.ts:13-17 (single-tenant scoping comment), :112-117 (__unowned__ special-case), :155-172 (the exhaustive switch + const _exhaustive: never)
    - 11-PATTERNS.md "humanize-snapshot.ts (dead job, still compiled)" section + Pitfall 1
    - 11-RESEARCH.md Open Question 3 + Assumption A3 (verify zero live caller before deleting)
    - src/worker/worker.ts (confirm no register* wiring invokes humanize-snapshot)
  </read_first>
  <behavior>
    - After this task, `npx tsc --noEmit` reports zero errors for the whole repo's worker tier (the never-guard is satisfied for all 8 kinds OR the file is deleted with no dangling import).
    - The __unowned__ special-case is removed (sentinel is dead per D-11).
    - If kept: the switch handles AWAITING_HUMAN, AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, SELF_RESOLVING, EXTERNAL, CYCLE, UNOWNED, UNCLASSIFIED; another exhaustive switch (classifyVerdict in blocker-chain.ts) still guards the union if this file is deleted.
  </behavior>
  <action>
    DECISION (RESEARCH Open Question 3 / Assumption A3): first confirm `humanize-snapshot.ts` has zero live callers — grep src/ for any `import` of it and inspect src/worker/worker.ts register* wiring. If and only if zero imports AND zero register wiring: DELETE the file (cleanest — removes one dead exhaustiveness site), since `classifyVerdict()` in blocker-chain.ts (Plan 11-01) provides the surviving exhaustive `never`-guard over the 8-kind union, so the union stays compile-protected. If any import or wiring remains: MIGRATE instead — update the `switch (t.kind)` (L155-172) to add `case 'AWAITING_HUMAN'` (replacing the renamed HUMAN_ACTION_ON case), `case 'AWAITING_AGENT_WORKING'`, `case 'AWAITING_AGENT_STUCK'`, `case 'UNOWNED'`, `case 'UNCLASSIFIED'`, keeping the `default: const _exhaustive: never = t` guard; and remove the `__unowned__` special-case at L112-117 (D-11). State which path you took (delete vs migrate) and the import-check result in the SUMMARY. Either way, after this task the full-repo typecheck is green.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit 2>&1 | Select-String -Pattern "error TS" | Measure-Object | Select-Object Count</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `__unowned__` no longer appears in src/worker/jobs/ (deleted or removed) — `grep -rc "__unowned__" src/worker/jobs/` == 0.
    - Behavior assertion: `npx tsc --noEmit` reports zero `error TS` lines across the worker tier (the compile gate is satisfied repo-wide except for any pending UI work in Plan 11-04).
    - Documentation assertion: the SUMMARY records delete-vs-migrate and the zero-import/zero-wiring verification (Assumption A3).
  </acceptance_criteria>
  <done>The dead-job exhaustive switch is resolved; the union stays compile-protected by at least one `never`-guard; the worker tier typechecks clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verdict → rendered row | awaitedPartyLabel is display; targetAgentUuid/targetIssueUuid are mutation-only and must not enter a rendered field (D-15). |
| chain-build throw → row | a failure must surface as an honest UNCLASSIFIED line, not a silent drop or a false assign. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Information Disclosure | build-employees-rollup split-identity passthrough | mitigate | Mirror the proven leafIssueUuid split (L329-353): awaitedPartyLabel scrubbed (zero raw UUIDs), *Uuid fields mutation-only; UUID source-scan/render-scan tests pin it (Pitfall 5 / D-15). |
| T-11-09 | Spoofing (honesty defect) | chain-build throw handling | mitigate | Throw ⇒ UNCLASSIFIED verdict (open affordance, no assign), never null/silent and never a false "assign owner" (TAX-03/D-09). |
| T-11-10 | Access Control | Needs-you viewer scoping | mitigate | need_you re-triage still keys on the UI-supplied viewerUserId (V4 inherited); the verdict swap does not widen scope. |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No installs this phase (RESEARCH "Package Legitimacy Audit: Not applicable"); first-party src/test edits only. |
</threat_model>

<verification>
- `node --test test/worker/situation/build-employees-rollup.test.mjs test/worker/situation/build-employees-rollup-needsyou.test.mjs` exits 0.
- `npx tsc --noEmit` reports zero worker-tier errors (compile gate satisfied).
- `__unowned__` and `ownerName === 'Unassigned'` are absent from src/worker/situation/build-employees-rollup.ts.
- UUID source-scan / render-scan tests confirm no raw UUID in awaitedPartyLabel (NO_UUID_LEAK).
</verification>

<success_criteria>
- Rollup reads ownership from the verdict only; no view-layer string-match (SC5/D-14).
- Split identity holds — no UUID leak (D-15).
- Throws degrade to honest UNCLASSIFIED (SC3/TAX-03).
- Exhaustive switches satisfied for all 8 kinds; worker tier typechecks clean (SC1).
</success_criteria>

<output>
Create `.planning/phases/11-honest-blocker-taxonomy-engine/11-03-SUMMARY.md` when done.
</output>
