---
phase: 11-honest-blocker-taxonomy-engine
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/blocker-chain.ts
  - src/worker/situation/agent-liveness.ts
  - src/shared/scrub-human-action.ts
  - test/shared/blocker-chain.test.mjs
autonomous: true
requirements: [TAX-01, TAX-03]
gap_closure: true

must_haves:
  truths:
    - "blocker-chain.ts stays pure and deterministic after the edits — the determinism test (100× JSON.stringify equality) and the AI-vendor-token grep guard both stay green (SC4 / PRIM-03)"
    - "A genuinely blocker-free chain carries a non-actionable verdict (actionAffordance 'none'), not a false 'open' affordance (WR-01 root)"
    - "resolveAgentState returns a positive stale window when expectedCadenceMs is 0 — an agent with a finite heartbeat is never falsely classified stuck because the window collapsed to 0 (WR-03)"
    - "Both EXTERNAL terminal branches name the SAME node (current/leafId) so the label and targetIssueUuid agree (WR-05)"
    - "resolveAgentState's return type is 'working' | 'stuck' (no dead | null) — nullability lives at the call site, not the helper (WR-04)"
  artifacts:
    - path: "src/shared/blocker-chain.ts"
      provides: "makeDegradedResult export; both EXTERNAL branches name current; classifyVerdict 'none' for the blocker-free synthetic case (per chosen approach)"
      contains: "makeDegradedResult"
    - path: "src/worker/situation/agent-liveness.ts"
      provides: "positive-cadence guard (> 0); return type narrowed to 'working' | 'stuck'"
      contains: "expectedCadenceMs > 0"
    - path: "src/shared/scrub-human-action.ts"
      provides: "step comments renumbered 1-2-3-4-5 (IN-02)"
  key_links:
    - from: "src/worker/situation/agent-liveness.ts"
      to: "RUNNING_WINDOW_MS fallback"
      via: "positive-value guard, not nullish coalescing"
      pattern: "expectedCadenceMs > 0"
    - from: "src/shared/blocker-chain.ts"
      to: "BlockerChainResult degrade rows"
      via: "shared makeDegradedResult helper"
      pattern: "export function makeDegradedResult"
---

<objective>
Harden the pure shared engine + the worker liveness helper so the Wave-2 scrub and Wave-3 Reader render build on a correct, single-source foundation. This plan closes the engine/shared-layer warnings from 11-REVIEW.md (WR-03 cadence-zero collapse, WR-04 dead `| null`, WR-05 EXTERNAL label mis-attribution, IN-02 step renumber, IN-04 shared degrade-result helper) and lands the `'none'` affordance plumbing the blocker-free case needs (WR-01 root). It does NOT touch the worker handlers or the UI (Waves 2-3 own those).

Purpose: TAX-01 (honest agent-aware classification — liveness boundary must not collapse), TAX-03 (degrade-safe — a blocker-free / degraded row must not surface a false action). Engine purity (PRIM-03 / SC4) is the hard invariant: the determinism test and AI-token grep guard must stay green through every edit here.
Output: a corrected pure engine + liveness helper; the determinism + AI-token-guard tests stay green; a new fixture proves the cadence-zero guard.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-REVIEW.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-VERIFICATION.md

<interfaces>
From src/shared/types.ts:
  actionAffordance: 'reply' | 'nudge' | 'assign' | 'open' | 'none';  // 'none' already valid — no type change needed
  Terminal union: 8 kinds; EXTERNAL = { kind: 'EXTERNAL'; label: string } (no id field)

From src/shared/blocker-chain.ts:
  export function classifyVerdict(terminal: Terminal): { tier; actionAffordance; needsYou }
  function makeResult(args: { startId; pathIds; terminal; isStale; leafId; degradeReason? }): BlockerChainResult
  // EXTERNAL branch A (reached-via-external) at :212-218 labels `External (${current})`
  // EXTERNAL branch B (only-external-children) at :219-228 labels `External (${externalEdge.to})` ← mis-attribution (WR-05)

From src/worker/situation/agent-liveness.ts:
  export function resolveAgentState(input): 'working' | 'stuck' | null  // ← WR-04 dead | null
  const staleWindowMs = 2 * (expectedCadenceMs ?? RUNNING_WINDOW_MS);   // ← WR-03 nullish passes 0 through
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the WR-03 cadence-zero collapse and WR-04 dead | null in agent-liveness.ts</name>
  <files>src/worker/situation/agent-liveness.ts</files>
  <read_first>
    - src/worker/situation/agent-liveness.ts (the file being modified — read the whole file; it is ~76 lines)
    - test/worker/ (locate the existing resolveAgentState test if any; grep for resolveAgentState)
  </read_first>
  <behavior>
    - Test: expectedCadenceMs = 0 with a finite, fresh lastHeartbeatMs (age well under 2*RUNNING_WINDOW_MS) → resolveAgentState returns 'working' (NOT 'stuck'). This fixture FAILS before the fix (staleWindowMs collapses to 0) and PASSES after.
    - Test: expectedCadenceMs = undefined, fresh heartbeat → 'working' (regression: fallback window still applies).
    - Test: expectedCadenceMs = 0, lastHeartbeatMs = null (no signal) → 'stuck' (D-04 conservative path unchanged).
  </behavior>
  <action>
    In resolveAgentState (agent-liveness.ts), replace the nullish-coalescing window computation with a POSITIVE-value guard: compute the cadence as `typeof expectedCadenceMs === 'number' && expectedCadenceMs > 0 ? expectedCadenceMs : RUNNING_WINDOW_MS`, then `staleWindowMs = 2 * cadence`. This closes WR-03 (a host value of 0 must fall back to RUNNING_WINDOW_MS, not pass through). Separately close WR-04: narrow the return type annotation from `'working' | 'stuck' | null` to `'working' | 'stuck'` (D-04: the helper never returns null itself — callers supply null when assigneeAgentId == null). Update the doc comment that currently says the function may signal null to state the nullability belongs at the call site. Do NOT change the call sites in this plan (flatten-blocker-chain.ts:184 and org-blocked-backlog.ts:289 are Wave 2's WR-03 follow-up); only the helper changes here.
  </action>
  <verify>
    <automated>node --test test/worker/ 2>&1 | Select-String -Pattern "agent-liveness|resolveAgentState|pass|fail"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String -Path src/worker/situation/agent-liveness.ts -Pattern "expectedCadenceMs > 0"` matches (positive-value guard present).
    - `Select-String -Path src/worker/situation/agent-liveness.ts -Pattern "'working' \| 'stuck' \| null"` returns NO match in the resolveAgentState signature (dead | null removed).
    - The new expectedCadenceMs=0 fresh-heartbeat fixture returns 'working' (asserted in the resolveAgentState test).
    - Existing resolveAgentState tests still pass (no regression in the D-04 no-signal → stuck path).
  </acceptance_criteria>
  <done>resolveAgentState falls back to RUNNING_WINDOW_MS when expectedCadenceMs is 0; the return type is 'working' | 'stuck'; a cadence-zero fixture proves the fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix WR-05 EXTERNAL label mis-attribution + export makeDegradedResult (IN-04) + add the 'none' affordance for the blocker-free synthetic case (WR-01 root) — all in blocker-chain.ts</name>
  <files>src/shared/blocker-chain.ts, test/shared/blocker-chain.test.mjs</files>
  <read_first>
    - src/shared/blocker-chain.ts (the file being modified — read the whole file; ~350 lines, already in context)
    - src/shared/types.ts:43-79 (Terminal union + BlockerChainResult + the 'reply'|'nudge'|'assign'|'open'|'none' affordance union)
    - src/worker/handlers/flatten-blocker-chain.ts:210-249 (degraded()/noBlockers() — the boilerplate IN-04 will replace; Wave 2 will adopt makeDegradedResult)
    - test/shared/blocker-chain.test.mjs (the determinism test + AI-token grep guard that MUST stay green; the per-kind cases)
  </read_first>
  <behavior>
    - Test: a leaf whose ONLY outgoing edges are external (only-external-children branch) → terminal.kind 'EXTERNAL' AND result.targetIssueUuid === the leaf id (current) AND terminal.label references the SAME id as targetIssueUuid (no longer the refused child id). FAILS before the fix (label names externalEdge.to), PASSES after.
    - Test: the reached-via-external EXTERNAL branch (unchanged) still names current — both branches now agree.
    - Test: makeDegradedResult(terminal, startId, degradeReason?) returns a BlockerChainResult whose verdict fields match classifyVerdict(terminal) and whose targetIssueUuid === startId, targetAgentUuid === null.
    - Test: classifyVerdict for the blocker-free path returns actionAffordance 'none' for whichever terminal/branch the blocker-free case uses (see action — chosen approach must be exercised).
    - Determinism: the existing 100× JSON.stringify-equality test stays green.
  </behavior>
  <action>
    Three edits to blocker-chain.ts, preserving the exhaustive all-8-kinds switch, the `const _exhaustive: never` guards, and byte-for-byte determinism (no Date.now/new Date, no AI tokens):
    1. WR-05: in the only-external-children EXTERNAL branch (currently labels `External (${externalEdge.to})`), change the label to reference `current` (the leaf / leafId) so it agrees with targetIssueUuid, matching the reached-via-external branch. Drop the now-unused externalEdge local if it becomes dead.
    2. IN-04: export a `makeDegradedResult(terminal: Terminal, startId: string, degradeReason?: string): BlockerChainResult` helper from blocker-chain.ts that internally calls makeResult-equivalent assembly (classifyVerdict, pathIds = startId ? [startId] : [], isStale false, leafId = startId, targetAgentUuid null, targetIssueUuid startId, optional degradeReason). This is the single shared degrade-row constructor so a future verdict field cannot be silently missed by the three hand-built objects in the worker handlers (Wave 2 adopts it). Keep makeResult itself unchanged for the success path.
    3. WR-01 root — blocker-free 'none' affordance: choose the cleanest of: (a) special-case the blocker-free synthetic terminal so its verdict carries actionAffordance 'none', OR (b) introduce a dedicated non-actionable handling for the blocker-free case. Do NOT change classifyVerdict's existing EXTERNAL → 'open' mapping for genuine externals (a real external blocker is still openable). Only the blocker-free synthetic case must resolve to 'none'. Document the chosen approach in a comment citing WR-01. (The worker's noBlockers() in Wave 2 will consume whatever shape you settle on here.)
    Engine purity is non-negotiable: no clock, no DB, no AI/LLM tokens introduced anywhere in this file.
  </action>
  <verify>
    <automated>node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/shared/blocker-chain.test.mjs` passes ALL cases including "Determinism — same input produces same output bytes across 100 invocations" and the "PRIM-03 deterministic-graph-only" AI-token guard.
    - AI-token guard stays green: `Select-String -Path src/shared/blocker-chain.ts -Pattern "(?i)\b(openai|anthropic|claude_local|llm|gpt|completion)\b"` returns NO match.
    - No clock introduced: `Select-String -Path src/shared/blocker-chain.ts -Pattern "Date\.now|new Date"` returns NO match.
    - `Select-String -Path src/shared/blocker-chain.ts -Pattern "export function makeDegradedResult"` matches.
    - For the only-external-children branch, the new test asserts terminal.label and result.targetIssueUuid reference the same node id (WR-05 closed).
    - The blocker-free synthetic case resolves to actionAffordance 'none' (asserted by the new test exercising the chosen approach).
  </acceptance_criteria>
  <done>Both EXTERNAL branches name current; makeDegradedResult is exported; the blocker-free synthetic case carries the 'none' affordance; determinism + AI-token guard stay green.</done>
</task>

<task type="auto">
  <name>Task 3: Renumber scrub-human-action.ts step comments (IN-02)</name>
  <files>src/shared/scrub-human-action.ts</files>
  <read_first>
    - src/shared/scrub-human-action.ts (the file being modified — full file, ~87 lines, already in context; comments label Step 1, 2, 4, 5, 6 — Step 3 missing)
  </read_first>
  <action>
    In scrub-human-action.ts renumber the inline step comments to a contiguous 1-2-3-4-5 sequence (the removed sentinel branch left a gap at Step 3). This is comment-only — do NOT change any branch logic, the UUID regexes, the function signature, or behavior. Closes IN-02 (a reader otherwise hunts for the missing Step 3).
  </action>
  <verify>
    <automated>node --test test/shared/scrub-human-action.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/shared/scrub-human-action.test.mjs` still passes (behavior unchanged).
    - The step-comment sequence in scrub-human-action.ts is contiguous (no skipped number); verify by reading the file — `Select-String -Path src/shared/scrub-human-action.ts -Pattern "Step 3"` now matches (the renumber filled the gap).
    - The function signature `scrubHumanAction(terminal, viewerUserId, nameByUuid)` is unchanged (no diff to the parameter list).
  </acceptance_criteria>
  <done>scrub-human-action.ts step comments are contiguous 1-5; behavior and signature unchanged; the scrub test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine → worker → DOM | Terminal labels constructed here flow through the worker scrub (Wave 2) into the Reader DOM (Wave 3). A label that embeds a raw UUID and skips the scrub is an information-disclosure leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information disclosure | EXTERNAL terminal label (blocker-chain.ts) | mitigate | WR-05 makes the label name `current` consistently; the downstream scrub (Wave 2) replaces any embedded UUID. This plan keeps the label UUID-bearing intentionally (the engine is pure) — the scrub is the boundary mitigation, asserted in 11-06/11-07. |
| T-11-05-02 | Tampering | engine purity (PRIM-03 / SC4) | mitigate | AI-token grep guard + 100× determinism test stay green (acceptance criteria on Task 2). A clock/AI token introduced into blocker-chain.ts fails the build. |
| T-11-05-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this plan; no install tasks. RESEARCH Package Legitimacy gate N/A. |
</threat_model>

<verification>
- `node --test test/shared/blocker-chain.test.mjs` → all pass (determinism + AI-token guard green).
- `node --test test/shared/scrub-human-action.test.mjs` → all pass.
- `node --test test/worker/` → resolveAgentState cadence-zero fixture green.
- `Select-String -Path src/shared/blocker-chain.ts -Pattern "(?i)\b(openai|anthropic|llm|gpt|completion)\b"` → no match (engine purity).
- Repo typecheck: `npx tsc --noEmit` reports zero new errors introduced by the agent-liveness return-type narrowing (callers already supply null themselves).
</verification>

<success_criteria>
WR-03, WR-04, WR-05, IN-02, IN-04 closed, and the blocker-free 'none' affordance plumbing landed — with the determinism test and AI-vendor-token grep guard staying green (SC4). The worker (Wave 2) can now call makeDegradedResult and consume the blocker-free 'none' shape.
</success_criteria>

<output>
Create `.planning/phases/11-honest-blocker-taxonomy-engine/11-05-SUMMARY.md` when done.
</output>
