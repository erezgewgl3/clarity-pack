---
phase: 12-needs-you-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/blocker-chain.ts
  - test/shared/blocker-chain.test.mjs
autonomous: true
requirements: [NY-03]
must_haves:
  truths:
    - "classifyVerdict(AWAITING_AGENT_STUCK) returns actionAffordance 'assign' (was 'nudge')"
    - "AWAITING_AGENT_STUCK stays tier 'watch' and needsYou false (it does NOT enter Needs-you)"
    - "The 'nudge' variant remains in the actionAffordance union (dormant, not deleted)"
    - "blocker-chain.ts stays AI-token-free and deterministic — both guards pass"
  artifacts:
    - path: "src/shared/blocker-chain.ts"
      provides: "Single-source per-kind {tier, actionAffordance, needsYou} table with the D-05 stuck-agent edit"
      contains: "case 'AWAITING_AGENT_STUCK'"
    - path: "test/shared/blocker-chain.test.mjs"
      provides: "Updated determinism-table expectation + AWAITING_AGENT_STUCK affordance assertion"
      contains: "AWAITING_AGENT_STUCK"
  key_links:
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "classifyVerdict"
      via: "import from blocker-chain.ts"
      pattern: "classifyVerdict"
    - from: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      to: "actionAffordance"
      via: "verdict-driven affordance gating"
      pattern: "actionAffordance"
---

<objective>
Make the engine's single-source per-kind table tell the truth about stuck agents: a stuck-agent terminal is a row where assignment (re-owning the issue) is the genuine answer, so its `actionAffordance` becomes `'assign'`. This is the 1-line pure-engine edit (D-05) that every consuming surface reads — the per-kind `{tier, actionAffordance, needsYou}` table stays the single source of truth (Phase 11 D-14), so triage and all three surfaces agree with no second mapping to drift.

Purpose: NY-03 requires the "Assign owner" affordance to appear on genuinely-unowned (UNOWNED) OR stuck-agent (AWAITING_AGENT_STUCK) rows. Expressing it in the engine table — not per-surface — keeps the gate honest and impossible to desync.
Output: An edited `classifyVerdict` AWAITING_AGENT_STUCK row + updated tests; the determinism test + AI-token grep guard stay green.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-needs-you-triage/12-CONTEXT.md
@.planning/phases/11-honest-blocker-taxonomy-engine/11-CONTEXT.md

<interfaces>
<!-- The pure per-kind verdict table (the ONLY thing this plan edits). From src/shared/blocker-chain.ts: -->
<!--
export function classifyVerdict(terminal: Terminal): {
  tier: 'needs-you' | 'in-motion' | 'watch';
  actionAffordance: 'reply' | 'nudge' | 'assign' | 'open' | 'none';
  needsYou: boolean;
}
Current AWAITING_AGENT_STUCK row:  { tier: 'watch', actionAffordance: 'nudge', needsYou: false }
Target  AWAITING_AGENT_STUCK row:  { tier: 'watch', actionAffordance: 'assign', needsYou: false }
-->
<!-- The actionAffordance union in src/shared/types.ts MUST keep 'nudge' (D-06 — dormant, not deleted). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-05 — flip AWAITING_AGENT_STUCK affordance to 'assign' in the per-kind table</name>
  <files>src/shared/blocker-chain.ts, src/shared/types.ts</files>
  <read_first>
    - src/shared/blocker-chain.ts (the file being edited — the `classifyVerdict` switch is the single source of truth; do NOT touch flattenBlockerChain, makeResult, pickTopChains)
    - src/shared/types.ts (the Terminal union + the actionAffordance union — confirm 'nudge' stays in the union per D-06)
    - test/shared/blocker-chain.test.mjs (the determinism + AI-token guard + the all-8-kinds table test you will update)
    - .planning/phases/12-needs-you-triage/12-CONTEXT.md (D-04, D-05, D-06 — the exact decision text)
  </read_first>
  <behavior>
    - classifyVerdict({kind:'AWAITING_AGENT_STUCK', agentId:'a', label:'l'}) returns { tier: 'watch', actionAffordance: 'assign', needsYou: false }.
    - classifyVerdict for the other 7 kinds is UNCHANGED (AWAITING_HUMAN→reply/needs-you/true; UNOWNED→assign/needs-you/true; AWAITING_AGENT_WORKING→none/in-motion/false; SELF_RESOLVING→none/watch/false; EXTERNAL→open/watch/false; CYCLE→open/watch/false; UNCLASSIFIED→open/watch/false).
    - The actionAffordance union in types.ts still contains 'nudge' (D-06 — dormant variant kept).
    - Determinism: flattenBlockerChain over the same input still produces byte-identical JSON across 100 invocations.
    - AI-token grep guard: blocker-chain.ts contains zero LLM/AI vendor tokens.
  </behavior>
  <action>
    In `classifyVerdict` (src/shared/blocker-chain.ts), change ONLY the `case 'AWAITING_AGENT_STUCK':` return so `actionAffordance` is `'assign'` instead of `'nudge'`; leave `tier: 'watch'` and `needsYou: false` exactly as they are (D-04 — stuck stays in Watch, never enters Needs-you). Do not edit any other case, the exhaustive `never` guard, `makeResult`, `makeDegradedResult`, `makeBlockerFreeResult`, `flattenBlockerChain`, or `pickTopChains`. Do NOT remove the `'nudge'` member from the `actionAffordance` union in src/shared/types.ts (D-06 — dormant, not deleted). Update the JSDoc on the AWAITING_AGENT_STUCK case (and the types.ts comment that reads "'nudge' for stuck agents") to state stuck agents now offer 'assign' per D-05, and that 'nudge' is reserved for the Phase 14 reply/nudge loop. Keep the comment factual and free of any AI-vendor token so the grep guard stays green.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the AWAITING_AGENT_STUCK case in classifyVerdict returns actionAffordance 'assign' (grep `grep -n "AWAITING_AGENT_STUCK" src/shared/blocker-chain.ts` shows the case; the return on that case has 'assign').
    - Source assertion: the actionAffordance union in src/shared/types.ts still lists 'nudge' (grep `grep -n "nudge" src/shared/types.ts` returns a match).
    - Test command: `node --test test/shared/blocker-chain.test.mjs` exits 0 with the determinism test, the AI-token grep guard test, and the all-8-kinds table test all passing.
  </acceptance_criteria>
  <done>classifyVerdict returns 'assign' for AWAITING_AGENT_STUCK; 'nudge' remains in the union; determinism + AI-token guards green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the determinism-table test to encode the D-05 stuck-agent affordance</name>
  <files>test/shared/blocker-chain.test.mjs</files>
  <read_first>
    - test/shared/blocker-chain.test.mjs (the file being edited — specifically the 'classifyVerdict — encodes the design-seed Section 1 table for all 8 kinds' test and the 'AWAITING_AGENT_STUCK — agentState missing ⇒ conservative STUCK' test)
    - src/shared/blocker-chain.ts (source of truth the test asserts against — the edited AWAITING_AGENT_STUCK case)
    - .planning/phases/12-needs-you-triage/12-CONTEXT.md (D-05 — the expected triple)
  </read_first>
  <behavior>
    - The all-8-kinds table test expects AWAITING_AGENT_STUCK → { tier: 'watch', actionAffordance: 'assign', needsYou: false } (needsYou stays false).
    - The AWAITING_AGENT_STUCK flatten test asserts result.actionAffordance === 'assign' (was 'nudge'), while result.tier === 'watch' and result.needsYou === false stay unchanged.
    - All other table rows and tests stay byte-identical.
  </behavior>
  <action>
    In test/shared/blocker-chain.test.mjs, update the `table` object inside the 'classifyVerdict — encodes the design-seed Section 1 table for all 8 kinds' test so the `AWAITING_AGENT_STUCK` entry reads `{ tier: 'watch', actionAffordance: 'assign', needsYou: false }`. In the 'AWAITING_AGENT_STUCK — agentState missing ⇒ conservative STUCK (D-04); watch tier + nudge' test, change the affordance assertion from `assert.equal(result.actionAffordance, 'nudge')` to `assert.equal(result.actionAffordance, 'assign')`, leave the `result.tier === 'watch'`, `result.needsYou === false`, and `result.targetAgentUuid` assertions unchanged, and update that test's title to say "watch tier + assign (D-05)". Do not weaken the determinism test, the AI-token grep guard test, or any other assertion. These edits make the tests fail-first if Task 1's table edit is ever reverted (the table test is the gate on the engine edit).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `node --test test/shared/blocker-chain.test.mjs` exits 0.
    - Behavior assertion: the all-8-kinds table test's AWAITING_AGENT_STUCK entry asserts actionAffordance 'assign' (grep `grep -n "AWAITING_AGENT_STUCK" test/shared/blocker-chain.test.mjs` shows the 'assign' expectation).
    - Regression guard: the determinism test ('same input produces same output bytes across 100 invocations') and the PRIM-03 AI-token grep guard test both still pass within the same run.
  </acceptance_criteria>
  <done>The table test + the stuck flatten test assert 'assign'; the suite is green and the engine edit is gated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure-engine ↔ consuming surfaces | classifyVerdict is the single source of truth; every surface reads the verdict triple. A wrong affordance here propagates to every assign-gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | classifyVerdict AWAITING_AGENT_STUCK row | mitigate | The table is the SINGLE source of truth; the all-8-kinds determinism table test (Task 2) gates the exact triple, so a silent revert/typo fails the build. |
| T-12-02 | Information disclosure | blocker-chain.ts purity | mitigate | The PRIM-03 AI-token grep guard test scans the source for LLM/AI vendor tokens; the JSDoc edit must stay token-free. No clock/DB/AI introduced — determinism test stays green. |
| T-12-03 | Elevation of privilege | affordance over-broadens to needs-you | mitigate | needsYou stays false and tier stays 'watch' for AWAITING_AGENT_STUCK; the table test asserts both, so stuck agents can never silently leak into the loud Needs-you list. |

No new fetch, no new mutation, no migration in this plan — it is a pure in-memory table edit. NO_UUID_LEAK is unaffected (classifyVerdict reads only the discriminant, emits no labels).
</threat_model>

<verification>
- `node --test test/shared/blocker-chain.test.mjs` passes (engine table edit + the two updated tests + determinism + AI-token guard).
- `grep -n "AWAITING_AGENT_STUCK" src/shared/blocker-chain.ts` shows the case returning 'assign'.
- `grep -n "nudge" src/shared/types.ts` confirms the 'nudge' union member is retained (D-06).
</verification>

<success_criteria>
- classifyVerdict(AWAITING_AGENT_STUCK) → { tier: 'watch', actionAffordance: 'assign', needsYou: false }.
- 'nudge' variant retained in the union (dormant).
- Determinism test + AI-token grep guard green.
- No other engine behavior changed.
</success_criteria>

<output>
Create `.planning/phases/12-needs-you-triage/12-01-SUMMARY.md` when done.
</output>
