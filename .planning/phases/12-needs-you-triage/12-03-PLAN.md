---
phase: 12-needs-you-triage
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/worker/handlers/org-blocked-backlog.ts
  - src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts
  - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/ui/surfaces/situation-room/employee-row.tsx
  - test/worker/org-blocked-backlog.test.mjs
autonomous: true
requirements: [NY-03]
must_haves:
  truths:
    - "The 'Assign owner' affordance appears ONLY on UNOWNED (genuinely-unowned) and AWAITING_AGENT_STUCK (stuck-agent) rows on EVERY surface — Situation Room, Reader blocker panel, and org-blocked backlog (D-09)"
    - "Assign NEVER appears on AWAITING_HUMAN, AWAITING_AGENT_WORKING, SELF_RESOLVING, EXTERNAL, CYCLE, or UNCLASSIFIED rows"
    - "Each surface gates assign off the engine actionAffordance === 'assign', never a terminal.kind list or an ownerName string-match"
    - "The org-blocked backlog carries actionAffordance per row so the expander can gate the OwnerPickerPopover (previously it rendered Assign unconditionally on every orphan row)"
  artifacts:
    - path: "src/worker/handlers/org-blocked-backlog.ts"
      provides: "OrgBlockedRow now carries actionAffordance from the engine verdict"
      contains: "actionAffordance"
    - path: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      provides: "OwnerPickerPopover rendered ONLY when row.actionAffordance === 'assign'"
      contains: "actionAffordance"
    - path: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      provides: "'assign' affordance gated to UNOWNED/stuck — assign label shows only for the assign affordance"
      contains: "actionAffordance"
  key_links:
    - from: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      to: "OwnerPickerPopover"
      via: "conditional render gated on actionAffordance === 'assign'"
      pattern: "actionAffordance === 'assign'"
    - from: "src/worker/handlers/org-blocked-backlog.ts"
      to: "chain.actionAffordance"
      via: "engine verdict carried onto the row"
      pattern: "actionAffordance"
---

<objective>
Enforce NY-03 on EVERY surface that shows an assign affordance (D-09): the "Assign owner" control appears ONLY on genuinely-unowned (UNOWNED) and stuck-agent (AWAITING_AGENT_STUCK) rows — never on AWAITING_HUMAN or UNCLASSIFIED (or any other kind). Each surface gates strictly off the engine `actionAffordance === 'assign'` (which, after 12-01, fires for exactly UNOWNED + AWAITING_AGENT_STUCK), never a terminal.kind list and never an ownerName string-match. Three surfaces read the same verdict (D-09):
1. Situation Room employee-row — already gates `showAssign = chain?.actionAffordance === 'assign'`; verify it now correctly shows Assign on stuck-agent rows too (since 12-01 made stuck → 'assign') and STILL never on AWAITING_HUMAN.
2. Reader-view blocker panel — currently renders NO button for 'assign' (no picker mounted). Make the 'assign' affordance honest there: it must show the assign label gated to assign-only rows (and, since there is no OwnerPickerPopover mounted on the Reader panel, route the assign action to the issue page so it is not a dead button — no Send/Reply leaking onto an assign row).
3. Org-blocked backlog expander — currently renders OwnerPickerPopover on EVERY orphan row unconditionally. Gate it on the engine verdict: render the picker ONLY when the row's actionAffordance === 'assign'; other rows render their honest non-assign action (Open ↗) instead.

Purpose: close the D-09 gap so a stale/wrong verdict can never expose an Assign action that mutates an issue inappropriately, and so the three surfaces agree by construction.
Output: org-blocked backlog rows carry actionAffordance; the expander + Reader panel gate assign; the SR row is verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-needs-you-triage/12-CONTEXT.md
@src/worker/handlers/org-blocked-backlog.ts
@src/ui/surfaces/situation-room/blocked-backlog-expander.tsx
@src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts
@src/ui/surfaces/situation-room/employee-row.tsx
@src/ui/surfaces/reader/live-blocker-panel.tsx
@src/ui/surfaces/situation-room/owner-picker-popover.tsx

<interfaces>
<!-- Engine verdict (BlockerChainResult): actionAffordance: 'reply'|'nudge'|'assign'|'open'|'none'.
     After 12-01, actionAffordance === 'assign' ⇔ terminal.kind ∈ {UNOWNED, AWAITING_AGENT_STUCK}. -->
<!-- OrgBlockedRow (org-blocked-backlog.ts + org-blocked-backlog-banner-types.ts) TODAY carries:
  { issueId, identifier, title, humanAction, terminalKind, ownerName, ownerAgentId, age_ms }
  It does NOT carry actionAffordance — this plan ADDS it from chain.actionAffordance. -->
<!-- The chain the org backlog builds per row already has chain.actionAffordance (from classifyVerdict inside flattenBlockerChain / unclassifiedChain). -->
<!-- employee-row.tsx already: const showAssign = chain?.actionAffordance === 'assign'; (verdict-gated, no change needed except verifying the stuck case). -->
<!-- live-blocker-panel.tsx primaryActionLabel maps 'assign' → 'Assign owner ▾' but onAction for 'assign' is currently null (no button). -->
<!-- OwnerPickerPopover mount in blocked-backlog-expander.tsx is UNCONDITIONAL — the D-09 gap. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry the engine actionAffordance onto OrgBlockedRow (worker + UI type)</name>
  <files>src/worker/handlers/org-blocked-backlog.ts, src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts, test/worker/org-blocked-backlog.test.mjs</files>
  <read_first>
    - src/worker/handlers/org-blocked-backlog.ts (the file being edited — the OrgBlockedRow type ~line 109 and the rows.push(...) emit ~line 481; the chain.actionAffordance is already computed inside flattenBlockerChain / unclassifiedChain)
    - src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts (the UI-side mirror type — must add the same field so the expander can read it)
    - src/shared/types.ts (BlockerChainResult.actionAffordance — the union the field is typed against)
    - test/worker/org-blocked-backlog.test.mjs (the file being extended — read existing row-shape assertions so you add an actionAffordance assertion without breaking them)
    - .planning/phases/12-needs-you-triage/12-CONTEXT.md (D-09 — assign on UNOWNED + stuck only)
  </read_first>
  <behavior>
    - Test: an org-backlog row whose chain terminal is UNOWNED carries actionAffordance 'assign'.
    - Test: an org-backlog row whose chain terminal is AWAITING_AGENT_STUCK carries actionAffordance 'assign' (post-12-01).
    - Test: an org-backlog row whose chain terminal is AWAITING_HUMAN carries actionAffordance 'reply' (NOT 'assign').
    - Test: an UNCLASSIFIED degrade row carries actionAffordance 'open' (NOT 'assign').
    - The existing OrgBlockedRow fields (issueId, identifier, title, humanAction, terminalKind, ownerName, ownerAgentId, age_ms) are unchanged.
  </behavior>
  <action>
    In src/worker/handlers/org-blocked-backlog.ts, add `actionAffordance: BlockerChainResult['actionAffordance']` to the `OrgBlockedRow` type, and in the `rows.push({...})` emit set `actionAffordance: chain.actionAffordance` (the verdict is already on the chain via classifyVerdict — no new compute, no new fetch). Mirror the same `actionAffordance` field addition in src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts's `OrgBlockedRow` (typed off the shared `BlockerChainResult['actionAffordance']`, same as the worker, so a 6th affordance is a compile error in both). Do NOT change terminalKind, the need_you_count logic, or the scrub. Extend test/worker/org-blocked-backlog.test.mjs with the four affordance assertions above (UNOWNED→assign, stuck→assign, AWAITING_HUMAN→reply, UNCLASSIFIED→open).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/org-blocked-backlog.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `node --test test/worker/org-blocked-backlog.test.mjs` exits 0 with the four new affordance assertions.
    - Source assertion: OrgBlockedRow carries actionAffordance in BOTH org-blocked-backlog.ts and org-blocked-backlog-banner-types.ts (grep both files for `actionAffordance`).
    - Behavior assertion: UNOWNED and AWAITING_AGENT_STUCK rows → 'assign'; AWAITING_HUMAN → 'reply'; UNCLASSIFIED → 'open'.
  </acceptance_criteria>
  <done>OrgBlockedRow carries the engine actionAffordance on both the worker and UI sides; tests assert the four kinds.</done>
</task>

<task type="auto">
  <name>Task 2: Gate the assign affordance on all three surfaces off actionAffordance === 'assign'</name>
  <files>src/ui/surfaces/situation-room/blocked-backlog-expander.tsx, src/ui/surfaces/reader/live-blocker-panel.tsx, src/ui/surfaces/situation-room/employee-row.tsx</files>
  <read_first>
    - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx (the file being edited — the unconditional OwnerPickerPopover mount ~line 91 is the D-09 gap to gate)
    - src/ui/surfaces/reader/live-blocker-panel.tsx (the file being edited — primaryActionLabel + the onAction switch where 'assign' currently resolves to null/no-button)
    - src/ui/surfaces/situation-room/employee-row.tsx (the file being edited — VERIFY `showAssign = chain?.actionAffordance === 'assign'` now correctly covers stuck-agent rows and never AWAITING_HUMAN; adjust only the comment/behavior if a stuck row needs the Open-leaf affordance alongside assign)
    - src/ui/surfaces/situation-room/owner-picker-popover.tsx (the picker the expander gates — its props: leafIssueId/leafIssueUuid/companyId/userId/onAssigned)
    - .planning/phases/12-needs-you-triage/12-CONTEXT.md (D-09 enforce on EVERY surface; assign only on UNOWNED + stuck)
    - CLAUDE.md (NO_UUID_LEAK — owner ids consumed only as dispatch args, never rendered)
  </read_first>
  <action>
    Org-blocked backlog (blocked-backlog-expander.tsx): render the `<OwnerPickerPopover>` ONLY when `row.actionAffordance === 'assign'` (the engine verdict from Task 1). For rows whose affordance is NOT 'assign', do NOT render the picker — render the honest non-assign action instead (keep the existing [Open ↗] button for all rows; the picker is the conditional part). This closes the D-09 gap where every orphan row showed [Assign ▾] regardless of whether assignment was the answer.
    Reader blocker panel (live-blocker-panel.tsx): the 'assign' affordance must be honest, not dead. Keep `primaryActionLabel` mapping 'assign' → 'Assign owner ▾'. In the onAction switch, wire 'assign' to navigate to the leaf issue page (`/<companyPrefix>/issues/<issueId>` via the existing openIssue handler / paperclip-issue-url-pattern) so the operator can assign an owner there — NOT null (which renders no button) and NEVER falling through to reply/nudge. The assign label/button must appear ONLY when actionAffordance === 'assign'; every other affordance keeps its existing wired handler (reply/nudge/open) or renders no button ('none'). Do NOT mount the OwnerPickerPopover on the Reader panel (it is not wired for the popover's roster/dispatch here); the navigate-to-issue route is the honest assign affordance for this surface.
    Situation Room employee-row (employee-row.tsx): VERIFY `showAssign = chain?.actionAffordance === 'assign'` correctly shows the OwnerPickerPopover on stuck-agent rows (now affordance 'assign') and never on AWAITING_HUMAN. The stuck-agent row buckets in the 'watch' tier (not needs_you), so confirm the employee-row's needs_you-gated assign cluster does not need duplication for watch rows here — if stuck-agent rows render through a different group branch, ensure the assign affordance is consistently gated off actionAffordance there too; if the current people-view does not render watch-tier rows with an assign control yet, leave the group/tier rendering to Phase 15 (D-routing) and only ensure that WHEREVER an assign control renders on this surface, it is gated on actionAffordance === 'assign'. Update the comment to cite D-05/D-09 (assign now also fires for stuck agents). Across all three surfaces: NO terminal.kind list, NO ownerName string-match — gate ONLY on actionAffordance === 'assign'. UUIDs stay dispatch-only (NO_UUID_LEAK).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: blocked-backlog-expander.tsx renders OwnerPickerPopover inside a `row.actionAffordance === 'assign'` guard (grep `grep -n "actionAffordance === 'assign'" src/ui/surfaces/situation-room/blocked-backlog-expander.tsx` returns a match wrapping the OwnerPickerPopover).
    - Source assertion: live-blocker-panel.tsx onAction switch case 'assign' resolves to a non-null handler (navigate to the issue page), and the 'assign' label renders only for the assign affordance (grep the file shows case 'assign' wired to openIssue, not null).
    - Source assertion: every assign-gate across the three files reads `actionAffordance === 'assign'` — NO `terminal.kind ===` list and NO `ownerName ===` string-match gates an assign control (grep the three files for `ownerName ===` and `terminalKind ===` near assign controls returns nothing).
    - Build assertion: `npm run build` completes with no TypeScript errors (the OrgBlockedRow actionAffordance field flows through both worker and UI types).
    - NO_UUID_LEAK: no raw UUID is interpolated into a rendered text node on any of the three surfaces (owner/agent/issue UUIDs stay dispatch args).
  </acceptance_criteria>
  <done>OwnerPickerPopover gated to assign-only rows on the backlog; Reader 'assign' is a live navigate (no dead/leaking button); SR row verified; all assign gates read the engine verdict; build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine verdict ↔ assign-rendering surfaces | The Assign control mutates a real Paperclip issue (situation.assignOwner / navigate-to-assign). A surface that renders Assign on a wrong row exposes an inappropriate mutation. |
| UI ↔ Paperclip HTTP (same-origin trust) | Plugin UI is trusted same-origin JS; the only guardrail against a wrong assign is gating the affordance on the honest engine verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Elevation of privilege | OwnerPickerPopover on org-backlog rows | mitigate | The picker (which dispatches situation.assignOwner, a real mutation) renders ONLY when actionAffordance === 'assign'; closing the prior unconditional mount so an AWAITING_HUMAN / in-motion / external row can no longer expose an assign mutation. |
| T-12-09 | Tampering | per-surface assign gate divergence | mitigate | All three surfaces gate off the SINGLE engine actionAffordance (D-09) — no terminal.kind list or ownerName string-match — so a stale/wrong verdict cannot be reinterpreted differently per surface. |
| T-12-10 | Information disclosure | owner/issue UUIDs on assign controls | mitigate | UUIDs (leafIssueUuid / ownerAgentId / targetIssueUuid) stay dispatch-only args; the rendered labels are the scrubbed humanAction / human identifier (NO_UUID_LEAK preserved — verified by the build + render-scan posture inherited from Phase 11). |
| T-12-11 | Spoofing | Reader 'assign' dead/leaking button | mitigate | The Reader 'assign' affordance is wired to navigate to the issue page (a real effect), never null (dead) and never falls through to reply/nudge — no dead Send button, no wrong action on an assign row. |

No new fetch, no new mutation surface, no migration in this plan — it re-gates existing affordances over already-fetched verdict data (D-10). The assign mutation itself (situation.assignOwner) is the pre-existing Phase 9 handler, unchanged.
</threat_model>

<verification>
- `node --test test/worker/org-blocked-backlog.test.mjs` passes (OrgBlockedRow carries actionAffordance for the four kinds).
- `npm run build` is green (the new field flows through worker + UI types).
- grep confirms every assign gate reads `actionAffordance === 'assign'`; no terminal.kind list / ownerName string-match gates an assign control.
- OwnerPickerPopover renders only inside the assign guard on the backlog expander.
- Reader 'assign' is a live navigate, not null.
</verification>

<success_criteria>
- NY-03 / D-09: Assign appears ONLY on UNOWNED + AWAITING_AGENT_STUCK rows on the Situation Room, the Reader blocker panel, and the org-blocked backlog; never on AWAITING_HUMAN / working / self-resolving / external / cycle / unclassified.
- Every gate reads the engine actionAffordance, not a kind list or string-match.
- No dead or leaking assign button; NO_UUID_LEAK preserved.
- Build green.
</success_criteria>

<output>
Create `.planning/phases/12-needs-you-triage/12-03-SUMMARY.md` when done.
</output>
