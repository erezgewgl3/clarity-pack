---
phase: 13-editor-agent-named-action
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/worker/agents/action-cards.ts
  - src/worker/agents/agent-task-delivery.ts
  - src/worker/handlers/situation-room.ts
  - test/worker/agents/action-cards.test.mjs
autonomous: true
requirements: [ACT-01, ACT-02, ACT-03]
must_haves:
  truths:
    - "driveActionCardsStep generates a grounded action card (named action + party + estimate bucket + optional decisionOptions) ONLY for needsYou===true rows, via the Editor-Agent operation-issue handoff — never inventing urgency."
    - "The step NEVER throws: every host call is wrapped and a hiccup yields a non-error status, so the SR snapshot still returns."
    - "A stale (content-hash mismatch OR generated_at age > 10 min) or absent card resolves to no card on that row, so the UI degrades to the deterministic engine line."
    - "OperationKind includes 'action-cards'; generation runs as the same managed Editor-Agent (governance parity, circuit breaker) — no new cron, no direct LLM call, no blocker-chain.ts edit."
  artifacts:
    - path: "src/worker/agents/action-cards.ts"
      provides: "driveActionCardsStep — 1:1 mirror of driveBulletinGlossStep, emitting a {sourceIssueId -> ActionCard fields} map"
      exports: ["driveActionCardsStep"]
      min_lines: 120
    - path: "src/worker/agents/agent-task-delivery.ts"
      provides: "'action-cards' added to the OperationKind union"
      contains: "action-cards"
    - path: "src/worker/handlers/situation-room.ts"
      provides: "driveActionCardsStep wired into the situation.snapshot data handler after buildEmployeesRollup; per-row actionCard attached to the payload"
      contains: "driveActionCardsStep"
  key_links:
    - from: "src/worker/handlers/situation-room.ts"
      to: "src/worker/agents/action-cards.ts"
      via: "driveActionCardsStep(ctx, {companyId, needsYouRows}) after buildEmployeesRollup, degrade-safe"
      pattern: "driveActionCardsStep"
    - from: "src/worker/agents/action-cards.ts"
      to: "src/worker/db/action-cards-repo.ts"
      via: "upsertActionCard / getActionCardBySource (per-leaf cache, content-hash idempotency)"
      pattern: "upsertActionCard|getActionCardBySource"
---

<objective>
Build the Editor-Agent action-card generation step as a 1:1 structural mirror of driveBulletinGlossStep, add the 'action-cards' OperationKind, and wire it into the situation.snapshot data handler (the valid-scope 60s recompute) so each needsYou row gets a grounded named action — with hard anti-fabrication + stale-degrade guardrails.

Purpose: Satisfies ACT-01 (named action + party + estimate generated by the Editor-Agent), ACT-02 (stale/absent → degrade, never throw/blank/fabricate), ACT-03 (annotate ONLY engine-flagged rows; conservative binary detection). Reuses the grounded-summary pipeline verbatim per SC4 with governance parity (D-04..D-06).
Output: action-cards.ts step, 'action-cards' OperationKind, situation-room.ts wiring, unit test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-editor-agent-named-action/13-CONTEXT.md
@CLAUDE.md
@.planning/phases/13-editor-agent-named-action/13-01-SUMMARY.md

<interfaces>
<!-- The step's template + the pipeline primitives it reuses verbatim. Use these directly — no codebase scavenging. -->

From src/worker/bulletin/bulletin-gloss.ts (THE template — driveActionCardsStep is a 1:1 structural mirror):
- Skeleton: build a content-hash → CACHE CHECK (short-circuit on match) → resolveEditorAgentId (null → 'unavailable' status, no card) → READ-BACK an existing done op BEFORE spawning (consume-before-spawn BUG-2 fix) → PAUSED check (no auto-resume) → startAgentTask + ONE pollAgentTaskResult → ready: parse JSON map defensively, finalize into cache, mark op done. NEVER throws — every host call wrapped, hiccup → status only.
- Status union pattern: 'glossed' | 'compiling' | 'paused' | 'unavailable' — mirror as 'ready' | 'compiling' | 'paused' | 'unavailable'.
- Operation id helper `glossOperationId(cycleNumber)` → mirror as `action-cards-<companyId>` (per-company per recompute; Claude's-discretion D scoping).
- `parseGlossMap(body)` defensive JSON parse → mirror as a map parser that yields {sourceIssueId -> {namedAction, awaitedParty, estBucket, actionKind, decisionOptions?}}.

From src/worker/agents/editor.ts:
- `resolveEditorAgentId(ctx, companyId): Promise<string|null>` — op-issue agent discovery, no dead reconcile.
- `EDITOR_AGENT_KEY = 'editor-agent'`.
- `handleEditorHeartbeat` — the secondary-trigger heartbeat path (D-06 secondary; OPTIONAL this phase — see Task 3).
- `TldrViewDriverCtx` — the ctx shape (the BulletinGlossCtx = TldrViewDriverCtx & CompileTldrCtx).

From src/worker/agents/compile-tldr.ts:
- `tldrContentHash({surface, scopeId, inputs:{body, comments, refs}}): string` — the staleness-hash recipe (deterministic, no clock). Use for the content_hash over the needsYou verdict fingerprint + issue-revision inputs (D-11).
- `finalizeTldr(ctx, {...})` — validate + circuit breaker + cache write (the gloss step finalizes the raw body into tldr_cache; here finalize into action_cards via the new repo, applying the same try/catch-degrade discipline).
- `polishTldr(input): string` — deterministic ISO→human-date + jargon polish; apply to named_action for voice parity (D-spec, Claude's-discretion prompt-voice).
- `CompileTldrCtx`.

From src/worker/agents/agent-task-delivery.ts:
- `export type OperationKind = 'bulletin-compile' | 'tldr-compile' | 'bulletin-gloss'` (line 93) → ADD 'action-cards'.
- `startAgentTask(ctx, {agentId, companyId, operationKind, operationId, title, prompt})`, `pollAgentTaskResult(ctx, {operationIssueId, companyId, operationKind, agentId})`, `OPERATION_ORIGIN_KIND_PREFIX`, `RESULT_DOCUMENT_KEY = 'compile-result'`.

From src/worker/db/action-cards-repo.ts (plan 13-01):
- `upsertActionCard(ctx, row)`, `getActionCardBySource(ctx, companyId, sourceIssueId)`.

From src/worker/situation/build-employees-rollup.ts (the input — read-only):
- The needsYou subset: rows where `blockerChain.needsYou === true`. Each carries `blockerChain.targetIssueUuid` (the LEAF UUID = source_issue_id key, D-03), `blockerChain.awaitedPartyLabel` (scrubbed display party — grounding input), `blockerChain.humanAction` (the deterministic degrade line), `blockerChain.leafIssueId` (human identifier), `blockerChain.actionAffordance` (engine affordance → action_kind mapping, D-discretion).

From src/worker/handlers/situation-room.ts (the wire point):
- The situation.snapshot data handler computes `employees` + `needsYou` via buildEmployeesRollup, then returns {org_blocked_backlog, situation_employees, needsYou, taken_at}. driveActionCardsStep slots in AFTER buildEmployeesRollup, degrade-safe (try/catch → no cards), and the per-row actionCard is attached before return.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 'action-cards' OperationKind + write driveActionCardsStep (graceful, grounded, anti-fabrication)</name>
  <read_first>
    src/worker/bulletin/bulletin-gloss.ts (THE 1:1 template — copy the skeleton),
    src/worker/agents/editor.ts (resolveEditorAgentId, EDITOR_AGENT_KEY, TldrViewDriverCtx),
    src/worker/agents/compile-tldr.ts (tldrContentHash, finalizeTldr, polishTldr, CompileTldrCtx),
    src/worker/agents/agent-task-delivery.ts (OperationKind union line 93, startAgentTask/pollAgentTaskResult/RESULT_DOCUMENT_KEY),
    src/worker/db/action-cards-repo.ts (upsertActionCard/getActionCardBySource),
    src/shared/types.ts (ActionCard),
    src/worker/situation/build-employees-rollup.ts (the needsYou row fields used as generation input + the degrade line),
    .planning/phases/13-editor-agent-named-action/13-CONTEXT.md (D-04..D-12, D-15)
  </read_first>
  <action>
    In src/worker/agents/agent-task-delivery.ts add the value 'action-cards' to the OperationKind union (now four values). Create src/worker/agents/action-cards.ts exporting `driveActionCardsStep(ctx, {companyId, needsYouRows})` as a 1:1 structural mirror of driveBulletinGlossStep. needsYouRows is the engine-flagged subset (each item: sourceIssueId = blockerChain.targetIssueUuid, awaitedPartyLabel, leafIssueId, humanAction, actionAffordance, plus the source issue body/comments/refs the prompt grounds in). Per recompute, emit ONE Editor-Agent operation producing a STRICT-JSON map keyed by sourceIssueId → {namedAction, awaitedParty, estBucket, actionKind, decisionOptions?}. Steps, all wrapped so the function NEVER throws (return a status, never an exception):
    1. If needsYouRows is empty → return {cards: {}, status: 'ready'}.
    2. Compute a content_hash via tldrContentHash over a canonical fingerprint of the needsYou verdict set (sorted sourceIssueIds + each row's awaitedPartyLabel + actionAffordance + issue-revision inputs) — deterministic, no clock (D-11 correctness arm).
    3. CACHE CHECK: for each row, getActionCardBySource(companyId, sourceIssueId); a row whose cached content_hash equals the recomputed hash AND whose generated_at is within 10 minutes of now is FRESH → use it (D-11). Stale/absent rows are the compile set.
    4. If all rows fresh → return cached cards, status 'ready'.
    5. resolveEditorAgentId; null → status 'unavailable', no cards (UI degrades).
    6. READ-BACK an existing done op for operationId `action-cards-<companyId>` (consume-before-spawn) before spawning; a ready body → parse + finalize into action_cards + return.
    7. PAUSED check (no auto-resume on a passive view) → status 'paused', no cards.
    8. startAgentTask with operationKind 'action-cards', the grounding prompt (Task 2), + ONE pollAgentTaskResult. Pending → status 'compiling', no cards.
    9. Ready → parse the JSON map defensively; for each emitted entry, run named_action + awaited_party through the existing UUID-strip/scrub guard (belt-and-suspenders NO_UUID_LEAK, D-10), normalize est_bucket to {quick,focused,deep} (garbage/missing → omit estimate, D-09), default decisionOptions to null unless an explicit binary array is present (D-08), derive action_kind from the engine actionAffordance (reply→answer, assign→assign, else map per D-discretion); upsertActionCard each row (content_hash, compiled_by_agent_id = the resolved Editor-Agent id, source_revisions parity). Mark the op done best-effort. Return the cards map + status 'ready'. A finalize/validation hiccup degrades to no-card for that row, never throws.
    Define the step ctx type as `TldrViewDriverCtx & CompileTldrCtx & {db}` (mirror BulletinGlossCtx). DO NOT import or modify src/shared/blocker-chain.ts (read its verdict only via the rollup rows passed in). Instance-agnostic — no company-prefix literal in the prompt or code.
  </action>
  <acceptance_criteria>
    - src/worker/agents/agent-task-delivery.ts OperationKind union includes the literal `'action-cards'` (grep `action-cards` in the union line).
    - src/worker/agents/action-cards.ts exports `driveActionCardsStep`; its return type is a discriminated status union including 'ready' | 'compiling' | 'paused' | 'unavailable' (no throw path).
    - action-cards.ts imports resolveEditorAgentId (editor.ts), tldrContentHash + finalizeTldr (compile-tldr.ts), upsertActionCard/getActionCardBySource (action-cards-repo.ts), startAgentTask/pollAgentTaskResult (agent-task-delivery.ts).
    - action-cards.ts does NOT import from src/shared/blocker-chain.ts (grep returns zero) — the engine stays read-only and untouched.
    - The 10-minute staleness predicate is present (a literal `10 * 60 * 1000` or named constant) AND a content-hash equality check (D-11).
    - The decisionOptions default is null and is set non-null ONLY when an array is parsed (D-08); est_bucket normalization restricts to quick|focused|deep.
    - `node scripts/build-worker.mjs` compiles.
    - The blocker-chain determinism + AI-token grep guard stays green: `node --test test/shared/blocker-chain.test.mjs` passes (blocker-chain.ts unchanged).
  </acceptance_criteria>
  <verify>
    <automated>node scripts/build-worker.mjs && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <done>'action-cards' OperationKind added; driveActionCardsStep mirrors the gloss step, never throws, grounds only on needsYou rows, degrades stale/absent to no-card, leaves blocker-chain.ts untouched; worker build + engine guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grounding prompt + pure helpers (bucket normalizer, binary detector, staleness predicate) with unit tests; wire into situation.snapshot + heartbeat</name>
  <read_first>
    src/worker/bulletin/bulletin-gloss.ts (buildGlossPrompt STRICT-JSON instance-agnostic voice — the prompt template),
    src/worker/agents/compile-tldr.ts (buildPrompt Editorial Desk voice + polishTldr),
    src/worker/handlers/situation-room.ts (the situation.snapshot handler wire point + the degrade-safe try/catch pattern),
    src/worker/situation/build-employees-rollup.ts (needsYouRows derivation + per-row fields),
    src/worker/agents/editor.ts (handleEditorHeartbeat secondary-trigger path),
    .planning/phases/13-editor-agent-named-action/13-CONTEXT.md (D-06 wiring, D-07 grounding, D-08 conservative binary, D-09 bucket, D-11 staleness, D-12 degrade)
  </read_first>
  <behavior>
    - normalizeEstBucket('quick'|'focused'|'deep') returns the same; any other/empty/garbage returns null (omit estimate, never a fake number — D-09).
    - parseDecisionOptions: returns a non-empty string[] ONLY when given an array of >=2 short strings; returns null for non-arrays, single-element, or open-ended input (conservative binary — D-08).
    - isActionCardFresh(card, recomputedHash, nowMs): true ONLY when card.content_hash === recomputedHash AND (nowMs - Date.parse(card.generated_at)) <= 10*60*1000; false on hash mismatch OR age > 10 min OR null card (D-11). Pure — clock injected, never read internally.
    - The grounding prompt string instructs STRICT JSON, Editorial-Desk voice, "describe only what the issue says; do NOT invent deadlines, urgency, or parties not named in the source", emit decisionOptions ONLY for an explicit binary, no UUIDs/internal ids, and contains NO company-prefix literal.
    - situation.snapshot handler: after buildEmployeesRollup, driveActionCardsStep is called inside a try/catch so a throw leaves employees/needsYou/backlog intact (degrade-safe); fresh cards attach per row, stale/absent rows carry actionCard:null.
  </behavior>
  <action>
    In src/worker/agents/action-cards.ts add and export the pure helpers: `normalizeEstBucket`, `parseDecisionOptions`, `isActionCardFresh` (clock injected), and `buildActionCardPrompt(needsYouRows)` (the grounding prompt mirroring buildGlossPrompt's STRICT-JSON + the compile-tldr Editorial Desk voice; instance-agnostic; grounded in each row's source issue body/title/comments + the scrubbed awaitedPartyLabel; the anti-fabrication + conservative-binary + no-UUID instructions per D-07/D-08/D-10). Apply polishTldr to the named_action before caching (voice parity). In src/worker/handlers/situation-room.ts: after the buildEmployeesRollup block, derive the needsYou row subset (rows where blockerChain?.needsYou === true) and call driveActionCardsStep inside a try/catch (mirror the existing degrade-safe wrappers — a throw logs a warn and leaves the snapshot intact). Attach the resulting card per employee row as an optional `actionCard: ActionCard | null` field on the row's payload (fresh card → the ActionCard; stale/absent/degrade → null so the UI falls back to the deterministic line, D-12). SECONDARY TRIGGER (D-06, planner-elected to SHIP this phase for parity): in handleEditorHeartbeat (editor.ts), after the existing TL;DR compile loop, best-effort invoke driveActionCardsStep for the heartbeat's company using the same valid-scope pattern, wrapped so a failure is logged and never propagates (heartbeat must remain best-effort; no auto-resume). Write test/worker/agents/action-cards.test.mjs (node --test) covering: normalizeEstBucket (valid + garbage→null), parseDecisionOptions (binary→array, open-ended→null, single→null), isActionCardFresh (fresh, hash-mismatch→stale, age>10min→stale, null→stale), buildActionCardPrompt (contains the anti-fabrication + no-UUID instructions, no company-prefix literal), and a driveActionCardsStep smoke with a fake ctx asserting: empty needsYouRows → status 'ready' no cards; a thrown host call yields a status (never throws); a paused agent → status 'paused'.
  </action>
  <acceptance_criteria>
    - normalizeEstBucket, parseDecisionOptions, isActionCardFresh, buildActionCardPrompt are exported from action-cards.ts.
    - isActionCardFresh enforces BOTH the hash-equality arm AND the 10-minute age arm (the test asserts each independently fails freshness).
    - parseDecisionOptions returns null for open-ended/single-element input and a string[] only for an explicit >=2-element binary (the test asserts conservative behavior — D-08/ACT-03).
    - buildActionCardPrompt output contains an anti-fabrication instruction (e.g. "do NOT invent") AND a no-UUID instruction AND no company-prefix literal (e.g. no 'BEAAA').
    - situation-room.ts calls driveActionCardsStep within a try/catch; a thrown step leaves situation_employees/needsYou/org_blocked_backlog populated (the existing handler test still passes).
    - The per-row payload carries an optional `actionCard` field (ActionCard | null).
    - handleEditorHeartbeat invokes driveActionCardsStep best-effort, wrapped so it never propagates.
    - `node --test test/worker/agents/action-cards.test.mjs` passes; `node --test test/worker/situation-room-handler.test.mjs` passes; `node scripts/build-worker.mjs` compiles.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/build-worker.mjs && node --test test/worker/agents/action-cards.test.mjs test/worker/situation-room-handler.test.mjs</automated>
  </verify>
  <done>Pure helpers + grounding prompt unit-verified (anti-fabrication, conservative binary, dual-arm staleness, coarse bucket); driveActionCardsStep wired degrade-safe into situation.snapshot + the heartbeat; per-row actionCard attached; handler test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issue content (untrusted) → Editor-Agent prompt | Source issue body/title/comments are attacker-influenceable text injected into the LLM prompt |
| Editor-Agent output → operator display | AI-generated named_action/awaited_party rendered to the operator (plan 13-03) |
| worker → host via MCP/operation-issue | Editor-Agent READS via the operation-issue handoff and WRITES only to its own plugin namespace — no new mutation surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Spoofing/Repudiation | AI fabricating a false action or urgency that misleads the operator | mitigate | Prompt grounds only on issue text + scrubbed party; annotates ONLY needsYou===true rows (engine decides urgency, ACT-03); stale/absent → degrade to deterministic line (ACT-02), never invent (T-13 anti-fabrication is the core mitigation) |
| T-13-06 | Tampering (prompt injection) | issue content escalating into a host mutation | mitigate | The step only READS issue inputs and WRITES cards to the plugin namespace; no mutation primitive is reachable from card generation; governance parity (budget caps / pause / circuit breaker) bounds the agent |
| T-13-07 | Information disclosure | raw UUID leaking into named_action/awaited_party | mitigate | Prompt forbids UUIDs; persisted strings run through the UUID-strip/scrub guard before cache (D-10); render-scan guard extended in plan 13-03 |
| T-13-08 | Denial of Service | a slow/throwing Editor-Agent stalling the SR snapshot | mitigate | driveActionCardsStep NEVER throws (every host call wrapped → status); situation.snapshot wraps the call in try/catch; the deterministic line always renders |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in this phase (reuses existing SDK + MCP deps already pinned/audited); no install task introduced |
</threat_model>

<verification>
- driveActionCardsStep never throws; stale/absent → no card → deterministic-line fallback (ACT-02).
- Generation grounds only on needsYou===true rows (ACT-03); conservative binary detection (decisionOptions default null).
- 'action-cards' OperationKind added; governance parity (same Editor-Agent, circuit breaker); no new cron, no direct LLM call.
- blocker-chain.ts untouched; determinism + AI-token grep guard green.
- Worker build green.
</verification>

<success_criteria>
- ACT-01: each needsYou row's grounded named action + party + estimate bucket is generated by the Editor-Agent and cached per leaf, carrying its source issue.
- ACT-02: stale (hash-mismatch OR age>10min) or absent → no card → the row degrades to the deterministic line; the step never throws/blanks/fabricates.
- ACT-03: only engine-flagged rows are annotated; decisionOptions appear only on an explicit binary.
</success_criteria>

<output>
Create `.planning/phases/13-editor-agent-named-action/13-02-SUMMARY.md` when done.
</output>
