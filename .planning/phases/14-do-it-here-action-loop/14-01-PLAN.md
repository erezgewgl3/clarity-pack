---
phase: 14-do-it-here-action-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0016_reply_resume_dedup.sql
  - src/worker/db/reply-resume-repo.ts
  - src/worker/handlers/situation-reply-and-resume.ts
  - src/worker.ts
  - test/worker/handlers/situation-reply-and-resume.test.mjs
  - test/worker/db/reply-resume-repo.test.mjs
autonomous: true
requirements: [DO-01, DO-02]
must_haves:
  truths:
    - "The operator's reply posts to the awaited leaf's thread as a canonical public.issue_comments comment via ctx.issues.createComment, then the agent resumes natively (Phase-10 recipe)."
    - "A Shape-B reply (needsDurabilityFlip === true, i.e. the leaf is status==='blocked') ALSO applies a durable {status:'in_progress'} flip, operator-attributed via the issues.update actor; a Shape-A awaiting-answer reply (needsDurabilityFlip === false) does NOT flip."
    - "needsDurabilityFlip is a REAL boolean the handler receives from the caller (emitted by the worker rollup/backlog in 14-04 from the leaf status) — NOT proxied from terminal.kind. The handler trusts the flag; it does not re-derive shape."
    - "The same situation.replyAndResume handler serves both free-text replies and quick-decision chip clicks (chip = a canned body)."
    - "A retry with the SAME client messageUuid posts exactly one comment and applies at most one flip (idempotent replay)."
    - "An opted-out caller is rejected with OPT_IN_REQUIRED before any host mutation."
    - "The fire-and-forget requestWakeup call carries idempotencyKey:messageUuid (so a resend never double-wakes)."
  artifacts:
    - path: "src/worker/handlers/situation-reply-and-resume.ts"
      provides: "The situation.replyAndResume worker action handler (comment-write + conditional Shape-B flip + messageUuid dedup + opt-in guard)"
      contains: "registerSituationReplyAndResume"
      min_lines: 80
    - path: "migrations/0016_reply_resume_dedup.sql"
      provides: "Additive plugin-namespace dedup map (messageUuid -> comment_id)"
      contains: "plugin_clarity_pack_cdd6bda4bd.reply_resume_dedup"
    - path: "src/worker/db/reply-resume-repo.ts"
      provides: "getReplyResumeByUuid + insertReplyResume (the dedup id-map, mirroring chat-topics-repo)"
      contains: "getReplyResumeByUuid"
  key_links:
    - from: "src/worker.ts"
      to: "situation.replyAndResume"
      via: "registerSituationReplyAndResume(ctx) next to registerSituationAssignOwner"
      pattern: "registerSituationReplyAndResume"
    - from: "src/worker/handlers/situation-reply-and-resume.ts"
      to: "ctx.issues.createComment"
      via: "canonical comment write before the conditional flip"
      pattern: "createComment\\("
    - from: "src/worker/handlers/situation-reply-and-resume.ts"
      to: "ctx.issues.update"
      via: "Shape-B durability flip with actor={actorUserId:userId}"
      pattern: "issues\\.update\\("
    - from: "src/worker/handlers/situation-reply-and-resume.ts"
      to: "ctx.issues.requestWakeup"
      via: "fire-and-forget wake with idempotencyKey:messageUuid"
      pattern: "idempotencyKey"
---

<objective>
Build the single worker mutation handler `situation.replyAndResume` that implements the proven Phase-10 unblock-resume recipe: post the operator's reply as a canonical `public.issue_comments` comment (the native resume trigger for both Shape A and Shape B), then — when the caller passes `needsDurabilityFlip === true` (the leaf was `status==='blocked'`, the dominant BEAAA shape) — apply the operator-attributed durable `{status:'in_progress'}` flip. Idempotent on a client `messageUuid` via an additive plugin-namespace dedup table (migration 0016). This is the make-or-break correctness unit; the entire UI in waves 2-3 dispatches to it.

**Shape selection is a REAL signal, not a proxy.** The handler does NOT inspect or re-derive `terminal.kind`. It receives `needsDurabilityFlip` — a boolean the worker rollup/backlog (14-04) emits from the leaf issue's status at build time — and acts on it directly. This phase's reply-reachability is scoped to **AWAITING_HUMAN** leaves (the proven dominant shape the spike ran on: AWAITING_HUMAN awaiting-answer = Shape A, AWAITING_HUMAN on a `status='blocked'` leaf = Shape B); the handler itself is shape-agnostic and trusts the flag. AWAITING_AGENT_STUCK reply is DEFERRED (see Note below).

Purpose: One atomic, idempotent, opt-in-guarded, governance-parity-preserving action that mutates the LIVE Paperclip model honestly — no orphaned half-action, no double-post, no silent disposition-recovery.
Output: `migrations/0016_reply_resume_dedup.sql`, `src/worker/db/reply-resume-repo.ts`, `src/worker/handlers/situation-reply-and-resume.ts`, its `worker.ts` registration, and two test files.

**Note (DEFERRED — AWAITING_AGENT_STUCK reply):** Phase 12 D-05 LOCKS `classifyVerdict(AWAITING_AGENT_STUCK)` to `actionAffordance:'assign'` — re-owning is the honest answer for a stuck agent, and this engine mapping is NOT reversed here. The spike (10-03) ran ONLY on AWAITING_HUMAN (Shape A) + `status='blocked'` (Shape B); it never validated a comment-reply resuming a stuck *agent* thread. Adding a reply affordance for AWAITING_AGENT_STUCK would require an engine affordance change (out of scope per 14-CONTEXT D-domain "no engine change") and is therefore deferred to a future phase. This handler stays generic, but only AWAITING_HUMAN reply rows will dispatch to it (gated in 14-02/14-04).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-do-it-here-action-loop/14-CONTEXT.md
@.planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md
@CLAUDE.md

<interfaces>
<!-- Extracted contracts the executor needs — no codebase exploration required. -->

THE handler template — src/worker/handlers/situation-assign-owner.ts:
  export type SituationAssignOwnerCtx = OptInGuardActionCtx & {
    issues: Pick<PluginIssuesClient, 'update'>;
    agents: Pick<PluginAgentsClient, 'get'>;
    logger?: PluginLogger;
  };
  // pattern: wrapActionHandler(ctx, 'situation.assignOwner', async (params) => {...})
  // reqStr(params, key) throws "situation.assignOwner: <key> required" on a missing string
  // actor: PluginIssueMutationActor = { actorUserId: userId }
  // ctx.issues.update(leafIssueUuid, patch, companyId, actor)  // UUID first-arg (R3 fix)
  // returns { ok: true, leafIssueId, assignedTo } | { error: 'ASSIGN_FAILED' | 'NOT_FOUND' | 'BAD_REQUEST' }

THE comment-write + idempotency path — src/worker/handlers/chat-send.ts:
  // 1. dedup: const existing = await getChatMessageByUuid(ctx, companyId, messageUuid);
  //    if (existing && existing.comment_id) return { ok: true, commentId: existing.comment_id };
  // 2. write: comment = await ctx.issues.createComment(topicIssueId, body, companyId);  // throws -> { error } NO side-table row (no orphan)
  // 3. side-table: await insertChatMessage(ctx, { message_uuid, company_id, topic_issue_id, comment_id, ... });
  // 5. void Promise.resolve().then(() => ctx.issues.requestWakeup(topicIssueId, companyId, { reason, idempotencyKey: messageUuid })).catch(...)
  //    // FIRE-AND-FORGET, NEVER awaited, NEVER fails the action. idempotencyKey IS messageUuid (chat-send.ts L133).

SDK signatures — node_modules/@paperclipai/plugin-sdk/dist/types.d.ts:
  PluginIssuesClient.createComment(issueId, body, companyId, options?: { authorAgentId? }): Promise<{ id: string }>  // NO operator actor (D-13)
  PluginIssuesClient.update(issueId, patch, companyId, actor?: PluginIssueMutationActor): Promise<...>  // actor = { actorUserId } accepted
  PluginIssuesClient.requestWakeup(issueId, companyId, { reason, idempotencyKey }): Promise<...>  // unreliable on 2026.525.0; fire-and-forget only

The dedup-repo analog — src/worker/db/chat-topics-repo.ts:
  getChatMessageByUuid(ctx, companyId, messageUuid) -> { comment_id } | null
  insertChatMessage(ctx, row)  // execute returns rowCount only; no array cols here -> no toPgTextArrayLiteral

The 0015 migration template (additive, plugin-namespace, validator rules) — migrations/0015_action_cards.sql:
  CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.<name> ( ... INLINE UNIQUE (...) );
  COMMENT ON TABLE ... IS '... apostrophe-free ...';
  // host validator: fully-qualified schema literal, NO standalone create-index, NO procedural blocks, apostrophe-free COMMENT body
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive dedup migration 0016 + the reply-resume id-map repo</name>
  <read_first>
    - migrations/0015_action_cards.sql (the additive plugin-namespace migration template + the host-validator legality rules: fully-qualified `plugin_clarity_pack_cdd6bda4bd` literal, `CREATE TABLE IF NOT EXISTS`, inline `UNIQUE`, NO standalone create-index, apostrophe-free `COMMENT ON`)
    - src/worker/db/chat-topics-repo.ts L361-410 (getChatMessageByUuid + insertChatMessage — the message_uuid -> comment_id map this repo mirrors; the host-faithful `ctx.db.execute` rowCount-only contract)
    - src/worker/db/action-cards-repo.ts L1-40 (the repo ctx shape `db: PluginDatabaseClient` + the company-scoped idempotency discipline)
    - CLAUDE.md (additive-only; plugin namespace `plugin_clarity_pack_cdd6bda4bd`; public.* is host-owned — NO DDL against it)
  </read_first>
  <action>
    Create `migrations/0016_reply_resume_dedup.sql` exactly mirroring 0015's validator-legal shape. One additive table `plugin_clarity_pack_cdd6bda4bd.reply_resume_dedup` with columns: `id bigserial PRIMARY KEY`, `company_id text NOT NULL`, `message_uuid text NOT NULL`, `leaf_issue_id text NOT NULL` (human key, log/echo only), `comment_id text NOT NULL` (the host comment id returned by createComment), `durable boolean NOT NULL DEFAULT false` (whether the Shape-B flip was applied), `created_at timestamptz NOT NULL DEFAULT now()`, and an inline `UNIQUE (company_id, message_uuid)` (the idempotency key + its covering btree index for the dedup read). Add an apostrophe-free `COMMENT ON TABLE` documenting it as additive plugin-namespace, message_uuid -> comment_id dedup map, plugin disable leaves data intact, comment_id and leaf_issue_id are key/echo only. NO standalone create-index, NO public.* reference.
    Create `src/worker/db/reply-resume-repo.ts` with a `ReplyResumeRepoCtx = { db: PluginDatabaseClient }` type and two functions mirroring chat-topics-repo: `getReplyResumeByUuid(ctx, companyId, messageUuid): Promise<{ comment_id: string; durable: boolean } | null>` (SELECT comment_id, durable WHERE company_id=$1 AND message_uuid=$2 LIMIT 1) and `insertReplyResume(ctx, { company_id, message_uuid, leaf_issue_id, comment_id, durable }): Promise<void>` (INSERT ... ON CONFLICT (company_id, message_uuid) DO NOTHING, matching the host-faithful execute contract — no array columns, so no toPgTextArrayLiteral). Document NO_UUID_LEAK: comment_id/leaf_issue_id are dispatch/echo keys, never rendered.
  </action>
  <verify>
    <automated>node --test test/worker/db/reply-resume-repo.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `migrations/0016_reply_resume_dedup.sql` creates exactly one table fully-qualified to `plugin_clarity_pack_cdd6bda4bd.reply_resume_dedup`, with an inline `UNIQUE (company_id, message_uuid)`, NO standalone create-index, an apostrophe-free `COMMENT ON TABLE`, and ZERO references to any `public.*` object (additive-only; coexistence guarantee #3).
    - `getReplyResumeByUuid` returns `{ comment_id, durable }` for a stored uuid and `null` otherwise; `insertReplyResume` uses `ON CONFLICT (company_id, message_uuid) DO NOTHING` (a duplicate insert is a no-op).
    - `node scripts/build-worker.mjs` is green (repo type-checks against `PluginDatabaseClient`).
  </acceptance_criteria>
  <done>Migration 0016 exists, additive + namespaced; the reply-resume id-map repo is built and its repo test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: situation.replyAndResume handler — comment + conditional Shape-B flip + messageUuid dedup + opt-in guard</name>
  <read_first>
    - src/worker/handlers/situation-assign-owner.ts (FULL — the structural mirror: wrapActionHandler, reqStr, leafIssueUuid-vs-leafIssueId split, actor={actorUserId:userId}, single ctx.issues.update call, structured {error} returns, human-key echo)
    - src/worker/handlers/chat-send.ts L60-145 (the createComment write, the messageUuid dedup, the SEND_FAILED-no-orphan rule, the fire-and-forget non-awaited requestWakeup at L129-141 with idempotencyKey:messageUuid)
    - .planning/phases/10-unblock-resume-spike/10-03-SPIKE-FINDINGS.md (THE recipe: comment alone triggers Shape A & B; Shape B adds {status:'in_progress'} for durability; the CTT-07 exception is operator-attributed + audited)
    - src/worker/db/reply-resume-repo.ts (Task 1 output — getReplyResumeByUuid + insertReplyResume)
    - src/worker.ts L370-376 (the registerSituationAssignOwner registration site + the `ctx as unknown as SituationAssignOwnerCtx` pattern)
    - test/worker/handlers/situation-assign-owner.test.mjs L1-70 (the fake-ctx harness + the _issueUpdateCalls / agentsGetCalls spies + the isUuid matcher — the test shape to mirror)
  </read_first>
  <behavior>
    - Missing companyId / leafIssueUuid / leafIssueId / body / userId / messageUuid → THROW "situation.replyAndResume: &lt;key&gt; required" (reqStr).
    - Opted-out caller → { error: 'OPT_IN_REQUIRED' } (via wrapActionHandler) BEFORE any host call.
    - Idempotent replay: a second call with the SAME messageUuid returns the original { ok: true, commentId, durable } from the dedup row WITHOUT calling createComment or update again.
    - Happy path Shape A (needsDurabilityFlip === false): exactly ONE ctx.issues.createComment(leafIssueUuid, body, companyId); ZERO ctx.issues.update; inserts a dedup row with durable=false; returns { ok: true, commentId, leafIssueId, durable: false }.
    - Happy path Shape B (needsDurabilityFlip === true): createComment THEN exactly ONE ctx.issues.update(leafIssueUuid, { status: 'in_progress' }, companyId, { actorUserId: userId }); dedup row durable=true; returns durable: true.
    - createComment throws → { error: 'REPLY_FAILED' }; NO update, NO dedup row (no orphan).
    - update (Shape-B flip) throws → STILL returns { ok: true, commentId, durable: false } (the comment already triggered the resume; flip is the non-fatal durability add-on) + logs the human leafIssueId. The dedup row is inserted with durable=false (so a replay does not re-attempt the flip).
    - The fire-and-forget requestWakeup carries idempotencyKey:messageUuid and is NEVER awaited / NEVER fails the action.
    - The mutation always uses leafIssueUuid; the human leafIssueId is logged/echoed only, never the first arg of createComment or update.
  </behavior>
  <action>
    Create `src/worker/handlers/situation-reply-and-resume.ts` exporting `registerSituationReplyAndResume(ctx: SituationReplyAndResumeCtx)`. Define `SituationReplyAndResumeCtx = OptInGuardActionCtx & ReplyResumeRepoCtx & { issues: PluginIssuesClient; logger?: PluginLogger }`. Use `reqStr` (throwing "situation.replyAndResume: &lt;key&gt; required") for the six required string params: `companyId`, `leafIssueUuid`, `leafIssueId`, `body`, `userId`, `messageUuid`. Read `needsDurabilityFlip` as `params?.needsDurabilityFlip === true` — a REAL caller-supplied signal (14-04 emits it from the leaf status); the handler does NOT inspect terminal.kind to decide. Add a code comment: the flag is the worker-emitted Shape-B signal, NOT a terminal-kind proxy; an absent flag means comment-only (Shape A), which is the spike-proven-sufficient trigger for the degrade-safe edge — but for the dominant AWAITING_HUMAN+status=blocked shape the caller ALWAYS passes the real boolean (no "default false when unknown" gap on the real path).
    Implement the LOCKED recipe in order: (1) DEDUP — `const existing = await getReplyResumeByUuid(ctx, companyId, messageUuid); if (existing) return { ok: true, commentId: existing.comment_id, leafIssueId, durable: existing.durable };`. (2) COMMENT — `try { comment = await ctx.issues.createComment(leafIssueUuid, body, companyId); } catch { log warn(leafIssueId); return { error: 'REPLY_FAILED' }; }`. (3) FLIP (Shape B only) — `let durable = false; if (needsDurabilityFlip) { try { await ctx.issues.update(leafIssueUuid, { status: 'in_progress' }, companyId, { actorUserId: userId }); durable = true; } catch { log warn(leafIssueId); /* non-fatal */ } }`. (4) DEDUP INSERT — `await insertReplyResume(ctx, { company_id: companyId, message_uuid: messageUuid, leaf_issue_id: leafIssueId, comment_id: comment.id, durable });`. (5) FIRE-AND-FORGET wake — `void Promise.resolve().then(() => ctx.issues.requestWakeup(leafIssueUuid, companyId, { reason: 'clarity-pack reply: operator answer', idempotencyKey: messageUuid })).catch((e) => ctx.logger?.info?.(...));` (NEVER awaited, NEVER fails the action; idempotencyKey IS messageUuid). (6) `return { ok: true, commentId: comment.id, leafIssueId, durable };`.
    Register in `src/worker.ts`: add the import + type alias next to chat-send/assign-owner imports, and call `registerSituationReplyAndResume(ctx as unknown as SituationReplyAndResumeCtx)` immediately after the `registerSituationAssignOwner(...)` line at L375. Add a comment noting capabilities `issue.comments.create` + `issues.update` are ALREADY declared (D-14, no new cap; NOT issue.relations.write).
    Write `test/worker/handlers/situation-reply-and-resume.test.mjs` mirroring the assign-owner harness: a fake ctx with createComment/update/requestWakeup spies, a fake reply-resume repo (in-memory map keyed by messageUuid), opt-in toggle, and assertions for every &lt;behavior&gt; bullet (use the isUuid matcher to assert the mutation first-arg is the UUID and the echo is the human key). Include an explicit assertion that the requestWakeup spy was called with an options object whose `idempotencyKey === messageUuid`.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/situation-reply-and-resume.test.mjs && node scripts/build-worker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The handler posts via `ctx.issues.createComment(leafIssueUuid, body, companyId)` then, ONLY when `needsDurabilityFlip === true`, calls `ctx.issues.update(leafIssueUuid, { status: 'in_progress' }, companyId, { actorUserId: userId })` — comment first, flip after (the spike ordering). Shape selection reads the caller-supplied boolean, NOT terminal.kind (no proxy).
    - Two calls with the same `messageUuid` produce exactly ONE createComment and at most ONE update (test asserts spy call counts === 1 and the second call returns the original commentId).
    - A createComment failure returns `{ error: 'REPLY_FAILED' }` with NO update and NO dedup-row insert; a flip failure still returns `{ ok: true }` with `durable: false` and a dedup row with durable=false.
    - An opted-out caller gets `{ error: 'OPT_IN_REQUIRED' }` with zero host calls.
    - The fire-and-forget `requestWakeup` is invoked with `idempotencyKey === messageUuid` (test asserts the spy's options arg).
    - The mutation first-arg is always the UUID (`leafIssueUuid`); the human `leafIssueId` never reaches createComment/update (NO_UUID_LEAK in the dispatch).
    - `src/worker.ts` registers `situation.replyAndResume` next to `situation.assignOwner`; `node scripts/build-worker.mjs` is green.
    - `node --test test/shared/blocker-chain.test.mjs` still passes (engine untouched — determinism + AI-token grep guard green).
  </acceptance_criteria>
  <done>situation.replyAndResume is registered and implements the exact locked recipe; idempotent on messageUuid; opt-in-guarded; comment-only vs comment+flip is driven by the real caller-supplied needsDurabilityFlip (not a terminal-kind proxy); the wake carries idempotencyKey:messageUuid; all handler tests pass and the worker build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI (same-origin trusted JS) → worker action RPC | Operator-supplied params (`leafIssueUuid`, `body`, `messageUuid`, `needsDurabilityFlip`) cross here untrusted |
| worker handler → host `ctx.issues.*` | The ONLY mutation path to host-owned `public.issue_comments` + `public.issues` |
| worker handler → plugin-namespace `ctx.db` | The dedup table (additive, namespace-scoped) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering/DoS | double-post on network retry | mitigate | client `messageUuid` dedup (step 1) + `UNIQUE (company_id, message_uuid)` on 0016 + ON CONFLICT DO NOTHING — a lost-ACK retry returns the original commentId, never re-posts or re-flips |
| T-14-02 | Elevation of Privilege | opted-out / unidentifiable caller mutating the live board | mitigate | `wrapActionHandler` opt-in guard returns OPT_IN_REQUIRED server-side before any host call (mirrors assignOwner / chat.send) |
| T-14-03 | Tampering | crafted `leafIssueUuid` targeting an arbitrary issue | mitigate (residual accepted) | the UUID is dispatched from the engine verdict's `targetIssueUuid` (wave 2/3), not free-form operator text; the host `ctx.issues.*` enforces company-scope on `companyId`; same-origin trust model means UI code is trusted (PLUGIN_SPEC §19) — tightening beyond company-scope is out of scope per CLAUDE.md |
| T-14-04 | Repudiation | the Shape-B status flip with no audit trail | mitigate | the flip carries `actor={actorUserId:userId}` (operator-attributed, audited) — the deliberate CTT-07 exception the spike authorizes; it is operator-initiated + one-shot, NOT a silent per-poll sweep (the topic-watchdog ban) |
| T-14-05 | Information Disclosure | a raw UUID leaking into the comment body or a log line | mitigate | `body` is the operator's plain answer text (no UUID interpolation); logs echo only the human `leafIssueId`; the dedup row stores comment_id/leaf_issue_id as keys, never rendered |
| T-14-06 | Tampering | additive migration 0016 breaking coexistence (#3/#6) | mitigate | DDL is fully-qualified to the plugin namespace, `CREATE TABLE IF NOT EXISTS`, zero `public.*` references; plugin disable/uninstall leaves the table intact |
| T-14-07 | DoS | the unreliable `requestWakeup` blocking the ACK | accept | fire-and-forget (`void Promise.resolve().then(...).catch(...)`), never awaited, never fails the action; idempotencyKey:messageUuid prevents double-wake; native wake (the comment) is the real trigger |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies in this plan; no install tasks |
</threat_model>

<verification>
- `node --test test/worker/handlers/situation-reply-and-resume.test.mjs` — full recipe + idempotency + opt-in + no-orphan + non-fatal-flip + idempotencyKey:messageUuid on wake.
- `node --test test/worker/db/reply-resume-repo.test.mjs` — dedup map get/insert + ON CONFLICT no-op.
- `node scripts/build-worker.mjs` — worker bundle green (handler + repo + registration type-check).
- `node --test test/shared/blocker-chain.test.mjs` — engine determinism + AI-token guard still green (engine untouched).
- Migration 0016 is additive, plugin-namespace-scoped, zero public.* DDL.
</verification>

<success_criteria>
- SC1 (partial — worker half): a reply posts as a canonical comment and resumes the agent via the Phase-10 recipe; Shape B adds the durable flip when the real needsDurabilityFlip signal says so; honest structured errors on failure.
- SC2 (partial — worker half): a chip click and a free-text reply both run the identical comment+flip path (one handler, one idempotency surface).
- Idempotent on messageUuid (incl. the wake's idempotencyKey); opt-in-guarded; governance parity (agent resumes on its own heartbeat; only the operator-attributed flip mutates state).
</success_criteria>

<output>
Create `.planning/phases/14-do-it-here-action-loop/14-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
