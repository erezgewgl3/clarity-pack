---
phase: 14-do-it-here-action-loop
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/shared/reply-reachable.ts
  - src/ui/surfaces/_shared/reply-in-place.tsx
  - test/shared/reply-reachable.test.mjs
  - test/ui/surfaces/_shared/reply-in-place.test.mjs
  - test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs
autonomous: true
requirements: [DO-01, DO-02, DO-04, DO-05]
must_haves:
  truths:
    - "ONE shared <ReplyInPlace> component owns the free-text reply input + Send, the optional decision chips, the Open↗ escape, the situation.replyAndResume dispatch, the await-confirm posture, and the success/failure toast."
    - "Quick-decision chips render ONLY when decisionOptions is a non-empty array (Phase-13 binary signal); each chip dispatches the SAME replyAndResume path with a canned plain-sentence body."
    - "When reachable === false, the component renders ONLY the named action + an Open↗ button (no input, no chips, no Send) — no dead Send button."
    - "The component dispatches leafIssueUuid (mutation) + leafIssueId (echo) + a fresh client messageUuid + needsDurabilityFlip; it renders leafIssueId / awaitedPartyLabel only — never a UUID."
    - "isReplyReachable(verdict) is a pure helper keyed on terminal.kind + targetAgentUuid (NOT a string match); EXTERNAL/CYCLE/UNCLASSIFIED/UNOWNED -> false."
    - "Await-confirm: on { ok } -> success toast + onActed(); on { error } -> honest error toast, input stays populated, no false resume."
  artifacts:
    - path: "src/shared/reply-reachable.ts"
      provides: "Pure isReplyReachable(verdict) predicate (instance-agnostic, unit-testable)"
      contains: "export function isReplyReachable"
    - path: "src/ui/surfaces/_shared/reply-in-place.tsx"
      provides: "The single shared <ReplyInPlace> primitive consumed by all three surfaces"
      contains: "export function ReplyInPlace"
      min_lines: 90
  key_links:
    - from: "src/ui/surfaces/_shared/reply-in-place.tsx"
      to: "situation.replyAndResume"
      via: "usePluginAction dispatch with leafIssueUuid + messageUuid + needsDurabilityFlip"
      pattern: "usePluginAction\\('situation.replyAndResume'\\)"
    - from: "src/ui/surfaces/_shared/reply-in-place.tsx"
      to: "isReplyReachable / reachable prop"
      via: "reachable===false renders Open↗ only"
      pattern: "reachable"
---

<objective>
Build the ONE shared `<ReplyInPlace>` UI primitive (DO-04/SC3) plus the pure `isReplyReachable(verdict)` predicate (DO-05/SC4). The primitive owns the entire reply-in-place mechanic — free-text input + Send, optional quick-decision chips (DO-02/SC2), the Open↗ escape for out-of-system humans (DO-05/SC4), the `situation.replyAndResume` dispatch, await-confirm honesty (SC1), and split-identity NO_UUID_LEAK (SC5). No surface wiring yet — that is wave 3. This wave produces the single re-used component so wave 3 cannot triplicate logic.

Purpose: Display/dispatch split enforced in ONE place; the reachable predicate is verdict-driven, not a string match; await-confirm never fakes success.
Output: `src/shared/reply-reachable.ts`, `src/ui/surfaces/_shared/reply-in-place.tsx`, and three test files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-do-it-here-action-loop/14-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The dispatch template + the verdict/card shapes the executor needs. -->

THE dispatch template — src/ui/surfaces/situation-room/owner-picker-popover.tsx:
  const assignOwner = usePluginAction('situation.assignOwner');
  // dispatchAssign: const result = await assignOwner({ companyId, leafIssueId, leafIssueUuid: leafIssueUuid ?? leafIssueId, userId, ...params }) as AssignResult;
  //   if (result && 'ok' in result && result.ok) { setOpen(false); onAssigned(result); }
  // outside-click + Esc close; React text nodes only; ids are dispatch args only, never rendered.

The verdict + card types (read-only) — src/shared/types.ts:
  Terminal kinds: AWAITING_HUMAN | AWAITING_AGENT_WORKING | AWAITING_AGENT_STUCK | SELF_RESOLVING | EXTERNAL | CYCLE | UNOWNED | UNCLASSIFIED
  BlockerChainResult.actionAffordance: 'reply' | 'nudge' | 'assign' | 'open' | 'none'
  BlockerChainResult.awaitedPartyLabel (scrubbed display string), .targetAgentUuid, .targetIssueUuid (dispatch-only, never rendered)
  ActionCard.decisionOptions: string[] | null  (D-08 — non-null ONLY when the issue poses an explicit binary)
  ActionCard.actionKind: 'answer' | 'decide' | 'assign' | 'none'

The handler result (wave 1 — 14-01) — situation.replyAndResume returns:
  { ok: true; commentId: string; leafIssueId: string; durable: boolean } | { error: 'REPLY_FAILED' | 'OPT_IN_REQUIRED' } | null
  // dispatch params: { companyId, leafIssueUuid, leafIssueId, body, userId, messageUuid, needsDurabilityFlip }

The toast primitive — src/ui/primitives/toast.tsx: const { showToast } = useToast(); showToast({ message, duration }) // 6000ms host-call-pending pattern
NO_UUID_LEAK render-scan convention — test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (source-grep based: no jsdom; assert the source never interpolates target*Uuid/leafIssueUuid inside a {...} render expression)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure isReplyReachable(verdict) predicate (DO-05/SC4) — verdict-driven, not a string match</name>
  <read_first>
    - src/shared/types.ts (the Terminal 8-kind union + BlockerChainResult.actionAffordance/targetAgentUuid — the predicate inputs)
    - src/shared/blocker-chain.ts L1-60 (the classifyVerdict family + the PRIM-03 no-AI-token boundary the predicate must respect — it is structural, adds no token)
    - .planning/phases/14-do-it-here-action-loop/14-CONTEXT.md D-10 (the exact reachable rule: AWAITING_AGENT_STUCK with targetAgentUuid OR an AWAITING_HUMAN in-system user-issue leaf -> reachable; EXTERNAL/CYCLE/UNCLASSIFIED/UNOWNED -> NOT reachable)
    - .planning/phases/11-honest-blocker-taxonomy-engine/11-CONTEXT.md (D-13/D-14 the kind->affordance discipline — the predicate keys off kind + targetAgentUuid, NEVER ownerName)
  </read_first>
  <behavior>
    - AWAITING_HUMAN -> true (the dominant BEAAA shape: the operator answers his own awaited-decision issue; posting the answer-comment triggers the assigned agent's resume — Shape A/B reachable).
    - AWAITING_AGENT_STUCK WITH a non-null targetAgentUuid -> true (an in-system agent thread a comment can wake).
    - AWAITING_AGENT_STUCK with null/absent targetAgentUuid -> false (no in-system thread to reach).
    - EXTERNAL -> false; CYCLE -> false; UNCLASSIFIED -> false; UNOWNED -> false (assignment, not reply, is the answer).
    - AWAITING_AGENT_WORKING -> false (in motion, no action needed); SELF_RESOLVING -> false.
    - The function reads ONLY terminal.kind + targetAgentUuid (a structural input) — NO string match on awaitedPartyLabel, NO ownerName comparison, NO AI/LLM token.
  </behavior>
  <action>
    Create `src/shared/reply-reachable.ts` exporting a pure `isReplyReachable(verdict: Pick&lt;BlockerChainResult, 'terminal' | 'targetAgentUuid'&gt;): boolean`. Implement an exhaustive `switch (verdict.terminal.kind)` over the 8 kinds with a `const _exhaustive: never` guard (the established total-function idiom from classifyVerdict): return `true` for `AWAITING_HUMAN`; return `verdict.targetAgentUuid != null` for `AWAITING_AGENT_STUCK`; return `false` for `AWAITING_AGENT_WORKING`, `SELF_RESOLVING`, `EXTERNAL`, `CYCLE`, `UNOWNED`, `UNCLASSIFIED`. No SDK import, no I/O, no wall-clock read, no AI-vendor token — the same purity boundary `blocker-chain.ts` holds. Document that this is the SC4 reachable-vs-out-of-system predicate, keyed structurally, never on a scrubbed label.
    Write `test/shared/reply-reachable.test.mjs` asserting every &lt;behavior&gt; bullet (all 8 kinds + the targetAgentUuid presence/absence split on AWAITING_AGENT_STUCK) AND a grep guard that the source contains no AI-vendor token and no `awaitedPartyLabel`/`ownerName` string-match (mirroring the blocker-chain AI-token guard convention).
  </action>
  <verify>
    <automated>node --test test/shared/reply-reachable.test.mjs && node scripts/build-worker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `isReplyReachable` returns true for AWAITING_HUMAN and for AWAITING_AGENT_STUCK-with-targetAgentUuid; false for EXTERNAL, CYCLE, UNCLASSIFIED, UNOWNED, AWAITING_AGENT_WORKING, SELF_RESOLVING, and AWAITING_AGENT_STUCK-without-targetAgentUuid.
    - The source reads only `terminal.kind` + `targetAgentUuid`; the test's grep guard confirms NO `ownerName`/`awaitedPartyLabel` string-match and NO AI-vendor token (purity boundary held — engine class untouched).
    - `node --test test/shared/blocker-chain.test.mjs` still green (the existing engine determinism + AI-token guard unaffected).
  </acceptance_criteria>
  <done>The pure, verdict-driven reachable predicate exists in src/shared/ next to the classifyVerdict family, with full kind coverage and a purity guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The shared <ReplyInPlace> primitive — input + Send + chips + Open↗ + dispatch + await-confirm</name>
  <read_first>
    - src/ui/surfaces/situation-room/owner-picker-popover.tsx (FULL — the dispatch template: usePluginAction, the `'ok' in result` structured guard, the `leafIssueUuid ?? leafIssueId` fallback, outside-click/Esc, React-text-only rendering)
    - src/ui/surfaces/reader/live-blocker-panel.tsx L163-270 (the openIssue → /<prefix>/issues/<identifier> pattern for Open↗; the CR-01 multi-hop no-button discipline at L261 — no leaf human key -> no button)
    - src/ui/surfaces/situation-room/employee-row.tsx L203-209, L233-248 (openIssue callback + the 6000ms host-call-pending toast pattern)
    - .planning/phases/14-do-it-here-action-loop/14-CONTEXT.md D-06/D-07/D-08/D-11/D-12 (the canned chip-text mapping is planner discretion but a plain operator sentence; the props shape; Open↗ uses the human key; await-confirm honest error)
    - src/shared/reply-reachable.ts (Task 1 — the reachable semantics the `reachable` prop carries)
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (the source-grep render-scan convention to extend to the new primitive)
  </read_first>
  <behavior>
    - reachable === false: renders ONLY the named-action line + an Open↗ button (when leafIssueId is non-null); NO input, NO chips, NO Send. When leafIssueId is null (multi-hop, no leaf key), renders the named-action line with NO Open↗ button (CR-01 honest degrade, no 404).
    - reachable === true, decisionOptions is a non-empty array: renders the free-text input + Send AND one chip per option; a chip click dispatches replyAndResume with the canned plain-sentence body.
    - reachable === true, decisionOptions null/empty: renders the free-text input + Send only (no chips).
    - Dispatch always sends { companyId, leafIssueUuid: leafIssueUuid ?? leafIssueId, leafIssueId, body, userId, messageUuid: <fresh per click>, needsDurabilityFlip }; a Retry of the same click reuses the same messageUuid.
    - On { ok }: success toast "Replied to <awaitedPartyLabel> · <leafIssueId>" + onActed(); input cleared/disabled during the pending window ("Sending…").
    - On { error }: honest error toast (no false "resumed"); input stays populated for retry; onActed() is NOT called.
    - Render: leafIssueId and awaitedPartyLabel are the ONLY identifiers shown; leafIssueUuid / targetAgentUuid / targetIssueUuid never appear in any rendered text (read into dispatch-only consts, never inside a {...} render expression).
  </behavior>
  <action>
    Create the new directory + file `src/ui/surfaces/_shared/reply-in-place.tsx` exporting `ReplyInPlace(props)`. Props (D-08): `leafIssueId: string | null`, `leafIssueUuid: string | null`, `awaitedPartyLabel: string`, `namedAction: string` (the Editorial sentence to show as the row label), `reachable: boolean`, `needsDurabilityFlip: boolean`, `decisionOptions: string[] | null`, `companyId: string`, `userId: string`, `companyPrefix: string`, `navigate: (to: string) => void`, `onActed: () => void`. Use `usePluginAction('situation.replyAndResume')` and `useToast()`.
    Implement a `dispatchReply(body: string)` callback mirroring owner-picker's `dispatchAssign`: guard against concurrent sends with a `sending` state; generate ONE `messageUuid` (use `crypto.randomUUID()` with a fallback) captured so a Retry reuses it; `const result = await reply({ companyId, leafIssueUuid: leafIssueUuid ?? leafIssueId, leafIssueId, body, userId, messageUuid, needsDurabilityFlip }) as ReplyResult;` then the structured `if (result && 'ok' in result && result.ok)` guard → success toast + `onActed()`; else → honest error toast + keep the input text. Catch → honest error toast (never optimistic). Set/clear `sending` in finally.
    Render branches: when `!reachable` → the `namedAction` line + (when `leafIssueId` non-null) an Open↗ button calling `navigate(\`/${companyPrefix}/issues/${leafIssueId}\`)` — reuse the exact human-key URL pattern (NEVER the UUID); when `leafIssueId` is null, render NO Open↗ (CR-01 honest degrade). When `reachable` → a controlled text input + a Send button (disabled while `sending`, dispatches the typed body) and, when `decisionOptions` is a non-empty array, a chip row: one `<button>` per option whose onClick dispatches `dispatchReply(cannedSentence(option))`. Add a small pure `cannedSentence(option: string): string` helper (planner discretion per D-06): map "Approve"→"Approved.", "Reject"→"Rejected.", "Yes"→"Yes.", "No"→"No.", any other pick-one option `X`→`"${X}."` — always a plain operator answer sentence, never a structured command. Every visible string is a React text node; the three *Uuid values are read into dispatch-only consts so the render-scan passes.
    Write `test/ui/surfaces/_shared/reply-in-place.test.mjs` (source-grep convention, no jsdom) asserting: usePluginAction('situation.replyAndResume') is wired; the reachable===false branch renders Open↗ with the `/${companyPrefix}/issues/${leafIssueId}` URL and NO input/Send/chips; the decisionOptions non-empty branch renders chips that route through the SAME dispatchReply; the await-confirm structured `'ok' in result` guard + onActed gating + the honest error path (no optimistic success); the messageUuid-reuse-on-retry; the cannedSentence mapping. Write `test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs` extending the Phase-11 11-07 render-scan: assert the source never interpolates `leafIssueUuid` / `targetAgentUuid` / `targetIssueUuid` inside a JSX `{...}` render expression (they are dispatch-only).
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/_shared/reply-in-place.test.mjs test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The Send affordance is ABSENT when `reachable === false`; that branch renders only the named action + an Open↗ button (or nothing when leafIssueId is null) — no dead Send button (SC4).
    - Quick-decision chips render iff `decisionOptions` is a non-empty array, and each chip dispatches the SAME `situation.replyAndResume` path with a canned plain-sentence body (SC2) — no separate decide handler.
    - The dispatch sends `leafIssueUuid` (mutation) + `leafIssueId` (echo) + a fresh `messageUuid` + `needsDurabilityFlip`; a Retry reuses the same `messageUuid` (idempotency holds end-to-end with 14-01).
    - Await-confirm: success only on the structured `{ ok }` (then toast + onActed); on `{ error }` an honest error toast fires and onActed is NOT called (no fake resume) — SC1.
    - The no-uuid-leak render-scan passes: `leafIssueUuid` / `targetAgentUuid` / `targetIssueUuid` never appear inside a render `{...}`; only `leafIssueId` / `awaitedPartyLabel` are shown (SC5).
    - `node scripts/build-ui.mjs` is green.
  </acceptance_criteria>
  <done>The single shared <ReplyInPlace> primitive exists under src/ui/surfaces/_shared/, owns the full reply/chip/Open↗/await-confirm mechanic, passes its behavior tests and the NO_UUID_LEAK render-scan, and the UI build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator input (typed reply / chip) → UI dispatch | Free-text `body` + chosen option cross here |
| UI dispatch → worker `situation.replyAndResume` | The leafIssueUuid + messageUuid + needsDurabilityFlip params |
| UI render → DOM | The display strings (must be UUID-free) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Information Disclosure | a UUID rendered into the row text | mitigate | split-identity: only `leafIssueId` + `awaitedPartyLabel` are React text nodes; the *Uuid values are dispatch-only consts; the no-uuid-leak render-scan guard (extends 11-07) blocks any `{...target*Uuid}` interpolation |
| T-14-09 | Spoofing/Integrity | a dead Send button that silently no-ops | mitigate | `reachable === false` removes the Send affordance entirely (verdict-driven via isReplyReachable, not a string match) — Open↗ navigates to the real human-key URL; SC4 no-dead-button |
| T-14-10 | Tampering | double-dispatch from a double-click / retry | mitigate | a `sending` guard blocks concurrent dispatch; a single `messageUuid` per click reused on Retry → the wave-1 handler dedups (T-14-01) |
| T-14-11 | Spoofing | optimistic "resumed" masking a failed mutation | mitigate | await-confirm: success only on the structured `{ ok }`; `{ error }` shows an honest error toast and keeps the input — never a false-positive (SC1, the milestone's core honesty value) |
| T-14-12 | Tampering | Open↗ navigating with a UUID (404) | mitigate | Open↗ uses `/<companyPrefix>/issues/<leafIssueId>` (the human key per the paperclip-issue-url-pattern memory); when no leaf human key exists, renders no button (CR-01 honest degrade) |
| T-14-13 | Injection | a chip producing a structured command instead of an answer | mitigate | `cannedSentence` emits a plain operator answer sentence (e.g. "Approved.") posted as a normal comment the agent reads as the answer — no command grammar, no template injection |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies in this plan; no install tasks |
</threat_model>

<verification>
- `node --test test/shared/reply-reachable.test.mjs` — predicate kind coverage + purity guard.
- `node --test test/ui/surfaces/_shared/reply-in-place.test.mjs` — reply/chip/Open↗/await-confirm/messageUuid behavior.
- `node --test test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs` — render-scan (no UUID in rendered text).
- `node scripts/build-ui.mjs` && `node scripts/build-worker.mjs` — both bundles green.
- `node --test test/shared/blocker-chain.test.mjs` — engine determinism + AI-token guard untouched.
</verification>

<success_criteria>
- SC2: chips render only on a clean Phase-13 binary and complete the same unblock+resume path.
- SC3 (component half): ONE shared `<ReplyInPlace>` exists for all three surfaces to import (no copies) — wiring lands in wave 3.
- SC4: out-of-system rows (verdict-driven) surface named action + Open↗, never a dead Send.
- SC5: UUIDs never rendered; the mutation carries the UUID, the display stays human-readable.
- SC1 (UI half): await-confirm honesty — no optimistic false success.
</success_criteria>

<output>
Create `.planning/phases/14-do-it-here-action-loop/14-02-SUMMARY.md` when done.
</output>
