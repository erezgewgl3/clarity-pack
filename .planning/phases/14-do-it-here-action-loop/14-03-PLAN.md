---
phase: 14-do-it-here-action-loop
plan: 03
type: execute
wave: 3
depends_on: [14-02, 14-04]
files_modified:
  - src/ui/surfaces/situation-room/employee-row.tsx
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx
  - test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs
  - test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs
  - test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs
autonomous: true
requirements: [DO-01, DO-04, DO-05]
must_haves:
  truths:
    - "The SAME <ReplyInPlace> from src/ui/surfaces/_shared is imported and mounted on the Situation Room employee row, the Reader-view live blocker panel, and the org-blocked backlog expander — no copies (SC3)."
    - "On each surface, <ReplyInPlace> mounts on the reply/reachable branch (isReplyReachable(terminalKind) true ⇔ AWAITING_HUMAN), passing the row's leafIssueId/leafIssueUuid/awaitedPartyLabel + decisionOptions + the REAL worker-emitted needsDurabilityFlip (NOT data.terminal.kind)."
    - "reachable is computed via isReplyReachable(row.terminalKind / chain.terminalKind) — the terminalKind threaded in 14-04 — never an inline-constructed Terminal object and never a string match."
    - "needsDurabilityFlip is read from the row (14-04's leaf-status-derived boolean), NOT proxied from data.terminal.kind anywhere."
    - "An out-of-system row (reachable false, i.e. not AWAITING_HUMAN) surfaces the named action + Open↗ via the same primitive — no dead Send (SC4)."
    - "In live-blocker-panel, when the reply branch mounts <ReplyInPlace>, the existing <p className=clarity-blocker-label>{blockerLine(data)}</p> does NOT also render (no duplicate line)."
    - "Each surface forwards its existing snapshot force-refetch as onActed; no UUID is rendered on any of the three surfaces."
  artifacts:
    - path: "src/ui/surfaces/situation-room/employee-row.tsx"
      provides: "ReplyInPlace mounted on the needs_you reply/reachable branch"
      contains: "ReplyInPlace"
    - path: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      provides: "ReplyInPlace replacing the navigate-to-chat 'reply' affordance; no duplicate blockerLine"
      contains: "ReplyInPlace"
    - path: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      provides: "ReplyInPlace on reply/reachable orphan rows"
      contains: "ReplyInPlace"
  key_links:
    - from: "src/ui/surfaces/situation-room/employee-row.tsx"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "single import, no copy"
      pattern: "reply-in-place"
    - from: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "single import, no copy"
      pattern: "reply-in-place"
    - from: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "single import, no copy"
      pattern: "reply-in-place"
---

<objective>
Wire the ONE shared `<ReplyInPlace>` primitive (built in wave 2) into all three blocker surfaces — the Situation Room employee row, the Reader-view live blocker panel, and the org-blocked backlog expander — each on its `reply`/reachable branch, gated off the engine verdict via `isReplyReachable(terminalKind)` (AWAITING_HUMAN-only). Same import, no copies (SC3). The out-of-system (reachable-false) case flows through the same primitive's Open↗-only render (SC4). Each surface passes its existing force-refetch as `onActed` and the REAL worker-emitted `needsDurabilityFlip` from the row (14-04), never a `data.terminal.kind` proxy. This closes DO-01/DO-04/DO-05 end-to-end against the live model.

**Call-site shape (BLOCKER 5):** every surface computes `reachable` from the `terminalKind` field its row already carries after 14-04 (`chain.terminalKind` on the employee rollup, `row.terminalKind` on OrgBlockedRow, `data.terminal.kind` on the Reader's BlockerChainResult — the Reader's data already has `terminal.kind` natively, so pass that string). NO inline `{ terminal: { kind } as any }` construction — the predicate takes the kind string directly.

**Duplicate line (WARNING 2):** in `live-blocker-panel.tsx`, the existing `<p className="clarity-blocker-label">{blockerLine(data)}</p>` (L290) currently always renders. When the affordance is `reply` and `<ReplyInPlace>` mounts (which renders its own `namedAction` line), the standalone `blockerLine` `<p>` MUST be suppressed for that branch so the headline does not render twice. Keep `blockerLine` for the non-reply affordances.

Purpose: Prove SC3 by construction — one component, three mounts; the reply mechanic is now live on every surface that surfaces a blocker, gated on the proven AWAITING_HUMAN shape, with the real Shape-B signal threaded through.
Output: edits to the three surfaces + one render-scan/mount test per surface.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-do-it-here-action-loop/14-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The <ReplyInPlace> props (wave 2) the three surfaces must supply. -->

<ReplyInPlace> props — src/ui/surfaces/_shared/reply-in-place.tsx:
  { leafIssueId, leafIssueUuid, awaitedPartyLabel, namedAction, reachable, needsDurabilityFlip,
    decisionOptions, companyId, userId, companyPrefix, navigate, onActed }
  // reachable = isReplyReachable(<terminalKind string>) (src/shared/reply-reachable.ts) — AWAITING_HUMAN-only.

isReplyReachable — src/shared/reply-reachable.ts: isReplyReachable(terminalKind: Terminal['kind']) -> boolean

Mount point A — src/ui/surfaces/situation-room/employee-row.tsx:
  // needs_you cluster L315-407. chain (after 14-04) has: leafIssueId, leafIssueUuid, awaitedPartyLabel, targetAgentUuid,
  //   targetIssueUuid, actionAffordance, tier, needsYou, terminalKind, needsDurabilityFlip; row.actionCard has decisionOptions/namedAction.
  // showAssign = chain?.actionAffordance === 'assign' (OwnerPickerPopover stays for 'assign' — incl. AWAITING_AGENT_STUCK).
  // existing openIssue + onAssignSuccess (force-refetch) + companyPrefix/companyId/userId in scope.
  // The reply branch is the NON-assign needs_you case where actionAffordance === 'reply' (⇔ AWAITING_HUMAN per classifyVerdict).

Mount point B — src/ui/surfaces/reader/live-blocker-panel.tsx:
  // L223-270 affordance switch; case 'reply' L228-230 TODAY navigates to chat. Phase 14 REPLACES with <ReplyInPlace>.
  // data: BlockerChainResult (terminal {kind}, actionAffordance, awaitedPartyLabel, targetAgentUuid, targetIssueUuid, pathIds, needsYou).
  // data.terminal.kind is the kind string for isReplyReachable (native — no 14-04 field needed here).
  // companyPrefix (parsed), companyId, viewerUserId, nav.navigate in scope.
  // leafIssueId = issueId ONLY for single-hop (pathIds.length <= 1), else null (CR-01 honest degrade).
  // needsDurabilityFlip: the Reader's BlockerChainResult has NO leaf-status field; derive HONESTLY from data.terminal.kind
  //   ONLY as a last resort is WRONG — instead: pass needsDurabilityFlip={false} when the kind is AWAITING_HUMAN awaiting-answer
  //   and the surface cannot know the leaf status. NOTE: the Reader panel is single-issue; its reachable reply rows are
  //   AWAITING_HUMAN on THIS issue. If THIS issue's status is available to the panel (it is the open issue), pass
  //   needsDurabilityFlip = (open issue status === 'blocked'); otherwise false (comment-only, spike-safe). See task action.
  // EXISTING <p className="clarity-blocker-label">{blockerLine(data)}</p> at L290 — suppress for the reply branch (WARNING 2).
  // panel has no force-refetch today — usePluginData re-polls; pass a no-op onActed.

Mount point C — src/ui/surfaces/situation-room/blocked-backlog-expander.tsx:
  // L82-119 orphan rows. row (after 14-04) has: issueId (ROOT UUID), identifier (human key), title, humanAction,
  //   actionAffordance, terminalKind, awaitedPartyLabel, targetAgentUuid, decisionOptions (null), leafIssueUuid (LEAF UUID), needsDurabilityFlip.
  // existing openIssue(identifier) + onAssignSuccess (force-refetch) + companyPrefix/companyId/userId in scope.
  // OwnerPickerPopover stays for actionAffordance === 'assign'.

Render-scan convention — test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (source-grep; no jsdom)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount <ReplyInPlace> on the Situation Room employee row + the Reader live blocker panel (no duplicate line)</name>
  <read_first>
    - src/ui/surfaces/situation-room/employee-row.tsx (FULL — the needs_you cluster L315-407; showAssign gate L199; openIssue L203-209; onAssignSuccess force-refetch; the chain shape now carrying terminalKind/needsDurabilityFlip after 14-04; row.actionCard.decisionOptions/namedAction)
    - src/ui/surfaces/reader/live-blocker-panel.tsx (FULL — the affordance switch L223-270; the 'reply' case L228-230; the blockerLine <p> at L290 to suppress for the reply branch; the openIssue human-key URL L163-166; data.terminal.kind native)
    - src/ui/surfaces/_shared/reply-in-place.tsx (wave 2 — the props it consumes)
    - src/shared/reply-reachable.ts (wave 2 — isReplyReachable(terminalKind))
    - .planning/phases/14-do-it-here-action-loop/14-CONTEXT.md D-07/D-08/D-10/D-11 (one shared component; the props; reachable predicate AWAITING_HUMAN-only; Open↗ human-key)
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (the render-scan convention)
  </read_first>
  <behavior>
    - employee-row: on a needs_you row where actionAffordance === 'reply' (⇔ AWAITING_HUMAN), the row renders <ReplyInPlace> (NOT the chat-deep-link path), passing chain.leafIssueId/leafIssueUuid/awaitedPartyLabel, row.actionCard?.decisionOptions, namedAction = the action-card sentence or the deterministic chain line, needsDurabilityFlip = chain.needsDurabilityFlip (the REAL 14-04 boolean, NOT data.terminal.kind), reachable = isReplyReachable(chain.terminalKind), companyPrefix/companyId/userId, navigate, onActed = onAssignSuccess.
    - employee-row: actionAffordance === 'assign' still renders OwnerPickerPopover (unchanged; covers UNOWNED + AWAITING_AGENT_STUCK per Phase 12 D-05); 'reply' renders ReplyInPlace; mutually exclusive per row.
    - employee-row: a reply row that is somehow not reachable (defensive — should be AWAITING_HUMAN=reachable) renders named action + Open↗ via the primitive — no dead Send.
    - live-blocker-panel: the 'reply' affordance case mounts <ReplyInPlace> instead of the navigate-to-chat callback; leafIssueId = issueId for a single-hop chain (pathIds.length <= 1), else null (CR-01); leafIssueUuid = data.targetIssueUuid; awaitedPartyLabel/decisionOptions(null) from data; reachable = isReplyReachable(data.terminal.kind); needsDurabilityFlip derived from the open issue's status if available to the panel, else false (comment-only, spike-safe); onActed a no-op (usePluginData re-polls).
    - live-blocker-panel: when the affordance is 'reply', the standalone <p className="clarity-blocker-label">{blockerLine(data)}</p> is SUPPRESSED (the primitive shows its own namedAction line) — for all other affordances the blockerLine <p> renders as before (no duplicate, no regression).
    - Both surfaces: no UUID is interpolated into any rendered string; needsDurabilityFlip is never read from data.terminal.kind.
  </behavior>
  <action>
    employee-row.tsx: import `ReplyInPlace` from `../_shared/reply-in-place.tsx` and `isReplyReachable` from `../../../shared/reply-reachable.ts`. In the needs_you action cluster, add a branch for `chain.actionAffordance === 'reply'` (mutually exclusive with `showAssign`): render `<ReplyInPlace>` with `leafIssueId={chain.leafIssueId}`, `leafIssueUuid={chain.leafIssueUuid}`, `awaitedPartyLabel={chain.awaitedPartyLabel}`, `namedAction={row.actionCard?.namedAction ?? \`waiting on ${chain.awaitedPartyLabel}\`}`, `decisionOptions={row.actionCard?.decisionOptions ?? null}`, `needsDurabilityFlip={chain.needsDurabilityFlip}` (the REAL 14-04 boolean), `reachable={isReplyReachable(chain.terminalKind)}`, `companyId`, `userId`, `companyPrefix`, `navigate`, `onActed={onAssignSuccess}`. Keep OwnerPickerPopover for the 'assign' branch. Do NOT construct an inline Terminal object — pass `chain.terminalKind` (the string threaded in 14-04) straight to `isReplyReachable`.
    live-blocker-panel.tsx: import `ReplyInPlace` + `isReplyReachable`. In the affordance handling, treat the `reply` affordance as a RENDER branch (mount `<ReplyInPlace>`) rather than wiring `onAction = () => replyInChat(...)`. Pass `leafIssueId={data.pathIds.length <= 1 ? issueId : null}` (single-hop only — CR-01), `leafIssueUuid={data.targetIssueUuid ?? null}`, `awaitedPartyLabel={data.awaitedPartyLabel}`, `namedAction={blockerLine(data)}`, `decisionOptions={null}` (no action card on this surface), `needsDurabilityFlip={false}` (the panel's BlockerChainResult has no leaf-status; default comment-only is spike-safe — if the open issue's status is already in scope here as 'blocked', pass that comparison instead, but DO NOT proxy from data.terminal.kind), `reachable={isReplyReachable(data.terminal.kind)}`, `companyId`, `userId={viewerUserId}`, `companyPrefix`, `navigate={nav.navigate}`, `onActed={() => {}}`. SUPPRESS the standalone `<p className="clarity-blocker-label">{blockerLine(data)}</p>` (L290) when `data.actionAffordance === 'reply'` (so the headline does not render twice) — render it only for non-reply affordances. Remove the now-dead `replyInChat` path for the 'reply' affordance (keep nudge/assign/open/none as-is). Preserve primaryActionLabel/blockerLine for the non-reply affordances.
    Write `test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs` and `test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs` (source-grep): assert each surface imports ReplyInPlace from `_shared` (single shared import) and isReplyReachable from the shared helper; mounts ReplyInPlace on the reply affordance; passes leafIssueUuid (mutation) + leafIssueId (display); gates reachable via `isReplyReachable(<terminalKind string>)` (assert NO inline `{ terminal:` Terminal construction); passes `needsDurabilityFlip` from chain.needsDurabilityFlip on employee-row (assert it does NOT read `terminal.kind` for the flip); and never interpolates a UUID into rendered text (extend the 11-07 render-scan). For live-blocker-panel additionally assert: leafIssueId=null for the multi-hop case; the blockerLine <p> is suppressed for the reply branch (assert the blockerLine call is guarded by an affordance !== 'reply' condition); needsDurabilityFlip is NOT derived from data.terminal.kind.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both `employee-row.tsx` and `live-blocker-panel.tsx` import `ReplyInPlace` from `../_shared/reply-in-place.tsx` and `isReplyReachable` from the shared helper — the SAME component, no copied logic (SC3).
    - The reply affordance on each surface mounts `<ReplyInPlace>` with `leafIssueUuid` as the mutation id and `leafIssueId`/`awaitedPartyLabel` as the only displayed identifiers; `reachable = isReplyReachable(<terminalKind string>)` (chain.terminalKind / data.terminal.kind) — NO inline Terminal construction.
    - employee-row passes `needsDurabilityFlip={chain.needsDurabilityFlip}` (the real 14-04 boolean), NOT a data.terminal.kind proxy (test asserts).
    - live-blocker-panel: the standalone blockerLine `<p>` does NOT render for the reply branch (no duplicate headline); it still renders for non-reply affordances; leafIssueId=null for a multi-hop chain (CR-01).
    - employee-row still renders OwnerPickerPopover for `actionAffordance === 'assign'`; reply and assign are mutually exclusive per row.
    - The render-scan on both surfaces passes (no UUID in rendered text); `node scripts/build-ui.mjs` is green.
  </acceptance_criteria>
  <done>The Situation Room employee row and the Reader live blocker panel both mount the single shared <ReplyInPlace> on their reply branch, reachable computed off terminalKind (AWAITING_HUMAN-only), needsDurabilityFlip from the real row field, the Reader's duplicate blockerLine suppressed, and the multi-hop honest degrade preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount <ReplyInPlace> on the org-blocked backlog expander + full-suite green gate</name>
  <read_first>
    - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx (FULL — the orphan rows L82-119; the Assign gate L101; openIssue(identifier) L58; onAssignSuccess force-refetch)
    - src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts (the OrgBlockedRow type — after 14-04 it carries awaitedPartyLabel/targetAgentUuid/terminalKind/decisionOptions/leafIssueUuid/needsDurabilityFlip)
    - src/ui/surfaces/_shared/reply-in-place.tsx + src/shared/reply-reachable.ts (wave 2)
    - .planning/phases/14-do-it-here-action-loop/14-CONTEXT.md D-07/D-10 (one shared component on the backlog; reply on reachable orphan rows ⇔ AWAITING_HUMAN)
  </read_first>
  <behavior>
    - backlog expander: an orphan row whose actionAffordance === 'reply' AND isReplyReachable(row.terminalKind) true renders <ReplyInPlace> (passing row.identifier as leafIssueId, row.leafIssueUuid as leafIssueUuid — the LEAF UUID, NOT row.issueId which is the root; row.awaitedPartyLabel/targetAgentUuid/decisionOptions(null)/needsDurabilityFlip; companyPrefix/companyId/userId; navigate; onActed=onAssignSuccess).
    - backlog expander: actionAffordance === 'assign' still renders OwnerPickerPopover (unchanged; UNOWNED + AWAITING_AGENT_STUCK); mutually exclusive per orphan row.
    - backlog expander: a reachable-false reply row surfaces named action + Open↗ via the primitive (no dead Send); Open↗ keeps the existing identifier human-key URL.
    - needsDurabilityFlip = row.needsDurabilityFlip (the real 14-04 leaf-status boolean), NOT a terminalKind proxy.
    - No UUID rendered on the backlog surface (issueId/leafIssueUuid/targetAgentUuid stay dispatch-only; identifier is the displayed key).
  </behavior>
  <action>
    blocked-backlog-expander.tsx: import `ReplyInPlace` from `../_shared/reply-in-place.tsx` + `isReplyReachable` from `../../../shared/reply-reachable.ts`. In each orphan row's `clarity-orphan-actions` span, add a branch: when `row.actionAffordance === 'reply'`, render `<ReplyInPlace leafIssueId={row.identifier} leafIssueUuid={row.leafIssueUuid} awaitedPartyLabel={row.awaitedPartyLabel} namedAction={row.humanAction} decisionOptions={row.decisionOptions} needsDurabilityFlip={row.needsDurabilityFlip} reachable={isReplyReachable(row.terminalKind)} companyId={companyId} userId={userId} companyPrefix={companyPrefix} navigate={navigate} onActed={onAssignSuccess} />` (keep the existing OwnerPickerPopover for the 'assign' branch and the standalone Open↗ for the non-reply/non-reachable rows; ensure the 'reply' branch and the Assign branch are mutually exclusive — gate Assign on `=== 'assign'`, reply on `=== 'reply'`). Pass `row.leafIssueUuid` (the LEAF UUID from 14-04), NOT `row.issueId` (the root), as the mutation id. Pass `isReplyReachable(row.terminalKind)` — the string field threaded in 14-04, no inline Terminal construction.
    Run the full UI + worker + shared test suites and both builds as the phase gate.
    Write `test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs` (source-grep): assert the backlog imports the SAME `_shared/reply-in-place` (no copy) + the shared isReplyReachable; mounts it on the reply orphan rows; keeps OwnerPickerPopover for assign; passes `row.leafIssueUuid` as the mutation UUID (NOT row.issueId) + row.identifier as the display key; passes needsDurabilityFlip from row.needsDurabilityFlip; computes reachable via isReplyReachable(row.terminalKind) (no inline Terminal); and never renders a UUID (render-scan extension).
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs && node scripts/build-ui.mjs && node scripts/build-worker.mjs && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `blocked-backlog-expander.tsx` imports `ReplyInPlace` from the SAME `_shared/reply-in-place.tsx` the other two surfaces import + the shared isReplyReachable — proving SC3 (one primitive, three mounts, no copies) across the full set.
    - A reply/reachable orphan row mounts `<ReplyInPlace>` with `row.leafIssueUuid` (the LEAF UUID, NOT row.issueId) as the mutation id and `row.identifier` as the display key; needsDurabilityFlip = row.needsDurabilityFlip; reachable = isReplyReachable(row.terminalKind) (no inline Terminal).
    - An `assign` orphan row still renders OwnerPickerPopover; a reachable-false reply row shows named action + Open↗ (no dead Send); reply/assign mutually exclusive.
    - The backlog render-scan passes (no UUID rendered); `node scripts/build-ui.mjs` + `node scripts/build-worker.mjs` green.
    - Phase gate: `node --test test/shared/blocker-chain.test.mjs` green (engine untouched — determinism + AI-token guard); the three reply-in-place mount tests + the wave-1/wave-2/14-04 tests all pass.
  </acceptance_criteria>
  <done>All three surfaces mount the single shared <ReplyInPlace> from _shared (no copies, SC3); reply/assign/Open↗ are verdict-gated per row (reply ⇔ AWAITING_HUMAN via isReplyReachable(terminalKind)); the leaf-distinct leafIssueUuid + real needsDurabilityFlip are threaded; both builds and the engine determinism guard are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| three surfaces → shared <ReplyInPlace> | Verdict fields (terminalKind, leafIssueUuid, decisionOptions, needsDurabilityFlip) flow as props |
| surface render → DOM | The displayed identifiers (must stay UUID-free) on all three surfaces |
| row data (14-04) → reachable/flip decision | terminalKind drives reachable; needsDurabilityFlip drives the Shape-B flip — both REAL row fields, no proxies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-15 | Information Disclosure | a UUID rendered on any of the three surfaces | mitigate | the render-scan guard (extends 11-07) runs against all three mounts; leafIssueUuid/targetAgentUuid/targetIssueUuid/issueId(UUID) are passed as dispatch props only, never inside a rendered `{...}` text node |
| T-14-16 | Integrity/Spoofing | logic drift between three copies of the reply mechanic | mitigate | SC3 by construction — all three surfaces import the SAME `_shared/reply-in-place.tsx` + the SAME isReplyReachable; the per-surface tests assert the single import (no copied logic) |
| T-14-17 | Tampering | a reply affordance shown on a non-AWAITING_HUMAN row (dead/unproven Send) | mitigate | each mount computes `reachable` via `isReplyReachable(terminalKind)` (AWAITING_HUMAN-only) — the primitive removes Send when false (SC4); assign rows keep OwnerPickerPopover; reply/assign mutually exclusive |
| T-14-18 | Integrity | needsDurabilityFlip wrong because a surface proxied it from terminal.kind | mitigate | every surface passes the REAL row.needsDurabilityFlip / chain.needsDurabilityFlip (14-04 leaf-status boolean); the Reader (no leaf-status field) defaults false (comment-only, spike-safe); tests assert no `terminal.kind` proxy for the flip |
| T-14-19 | Repudiation | a successful reply not re-resolving the row (stale board) | mitigate | each surface forwards its existing force-refetch as `onActed` (Reader: usePluginData re-polls), so a successful reply re-resolves the row live (honest state) |
| T-14-20 | Spoofing | the Reader rendering the blocker headline twice (duplicate line) | mitigate | the standalone blockerLine `<p>` is suppressed for the reply branch (the primitive owns the namedAction line); test asserts the guard |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies in this plan; no install tasks |
</threat_model>

<verification>
- `node --test test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs` — all three mounts + render-scans + no-duplicate-line + no-terminal.kind-flip-proxy.
- `node scripts/build-ui.mjs` && `node scripts/build-worker.mjs` — both bundles green.
- `node --test test/shared/blocker-chain.test.mjs` — engine determinism + AI-token guard untouched.
- Manual deploy gate (separate from this plan): after build, the live BEAAA drill verifies a real AWAITING_HUMAN reply posts + the agent resumes (Phase-10 recipe); a Shape-B (status=blocked leaf) reply flips to in_progress durably — per DEPLOY-RUNBOOK, version-bump BOTH package.json and src/manifest.ts.
</verification>

<success_criteria>
- SC3: the reply-in-place + quick-decision primitive is available on the Situation Room, the Reader-view blocker panel, and the org-blocked backlog — the SAME shared primitive, not three copies (proven by the single-import assertions).
- SC4: out-of-system rows (reachable=false, i.e. not AWAITING_HUMAN) surface named action + Open↗, never a dead Send.
- SC5: UUIDs never rendered on any surface; the mutation carries the (leaf) UUID.
- DO-01/DO-04/DO-05 closed end-to-end (AWAITING_HUMAN reply posts + resumes via 14-01; Shape-B durable flip from the real needsDurabilityFlip; one shared primitive across three surfaces; honest out-of-system escape).
</success_criteria>

<output>
Create `.planning/phases/14-do-it-here-action-loop/14-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
