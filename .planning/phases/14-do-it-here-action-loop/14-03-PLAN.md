---
phase: 14-do-it-here-action-loop
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified:
  - src/ui/surfaces/situation-room/employee-row.tsx
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx
  - test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs
  - test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs
  - test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs
autonomous: true
requirements: [DO-01, DO-04, DO-05]
must_haves:
  truths:
    - "The SAME <ReplyInPlace> from src/ui/surfaces/_shared is imported and mounted on the Situation Room employee row, the Reader-view live blocker panel, and the org-blocked backlog expander — no copies (SC3)."
    - "On each surface, <ReplyInPlace> mounts on the reply/reachable branch (actionAffordance 'reply' and isReplyReachable true), passing the verdict's leafIssueId/leafIssueUuid/awaitedPartyLabel/targetAgentUuid + decisionOptions + needsDurabilityFlip."
    - "An out-of-system row (reachable false) surfaces the named action + Open↗ via the same primitive — no dead Send (SC4)."
    - "Each surface forwards its existing snapshot force-refetch as onActed so the row re-resolves after a successful reply."
    - "No UUID is rendered on any of the three surfaces (NO_UUID_LEAK render-scan extended to all three mounts)."
  artifacts:
    - path: "src/ui/surfaces/situation-room/employee-row.tsx"
      provides: "ReplyInPlace mounted on the needs_you reply/reachable branch"
      contains: "ReplyInPlace"
    - path: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      provides: "ReplyInPlace replacing the navigate-to-chat 'reply' affordance"
      contains: "ReplyInPlace"
    - path: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      provides: "ReplyInPlace on reply/reachable orphan rows"
      contains: "ReplyInPlace"
  key_links:
    - from: "src/ui/surfaces/situation-room/employee-row.tsx"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "single import, no copy"
      pattern: "from '\\.\\./_shared/reply-in-place"
    - from: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "single import, no copy"
      pattern: "reply-in-place"
    - from: "src/ui/surfaces/situation-room/blocked-backlog-expander.tsx"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "single import, no copy"
      pattern: "reply-in-place"
---

<objective>
Wire the ONE shared `<ReplyInPlace>` primitive (built in wave 2) into all three blocker surfaces — the Situation Room employee row, the Reader-view live blocker panel, and the org-blocked backlog expander — each on its `reply`/reachable branch, gated off the engine verdict. Same import, no copies (SC3). The out-of-system (reachable-false) case flows through the same primitive's Open↗-only render (SC4). Each surface passes its existing force-refetch as `onActed`. This closes DO-01/DO-04/DO-05 end-to-end against the live model.

Purpose: Prove SC3 by construction — one component, three mounts; the reply mechanic is now live on every surface that surfaces a blocker.
Output: edits to the three surfaces + one render-scan/mount test per surface.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-do-it-here-action-loop/14-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- The <ReplyInPlace> props (wave 2) the three surfaces must supply. -->

<ReplyInPlace> props — src/ui/surfaces/_shared/reply-in-place.tsx:
  { leafIssueId, leafIssueUuid, awaitedPartyLabel, namedAction, reachable, needsDurabilityFlip,
    decisionOptions, companyId, userId, companyPrefix, navigate, onActed }
  // reachable comes from isReplyReachable(verdict) (src/shared/reply-reachable.ts) — keyed on terminal.kind + targetAgentUuid.

Mount point A — src/ui/surfaces/situation-room/employee-row.tsx:
  // needs_you cluster L315-407. chain has: leafIssueId, leafIssueUuid, awaitedPartyLabel, targetAgentUuid,
  //   targetIssueUuid, actionAffordance, tier, needsYou; row.actionCard has decisionOptions/namedAction.
  // showAssign = chain?.actionAffordance === 'assign' (OwnerPickerPopover stays for 'assign').
  // existing openIssue + onAssignSuccess (the force-refetch) + companyPrefix/companyId/userId in scope.
  // The reply branch is the NON-assign needs_you case where actionAffordance === 'reply'.

Mount point B — src/ui/surfaces/reader/live-blocker-panel.tsx:
  // L228-230: case 'reply': onAction = () => replyInChat(agentDispatchTarget) — TODAY just navigates to chat.
  //   Phase 14 REPLACES this with a real <ReplyInPlace> dispatch.
  // data: BlockerChainResult (terminal, actionAffordance, awaitedPartyLabel, targetAgentUuid, targetIssueUuid, pathIds, needsYou).
  // companyPrefix (parsed), companyId, viewerUserId, nav.navigate in scope.
  // NOTE: this surface has NO leaf human key for a multi-hop chain (pathIds.length > 1) — pass leafIssueId
  //   = issueId ONLY for single-hop (pathIds.length <= 1), else null (CR-01 honest degrade carries into the primitive).
  // NOTE: this panel has no force-refetch callback today — usePluginData re-polls; pass a no-op onActed or trigger a light refresh.

Mount point C — src/ui/surfaces/situation-room/blocked-backlog-expander.tsx:
  // L82-119 orphan rows. row has: issueId (a UUID), identifier (human key), title, humanAction, actionAffordance.
  //   row currently lacks awaitedPartyLabel/targetAgentUuid/needsDurabilityFlip/decisionOptions — confirm the
  //   OrgBlockedBacklog row type (org-blocked-backlog-banner-types.ts) and the worker org-blocked-backlog builder
  //   carry the verdict fields; if the row type lacks awaitedPartyLabel/targetAgentUuid, thread them through the
  //   worker projection (read-only verdict fields already computed in the engine — additive to the row shape).
  // existing openIssue(identifier) + onAssignSuccess (force-refetch) + companyPrefix/companyId/userId in scope.
  // OwnerPickerPopover stays for actionAffordance === 'assign'.

The reachable predicate — src/shared/reply-reachable.ts: isReplyReachable({ terminal, targetAgentUuid }) -> boolean
Render-scan convention — test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (source-grep; no jsdom)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount <ReplyInPlace> on the Situation Room employee row + the Reader live blocker panel</name>
  <read_first>
    - src/ui/surfaces/situation-room/employee-row.tsx (FULL — the needs_you cluster L315-407; showAssign gate at L199; openIssue L203-209; onAssignSuccess force-refetch; the chain shape with leafIssueUuid/awaitedPartyLabel/targetAgentUuid/targetIssueUuid; row.actionCard.decisionOptions/namedAction)
    - src/ui/surfaces/reader/live-blocker-panel.tsx (FULL — the affordance switch L224-270; the 'reply' case L228-230 that today only navigates to chat; the CR-01 multi-hop no-button rule L261; the openIssue human-key URL L163-166)
    - src/ui/surfaces/_shared/reply-in-place.tsx (wave 2 — the props it consumes)
    - src/shared/reply-reachable.ts (wave 2 — isReplyReachable)
    - .planning/phases/14-do-it-here-action-loop/14-CONTEXT.md D-07/D-08/D-10/D-11 (one shared component; the props; reachable predicate; Open↗ human-key)
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (the render-scan convention)
  </read_first>
  <behavior>
    - employee-row: on a needs_you row where actionAffordance === 'reply', the row renders <ReplyInPlace> (NOT the chat-deep-link path), passing chain.leafIssueId/leafIssueUuid/awaitedPartyLabel/targetAgentUuid, row.actionCard?.decisionOptions, namedAction = the action-card sentence or the deterministic line, needsDurabilityFlip derived from the verdict (true when the leaf is a status='blocked' terminal), reachable = isReplyReachable(verdict-shaped input), companyPrefix/companyId/userId, navigate, and onActed = onAssignSuccess.
    - employee-row: actionAffordance === 'assign' still renders OwnerPickerPopover (unchanged); 'reply' renders ReplyInPlace; the two are mutually exclusive per row (design-spec "No Assign owner here" on a reply row).
    - employee-row: an out-of-system reply row (reachable false) renders the named action + Open↗ via the primitive — no dead Send.
    - live-blocker-panel: the 'reply' affordance case mounts <ReplyInPlace> instead of the navigate-to-chat callback; leafIssueId = issueId for a single-hop chain (pathIds.length <= 1), else null (CR-01 honest degrade); leafIssueUuid = targetIssueUuid; awaitedPartyLabel/targetAgentUuid/decisionOptions from data; onActed a light refresh/no-op.
    - Both surfaces: no UUID is interpolated into any rendered string.
  </behavior>
  <action>
    employee-row.tsx: import `ReplyInPlace` from `../_shared/reply-in-place.tsx`. In the needs_you action cluster, add a branch for `chain.actionAffordance === 'reply'` (alongside the existing `showAssign` and owned branches): render `<ReplyInPlace>` with `leafIssueId={chain.leafIssueId}`, `leafIssueUuid={chain.leafIssueUuid}`, `awaitedPartyLabel={chain.awaitedPartyLabel}`, `namedAction={row.actionCard?.namedAction ?? \`waiting on ${chain.awaitedPartyLabel}\`}`, `decisionOptions={row.actionCard?.decisionOptions ?? null}`, `needsDurabilityFlip={/* true when the leaf is a status='blocked' terminal — derive from the verdict the row already carries; if no explicit signal, pass false (comment-only default, D-04) */}`, `reachable={isReplyReachable({ terminal: /* the row carries terminal kind via tier/affordance; thread the terminal kind onto the chain row if not present, additively */, targetAgentUuid: chain.targetAgentUuid })}`, `companyId`, `userId`, `companyPrefix`, `navigate`, `onActed={onAssignSuccess}`. If the chain row does not yet carry `terminal.kind` (only affordance/tier), thread the engine `terminal.kind` onto the SituationEmployeeRow.blockerChain shape additively (worker build-employees-rollup already has the verdict — pass it through; it is a structural field, no new fetch). Keep OwnerPickerPopover for the 'assign' branch.
    live-blocker-panel.tsx: import `ReplyInPlace` + `isReplyReachable`. Replace the `case 'reply'` handling so that instead of wiring `onAction = () => replyInChat(...)`, the render emits `<ReplyInPlace>` for the reply affordance. Pass `leafIssueId={data.pathIds.length <= 1 ? issueId : null}` (single-hop only — CR-01), `leafIssueUuid={data.targetIssueUuid ?? null}`, `awaitedPartyLabel={data.awaitedPartyLabel}`, `namedAction={blockerLine(data)}`, `decisionOptions={null}` (no action card on this surface; chips are SR/backlog where the card is attached — pass null here), `needsDurabilityFlip={data.terminal.kind === 'AWAITING_AGENT_STUCK' /* status-blocked durability case; comment-only default otherwise */}`, `reachable={isReplyReachable(data)}`, `companyId`, `userId={viewerUserId}`, `companyPrefix`, `navigate={nav.navigate}`, `onActed={() => { /* light refresh; usePluginData re-polls */ }}`. Remove the now-dead `replyInChat` path for the 'reply' affordance (keep nudge/assign/open/none as-is). Preserve the existing primaryActionLabel/blockerLine for the non-reply affordances.
    Write `test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs` and `test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs` (source-grep): assert each surface imports ReplyInPlace from `_shared` (single shared import), mounts it on the reply affordance, passes leafIssueUuid (mutation) + leafIssueId (display) correctly, gates reachable via isReplyReachable, and never interpolates a UUID into rendered text (extend the 11-07 render-scan). Assert live-blocker-panel passes leafIssueId=null for the multi-hop case.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - Both `employee-row.tsx` and `live-blocker-panel.tsx` import `ReplyInPlace` from `../_shared/reply-in-place.tsx` (or the relative path) — the SAME component, no copied logic (SC3).
    - The reply affordance on each surface mounts `<ReplyInPlace>` with `leafIssueUuid` as the mutation id and `leafIssueId`/`awaitedPartyLabel` as the only displayed identifiers; `reachable` is computed via `isReplyReachable` (verdict-driven, not a string match).
    - live-blocker-panel passes `leafIssueId = null` for a multi-hop chain (pathIds.length > 1) so the primitive degrades honestly (no 404 Open↗) — CR-01 preserved.
    - employee-row still renders OwnerPickerPopover for `actionAffordance === 'assign'`; reply and assign are mutually exclusive per row.
    - The render-scan on both surfaces passes (no UUID in rendered text); `node scripts/build-ui.mjs` is green.
  </acceptance_criteria>
  <done>The Situation Room employee row and the Reader live blocker panel both mount the single shared <ReplyInPlace> on their reply branch, verdict-gated, with the mutation/display split intact and the multi-hop honest degrade preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount <ReplyInPlace> on the org-blocked backlog expander + full-suite green gate</name>
  <read_first>
    - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx (FULL — the orphan rows L82-119; the Assign gate L101; openIssue(identifier) L58; onAssignSuccess force-refetch)
    - src/ui/surfaces/situation-room/org-blocked-backlog-banner-types.ts (the OrgBlockedBacklog row type — confirm which verdict fields the row carries; thread awaitedPartyLabel/targetAgentUuid/terminal.kind/decisionOptions/leafIssueUuid additively if absent)
    - the worker org-blocked-backlog builder (grep `org-blocked-backlog` under src/worker — it already computes the verdict per row; pass the verdict display+dispatch fields through additively, no new fetch)
    - src/ui/surfaces/_shared/reply-in-place.tsx + src/shared/reply-reachable.ts (wave 2)
    - .planning/phases/14-do-it-here-action-loop/14-CONTEXT.md D-07/D-10 (one shared component on the backlog; reply on reachable orphan rows)
  </read_first>
  <behavior>
    - backlog expander: an orphan row whose actionAffordance === 'reply' AND isReplyReachable true renders <ReplyInPlace> (passing identifier as leafIssueId, issueId as leafIssueUuid, the row's awaitedPartyLabel/targetAgentUuid/decisionOptions/needsDurabilityFlip, companyPrefix/companyId/userId, navigate, onActed=onAssignSuccess).
    - backlog expander: actionAffordance === 'assign' still renders OwnerPickerPopover (unchanged); the two are mutually exclusive per orphan row.
    - backlog expander: a reachable-false reply row surfaces named action + Open↗ via the primitive (no dead Send); Open↗ keeps the existing identifier human-key URL.
    - If the OrgBlockedBacklog row type lacks the verdict display/dispatch fields, they are threaded through the worker projection additively (read-only verdict fields, no new host fetch).
    - No UUID rendered on the backlog surface.
  </behavior>
  <action>
    blocked-backlog-expander.tsx: import `ReplyInPlace` from `../_shared/reply-in-place.tsx` + `isReplyReachable` from the shared helper. In each orphan row's `clarity-orphan-actions` span, add a branch: when `row.actionAffordance === 'reply'`, render `<ReplyInPlace leafIssueId={row.identifier} leafIssueUuid={row.issueId} awaitedPartyLabel={row.awaitedPartyLabel} namedAction={row.humanAction} decisionOptions={row.decisionOptions ?? null} needsDurabilityFlip={/* derive from row terminal kind; default false */} reachable={isReplyReachable({ terminal: { kind: row.terminalKind } as any, targetAgentUuid: row.targetAgentUuid })} companyId={companyId} userId={userId} companyPrefix={companyPrefix} navigate={navigate} onActed={onAssignSuccess} />` (keep the existing OwnerPickerPopover for the 'assign' branch and the standalone Open↗ for non-reply rows). If `OrgBlockedBacklog` row type lacks `awaitedPartyLabel` / `targetAgentUuid` / `terminalKind` / `decisionOptions` / `issueId`-as-UUID, add them to `org-blocked-backlog-banner-types.ts` and thread them from the worker org-blocked-backlog builder additively (the engine verdict already carries them — projection only, no new fetch; mirror how build-employees-rollup passes the verdict fields).
    Run the full UI + worker + shared test suites and both builds as the phase gate.
    Write `test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs` (source-grep): assert the backlog imports the SAME `_shared/reply-in-place` (no copy), mounts it on the reply/reachable orphan rows, keeps OwnerPickerPopover for assign, passes issueId as the UUID + identifier as the display key, and never renders a UUID (render-scan extension).
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs && node scripts/build-ui.mjs && node scripts/build-worker.mjs && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `blocked-backlog-expander.tsx` imports `ReplyInPlace` from the SAME `_shared/reply-in-place.tsx` the other two surfaces import — proving SC3 (one primitive, three mounts, no copies) across the full set.
    - A reply/reachable orphan row mounts `<ReplyInPlace>` with `row.issueId` as the mutation UUID and `row.identifier` as the display key; an `assign` orphan row still renders OwnerPickerPopover; a reachable-false reply row shows named action + Open↗ (no dead Send).
    - Any verdict fields newly threaded onto the OrgBlockedBacklog row are additive (projection-only, no new host fetch); `node scripts/build-worker.mjs` green.
    - The backlog render-scan passes (no UUID rendered); `node scripts/build-ui.mjs` green.
    - Phase gate: `node --test test/shared/blocker-chain.test.mjs` green (engine untouched — determinism + AI-token guard); the three reply-in-place mount tests + the wave-1/wave-2 tests all pass.
  </acceptance_criteria>
  <done>All three surfaces mount the single shared <ReplyInPlace> from _shared (no copies, SC3); reply/assign/Open↗ are verdict-gated per row; both builds and the engine determinism guard are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| three surfaces → shared <ReplyInPlace> | Verdict fields (leafIssueUuid, targetAgentUuid, decisionOptions) flow as props |
| surface render → DOM | The displayed identifiers (must stay UUID-free) on all three surfaces |
| worker org-blocked-backlog projection → backlog row | Additive verdict fields threaded to the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Information Disclosure | a UUID rendered on any of the three surfaces | mitigate | the render-scan guard (extends 11-07) runs against all three mounts; leafIssueUuid/targetAgentUuid/targetIssueUuid/issueId(UUID) are passed as dispatch props only, never inside a rendered `{...}` text node |
| T-14-15 | Integrity/Spoofing | logic drift between three copies of the reply mechanic | mitigate | SC3 by construction — all three surfaces import the SAME `_shared/reply-in-place.tsx`; the per-surface tests assert the single import (no copied logic) |
| T-14-16 | Tampering | a reply affordance shown on a non-reachable row (dead Send) | mitigate | each mount computes `reachable` via `isReplyReachable(verdict)` — the primitive removes Send when false (SC4); assign rows keep OwnerPickerPopover, mutually exclusive |
| T-14-17 | Tampering | a non-additive change to the OrgBlockedBacklog projection breaking the worker | mitigate | new verdict fields are projection-only (the engine already computes them); no new host fetch, no schema change; worker build gate confirms type-safety |
| T-14-18 | Repudiation | a successful reply not re-resolving the row (stale board) | mitigate | each surface forwards its existing force-refetch as `onActed`, so a successful reply re-resolves the row live (honest state) |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | NO new dependencies in this plan; no install tasks |
</threat_model>

<verification>
- `node --test test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs test/ui/surfaces/situation-room/blocked-backlog-reply-in-place.test.mjs` — all three mounts + render-scans.
- `node scripts/build-ui.mjs` && `node scripts/build-worker.mjs` — both bundles green.
- `node --test test/shared/blocker-chain.test.mjs` — engine determinism + AI-token guard untouched.
- Manual deploy gate (separate from this plan): after build, the live BEAAA drill verifies a real reply posts + the agent resumes (Phase-10 recipe) — per DEPLOY-RUNBOOK, version-bump BOTH package.json and src/manifest.ts.
</verification>

<success_criteria>
- SC3: the reply-in-place + quick-decision primitive is available on the Situation Room, the Reader-view blocker panel, and the org-blocked backlog — the SAME shared primitive, not three copies (proven by the single-import assertions).
- SC4: out-of-system rows surface named action + Open↗, never a dead Send (verdict-driven on every surface).
- SC5: UUIDs never rendered on any surface; the mutation carries the UUID.
- DO-01/DO-04/DO-05 closed end-to-end (reply posts + resumes via 14-01; one shared primitive across three surfaces; honest out-of-system escape).
</success_criteria>

<output>
Create `.planning/phases/14-do-it-here-action-loop/14-03-SUMMARY.md` when done.
</output>
