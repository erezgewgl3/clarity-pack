---
phase: 15-cockpit-ia-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/situation/build-pulse-summary.ts
  - src/worker/handlers/situation-room.ts
  - test/worker/situation/build-pulse-summary.test.mjs
autonomous: true
requirements: [COCK-01]
user_setup: []

must_haves:
  truths:
    - "The situation.snapshot payload carries a worker-computed `pulse` summary with four integer vital-sign counts (need-you / in-motion / stuck / self-clearing)."
    - "The four counts are aggregations over the per-row engine verdicts already on situation_employees + the existing needsYou.count — NO new host fetch, NO new classification, NO ownership re-derivation in the view."
    - "When the rollup is empty or throws, the handler still returns a deterministic all-zero pulse — the snapshot never blanks."
  artifacts:
    - path: "src/worker/situation/build-pulse-summary.ts"
      provides: "Pure aggregation: situation_employees[] + needsYou.count -> {needYou,inMotion,stuck,selfClearing}"
      exports: ["buildPulseSummary", "PulseSummary"]
    - path: "src/worker/handlers/situation-room.ts"
      provides: "situation.snapshot return widened with an additive `pulse` field"
      contains: "pulse"
    - path: "test/worker/situation/build-pulse-summary.test.mjs"
      provides: "Count-definition + degrade tests for the aggregation"
  key_links:
    - from: "src/worker/handlers/situation-room.ts"
      to: "src/worker/situation/build-pulse-summary.ts"
      via: "buildPulseSummary(employeesWithCards, needsYou)"
      pattern: "buildPulseSummary"
---

<objective>
Add a worker-computed `pulse` summary to the `situation.snapshot` payload: four integer vital-sign counts (need-you / in-motion / stuck / self-clearing) aggregated from the per-row engine verdicts the rollup already produces. This keeps all verdict-derived numbers on the worker side of the bridge (SC3 — the view never re-derives ownership) and gives the Pulse header (Plan 15-02) a single tested aggregation function to render.

Purpose: COCK-01 / SC1 require a Pulse header stating company status + four vital signs. D-01 locks those counts as worker-side aggregations over existing verdicts (NO new fetch, NO migration). This plan delivers the worker half so Plan 15-02 stays a dumb renderer.

Output: `buildPulseSummary` pure function + `PulseSummary` type; the `situation.snapshot` handler returns an additive `pulse` field; a degrade-safe all-zero fallback; the aggregation test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md

<interfaces>
<!-- The verdict fields the aggregation sums. Read-only consumed; NEVER edited. -->

From src/shared/types.ts (BlockerChainResult.tier — the REAL engine tier values):
```
tier: 'needs-you' | 'in-motion' | 'watch'
Terminal kinds: AWAITING_HUMAN | AWAITING_AGENT_WORKING | AWAITING_AGENT_STUCK
              | SELF_RESOLVING | EXTERNAL | CYCLE | UNOWNED | UNCLASSIFIED
```

From src/worker/situation/build-employees-rollup.ts (the row shape the aggregation reads):
```
SituationEmployeeRow.group: 'needs_you' | 'working' | 'idle'   // agent-STATE axis
SituationEmployeeRow.blockerChain: { tier, terminalKind, actionAffordance, needsYou, ... } | null
NeedsYou = { count: number; topAction: {...} | null }          // per-leaf deduped human-actionable count
```

From src/worker/handlers/situation-room.ts (the return object this plan widens):
```
return { org_blocked_backlog, situation_employees: employeesWithCards, needsYou, taken_at }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildPulseSummary pure aggregation + PulseSummary type</name>
  <files>src/worker/situation/build-pulse-summary.ts, test/worker/situation/build-pulse-summary.test.mjs</files>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts (the SituationEmployeeRow + NeedsYou shapes this function consumes; the `group` vs `blockerChain.tier` two-axis distinction at lines ~79-176 — CRITICAL: group uses underscores, tier uses hyphens, they are NOT 1:1)
    - src/shared/types.ts (BlockerChainResult.tier values + the 8 Terminal kinds — read-only)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-01 (the LOCKED count definitions)
    - test/worker/situation/build-employees-rollup-needsyou.test.mjs (the .ts-via-node test import idiom + makeCtx fixture pattern to mirror)
  </read_first>
  <behavior>
    - need-you count: returns the passed needsYou.count verbatim (the Phase-12 per-leaf-deduped human-actionable count — per D-01, NOT re-counted from rows).
    - in-motion count: number of rows where blockerChain.tier === 'in-motion' (⇔ AWAITING_AGENT_WORKING) PLUS chainless rows where group === 'working' AND blockerChain == null (the calm "what each agent is working on" denominator per D-01/D-05; a row with an in-motion chain is counted once via the tier branch, never double-counted with the group branch).
    - stuck count: number of rows where blockerChain.terminalKind === 'AWAITING_AGENT_STUCK' (design-spec "1 agent stuck"; tier 'watch').
    - self-clearing count: number of rows where blockerChain.terminalKind === 'SELF_RESOLVING' (design-spec "2 clearing themselves").
    - degrade: empty employees[] + needsYou {count:0} -> all four counts 0 (deterministic floor, never throws).
    - purity: no clock, no I/O, no host fetch — a pure sum over the inputs.
  </behavior>
  <action>
    Create `src/worker/situation/build-pulse-summary.ts` exporting `PulseSummary = { needYou: number; inMotion: number; stuck: number; selfClearing: number }` and a pure `buildPulseSummary(employees: SituationEmployeeRow[], needsYou: NeedsYou): PulseSummary`. Import the `SituationEmployeeRow` + `NeedsYou` types from `build-employees-rollup.ts` (type-only import). Compute each count per the LOCKED D-01 definitions in `<behavior>`: `needYou` = `needsYou.count`; `inMotion` = rows with `blockerChain?.tier === 'in-motion'` plus rows with `blockerChain == null && group === 'working'` (guard the double-count: the group branch only counts chainless rows); `stuck` = rows with `blockerChain?.terminalKind === 'AWAITING_AGENT_STUCK'`; `selfClearing` = rows with `blockerChain?.terminalKind === 'SELF_RESOLVING'`. Use a single pass / `filter().length` reductions. No ownership re-derivation — every branch reads an existing engine verdict field. Write the test mirroring the rollup-needsyou test's `.ts`-via-node import idiom: build small fixture row arrays (a row per tier/terminalKind + a chainless working row + a chainless idle row), assert each count, assert the in-motion no-double-count (a working-group row WITH an in-motion chain counts once), and assert the all-zero degrade on empty input.
  </action>
  <verify>
    <automated>node --test test/worker/situation/build-pulse-summary.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `buildPulseSummary` + `PulseSummary` are exported from the new file.
    - Test asserts: need-you === needsYou.count; in-motion counts in-motion-tier rows + chainless working rows with NO double-count; stuck === AWAITING_AGENT_STUCK rows; self-clearing === SELF_RESOLVING rows; empty input -> {0,0,0,0}.
    - The function performs zero host calls / zero awaits (pure) and references only existing verdict fields — `grep -v '^//' src/worker/situation/build-pulse-summary.ts | grep -c 'ctx\.\|await\|fetch'` is 0.
    - `node --test test/worker/situation/build-pulse-summary.test.mjs` passes.
  </acceptance_criteria>
  <done>The aggregation exists, is pure, encodes the four LOCKED D-01 count definitions, degrades to all-zero, and its test is green.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the additive `pulse` field into the situation.snapshot return</name>
  <files>src/worker/handlers/situation-room.ts</files>
  <read_first>
    - src/worker/handlers/situation-room.ts (the FULL handler; the return object at lines ~201-206; the degrade-safe empty-rollup fallbacks at lines ~115-130 that this plan must mirror for the pulse)
    - src/worker/situation/build-pulse-summary.ts (the function created in Task 1 — its signature + return shape)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md (domain: "No migration" — the pulse is additive-payload-only; NOT a schema change)
  </read_first>
  <action>
    Import `buildPulseSummary` (+ the `PulseSummary` type) from `../situation/build-pulse-summary.ts`. After `employeesWithCards` is built (line ~188-192) and `needsYou` is resolved, compute `const pulse = buildPulseSummary(employeesWithCards, needsYou)`. Add `pulse` to the returned object alongside `org_blocked_backlog`, `situation_employees`, `needsYou`, `taken_at` (additive — no field removed, no migration, no DDL). The compute is pure and cannot throw, but for belt-and-suspenders consistency with the existing degrade pattern, the function already returns all-zero on empty input, so a degraded (empty employees) snapshot still carries a real `pulse: {needYou:0,inMotion:0,stuck:0,selfClearing:0}`. Confirm in the header comment that this is additive-payload-only per D-01 (no situation_snapshots write, no migration).
  </action>
  <verify>
    <automated>node scripts/build-worker.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `situation-room.ts` imports `buildPulseSummary` and returns a `pulse` field on the snapshot object.
    - No migration file is added (confirm: `ls migrations/ | wc -l` is unchanged from before this plan) and no `situation_snapshots` write is introduced.
    - `grep -c 'pulse' src/worker/handlers/situation-room.ts` is >= 2 (import + return).
    - `node scripts/build-worker.mjs` is green; `npx tsc --noEmit` reports no new errors in situation-room.ts.
    - The engine purity guard is unaffected: `node --test test/shared/blocker-chain.test.mjs` still passes (blocker-chain.ts untouched).
  </acceptance_criteria>
  <done>The snapshot returns an additive worker-computed `pulse` summary; no migration introduced; worker build + engine guard green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker -> UI bridge | The snapshot payload (now incl. `pulse`) crosses to same-origin trusted UI. The pulse carries only integers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information disclosure | buildPulseSummary output | mitigate | The pulse is four integers — no labels, no UUIDs, no prose. NO_UUID_LEAK by construction (integers cannot carry a UUID). |
| T-15-02 | Tampering | additive `pulse` payload | accept | Additive-only field; no schema/migration; plugin-disable leaves data intact (no new table). |
| T-15-03 | Denial of service | aggregation over rows | mitigate | Pure O(n) sum over an already-bounded employees[] (the rollup caps the fetch); no new fetch, no recursion. |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan — verify `git diff package.json` shows no dependency additions. |
</threat_model>

<verification>
- `node --test test/worker/situation/build-pulse-summary.test.mjs` (aggregation + degrade).
- `node scripts/build-worker.mjs` green.
- `node --test test/shared/blocker-chain.test.mjs` still green (engine untouched — PRIM-03 determinism + the LLM-token grep guard at line 408 unaffected).
- No migration added; no situation_snapshots write.
</verification>

<success_criteria>
- The situation.snapshot payload carries a worker-computed `pulse: {needYou,inMotion,stuck,selfClearing}` aggregated from existing verdicts (SC1 worker half, SC3 no view re-derivation).
- All four counts are integers; the field is additive (no migration — confirms the domain "No migration").
- Degrade-safe: an empty/thrown rollup still returns an all-zero pulse (SC4 floor for the count chips).
</success_criteria>

<output>
Create `.planning/phases/15-cockpit-ia-redesign/15-01-SUMMARY.md` when done.
</output>
