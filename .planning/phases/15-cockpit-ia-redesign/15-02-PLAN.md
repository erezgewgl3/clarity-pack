---
phase: 15-cockpit-ia-redesign
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - src/ui/surfaces/situation-room/pulse-header.tsx
  - src/ui/surfaces/situation-room/pulse-sentence.ts
  - src/ui/primitives/theme.css
  - test/ui/surfaces/situation-room/pulse-header.test.mjs
  - test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs
autonomous: true
requirements: [COCK-01]
user_setup: []

must_haves:
  truths:
    - "A <PulseHeader> renders a deterministic one-sentence company-status line + four labelled vital-sign chips (need-you / in-motion / stuck / self-clearing) sourced from snapshot.pulse."
    - "The Phase-8 banner role folds into the Pulse: the highest-leverage need-you state is reflected in the Pulse sentence; no second standalone status line."
    - "The Pulse sentence is a PURE deterministic template from the four counts — it never blanks, never depends on the Editor-Agent (SC4)."
    - "The Pulse renders human labels + integers only — zero company-prefix literals, zero UUID text (NO_UUID_LEAK, instance-agnostic)."
  artifacts:
    - path: "src/ui/surfaces/situation-room/pulse-sentence.ts"
      provides: "Pure counts -> deterministic status sentence + adjective"
      exports: ["buildPulseSentence"]
    - path: "src/ui/surfaces/situation-room/pulse-header.tsx"
      provides: "<PulseHeader> — sentence + four vital-sign chips"
      exports: ["PulseHeader", "PulseSummary"]
    - path: "src/ui/primitives/theme.css"
      provides: "scoped .clarity-pulse / .clarity-pulse-vitals / .clarity-pulse-vital CSS under [data-clarity-surface='situation-room']"
      contains: "clarity-pulse"
    - path: "test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs"
      provides: "render-scan UUID guard extended to PulseHeader"
  key_links:
    - from: "src/ui/surfaces/situation-room/pulse-header.tsx"
      to: "src/ui/surfaces/situation-room/pulse-sentence.ts"
      via: "buildPulseSentence(pulse)"
      pattern: "buildPulseSentence"
---

<objective>
Build the `<PulseHeader>` component: a deterministic one-sentence company-status line + four labelled vital-sign chips (need-you / in-motion / stuck / self-clearing), reading `snapshot.pulse` (from Plan 15-01). This is the always-visible "how's the company?" answer that supersedes the Phase-8 `needs-you-banner` (D-07). Ship the deterministic template floor only this phase (D-02/D-03 planner discretion: defer the optional Editor-Agent prose enrichment for a clean capstone). Map the mockup's `.pulse` / `.vitals` / `.vital` tokens to host CSS scoped under the situation-room surface.

Purpose: COCK-01 / SC1 — the Pulse header. D-02 locks the sentence as a deterministic template (always-on floor, the degrade target for SC4); the chips are always deterministic counts. D-07 folds the banner in. D-10 requires extending the NO_UUID_LEAK render-scan to this new render path.

Output: `buildPulseSentence` pure helper + `<PulseHeader>` + the `.clarity-pulse*` CSS + a render-contract test + a NO_UUID_LEAK render-scan test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md
@docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md

<interfaces>
<!-- The PulseSummary shape Plan 15-01 added to the snapshot. The UI carries a
     STRUCTURAL mirror (the UI bundle does not import worker types). -->

From snapshot.pulse (Plan 15-01 build-pulse-summary.ts):
```
PulseSummary = { needYou: number; inMotion: number; stuck: number; selfClearing: number }
```

From src/ui/surfaces/situation-room/needs-you-banner.tsx (the role being folded in — D-07):
```
// Three variants the banner showed (urgent+unowned / urgent+all-owned / neutral).
// The Pulse sentence + need-you chip SUPERSEDE these. The Phase-8 lock holds:
// top-callout-only; per-row highlight was REJECTED.
```

From the mockup .superpowers/brainstorm/1041-1780319036/content/cockpit-redesign-v3.html (visual ground truth):
```
.pulse -> .when (status line) + .head (gold <b> highlights) + .vitals (four .vital chips: you/mov/stk/slf)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildPulseSentence — pure deterministic counts -> status sentence</name>
  <files>src/ui/surfaces/situation-room/pulse-sentence.ts, test/ui/surfaces/situation-room/pulse-header.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/needs-you-banner.tsx (the three Phase-8 status copy variants this sentence supersedes; the "✓ 0 need you" neutral case, the "⚠ N actions needed" urgent case)
    - docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md §3 Section 3 ("one editorial sentence + four vital signs"; the example "The company is mostly moving")
    - .claude/skills/sketch-findings-clarity-pack/SKILL.md ("degraded states always name themselves — no silent failures"; the count->adjective voice)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-02 (deterministic template is the must-ship floor + the degrade target) and "Claude's Discretion" (exact wording + count->adjective mapping is yours)
  </read_first>
  <behavior>
    - need-you > 0: sentence leads with the human-actionable count, e.g. "N thing(s) need you" (singular/plural correct), then appends the in-motion reassurance, e.g. " · M in motion".
    - need-you === 0 and in-motion > 0: "Nothing needs you — M in motion." (calm/control voice).
    - all four counts 0: an honest floor sentence, e.g. "The board is clear." (never blank).
    - stuck / self-clearing surface in the sentence tail only when > 0, e.g. " · K stuck · J self-clearing" (omit a zero count from the tail — a zero is shown by its chip, not the prose).
    - PURE: a function of the four integers only — no clock, no Editor-Agent input, no host fetch. Same inputs -> same string (deterministic; this is the SC4 degrade target).
    - the returned string contains NO digits-as-uuid, NO company prefix, NO raw id — only counts + static English.
  </behavior>
  <action>
    Create `src/ui/surfaces/situation-room/pulse-sentence.ts` exporting a pure `buildPulseSentence(pulse: { needYou: number; inMotion: number; stuck: number; selfClearing: number }): string`. Implement the deterministic template per `<behavior>`, choosing the exact wording + count->adjective mapping (Claude's discretion per D-02) — keep it plain-English, singular/plural-correct, and degrade-honest. Do NOT call any Editor-Agent / AI path (D-03 deferral — deterministic floor only this phase). Write the test (`pulse-header.test.mjs`) covering: need-you>0 phrasing + plural; need-you===0+in-motion>0 calm phrasing; all-zero floor sentence; stuck/self-clearing appended only when >0; determinism (same input twice -> identical string). (This test file also covers the component render contract in Task 2 — add those cases there.)
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/pulse-header.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `buildPulseSentence` is exported and pure (no `import` of any hook/AI/fetch — `grep -v '^//' src/ui/surfaces/situation-room/pulse-sentence.ts | grep -c 'usePlugin\|fetch\|ctx\.\|driveAction\|driveGloss'` is 0).
    - Test asserts the four count-regime phrasings + plural correctness + stuck/self-clearing-omitted-when-zero + determinism.
    - The all-zero case returns a non-empty honest sentence (never "").
    - `node --test test/ui/surfaces/situation-room/pulse-header.test.mjs` passes.
  </acceptance_criteria>
  <done>The deterministic sentence helper exists, is pure, encodes the four count regimes + plural rules, never blanks, and its test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: <PulseHeader> component + scoped CSS</name>
  <files>src/ui/surfaces/situation-room/pulse-header.tsx, src/ui/primitives/theme.css, test/ui/surfaces/situation-room/pulse-header.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/needs-you-banner.tsx (the component shape + the React-text-node-only / no-dangerouslySetInnerHTML security posture to carry over; the banner this supersedes)
    - src/ui/surfaces/situation-room/pulse-sentence.ts (the helper from Task 1 the header calls)
    - src/ui/primitives/theme.css (the `[data-clarity-surface='situation-room']` scope block starting ~line 697; the existing `.clarity-employee-*` / `.clarity-needs-you-*` rules + token usage to match; the clarity- prefix convention)
    - docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md §3 Section 3 + the mockup .pulse/.vitals/.vital scaffold (visual ground truth)
    - .claude/skills/sketch-findings-clarity-pack/SKILL.md (palette: --you gold = needs-you, --live green = in-motion, --warn amber / --alert terracotta = watch; Instrument Serif italic display; inherit host CSS — ship NO parallel Tailwind)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-07 (banner fold), D-08 (Pulse never blanks), D-10 (instance-agnostic + NO_UUID_LEAK), "Claude's Discretion" (CSS class names + clarity- prefix; map mockup tokens to host tokens)
  </read_first>
  <behavior>
    - PulseHeader renders the deterministic sentence (from buildPulseSentence) in a status line.
    - It renders exactly four labelled vital-sign chips from snapshot.pulse: need-you (gold), in-motion (green), stuck (amber/terracotta), self-clearing (calm) — each chip shows its integer count + a static English label.
    - The four chips ALWAYS render even when a count is 0 (a zero is itself a signal — the partition-then-render-empty convention).
    - It renders human labels + integers only: no companyPrefix string, no UUID, no raw id.
    - Given a defensively-absent/undefined pulse prop, it renders the all-zero floor (never throws, never blanks — SC4).
  </behavior>
  <action>
    Create `src/ui/surfaces/situation-room/pulse-header.tsx` exporting a `PulseSummary` structural type (the UI mirror of Plan 15-01's shape — the UI bundle does NOT import worker types) and `<PulseHeader>` taking `{ pulse?: PulseSummary | null }`. Render: a `.clarity-pulse` root containing a `.clarity-pulse-sentence` line (text from `buildPulseSentence(pulse ?? {needYou:0,inMotion:0,stuck:0,selfClearing:0})`) and a `.clarity-pulse-vitals` row of four `.clarity-pulse-vital` chips, each with a count `<span>` + a static label text node (need you / in motion / stuck / self-clearing). Use modifier classes per vital (e.g. `clarity-pulse-vital-you|mov|stk|slf`) so CSS can tint them. Every visible string is a React text node (NO dangerouslySetInnerHTML). Do NOT render any companyPrefix / id / UUID. Add the scoped CSS to `src/ui/primitives/theme.css` UNDER the existing `[data-clarity-surface='situation-room']` block: `.clarity-pulse` (the always-visible header card), `.clarity-pulse-sentence` (Instrument Serif italic display per sketch-findings), `.clarity-pulse-vitals` (flex row), `.clarity-pulse-vital` + the four tint modifiers mapping to host tokens (gold/green/amber/terracotta families). Inherit host tokens — ship NO parallel Tailwind. Extend `pulse-header.test.mjs` (from Task 1) with the component-render-contract cases: source-grep that the component renders four vital chips + the sentence call + zero companyPrefix/UUID interpolation, and a string-render simulation asserting the four counts + labels appear and an undefined-pulse renders the all-zero floor.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/pulse-header.test.mjs && node scripts/build-ui.mjs && node scripts/check-css-scope.mjs</automated>
  </verify>
  <acceptance_criteria>
    - PulseHeader renders four labelled vital-sign chips from snapshot.pulse + the deterministic sentence (`grep -c 'clarity-pulse-vital' src/ui/surfaces/situation-room/pulse-header.tsx` is >= 4 chip references or a 4-element map).
    - PulseHeader calls `buildPulseSentence` and renders its output.
    - The component contains zero `companyPrefix` references and no `dangerouslySetInnerHTML` (`grep -c 'dangerouslySetInnerHTML\|companyPrefix' src/ui/surfaces/situation-room/pulse-header.tsx` is 0).
    - `theme.css` gains `.clarity-pulse*` rules nested under `[data-clarity-surface='situation-room']` — `node scripts/check-css-scope.mjs` passes (all new classes scoped).
    - An undefined/null pulse prop renders the all-zero floor (test asserts no throw + the floor sentence).
    - `node scripts/build-ui.mjs` green; bundle within ceiling (`node scripts/check-ui-bundle-size.mjs` passes).
  </acceptance_criteria>
  <done>PulseHeader renders the deterministic sentence + four always-on vital chips from snapshot.pulse, scoped CSS added, instance-agnostic, never blanks, render contract test green.</done>
</task>

<task type="auto">
  <name>Task 3: NO_UUID_LEAK render-scan extended to PulseHeader</name>
  <files>test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs</files>
  <read_first>
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (the EXACT render-scan pattern to mirror: UUID_RE, stripComments, structural source-grep + behavioral string-render scan)
    - src/ui/surfaces/situation-room/pulse-header.tsx (the component under test — created in Task 2)
    - src/ui/surfaces/situation-room/pulse-sentence.ts (the sentence the behavioral scan exercises)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-10 ("extend the Phase-11 11-07 / Phase-14 NO_UUID_LEAK render-scan UUID-pattern guard to the new <PulseHeader>")
  </read_first>
  <action>
    Create `test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs` mirroring the `employee-row-no-uuid-leak.test.mjs` structure: (1) STRUCTURAL source-grep — assert `pulse-header.tsx` contains no `*Uuid`/`companyPrefix`/`.id` interpolation in a JSX text node or template literal, and renders only count integers + static labels + the sentence; (2) BEHAVIORAL render-scan — simulate the PulseHeader text output (sentence + the four chip count+label strings) from a `pulse` whose counts are plausible integers, and assert the rendered text matches NO UUID pattern (UUID_RE) AND contains no company-prefix literal. Include a guard fixture proving the test is meaningful (assert a real UUID matches UUID_RE). Assert the sentence helper output is UUID-free for several count regimes.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts PulseHeader's rendered text (sentence + four chips) produces ZERO UUID_RE matches across multiple count regimes.
    - The test asserts no company-prefix literal and no `*Uuid`/`.id` JSX/template interpolation in the source.
    - A guard fixture confirms UUID_RE matches a real UUID (the scan is meaningful).
    - `node --test test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs` passes.
  </acceptance_criteria>
  <done>The NO_UUID_LEAK render-scan now covers the PulseHeader render path (D-10), structural + behavioral, and is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot.pulse -> PulseHeader DOM | Worker integers + a deterministic sentence cross into operator-visible DOM. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information disclosure | PulseHeader render | mitigate | NO_UUID_LEAK render-scan (Task 3) proves the sentence + chips emit zero UUIDs; instance-agnostic (no companyPrefix). |
| T-15-05 | Spoofing / fabrication | Pulse sentence honesty | mitigate | The sentence is a PURE deterministic template (Task 1), never AI — it cannot fabricate or lie when the Editor-Agent is down (SC4). |
| T-15-06 | Tampering | injected HTML in labels | mitigate | React text nodes only; no dangerouslySetInnerHTML (asserted in Task 2). |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — verify `git diff package.json` shows no dependency additions. |
</threat_model>

<verification>
- `node --test test/ui/surfaces/situation-room/pulse-header.test.mjs` (sentence + render contract).
- `node --test test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs` (D-10 render-scan).
- `node scripts/build-ui.mjs` + `node scripts/check-css-scope.mjs` + `node scripts/check-ui-bundle-size.mjs` green.
- Engine guard unaffected: `node --test test/shared/blocker-chain.test.mjs` still green (no engine touch).
</verification>

<success_criteria>
- <PulseHeader> states company status in one deterministic plain-English sentence + four vital-sign count chips (SC1 / COCK-01).
- The Phase-8 banner role is folded in: the need-you state lives in the Pulse, no second standalone status line (D-07).
- The Pulse never blanks and never depends on the Editor-Agent (SC4 — deterministic floor).
- Instance-agnostic + NO_UUID_LEAK: zero company-prefix literals, zero UUID text, render-scan extended (SC4 / D-10).
</success_criteria>

<output>
Create `.planning/phases/15-cockpit-ia-redesign/15-02-SUMMARY.md` when done.
</output>
