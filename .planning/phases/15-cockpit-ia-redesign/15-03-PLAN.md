---
phase: 15-cockpit-ia-redesign
plan: 03
type: execute
wave: 3
depends_on: [15-01, 15-02]
files_modified:
  - src/ui/surfaces/situation-room/tier-strip.tsx
  - src/ui/surfaces/situation-room/index.tsx
  - src/ui/surfaces/situation-room/employee-row.tsx
  - src/ui/primitives/theme.css
  - test/ui/surfaces/situation-room/tier-strip.test.mjs
  - test/ui/surfaces/situation-room/tier-degrade.test.mjs
autonomous: true
requirements: [COCK-02]
user_setup: []

must_haves:
  truths:
    - "The Situation Room is organized into Needs-you -> In-motion -> Watch tiers, loudest-on-top, partitioned by the engine blockerChain.tier field (NOT the Phase-9 EmployeeGroup)."
    - "Needs-you keeps the leverage order + the full Phase-13 action card + Phase-14 reply-in-place + Phase-12 assign gating (EmployeeRow reused)."
    - "In-motion is calm with legible 'what each agent is working on' text; Watch holds stuck-agent / external / cycle / self-resolving / unclassified rows + the relocated org-overflow backlog."
    - "Tier membership renders purely from the engine verdict — degrade-safe by construction, present even on a thrown/UNCLASSIFIED chain, honest when the Editor-Agent is down."
  artifacts:
    - path: "src/ui/surfaces/situation-room/tier-strip.tsx"
      provides: "<TierStrip> — partitions rows by blockerChain.tier (+ chainless fallback), renders three tiers loudest-on-top, reuses EmployeeRow, folds BlockedBacklogExpander into Watch"
      exports: ["TierStrip"]
    - path: "src/ui/surfaces/situation-room/index.tsx"
      provides: "SR body renders <PulseHeader> + <TierStrip>, replacing <NeedsYouBanner> + <EmployeeRowStrip>"
      contains: "PulseHeader"
    - path: "src/ui/surfaces/situation-room/employee-row.tsx"
      provides: "a calm tier-variant presentation for In-motion / Watch rows (legible focusLine, no loud action cluster)"
      contains: "tier"
  key_links:
    - from: "src/ui/surfaces/situation-room/index.tsx"
      to: "src/ui/surfaces/situation-room/tier-strip.tsx"
      via: "<TierStrip employees={...} ... />"
      pattern: "TierStrip"
    - from: "src/ui/surfaces/situation-room/tier-strip.tsx"
      to: "blockerChain.tier"
      via: "partition by engine verdict tier"
      pattern: "blockerChain.*tier"
    - from: "src/ui/surfaces/situation-room/index.tsx"
      to: "snapshot.pulse"
      via: "<PulseHeader pulse={payload.pulse} />"
      pattern: "payload.pulse"
---

<objective>
Replace the Phase-9 `Needs you / Working / Idle` people-strip group IA with the verdict-tier IA: Needs-you -> In-motion -> Watch, loudest-on-top, partitioned by the engine `blockerChain.tier` field (NOT the agent-state `EmployeeGroup`). Reuse `EmployeeRow` for rendering; give In-motion + Watch a calmer presentation (legible focus text, no loud action cluster); fold `<BlockedBacklogExpander>` into the Watch tier. Wire the SR body to render `<PulseHeader pulse={payload.pulse}>` (Plan 15-02) above `<TierStrip>`, dropping the standalone `<NeedsYouBanner>`.

Purpose: COCK-02 / SC2 — the three-tier loudest-on-top IA. D-04 locks the verdict->visual-tier mapping (the REAL hyphenated tier field). D-05 makes the partition by verdict tier with a chainless-row fallback (CRITICAL: group !== tier; a stuck agent is group `needs_you` but tier `watch`). D-06 makes In-motion/Watch calm. D-07 drops the banner (folded into the Pulse). SC3: consume the verdict, never re-derive. SC4: tier membership is degrade-safe by construction.

Output: `<TierStrip>` component; the EmployeeRow calm tier-variant; the SR body rewrite (PulseHeader + TierStrip); tier CSS; the partition-contract test + the degrade test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md
@docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md

<interfaces>
<!-- The verdict tier the partition keys on + the components reused. -->

From src/ui/surfaces/situation-room/employee-row.tsx (REUSED — the row renderer):
```
EmployeeGroup = 'needs_you' | 'working' | 'idle'   // agent-STATE axis (Phase 9) — NOT the partition key
SituationEmployeeRow.group: EmployeeGroup
SituationEmployeeRow.blockerChain: { tier: 'needs-you'|'in-motion'|'watch'; terminalKind; needsYou; actionAffordance; ... } | null
export function EmployeeRow({ row, companyPrefix, companyId, userId, navigate, onAssignSuccess }): React.ReactElement
// The needs-you action cluster currently gates on row.group === 'needs_you' && chain.
```

From src/ui/surfaces/situation-room/blocked-backlog-expander.tsx (REUSED — relocated to Watch):
```
<BlockedBacklogExpander backlog={...} criticalPathNarrative={...} companyId userId onAssignSuccess />
```

From src/ui/surfaces/situation-room/employee-row-strip.tsx (the partition-then-render-empty skeleton to mirror; SUPERSEDED by tier-strip):
```
// GROUP_META + GROUP_ORDER + the empty-group "— none —" rule + the BlockedBacklogExpander mount.
```

From src/ui/surfaces/situation-room/pulse-header.tsx (Plan 15-02):
```
<PulseHeader pulse={payload.pulse} />
```

CRITICAL — group vs tier (D-05, the locked reconciliation):
```
The PARTITION must key on blockerChain.tier ('needs-you'|'in-motion'|'watch'):
  Needs-you tier  = blockerChain.tier === 'needs-you'
  In-motion tier  = blockerChain.tier === 'in-motion'  OR  (blockerChain == null && group === 'working')
  Watch tier      = blockerChain.tier === 'watch'       (stuck/self-resolving/external/cycle/unclassified)
                    + chainless idle/stale rows as awareness  + the relocated overflow backlog
A stuck-agent row is group 'needs_you' but tier 'watch' -> it MUST land in Watch, NOT Needs-you.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: <TierStrip> — partition by engine verdict tier, reuse EmployeeRow, fold backlog into Watch</name>
  <files>src/ui/surfaces/situation-room/tier-strip.tsx, test/ui/surfaces/situation-room/tier-strip.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/employee-row-strip.tsx (the partition-then-render-empty skeleton + GROUP_META/GROUP_ORDER + the empty "— none —" convention + the BlockedBacklogExpander mount — re-axis this from group to tier)
    - src/ui/surfaces/situation-room/employee-row.tsx (the REUSED row renderer + the group-vs-tier note)
    - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx (the expander relocated into Watch — its props)
    - src/shared/types.ts (BlockerChainResult.tier values — the REAL hyphenated partition key)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-04 (the verdict->tier mapping TABLE), D-05 (the locked partition rule + chainless fallback + the stuck-agent group!=tier trap), D-07 (banner folded into Pulse; the lead-styled top Needs-you row IS the preserved top callout)
    - docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md §3 Section 3 (loudest-on-top tier order + "29 more blocked issues" in Watch)
    - .planning/phases/12-needs-you-triage/12-CONTEXT.md D-04 (stuck-agent -> Watch, NOT Needs-you — the tier-mapping lock)
  </read_first>
  <behavior>
    - Partitions employees into exactly three tiers, ALWAYS rendered in order Needs-you -> In-motion -> Watch (loudest-on-top), each with a header + count (a zero count still renders its header — a zero is a signal).
    - Needs-you tier = rows with blockerChain.tier === 'needs-you', in the worker's existing leverage order (NOT re-sorted in the view).
    - In-motion tier = rows with blockerChain.tier === 'in-motion' PLUS chainless rows with blockerChain == null && group === 'working'.
    - Watch tier = rows with blockerChain.tier === 'watch' PLUS chainless idle/stale rows (no chain, group !== 'working') as awareness, PLUS the <BlockedBacklogExpander> (org overflow) folded in at the end.
    - A stuck-agent row (group 'needs_you', tier 'watch') lands in Watch, NOT Needs-you (D-04 lock).
    - Every row renders via the reused EmployeeRow; no row is dropped (a defensive fall-through lands any unmatched row in Watch).
    - No view-layer re-classification: the partition reads blockerChain.tier verbatim; it never re-derives the tier from terminalKind or ownerName.
  </behavior>
  <action>
    Create `src/ui/surfaces/situation-room/tier-strip.tsx` exporting `<TierStrip>` with the props the SR body threads today (`employees, companyPrefix, companyId, userId, navigate, onAssignSuccess, orgBacklog, criticalPathNarrative`). Define `TIER_ORDER = ['needs-you','in-motion','watch']` and a `TIER_META` map (title + meta + emptyNote per tier — titles "Needs you" / "In motion" / "Watch", design-spec voice). Partition rows per the LOCKED D-05 rule in `<behavior>`: compute each row's visual tier from `row.blockerChain?.tier`; for a chainless row (blockerChain == null) fall back to group (`working` -> 'in-motion', else -> 'watch'); any unmatched row defensively -> 'watch'. Preserve worker order WITHIN each tier (NO `.sort()` in the view). Render each tier as a section mirroring `employee-row-strip.tsx`'s skeleton (header + count + empty "— none —" line + rows mapped through `<EmployeeRow>`). Mount `<BlockedBacklogExpander>` at the END of the WATCH tier (relocated from Needs-you, D-05). Use `clarity-tier*` class names (`clarity-tier-section clarity-tier-needs-you|in-motion|watch`, `clarity-tier-header`, `clarity-tier-title`, `clarity-tier-count`, `clarity-tier-rows`, `clarity-tier-empty`); CSS lands in Task 3. Write `tier-strip.test.mjs` (source-grep + fixture-driven partition assertions using the SR-UI test idiom): the three tiers render loudest-on-top; a stuck-agent row (tier 'watch', group 'needs_you') lands in Watch NOT Needs-you; an in-motion-tier row + a chainless working row both land in In-motion; a needs-you-tier row lands in Needs-you; the BlockedBacklogExpander mounts under Watch only; the partition keys on `blockerChain.tier` (source references `blockerChain` + `tier`, and does not partition on `row.group ===` as the primary tier key).
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/tier-strip.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - TierStrip partitions by `blockerChain.tier` (the hyphenated engine field) with the chainless group fallback (In-motion for working, Watch otherwise).
    - Test proves a stuck-agent row (tier 'watch') lands in Watch, NOT Needs-you (D-04 lock).
    - Test proves the three tiers render in Needs-you -> In-motion -> Watch order; each empty tier still renders its header + count.
    - `<BlockedBacklogExpander>` is mounted under Watch only (single mount in the watch branch).
    - The view does NOT re-sort (`grep -c "\.sort(" src/ui/surfaces/situation-room/tier-strip.tsx` is 0) and reuses `<EmployeeRow>` (no row re-implementation).
    - `node --test test/ui/surfaces/situation-room/tier-strip.test.mjs` passes.
  </acceptance_criteria>
  <done>TierStrip exists, partitions by the engine verdict tier with the chainless fallback, renders three tiers loudest-on-top reusing EmployeeRow, folds the backlog into Watch, and its partition test (incl. the stuck->Watch lock) is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: EmployeeRow calm tier-variant for In-motion / Watch</name>
  <files>src/ui/surfaces/situation-room/employee-row.tsx, test/ui/surfaces/situation-room/tier-strip.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/employee-row.tsx (the FULL row; the group-keyed branches: needs_you cluster ~L332, working "moving · no action needed" ~L447, idle cluster ~L452; the focusLine render ~L319; the actionAffordance gating)
    - src/shared/types.ts (BlockerChainResult.tier — the variant key)
    - docs/superpowers/specs/2026-06-01-situation-room-truthful-cockpit-design.md §3 Section 3 (In-motion "calm, lower-contrast reassurance — content text must be clearly legible, not the dimmest thing on the row"; Watch "quietly stalled — awareness, not act-now")
    - .claude/skills/sketch-findings-clarity-pack/SKILL.md (calm scales with control; --live green in-motion; --warn/--alert watch)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-06 (calm In-motion/Watch; CSS tier-class variant on EmployeeRow is the lean option; hard constraints: In-motion focusLine legible; Watch keeps honest affordance per D-04; Needs-you keeps full action card + reply-in-place)
  </read_first>
  <behavior>
    - A Needs-you row (visual tier 'needs-you') keeps the FULL Phase-13 action card + Phase-14 reply-in-place + Phase-12 assign gating — UNCHANGED.
    - An In-motion row renders calm: name + role + the legible focusLine ("what each agent is working on") + age, NO loud action cluster. The focus text is clearly legible (not the dimmest element).
    - A Watch row renders quiet/awareness: the verdict line + its honest affordance per D-04 (stuck -> assign/redirect; external/cycle/unclassified -> Open ↗; self-resolving -> none) — quieter than Needs-you but NOT dead.
    - The variant is selected by the engine tier (read blockerChain.tier with the chainless fallback matching TierStrip), NOT by row.group — a stuck-agent row (group 'needs_you', tier 'watch') gets the Watch calm variant, not the loud needs-you cluster.
    - Chainless idle/stale rows landing in Watch keep the Phase-9 stand-down/resume affordances (preserved behind the quiet Watch presentation).
    - Degrade-safe: a chainless In-motion/Watch row still renders name + role + (focusLine if present) without throwing.
  </behavior>
  <action>
    Add a tier-variant presentation to `EmployeeRow`. Compute a `visualTier` from `row.blockerChain?.tier` with the chainless fallback matching TierStrip (null+group 'working' -> 'in-motion', else -> 'watch'). Stamp a class modifier on the row root (`clarity-tier-row clarity-tier-row-${visualTier}`) for CSS tinting (Task 3). Gate the row BODY by `visualTier`, not `row.group`: visualTier === 'needs-you' -> the EXISTING full needs-you cluster (action card + reply-in-place + assign — unchanged); 'in-motion' -> a calm body (the legible focusLine + age, NO action buttons); 'watch' -> a quiet body with the verdict line + the honest affordance from `chain.actionAffordance` (assign -> OwnerPickerPopover for stuck; open -> Open ↗; none -> nothing) AND, for chainless idle/stale Watch rows, the EXISTING idle stand-down/resume cluster (preserve Phase-9 affordances). REUSE the existing handlers (openIssue / OwnerPickerPopover / standDown / resume) — add no new mutations, no new capability. Keep every visible string a React text node; render no UUID/companyPrefix. Add cases to `tier-strip.test.mjs`: a needs-you row still mounts the action card / reply / assign cluster; an in-motion row renders the legible focusLine and NO action button cluster; a watch stuck row keeps its assign affordance; a watch self-resolving row has no action button; the body gate keys on the verdict tier (source references `blockerChain` `tier` / a `visualTier` derived from it for the body gate, not `row.group ===` as the primary body key).
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/tier-strip.test.mjs && node --test test/ui/surfaces/situation-room/employee-row-actions.test.mjs && node --test test/ui/surfaces/situation-room/employee-row-action-card.test.mjs && node --test test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs && node --test test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - EmployeeRow selects its body presentation by the engine visual tier (needs-you full cluster / in-motion calm / watch quiet), not by `row.group` as the body gate.
    - A needs-you row keeps the full action card + reply-in-place + assign (the existing Phase-13/14 tests stay green).
    - An in-motion row renders the legible focusLine and no action button cluster; a watch stuck row keeps assign; a watch self-resolving row has no action button.
    - Chainless idle/stale Watch rows keep stand-down/resume.
    - NO_UUID_LEAK still holds (`employee-row-no-uuid-leak.test.mjs` green) — no UUID/companyPrefix added to any render path.
    - All listed test files pass.
  </acceptance_criteria>
  <done>EmployeeRow renders calm In-motion / quiet Watch variants keyed on the engine tier while Needs-you keeps the full action stack; the existing row tests + the new tier-variant cases are green; NO_UUID_LEAK preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Wire SR body (PulseHeader + TierStrip), drop the banner, add tier CSS + degrade test</name>
  <files>src/ui/surfaces/situation-room/index.tsx, src/ui/primitives/theme.css, test/ui/surfaces/situation-room/tier-degrade.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/index.tsx (the FULL page; the SituationRoomBody render block ~L226-254 — the `<NeedsYouBanner>` + `<EmployeeRowStrip>` mounts to replace; the SituationData type ~L53-65 to widen with `pulse`; the fetch/gate/poll plumbing to REUSE verbatim)
    - src/ui/surfaces/situation-room/pulse-header.tsx (the PulseHeader + PulseSummary mirror — Plan 15-02)
    - src/ui/surfaces/situation-room/tier-strip.tsx (the TierStrip — Task 1)
    - src/ui/primitives/theme.css (the `[data-clarity-surface='situation-room']` scope block ~L697; the existing `.clarity-group-*` rules to model the `.clarity-tier-*` rules on; the `.clarity-pulse-*` rules added by Plan 15-02 to sit above the tiers; the clarity- prefix + check-css-scope rule)
    - .claude/skills/sketch-findings-clarity-pack/SKILL.md (Instrument Serif italic tier titles; --you/--live/--warn/--alert tier tints; inherit host CSS, ship NO parallel Tailwind)
    - .planning/phases/15-cockpit-ia-redesign/15-CONTEXT.md D-07 (drop the standalone banner — Pulse is the single status surface), D-08 (degrade contract per layer), D-09 (consume never re-derive — no engine/mutation/data change), D-10 (instance-agnostic + NO_UUID_LEAK)
  </read_first>
  <behavior>
    - The SR body renders `<PulseHeader pulse={payload.pulse}>` at the top, then `<TierStrip>` — replacing `<NeedsYouBanner>` + `<EmployeeRowStrip>`.
    - The `<NeedsYouBanner>` mount is removed (its role folded into the Pulse, D-07); the fetch/gate/poll/forceRefetch plumbing is unchanged.
    - The SituationData type is widened with an additive optional `pulse?: PulseSummary` (the UI mirror).
    - The tier CSS gives Needs-you loud (gold/lead top row), In-motion calm/green, Watch quiet/amber — scoped under the situation-room surface, inheriting host tokens.
    - DEGRADE (SC4): with `payload.pulse` absent AND no actionCards on any row (Editor-Agent down), the Pulse renders its deterministic floor sentence + real counts and every tier still renders from the engine verdict (deterministic) — the board is fully legible with zero AI output.
  </behavior>
  <action>
    Rewrite `SituationRoomBody`'s render block in `src/ui/surfaces/situation-room/index.tsx`: import `PulseHeader` (+ the `PulseSummary` type) and `TierStrip`; widen the `SituationData` type with `pulse?: PulseSummary`; replace the `<NeedsYouBanner ... />` + `<EmployeeRowStrip ... />` JSX with `<PulseHeader pulse={payload.pulse} />` followed by `<TierStrip employees={employees} companyPrefix={companyPrefix} companyId={companyId} userId={userId} navigate={navigate} onAssignSuccess={forceRefetch} orgBacklog={payload.org_blocked_backlog ?? null} criticalPathNarrative={payload.narrative ?? null} />`. Remove the now-unused `NeedsYouBanner` import (leave `needs-you-banner.tsx` on disk — superseded, not deleted, additive-only spirit). Do NOT touch the fetch/gate/poll/ping/forceRefetch plumbing. Add the `.clarity-tier-*` CSS to `src/ui/primitives/theme.css` UNDER `[data-clarity-surface='situation-room']`: `.clarity-tier-section` + the three tier modifiers (Needs-you loud, In-motion calm/green, Watch quiet/amber), `.clarity-tier-header` (Instrument Serif italic title per sketch-findings) + `.clarity-tier-title` + `.clarity-tier-count` + `.clarity-tier-rows` + `.clarity-tier-empty`, and the `.clarity-tier-row-needs-you|in-motion|watch` row tints (map mockup tokens to host tokens; In-motion focusLine stays legible — NOT the dimmest element). Write `tier-degrade.test.mjs`: assert that with no `pulse` field and no actionCards, (a) the Pulse path falls back to the deterministic floor sentence (exercise `buildPulseSentence` with all-zero / real counts → non-blank, UUID-free), and (b) the tier partition still classifies rows from `blockerChain.tier`/`terminalKind` only (no actionCard dependency) — assert a needs-you/in-motion/watch fixture partitions correctly with `actionCard: null` on every row; assert the index body references `payload.pulse` and `TierStrip` and NO LONGER mounts `NeedsYouBanner`.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/situation-room/tier-degrade.test.mjs && node scripts/build-ui.mjs && node scripts/check-css-scope.mjs && node scripts/check-ui-bundle-size.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `index.tsx` renders `<PulseHeader pulse={payload.pulse}>` + `<TierStrip>` and NO LONGER mounts `<NeedsYouBanner>` (`grep -c "NeedsYouBanner" src/ui/surfaces/situation-room/index.tsx` is 0).
    - `SituationData` is widened with `pulse?: PulseSummary` (additive optional).
    - The fetch/gate/poll plumbing is unchanged (the snapshot fetch + forceRefetch + ping effects untouched).
    - `theme.css` gains `.clarity-tier-*` rules scoped under the situation-room surface; `node scripts/check-css-scope.mjs` passes.
    - The degrade test proves: no-pulse -> deterministic floor sentence (non-blank, UUID-free); tier partition works with `actionCard: null` on every row (no AI dependency, SC4).
    - `node scripts/build-ui.mjs` green; `node scripts/check-ui-bundle-size.mjs` within ceiling.
  </acceptance_criteria>
  <done>The SR body renders the Pulse + the three-tier IA, the banner is gone (folded into the Pulse), tier CSS scoped, and the degrade test proves an honest board with zero AI output (SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot -> tier DOM | Engine verdict rows + (optional) action cards cross into operator-visible DOM via the reused EmployeeRow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | Information disclosure | TierStrip / EmployeeRow tier-variant render | mitigate | Reuses EmployeeRow's existing NO_UUID_LEAK render path; the no-uuid-leak test stays green (Task 2 verify); no UUID/companyPrefix added to TierStrip or the new variant. |
| T-15-08 | Spoofing / fabrication | tier membership honesty | mitigate | Tiers render from the deterministic engine verdict (blockerChain.tier/terminalKind), present even on a thrown UNCLASSIFIED chain — degrade-safe by construction; the degrade test proves no AI dependency (SC4). |
| T-15-09 | Elevation of privilege | reused mutation handlers | accept | TierStrip + the tier-variant reuse ONLY existing handlers (assignOwner / replyAndResume / pauseHeartbeat / resumeHeartbeat); no new mutation, no new capability (D-09). |
| T-15-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — verify `git diff package.json` shows no dependency additions. |
</threat_model>

<verification>
- `node --test test/ui/surfaces/situation-room/tier-strip.test.mjs` (partition + stuck->Watch lock + tier-variant).
- `node --test test/ui/surfaces/situation-room/tier-degrade.test.mjs` (SC4 honest-when-AI-down).
- `node --test test/ui/surfaces/situation-room/employee-row-*.test.mjs` (existing Phase-13/14 + NO_UUID_LEAK stay green).
- `node scripts/build-ui.mjs` + `node scripts/check-css-scope.mjs` + `node scripts/check-ui-bundle-size.mjs` green.
- Engine + worker untouched here: `node --test test/shared/blocker-chain.test.mjs` + `node --test test/worker/situation/build-pulse-summary.test.mjs` still green (no engine/mutation/data change — D-09; no migration).
</verification>

<success_criteria>
- The Situation Room is organized Needs-you -> In-motion -> Watch, loudest-on-top, partitioned by the engine `blockerChain.tier` (SC2 / COCK-02).
- Needs-you keeps leverage order + action cards + reply-in-place + assign; In-motion is calm + legible; Watch holds stuck/external/cycle/self-resolving/overflow + the relocated backlog (SC2 / D-04/05/06).
- The view consumes the verdict + cards directly — zero ownership re-derivation; no engine/mutation/data change; no migration (SC3 / D-09).
- The board renders honestly when the Editor-Agent is down — deterministic Pulse floor + verdict-only tiers (SC4 / D-08); instance-agnostic + NO_UUID_LEAK preserved (SC4 / D-10).
</success_criteria>

<output>
Create `.planning/phases/15-cockpit-ia-redesign/15-03-SUMMARY.md` when done.
</output>
