---
phase: 16-legibility-no-raw-identifiers-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/verdict-wording.ts
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/ui/surfaces/situation-room/employee-row.tsx
  - test/shared/verdict-wording.test.mjs
autonomous: true
requirements: [LEG-03, LEG-05]
must_haves:
  truths:
    - "The Reader blocker panel never renders the raw enum token (no `AWAITING AGENT STUCK` heading); it shows a plain-English category headline."
    - "The same blocked terminal kind produces the same plain-English verdict wording on the Reader and the Situation Room."
    - "A new shared, pure, worker-import-free helper owns the kind→headline and verdict-line wording; both surfaces import it."
  artifacts:
    - path: "src/shared/verdict-wording.ts"
      provides: "Pure Terminal['kind']-keyed wording helper (kindHeadline + verdictLine), exhaustive switch + never guard, no worker/UI-module import"
      contains: "export function kindHeadline"
    - path: "test/shared/verdict-wording.test.mjs"
      provides: "Wording-parity + exhaustiveness test for all 8 Terminal kinds"
  key_links:
    - from: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      to: "src/shared/verdict-wording.ts"
      via: "import { kindHeadline, verdictLine }"
      pattern: "verdict-wording"
    - from: "src/ui/surfaces/situation-room/employee-row.tsx"
      to: "src/shared/verdict-wording.ts"
      via: "import { verdictLine }"
      pattern: "verdict-wording"
---

<objective>
Eliminate the Reader's raw enum-token leak (LEG-03) and unify verdict wording across Reader and Situation Room (LEG-05) by relocating the plain-English wording into ONE shared pure helper that both surfaces consume.

Purpose: Today `live-blocker-panel.tsx:287` renders `terminal.kind.replace(/_/g,' ')` → `AWAITING AGENT STUCK` (the LEG-03 defect), and the kind→sentence wording is duplicated/divergent between the Reader (`blockerLine()` at :83-113) and the SR (`employee-row.tsx` :379,:482). A5 locks the fix as a single shared producer so the two surfaces stay in lockstep by construction.
Output: `src/shared/verdict-wording.ts` (new), Reader + SR rewired to consume it, parity test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-CONTEXT.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-RESEARCH.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-PATTERNS.md

<interfaces>
<!-- The precedent file to mirror EXACTLY for the new helper's shape, purity boundary, and never-guard idiom -->
From src/shared/reply-reachable.ts:
- `import type { Terminal } from './types.ts';`
- `export function isReplyReachable(terminalKind: Terminal['kind']): boolean { switch (terminalKind) { case 'AWAITING_HUMAN': ... default: { const _exhaustive: never = terminalKind; throw new Error(...); } } }`
- Pure: input is the discriminant string ALONE; no I/O, no AI token, no worker import. Already cross-imported by Reader (live-blocker-panel.tsx) AND SR (employee-row.tsx).

The 8 Terminal kinds (from the existing exhaustive switches): AWAITING_HUMAN, AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, SELF_RESOLVING, EXTERNAL, CYCLE, UNOWNED, UNCLASSIFIED.

`BlockerChainResult` carries `terminal`, `awaitedPartyLabel` (already scrubbed), `degradeReason`. Imported in live-blocker-panel.tsx as `import type { BlockerChainResult } from '../../../shared/types.ts';`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared verdict-wording helper</name>
  <files>src/shared/verdict-wording.ts, test/shared/verdict-wording.test.mjs</files>
  <read_first>
    - src/shared/reply-reachable.ts (the EXACT pattern to mirror — pure, Terminal['kind']-keyed, exhaustive switch + `never` guard, no worker import, file-header rationale style)
    - src/ui/surfaces/reader/live-blocker-panel.tsx:83-113 (the existing correct `blockerLine(data: BlockerChainResult)` body to lift verbatim into the new helper)
    - src/ui/surfaces/situation-room/employee-row.tsx:374-386,476-488 (the SR's current divergent wording `"<leaf> — agent stuck"` / `"<leaf> has no owner"` / `"waiting on <party>"` — the parity target phrasing)
    - src/shared/types.ts (Terminal union + BlockerChainResult shape)
  </read_first>
  <action>
    Create `src/shared/verdict-wording.ts` mirroring `reply-reachable.ts` (pure, `import type { Terminal, BlockerChainResult } from './types.ts';` only — NO worker or UI-module import). Export TWO pure functions:
    (a) `kindHeadline(kind: Terminal['kind']): string` — the plain-English CATEGORY label that REPLACES the Reader's `terminal.kind.replace(/_/g,' ')` leak. Exhaustive switch over all 8 kinds with the `never` default-guard. Wording examples: AWAITING_AGENT_STUCK → "Waiting on an agent"; AWAITING_AGENT_WORKING → "Waiting on an agent"; AWAITING_HUMAN → "Waiting on you"; UNOWNED → "Needs an owner"; SELF_RESOLVING → "Resolving on its own"; EXTERNAL → "Waiting on an external party"; CYCLE → "Circular dependency"; UNCLASSIFIED → "Needs a closer look". No enum tokens, no underscores in output.
    (b) `verdictLine(data: BlockerChainResult): string` — lift the body of `blockerLine()` (live-blocker-panel.tsx:83-113) verbatim, keeping its exact signature and `never` guard. This is the ONE body sentence both surfaces render so Reader and SR collapse to one phrasing.
    Add a file-header comment in the `reply-reachable.ts` rationale style explaining: pure cross-surface wording (A5/LEG-05), the home is `src/shared/` because `blockerLine()` inside the Reader module cannot be imported by the SR without dragging a Reader UI module into the SR; do NOT edit `blocker-chain.ts`.
    Create `test/shared/verdict-wording.test.mjs` (node:test) asserting: every one of the 8 kinds yields a non-empty plain-English string from `kindHeadline`; the output contains NO underscore and is NOT equal to the raw kind; `verdictLine` for a constructed `AWAITING_AGENT_STUCK` BlockerChainResult returns the SAME noun phrasing the SR uses (parity assertion — pick the single locked phrasing and assert both code paths resolve to it); a new (fake) kind would fail the build (exhaustiveness is compile-time — assert via a comment + the `never` guard presence).
  </action>
  <acceptance_criteria>
    - `src/shared/verdict-wording.ts` exists, exports `kindHeadline` and `verdictLine`, imports ONLY from `./types.ts` (grep the file: no `worker`, no `../ui`, no `react` import).
    - For all 8 kinds, `kindHeadline(kind)` returns a string matching `assert.doesNotMatch(out, /_/)` and `assert.notEqual(out, kind)`.
    - Test file asserts `verdictLine` for AWAITING_AGENT_STUCK equals the locked parity phrasing.
    - `node --test test/shared/verdict-wording.test.mjs` exits 0.
    - The file contains an exhaustive `switch` with a `const _exhaustive: never =` guard in BOTH functions.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/shared/verdict-wording.test.mjs</automated>
  </verify>
  <done>The shared helper exists with two pure functions, both surfaces' wording can be sourced from it, and the parity test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire Reader (LEG-03 enum fix) and SR (LEG-05 parity) to consume the shared helper</name>
  <files>src/ui/surfaces/reader/live-blocker-panel.tsx, src/ui/surfaces/situation-room/employee-row.tsx</files>
  <read_first>
    - src/ui/surfaces/reader/live-blocker-panel.tsx:32-34 (the existing `import { isReplyReachable } from '../../../shared/reply-reachable.ts';` import precedent to extend), :83-113 (the local `blockerLine` to delete/replace), :287 (THE LEG-03 enum-leak span), :295 (the `<p className="clarity-blocker-label">{blockerLine(data)}</p>` body render)
    - src/ui/surfaces/situation-room/employee-row.tsx:40 (the `reply-reachable.ts` import precedent), :374-386 (needs-you tier wording), :476-488 (watch tier wording)
    - src/shared/verdict-wording.ts (the new helper from Task 1)
  </read_first>
  <behavior>
    - Reader: the `<span className="clarity-blocker-kind">` no longer renders `terminal.kind.replace(/_/g,' ')`; it renders `kindHeadline(terminal.kind)` (a plain-English category, no underscores, no enum token).
    - Reader: the body `<p>` at :295 renders `verdictLine(data)` from the shared helper (behavior unchanged from the lifted `blockerLine`).
    - SR: the watch-tier `"<leaf> — agent stuck"` and needs-you `"<leaf> has no owner"` literal templates resolve to the SAME phrasing the shared helper produces (LEG-05 parity).
  </behavior>
  <action>
    In `live-blocker-panel.tsx`: add `import { kindHeadline, verdictLine } from '../../../shared/verdict-wording.ts';` (mirroring the line-32 import style). Replace the local `blockerLine` function (:83-113) with the import — delete the local definition; update the call at :295 to `verdictLine(data)`. Replace the `:287` span content `{terminal.kind.replace(/_/g, ' ')}` with `{kindHeadline(terminal.kind)}`. Do NOT restyle `clarity-blocker-kind` (confirmed unstyled inline span — text-only change, no sketches restyle).
    In `employee-row.tsx`: add `import { verdictLine } from '../../../shared/verdict-wording.ts';` (mirror line-40 import). Route the watch-tier (:482) and needs-you-tier (:379) verdict wording through the shared helper so the phrasing lives in ONE file. Keep `chain.awaitedPartyLabel` as the already-scrubbed display value (it becomes "the CEO's agent" / "an agent" once 16-02 lands — never a hash). Keep the existing `showAssign ? … : "waiting on <party>"` affordance branching; only the verdict noun must match the shared phrasing — do not change which branch shows for which tier.
    Keep the `clarity-employee-chain-leaf` `(${chain.leafIssueId})` suffix as-is — that is the human BEAAA-NNN identifier, not a leak.
  </action>
  <acceptance_criteria>
    - Grep `live-blocker-panel.tsx`: `terminal.kind.replace` no longer present; `kindHeadline(terminal.kind)` present; the local `function blockerLine` definition is gone; `verdictLine(data)` is rendered at the body `<p>`.
    - Grep `employee-row.tsx`: imports `verdict-wording`; the verdict noun for the stuck/owner cases is sourced from the shared helper.
    - `pnpm typecheck` exits 0.
    - `pnpm test` passes (existing Reader/SR tests stay green; no behavior regression in affordance branching).
    - `node scripts/check-css-scope.mjs` exits 0 and `pnpm check-ui-bundle-size` passes (note the ceiling; text-only change must not exceed it).
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm build:ui && pnpm test && node scripts/check-css-scope.mjs && pnpm check-ui-bundle-size</automated>
  </verify>
  <done>The Reader renders a plain-English headline (no enum), both surfaces import the shared wording, and the same blocked kind reads identically on both — verified by Task 1's parity test and a green build/test/scope/bundle gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine→UI render | Raw `terminal.kind`/`terminal.label` (may embed UUIDs) crosses into operator-visible DOM here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure | live-blocker-panel.tsx:287 enum render | mitigate | Replace raw-enum render with `kindHeadline()` plain-English category; no machine token reaches the DOM |
| T-16-02 | Tampering | shared helper purity | mitigate | New helper imports ONLY `./types.ts`; no worker/AI import (mirrors reply-reachable.ts purity); `blocker-chain.ts` untouched |
| T-16-03 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan (zero new deps) |
</threat_model>

<verification>
- `kindHeadline` covers all 8 Terminal kinds with a `never` guard (compile-time exhaustiveness).
- Reader no longer renders any `*_*` enum token in the blocker header.
- Reader and SR render the same verdict noun for the same kind (parity test green).
- `blocker-chain.ts` is NOT in `files_modified` (engine untouched).
</verification>

<success_criteria>
- LEG-03: no enum/code token renders as the Reader blocker headline.
- LEG-05: same blocked item reads with the same plain-English wording on Reader + SR, sourced from one file.
- `pnpm typecheck && pnpm build:ui && pnpm test && node scripts/check-css-scope.mjs && pnpm check-ui-bundle-size` all pass.
</success_criteria>

<output>
Create `.planning/phases/16-legibility-no-raw-identifiers-pass/16-01-SUMMARY.md` when done
</output>
