---
phase: 16-legibility-no-raw-identifiers-pass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/scrub-human-action.ts
  - test/shared/scrub-human-action.test.mjs
  - test/worker/handlers/flatten-blocker-chain-scrub.test.mjs
  - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs
  - test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs
  - test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs
  - test/shared/scrub-partial-hash-guard.test.mjs
autonomous: true
requirements: [LEG-01, LEG-02]
must_haves:
  truths:
    - "The scrub fallback emits the literal noun `'an agent'` instead of `agent#<8hex>` when a UUID does not resolve to a human name."
    - "The common case still shows the real human name/role (the nameByUuid resolution path is untouched and runs first)."
    - "The NO_UUID_LEAK guard fails the build on `agent#<hex{6,}>` partial-hash labels AND bare UUIDs, proven by a named regression test."
  artifacts:
    - path: "src/shared/scrub-human-action.ts"
      provides: "Scrub helper with all 6 fallbacks → 'an agent' and a new anchored PARTIAL_HEX_RE"
      contains: "PARTIAL_HEX_RE"
    - path: "test/shared/scrub-partial-hash-guard.test.mjs"
      provides: "Named LEG-02 regression test asserting agent#<8> and bare UUIDs FAIL the guard"
  key_links:
    - from: "test/shared/scrub-partial-hash-guard.test.mjs"
      to: "src/shared/scrub-human-action.ts"
      via: "import { PARTIAL_HEX_RE, UUID_RE }"
      pattern: "PARTIAL_HEX_RE"
---

<objective>
Fix the root-cause identifier leak (LEG-01): replace the `agent#${uuid.slice(0,8)}` fallback in the scrub with the literal lowercase noun `'an agent'` (A1), and extend the NO_UUID_LEAK guard to FAIL on partial-hash labels (LEG-02, A2), inverting the tests that currently bless `agent#<8>`.

Purpose: `agent#04fcac7c` seen live on BEAAA-972 IS the scrub's own designed fallback. A1 reclassifies a partial hash as a leak; the common name-resolution path stays (so most rows show the real human name) and `'an agent'` is the last-resort fallback only. A2 anchors the new guard to `agent#<hex>` + the UUID shape — NOT a blanket short-hex rule that would false-positive on git SHAs.
Output: scrub fallbacks → noun, anchored `PARTIAL_HEX_RE`, inverted + named regression tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-CONTEXT.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-RESEARCH.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-PATTERNS.md

<interfaces>
<!-- Current scrub state — the 6 fallback sites + the regex source -->
From src/shared/scrub-human-action.ts:
- `export const UUID_RE_G` / `export const UUID_RE` (full-UUID 8-4-4-4-12 only) at :22-23.
- Fallback `?? \`agent#${uuid.slice(0, 8)}\`` appears at :65, :66, :71, :86; viewer-resolve `?? \`agent#${terminal.userId.slice(0,8)}\`` at :78.
- The `:59` UNOWNED branch already returns a plain noun: `'Owner unknown — assign an owner first'` — the existing "noun, not hash" model.
- Doc-comments at :33-34,:42,:44 say "NEVER the raw UUID … agent#<8> fallback" — must be rewritten.

Render-scan guard analog (the pattern to extend): test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs uses `stripComments(src)` + `UUID_RE` source-grep (no jsdom). Add an anchored `PARTIAL_HEX_RE` scan alongside.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the agent#<8> fallback with 'an agent' + add anchored PARTIAL_HEX_RE</name>
  <files>src/shared/scrub-human-action.ts</files>
  <read_first>
    - src/shared/scrub-human-action.ts (the whole file — the 6 fallback sites at :65,:66,:71,:78,:86; the regex source :22-23; the doc-comments :33-34,:42,:44; the :59 UNOWNED noun model to mirror)
    - src/shared/reply-reachable.ts (purity-boundary reference — no I/O, no worker import; the scrub must stay pure)
  </read_first>
  <behavior>
    - When `nameOf(uuid)` returns a name, the scrub still emits that name (common-case path unchanged — verify with a populated nameByUuid map test).
    - When `nameOf(uuid)` returns null, the scrub emits the literal lowercase `'an agent'` — NOT `agent#<8hex>`, NOT an empty deletion.
    - The viewer-resolve at :78 (AWAITING_HUMAN "You" substitution): when the viewer's id does not resolve, it falls back to `'an agent'` too; the "You" substitution for the matching viewer is preserved.
    - A new exported `PARTIAL_HEX_RE` anchored to the `agent#` prefix (e.g. `/\bagent#[0-9a-f]{6,}\b/i`) matches `agent#04fcac7c` but does NOT match a bare git SHA `deadbeef` or a hex color.
  </behavior>
  <action>
    In `src/shared/scrub-human-action.ts`: replace every `?? \`agent#${uuid.slice(0, 8)}\`` (at :65, :66, :71, :86) and the `?? \`agent#${terminal.userId.slice(0,8)}\`` (at :78) with the literal `?? 'an agent'` (lowercase, sentence-fit, per A1). For the step-5 belt-and-suspenders pass (:86) and step-2 belt pass (:66) that currently `.replace(UUID_RE_G, (uuid) => \`agent#...\`)`, change the replacement to return `'an agent'` (a surviving UUID becomes the noun, never a partial hash, never an empty string — see Pitfall: deletion leaves " is stuck").
    Add `export const PARTIAL_HEX_RE = /\bagent#[0-9a-f]{6,}\b/i;` near the UUID_RE exports (:22-23). Anchor it to the `agent#` prefix per A2 — do NOT add a blanket `/[0-9a-f]{8,}/` short-hex rule (false-positives on git SHAs / hex colors — Pitfall 3).
    Rewrite the doc-comments at :33-34,:42,:44 (and the step-3/step-6 inline comments) from "NEVER the raw UUID … agent#<8> fallback" to "NEVER a raw UUID OR partial hash; the last-resort fallback is the lowercase noun 'an agent'. The COMMON path is nameOf(uuid) resolving to the real human name — 'an agent' is last-resort only." Keep the :59 UNOWNED branch unchanged (it is already the noun model).
    Do NOT touch the name-resolution precedence — `nameOf(uuid)` still runs first at every site.
  </action>
  <acceptance_criteria>
    - Grep `scrub-human-action.ts`: ZERO occurrences of `agent#${` and ZERO of `agent#<` in code; `'an agent'` present at the 5 former-fallback sites.
    - `export const PARTIAL_HEX_RE` present and anchored to `agent#`.
    - `PARTIAL_HEX_RE.test('agent#04fcac7c') === true`; `PARTIAL_HEX_RE.test('deadbeef') === false`; `PARTIAL_HEX_RE.test('#ff00aa') === false`.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>The scrub fallback is the noun `'an agent'`, the anchored `PARTIAL_HEX_RE` is exported and does not false-positive, and the name-resolution common path is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Invert the blessing tests + extend render-scans + add named LEG-02 regression test</name>
  <files>test/shared/scrub-human-action.test.mjs, test/worker/handlers/flatten-blocker-chain-scrub.test.mjs, test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs, test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs, test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs, test/shared/scrub-partial-hash-guard.test.mjs</files>
  <read_first>
    - test/shared/scrub-human-action.test.mjs:85-146 (the blessing assertions at :94 `/agent#eeeeeeee/`, :123 `/agent#dddddddd/`, :144 `/agent#12345678/` — must flip)
    - test/worker/handlers/flatten-blocker-chain-scrub.test.mjs:110-122 (`:121` `assert.ok(scrubbed.awaitedPartyLabel.includes('agent#'))` — must flip to NOT include)
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs:1-55 (the render-scan pattern: `stripComments` + `UUID_RE` source-grep — extend with PARTIAL_HEX_RE)
    - test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs (full-UUID + prefix-literal scan — extend)
    - test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs (full-UUID scan — extend)
    - src/shared/scrub-human-action.ts (the PARTIAL_HEX_RE export from Task 1)
  </read_first>
  <action>
    INVERT the blessing assertions: in `scrub-human-action.test.mjs` change `:94` `assert.match(stuck, /agent#eeeeeeee/)` → `assert.match(stuck, /an agent/)` + `assert.doesNotMatch(stuck, PARTIAL_HEX_RE)`; same for `:123` (EXTERNAL) and `:144` (belt-and-suspenders) — assert the noun and `doesNotMatch(PARTIAL_HEX_RE)` instead of matching the hash. Import `PARTIAL_HEX_RE` from `src/shared/scrub-human-action.ts`. In `flatten-blocker-chain-scrub.test.mjs:121` flip `assert.ok(...includes('agent#'))` → `assert.ok(!scrubbed.awaitedPartyLabel.includes('agent#'))` and assert it includes `'an agent'`.
    EXTEND the three render-scan guards (employee-row / pulse-header / reply-in-place no-uuid-leak tests): add a `PARTIAL_HEX_RE` source-grep alongside the existing `UUID_RE` scan, asserting the stripped source contains NO `agent#<hex>` rendered literal. Reuse each file's existing `stripComments` helper and source-read pattern; do NOT add jsdom.
    CREATE the named LEG-02 regression test `test/shared/scrub-partial-hash-guard.test.mjs` (node:test): import `scrubHumanAction`, `PARTIAL_HEX_RE`, `UUID_RE` from the source. Assert (a) a scrub of AWAITING_AGENT_STUCK with an UNRESOLVED agentId (empty nameByUuid) returns a string that matches `/an agent/` AND `assert.doesNotMatch(out, PARTIAL_HEX_RE)` AND `assert.doesNotMatch(out, UUID_RE)`; (b) the guard itself FAILS on a literal `agent#04fcac7c` (`assert.match('agent#04fcac7c', PARTIAL_HEX_RE)`) and on a bare UUID (`assert.match('<uuid>', UUID_RE)`); (c) the anchored guard does NOT false-positive on a git SHA / hex color (`assert.doesNotMatch('deadbeef cafe', PARTIAL_HEX_RE)`).
  </action>
  <acceptance_criteria>
    - `scrub-human-action.test.mjs` no longer asserts any `/agent#[0-9a-f]/` match; it asserts `/an agent/` + `doesNotMatch(PARTIAL_HEX_RE)`.
    - `flatten-blocker-chain-scrub.test.mjs:~121` asserts the label does NOT include `agent#`.
    - The 3 render-scan guards each include a PARTIAL_HEX_RE scan.
    - `test/shared/scrub-partial-hash-guard.test.mjs` exists with assertions (a)(b)(c) above.
    - `pnpm test` exits 0 (all inverted + new + existing tests green).
  </acceptance_criteria>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <done>Every test that previously blessed `agent#<8>` now forbids it, the render-scans catch partial hashes, the named LEG-02 regression test proves `agent#<8>` and bare UUIDs FAIL the guard while git SHAs do not false-positive, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine label→scrub→UI | Raw `terminal.label` embeds UUIDs; the scrub is the NO_UUID_LEAK chokepoint before any surface render |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information Disclosure | scrub-human-action.ts fallback | mitigate | Partial-hash fallback replaced with the noun 'an agent'; no machine token survives the scrub |
| T-16-05 | Information Disclosure | render-scan guard coverage | mitigate | Guard extended to fail on `agent#<hex{6,}>` + bare UUID via anchored PARTIAL_HEX_RE; named regression test |
| T-16-06 | Tampering | over-broad guard regex | accept | Anchored to `agent#` prefix per A2 — accepts that bare short-hex in prose is NOT scanned (avoids git-SHA/hex-color false positives) |
| T-16-07 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan |
</threat_model>

<verification>
- No `agent#${` literal remains in `src/shared/scrub-human-action.ts`.
- The scrub common-case (resolved name) path is unchanged — a populated-map test still shows the name.
- `PARTIAL_HEX_RE` matches `agent#04fcac7c`, does NOT match git SHAs / hex colors.
- All previously-blessing tests now forbid the partial hash; named regression test present.
</verification>

<success_criteria>
- LEG-01: the scrub never emits a partial agent hash; the unresolved case reads "an agent".
- LEG-02: the NO_UUID_LEAK guard fails on partial-hash labels + bare UUIDs, with a named regression test.
- `pnpm typecheck && pnpm test` pass.
</success_criteria>

<output>
Create `.planning/phases/16-legibility-no-raw-identifiers-pass/16-02-SUMMARY.md` when done
</output>
