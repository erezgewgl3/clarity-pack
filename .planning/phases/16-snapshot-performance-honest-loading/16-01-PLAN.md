---
phase: 16-snapshot-performance-honest-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/util/map-bounded.ts
  - test/worker/util/map-bounded.test.mjs
  - .planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md
autonomous: true
requirements: [SNAP-02]
must_haves:
  truths:
    - "A bounded-concurrency pool keeps at most N relations.get walks in flight (caps host Postgres load)."
    - "A hung promise floors to a deterministic fallback within a bounded deadline instead of waiting the 30s default."
    - "The live public.issues / public.agents snake_case column names are confirmed before any raw SQL is written."
    - "It is confirmed whether per-call timeoutMs threads through ctx.issues.relations.get, else withDeadline is the chosen floor."
  artifacts:
    - path: "src/worker/util/map-bounded.ts"
      provides: "Hand-rolled mapBounded pool + withDeadline deadline floor (no p-limit dep)"
      exports: ["mapBounded", "withDeadline"]
      min_lines: 25
    - path: "test/worker/util/map-bounded.test.mjs"
      provides: "Tests asserting concurrency ceiling + deadline floor behavior"
    - path: ".planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md"
      provides: "Locked snake_case column list + timeoutMs-reachability finding for Wave A/B"
  key_links:
    - from: "src/worker/util/map-bounded.ts"
      to: "src/worker/situation/build-employees-rollup.ts (Wave B, 16-03)"
      via: "import { mapBounded, withDeadline }"
      pattern: "mapBounded|withDeadline"
---

<objective>
Build the only genuinely-new primitive in the phase (the hand-rolled bounded-concurrency pool + deadline floor) and resolve the two Wave-0 unknowns the SQL/bounding waves depend on, so Waves A/B (16-02, 16-03) can be written deterministically.

Purpose: Waves A and B both touch the live DB schema (raw SQL needs real snake_case column names) and the irreducible relations.get RPC (needs a per-call deadline). This plan locks both facts and ships the reusable pool, removing the two RESEARCH open questions (OQ#1 timeoutMs reachability, OQ#2 column names) before any builder is edited.
Output: `src/worker/util/map-bounded.ts` (+ test), and `16-SCHEMA-VERIFY.md` recording the locked column list and the timeoutMs-vs-withDeadline decision.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-snapshot-performance-honest-loading/16-RESEARCH.md
@.planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md

<interfaces>
<!-- The canonical mapBounded + withDeadline shapes (from 16-RESEARCH.md Code Examples, lines 311-338). -->
mapBounded<T,R>(items: T[], limit: number, fn: (item: T, index: number) => Promise<R>): Promise<R[]>  // preserves order
withDeadline<T>(p: Promise<T>, ms: number, onTimeout: () => T): Promise<T>  // resolves onTimeout() if p neither resolves nor rejects within ms; also floors on reject

<!-- SDK per-call RPC timeout surface to probe (worker-rpc-host.js:46,153). -->
DEFAULT_RPC_TIMEOUT_MS = 30_000   // far too coarse for a per-row floor
callHost(method, params, timeoutMs)  // internal override; verify whether ctx.issues.relations.get threads it through
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 verify live schema columns + relations.get timeoutMs reachability</name>
  <read_first>
    - src/worker/bulletin/standing-numbers.ts (lines 1-130 — the proven ctx.db.query SQL shape, the EXCLUDE_OPERATION_ISSUES_SQL constant, and the column-name caveat citing 03-10-SCHEMA-FINDINGS.md)
    - src/worker/handlers/org-blocked-backlog.ts (the buildEdges signature + relations.get call site ~lines 289-299, 366-415)
    - node_modules/@paperclipai/plugin-sdk/dist/worker-rpc-host.js (lines 46, 153-178 — DEFAULT_RPC_TIMEOUT_MS + callHost(method,params,timeoutMs))
    - node_modules/@paperclipai/plugin-sdk/dist/types.d.ts (the PluginIssuesClient.relations.get typed signature — does it accept a timeout/options arg?)
  </read_first>
  <action>
    Resolve RESEARCH OQ#1 and OQ#2 and record both in a new doc `16-SCHEMA-VERIFY.md` under the phase dir. The doc MUST contain exactly two H2 section headings written verbatim as `## Columns` and `## timeoutMs decision` (the automated check greps for these exact markers).
    (a) Under `## Columns` (OQ#2 / Pitfall 1): confirm the snake_case columns Wave A's SQL will project. Source-of-truth is `03-10-SCHEMA-FINDINGS.md §2` — read it. Lock this exact target set for public.issues: `id, identifier, title, status, assignee_agent_id, assignee_user_id, updated_at, company_id, hidden_at, origin_kind` and for public.agents: `id, name, role, title, last_heartbeat_at, status, paused_at, company_id`. For any column whose live name is uncertain (notably the heartbeat column `last_heartbeat_at` and any activity column), record the confirmed name OR mark it `LIVE-CHECK-REQUIRED` so 16-04's bookended BEAAA drill runs `\d public.issues` / `\d public.agents` in the deploy window and back-fills it. Do NOT block the phase on the live check — record the 03-10 names as the working set with the defensive `?? null` posture noted.
    (b) Under `## timeoutMs decision` (OQ#1 / A5): inspect the typed `ctx.issues.relations.get` signature in the SDK `.d.ts`. If it accepts a per-call `timeoutMs`/options argument, record "USE SDK timeoutMs override" and the exact arg shape; if it does NOT, record "USE withDeadline wrapper" — this DECIDES which floor Wave B (16-03) uses. Either path satisfies SNAP-02; do not add any dependency.
    Write `16-SCHEMA-VERIFY.md` with those two sections, each citing the file:line evidence.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md && grep -q '^## Columns' .planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md && grep -q '^## timeoutMs decision' .planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md && grep -q 'public.agents' .planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md</automated>
  </verify>
  <acceptance_criteria>
    - 16-SCHEMA-VERIFY.md exists with a `## Columns` section listing the locked snake_case names for public.issues and public.agents, each name either confirmed-from-03-10 or marked LIVE-CHECK-REQUIRED.
    - The `## timeoutMs decision` section states explicitly whether ctx.issues.relations.get accepts a per-call timeoutMs (USE SDK override) or not (USE withDeadline), citing the SDK .d.ts line.
    - No dependency added; no source file under src/ modified by this task.
  </acceptance_criteria>
  <done>Both RESEARCH open questions (OQ#1, OQ#2) are resolved and recorded; Wave A/B can be written against locked column names and a chosen floor mechanism.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the hand-rolled bounded-concurrency pool + deadline floor</name>
  <files>src/worker/util/map-bounded.ts, test/worker/util/map-bounded.test.mjs</files>
  <read_first>
    - .planning/phases/16-snapshot-performance-honest-loading/16-RESEARCH.md (Code Examples, lines 311-338 — the canonical mapBounded + withDeadline implementations to copy verbatim)
    - .planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md (the map-bounded.ts section, lines 282-310 — the two exports + the "test it like the existing builders" note)
    - src/worker/situation/agent-liveness.ts (an existing pure-util sibling — match its export/file style and the project's .ts ESM import convention)
  </read_first>
  <behavior>
    - mapBounded([a,b,c,d], 2, fn) never has more than 2 fn invocations in flight at once (assert via an in-flight counter that records its max).
    - mapBounded preserves input order in the result array regardless of per-item completion order.
    - mapBounded(items, limit) where limit > items.length runs items.length workers (no over-spawn), still returns all results.
    - mapBounded([], N, fn) resolves to [] without calling fn.
    - withDeadline(p, ms, onTimeout) resolves to onTimeout() when p neither resolves nor rejects within ms.
    - withDeadline floors to onTimeout() when p REJECTS (a thrown relations.get must not escape — it floors to the deterministic row).
    - withDeadline resolves to p's value (and clears the timer) when p resolves before ms.
  </behavior>
  <action>
    Create `src/worker/util/map-bounded.ts` exporting exactly two functions: `mapBounded<T,R>(items, limit, fn)` (a Promise pool that keeps at most `limit` workers pulling from a shared cursor, writing into a pre-sized results array by index so order is preserved) and `withDeadline<T>(p, ms, onTimeout)` (a Promise.race-style floor that resolves `onTimeout()` on a setTimeout, and ALSO floors `onTimeout()` on rejection, clearing the timer on settle). Copy the 16-RESEARCH.md Code Examples implementations verbatim (they are the canonical, reviewed shapes) and adapt only import style to the repo's `.ts` ESM convention. NO p-limit, NO new dependency (the bundle-size CI ceiling forbids it per RESEARCH lines 77-82). Write `test/worker/util/map-bounded.test.mjs` covering every <behavior> case using the repo's existing node:test harness style.
  </action>
  <verify>
    <automated>node --test test/worker/util/map-bounded.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - map-bounded.ts exports `mapBounded` and `withDeadline`; no other exports; no import of p-limit or any new dependency (grep -v '^//' src/worker/util/map-bounded.ts | grep -c "p-limit" returns 0).
    - The concurrency-ceiling test asserts the observed max-in-flight equals min(limit, items.length), not just <= limit (proves the pool actually parallelizes up to the ceiling).
    - The withDeadline reject-floors-to-onTimeout test passes (a rejecting promise yields onTimeout(), never throws).
    - `node --test test/worker/util/map-bounded.test.mjs` is green.
  </acceptance_criteria>
  <done>The reusable bounded pool + deadline floor exists and is fully tested; Wave B (16-03) can import both without further work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → host JSON-RPC (stdio) | Each relations.get is an untrusted-latency round-trip; the host runs it against Postgres under the org's own agent load. The deadline floor is the mitigation boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Denial of Service | unbounded parallel relations.get fan-out | mitigate | `mapBounded(limit=4-6)` caps in-flight host load; this plan ships the pool that Wave B uses. The concurrency-ceiling test proves the cap holds. |
| T-16-02 | Denial of Service | a single hung relations.get consuming the full 30s default → 502 | mitigate | `withDeadline(ms)` floors a hung/slow call to a deterministic fallback well under the host timeout; the reject-AND-timeout-floor tests prove neither path hangs. |
| T-16-SC | Tampering | npm/pip/cargo installs | mitigate | No package installed this plan (hand-rolled pool, no p-limit). No legitimacy checkpoint needed — zero external packages per RESEARCH "Package Legitimacy Audit: Not applicable". |
</threat_model>

<verification>
- `node --test test/worker/util/map-bounded.test.mjs` green.
- 16-SCHEMA-VERIFY.md exists with locked columns + a timeoutMs decision.
- No new dependency in package.json; no edit to any builder or handler yet (this plan is foundation-only).
</verification>

<success_criteria>
The bounded pool + deadline floor are shipped and tested, and the two Wave-0 unknowns (column names, timeoutMs reachability) are resolved and recorded — Waves A/B are unblocked to be written deterministically. (Contributes to SNAP-02 degrade-safety; the actual cold-time win lands in 16-02/16-03/16-04.)
</success_criteria>

<output>
Create `.planning/phases/16-snapshot-performance-honest-loading/16-01-SUMMARY.md` when done. Record the locked column set, the timeoutMs decision (SDK override vs withDeadline), and any column marked LIVE-CHECK-REQUIRED for the 16-04 BEAAA drill.
</output>
</content>
</invoke>
