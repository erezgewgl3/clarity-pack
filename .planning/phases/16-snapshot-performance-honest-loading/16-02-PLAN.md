---
phase: 16-snapshot-performance-honest-loading
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/worker/handlers/situation-room.ts
  - src/worker/handlers/org-blocked-backlog.ts
  - src/worker/situation/build-employees-rollup.ts
  - test/worker/situation/snapshot-prefetch.test.mjs
autonomous: true
requirements: [SNAP-01, SNAP-02]
must_haves:
  truths:
    - "The blocked-issue list, the agent roster, the per-agent focus issues, and every uuid→name resolution come from 2-3 ctx.db.query SQL reads, not from dozens of issues.list / agents.list / agents.get RPC round-trips."
    - "The blocker BFS (buildEdges) is computed ONCE for the union of blocked roots + each blocked agent's focus issue and shared by both builders, not walked twice."
    - "Every public.* SELECT filters WHERE company_id = $1 with a parameterized placeholder and NO company-prefix literal."
    - "Each snapshot stage logs its wall-clock ms via ctx.logger so the N+1 reduction is measurable in worker logs."
  artifacts:
    - path: "src/worker/handlers/situation-room.ts"
      provides: "One shared SQL prefetch + shared edge graph built once, passed to both builders; per-stage timing instrumentation"
      contains: "ctx.db.query"
    - path: "src/worker/handlers/org-blocked-backlog.ts"
      provides: "Blocked list + name resolution served from the prefetch instead of issues.list + per-uuid agents.get"
      contains: "company_id = $1"
    - path: "src/worker/situation/build-employees-rollup.ts"
      provides: "Roster + per-agent focus issues + name resolution served from SQL; shared BFS"
      contains: "company_id = $1"
    - path: "test/worker/situation/snapshot-prefetch.test.mjs"
      provides: "Round-trip-count assertion: ≤K RPC calls for N agents/M blocked issues; viewer-scoped needsYou unchanged"
  key_links:
    - from: "src/worker/handlers/situation-room.ts"
      to: "public.issues / public.agents"
      via: "ctx.db.query (one prefetch)"
      pattern: "ctx\\.db\\.query"
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "shared buildEdges result"
      via: "memoized edge graph passed via ctx/param"
      pattern: "buildEdges"
---

<objective>
Wave A — the single biggest cold-time win: collapse the N+1 RPC fan-out by serving the blocked-issue list, the agent roster, every per-agent focus issue, and all uuid→name resolution from 2-3 `ctx.db.query` SQL reads, and compute the blocker BFS ONCE shared across both builders. Add per-stage timing so the reduction is measurable.

Purpose: The cold 25.7s is an N+1 RPC round-trip amplification, not CPU (RESEARCH Summary). `issues`/`agents`/`companies` are already in `coreReadTables`, so the lists and name resolution can move to SQL; the two builders currently walk the SAME relations graph twice. This wave alone is expected to cut cold time from ~25s to a few seconds (RESEARCH "Recommended approach ordering").
Output: SQL-prefetch + shared edge graph wired through `situation-room.ts` into both builders, with stage timing and a round-trip-count test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-snapshot-performance-honest-loading/16-RESEARCH.md
@.planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md
@.planning/phases/16-snapshot-performance-honest-loading/16-01-SUMMARY.md

<interfaces>
<!-- Locked target SQL projections (16-PATTERNS.md lines 64-86; final column names per 16-SCHEMA-VERIFY.md from 16-01). -->
public.issues:  id, identifier, title, status, assignee_agent_id, assignee_user_id, updated_at  (WHERE company_id = $1 AND status='blocked' AND hidden_at IS NULL [+ EXCLUDE_OPERATION_ISSUES_SQL on the rollup focus read])
public.agents:  id, name, role, title, last_heartbeat_at, status, paused_at  (WHERE company_id = $1)

<!-- The exclusion clause to reuse verbatim (standing-numbers.ts:65-66). -->
EXCLUDE_OPERATION_ISSUES_SQL = "AND (origin_kind IS NULL OR origin_kind NOT LIKE 'plugin:clarity-pack%')"

<!-- The exported BFS to call ONCE + memoize (org-blocked-backlog.ts:270-355). DO NOT re-implement. -->
export async function buildEdges(ctx, companyId, startId): Promise<{ edges: BlockerEdge[]; nodeMeta: Record<string, EdgeNodeMeta> }>

<!-- Builder signatures (unchanged externally; ctx widened with db). -->
buildOrgBlockedBacklog(ctx: OrgBlockedBacklogCtx, companyId, viewerUserId): Promise<OrgBlockedBacklog>
buildEmployeesRollup(ctx: EmployeesRollupCtx, companyId, viewerUserId): Promise<{ employees, needsYou }>

<!-- ctx widening to mirror (standing-numbers.ts:117-121). -->
db: Pick<PluginDatabaseClient, 'query'>
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQL prefetch + shared edge graph in situation-room.ts; widen builder ctx with db</name>
  <files>src/worker/handlers/situation-room.ts, test/worker/situation/snapshot-prefetch.test.mjs</files>
  <read_first>
    - src/worker/handlers/situation-room.ts (the full handler — the valid-scope assertion lines 5-23, the builder invocations lines 108-141, the viewer-scoping line 101-104, the return shape lines 228-236)
    - src/worker/bulletin/standing-numbers.ts (lines 65-130 — the ctx.db.query SQL template, EXCLUDE_OPERATION_ISSUES_SQL, StandingNumbersCtx db typing, per-slot try/catch)
    - .planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md (the locked column names + any LIVE-CHECK-REQUIRED markers from 16-01)
    - src/worker/handlers/org-blocked-backlog.ts (the exported buildEdges signature ~line 270-355 — what the shared graph must produce)
    - .planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md (the situation-room.ts section lines 203-243 — shared prefetch, stage timing, viewer-invariant note)
  </read_first>
  <behavior>
    - Given a stubbed ctx.db.query returning M blocked issues + N agents, the handler issues exactly TWO db.query calls for the prefetch (one issues SELECT, one agents SELECT) — assert the call count.
    - The handler builds a `nameByUuid` Map once from the single agents SELECT (no per-uuid agents.get); assert agents.get is never called when the prefetch covers the uuids.
    - The handler computes buildEdges once per distinct startId across {blocked roots} ∪ {blocked agents' focus issues} and memoizes by startId; a startId appearing in both sets triggers exactly ONE buildEdges call — assert via a relations.get spy.
    - needsYou stays viewer-scoped on params.userId (unchanged); two different userId params over the SAME cached rows yield different needsYou.count (no cross-viewer bleed).
    - Each stage (prefetch, org-backlog, employees-rollup) emits a `ctx.logger.info('snap.stage', {stage, ms, companyId})` line.
  </behavior>
  <action>
    In `situation-room.ts`, before invoking the two builders, run the shared SQL prefetch using `ctx.db.query` (the handler's runtime ctx carries `db` — cast/extend `SituationRoomCtx` to include `db: Pick<PluginDatabaseClient,'query'>` mirroring StandingNumbersCtx at standing-numbers.ts:117-121). Issue TWO parameterized SELECTs: (1) blocked issues — `SELECT id, identifier, title, status, assignee_agent_id, assignee_user_id, updated_at FROM public.issues WHERE company_id = $1 AND status = 'blocked' AND hidden_at IS NULL` (apply `EXCLUDE_OPERATION_ISSUES_SQL` only where the rollup needs per-agent focus — see Task 2/3); (2) roster — `SELECT id, name, role, title, last_heartbeat_at, status, paused_at FROM public.agents WHERE company_id = $1`, params `[companyId]` for both. Build a `nameByUuid: Map<string,string>` from the agents rows. Build a shared edge-graph memo: a `Map<startId, {edges,nodeMeta}>` populated by calling the EXPORTED `buildEdges(ctx, companyId, startId)` ONCE per distinct startId across the union of {each blocked issue id} ∪ {each blocked agent's focus issue id}; on a thrown buildEdges, store an UNCLASSIFIED-floor sentinel for that startId (do NOT abort the prefetch). Pass `{ ...existing ctx, db, blockedIssues, nameByUuid, edgeGraph }` into both builders (extend OrgBlockedBacklogCtx / EmployeesRollupCtx to accept these optional prefetch fields; the builders fall back to their old RPC path only if a field is absent, preserving degrade-safety). Wrap the prefetch, the org-backlog build, and the rollup build each in `const t0=Date.now(); ... ctx.logger?.info?.('snap.stage',{stage,ms:Date.now()-t0,companyId})`. Keep `viewerUserId` from `params.userId` and the return shape `{org_blocked_backlog, situation_employees, needsYou, pulse, taken_at}` UNCHANGED. Do NOT re-enable ACTION_CARDS_ENABLED (stays gated OFF). Do NOT touch buildPulseSummary. Use parameterized `$1` only — NO interpolation, NO company-prefix literal. Add `test/worker/situation/snapshot-prefetch.test.mjs` covering every <behavior> case with stubbed ctx clients (spy on db.query / relations.get / agents.get counts).
  </action>
  <verify>
    <automated>node --test test/worker/situation/snapshot-prefetch.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - situation-room.ts issues exactly two ctx.db.query prefetch calls per snapshot; the round-trip test asserts agents.get and per-agent issues.list are NOT called when the prefetch covers them.
    - buildEdges is called at most once per distinct startId across the blocked∪focus union (memoized) — proven by a relations.get spy count in the test.
    - Two distinct userId params over identical prefetched rows produce distinct needsYou.count (viewer-scoping preserved; T-16-03 mitigation).
    - grep confirms NO company-prefix literal and NO `${` interpolation in any new SQL string in situation-room.ts (grep -v '^//' src/worker/handlers/situation-room.ts | grep -Ec "'BEAAA-|'COU-|\\$\\{" returns 0).
    - Each stage logs `snap.stage` with `{stage, ms, companyId}`.
  </acceptance_criteria>
  <done>One shared SQL prefetch + one shared edge graph + stage timing are wired through the handler; both builders receive the prefetch and the viewer-scoped needsYou is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: org-blocked-backlog.ts reads from the prefetch (blocked list + uuid→name)</name>
  <read_first>
    - src/worker/handlers/org-blocked-backlog.ts (lines 360-415 the list+pairing loop, the per-uuid agents.get name-resolution block ~482-503, the unclassifiedChain floor 220-239, the "no company-prefix literal" invariant 94-96, the row-emit 511-571)
    - src/worker/handlers/situation-room.ts (the prefetch fields just added in Task 1 — blockedIssues, nameByUuid, edgeGraph — and how they are passed)
    - .planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md (the org-blocked-backlog section lines 89-149 — "What changes" line 146-149)
  </read_first>
  <action>
    In `buildOrgBlockedBacklog`, replace the `ctx.issues.list({companyId,status:'blocked'})` call (~line 374) with the prefetched `ctx.blockedIssues` when present (fall back to the existing RPC list only if the prefetch field is absent — keeps old fixtures + degrade-safety working). Replace the per-UUID `ctx.agents.get` name-resolution loop (~lines 482-503) with lookups in the prefetched `ctx.nameByUuid` Map (a missing uuid → `null` name, NEVER the raw UUID — preserve the existing NO_UUID_LEAK posture). Read each issue's edges from the shared `ctx.edgeGraph.get(startId)` instead of calling `buildEdges` directly; if the shared memo holds the UNCLASSIFIED sentinel for a startId, emit the existing `unclassifiedChain(startId, 'relations-walk-failed')` floor row (reuse verbatim — do NOT invent a new shape). Leave `flattenBlockerChain`, `pickTopChains`, `scrubHumanAction`, and the row-emit (511-571) UNCHANGED. Preserve the "no company-prefix literal" invariant. No new SQL is written in THIS file (the SELECT lives in the handler); this file only consumes the prefetch.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/org-blocked-backlog.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - buildOrgBlockedBacklog no longer calls ctx.agents.get inside the name-resolution loop when ctx.nameByUuid is supplied (source assertion: the per-uuid agents.get loop is replaced by a Map lookup).
    - When the prefetch is absent (old fixture), the builder still works via the original RPC path (existing org-blocked-backlog tests stay green).
    - A startId carrying the shared UNCLASSIFIED sentinel emits an `unclassifiedChain(... 'relations-walk-failed')` row, not a dropped issue.
    - No raw UUID ever appears as an owner name (a missing nameByUuid entry yields null, the existing NO_UUID_LEAK behavior).
    - Existing org-blocked-backlog.test.mjs is green.
  </acceptance_criteria>
  <done>The org backlog reads its blocked list + names from the shared prefetch and its edges from the shared graph; no per-uuid name RPCs, no second relations walk.</done>
</task>

<task type="auto">
  <name>Task 3: build-employees-rollup.ts reads roster + per-agent focus + names from the prefetch</name>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts (lines 300-573 buildOneEmployeeRow — the per-agent issues.list ~307, the buildEdges call ~357-361, the per-uuid agents.get nested Promise.all ~416-430, the multi-hop leaf issues.get ~468-488, the inline UNCLASSIFIED block 541-571; and the agents.list + Promise.all fan-out 611-636)
    - src/worker/bulletin/standing-numbers.ts (lines 65-66 EXCLUDE_OPERATION_ISSUES_SQL — to apply to the per-agent focus read per Open Question #2)
    - src/worker/handlers/situation-room.ts (the prefetch fields from Task 1)
    - .planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md (the build-employees-rollup section lines 153-199 — "per-agent reads to SQL-ify / share", the inline UNCLASSIFIED block to KEEP, the ctx-widening note 197-199)
  </read_first>
  <action>
    In `buildEmployeesRollup`, replace `ctx.agents.list({companyId})` (~line 617) with the prefetched roster (`ctx.roster` / the agents rows passed from the handler; fall back to the RPC only if absent). In `buildOneEmployeeRow`: serve the per-agent focus issues from the prefetched issue set grouped by `assignee_agent_id` instead of `ctx.issues.list({companyId,assigneeAgentId,limit:50})` (~line 307) — and ensure the handler's per-agent focus SELECT applies `EXCLUDE_OPERATION_ISSUES_SQL` so the Editor-Agent's own operation issues never show as an agent's focus (OQ#2). Read edges from the shared `ctx.edgeGraph` (Pattern 2) instead of calling `buildEdges` directly (~357). Replace the nested per-UUID `ctx.agents.get` Promise.all (~416-430) with lookups in `ctx.nameByUuid` (missing → null, never the UUID). Serve the multi-hop leaf `ctx.issues.get(leafNodeId,companyId)` (~468-488) from the prefetched issue set when the leaf is in-company; fall back to the RPC ONLY for a leaf not in the prefetch. KEEP the per-row try/catch → `degradeSafeRow` (624-636) and the inline UNCLASSIFIED degrade block (541-571) UNCHANGED — they are the per-row floor. Add `db`/prefetch fields to `EmployeesRollupCtx` as optional (mirror StandingNumbersCtx). Do NOT change the deterministic sort. NOTE: this task leaves the rollup's `Promise.all(agents.map(...))` fan-out as-is — Wave B (16-03) swaps it for the bounded pool.
  </action>
  <verify>
    <automated>node --test test/worker/situation/build-employees-rollup.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - buildOneEmployeeRow no longer calls ctx.agents.get inside the per-row loop when ctx.nameByUuid is supplied (source assertion).
    - The per-agent focus issues come from the prefetched set grouped by assignee_agent_id, not a per-agent ctx.issues.list, when the prefetch is present.
    - The per-agent focus read applies EXCLUDE_OPERATION_ISSUES_SQL so a plugin:clarity-pack% operation issue never appears as an agent's focus (assert with a fixture containing one operation issue assigned to an agent).
    - A leaf issue present in the prefetch is served without a leaf ctx.issues.get; a leaf NOT in the prefetch still falls back to the RPC.
    - The inline UNCLASSIFIED block and degradeSafeRow paths are unchanged; existing build-employees-rollup.test.mjs is green.
  </acceptance_criteria>
  <done>The rollup reads roster, per-agent focus, names, and leaf identifiers from the shared prefetch + edge graph; the only remaining per-agent fan-out is the (still-unbounded) Promise.all that Wave B bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → host Postgres (ctx.db.query) | New raw SQL crosses here; untrusted only insofar as companyId/userId are handler params — they must be parameterized, never interpolated. |
| per-company snapshot rows → per-viewer needsYou | The prefetched rows are company-scoped; the needsYou partition is viewer-scoped and must be recomputed per call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | viewer-scoped needsYou served from shared per-company rows | mitigate | needsYou stays keyed on params.userId; the prefetch caches only viewer-invariant rows; a test asserts two userIds over identical rows yield different needsYou.count (no cross-viewer leak). |
| T-16-04 | Information Disclosure | cross-company leak via a missing company filter | mitigate | Every prefetch SELECT filters `WHERE company_id = $1`; an acceptance grep asserts the filter is present and no prefix literal exists. |
| T-16-05 | Tampering | SQL injection via the new ctx.db.query calls | mitigate | Parameterized `$1`/`$2` only; an acceptance grep asserts no `${` interpolation in the new SQL strings (mirrors standing-numbers.ts T-03-10). |
| T-16-06 | Information Disclosure | raw UUID rendered as an owner name when nameByUuid misses | mitigate | A missing Map entry yields `null` (the existing NO_UUID_LEAK posture), never the raw UUID. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installed this plan (read-path SQL only); RESEARCH "Package Legitimacy Audit: Not applicable". |
</threat_model>

<verification>
- All three suites green: snapshot-prefetch, org-blocked-backlog, build-employees-rollup.
- Round-trip-count test proves the prefetch issues ≤K RPC calls for N agents/M blocked issues vs the old per-agent/per-uuid fan-out.
- Stage timing visible in worker logs (`snap.stage`).
- No company-prefix literal, no SQL interpolation, no re-enabling of action-cards, no edit to blocker-chain.ts / build-pulse-summary.ts / leverage.ts / agent-liveness.ts / classify-employee-state.ts / scrub-human-action.ts.
</verification>

<success_criteria>
The N+1 RPC fan-out is collapsed to 2-3 SQL reads + one shared BFS, with stage timing to prove it — the expected primary contributor to bringing cold time from ~25.7s toward a few seconds (SNAP-01), while every public.* read is company-scoped, parameterized, and prefix-literal-free (SNAP-02 degrade-safety preserved per-row).
</success_criteria>

<output>
Create `.planning/phases/16-snapshot-performance-honest-loading/16-02-SUMMARY.md` when done. Record the measured per-stage timings from the unit-test/local run, the round-trip-count before/after, and any column that still needs the 16-04 live `\d` confirmation.
</output>
