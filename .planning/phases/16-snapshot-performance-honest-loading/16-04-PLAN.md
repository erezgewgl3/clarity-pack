---
phase: 16-snapshot-performance-honest-loading
plan: 04
type: execute
wave: 4
depends_on: ["16-02", "16-03"]
files_modified:
  - src/worker/situation/snapshot-cache.ts
  - src/worker/handlers/situation-room.ts
  - test/worker/situation/snapshot-cache.test.mjs
  - migrations/0017_situation_snapshot_index.sql
autonomous: false
requirements: [SNAP-01, SNAP-02, SNAP-03]
must_haves:
  truths:
    - "On a snapshot call the most-recent fresh cached row (viewer-invariant slice) is served immediately when present, and a fresh recompute is kicked off from inside the valid data-handler scope (no cron, no setInterval)."
    - "Only the viewer-invariant slice (org backlog rows, employees rollup, pulse) is cached; the viewer-scoped needsYou is recomputed per call from the cached rows as a pure filter — no cross-viewer leak."
    - "Cold-cache Situation Room load returns well under the 30s host timeout (target p95 < ~5s) and never 502s, measured live on BEAAA against the recorded 25.7s baseline."
    - "The fix is instance-agnostic (no company-prefix literals) and additive-only (the SWR write reuses the existing situation_snapshots table; any index is additive, namespace-qualified, no procedural blocks; disable/uninstall preserves data)."
  artifacts:
    - path: "src/worker/situation/snapshot-cache.ts"
      provides: "SWR read (most-recent fresh row) + write (ON CONFLICT DO NOTHING) repo for situation_snapshots"
      exports: ["readLatestSnapshot", "writeSnapshot"]
    - path: "src/worker/handlers/situation-room.ts"
      provides: "Serve-last-good + valid-scope fire-and-forget recompute + per-call needsYou recompute"
    - path: "test/worker/situation/snapshot-cache.test.mjs"
      provides: "SWR serve/write + viewer-invariant caching + per-call needsYou recompute tests"
    - path: "migrations/0017_situation_snapshot_index.sql"
      provides: "OPTIONAL additive (company_id, taken_at DESC) index — ONLY if profiling shows the most-recent-row SELECT is slow"
  key_links:
    - from: "src/worker/handlers/situation-room.ts"
      to: "plugin_clarity_pack_cdd6bda4bd.situation_snapshots"
      via: "readLatestSnapshot / writeSnapshot (ctx.db)"
      pattern: "situation_snapshots"
    - from: "live BEAAA cold load"
      to: "recorded SNAP-03 baseline (cold 25.7s, 6/6 200, no 502)"
      via: "bookended deploy drill before/after timing"
      pattern: "25.7s|snap.stage"
---

<objective>
Wave C + phase close — make the cold load instant via stale-while-revalidate (serve the last good snapshot, recompute in scope), then PROVE the whole phase live on BEAAA against the recorded SNAP-03 baseline (cold 25.7s, 6/6 snapshot calls 200, no 502).

Purpose: 16-02 + 16-03 cut the recompute cost dramatically, but the cold case still pays the recompute. SWR decouples "view loads fast" from "compute finishes" by serving the last good (viewer-invariant) snapshot immediately and revalidating from inside the valid data-handler scope. The phase's success criteria are LIVE thresholds — they can only be confirmed by the bookended BEAAA drill, which also back-fills any column marked LIVE-CHECK-REQUIRED in 16-01.
Output: a new `snapshot-cache.ts` repo, SWR wiring in the handler (with per-call viewer-scoped needsYou recompute), an OPTIONAL additive index migration, and a live before/after timing record vs the 25.7s baseline.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-snapshot-performance-honest-loading/16-RESEARCH.md
@.planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md
@.planning/phases/16-snapshot-performance-honest-loading/16-SCHEMA-VERIFY.md
@.planning/phases/16-snapshot-performance-honest-loading/16-02-SUMMARY.md
@.planning/phases/16-snapshot-performance-honest-loading/16-03-SUMMARY.md

<interfaces>
<!-- The SWR repo analog (tldr-cache.ts:97-111 read; :73-89 ON CONFLICT write). Copy this shape. -->
readLatestSnapshot(ctx, companyId): Promise<{ payload: ViewerInvariantSlice; takenAt: string } | null>
writeSnapshot(ctx, companyId, payload, contentHash): Promise<void>   // ON CONFLICT (computed_for_company_id, content_hash) DO NOTHING

<!-- Existing situation_snapshots columns (migrations/0003:37-44) — NO viewer_user_id, NO taken_at index beyond PK. -->
(id, taken_at, computed_for_company_id, payload jsonb, content_hash, UNIQUE(company_id, content_hash))

<!-- SWR read SQL (16-PATTERNS.md line 262-263). -->
SELECT payload, taken_at FROM plugin_clarity_pack_cdd6bda4bd.situation_snapshots
  WHERE computed_for_company_id = $1 ORDER BY taken_at DESC LIMIT 1

<!-- Freshness window: RESEARCH OQ#3 — start ~60s; record the chosen value. -->
<!-- Viewer-invariant slice = { org_blocked_backlog rows, situation_employees, pulse }. needsYou is NOT cached. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: snapshot-cache.ts SWR repo + handler serve-last-good with per-call needsYou recompute</name>
  <files>src/worker/situation/snapshot-cache.ts, src/worker/handlers/situation-room.ts, test/worker/situation/snapshot-cache.test.mjs</files>
  <read_first>
    - src/worker/db/tldr-cache.ts (the canonical most-recent SELECT lines 97-111 + the ON CONFLICT DO NOTHING write 73-89 + the TldrCacheCtx db typing — copy this repo shape verbatim)
    - migrations/0003_situation_and_optin.sql (lines 37-44 — the existing situation_snapshots columns; confirm computed_for_company_id, payload, content_hash, taken_at exist; NO viewer_user_id)
    - src/worker/handlers/situation-room.ts (the valid-scope assertion 5-23, the viewer-scoping line 101-104, the return shape 228-236, the stage timing + prefetch from 16-02/16-03)
    - .planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md (the snapshot-cache.ts section lines 246-279 + the "valid-scope, no background loop" pattern 356-360)
    - .planning/phases/16-snapshot-performance-honest-loading/16-RESEARCH.md (Pattern 4 lines 193-204 + Pitfall 2 + Pitfall 4)
  </read_first>
  <behavior>
    - readLatestSnapshot returns the single most-recent row for a company (ORDER BY taken_at DESC LIMIT 1) or null when none exists.
    - writeSnapshot inserts the viewer-invariant slice with ON CONFLICT (computed_for_company_id, content_hash) DO NOTHING (idempotent; same hash twice is a no-op).
    - On a snapshot call WITH a fresh cached row (< FRESHNESS_MS old): the handler returns the cached viewer-invariant slice immediately AND triggers a fresh recompute from inside the handler scope (fire-and-forget; no cron, no setInterval).
    - On a snapshot call with NO cached row OR a stale one: the handler recomputes synchronously, returns the fresh result, and writes it to the cache for the next caller.
    - needsYou is NEVER read from the cache — it is recomputed per call from the cached/fresh viewer-invariant rows as a pure filter on params.userId; two different userIds over the SAME cached rows yield different needsYou.count (no cross-viewer leak).
    - The cached payload contains ONLY the viewer-invariant slice (org backlog rows, employees rollup, pulse); it does NOT contain a viewer-scoped needsYou count.
  </behavior>
  <action>
    Create `src/worker/situation/snapshot-cache.ts` exporting `readLatestSnapshot(ctx, companyId)` and `writeSnapshot(ctx, companyId, payload, contentHash)`, copying the tldr-cache.ts repo shape verbatim. Read SQL: `SELECT payload, taken_at FROM plugin_clarity_pack_cdd6bda4bd.situation_snapshots WHERE computed_for_company_id = $1 ORDER BY taken_at DESC LIMIT 1`, params `[companyId]`, return `rows[0] ?? null`, parse `payload` jsonb. Write SQL: `INSERT INTO plugin_clarity_pack_cdd6bda4bd.situation_snapshots (taken_at, computed_for_company_id, payload, content_hash) VALUES (now(), $1, $2::jsonb, $3) ON CONFLICT (computed_for_company_id, content_hash) DO NOTHING` via `ctx.db.execute` (namespace-only DML, returns {rowCount}, no RETURNING — see reply-resume-repo.ts:71). In `situation-room.ts`: cache ONLY the viewer-invariant slice `{ org_blocked_backlog, situation_employees, pulse }` (NOT needsYou — Pitfall 2 / T-16-03). On each call: read the latest cached row; if present and `Date.now() - Date.parse(takenAt) < FRESHNESS_MS` (start FRESHNESS_MS=60000; record the chosen value per OQ#3), serve it immediately and ALSO kick off the fresh recompute fire-and-forget from inside the handler scope (do NOT await it before responding; do NOT add a jobs[] cron or setInterval — Pitfall 4); the recompute writes back via writeSnapshot. If absent/stale, recompute synchronously, respond, and write back. In BOTH paths, recompute the viewer-scoped `needsYou` per call from the (cached or fresh) viewer-invariant rows — a pure filter over already-computed rows keyed on `viewerUserId`, NO new fetch. Compute `contentHash` deterministically over the viewer-invariant slice (reuse the existing content-hash helper if one exists in the repo; otherwise a stable JSON hash). Keep the return shape `{org_blocked_backlog, situation_employees, needsYou, pulse, taken_at}` UNCHANGED — SWR adds nothing to the shape; `taken_at` already signals snapshot age. Add `test/worker/situation/snapshot-cache.test.mjs` covering every <behavior> case (stubbed ctx.db with controllable rows + a spy on writeSnapshot / the recompute trigger).
  </action>
  <verify>
    <automated>node --test test/worker/situation/snapshot-cache.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - snapshot-cache.ts exports readLatestSnapshot + writeSnapshot; the write uses ON CONFLICT (computed_for_company_id, content_hash) DO NOTHING via ctx.db.execute; the read is ORDER BY taken_at DESC LIMIT 1.
    - The cached payload contains org_blocked_backlog + situation_employees + pulse and does NOT contain a viewer-scoped needsYou count (source + test assertion: T-16-03 no cross-viewer leak).
    - A fresh cached row is served immediately and a fire-and-forget recompute is triggered inside the handler scope — NO jobs[] entry, NO setInterval anywhere (grep -v '^//' src/worker/handlers/situation-room.ts | grep -Ec "setInterval|jobs\\.schedule" returns 0).
    - Two distinct userId params over identical cached rows yield distinct needsYou.count (per-call recompute, pure filter, no fetch).
    - All SQL is namespace-qualified literally (plugin_clarity_pack_cdd6bda4bd.situation_snapshots) and parameterized ($1/$2/$3); no company-prefix literal, no `${` interpolation.
  </acceptance_criteria>
  <done>SWR serves the last-good viewer-invariant snapshot instantly, revalidates in scope, and recomputes the viewer-scoped needsYou per call with no cross-viewer leak and no background loop.</done>
</task>

<task type="auto">
  <name>Task 2: OPTIONAL additive index migration (only if profiling shows the most-recent-row SELECT is slow)</name>
  <read_first>
    - migrations/0008_chat_topics_archived_at.sql (the additive ALTER/IF NOT EXISTS template + the validator constraints: namespace-qualified literal, no procedural blocks, apostrophe-free comments, ends on a semicolon-terminated statement)
    - migrations/0003_situation_and_optin.sql (the situation_snapshots table header + validator notes 13-27)
    - test/migrations/no-procedural-blocks.test.mjs (the validator the new file must pass)
    - .planning/phases/16-snapshot-performance-honest-loading/16-PATTERNS.md (the 0017 section lines 314-331)
    - .planning/phases/16-snapshot-performance-honest-loading/16-02-SUMMARY.md + 16-03-SUMMARY.md (the measured stage timings — does the most-recent-row SELECT itself register as slow?)
  </read_first>
  <action>
    DECISION GATE (RESEARCH OQ#3): only create `migrations/0017_situation_snapshot_index.sql` IF the 16-02/16-03 stage timings (or the Task-1 SWR read timing) show the most-recent-row SELECT is itself a measurable cost. The default per RESEARCH is the NO-MIGRATION path — if the SELECT is fast (it is a single-row ORDER BY LIMIT 1 on a small table), SKIP this task and record "no migration needed — SWR read is sub-ms" in the SUMMARY. IF needed, create the additive index: `CREATE INDEX IF NOT EXISTS situation_snapshots_company_taken_idx ON plugin_clarity_pack_cdd6bda4bd.situation_snapshots (computed_for_company_id, taken_at DESC);` — fully namespace-qualified literally (NO template substitution), NO anonymous procedural block (no `DO $$`/dollar-quoting — the host validator rejects them), apostrophe-free comments, ending on a semicolon-terminated statement. Do NOT add a viewer_user_id column (the viewer-invariant-cache design from Task 1 makes it unnecessary). Migrations are additive-only; disable/uninstall preserves data by construction.
  </action>
  <verify>
    <automated>node --test test/migrations/no-procedural-blocks.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - EITHER no 0017 file exists and the SUMMARY records "no migration needed" with the SWR-read timing evidence, OR 0017 exists as a single additive CREATE INDEX IF NOT EXISTS, namespace-qualified literally, with no procedural block.
    - If created, test/migrations/no-procedural-blocks.test.mjs passes for the new file.
    - No viewer_user_id column is added; the migration touches only the plugin namespace (no public.* DDL).
  </acceptance_criteria>
  <done>The SWR read is either fast enough to need no migration (recorded) or backed by one additive, namespace-qualified, procedural-block-free index — additive-only and uninstall-safe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bookended live BEAAA drill — cold/warm timing vs the 25.7s baseline + column back-fill</name>
  <what-built>
    The full Phase 16 worker change is built and locally green: SQL prefetch + shared BFS (16-02), bounded concurrency + deadline floor (16-03), and SWR serve-last-good (16-04 Task 1). The worker + UI bundles rebuild via the existing build scripts; the version is bumped in BOTH package.json AND src/manifest.ts per DEPLOY-RUNBOOK. The change is read-path-only and additive (SWR reuses the existing situation_snapshots table).
  </what-built>
  <how-to-verify>
    Claude has already (autonomously, per the deploy authorization): rebuilt both bundles, bumped the version in package.json + src/manifest.ts, confirmed the automated DO backup is the active bookend, and deployed to BEAAA via the recorded local-tarball procedure (uninstall-then-install, extract-dir not tgz, from ~/paperclip). The human verification steps:
    1. Confirm the automated DO backup completed immediately before the deploy (the bookend) — note the backup id/time.
    2. (Back-fill 16-01 LIVE-CHECK-REQUIRED columns, if any) run `\d public.issues` and `\d public.agents` in the deploy window and confirm the snake_case names the SQL projects exist; if any differ, that is a blocking defect → fix the SELECT before measuring.
    3. Restart the clarity-pack worker (or trigger a cold cache) and open `/BEAAA/situation-room` COLD; record the wall-clock load time AND the worker `snap.stage` log totals + the relations.get round-trip count. Expect well under the 30s timeout (target ~5s or less), and NO 502 (compare to the recorded baseline: cold 25.7s, 6/6 200, no 502).
    4. Reload WARM (within the SWR freshness window); confirm a near-instant serve-last-good (sub-second) and that a fresh recompute ran in the background (a new situation_snapshots row appears).
    5. Confirm the room renders honestly: a deliberately-slow/failed sub-read (or a naturally slow row) shows the deterministic UNCLASSIFIED line, the rest of the view intact, no blank/forever-spinner.
    6. Confirm no notification storm and no CPU spike attributable to the worker (it should stay near idle; the box's host-Postgres/agent CPU is the company's, not Clarity's — per the v1.4.3 incident finding).
  </how-to-verify>
  <files>(none — verification + deploy + measurement; recorded in 16-04-SUMMARY.md)</files>
  <action>Run the bookended live drill per the how-to-verify steps: confirm the automated DO backup bookend, back-fill any LIVE-CHECK-REQUIRED column via `d public.issues` / `d public.agents`, open /BEAAA/situation-room cold and record wall-clock + snap.stage totals + relations.get round-trip count vs the 25.7s baseline, reload warm to confirm serve-last-good + a background recompute row, confirm a slow/failed sub-read floors to the deterministic line with the view intact, and confirm no notification storm / no worker CPU spike.</action>
  <verify>
    <human-check>Cold `/BEAAA/situation-room` returns well under 30s (target p95 < ~5s) and never 502s vs the 25.7s baseline; warm reload serves near-instant; a slow/failed sub-read floors to the deterministic line with the view intact; the deploy is bookended by the automated DO backup; any LIVE-CHECK-REQUIRED column is confirmed.</human-check>
  </verify>
  <resume-signal>Type "approved" with the recorded cold/warm timings + round-trip counts, or describe the regression (e.g. a column mismatch, a 502, or a non-floored hang) for a gap-closure pass.</resume-signal>
  <done>Cold /BEAAA/situation-room returns well under the 30s host timeout (target p95 < ~5s) and never 502s vs the 25.7s baseline; warm reload serves near-instant with a background recompute; a slow/failed sub-read floors to the deterministic line with the view intact; the deploy is bookended by the automated DO backup; any LIVE-CHECK-REQUIRED column is confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-company cache row → per-viewer response | The cached slice is company-keyed; the viewer-scoped needsYou must be recomputed per call, never served from a company-only-keyed count. |
| worker → namespace DML (ctx.db.execute) | The SWR write goes to the plugin namespace only; no public.* write. |
| deploy → live BEAAA Postgres + filesystem | Bookended-by-snapshots rule: the automated DO backup is the verified before-snapshot; the change is additive + read-path so uninstall preserves data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Information Disclosure | viewer-scoped needsYou served from a per-company cache (cross-viewer leak) | mitigate | Cache ONLY the viewer-invariant slice; recompute needsYou per call as a pure filter on params.userId; a test asserts two userIds over identical cached rows yield different counts. |
| T-16-08 | Tampering | non-additive / public.* DDL or a procedural-block migration the host rejects | mitigate | The SWR write reuses the existing table (no DDL); the OPTIONAL 0017 is a single additive CREATE INDEX IF NOT EXISTS, namespace-qualified, no procedural block, validated by no-procedural-blocks.test.mjs. |
| T-16-04 | Information Disclosure | cross-company leak via a missing company filter on the SWR read/write | mitigate | readLatestSnapshot/writeSnapshot filter/insert on computed_for_company_id = $1; parameterized only; no prefix literal. |
| T-16-09 | Denial of Service / Availability | a bad deploy bricking the live cockpit | mitigate | Bookended by the verified automated DO backup (the rehearsed restore path); read-path + additive change; disable/uninstall preserves data. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installed (SWR uses ctx.db + existing helpers); RESEARCH "Package Legitimacy Audit: Not applicable". |
</threat_model>

<verification>
- snapshot-cache.test.mjs + no-procedural-blocks.test.mjs green.
- Full worker test suite green (the SWR + bounding + prefetch changes integrate).
- Live BEAAA drill (Task 3) records cold/warm timings + round-trip counts vs the 25.7s baseline; no 502; degrade line renders honestly; deploy bookended by the DO backup.
- No background loop / cron; no DO-NOT-TOUCH file edited; no company-prefix literal; no public.* DDL.
</verification>

<success_criteria>
The cockpit loads fast and honestly LIVE: cold `/BEAAA/situation-room` returns well under the 30s host timeout (target p95 < ~5s) and never 502s — the 25.7s cold near-cliff is eliminated (SNAP-01) — with serve-last-good SWR, a degrade-safe per-row floor (SNAP-02), measured against the recorded confirm-first baseline (SNAP-03), shipped instance-agnostic + additive-only via the DO-backup-bookended BEAAA deploy.
</success_criteria>

<output>
Create `.planning/phases/16-snapshot-performance-honest-loading/16-04-SUMMARY.md` when done. Record: the chosen FRESHNESS_MS; whether 0017 was created or skipped (with the SWR-read timing); the live cold/warm wall-clock + snap.stage totals + round-trip counts vs the 25.7s/6-of-6-200 baseline; the DO backup id used as the bookend; and any column back-filled from the live `\d` check.
</output>
