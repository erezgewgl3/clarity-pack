---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 01
subsystem: worker/db (durable loop-governor foundation)
tags: [migration, repo, loop-fix, wake-governor, kill-switch, provenance, tdd]
requires: []
provides:
  - "migration 0017_loop_governor.sql (own_operation_issues, wake_ledger, wake_kill_switch tables, plugin namespace, additive)"
  - "own-operation-issues-repo: isOwnOperationIssue + recordOwnOperationIssue (D-03/D-04 durable provenance)"
  - "wake-ledger-repo: appendWake + countTrailingWakes + pruneOldWakes (D-06 sliding-window rate source)"
  - "wake-kill-switch-repo: isEngaged + engage + clear (D-08 durable version-scoped operator-clear-only)"
affects:
  - "downstream plans 16.1-02..06 (loop-break, wake-governor, ingress gate) read/write through these repos"
tech-stack:
  added: []
  patterns:
    - "reply-resume-repo ctx contract (query=SELECT-only; execute=namespace DML, rowCount only, never rows)"
    - "circuit-breaker version-scope (import manifest; plugin_version = manifest.version; fail-open durable read)"
    - "host migration validator rules (fully-qualified namespace literal, inline UNIQUE, apostrophe-free COMMENT ON, no CREATE INDEX, no DO blocks)"
    - "Node strip-only TS (no parameter-property shorthand; .mjs-importable)"
key-files:
  created:
    - migrations/0017_loop_governor.sql
    - src/worker/db/own-operation-issues-repo.ts
    - src/worker/db/wake-ledger-repo.ts
    - src/worker/db/wake-kill-switch-repo.ts
    - test/worker/db/own-operation-issues-repo.test.mjs
    - test/worker/db/wake-ledger-repo.test.mjs
    - test/worker/db/wake-kill-switch-repo.test.mjs
  modified: []
decisions:
  - "Kill-switch read is version-scoped to manifest.version (Open Q #3 = YES) so a corrected build is not DOA on a pre-fix tripped switch."
  - "No auto-clear path in worker code; clearing the kill-switch is an explicit operator gesture only (governance parity with circuit-breaker, coexistence #4)."
  - "wake_ledger has no index — repo-pruned alongside each append keeps it to a few dozen rows; a standalone CREATE INDEX would also violate the host validator."
metrics:
  duration: ~25m
  completed: 2026-06-08
  tasks: 3
  files: 7
  commits: 5
  tests_added: 14
---

# Phase 16.1 Plan 01: Durable Loop-Governor Foundation Summary

Durable Postgres foundation for the Editor-Agent loop fix: one additive plugin-namespace migration (`0017`) creating `own_operation_issues`, `wake_ledger`, and `wake_kill_switch`, plus three thin repos that survive a worker restart — the exact failure mode (in-memory op-issue set empty-on-restart) behind the 2026-06-04 loop storm.

## What Was Built

- **Migration `0017_loop_governor.sql`** — three `CREATE TABLE IF NOT EXISTS` in `plugin_clarity_pack_cdd6bda4bd.*`, each with inline `UNIQUE` where needed and an apostrophe-free `COMMENT ON`. Additive-only, zero `public.*` DDL, no `CREATE INDEX`, no procedural blocks. Auto-scanned and passed by `ddl-prefix-validator` + `no-procedural-blocks`.
- **`own-operation-issues-repo.ts`** (D-03/D-04) — `isOwnOperationIssue` (durable provenance read, company-scoped) + `recordOwnOperationIssue` (`ON CONFLICT (company_id, issue_id) DO NOTHING`). The restart-safe backstop for the ingress event gate.
- **`wake-ledger-repo.ts`** (D-06) — `appendWake`, `countTrailingWakes(windowSeconds)` (the throughput-governor rate source), `pruneOldWakes` (self-draining sliding window).
- **`wake-kill-switch-repo.ts`** (D-08) — `isEngaged` (version-scoped, fail-open), `engage` (atomic `ON CONFLICT (company_id) DO UPDATE` upsert), `clear` (operator-only reset). Version source = `manifest.version` (same import as circuit-breaker).

## How It Was Verified

- `node --test` across both migration validators + all three repo test files: **46/46 pass**.
- Migration greps: 7 fully-qualified namespace refs (≥6 required), 0 `CREATE INDEX`, all three table names present, apostrophe-free comments.
- `grep -c 'private readonly'` = 0 across all three repos (Node strip-only TS).
- `git diff --name-status 894fc88 HEAD` shows **7 files, all additions (`A`), zero modifications** to existing source — read-time no-touch boundary (LOOP-06) preserved by construction.

## TDD Gate Compliance

Tasks 2 and 3 (`tdd="true"`) followed RED → GREEN:
- RED: `test(16.1-01)` commits 169ecc4 (own-op + ledger) and 25dfb8d (kill-switch) — tests fail because repo modules do not yet exist.
- GREEN: `feat(16.1-01)` commits 2df692b and 7d5c383 make them pass.
Task 1 is the migration (DDL, no behavior cycle) — covered by the always-on host-validator scanners.

## Deviations from Plan

None — plan executed exactly as written.

## Threat Model Coverage

- **T-161-01** (SQL injection): all repo SQL parameterized `$1`/`$2`, zero identifier interpolation. ✓
- **T-161-02** (ledger bloat DoS): `pruneOldWakes` provided for beside-append calling; table stays tiny. ✓
- **T-161-03** (kill-switch DOA): `isEngaged` version-scoped on `plugin_version = manifest.version`. ✓
- **T-161-04** (migration mutates `public.*`): additive plugin-namespace only; both DDL validators green. ✓
- **T-161-05** (governor read wedges dispatch): `isEngaged` fails open (returns false) on query error. ✓ (accept disposition)

## Commits

- 67d5645 `feat(16.1-01): add migration 0017 loop governor tables`
- 169ecc4 `test(16.1-01): add failing tests for own-operation + wake-ledger repos`
- 2df692b `feat(16.1-01): implement own-operation-issues + wake-ledger repos`
- 25dfb8d `test(16.1-01): add failing test for wake-kill-switch repo`
- 7d5c383 `feat(16.1-01): implement wake-kill-switch repo (durable, version-scoped)`

## Known Stubs

None. All three repos are fully implemented and exercised by unit tests; the durable tables are consumed by downstream plans 16.1-02..06.

## Self-Check: PASSED

- Created files all present (migration + 3 repos + 3 tests verified on disk).
- All 5 task commits present in `git log`.
- 46/46 tests green; diff scope is 7 additions, no existing-source edits.
