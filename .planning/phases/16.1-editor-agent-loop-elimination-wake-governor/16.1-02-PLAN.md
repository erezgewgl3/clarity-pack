---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 02
type: execute
wave: 2
depends_on: ["16.1-01"]
files_modified:
  - src/worker/agents/wake-governor.ts
  - src/worker/agents/agent-task-delivery.ts
  - test/worker/agents/wake-governor.test.mjs
autonomous: true
requirements: [LOOP-02, LOOP-03, LOOP-05]
must_haves:
  truths:
    - "Every op-issue Clarity creates (create branch AND reuse branch) is recorded in the durable provenance table at the moment of creation."
    - "The delivery path no longer contains any requestWakeup call — native heartbeat pull is the only dispatch."
    - "A wake request above CLARITY_WAKE_CEILING_PER_MIN is suppressed and engages the durable kill-switch; an already-engaged switch suppresses all further wakes."
  artifacts:
    - path: "src/worker/agents/wake-governor.ts"
      provides: "checkAndRecordWake throughput governor + kill-switch gate"
      exports: ["checkAndRecordWake"]
    - path: "src/worker/agents/agent-task-delivery.ts"
      provides: "Durable provenance write at op-issue creation; requestWakeup block removed"
      contains: "recordOwnOperationIssue"
  key_links:
    - from: "src/worker/agents/agent-task-delivery.ts"
      to: "src/worker/db/own-operation-issues-repo.ts"
      via: "recordOwnOperationIssue at op-issue create + reuse branch"
      pattern: "recordOwnOperationIssue"
    - from: "src/worker/agents/wake-governor.ts"
      to: "src/worker/db/wake-ledger-repo.ts"
      via: "appendWake + countTrailingWakes + pruneOldWakes"
      pattern: "countTrailingWakes"
    - from: "src/worker/agents/wake-governor.ts"
      to: "src/worker/db/wake-kill-switch-repo.ts"
      via: "isEngaged + engage"
      pattern: "engage"
---

<objective>
Sever the wake from the delivery path and add the throughput governor. Two structural changes: (1) record durable own-operation provenance the moment Clarity creates an op-issue (D-03/D-04), and DELETE the fire-and-forget requestWakeup block entirely (D-05); (2) build wake-governor.ts — a check-before-wake service that bounds wakes by trailing-60s throughput against an env ceiling and engages a durable kill-switch (D-06/D-07/D-08).

Purpose: this is the loop-break + the throughput cap. After this plan, no Clarity write can wake an agent from the delivery path, and any wake (from the legitimate heartbeat/cron pull path) is rate-bounded with a restart-durable kill-switch.
Output: wake-governor.ts, modified agent-task-delivery.ts, governor unit test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-SPEC.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-PATTERNS.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (now exists): -->
<!-- own-operation-issues-repo: recordOwnOperationIssue(ctx,companyId,issueId):Promise<void>; isOwnOperationIssue(ctx,companyId,issueId):Promise<boolean> -->
<!-- wake-ledger-repo: appendWake(ctx,companyId):Promise<void>; countTrailingWakes(ctx,companyId,windowSeconds):Promise<number>; pruneOldWakes(ctx,windowSeconds):Promise<void> -->
<!-- wake-kill-switch-repo: isEngaged(ctx,companyId):Promise<boolean>; engage(ctx,companyId,reason):Promise<void>; clear(ctx,companyId):Promise<void> -->
<!-- circuit-breaker.ts governance precedent: engage once at threshold, never auto-clear; import manifest for version. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build wake-governor.ts (throughput check-before-wake + kill-switch engage)</name>
  <files>src/worker/agents/wake-governor.ts, test/worker/agents/wake-governor.test.mjs</files>
  <read_first>
    - src/worker/agents/circuit-breaker.ts (full — exact analog; engage-once-at-threshold + never-auto-clear + fail-open + version source + narrow Pick ctx; PATTERNS section wake-governor.ts lines 171-205)
    - src/worker/db/wake-ledger-repo.ts + src/worker/db/wake-kill-switch-repo.ts (Plan 01 — the repos this service composes)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 2 (pruning) + section 6 Open Q #4 (dispatcher disposition is decided in Plan 04, not here)
    - CONTEXT.md D-06, D-07 (env CLARITY_WAKE_CEILING_PER_MIN default 6), D-08 (durable kill-switch, manual clear only)
  </read_first>
  <behavior>
    - checkAndRecordWake under ceiling: appends a ledger row, returns true (wake allowed).
    - checkAndRecordWake driven >6 times within 60s: returns false for excess calls AND calls kill-switch engage().
    - When kill-switch isEngaged already true at entry: returns false immediately (no wake), without needing to re-exceed the ceiling.
    - Ceiling is read from env CLARITY_WAKE_CEILING_PER_MIN; absent → default 6.
    - On trip, a log line is emitted (LOOP-03 "observable in logs").
  </behavior>
  <action>
    Create src/worker/agents/wake-governor.ts mirroring circuit-breaker.ts. Define a narrow WakeGovernorCtx Pick-style ctx (db with query+execute, optional logger) — import-friendly for .mjs tests. Export checkAndRecordWake(ctx, companyId): Promise of boolean:
    (1) Read ceiling from process.env.CLARITY_WAKE_CEILING_PER_MIN coerced via Number, default literal 6 (the safe out-of-box value per D-07).
    (2) If isEngaged(ctx, companyId) resolves true → log info "wake suppressed: kill-switch engaged" and return false (restart-durable backstop — already-tripped switch stays tripped, D-08).
    (3) await appendWake(ctx, companyId) then await pruneOldWakes(ctx, 120) (window plus slack; self-drain).
    (4) const rate = await countTrailingWakes(ctx, companyId, 60). If rate > ceiling → await engage(ctx, companyId, reason string with rate+ceiling), log warn, return false (suppress). Else return true.
    Optional in-memory fast counter layered over the durable read (mirror circuit-breaker counters Map) — Discretion; if added, the durable read remains the authoritative restart backstop. Node strip-only TS (no parameter-property shorthand). Do NOT call ctx.agents.* here — this service only gates; the caller (heartbeat/cron in Plan 04) performs the legitimate pull.
    Create test/worker/agents/wake-governor.test.mjs using the fake-ctx idiom: a fake db with in-memory wakeLedger array plus killSwitchEngaged flag driven off SQL regex (same shape as RESEARCH section 3 makeStormCtx). Set/unset process.env.CLARITY_WAKE_CEILING_PER_MIN to assert env-default plus override; exercise the four behaviors above.
  </action>
  <verify>
    <automated>cd "c:\Users\erezg\Documents\Claude\Projects\Clarity Pack" && node --test test/worker/agents/wake-governor.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/worker/agents/wake-governor.test.mjs exits 0.
    - wake-governor.ts exports checkAndRecordWake; reads process.env.CLARITY_WAKE_CEILING_PER_MIN with literal default 6.
    - Test proves: ≤ceiling → true; >ceiling → excess false plus engage() called; pre-engaged → immediate false.
    - grep -c 'ctx\.agents' src/worker/agents/wake-governor.ts returns 0 (the governor gates, it does not wake).
    - grep -c 'private readonly' src/worker/agents/wake-governor.ts returns 0.
    - Maps SPEC Acceptance: "> ceiling wake requests/min → excess suppressed + kill-switch engaged; ceiling from env var with a safe default" (LOOP-03).
  </acceptance_criteria>
  <done>wake-governor.ts exists, gates wakes by trailing-60s throughput against the env ceiling (default 6), engages the durable kill-switch on overflow, short-circuits when already engaged, logs trips, and passes its unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Record durable provenance + delete the requestWakeup block in agent-task-delivery.ts</name>
  <files>src/worker/agents/agent-task-delivery.ts</files>
  <read_first>
    - src/worker/agents/agent-task-delivery.ts:170,419,441,443-463 (the Pick ctx type; op-issue create; in-memory rememberOwnOperationIssue; the fire-and-forget requestWakeup block to delete; PATTERNS section agent-task-delivery lines 242-263)
    - src/worker/db/own-operation-issues-repo.ts (Plan 01 — recordOwnOperationIssue)
    - src/worker/agents/op-issue-set.ts (the in-memory fast-path kept as cache per D-04)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 1 lines 111-123 + L-4 (requestWakeup removal must not break type/tests)
    - test/worker/agents/editor-heartbeat-recursion.test.mjs:82 (provides a requestWakeup stub — confirm no test ASSERTS it was called)
    - CONTEXT.md D-03, D-04, D-05
  </read_first>
  <action>
    In src/worker/agents/agent-task-delivery.ts: import recordOwnOperationIssue from ../db/own-operation-issues-repo.ts. After the issue variable is assigned (so it covers BOTH the create branch at :419 AND the reuse branch), beside the existing in-memory rememberOwnOperationIssue(issue.id) at :441, add await recordOwnOperationIssue(ctx, opts.companyId, issue.id) — the durable authoritative provenance write (D-03/D-04). Keep the in-memory rememberOwnOperationIssue call as a pure fast-path cache (D-04: durable is authoritative, in-memory is non-sole).
    DELETE the entire fire-and-forget requestWakeup block at :443-463 (D-05) — the const wakeIssueId = issue.id; void Promise.resolve().then(() => ctx.issues.requestWakeup(...)).catch(...) stanza. Its own comment already states the next heartbeat picks the op-issue up via native wake.
    Drop requestWakeup from the AgentTaskDeliveryCtx Pick at :170 (no longer referenced). Per L-4, leaving the stub in test fakes is harmless — but confirm via grep that no test asserts requestWakeup WAS called (if one does, flip that assertion to "not called"). Verify the call sites (handleEditorHeartbeat editor.ts:288, compile-bulletin.ts) already pass a ctx carrying db for the new repo write (they do — the breaker uses it).
  </action>
  <verify>
    <automated>cd "c:\Users\erezg\Documents\Claude\Projects\Clarity Pack" && node --test test/worker/agents/editor-heartbeat-recursion.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/worker/agents/editor-heartbeat-recursion.test.mjs exits 0 (existing recursion guard test still green after the change).
    - Comment-stripped src/worker/agents/agent-task-delivery.ts contains ZERO occurrences of requestWakeup (proves D-05; the no-wake-from-ingress static gate in Plan 05 re-asserts this).
    - Comment-stripped agent-task-delivery.ts contains recordOwnOperationIssue (durable provenance write present, D-03).
    - The durable write is positioned to cover both the create and reuse branch (after issue is assigned, beside rememberOwnOperationIssue).
    - Maps SPEC Acceptance: "a Clarity-authored op write injected as an event produces zero downstream wakes" (LOOP-02) and the source-level half of LOOP-01.
  </acceptance_criteria>
  <done>agent-task-delivery.ts records durable provenance at op-issue creation (both branches), contains no requestWakeup call, and the existing recursion test stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| heartbeat/cron pull → wake-governor | The only legitimate wake origin after this phase; must pass the throughput gate |
| Clarity op-issue write → host event bus | Clarity's own writes re-enter the bus; provenance must mark them before they can re-trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-06 | Denial of Service | Unbounded wake amplification (the incident class) | mitigate | wake-governor caps trailing-60s wakes at env ceiling (default 6); excess suppressed; durable kill-switch engages on overflow |
| T-161-07 | Denial of Service | Restart-window guard bypass (in-memory empty onset) | mitigate | Durable recordOwnOperationIssue written at creation; provenance survives restart so a re-entrant own-write is suppressed even with empty in-memory set |
| T-161-08 | Elevation of Privilege | requestWakeup re-introduced into a wake path resurrects the storm | mitigate | requestWakeup block deleted from delivery path; Plan 05 static gate fails build if reintroduced anywhere reachable from ingress |
| T-161-09 | Tampering | Provenance write skipped on the op-issue reuse branch leaves a hole | mitigate | Write placed after issue assignment to cover BOTH create and reuse branches (acceptance asserts position) |
| T-161-10 | Denial of Service | Governor DB error wedges all wakes | accept | isEngaged fails open; a transient error never blocks legitimate dispatch — the ledger count remains the hard cap |
</threat_model>

<verification>
- node --test test/worker/agents/wake-governor.test.mjs test/worker/agents/editor-heartbeat-recursion.test.mjs all green.
- Comment-stripped agent-task-delivery.ts: 0 requestWakeup, ≥1 recordOwnOperationIssue.
- git diff --stat shows only wake-governor.ts (new), agent-task-delivery.ts (modified), wake-governor.test.mjs (new).
</verification>

<success_criteria>
- wake-governor.ts bounds wakes by throughput against the env ceiling with a durable, version-scoped, manual-clear kill-switch.
- agent-task-delivery.ts writes durable provenance at every op-issue creation and contains no requestWakeup.
- Existing recursion test stays green; governor unit test passes.
</success_criteria>

<output>
Create `.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-02-SUMMARY.md` when done.
</output>
