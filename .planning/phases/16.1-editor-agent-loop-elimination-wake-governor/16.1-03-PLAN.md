---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 03
type: execute
wave: 3
depends_on: ["16.1-01", "16.1-02"]
files_modified:
  - src/worker/opt-in-guard.ts
  - src/worker/opted-in-company-set.ts
  - src/worker.ts
  - src/worker/agents/heartbeat-dispatcher.ts
  - test/worker/opted-in-company-set.test.mjs
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-04]
must_haves:
  truths:
    - "An issue/comment event whose companyId is not in the opted-in-company set produces zero host calls and zero enqueue, short-circuiting before any work."
    - "Event handlers are observe-only: they may write a durable dirty-marker and check provenance, but reach no requestWakeup / ctx.agents.* / reconcile / runHeartbeat / handleEditorHeartbeat."
    - "A Clarity-authored op-issue event (in the durable provenance table) is dropped at ingress before any host call, even when the in-memory fast-path set is empty (simulated restart)."
    - "The opted-in-company set is seeded lazily on the first valid invocation (never at boot), maps opted-in users to companies via the durable clarity_agent_owners table (pure plugin-namespace SQL, no host call), refreshed on opt-in-change events and a periodic TTL re-read, with zero per-event DB call."
    - "An opted-in user whose company is recorded in clarity_agent_owners IS present in the opted-in-company set after a lazy seed (the gate never silently seeds empty when a real opted-in mapping exists)."
    - "D-02: event handlers are observe-only — they may write a durable dirty-marker and check provenance, but no requestWakeup / ctx.agents.* / reconcile / runHeartbeat is reachable from any ctx.events.on handler."
    - "D-04: the durable provenance table is checked at ingress before any host call (isOwnOperationIssue), authoritative over the in-memory fast-path set."
    - "D-12: the opted-in-company set is seeded lazily on first invocation (never at boot) and refreshed via opt-in-change invalidation plus a periodic TTL re-read."
    - "D-13: every event handler short-circuits when companyId is out of the opted-in set before any reconcile/get/enqueue, and the same scope gate wraps the company.created and situation cron paths (the bulletin cron is gated in Plan 04)."
  artifacts:
    - path: "src/worker/opted-in-company-set.ts"
      provides: "Lazy-seeded in-memory opted-in-company set with TTL refresh + opt-in-change update; user->company mapping via clarity_agent_owners"
      exports: ["isCompanyOptedIn", "ensureSeeded", "invalidateOptedInCache"]
    - path: "src/worker.ts"
      provides: "Observe-only ingress (dirty-marker only), scope+provenance short-circuit, scoped company.created + chat bridge"
      contains: "isCompanyOptedIn"
  key_links:
    - from: "src/worker.ts"
      to: "src/worker/opted-in-company-set.ts"
      via: "isCompanyOptedIn scope gate at ingress before any host call"
      pattern: "isCompanyOptedIn"
    - from: "src/worker.ts"
      to: "src/worker/db/own-operation-issues-repo.ts"
      via: "isOwnOperationIssue durable provenance check at ingress"
      pattern: "isOwnOperationIssue"
    - from: "src/worker/opted-in-company-set.ts"
      to: "src/worker/opt-in-guard.ts"
      via: "reuses the clarity_user_prefs opted-in SELECT for the seed"
      pattern: "clarity_user_prefs"
    - from: "src/worker/opted-in-company-set.ts"
      to: "src/worker/db/clarity-agent-owners-repo.ts"
      via: "maps opted-in user_id -> company_id via the clarity_agent_owners durable table"
      pattern: "clarity_agent_owners"
---

<objective>
Convert event ingress to observe-only and gate it on opt-in / active-company scope (LOOP-01, LOOP-04), with the durable provenance check as the restart-safe loop-break at ingress (LOOP-02). Resolve the four discretion HOW-gaps the SPEC deferred: lazy per-company seed (D-12 with the clarity_user_prefs no-company-column reality — mapped via the durable clarity_agent_owners table), the company.created + chat-bridge event-site dispositions (L-5), and the heartbeat-dispatcher disposition (Open Q #4).

Purpose: this is the ingress half of the loop fix — after this plan, no event handler does host work for an out-of-scope company and no event handler can reach a wake. Combined with Plan 02's delivery-path severance, the loop is broken by construction.
Output: opted-in-company-set.ts (new), modified opt-in-guard.ts, worker.ts, heartbeat-dispatcher.ts, company-set unit test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-SPEC.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-PATTERNS.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-02-SUMMARY.md

<interfaces>
<!-- From Plan 01/02 (now exist): -->
<!-- own-operation-issues-repo: isOwnOperationIssue(ctx,companyId,issueId):Promise<boolean> -->
<!-- wake-governor: checkAndRecordWake(ctx,companyId):Promise<boolean> (used by the dispatch/cron path) -->
<!-- opt-in-guard.ts isOptedIn(ctx,userId): per-call SELECT opted_in_at FROM clarity_user_prefs WHERE user_id=$1 (REUSE the SQL for the seed) -->
<!-- clarity-agent-owners-repo.ts listClarityAgentOwnersForCompany(ctx,companyId); table clarity_agent_owners (migration 0013) columns (agent_id, owner_user_id, company_id, set_at) — plugin-namespace, pure ctx.db.query, invocation-scope-safe. THIS is the durable user_id -> company_id map that resolves W-2. -->
<!-- worker.ts:452 company.created handler -> reconcileEditorAgent (host round-trip; scope it). worker.ts:536 registerChatStreamBridge (SSE re-emit, no wake; scope for cost parity, must not false-positive static gate). worker.ts:497-507 heartbeatDispatcher wiring (config keys resolveAgentId/runHeartbeat -> handleEditorHeartbeat). NOTE: this `new HeartbeatDispatcher({...})` constructor lives in MODULE scope OUTSIDE any ctx.events.on body — Plan 05 Task 1's static gate is handler-body-scoped (asserts only over each ctx.events.on body), so the dispatcher wiring tokens runHeartbeat/handleEditorHeartbeat here MUST NOT false-positive the gate. Task 2's chosen dispatcher disposition is a hard precondition the Plan 05 gate scope references. -->
<!-- worker.ts:509-529 issue.*/comment.* loop (the ingress to convert). -->
<!-- clarity_user_prefs is user_id-keyed, NO company_id column (L-2). Boot-time host calls dead on 525.0 (L-3) — seed LAZILY inside first valid invocation. The user_id -> company_id mapping is RESOLVED via the durable clarity_agent_owners table (migration 0013, plugin namespace): SELECT DISTINCT company_id FROM ...clarity_agent_owners WHERE owner_user_id = ANY($1) over the opted-in user_id list. Pure ctx.db.query plugin-namespace SQL, NO host call (invocation-scope-safe — same query class the breaker/owner-resolver run in handler scope). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build opted-in-company-set.ts (lazy first-invocation seed + TTL refresh) — resolves D-12 / L-2 / L-3 with the clarity_agent_owners mapping (W-2)</name>
  <files>src/worker/opted-in-company-set.ts, test/worker/opted-in-company-set.test.mjs</files>
  <read_first>
    - src/worker/opt-in-guard.ts:68-81 (the isOptedIn SELECT opted_in_at FROM clarity_user_prefs WHERE user_id=$1 — the seed reuses this; PATTERNS section opt-in-scope-gate lines 271-301)
    - src/worker/db/clarity-agent-owners-repo.ts (the durable user->company map: listClarityAgentOwnersForCompany + the (owner_user_id, company_id) columns of clarity_agent_owners, migration 0013; pure ctx.db.query, invocation-scope-safe)
    - migrations/0013_clarity_agent_owners.sql (confirms columns owner_user_id + company_id, plugin namespace)
    - src/worker/agents/op-issue-set.ts (Node strip-only TS, in-memory set + TTL idiom to mirror)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md L-2 (no company_id column — must map opted-in users to companies), L-3 (boot host calls dead on 525.0; ctx.db.query works in handler scope), section 6 Open Q #1 (lazy first-event seed recommendation), Assumption A3
    - CONTEXT.md D-12, D-13
  </read_first>
  <behavior>
    - With clarity_user_prefs holding an opted-in user U whose company c1 is recorded in clarity_agent_owners (owner_user_id=U, company_id=c1): after ensureSeeded runs in handler scope, isCompanyOptedIn(c1) === true and isCompanyOptedIn(c-other) === false. (W-2 behavioral acceptance — proves the gate does NOT silently seed empty when a real opted-in mapping exists.)
    - With an opted-in user U but NO clarity_agent_owners row for U (no claimed company): ensureSeeded yields an empty set for that user; isCompanyOptedIn(anything) === false. Document this as the known mapping limitation (only operator-claimed ownership links a user to a company in the plugin namespace).
    - With NO opted-in user: ensureSeeded yields an empty set; isCompanyOptedIn(anything) === false (default OFF).
    - ensureSeeded does NOT run at module load / boot — only when first called inside a handler invocation (lazy; L-3).
    - After the TTL window elapses (or invalidateOptedInCache is called on an opt-in-change event), the next ensureSeeded re-reads.
    - Fast path: a repeat isCompanyOptedIn call within the TTL makes zero DB calls (assert the seed query spy fires once).
  </behavior>
  <action>
    Create src/worker/opted-in-company-set.ts. Maintain a module-level in-memory Set of companyId plus a lastSeededAt timestamp and a TTL constant (Discretion — pick a conservative value such as 60000 ms; document it). Export:
    ensureSeeded(ctx): Promise of void — if the set is unseeded OR the TTL has elapsed, run the seed in two pure plugin-namespace ctx.db.query steps (NO host call, invocation-scope-safe per L-3/A3):
      (a) SELECT user_id FROM clarity_user_prefs WHERE opted_in_at IS NOT NULL (reuse opt-in-guard.ts SQL shape) — the opted-in user_id list.
      (b) Map those user_ids to companies via the durable clarity_agent_owners table (migration 0013): SELECT DISTINCT company_id FROM plugin_clarity_pack_cdd6bda4bd.clarity_agent_owners WHERE owner_user_id = ANY($1), passing the opted-in user_id list. This is the CONCRETE W-2 mechanism — a single pure plugin-namespace SELECT, not a host call; it runs at lazy-seed boundary inside the first valid invocation, never at boot. (Reuse the clarity-agent-owners-repo module rather than duplicating the SQL where practical; if a repo helper does not fit the bulk owner_user_id IN-list shape, add a narrow plugin-namespace query in this module following the same fully-qualified-namespace convention.)
    Cache the resolved companyId set plus lastSeededAt.
    isCompanyOptedIn(companyId): boolean — pure in-memory membership test (zero DB call; caller awaits ensureSeeded first).
    invalidateOptedInCache(): void — clears lastSeededAt so the next ensureSeeded re-reads (wired to the opt-in-change event/RPC in Task 2). Fail-closed: on seed query error, leave the set as-is and log; never treat an error as everyone-opted-in.
    Node strip-only TS (no parameter-property shorthand). Create test/worker/opted-in-company-set.test.mjs with a fake-ctx whose db.query returns scripted clarity_user_prefs rows AND scripted clarity_agent_owners rows (keyed off the SQL regex so the two seed queries return their respective rows); assert the behaviors above — explicitly including the W-2 acceptance that an opted-in user with a clarity_agent_owners company IS present in the set after a lazy seed, plus the no-owner-row empty case, the no-opted-in-user empty case, and the seed-query-fires-once fast-path.
  </action>
  <verify>
    <automated>node --test test/worker/opted-in-company-set.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/worker/opted-in-company-set.test.mjs exits 0.
    - opted-in-company-set.ts exports isCompanyOptedIn, ensureSeeded, invalidateOptedInCache.
    - The seed maps opted-in user_ids to companies via a pure plugin-namespace SELECT against clarity_agent_owners (grep confirms the module references clarity_agent_owners / the owners repo, and makes NO host call — no ctx.companies / ctx.agents / ctx.http in the seed path).
    - Test proves (W-2): an opted-in user whose company is recorded in clarity_agent_owners IS present in the set after a lazy seed (NOT silently empty); an opted-in user with no owners row yields empty (documented limitation); no opted-in user means empty set (default OFF); repeat membership test within TTL means zero additional DB calls; invalidate forces re-read.
    - The seed query is reachable ONLY from ensureSeeded (handler-scope-callable), never at module top-level (no top-level await of the seed) — grep confirms no module-level seed invocation.
    - grep count of 'private readonly' in src/worker/opted-in-company-set.ts is 0.
    - Maps SPEC Acceptance: "with opt-in OFF, a burst of events produces zero host calls and zero wakes" — the membership primitive (LOOP-04). Resolves open-question #1 (lazy first-event company seed) with the concrete clarity_agent_owners SQL mapping.
  </acceptance_criteria>
  <done>opted-in-company-set.ts lazily seeds the opted-in-company set on first invocation (never boot), maps opted-in users to companies via the durable clarity_agent_owners plugin-namespace table (pure SQL, no host call), refreshes on TTL plus invalidate, answers membership with zero per-event DB call, fails closed, and passes its unit test including the W-2 non-empty-seed assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Convert worker.ts ingress to observe-only + scope/provenance short-circuit; scope company.created + chat bridge; disposition the dispatcher — resolves L-5 / Open Q #4</name>
  <files>src/worker.ts, src/worker/opt-in-guard.ts, src/worker/agents/heartbeat-dispatcher.ts</files>
  <read_first>
    - src/worker.ts:452 (company.created -> reconcileEditorAgent), :497-507 (heartbeatDispatcher wiring — MODULE scope, outside any ctx.events.on body), :509-529 (issue.*/comment.* ingress loop), :536 (registerChatStreamBridge); PATTERNS section worker.ts:509 lines 209-238
    - src/worker/opted-in-company-set.ts (Task 1 — isCompanyOptedIn, ensureSeeded, invalidateOptedInCache)
    - src/worker/db/own-operation-issues-repo.ts (Plan 01 — isOwnOperationIssue ingress guard)
    - src/worker/agents/heartbeat-dispatcher.ts:146-156 (the enqueue guards; the dirty-marker write slots here OR in the worker.ts handler before enqueue)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 1 lines 105-140 + L-5 (company.created + chat bridge dispositions) + Open Q #4 (dispatcher survives or removed)
    - src/worker/opt-in-guard.ts (where the set-opt-in RPC lives — wire invalidateOptedInCache there)
    - CONTEXT.md D-02 (observe-only), D-04 (durable check at ingress before host call), D-13 (every handler short-circuits before reconcile/get/enqueue; same gate wraps crons)
  </read_first>
  <action>
    In src/worker.ts, convert the issue.created/issue.updated/issue.comment.created handler (:509-529) to OBSERVE-ONLY. The handler stays async. Order of short-circuits, ALL before any host call (D-04/D-13):
    (1) await ensureSeeded(ctx); if companyId not opted in via isCompanyOptedIn(event.companyId) then return — scope gate, zero host work for out-of-scope companies (LOOP-04).
    (2) if await isOwnOperationIssue(ctx, event.companyId, event.entityId) is true then return — durable provenance check; the restart-safe loop-break (LOOP-02). The in-memory isRememberedOwnOperationIssue may run first as a fast-path, but the durable read is authoritative (D-04).
    (3) Surviving events write ONLY a durable dirty-marker (no enqueue-to-wake). DECIDE THE DISPATCHER DISPOSITION (Open Q #4) and state it in the SUMMARY: either (a) keep HeartbeatDispatcher as the agent's own pull-side batching (fed by the dirty-marker queue, NOT event-triggered to a wake), or (b) reduce the event handler to a pure dirty-marker write and let the agent's native heartbeat pull the op-issues. EITHER WAY the enqueue from ctx.events.on MUST NOT lead to runHeartbeat/handleEditorHeartbeat/requestWakeup/ctx.agents.* — sever that wiring (the :497-507 runHeartbeat path must not be reachable from the event handler). THE CHOSEN DISPOSITION IS A HARD PRECONDITION OF THE PLAN 05 TASK 1 STATIC GATE: whichever disposition is picked, the HeartbeatDispatcher constructor at :497-507 stays in MODULE scope (outside every ctx.events.on body), so the handler-body-scoped gate does not match its runHeartbeat/handleEditorHeartbeat config keys. Record the disposition in the SUMMARY so Plan 05's gate-scope precondition is satisfiable.
    The dirty-marker can be a lightweight namespace write or reuse an existing marker mechanism; it carries no wake.
    Scope the OTHER two event sites (L-5): (4) the company.created handler (:452) — wrap reconcileEditorAgent so it only runs for opted-in companies (await ensureSeeded; isCompanyOptedIn(event.companyId) gate) — reconcile is not a wake but it is unscoped host work; give it the explicit scoped disposition. (5) registerChatStreamBridge (:536) — add the isCompanyOptedIn scope short-circuit for cost parity; it does SSE re-emit, NO wake — ensure it is not within the issue.*/comment.* ingress handler bodies the static gate scans, and that its SSE publish does not match the forbidden wake tokens (it must not false-positive the Plan 05 static gate).
    Wire invalidateOptedInCache() into the set-opt-in RPC path in opt-in-guard.ts (or wherever the opt-in toggle is handled) so an opt-in change refreshes the set (D-12). If a heartbeat-dispatcher edit is needed for the disposition, make it in heartbeat-dispatcher.ts (the dirty-marker enqueue rewire). Node strip-only TS for any touched module imported by .mjs tests.
  </action>
  <verify>
    <automated>node --test test/worker/agents/editor-heartbeat-recursion.test.mjs test/worker/self-loop-filter.test.mjs test/worker/opted-in-company-set.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test on the three tests above exits 0 (existing recursion + self-loop-filter behavior preserved; new company-set test green).
    - Comment-stripped src/worker.ts: the issue.*/comment.* handler body contains isCompanyOptedIn AND isOwnOperationIssue, and contains ZERO of requestWakeup, requestWakeups, .pause(, .resume(, managed.reconcile(, runHeartbeat, handleEditorHeartbeat (the Plan 05 static gate enforces this over the handler body only; assert it here too over the handler body).
    - The HeartbeatDispatcher constructor (:497-507, runHeartbeat/handleEditorHeartbeat config keys) remains in module scope OUTSIDE every ctx.events.on body — confirmed so Plan 05 Task 1's handler-body-scoped gate does not false-positive on it.
    - The company.created handler at :452 is wrapped by an isCompanyOptedIn scope check before reconcileEditorAgent (L-5 disposition #1 explicit).
    - registerChatStreamBridge has an isCompanyOptedIn short-circuit (L-5 disposition #2 explicit) and does not contain any forbidden wake token (no static-gate false positive).
    - invalidateOptedInCache is referenced from the set-opt-in path (opt-in change refreshes the set).
    - SUMMARY states the dispatcher disposition chosen (Open Q #4 resolved) AND confirms the dispatcher wiring stays outside handler bodies (Plan 05 gate precondition).
    - Maps SPEC Acceptance: LOOP-01 (no wake reachable from ingress, source half) + LOOP-04 (opt-in OFF -> zero host calls/wakes) + LOOP-02 (durable provenance check at ingress).
  </acceptance_criteria>
  <done>worker.ts ingress is observe-only with a scope gate and durable provenance short-circuit before any host call; the wake wiring is severed from event handlers; the dispatcher constructor stays in module scope (Plan 05 gate precondition); company.created and the chat bridge are company-scoped with explicit dispositions; the dispatcher disposition is decided and recorded; opt-in changes invalidate the set; the named tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host event bus -> worker event handler | Untrusted instance-wide events (any company) cross here; must be scoped + provenance-checked before any work |
| opt-in toggle -> opted-in-company set | Per-user opt-in state drives the ingress scope; default OFF must throttle the worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-11 | Denial of Service | Opt-in scope bypass at ingress (work done for non-opted-in companies) | mitigate | isCompanyOptedIn gate runs FIRST, before any host call; default OFF (empty set) means zero work; fail-closed on seed error |
| T-161-12 | Denial of Service | Restart-window guard bypass via empty in-memory set | mitigate | Durable isOwnOperationIssue read at ingress is authoritative; in-memory set is fast-path only — re-entrant own writes dropped even after restart |
| T-161-13 | Elevation of Privilege | A wake reachable from an event handler resurrects the loop | mitigate | Observe-only conversion severs runHeartbeat/requestWakeup wiring from ingress; dispatcher constructor kept in module scope; Plan 05 handler-body static gate fails build on reintroduction inside a handler |
| T-161-14 | Denial of Service | company.created reconcile / chat bridge do unscoped host work across the instance | mitigate | Both wrapped with isCompanyOptedIn scope short-circuit (L-5 explicit dispositions) |
| T-161-15 | Spoofing | Boot-time host call rejected on 525.0 leaves the set unseeded -> fails open | mitigate | Lazy first-invocation seed (never boot, L-3) via pure plugin-namespace SQL (clarity_user_prefs + clarity_agent_owners, no host call); default empty set is fail-CLOSED (no work), not fail-open |
</threat_model>

<verification>
- node --test on the recursion, self-loop-filter, and opted-in-company-set tests all green.
- Comment-stripped worker.ts ingress handler body: present isCompanyOptedIn + isOwnOperationIssue; absent all forbidden wake tokens. Dispatcher constructor confirmed outside handler bodies.
- git diff --stat touches only opt-in-guard.ts, opted-in-company-set.ts (new), worker.ts, heartbeat-dispatcher.ts, the new test.
</verification>

<success_criteria>
- Ingress is observe-only and scoped: out-of-scope companies and Clarity's own op-issues are dropped before any host call.
- The four deferred HOW-gaps are resolved in code: lazy company seed (mapped via clarity_agent_owners), company.created scope, chat-bridge scope, dispatcher disposition.
- No wake is reachable from any event handler; opt-in changes refresh the set; named tests stay green.
</success_criteria>

<output>
Create `.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
