---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 04
type: execute
wave: 4
depends_on: ["16.1-02", "16.1-03"]
files_modified:
  - src/worker/situation/awaiting-you-selector.ts
  - src/worker/agents/editor.ts
  - src/worker/jobs/compile-bulletin.ts
  - test/worker/situation/awaiting-you-selector.test.mjs
autonomous: true
requirements: [LOOP-03, LOOP-04]
must_haves:
  truths:
    - "The heartbeat warms at most N=5 (env-tunable) SWR-stale awaiting-you TL;DR rows per opted-in company; fresh cache hits are skipped."
    - "The bounded warm and the bulletin cron both pass through the opt-in scope gate AND the wake-governor before any compile/wake — default OFF and the kill-switch both throttle them."
    - "Staleness for the bounded warm is checked against the per-row tldrs cache (tldr-cache.ts), not situation_snapshots."
  artifacts:
    - path: "src/worker/situation/awaiting-you-selector.ts"
      provides: "Pure selector of awaiting-you issue ids per company for the bounded warm"
      exports: ["selectAwaitingYouIssueIds"]
    - path: "src/worker/agents/editor.ts"
      provides: "Bounded warm-on-heartbeat hook (<=N stale rows) gated by scope + governor"
      contains: "selectAwaitingYouIssueIds"
  key_links:
    - from: "src/worker/agents/editor.ts"
      to: "src/worker/db/tldr-cache.ts"
      via: "SWR-staleness check against the per-row tldrs cache"
      pattern: "tldr"
    - from: "src/worker/agents/editor.ts"
      to: "src/worker/agents/wake-governor.ts"
      via: "checkAndRecordWake before each warm compile"
      pattern: "checkAndRecordWake"
    - from: "src/worker/jobs/compile-bulletin.ts"
      to: "src/worker/opted-in-company-set.ts"
      via: "isCompanyOptedIn scope gate wrapping the cron"
      pattern: "isCompanyOptedIn"
---

<objective>
Preserve scheduled proactivity under the new pull model: a bounded warm-on-heartbeat that compiles only awaiting-you TL;DRs (D-10), capped at N=5 SWR-stale rows per opted-in company (D-11), and ensure both the warm and the daily bulletin cron flow through the opt-in scope gate (LOOP-04 / D-13) and the wake-governor (LOOP-03). Resolve the SWR-staleness-source HOW-gap: the per-row TL;DR cache (tldr-cache.ts), NOT situation_snapshots (A2).

Purpose: the value boundary keeps scheduled proactivity (bulletin + bounded warm) alive — but now PULL-based, SCOPED, SWR-served, and governed. This is the "what the user gains is a usable instance" half — proactive compile is preserved without the storm.
Output: awaiting-you-selector.ts (new), modified editor.ts heartbeat hook, scoped compile-bulletin.ts, selector unit test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-SPEC.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-PATTERNS.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-03-SUMMARY.md

<interfaces>
<!-- From Plan 02/03 (now exist): -->
<!-- wake-governor: checkAndRecordWake(ctx,companyId):Promise<boolean> — false means suppress -->
<!-- opted-in-company-set: ensureSeeded(ctx):Promise<void>; isCompanyOptedIn(companyId):boolean -->
<!-- snapshot-cache.ts: readLatestSnapshot returns { payload, takenAt } | null; staleness is caller-computed (Date.now()-Date.parse(takenAt)) — A2 caveat: that is company-granular, NOT per-row -->
<!-- tldr-cache.ts: the per-row tldrs cache — the CORRECT SWR-staleness source for the bounded warm (A2 resolution) -->
<!-- build-employees-rollup.ts buildNeedsYou/partitionNeedsYouRows: SituationEmployeeRow carries blockerChain.terminalKind/awaitedUserId/needsYou — the awaiting-you source -->
<!-- editor.ts:288 handleEditorHeartbeat → startAgentTask (the legitimate pull path; warm hook lives in the heartbeat) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build awaiting-you-selector.ts (pure selector of awaiting-you issue ids per company)</name>
  <files>src/worker/situation/awaiting-you-selector.ts, test/worker/situation/awaiting-you-selector.test.mjs</files>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts:752,798-828 (buildNeedsYou / partitionNeedsYouRows; SituationEmployeeRow blockerChain.terminalKind/awaitedUserId/needsYou metadata; PATTERNS section bounded-warm lines 305-337)
    - src/worker/handlers/situation-room.ts (READ-ONLY no-touch boundary — read for the rollup shape only; do NOT edit, LOOP-06)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 1 lines 171-174 (expose a pure selector the heartbeat can call) + A2 (staleness source)
    - CONTEXT.md D-10, D-11
  </read_first>
  <behavior>
    - selectAwaitingYouIssueIds(rows) returns only the issue ids of rows whose blockerChain marks them awaiting-you (terminalKind AWAITING_HUMAN / needsYou true), in stable order.
    - Rows not awaiting-you are excluded.
    - Empty rollup -> empty array (no throw).
    - The selector is pure (no ctx, no DB, no host call) so it is trivially unit-testable.
  </behavior>
  <action>
    Create src/worker/situation/awaiting-you-selector.ts. Export a PURE function selectAwaitingYouIssueIds(rows: SituationEmployeeRow[]): string[] that filters the rollup rows to the awaiting-you set using the viewer-invariant blockerChain metadata already carried on each row (terminalKind/awaitedUserId/needsYou — mirror the partition logic in build-employees-rollup.ts without duplicating its private internals; import its row type). No DB, no ctx, no host call — the heartbeat (Task 2) supplies the rows and does the staleness/governor work. Node strip-only TS. Create test/worker/situation/awaiting-you-selector.test.mjs with hand-built SituationEmployeeRow fixtures asserting the four behaviors above.
  </action>
  <verify>
    <automated>node --test test/worker/situation/awaiting-you-selector.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/worker/situation/awaiting-you-selector.test.mjs exits 0.
    - awaiting-you-selector.ts exports selectAwaitingYouIssueIds; the function takes rows and returns string[] with no ctx parameter (pure).
    - Test proves: awaiting-you rows included, others excluded, empty input gives empty output.
    - grep count of 'ctx.' in src/worker/situation/awaiting-you-selector.ts is 0 (pure, no host/DB access).
    - Maps SPEC: the awaiting-you source for the bounded warm (D-10).
  </acceptance_criteria>
  <done>awaiting-you-selector.ts exposes a pure selector returning awaiting-you issue ids from a rollup, with no DB/host coupling, and passes its unit test.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the bounded warm into the heartbeat (<=N SWR-stale rows, scope + governor gated) and scope the bulletin cron — resolves A2</name>
  <files>src/worker/agents/editor.ts, src/worker/jobs/compile-bulletin.ts</files>
  <read_first>
    - src/worker/agents/editor.ts:288 (handleEditorHeartbeat -> startAgentTask; the legitimate pull path where the warm hook lives)
    - src/worker/db/tldr-cache.ts (the per-row tldrs cache — the SWR-staleness source per A2; read its read API and takenAt/generated_at field)
    - src/worker/situation/awaiting-you-selector.ts (Task 1 — selectAwaitingYouIssueIds)
    - src/worker/agents/wake-governor.ts (Plan 02 — checkAndRecordWake)
    - src/worker/opted-in-company-set.ts (Plan 03 — ensureSeeded, isCompanyOptedIn)
    - src/worker/jobs/compile-bulletin.ts (the daily bulletin cron — wrap with the same scope gate + governor; PATTERNS section opt-in-scope-gate lines 289-301; cron pattern RESEARCH section 1 line 123)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md A2 (SWR-stale checked against tldr-cache.ts, NOT situation_snapshots) + section 1 lines 162-174
    - CONTEXT.md D-10, D-11, D-13
  </read_first>
  <action>
    In src/worker/agents/editor.ts (the heartbeat path), add a bounded warm step. For each opted-in company (await ensureSeeded(ctx); iterate isCompanyOptedIn-true companies): obtain the awaiting-you issue ids via selectAwaitingYouIssueIds over the rollup the heartbeat already has access to; for each candidate, check SWR-staleness AGAINST THE PER-ROW tldrs CACHE (tldr-cache.ts) — A2 resolution: read the row's cached TL;DR generated_at/takenAt and treat it stale when Date.now() minus that timestamp exceeds a freshness window; SKIP fresh cache hits (D-11). Take at most N stale rows where N = Number(process.env.CLARITY_WARM_MAX_ROWS or default 5), env-tunable (D-11). Before each warm compile that would create an op-issue / wake, call checkAndRecordWake(ctx, companyId) and SKIP the warm if it returns false (governor + kill-switch gate, LOOP-03). The warm creates op-issues for the agent to pull — it does NOT requestWakeup (D-05 already removed it).
    In src/worker/jobs/compile-bulletin.ts, wrap the daily bulletin cron body with the same gate: await ensureSeeded(ctx); skip companies where isCompanyOptedIn is false (LOOP-04 / D-13 — "same scope gating wraps the bulletin cron, not only event ingress"), and pass its op-issue-creating/wake step through checkAndRecordWake. Keep the bulletin CADENCE unchanged (out of scope — only its loop-safety/scope-gating is in scope). Node strip-only TS for touched modules imported by .mjs tests.
  </action>
  <verify>
    <automated>node --test test/worker/situation/awaiting-you-selector.test.mjs test/worker/agents/wake-governor.test.mjs test/worker/opted-in-company-set.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test on the three tests above exits 0 (selector + governor + company-set behavior intact after wiring).
    - editor.ts heartbeat references selectAwaitingYouIssueIds, the tldr-cache staleness read, and checkAndRecordWake before the warm compile; the warm cap reads process.env.CLARITY_WARM_MAX_ROWS with literal default 5.
    - The staleness check reads the tldrs cache (tldr-cache.ts), NOT situation_snapshots (A2 resolved) — grep confirms the warm path imports from db/tldr-cache, not situation/snapshot-cache, for the staleness source.
    - compile-bulletin.ts cron body references isCompanyOptedIn (scope gate) and checkAndRecordWake (governor) before its compile/wake; the bulletin cron cadence (cron string) is unchanged.
    - Maps SPEC Acceptance: bounded warm preserved under pull model (Boundaries "bounded warm-on-heartbeat") + LOOP-04 cron scope gating + LOOP-03 governor on the cron/warm path. Resolves open-question #4 (bounded-warm SWR staleness source) and #5 partially (dispatcher batching — see Plan 03 SUMMARY).
  </acceptance_criteria>
  <done>The heartbeat warms at most N SWR-stale awaiting-you TL;DRs per opted-in company (staleness against tldr-cache), gated by scope + governor; the bulletin cron is scope-gated + governed with its cadence unchanged; named tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| heartbeat/cron pull -> proactive compile | The legitimate proactive-compile origin; must be scoped + governed so it cannot become an unbounded wake source |
| tldrs cache -> warm decision | Staleness drives whether a row is warmed; an always-stale read would re-introduce churn — bounded by N + governor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-16 | Denial of Service | Bounded warm becomes an unbounded compile loop | mitigate | Hard cap N=5 stale rows/heartbeat (env-tunable); only SWR-stale rows warmed; checkAndRecordWake gates each warm against the ceiling/kill-switch |
| T-161-17 | Denial of Service | Bulletin cron runs instance-wide regardless of opt-in | mitigate | Cron body wrapped with isCompanyOptedIn scope gate + checkAndRecordWake (D-13) — default OFF throttles the cron, not just the UI |
| T-161-18 | Tampering | Wrong staleness source (situation_snapshots) warms every row every heartbeat | mitigate | Staleness checked against per-row tldrs cache (A2) — fresh per-row hits skipped, preventing whole-company re-warm churn |
| T-161-19 | Denial of Service | Kill-switch engaged but warm still compiles | mitigate | checkAndRecordWake returns false when engaged -> warm skipped; degrades only proactive TL;DR freshness, read path untouched (LOOP-06) |
</threat_model>

<verification>
- node --test on selector + governor + company-set tests all green.
- editor.ts warm path: present checkAndRecordWake + tldr-cache staleness + CLARITY_WARM_MAX_ROWS default 5; compile-bulletin.ts: present isCompanyOptedIn + checkAndRecordWake, cron string unchanged.
- git diff --stat touches only awaiting-you-selector.ts (new), editor.ts, compile-bulletin.ts, the new test.
</verification>

<success_criteria>
- Scheduled proactivity (bounded warm + bulletin cron) is preserved under the pull model, scoped to opted-in companies and governed by the throughput cap/kill-switch.
- The bounded warm is capped at N SWR-stale awaiting-you rows with staleness against the correct (tldrs) cache.
- The bulletin cadence is unchanged; named tests stay green.
</success_criteria>

<output>
Create `.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-04-SUMMARY.md` when done.
</output>
