---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 05
type: tdd
wave: 5
depends_on: ["16.1-02", "16.1-03", "16.1-04"]
files_modified:
  - test/loop/no-wake-from-ingress.test.mjs
  - test/loop/storm-safety.test.mjs
  - test/loop/opt-in-ingress-gate.test.mjs
autonomous: true
requirements: [LOOP-01, LOOP-02, LOOP-05, LOOP-06]
must_haves:
  truths:
    - "A static source-grep gate fails the build if any wake token (requestWakeup/ctx.agents.*/runHeartbeat/handleEditorHeartbeat) is reachable from a ctx.events.on handler, or if requestWakeup reappears in the delivery path."
    - "A storm-safety test feeds >=100 mixed events incl. own op-issues + agent result writes, clears the in-memory fast-path mid-burst (simulated restart), and asserts zero synchronous wakes from ingress, zero self-trigger recursion, total pull-wakes <= governor ceiling, and kill-switch engaged on overflow."
    - "An opt-in-OFF burst produces zero host calls and zero wakes."
    - "The read-time resolve-refs / blocker-chain-flatten / deliverable-preview tests stay green and a diff review confirms zero logic change to those files (LOOP-06)."
  artifacts:
    - path: "test/loop/no-wake-from-ingress.test.mjs"
      provides: "LOOP-01 static source-grep gate + behavioral zero-wake assertion"
    - path: "test/loop/storm-safety.test.mjs"
      provides: "LOOP-02/03/05 burst + simulated restart + ceiling + kill-switch"
    - path: "test/loop/opt-in-ingress-gate.test.mjs"
      provides: "LOOP-04 opt-in-OFF zero-host-call / zero-wake assertion"
  key_links:
    - from: "test/loop/no-wake-from-ingress.test.mjs"
      to: "src/worker.ts"
      via: "comment-stripped source-grep for forbidden wake tokens in event-handler region"
      pattern: "requestWakeup"
    - from: "test/loop/storm-safety.test.mjs"
      to: "src/worker/agents/wake-governor.ts"
      via: "drives >ceiling pull-wakes through checkAndRecordWake"
      pattern: "checkAndRecordWake"
---

<objective>
Make the loop fix falsifiable. Add the three CI tests that prove the corrected build is storm-safe and fail if the loop is reintroduced (LOOP-01, LOOP-02, LOOP-04, LOOP-05), and lock the read-time no-touch boundary as a regression guard (LOOP-06).

Purpose: these tests are the contract that lets Clarity be reinstalled on BEAAA. The static gate fails the build the instant a wake reappears in an event handler; the storm harness reproduces the exact 2026-06-04 incident conditions and proves boundedness; the no-touch guard proves the value (zero rabbit-holes) is untouched.
Output: three test/loop/*.test.mjs files + the LOOP-06 no-touch diff-review verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-SPEC.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-PATTERNS.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-03-SUMMARY.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-04-SUMMARY.md

<interfaces>
<!-- The ingress/governor/provenance surface to exercise (now exists, Plans 02-04): -->
<!-- worker.ts observe-only ingress handler; wake-governor checkAndRecordWake; own-operation-issues-repo isOwnOperationIssue/recordOwnOperationIssue; opted-in-company-set isCompanyOptedIn/ensureSeeded -->
<!-- Test idioms: ctt07 comment-stripping source-grep gate; editor-heartbeat-recursion.test.mjs makeCtx fake-ctx + capture arrays + createCalls.length===0 -->
<!-- Storm harness sketch: RESEARCH section 3 makeStormCtx (in-memory provenance Set + wakeLedger array + killSwitchEngaged flag driven off SQL regex) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: no-wake-from-ingress.test.mjs (LOOP-01 static source-grep gate + behavioral backstop)</name>
  <files>test/loop/no-wake-from-ingress.test.mjs</files>
  <read_first>
    - test/ctt07/agent-take-ownership-no-issue-update.test.mjs (exact analog — comment-stripping source-grep gate, lines 31-50; PATTERNS section no-wake-from-ingress lines 343-370)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 4 (the wake surface to forbid + two-layer approach; recommend layer 1 hard gate + behavioral backstop)
    - src/worker.ts (the ingress region to scan) + src/worker/agents/agent-task-delivery.ts (must contain no requestWakeup)
    - SPEC LOOP-01 acceptance line 45
  </read_first>
  <behavior>
    - Static: comment-stripped src/worker.ts contains ZERO of requestWakeup, requestWakeups, .pause(, .resume(, managed.reconcile(, runHeartbeat, handleEditorHeartbeat anywhere reachable from the ctx.events.on registration region (scan the event-handler region, or whole-file if handlers are short).
    - Static: comment-stripped src/worker/agents/agent-task-delivery.ts contains ZERO requestWakeup (proves D-05).
    - Static must NOT false-positive on registerChatStreamBridge's SSE publish (confirm the chat bridge contains no forbidden token).
    - Behavioral: feeding a burst of issue/comment events through the observe-only ingress emits zero synchronous agent wakes (wakeCalls.length === 0).
  </behavior>
  <action>
    Create test/loop/no-wake-from-ingress.test.mjs, forking the ctt07 comment-stripping source-grep idiom verbatim: read src/worker.ts, strip line and block comments, assert zero matches for each forbidden wake token reachable from the event-handler region. Add a second assertion that comment-stripped src/worker/agents/agent-task-delivery.ts has zero requestWakeup. Add a behavioral backstop: build a fake ctx (editor-heartbeat-recursion.test.mjs makeCtx style) with a wakeCalls capture array, feed a small burst of issue/comment events through the observe-only ingress entrypoint, and assert wakeCalls.length === 0. Ensure the gate scopes correctly so registerChatStreamBridge (SSE re-emit) does not trip it (the chat bridge is observe-only, no wake token).
  </action>
  <verify>
    <automated>node --test test/loop/no-wake-from-ingress.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/loop/no-wake-from-ingress.test.mjs exits 0 on the corrected build.
    - The test FAILS if requestWakeup is re-added to a worker.ts event handler or to agent-task-delivery.ts (verify by a scratch reintroduction during dev, then revert).
    - Behavioral assertion wakeCalls.length === 0 holds for an ingress burst.
    - Maps SPEC Acceptance: "Static check: no requestWakeup/ctx.agents.* wake/dispatch reachable from any ctx.events.on handler" + "a burst of issue/comment events emits zero synchronous agent wakes from ingress" (LOOP-01).
  </acceptance_criteria>
  <done>no-wake-from-ingress.test.mjs statically forbids all wake tokens in event handlers + the delivery path and behaviorally proves zero ingress wakes; passes on the corrected build and fails on reintroduction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: storm-safety.test.mjs + opt-in-ingress-gate.test.mjs (LOOP-02/03/04/05)</name>
  <files>test/loop/storm-safety.test.mjs, test/loop/opt-in-ingress-gate.test.mjs</files>
  <read_first>
    - test/worker/agents/editor-heartbeat-recursion.test.mjs (full — fake-ctx makeCtx, capture arrays, createCalls.length===0 recursion proof; PATTERNS section storm-safety lines 374-410 + opt-in-ingress-gate lines 414-418)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 3 (makeStormCtx sketch + the five locked assertions, lines 261-340)
    - src/worker/agents/wake-governor.ts + src/worker/db/own-operation-issues-repo.ts + src/worker/opted-in-company-set.ts (the surfaces under test)
    - SPEC LOOP-02/03/05 acceptance lines 50,55,65; CONTEXT.md D-04 (durable backstop), D-06/D-07/D-08 (ceiling 6, kill-switch)
  </read_first>
  <behavior>
    - Storm: feed >=100 mixed events (real user issues + the plugin's own op-issue creates in provenance + agent result writes actorType agent). Assert wakeCalls.length === 0 from pure ingress (LOOP-01 behavioral).
    - Storm: clear the IN-MEMORY fast-path mid-burst while the fake-db durable provenance Set persists; re-entrant own-op / agent-result events produce ZERO new op-issue creates (createCalls for own-op events === 0) — restart-durable loop-break (LOOP-02).
    - Storm: drive >6 pull-wakes/min through checkAndRecordWake; assert total wakeCalls <= 6 (ceiling) AND killSwitchEngaged === true (LOOP-03). Then simulate restart (clear in-memory) and assert the kill-switch still reads engaged from the durable fake-db row.
    - Opt-in gate: with the opted-in-company set EMPTY (opt-in OFF), feed a burst of issue/comment events; assert zero host calls (issues.get/list/create, agents.*, db.query/execute spies all length 0 beyond the scope check) AND zero wakes.
  </behavior>
  <action>
    Create test/loop/storm-safety.test.mjs following the RESEARCH section 3 makeStormCtx sketch: a fake ctx whose db.query/execute simulate the three durable tables with in-memory structures (provenance Set, wakeLedger array, killSwitchEngaged flag) keyed off SQL regex; capture arrays for wakeCalls and opIssueCreates. Implement the burst + mid-burst in-memory clear (simulated restart) + the four behaviors above with node:assert/strict. Lock the five decisive assertions from RESEARCH section 3: zero ingress wakes, restart-durable zero recursion, ceiling cap (max 6), kill-switch engaged, kill-switch survives restart read.
    Create test/loop/opt-in-ingress-gate.test.mjs (analog editor-heartbeat-recursion makeCtx): spy on every host call into capture arrays; with the opted-in-company set empty, feed an issue/comment burst and assert zero host calls AND zero wakes from ingress (LOOP-04). Both tests live under test/loop/ so the glob node --test of test/loop runs the whole phase suite.
  </action>
  <verify>
    <automated>node --test test/loop/storm-safety.test.mjs test/loop/opt-in-ingress-gate.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - node --test test/loop/storm-safety.test.mjs test/loop/opt-in-ingress-gate.test.mjs exits 0.
    - storm-safety asserts: wakeCalls.length === 0 from ingress; zero recursion creates after mid-burst in-memory clear; total pull-wakes max 6; killSwitchEngaged === true; kill-switch reads engaged after a second simulated restart.
    - opt-in-ingress-gate asserts: with empty opted-in set, host-call spies and wakeCalls are all length 0.
    - The storm test fails if the governor ceiling is removed or requestWakeup is reachable from ingress (verify by scratch reintroduction then revert).
    - Maps SPEC Acceptance: the storm-safety CI test line (LOOP-02/05) + governor line (LOOP-03) + opt-in gate line (LOOP-04).
  </acceptance_criteria>
  <done>storm-safety.test.mjs reproduces the incident conditions across a simulated restart and proves bounded wakes + zero recursion + durable kill-switch; opt-in-ingress-gate.test.mjs proves opt-in-OFF does zero host work; both pass and fail on reintroduction.</done>
</task>

<task type="auto">
  <name>Task 3: LOOP-06 read-time no-touch regression guard (existing tests green + zero-diff review)</name>
  <files>(verification task — runs existing tests + git diff review; no new files)</files>
  <read_first>
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md section 5 (the exact no-touch source file list + the regression tests that must stay green, lines 365-383)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-PATTERNS.md section Read-Time No-Touch Boundary lines 464-466
    - SPEC LOOP-06 acceptance line 70
  </read_first>
  <action>
    This is a verification task, not new code. Run the full suite (pnpm test) which includes the read-time regression tests that MUST stay green (RESEARCH section 5): resolve-refs.test.mjs, resolve-refs-hide-operation.test.mjs, reference-resolver.test.mjs, flatten-blocker-chain-parity.test.mjs, flatten-blocker-chain-scrub.test.mjs, blocker-chain.test.mjs, deliverable-preview.test.mjs (worker + ui), issue-reader.test.mjs, reader-view.test.mjs.
    Then perform the zero-diff review: run git diff --stat across the phase branch and confirm ZERO edits to the no-touch source files (the hard no-edit list, RESEARCH section 5): src/worker/handlers/resolve-refs.ts, flatten-blocker-chain.ts, deliverable-preview.ts, issue-reader.ts, reader-ac-autostatus.ts, ac-checklist.ts; situation-room.ts READ logic (its fire-and-forget recompute must not have gained a wake, but the read logic is untouched); src/shared/reference-resolver.* and the blocker-chain shared modules. Record the diff-review result (the list of no-touch files confirmed unchanged) in the SUMMARY. If ANY no-touch file shows a logic edit, the phase fails LOOP-06 and the edit must be reverted/relocated.
  </action>
  <verify>
    <automated>pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - pnpm test exits 0 — the full suite green, including all read-time regression tests named in RESEARCH section 5.
    - git diff --stat shows ZERO edits to every file in the no-touch list (recorded verbatim in the SUMMARY).
    - The situation-room.ts fire-and-forget recompute path gained no wake token (cross-checked against the Plan 05 Task 1 static gate region if applicable).
    - Maps SPEC Acceptance: "resolve-refs / blocker-chain-flatten / deliverable-preview tests stay green; no logic diff in those paths" (LOOP-06).
  </acceptance_criteria>
  <done>The full suite is green including every read-time regression test, and a git diff --stat review confirms zero edits to the no-touch source list — LOOP-06 is regression-proofed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/CI -> shipped artifact | The static gate + storm test are the CI guard that a storm-prone build cannot ship/reinstall |
| event-handler source region -> wake surface | The static gate enforces this boundary at the source-text level (survives alias refactors) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-20 | Tampering | A future edit re-introduces requestWakeup into an event handler (the recurring storm class) | mitigate | no-wake-from-ingress static gate fails CI on any forbidden wake token in the event-handler region or the delivery path |
| T-161-21 | Denial of Service | A regression removes the governor ceiling and re-enables unbounded wakes | mitigate | storm-safety test asserts wakes max ceiling + kill-switch engaged; fails CI if the cap is removed |
| T-161-22 | Tampering | A change silently breaks the read-time value (zero rabbit-holes) while fixing the loop | mitigate | LOOP-06 guard: existing resolve/blocker/preview tests must stay green + zero-diff review of the no-touch source list |
| T-161-23 | Repudiation | Restart leaves a tripped kill-switch unenforced (the original onset condition) | mitigate | storm-safety asserts the kill-switch reads engaged after a simulated restart (durable, not in-memory) |
</threat_model>

<verification>
- node --test test/loop/no-wake-from-ingress.test.mjs test/loop/storm-safety.test.mjs test/loop/opt-in-ingress-gate.test.mjs all green.
- pnpm test green (full suite incl. read-time regression tests).
- git diff --stat confirms zero edits to the no-touch source list (recorded in SUMMARY).
</verification>

<success_criteria>
- The three test/loop tests pass on the corrected build and fail on loop reintroduction (static + storm + opt-in).
- The full suite is green; the read-time no-touch boundary is confirmed unchanged by diff review.
- All of LOOP-01, LOOP-02, LOOP-04, LOOP-05 have a falsifiable automated proof; LOOP-06 has a green-tests + zero-diff guard.
</success_criteria>

<output>
Create `.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-05-SUMMARY.md` when done.
</output>
