---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 06
type: execute
wave: 6
depends_on: ["16.1-01", "16.1-02", "16.1-03", "16.1-04", "16.1-05"]
files_modified:
  - package.json
  - src/manifest.ts
autonomous: false
requirements: [LOOP-07]
user_setup:
  - service: digitalocean
    why: "Bookend the live BEAAA reinstall with a verified DO snapshot (rollback path) per the bookended-by-snapshots rule"
    dashboard_config:
      - task: "Take a DO droplet snapshot of the AriClaw droplet immediately before reinstall and record its id"
        location: "DigitalOcean dashboard -> AriClaw droplet -> Snapshots"
  - service: beaaa-ssh
    why: "Reinstall the corrected build on the live BEAAA Paperclip instance and run the no-storm drill"
    dashboard_config:
      - task: "Open a local SSH window to AriClaw (ssh ariclaw -> root; Paperclip runs as beai-agent via npx from ~), not a cloud session"
        location: "Local terminal with the AriClaw SSH key"
must_haves:
  truths:
    - "package.json version AND src/manifest.ts version are bumped in lockstep above 1.4.5, and the built dist/manifest.js carries the new version."
    - "A verified DO snapshot id is recorded before the live reinstall (rollback bookend)."
    - "After reinstall on BEAAA, a single issue/agent change produces no 4-5-agent cascade, the Clarity worker stays near-idle (approx 0% CPU), wakes stay within the governor ceiling in logs, and the Situation Room loads with no 502."
  artifacts:
    - path: "package.json"
      provides: "Bumped plugin version (two-source bump, source 1)"
      contains: "version"
    - path: "src/manifest.ts"
      provides: "Bumped manifest version literal (two-source bump, source 2 — host reads dist/manifest.js)"
      contains: "version:"
  key_links:
    - from: "src/manifest.ts"
      to: "src/worker/db/wake-kill-switch-repo.ts"
      via: "manifest.version drives kill-switch version-scoping — must match the shipped build"
      pattern: "version"
---

<objective>
Ship and prove. Bump the plugin version in BOTH sources (package.json + src/manifest.ts — the host reads dist/manifest.js, and the kill-switch version-scoping reads manifest.version, so a mismatch is silently wrong), rebuild, run the full suite green, then reinstall the corrected build on the live BEAAA instance bookended by a verified DO snapshot and run the no-storm drill (LOOP-07).

Purpose: this is the make-safe verification — Clarity was uninstalled from BEAAA because the prior build stormed. This plan proves the corrected build is safe to run on the live box.
Output: bumped version (both sources), a clean rebuild, a recorded DO snapshot id, and a passing live no-storm drill.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-SPEC.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-RESEARCH.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-PATTERNS.md
@.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-05-SUMMARY.md

<interfaces>
<!-- MEMORY (query MemPalace clarity_pack/runbook before VPS commands): -->
<!-- BEAAA deploy mechanics: AriClaw DO droplet; SSH ariclaw -> root; Paperclip runs as beai-agent via `npx -y paperclipai` from ~ (NOT /root/paperclip, NOT ~/paperclip). NO safety-CLI on BEAAA — the bookend is the DO snapshot. Path A scp+here-string vs fail2ban -> Path B (push master + flip repo public). Local-tarball install: extract-dir not tgz; uninstall-then-install. -->
<!-- Two-source version bump: package.json:3 + src/manifest.ts:626 (currently 1.4.5 / '1.4.5'). circuit-breaker.ts:38 + new kill-switch read manifest.version. -->
<!-- Governor env: CLARITY_WAKE_CEILING_PER_MIN default 6; CLARITY_WARM_MAX_ROWS default 5. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-source version bump + clean rebuild + full suite green</name>
  <files>package.json, src/manifest.ts</files>
  <read_first>
    - package.json:3 (version, currently 1.4.5) + src/manifest.ts:626 (version: '1.4.5')
    - MEMORY plugin-version-bump-two-sources (host reads dist/manifest.js; both sources must match) + L-7 (kill-switch version-scoping reads manifest.version)
    - SPEC Constraints "Two-source version bump"
  </read_first>
  <action>
    Bump the plugin version in BOTH package.json "version" and src/manifest.ts version: literal in lockstep (choose the next version above 1.4.5 — e.g. 1.4.6 for this make-safe patch, or 1.5.0 if the milestone scope warrants; pick one and apply identically to both files). The two values MUST be byte-identical. Run pnpm build (build:worker + build:ui + build:manifest) and confirm dist/manifest.js carries the new version (built from src/manifest.ts). Run pnpm typecheck and pnpm test (full suite incl. the new test/loop tests + migration validators + read-time regression). Do NOT run any live/VPS command in this task — this is local build/verify only.
  </action>
  <verify>
    <automated>pnpm build && pnpm typecheck && pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - pnpm build + pnpm typecheck + pnpm test all exit 0.
    - package.json version and the src/manifest.ts version: literal are byte-identical and both greater than 1.4.5.
    - dist/manifest.js (built) contains the new version string (the host reads this artifact).
    - The full suite includes and passes test/loop/no-wake-from-ingress, storm-safety, opt-in-ingress-gate, the three repo tests, wake-governor, opted-in-company-set, awaiting-you-selector, and the migration validators.
    - Maps SPEC Constraints: two-source version bump + rebuild.
  </acceptance_criteria>
  <done>Both version sources are bumped in lockstep, the build is clean, dist/manifest.js carries the new version, and the full suite is green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Bookend DO snapshot + live BEAAA reinstall + no-storm drill (LOOP-07)</name>
  <files>(live BEAAA box — no repo files; deploy artifact = the built dist + the local-tarball install)</files>
  <what-built>
    The corrected, version-bumped, fully-tested clarity-pack build (Task 1). It eliminates the event-amplification loop by construction (observe-only ingress + durable provenance), bounds wakes with a durable throughput governor + kill-switch (default 6/min), and gates ingress + crons on opt-in scope — while leaving the read-time zero-rabbit-holes path untouched.
  </what-built>
  <read_first>
    - MEMORY clarity_pack/runbook + beaaa-deploy-mechanics (query MemPalace BEFORE any VPS command per CLAUDE.md): AriClaw SSH, beai-agent npx-from-~, NO safety-CLI on box (bookend = DO snapshot), Path A vs Path B install, local-tarball extract-dir gotcha, uninstall-then-install
    - MEMORY incident-editor-loop-storm-phase-16.1 (the storm signature to confirm is gone: ~3.8 self-heartbeats/sec, 4-5 agent cascade per change, host CPU spike)
    - .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-SPEC.md LOOP-07 acceptance line 75 + Constraints "Bookended-by-snapshots"
    - autonomous-deploy-authorization MEMORY (Eric runs fully autonomous; deploy/drill need a LOCAL window, not cloud)
  </read_first>
  <action>
    Operator-driven live deploy + drill (not an autonomous repo edit). Claude AUTOMATES every step it can via the local SSH/CLI window (trigger/confirm the bookend snapshot, uninstall-then-install the corrected build, set env, observe the drill via worker logs + top/htop) and pauses ONLY for the truly human gates: DO dashboard snapshot confirmation, browser opt-in for one company, and the visual Situation-Room/Reader check. Execute the how-to-verify sequence exactly, querying MemPalace clarity_pack/runbook FIRST for the current canonical commands (do not extrapolate VPS commands from spec).
  </action>
  <how-to-verify>
    Operator (Eric) runs, from a LOCAL SSH window, querying MemPalace clarity_pack/runbook first for the exact current commands:
    1. PRE-BOOKEND: take a DO droplet snapshot of AriClaw and RECORD the snapshot id. Confirm a rehearsed rollback path (uninstall corrected build -> reinstall prior 1.4.5, or restore the snapshot) is understood before proceeding. (NO safety-CLI on BEAAA — the DO snapshot IS the bookend.)
    2. REINSTALL: deploy the corrected build per the recorded mechanics (uninstall-then-install; local-tarball extract-dir; as beai-agent via npx from ~). Set CLARITY_WAKE_CEILING_PER_MIN if tuning from the default 6 (default is safe out of the box).
    3. DRILL: opt in for one company; create or change a single issue, and dispatch one agent task. Observe for several minutes:
       - The Clarity worker process stays near-idle (approx 0% CPU) — confirm via top/htop on the box.
       - A single change does NOT spawn a 4-5-agent wake cascade (the incident signature is gone).
       - Agent wakes stay within the governor ceiling — confirm in the worker logs (the governor logs trips/suppressions).
       - The Situation Room (/<companyPrefix>/situation-room) loads with NO 502.
       - Reader view still resolves references inline and the blocker chain still flattens (LOOP-06 value intact on the live box).
    4. RECORD: the DO snapshot id, the deployed version, and the drill observations.
    If the drill shows ANY storm signature (CPU spike, multi-agent cascade, wakes exceeding ceiling), roll back via the rehearsed path and report — do NOT leave a storming build live.
  </how-to-verify>
  <acceptance_criteria>
    - A DO snapshot id is recorded BEFORE reinstall (bookend present).
    - The corrected, version-bumped build is installed on BEAAA (plugin list shows the new version).
    - A single issue/agent change produces NO 4-5-agent cascade; Clarity worker stays approx 0% CPU; wakes within the ceiling in logs; Situation Room loads with no 502.
    - Reader inline ref resolution + blocker-chain flatten still work live (LOOP-06).
    - All observations + the snapshot id are recorded in the SUMMARY.
    - Maps SPEC Acceptance: the LIVE line (LOOP-07) verbatim.
  </acceptance_criteria>
  <resume-signal>Type "drill passed" with the recorded DO snapshot id + deployed version, or describe the storm signature observed and the rollback taken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build -> live BEAAA production box | A misbehaving plugin can hit Paperclip APIs directly (same-origin trust); the reinstall must be bookended and reversible |
| operator -> production filesystem/DB | Single-tenant filesystem-persistent host; the DO snapshot is the only rollback bookend (no safety-CLI on BEAAA) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-161-24 | Denial of Service | Reinstalled build still storms and bricks the live box | mitigate | DO snapshot bookend taken BEFORE reinstall + rehearsed rollback; the drill confirms approx 0% CPU + no cascade before declaring pass; storm signature -> immediate rollback |
| T-161-25 | Tampering | Version mismatch ships v_new code with a v_old label (kill-switch version-scoped wrong) | mitigate | Two-source lockstep bump (acceptance asserts byte-identical); dist/manifest.js confirmed to carry the new version |
| T-161-26 | Repudiation | No recorded rollback point if the reinstall goes wrong | mitigate | DO snapshot id recorded in the SUMMARY before reinstall (bookended-by-snapshots rule) |
| T-161-27 | Denial of Service | Governor ceiling mis-set on the live box re-enables a storm | accept | Default 6/min is safe out of the box; CLARITY_WAKE_CEILING_PER_MIN only tunes upward by operator choice — documented |
</threat_model>

<verification>
- pnpm build + pnpm typecheck + pnpm test green; both version sources byte-identical and bumped; dist/manifest.js carries the new version.
- DO snapshot id recorded before reinstall; live drill observations recorded (CPU near-idle, no cascade, wakes within ceiling, no 502, Reader/blocker-chain intact).
</verification>

<success_criteria>
- The corrected build ships with a lockstep two-source version bump and a clean full-suite-green rebuild.
- The live BEAAA reinstall is bookended by a recorded DO snapshot and a rehearsed rollback.
- The live no-storm drill passes: no cascade, near-idle worker, wakes within ceiling, no 502, read-time value intact.
</success_criteria>

<output>
Create `.planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-06-SUMMARY.md` when done.
</output>
