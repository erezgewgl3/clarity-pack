---
phase: 16.1-editor-agent-loop-elimination-wake-governor
plan: 07
subsystem: editor-agent-dispatch
tags: [loop-07, wake-governor, write-dispatch, gap-closure, version-bump]
requires:
  - "16.1-02 wake-governor (checkAndRecordWake) shipped"
  - "16.1-01 wake-ledger + wake-kill-switch repos"
  - "16.1-03/05 observe-only provenance-gated ingress (storm severance)"
provides:
  - "Single GOVERNED requestWakeup at op-issue creation (write-capable normal_model dispatch restored)"
  - "Degrade-safe suppression (kill-switch/ceiling -> op-issue still created)"
  - "v1.5.1 (two-source) ready for bookended BEAAA reinstall + live LOOP-07 re-drill"
affects:
  - "Editor-Agent TL;DR/narrative/bulletin persistence (the plain-English layer)"
tech-stack:
  added: []
  patterns:
    - "Governed creation-time wake: checkAndRecordWake gates a single ctx.issues.requestWakeup, non-fatal try/catch, degrade-safe on suppress"
key-files:
  created:
    - test/loop/governed-wake.test.mjs
  modified:
    - src/worker/agents/agent-task-delivery.ts
    - test/worker/agents/agent-task-delivery.test.mjs
    - test/loop/no-wake-from-ingress.test.mjs
    - src/manifest.ts
    - package.json
    - test/worker/bulletin/compile-bulletin-end-to-end.test.mjs
    - test/worker/bulletin/compile-bulletin-host-faithful.test.mjs
decisions:
  - "Re-introduce the wake GOVERNED (not raw) at op-issue creation in startAgentTask, never in ingress — supersedes D-05's over-correction"
  - "Re-point the whole-file D-05 'zero requestWakeup' assertion to 'exactly one governed requestWakeup' (count call sites, require checkAndRecordWake precedes); leave the region-scoped ctx.events.on-body gate untouched"
  - "Re-point two pre-existing D-05 e2e wake assertions (compile-bulletin-{end-to-end,host-faithful}) as a Rule-1 deviation — Task 1 legitimately superseded them"
metrics:
  duration: ~30m
  completed: 2026-06-09
  tasks: 3
  files: 8
  commits: 3
---

# Phase 16.1 Plan 07: LOOP-07 Gap Closure — Governed Wake at Op-Issue Creation Summary

Re-introduced a single GOVERNED `requestWakeup` at op-issue creation in `startAgentTask` (gated by the already-shipped `checkAndRecordWake`), restoring write-capable `normal_model` dispatch so the Editor-Agent's TL;DRs/narratives persist — closing the LOOP-07 gap where D-05 deleted the wake entirely and stranded op-issues on Paperclip's status_only recovery sweep — without reopening the 2026-06-04 storm.

## What Was Built

**Task 1 — Governed creation-time wake** (`b16516f`)
- Added `'requestWakeup'` back to the `AgentTaskDeliveryCtx.issues` Pick union.
- Imported `checkAndRecordWake` from `./wake-governor.ts` (call-only; the governor was not edited).
- In `startAgentTask`, immediately after `recordOwnOperationIssue` and before `return`: `const allowed = await checkAndRecordWake(ctx, opts.companyId)`. On `true`, exactly one `ctx.issues.requestWakeup(issue.id, opts.companyId, { reason: 'clarity-pack:operation:' + opts.operationKind, idempotencyKey: opts.operationId })` inside a try/catch that logs a warn and does NOT rethrow (non-fatal). On `false`, logs an info line and does nothing (op-issue already created — degrade-safe).
- Replaced the D-05 removal comment with a LOOP-07 comment documenting the governed, creation-only wake and the intact storm-severance.
- Verified: `tsc --noEmit` clean; comment-stripped delivery file has EXACTLY ONE `requestWakeup(` call site, reached only inside the `if (allowed)` branch after `checkAndRecordWake`.

**Task 2 — Tests + re-pointed severance assertion** (`cd6b6c3`)
- New `test/loop/governed-wake.test.mjs` (modeled on `storm-safety.test.mjs`'s `makeStormCtx`, driving the REAL `startAgentTask` + governor + `engage`/`isEngaged` repos): governed-wake-fires-once (1 wake + 1 ledger row), kill-switch-degrade-still-creates (op-issue created, 0 wakes), over-ceiling-degrade (10 creates -> wakes capped at 6, kill-switch engaged, every op-issue created).
- Re-pointed the whole-file D-05 assertion in `no-wake-from-ingress.test.mjs` (~:155) from "ZERO requestWakeup" to "EXACTLY ONE governed requestWakeup" — counts `requestWakeup(` call sites (=== 1, ignoring the type-position string literal in the Pick) and asserts `checkAndRecordWake(` precedes it. The region-scoped `ctx.events.on`-body gate (~:108-133) was left UNTOUCHED.
- Re-pointed the flipped assertions in `agent-task-delivery.test.mjs`: happy-path + create-shape tests now expect exactly one governed wake; the old D-05 "ZERO wakes" test split into governed-wake-fires-once, non-fatal-throw, and governor-suppress definite expectations.

**Task 3 — Two-source version bump + clean rebuild** (`e28a524`)
- `package.json` and `src/manifest.ts` 1.5.0 -> 1.5.1 in lockstep.
- Clean rebuild: `dist/worker.js`, `dist/ui/index.js`, and `dist/manifest.js` all carry 1.5.1 (host reads `dist/manifest.js`; kill-switch version-scope reads `manifest.version`).
- Full suite green except the 7 documented pre-existing traceability rows; `tsc --noEmit` clean.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Re-pointed two pre-existing D-05 e2e wake assertions outside `files_modified`**
- **Found during:** Task 3 (full-suite run)
- **Issue:** Task 1's legitimate re-introduction of the governed wake broke two pre-existing tests that asserted the now-superseded D-05 "zero requestWakeup" contract: `test/worker/bulletin/compile-bulletin-end-to-end.test.mjs:501` and `test/worker/bulletin/compile-bulletin-host-faithful.test.mjs:172`. These are the same flip the plan instructed re-pointing for `agent-task-delivery.test.mjs`, but they live in two files not listed in `files_modified`.
- **Fix:** Re-pointed both `wakeups.length === 0` assertions to `=== 1` ("exactly one GOVERNED requestWakeup fired at op-issue creation"), with LOOP-07 rationale comments. The plan's "only 6 files in the diff" constraint conflicts with its "zero NEW failures" constraint; the latter wins — leaving a red suite asserting a deleted contract is incorrect.
- **Files modified:** test/worker/bulletin/compile-bulletin-end-to-end.test.mjs, test/worker/bulletin/compile-bulletin-host-faithful.test.mjs
- **Commit:** e28a524

The final phase diff therefore lists 8 files (the 6 planned + these 2 deviation re-points), all test-assertion-only beyond the planned source change.

## Verification Results

- `npx tsc --noEmit` — exits 0 (Task 1, Task 3).
- Comment-stripped `agent-task-delivery.ts` — EXACTLY ONE `requestWakeup(` call site, governor-gated (`checkAndRecordWake(` precedes it).
- `node --test test/loop/governed-wake.test.mjs` — 3/3 green.
- `node --test test/loop/storm-safety.test.mjs test/loop/opt-in-ingress-gate.test.mjs` — unchanged green (storm severance preserved).
- `node --test test/loop/no-wake-from-ingress.test.mjs` — green: region-scoped handler-body gate UNTOUCHED + re-pointed whole-file assertion passes.
- `node --test test/worker/agents/agent-task-delivery.test.mjs` — 20/20 green with re-pointed governed-wake assertions.
- Both version sources read 1.5.1; `dist/manifest.js` carries 1.5.1; no stale 1.5.0 literal.
- Full suite: 2711 tests, 2702 pass, **7 fail** — exactly the documented pre-existing CHAT-01..11 / CTT-01..08 REQUIREMENTS.md traceability rows. ZERO new failures.
- No edit to `src/worker.ts`, any `ctx.events.on` handler, or any read-time handler (LOOP-06 no-touch preserved).

## Threat Surface

No new security surface beyond the plan's `<threat_model>`. The re-introduced wake is the bounded, governed, creation-time wake the threat register (T-161-25..28) anticipated and mitigated; the recursion edge (event-ingress -> wake) remains severed. No package installs (T-161-SC satisfied). No threat flags.

## Next

v1.5.1 is ready for the bookended BEAAA reinstall (DO snapshot bookend) + live LOOP-07 re-drill: confirm the Editor-Agent's TL;DRs now PERSIST (surfaces show plain-English summaries, not raw task numbers) AND the storm stays absent (worker CPU ambient, zero event-driven wakes).

## Self-Check: PASSED

- FOUND: .planning/phases/16.1-editor-agent-loop-elimination-wake-governor/16.1-07-SUMMARY.md
- FOUND: test/loop/governed-wake.test.mjs
- FOUND: src/worker/agents/agent-task-delivery.ts
- FOUND commit: b16516f (Task 1)
- FOUND commit: cd6b6c3 (Task 2)
- FOUND commit: e28a524 (Task 3)
