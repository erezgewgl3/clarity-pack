---
status: verified
phase: 16.1-editor-agent-loop-elimination-wake-governor
verified: 2026-06-10
method: live LOOP-07 re-drill on BEAAA (v1.5.1) + host source/log forensics; original v1.5.0 storm drill 2026-06-09
requirements_passed: [LOOP-01, LOOP-02, LOOP-03, LOOP-04, LOOP-05, LOOP-06, LOOP-07]
requirements_gap: []
---

# Phase 16.1 Verification — storm fix PASSES; LOOP-07 gap CLOSED & live-verified (v1.5.1)

## RE-DRILL UPDATE — LOOP-07 CLOSED, live PASS on BEAAA v1.5.1 (2026-06-10)

The LOOP-07 gap below was closed by plan 16.1-07 (governed `requestWakeup` at op-issue
creation, gated by `checkAndRecordWake`) and shipped as **v1.5.1**. Bookended live re-drill
on BEAAA (DO daily-backup bookend + local `clarity-pack-1.5.0.tgz` rollback artifact;
uninstall-then-install Path A; UUID `a763176a` preserved):

- **Deploy:** `status=ready version=1.5.1`; worker online, **cpu=0%**, eventSubscriptions:5, jobs:1.
- **TL;DR PERSISTENCE (the gap):** triggered on-demand compiles for BEAAA-143 + BEAAA-260.
  Each created **exactly one** Editor-Agent op-issue (assignee `ca0edebc`), dispatched
  **write-capable**, and wrote its `compile-result` document with **HTTP 201** —
  **2 successful 201 writes, ZERO 403/422 rejections** in the post-restart window.
  `issue.reader` then served the TL;DR as `tldrStatus:"cached"` with real plain-English
  body (e.g. BEAAA-143: *"The Top-10 IFA outreach pack is ready; you decide whether you'll
  personally own the top 3 — Aidoc, Gloat, Hyro…"*), `compiled_by_agent_id:clarity-pack-editor-agent`,
  `generated_at` = drill time. **Not raw task numbers, not stuck "compiling".** The
  recovery-sweep `status_only` write-block (the root cause) is gone.
- **STORM STAYS SEVERED:** only the 2 deliberately-triggered op-issues were created
  (no cascade), **0** wake-suppressions / kill-switch engagements (under the governor
  ceiling), worker CPU sampled 0% across the window, `situation.snapshot` handler → HTTP 200
  (no 502). The historical 403s in the log are all from **prior days** (the bug period).

Net: every LOOP-07 must-have is satisfied live. The plain-English layer is functional on
BEAAA. Phase 16.1 goal achieved. (Original gap analysis preserved below for the record.)

---

# (ORIGINAL 2026-06-09) Phase 16.1 Verification — storm fix PASSES, drill surfaced a write-dispatch gap

## Summary

Plans 16.1-01..05 implemented and committed; 16.1-06 Task 1 (two-source version
bump to **1.5.0** + clean rebuild + full suite green) committed and **deployed live
to BEAAA** (uninstall-then-install, UUID `a763176a` preserved, migration 0017 applied).
DO snapshot bookend recorded: **`AriClaw-1780833542088`**.

The LOOP-07 live drill **passes on storm-safety** but surfaced a **gap** that blocks
the phase goal: the loop fix broke the Editor-Agent's write-capable dispatch.

## What PASSED (storm-safety — the make-safe goal)

- A live issue comment + agent activity produced **no cascade**: clarity worker held
  **0.0–0.3% CPU**, host load stayed ambient (~2.5–3.2, unchanged), **zero**
  event-driven wakes, zero recursion. The 2026-06-04 storm signature is gone.
- Observe-only ingress + durable provenance + opt-in scope gate confirmed live.
- LOOP-06 read-time value intact: inline ref resolution renders titles; blocker chain
  flattens (the Reader loads, refs resolve).

## In-drill fix already shipped (commit 8b333d9)

`opted-in-company-set.ts` used a bare `= ANY($1)` with a raw JS array param; the host
db bridge serializes that lossily (1-element array → scalar `local-board`), so the
opt-in scope-gate seed query failed live (fail-closed). Fixed to bind a Postgres
array-literal via `$1::text[]` (toPgTextArrayLiteral, the v0.6.5 Bug 2 pattern) +
regression test pinning the param shape. (Build/test/redeploy done; 0 failed queries after.)

## GAP (LOOP-07 not fully met) — Editor-Agent write dispatch broken by the loop fix

**Symptom:** Reader TL;DRs show the agent's status text ("cheap/recovery profile —
harness blocks document writes, resumeRequiresNormalModel: true") instead of a
summary. The Editor-Agent computes CORRECT plain-English TL;DRs (verified: e.g.
"The PII-scrubbing scanner activation is fully approved — you do not need to act")
but can never persist them. The whole plain-English layer (TL;DRs + narratives) is
non-functional live → surfaces fall back to raw task numbers.

**Root cause (hard-verified, host source + live log):**
- Paperclip's normal, write-capable dispatch path is `issue-assignment-wakeup`,
  triggered by `requestWakeup`. 16.1 Plan 16.1-02 (D-05) DELETED the `requestWakeup`
  call from `agent-task-delivery.ts` (the storm source) and assumed the Editor-Agent's
  native heartbeat would pull op-issues.
- It does not. Op-issue assignments sit undispatched and fall to Paperclip's
  **periodic recovery sweep**, which dispatches them via
  `recoveryAssigneeAdapterOverrides("status_only")` → `modelProfile:cheap`,
  `allowDocumentUpdates:false`, `resumeRequiresNormalModel:true`.
- `routes/issues.js` (`assertDeliverableMutationAllowedByRunContext` /
  `isStatusOnlyCheapRecoveryContext`) then **hard-rejects** any document/deliverable
  write from such runs (403/422). → write-block.
- Evidence: 8 `assignmentDispatched:1, continuationRequeued:0` recovery events across
  04:52–10:43, each a different op-issue UUID; latest = the f584657c TL;DR compile
  op-issue `e2d549b7` at 10:43:48. TL;DRs wrote fine on 2026-06-04 under the old build
  that still had `requestWakeup` (`result DOCUMENT received`). Nothing dispatched
  normally since the 1.5.0 reinstall — only recovery (status_only).
- NOT the model (gpt-5.4/mini both fine), NOT the cheap-model toggle (flipping it off
  did not stop the block — 53 blocks after), NOT clarity's compile logic.

## Fix direction (for gap-closure planning)

Re-introduce a **single GOVERNED `requestWakeup`** at op-issue CREATION in
`agent-task-delivery.ts` (right after `recordOwnOperationIssue`, ctx.issues.create) —
**not** from event ingress. Gate it through `checkAndRecordWake` (the wake-governor
16.1 already shipped).

- Restores prompt **normal_model** (write-capable) dispatch → TL;DRs persist.
- Storm-safe by construction: the storm mechanism (event-ingress → wake → re-entry)
  stays severed — ingress is observe-only + provenance-gated (LOOP-01/02), so the
  agent's resulting writes re-enter ingress and are dropped at the provenance gate.
  The wake now fires only at deliberate, bounded op-issue creation (bounded warm
  ≤5/company + on-demand), governed by the trailing-60s ceiling + kill-switch.
- Degrade-safe: if the kill-switch is engaged (wake suppressed), still create the
  op-issue — the recovery sweep eventually runs it (status_only), i.e. no worse than today.

**Tests:** existing `test/loop/storm-safety.test.mjs` must still pass (burst exercises
the governor). Add: op-issue creation triggers exactly one governed wake (not zero,
not unbounded); kill-switch-engaged path still creates the op-issue without a wake.

**16.1 design note:** D-05 over-corrected — it deleted the wake entirely instead of
governing it. The governor (16.1-02) was built precisely to make a bounded wake safe;
the gap plan uses it.

## Gaps

- gap: Editor-Agent op-issue dispatch falls to status_only recovery (write-blocked);
  TL;DR/narrative layer non-functional live. fix: single governed requestWakeup at
  op-issue creation. status: failed
