---
phase: 17-structured-human-wait-truthful-verdicts-centerpiece
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0018_structured_human_wait.sql
  - src/worker/db/clarity-human-wait-repo.ts
  - src/shared/blocker-chain.ts
  - src/worker/situation/founder-resolution.ts
  - test/worker/structured-human-wait-verdict.test.mjs
autonomous: true
requirements: [WAIT-01, WAIT-02, WAIT-03]
must_haves:
  truths:
    - "A structured human-wait can be persisted additively in the plugin namespace, one live row per (company, issue)."
    - "The deterministic engine, fed a structuredWaitOwnerUserId on a node's nodeMeta, classifies that node AWAITING_HUMAN."
    - "A structured wait WINS over a nominal agent assignee (D-07) and over a native status==='awaiting' row."
    - "blocker-chain.ts stays AI-free — the determinism + AI-token grep guards still pass."
    - "The company's primary human (founder) is resolvable generically from clarity_agent_owners with zero name/prefix literals."
  artifacts:
    - path: "migrations/0018_structured_human_wait.sql"
      provides: "Additive plugin-namespace clarity_human_waits table"
      contains: "CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.clarity_human_waits"
    - path: "src/worker/db/clarity-human-wait-repo.ts"
      provides: "upsert / list-for-company / delete repo over clarity_human_waits"
      exports: ["upsertClarityHumanWait", "listClarityHumanWaitsForCompany", "deleteClarityHumanWait"]
    - path: "src/shared/blocker-chain.ts"
      provides: "structuredWaitOwnerUserId/structuredWaitOneLiner nodeMeta fields + the priority-0 leaf branch"
      contains: "structuredWaitOwnerUserId"
    - path: "src/worker/situation/founder-resolution.ts"
      provides: "instance-agnostic founder resolution via clarity_agent_owners"
      exports: ["resolveFounderUserId"]
  key_links:
    - from: "src/shared/blocker-chain.ts"
      to: "nodeMeta.structuredWaitOwnerUserId"
      via: "leaf-cascade branch returning AWAITING_HUMAN before the status==='awaiting' branch"
      pattern: "structuredWaitOwnerUserId != null"
    - from: "src/worker/db/clarity-human-wait-repo.ts"
      to: "plugin_clarity_pack_cdd6bda4bd.clarity_human_waits"
      via: "ctx.db.execute INSERT ... ON CONFLICT (company_id, issue_id) DO UPDATE"
      pattern: "ON CONFLICT \\(company_id, issue_id\\) DO UPDATE"
---

<objective>
Lay the Phase 17 foundation: the additive persistence layer, the founder resolver, and the single surgical engine change that turns a persisted structured-wait row into an AWAITING_HUMAN verdict.

Purpose: Everything downstream (the SC5 write-site merge in 17-02, the Editor-Agent populator in 17-03, the full matrix in 17-05) consumes these contracts. Defining them first prevents a scavenger hunt and pins the engine purity guard before any consumer exists.
Output: migration 0018, clarity-human-wait-repo, the two optional nodeMeta fields + the priority-0 leaf branch in blocker-chain.ts, a generic founder resolver, and an engine-level verdict test proving D-07/D-08.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-CONTEXT.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-RESEARCH.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-PATTERNS.md

<interfaces>
<!-- Contracts this plan CREATES that 17-02/17-03/17-05 consume. Extracted from 17-RESEARCH.md + 17-PATTERNS.md + verified source. -->

Engine nodeMeta value type gains two OPTIONAL fields (src/shared/blocker-chain.ts:25-39):
  structuredWaitOwnerUserId?: string | null;   // D-07 — founder id when a structured wait exists for this node
  structuredWaitOneLiner?: string | null;       // D-05 — polished decision one-liner for the label

Repo row type (src/worker/db/clarity-human-wait-repo.ts):
  export type ClarityHumanWaitRow = {
    company_id: string; issue_id: string; owner_user_id: string;
    decision_one_liner: string; content_hash: string; generated_at: string;
    compiled_by_agent_id: string; source_revisions: string[];
  };
  export type ClarityHumanWaitRepoCtx = { db: PluginDatabaseClient };
  upsertClarityHumanWait(ctx, row): Promise<void>
  listClarityHumanWaitsForCompany(ctx, companyId): Promise<ClarityHumanWaitRow[]>
  deleteClarityHumanWait(ctx, companyId, issueId): Promise<void>

Founder resolver (src/worker/situation/founder-resolution.ts):
  resolveFounderUserId(ctx, companyId): Promise<string | null>   // null = no owner claimed → degrade-safe skip

Existing repo to mirror EXACTLY for binding/idempotency: src/worker/db/action-cards-repo.ts (read_first).
Existing owner list to REUSE for founder resolution: listClarityAgentOwnersForCompany (clarity-agent-owners-repo.ts:84-94).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0018 + clarity-human-wait-repo</name>
  <files>migrations/0018_structured_human_wait.sql, src/worker/db/clarity-human-wait-repo.ts</files>
  <read_first>
    - migrations/0015_action_cards.sql (the exact additive-table analog — copy its header validator-constraints block + table structure discipline verbatim)
    - migrations/0013_clarity_agent_owners.sql (validator constraints reference per 17-RESEARCH Area 3)
    - src/worker/db/action-cards-repo.ts (the 1:1 repo template — ctx shape, ctx.db.execute/query split, toPgTextArrayLiteral + $N::text[] binding)
    - src/worker/db/clarity-agent-owners-repo.ts:84-94 (the ON CONFLICT DO UPDATE upsert + list-for-company SELECT shapes)
    - 17-PATTERNS.md (the migration + repo excerpts, lines 35-151)
  </read_first>
  <action>
    Create migrations/0018_structured_human_wait.sql mirroring 0015 EXACTLY: copy the header validator-constraints block; emit ONE `CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.clarity_human_waits` with columns id (bigserial PK), company_id, issue_id, owner_user_id, decision_one_liner, content_hash, generated_at (timestamptz default now()), compiled_by_agent_id, source_revisions (text[] default '{}'); the idempotency/index key is an INLINE `UNIQUE (company_id, issue_id)` (NOT a standalone CREATE INDEX — the host extractQualifiedRefs validator rejects it); one apostrophe-free `COMMENT ON TABLE`; file ends semicolon-terminated. NO UPDATE, NO public.* reference, no template substitution (fully-qualified namespace literal). The D-04 divergence vs 0015 is the 2-col UNIQUE (company_id, issue_id) with ON CONFLICT DO UPDATE (one live row per issue), NOT 0015's 3-col DO NOTHING. Drop 0015's enum CHECK columns and decision_options jsonb — not needed.

    Create src/worker/db/clarity-human-wait-repo.ts mirroring action-cards-repo.ts: import PluginDatabaseClient + reuse toPgTextArrayLiteral from './tldr-cache.ts' verbatim. Export ClarityHumanWaitRow + ClarityHumanWaitRepoCtx types per the interfaces block. Export upsertClarityHumanWait (INSERT ... VALUES ($1..$7, $8::text[]) ON CONFLICT (company_id, issue_id) DO UPDATE SET all non-key columns = EXCLUDED.*), listClarityHumanWaitsForCompany (SELECT ... WHERE company_id = $1, returning all live rows), and deleteClarityHumanWait (DELETE ... WHERE company_id=$1 AND issue_id=$2 — DML in the repo, NEVER in the migration; this is the D-04 self-clear). Every query parameterized; bind source_revisions via toPgTextArrayLiteral + $N::text[].
  </action>
  <verify>
    <automated>node --check src/worker/db/clarity-human-wait-repo.ts && grep -q "ON CONFLICT (company_id, issue_id) DO UPDATE" src/worker/db/clarity-human-wait-repo.ts && grep -q "CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.clarity_human_waits" migrations/0018_structured_human_wait.sql</automated>
  </verify>
  <acceptance_criteria>
    - migrations/0018_structured_human_wait.sql contains `CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.clarity_human_waits` and an inline `UNIQUE (company_id, issue_id)`.
    - migrations/0018_structured_human_wait.sql contains NO standalone `CREATE INDEX`, NO `UPDATE`, NO `public.` reference, and the COMMENT ON body contains no apostrophe.
    - clarity-human-wait-repo.ts exports upsertClarityHumanWait (with `ON CONFLICT (company_id, issue_id) DO UPDATE`), listClarityHumanWaitsForCompany, and deleteClarityHumanWait.
    - clarity-human-wait-repo.ts binds source_revisions through `toPgTextArrayLiteral` + a `$N::text[]` cast (reused from tldr-cache.ts, not re-implemented).
  </acceptance_criteria>
  <done>The additive table + typed repo exist; upsert is DO UPDATE (one live row per issue, D-04), delete is the self-clear path, all DML is in the repo not the migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine nodeMeta fields + priority-0 leaf branch (+ verdict test)</name>
  <files>src/shared/blocker-chain.ts, test/worker/structured-human-wait-verdict.test.mjs</files>
  <read_first>
    - src/shared/blocker-chain.ts (lines 25-39 nodeMeta value type; 60-93 classifyVerdict; 274-360 leaf cascade incl. the two EXTERNAL guards at 284-303 and the status==='awaiting' branch at 308-315; 398-425 pickTopChains)
    - src/shared/types.ts:43-51 (the 8-kind Terminal union + BlockerChainResult — confirm NO change is needed)
    - test/shared/blocker-chain.test.mjs:287-309 (100-run JSON.stringify determinism guard) and :408-415 (AI-token grep guard)
    - test/worker/blocked-no-edge-verdict-consistency.test.mjs:155-204 (the synthetic-root MATRIX case shape to mirror for the new test)
    - 17-PATTERNS.md (the engine excerpt, lines 163-201; the SC5/purity trap callout, lines 419-431)
  </read_first>
  <behavior>
    - Given nodeMeta[startId].structuredWaitOwnerUserId = FOUNDER_UUID and an agent assignee also present, flattenBlockerChain returns terminal.kind === 'AWAITING_HUMAN' with terminal.userId === FOUNDER_UUID (D-07: structured wait wins over agent ownership).
    - Given structuredWaitOneLiner set, terminal.label contains the one-liner (so the wording layer can render "<owner> to decide: <one-liner>").
    - classifyVerdict over the resulting terminal yields { tier:'needs-you', actionAffordance:'reply', needsYou:true } with ZERO change to classifyVerdict (D-08 reuse of AWAITING_HUMAN).
    - Determinism: flattenBlockerChain over the same input 100× JSON.stringify-equal (the existing guard stays green).
  </behavior>
  <action>
    In src/shared/blocker-chain.ts: add `structuredWaitOwnerUserId?: string | null` and `structuredWaitOneLiner?: string | null` to the nodeMeta value type (25-39) — OPTIONAL keeps every pre-17 caller type-clean exactly as assigneeAgentId?/agentState? do. Insert ONE leaf-cascade branch at ~line 305, immediately AFTER the two EXTERNAL guards (284-303) and BEFORE the `meta?.status === 'awaiting'` branch (308), mirroring that branch's exact shape: when `meta?.structuredWaitOwnerUserId != null`, build a Terminal of kind 'AWAITING_HUMAN' with userId = meta.structuredWaitOwnerUserId and label = the one-liner form when structuredWaitOneLiner is set (else a fallback), then `return makeResult({...})`. This wins over both status==='awaiting' AND assigneeAgentId (D-07). Make NO change to classifyVerdict, pickTopChains, or the Terminal union (D-08 by construction). Add NO import, comment, or token matching /openai|anthropic|claude_local|llm|gpt|completion/i — the change is PURE field reads.

    Create test/worker/structured-human-wait-verdict.test.mjs (node:test) mirroring the synthetic-root case shape from blocked-no-edge-verdict-consistency.test.mjs: assert the behaviors above directly against flattenBlockerChain + classifyVerdict (engine-level D-07/D-08 proof). Keep it self-contained (no external harness) so Phase 20 can wire it into CI unchanged.
  </action>
  <verify>
    <automated>node --test test/worker/structured-human-wait-verdict.test.mjs && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/worker/structured-human-wait-verdict.test.mjs` exits 0; it asserts AWAITING_HUMAN wins over a present agent assignee (D-07) and the verdict maps to needs-you/reply/needsYou:true.
    - `node --test test/shared/blocker-chain.test.mjs` exits 0 — both the 100-run determinism guard and the AI-token grep guard pass (engine stays pure).
    - `grep -niE 'openai|anthropic|claude_local|llm|gpt|completion' src/shared/blocker-chain.ts | grep -v '^[0-9]*:[[:space:]]*//' ` returns no AI-token line introduced by this change (purity preserved).
    - classifyVerdict, pickTopChains, and the Terminal union in types.ts are byte-unchanged (D-08).
  </acceptance_criteria>
  <done>The engine reads two optional nodeMeta fields and emits AWAITING_HUMAN at priority 0 winning over agent ownership; purity + determinism guards green; no 9th kind.</done>
</task>

<task type="auto">
  <name>Task 3: Generic founder resolution</name>
  <files>src/worker/situation/founder-resolution.ts</files>
  <read_first>
    - src/worker/db/clarity-agent-owners-repo.ts:84-94 (listClarityAgentOwnersForCompany returns { agent_id, owner_user_id }[] — REUSE, do not write a new query)
    - src/worker/opted-in-company-set.ts:110-115 (the existing instance-agnostic owner↔company mapping the opt-in gate uses — confirms this is the canonical source)
    - 17-RESEARCH.md Area 6 (lines 277-286) + Open Question 1 (tie-break rule)
    - 17-PATTERNS.md (founder-resolution section, lines 155-160)
  </read_first>
  <action>
    Create src/worker/situation/founder-resolution.ts exporting `resolveFounderUserId(ctx, companyId): Promise<string | null>`. Call listClarityAgentOwnersForCompany(ctx, companyId), take the DISTINCT owner_user_id. Resolve the open tie-break (multiple distinct owners) with the conservative solo-operator default documented in 17-RESEARCH Open Question 1: take the single owner; if more than one distinct owner_user_id exists, pick deterministically (earliest set_at if available from the repo, else lexicographically smallest owner_user_id) and note the choice in a code comment. Return null when NO owner row exists — the caller (17-03 populator) then SKIPS writing the wait (degrade-safe, the issue falls to the conservative Watch floor). NO company-prefix or name literal; do NOT use the bulletin prepareForName (it returns a company NAME, not a user id).
  </action>
  <verify>
    <automated>node --check src/worker/situation/founder-resolution.ts && grep -q "resolveFounderUserId" src/worker/situation/founder-resolution.ts && grep -q "listClarityAgentOwnersForCompany" src/worker/situation/founder-resolution.ts</automated>
  </verify>
  <acceptance_criteria>
    - founder-resolution.ts exports resolveFounderUserId and resolves the owner by calling listClarityAgentOwnersForCompany (no new SQL query, no prepareForName).
    - On zero owner rows, resolveFounderUserId returns null (the documented degrade-safe skip path).
    - The multi-owner tie-break rule is deterministic and documented in a code comment (per 17-RESEARCH Open Question 1).
    - `grep -niE 'BEAAA|Eric|companyPrefix|prepareForName' src/worker/situation/founder-resolution.ts` returns no name/prefix/company-name literal (instance-agnostic).
  </acceptance_criteria>
  <done>The founder is resolvable generically with a documented deterministic tie-break and a null-on-absent degrade path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor-Agent / worker → plugin-namespace DB | The structured-wait row (decision one-liner, owner_user_id) is worker-authored; untrusted comment/issue text feeds the eventual one-liner (populated in 17-03) but is persisted here via parameterized SQL. |
| company scope → row visibility | Every read/write is scoped by company_id; a leak would expose another company's owner/decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | clarity_human_waits write path (repo) | mitigate | Parameterized `ctx.db.execute` only ($1..$N binds); source_revisions via toPgTextArrayLiteral + `$N::text[]` cast — no string interpolation of issue/comment content into SQL. |
| T-17-02 | Information disclosure | listClarityHumanWaitsForCompany / founder resolution | mitigate | Every query carries `WHERE company_id = $1`; UNIQUE(company_id, issue_id) scopes rows per company; resolveFounderUserId reads only the requested company's clarity_agent_owners — no cross-company owner leak. |
| T-17-03 | Tampering | blocker-chain.ts engine purity | mitigate | Engine change is pure field reads; AI-token grep guard (blocker-chain.test.mjs:408-415) blocks any AI import/token entering the engine. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | This plan installs ZERO packages (17-RESEARCH Package Legitimacy Audit: N/A); no slopcheck surface. Residual: none. |

Residual (medium/low, not blocking): prose-injection fabricating a decision one-liner is a precision concern handled in 17-03 (D-03 high-precision detection); no high-severity threat in this plan.
</threat_model>

<verification>
- `node --test test/worker/structured-human-wait-verdict.test.mjs` passes (D-07/D-08 engine proof).
- `node --test test/shared/blocker-chain.test.mjs` passes (determinism + AI-token purity guards green).
- Migration 0018 mirrors 0015's validator discipline (inline UNIQUE, DDL-only, apostrophe-free COMMENT, fully-qualified namespace).
- Repo + founder resolver compile (`node --check`) and contain no name/prefix literals.
</verification>

<success_criteria>
- WAIT-01 (capture): the additive plugin-namespace table + repo exist; one live row per (company, issue) with a self-clear delete path.
- WAIT-02 (classify): the engine emits AWAITING_HUMAN from a structuredWaitOwnerUserId field.
- WAIT-03 (precedence): the structured wait wins over a nominal agent assignee (D-07), proven by the engine test.
- Engine purity preserved (SC4).
</success_criteria>

<output>
Create `.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-01-SUMMARY.md` when done.
</output>
