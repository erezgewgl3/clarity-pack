---
phase: 17-structured-human-wait-truthful-verdicts-centerpiece
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/worker/situation/apply-structured-wait.ts
  - src/worker/handlers/situation-room.ts
  - src/worker/handlers/org-blocked-backlog.ts
  - src/worker/handlers/flatten-blocker-chain.ts
  - src/worker/situation/build-employees-rollup.ts
  - test/worker/handlers/flatten-blocker-chain-parity.test.mjs
autonomous: true
requirements: [WAIT-02, WAIT-03, WAIT-04]
must_haves:
  truths:
    - "One waitMap is built once per company in the situation-room prefetch and threaded into BOTH builders."
    - "The structured wait is merged into nodeMeta[rootId] IDENTICALLY at all three root-meta write sites via a single shared helper."
    - "The same blocked issue reads ONE consistent verdict across the Reader path and the Situation-Room path."
    - "EdgeNodeMeta and WalkOutput.nodeMeta stay in lockstep (the parity test still pins them equal after both gain the two fields)."
  artifacts:
    - path: "src/worker/situation/apply-structured-wait.ts"
      provides: "the single SC5 anti-divergence merge helper"
      exports: ["applyStructuredWait"]
    - path: "src/worker/handlers/situation-room.ts"
      provides: "the per-company waitMap prefetch threaded into both builders"
      contains: "listClarityHumanWaitsForCompany"
  key_links:
    - from: "src/worker/handlers/situation-room.ts"
      to: "clarity_human_waits"
      via: "listClarityHumanWaitsForCompany -> Map<issueId, row> -> both builders' ctx"
      pattern: "new Map\\("
    - from: "src/worker/handlers/flatten-blocker-chain.ts"
      to: "applyStructuredWait"
      via: "root-meta merge call after nodeMeta[startId] = {...}"
      pattern: "applyStructuredWait\\("
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "applyStructuredWait"
      via: "root-meta merge call after nodeMeta[focusIssue.id] = {...}"
      pattern: "applyStructuredWait\\("
    - from: "src/worker/handlers/org-blocked-backlog.ts"
      to: "applyStructuredWait"
      via: "root-meta merge call"
      pattern: "applyStructuredWait\\("
---

<objective>
Thread the persisted structured-wait row from one per-company prefetch into nodeMeta at all THREE root-meta write sites via a single shared helper — the SC5 anti-divergence primitive that kills the BEAAA-972 cross-surface bug by construction.

Purpose: This is the #1 landmine of the phase. A wait merged on one path but not the others reproduces the exact bug SC5 exists to kill (same issue reads AWAITING_HUMAN in the Situation Room and AWAITING_AGENT_STUCK in the Reader). One helper + one waitMap is the only safe shape.
Output: applyStructuredWait helper, the waitMap prefetch in situation-room.ts, the two new fields on EdgeNodeMeta + WalkOutput.nodeMeta, the identical merge call at all three sites, and the updated parity test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-CONTEXT.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-RESEARCH.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-PATTERNS.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-01-SUMMARY.md

<interfaces>
<!-- Contracts consumed from 17-01 + created here. -->

From 17-01 (src/worker/db/clarity-human-wait-repo.ts):
  listClarityHumanWaitsForCompany(ctx, companyId): Promise<ClarityHumanWaitRow[]>
  // ClarityHumanWaitRow has: issue_id, owner_user_id, decision_one_liner (the fields the merge reads)

From 17-01 (src/shared/blocker-chain.ts nodeMeta value type — already extended):
  structuredWaitOwnerUserId?: string | null;
  structuredWaitOneLiner?: string | null;

New helper (src/worker/situation/apply-structured-wait.ts):
  applyStructuredWait(
    nodeMeta: Record<string, { structuredWaitOwnerUserId: string | null; structuredWaitOneLiner: string | null }>,
    startId: string,
    waitMap: Map<string, { owner_user_id: string; decision_one_liner: string }>,
  ): void
  // pure: reads waitMap.get(startId); if present, sets the two fields on nodeMeta[startId]; else no-op.

The three root-meta write sites (the SC5 parity trap):
  1. src/worker/handlers/flatten-blocker-chain.ts ~354 (Reader)   — nodeMeta[startId] = {...}
  2. src/worker/situation/build-employees-rollup.ts ~441 (SR rollup) — nodeMeta[focusIssue.id] = {...}
  3. src/worker/handlers/org-blocked-backlog.ts root meta (SR backlog)

The prefetch Map-build pattern to mirror: situation-room.ts buildSnapshotPrefetch nameByUuid/edgeGraph (~350-431), one query per company, threaded into both builders' ctx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: applyStructuredWait helper + waitMap prefetch</name>
  <files>src/worker/situation/apply-structured-wait.ts, src/worker/handlers/situation-room.ts</files>
  <read_first>
    - src/worker/handlers/situation-room.ts:286-432 (buildSnapshotPrefetch — the nameByUuid/edgeGraph Map-build pattern + SnapshotPrefetchBundle return shape; thread waitMap identically, one query per company)
    - src/worker/db/clarity-human-wait-repo.ts (from 17-01 — listClarityHumanWaitsForCompany signature + ClarityHumanWaitRow)
    - 17-PATTERNS.md (the Shared Patterns applyStructuredWait helper excerpt, lines 378-393; the situation-room prefetch excerpt, lines 259-267; the SC5 PARITY TRAP callout, lines 419-431)
  </read_first>
  <action>
    Create src/worker/situation/apply-structured-wait.ts exporting `applyStructuredWait(nodeMeta, startId, waitMap)` exactly per the interfaces block: `const w = waitMap.get(startId); if (!w) return; nodeMeta[startId].structuredWaitOwnerUserId = w.owner_user_id; nodeMeta[startId].structuredWaitOneLiner = w.decision_one_liner;`. Pure, no I/O.

    In src/worker/handlers/situation-room.ts buildSnapshotPrefetch: add ONE `listClarityHumanWaitsForCompany(ctx, companyId)` SELECT and build `const waitMap = new Map(waitRows.map((w) => [w.issue_id, w]))` — exactly mirroring the nameByUuid/edgeGraph one-query-per-company pattern. Add waitMap to the SnapshotPrefetchBundle return shape and thread it into BOTH builders' ctx alongside nameByUuid/edgeGraph. Degrade-safe: a throw on the wait SELECT must NOT abort the prefetch — wrap it so an empty waitMap is used on failure (no wait merged → conservative floor), matching the existing degrade discipline (the issues/agents SELECT failure returns a null bundle; a wait SELECT failure should default waitMap to empty and continue, since the wait is an enhancement not a prerequisite).
  </action>
  <verify>
    <automated>node --check src/worker/situation/apply-structured-wait.ts && grep -q "listClarityHumanWaitsForCompany" src/worker/handlers/situation-room.ts && grep -q "applyStructuredWait" src/worker/situation/apply-structured-wait.ts</automated>
  </verify>
  <acceptance_criteria>
    - apply-structured-wait.ts exports applyStructuredWait; it reads waitMap.get(startId) and is a no-op when absent.
    - situation-room.ts issues exactly ONE listClarityHumanWaitsForCompany query per company in buildSnapshotPrefetch and builds a Map<issue_id, row>.
    - waitMap is present on the prefetch bundle and threaded into both builders' ctx.
    - A failed wait SELECT defaults to an empty waitMap and does not abort the prefetch (degrade-safe).
  </acceptance_criteria>
  <done>One waitMap is built once per company and reaches both builders; the merge helper exists and is pure.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the wait at all three root-meta write sites (SC5 parity)</name>
  <files>src/worker/handlers/flatten-blocker-chain.ts, src/worker/situation/build-employees-rollup.ts, src/worker/handlers/org-blocked-backlog.ts</files>
  <read_first>
    - src/worker/handlers/flatten-blocker-chain.ts:93-105 (WalkOutput.nodeMeta inline type) + 354-365 (the root-meta write block, the BEAAA-972 empty-edges fix)
    - src/worker/situation/build-employees-rollup.ts:441-451 (the focus/root nodeMeta inject)
    - src/worker/handlers/org-blocked-backlog.ts:60-66 (EdgeNodeMeta type — note the line-58 comment stating flatten-blocker-chain mirrors this exact field set for SC5) + the root-meta build site
    - src/worker/situation/apply-structured-wait.ts (the helper from Task 1)
    - 17-PATTERNS.md (the three write-site excerpts, lines 204-256; the SC5 PARITY TRAP table, lines 419-429)
  </read_first>
  <action>
    Add `structuredWaitOwnerUserId: string | null` and `structuredWaitOneLiner: string | null` to BOTH the WalkOutput.nodeMeta inline type (flatten-blocker-chain.ts:93-105) AND the EdgeNodeMeta type (org-blocked-backlog.ts:60-66) so the two stay in lockstep (SC5). At each of the three root-meta write sites, initialize the two new fields to null in the nodeMeta[rootId] = {...} literal, then call `applyStructuredWait(nodeMeta, rootId, waitMap)` IDENTICALLY (rootId = startId for flatten-blocker-chain, focusIssue.id for build-employees-rollup, the root issue id for org-blocked-backlog). Do NOT inline-duplicate the merge logic at any site — the single helper is the whole point. waitMap reaches each builder via the threaded ctx from Task 1.
  </action>
  <verify>
    <automated>node --check src/worker/handlers/flatten-blocker-chain.ts && node --check src/worker/situation/build-employees-rollup.ts && node --check src/worker/handlers/org-blocked-backlog.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "applyStructuredWait(" src/worker/handlers/flatten-blocker-chain.ts src/worker/situation/build-employees-rollup.ts src/worker/handlers/org-blocked-backlog.ts` shows the helper called in all three files.
    - WalkOutput.nodeMeta (flatten-blocker-chain.ts) and EdgeNodeMeta (org-blocked-backlog.ts) both declare structuredWaitOwnerUserId and structuredWaitOneLiner.
    - No site inline-duplicates the waitMap.get/field-set logic (only the shared helper performs the merge).
  </acceptance_criteria>
  <done>The wait is merged into nodeMeta at all three root-meta sites via the single helper, fed by the one threaded waitMap.</done>
</task>

<task type="auto">
  <name>Task 3: Update the flatten-blocker-chain parity test</name>
  <files>test/worker/handlers/flatten-blocker-chain-parity.test.mjs</files>
  <read_first>
    - test/worker/handlers/flatten-blocker-chain-parity.test.mjs (the same-shape assertion pinning EdgeNodeMeta ≡ WalkOutput.nodeMeta)
    - src/worker/handlers/org-blocked-backlog.ts:60-66 + src/worker/handlers/flatten-blocker-chain.ts:93-105 (the two now-extended field sets)
    - 17-PATTERNS.md (the parity-test section, lines 370-372)
  </read_first>
  <action>
    Update the same-shape assertion in flatten-blocker-chain-parity.test.mjs to include structuredWaitOwnerUserId and structuredWaitOneLiner so the parity test still pins EdgeNodeMeta ≡ WalkOutput.nodeMeta after both gained the two fields. Do not change the test's structure — only widen the expected field set.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/flatten-blocker-chain-parity.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/worker/handlers/flatten-blocker-chain-parity.test.mjs` exits 0.
    - The parity assertion includes structuredWaitOwnerUserId and structuredWaitOneLiner in the pinned field set.
  </acceptance_criteria>
  <done>The parity guard pins the two nodeMeta shapes equal including the new fields; lockstep is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin DB → snapshot builders | The persisted wait row crosses into nodeMeta; the owner_user_id flows toward the rendered verdict label. |
| company scope → snapshot | The prefetch is company-scoped; a leak would surface another company's wait in this company's snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Information disclosure | situation-room waitMap prefetch | mitigate | listClarityHumanWaitsForCompany is `WHERE company_id = $1`; the waitMap is built per-company and threaded only into that company's snapshot build — no cross-company merge. |
| T-17-05 | Tampering | SC5 divergence (the BEAAA-972 bug class) | mitigate | Single shared applyStructuredWait helper + single waitMap; the parity test (Task 3) and the full matrix (17-05) pin all paths to one verdict. |
| T-17-06 | Denial of service | wait SELECT in the hot prefetch path | mitigate | One additional bounded `WHERE company_id=$1` SELECT per snapshot; failure defaults to an empty waitMap and continues (degrade-safe), inheriting the Phase-16 prefetch degrade discipline. |
| T-17-SC | Tampering | npm installs | accept | Zero packages installed in this plan. Residual: none. |

Residual (low, not blocking): the owner_user_id reaches the label as a raw id until scrubbed at render — scrub is the existing NO_UUID_LEAK path on the AWAITING_HUMAN arm; no new high-severity surface here.
</threat_model>

<verification>
- All three builders call applyStructuredWait; both nodeMeta field sets extended in lockstep.
- The parity test passes with the widened field set.
- The waitMap is built once per company in the prefetch and threaded into both builders.
- `node --check` passes on all four modified worker files.
</verification>

<success_criteria>
- WAIT-02 (surfacing): the persisted wait reaches nodeMeta so the engine classifies AWAITING_HUMAN on every consuming surface.
- WAIT-03 / WAIT-04 (consistency): all three root-meta sites merge identically — the SC5 dual/tri-path parity that prevents the BEAAA-972 divergence.
</success_criteria>

<output>
Create `.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-02-SUMMARY.md` when done.
</output>
