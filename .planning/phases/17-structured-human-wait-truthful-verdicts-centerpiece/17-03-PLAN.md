---
phase: 17-structured-human-wait-truthful-verdicts-centerpiece
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/worker/agents/human-wait-detect.ts
  - src/worker/agents/editor.ts
autonomous: true
requirements: [WAIT-01, WAIT-02]
must_haves:
  truths:
    - "The Editor-Agent detects 'blocked on a human decision X' from comments it already reads, tuned for HIGH PRECISION (false positive << missed wait)."
    - "On positive detection the structured-wait row is upserted with the founder owner + a polishTldr-voiced one-liner."
    - "On negative detection OR a non-blocked issue, any stale wait row for that issue is deleted (D-04 self-clear; not sticky)."
    - "Detection rides the existing heartbeat governance (wake-governor, opt-in scope gate, self-loop filter) and adds NO new wake path (no-storm)."
    - "When the Editor-Agent is down, NO rows are written — issues fall to the conservative Watch floor, never a fabricated needs-you."
  artifacts:
    - path: "src/worker/agents/human-wait-detect.ts"
      provides: "the high-precision detection step (prompt build + op-issue delivery + parse + upsert/delete)"
      exports: ["detectAndPersistHumanWait"]
    - path: "src/worker/agents/editor.ts"
      provides: "the sibling detection call in the per-issue heartbeat loop"
      contains: "detectAndPersistHumanWait"
  key_links:
    - from: "src/worker/agents/editor.ts"
      to: "detectAndPersistHumanWait"
      via: "sibling step in the per-issue heartbeat loop (same comments already fetched)"
      pattern: "detectAndPersistHumanWait\\("
    - from: "src/worker/agents/human-wait-detect.ts"
      to: "clarity_human_waits"
      via: "upsertClarityHumanWait on positive / deleteClarityHumanWait on negative or not-blocked"
      pattern: "deleteClarityHumanWait\\("
---

<objective>
Populate the structured-wait row: the Editor-Agent reads the comments it already fetches per issue, runs a HIGH-PRECISION "blocked on a human decision" detection through the existing op-issue LLM delivery layer, and upserts (or self-clears) the wait row under the existing Phase 16.1 governance.

Purpose: This is the producer half of the producer/consumer split — structurally a sibling of the TL;DR compile. It is the ONLY AI in the path (the engine stays pure). High precision is the trust contract: a false positive puts a fake item in the centerpiece Needs-you tier; a missed wait is no worse than today's conservative floor.
Output: human-wait-detect.ts (prompt + delivery + parse + upsert/delete) and the one sibling call wired into the editor heartbeat loop.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-CONTEXT.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-RESEARCH.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-PATTERNS.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-01-SUMMARY.md

<interfaces>
<!-- Contracts consumed from 17-01 + the existing Editor scaffolding. -->

From 17-01:
  upsertClarityHumanWait(ctx, row): Promise<void>          // clarity-human-wait-repo.ts
  deleteClarityHumanWait(ctx, companyId, issueId): Promise<void>
  resolveFounderUserId(ctx, companyId): Promise<string | null>   // founder-resolution.ts; null = skip (degrade-safe)

Existing Editor scaffolding to REUSE (editor.ts):
  handleEditorHeartbeat per-issue loop (259-330): ctx.issues.get -> isOwnOperationIssue guard -> ctx.issues.listComments(278) -> prepareTldrCompile(293) -> startAgentTask/pollAgentTaskResult(305-318) -> finalizeTldr(320)
  startAgentTask(ctx, { agentId, companyId, operationKind, operationId, title, prompt })
  pollAgentTaskResult(ctx, { operationIssueId, companyId, operationKind, agentId }) -> { status, body }
  isOwnOperationIssue (recursion guard — covers the whole plugin:clarity-pack:operation:* namespace)
  filterSelfLoopEvents / checkAndRecordWake / opt-in scope gate (Phase 16.1 — already wrap the heartbeat)

Voice helper (REUSE, do not rebuild): polishTldr(input) (compile-tldr.ts:370-378), then truncate <=80 chars with '…' (the build-employees-rollup.ts:384-385 usage model).
Robust parse-out-of-prose: extractJsonObject (compile-pass-1.ts:297) — for { isHumanWait, decisionOneLiner }.
content_hash: the tldrContentHash pattern (compile-tldr.ts) over the same comment-input set the TL;DR uses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: human-wait-detect.ts — detect, voice, upsert/self-clear</name>
  <files>src/worker/agents/human-wait-detect.ts</files>
  <read_first>
    - src/worker/agents/compile-tldr.ts:370-378 (polishTldr signature + behavior — REUSE for voice parity)
    - src/worker/agents/compile-pass-1.ts:249-297 (extractJsonObject robust JSON-out-of-prose parse; note prepareForName at 249-272 returns a company NAME — do NOT use it for the owner)
    - src/worker/agents/editor.ts:305-329 (startAgentTask/pollAgentTaskResult op-issue handoff to mirror)
    - src/worker/db/clarity-human-wait-repo.ts + src/worker/situation/founder-resolution.ts (from 17-01 — upsert/delete/resolveFounderUserId)
    - 17-RESEARCH.md Area 4 (lines 263-271) + Pitfall 3 (false positive) + Pitfall 4 (sticky waits)
    - 17-PATTERNS.md (the editor.ts + human-wait-detect.ts section, lines 271-300)
  </read_first>
  <action>
    Create src/worker/agents/human-wait-detect.ts exporting `detectAndPersistHumanWait(ctx, { agentId, companyId, issueId, issue, comments })`. Steps:
    1. Self-clear precondition: if the issue is NOT blocked status, call deleteClarityHumanWait(ctx, companyId, issueId) and return (D-04 — the wait only exists while blocked).
    2. Build a HIGH-PRECISION detection prompt over the issue body + comments that returns a structured `{ isHumanWait: boolean, decisionOneLiner: string | null }`. The prompt MUST set isHumanWait true ONLY when the prose clearly names a decision/question awaiting a specific person; default false on ambiguity (D-03 precision-over-recall). (Exact prompt wording is Claude's discretion within D-03.)
    3. Deliver the prompt through the existing op-issue layer: startAgentTask with a NEW operationKind 'human-wait-detect' (operationId `human-wait-${issueId}`) in the same plugin:clarity-pack:operation:* namespace (so isOwnOperationIssue auto-excludes it), then pollAgentTaskResult. Parse the result with extractJsonObject. On a not-ready poll, return without writing (the drainer/next heartbeat re-evaluates; degrade-safe).
    4. On isHumanWait === false (or unparseable / ambiguous): deleteClarityHumanWait (self-clear) and return.
    5. On isHumanWait === true: resolveFounderUserId(ctx, companyId); if null, SKIP writing (no owner claimed → conservative floor, degrade-safe). Else run polishTldr(decisionOneLiner) then truncate <=80 chars and upsertClarityHumanWait with { company_id, issue_id, owner_user_id: founder, decision_one_liner: polished, content_hash (over the same comment-input set), generated_at: now ISO, compiled_by_agent_id: agentId, source_revisions }.
    Idempotency: skip the LLM call when content_hash is unchanged since the last persisted row (mirror the prepareTldrCompile cache-hit short-circuit) so a heartbeat over unchanged inputs is a no-op.
  </action>
  <verify>
    <automated>node --check src/worker/agents/human-wait-detect.ts && grep -q "detectAndPersistHumanWait" src/worker/agents/human-wait-detect.ts && grep -q "deleteClarityHumanWait" src/worker/agents/human-wait-detect.ts && grep -q "polishTldr" src/worker/agents/human-wait-detect.ts</automated>
  </verify>
  <acceptance_criteria>
    - human-wait-detect.ts exports detectAndPersistHumanWait; on a non-blocked issue OR negative detection it calls deleteClarityHumanWait (D-04 self-clear).
    - On positive detection it calls resolveFounderUserId and SKIPS the write when null (degrade-safe); otherwise upserts with the founder as owner_user_id.
    - The one-liner passes through polishTldr and is truncated <=80 chars before persistence (D-05 voice parity).
    - The detection prompt returns a structured { isHumanWait, decisionOneLiner } and defaults to false on ambiguity (D-03); result parsed via extractJsonObject.
    - The op-issue handoff uses operationKind 'human-wait-detect' in the plugin:clarity-pack:operation:* namespace (auto-excluded by isOwnOperationIssue).
    - `grep -niE 'prepareForName|companyPrefix|BEAAA|Eric' src/worker/agents/human-wait-detect.ts` returns no company-name/prefix/name literal for the owner.
  </acceptance_criteria>
  <done>Detection writes a voiced, founder-owned wait only on a high-precision positive over a blocked issue, and self-clears otherwise.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the sibling detection call into the heartbeat loop</name>
  <files>src/worker/agents/editor.ts</files>
  <read_first>
    - src/worker/agents/editor.ts:246-336 (handleEditorHeartbeat — the per-issue loop, the isOwnOperationIssue guard at 270, the comments fetch at 278, the per-issue try/catch at 260/331)
    - src/worker/agents/human-wait-detect.ts (from Task 1 — detectAndPersistHumanWait signature)
    - 17-RESEARCH.md Area 4 governance-reuse paragraph (line 268) — ride the existing heartbeat, add NO new wake path
    - 17-PATTERNS.md (the editor heartbeat sibling-step excerpt, lines 275-300)
  </read_first>
  <action>
    In handleEditorHeartbeat's per-issue loop (editor.ts:259-330), after the comments are fetched (line 278) and AFTER the existing TL;DR compile block, add ONE sibling call: `await detectAndPersistHumanWait(ctx, { agentId: payload.agentId, companyId: payload.companyId, issueId, issue, comments })`, passing the SAME comments already fetched (no second listComments). Wrap it so a throw inside detection is caught and logged per-issue (mirror the existing per-issue try/catch posture) and never aborts the loop or the TL;DR compile. Add NO new schedule, NO new requestWakeup, NO new event subscription — detection rides the existing heartbeat pull + wake-governor + opt-in gate + self-loop filter that already wrap this loop (Phase 16.1 no-storm). The isOwnOperationIssue guard at line 270 already excludes the plugin's own operation issues, including the new human-wait-detect op kind.
  </action>
  <verify>
    <automated>node --check src/worker/agents/editor.ts && grep -q "detectAndPersistHumanWait" src/worker/agents/editor.ts</automated>
  </verify>
  <acceptance_criteria>
    - editor.ts calls detectAndPersistHumanWait once per issue in the existing heartbeat loop, reusing the comments already fetched (no second listComments).
    - The call is inside per-issue error isolation so a detection throw does not abort the loop or the TL;DR compile.
    - `grep -niE 'requestWakeup|jobs.schedule|events.subscribe|setInterval' src/worker/agents/editor.ts` shows NO new wake path / subscription / timer introduced by this change (no-storm; only the pre-existing governed paths remain).
  </acceptance_criteria>
  <done>Detection runs as a governed sibling of the TL;DR compile on the existing heartbeat, with no new wake path and full per-issue isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| employee-agent comment prose → detection LLM | Untrusted prose is interpreted by the detection prompt; a crafted comment could try to manufacture a needs-you (prompt manipulation). |
| detection output → plugin DB | The LLM-produced one-liner + founder owner are persisted; untrusted-derived text crosses into SQL (via the parameterized repo). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | Spoofing | detection prompt (prose-injection fabricating a needs-you) | mitigate | HIGH-PRECISION detection (D-03): isHumanWait true only when a decision is clearly addressed to a specific person; default false on ambiguity; the row self-clears each compile; the Editor's existing governance caps bound abuse. |
| T-17-08 | Tampering | upsert of the one-liner into clarity_human_waits | mitigate | Persistence uses the parameterized repo from 17-01 ($N binds, text[] via cast) — no string interpolation of comment content into SQL. |
| T-17-09 | Elevation of privilege / DoS | new detection wake path | mitigate | NO new wake path, schedule, or subscription; detection rides the existing Phase-16.1 wake-governor + opt-in scope gate + self-loop filter (verified by the grep gate in Task 2). |
| T-17-10 | Information disclosure | founder owner_user_id resolution | mitigate | resolveFounderUserId reads only `WHERE company_id=$1` clarity_agent_owners — no cross-company owner leak; null → skip. |
| T-17-SC | Tampering | npm installs | accept | Zero packages installed; detection hand-written per 17-RESEARCH Don't-Hand-Roll guidance. Residual: none. |

Residual (low, not blocking): the raw owner_user_id is scrubbed at render via the existing NO_UUID_LEAK AWAITING_HUMAN arm — no new render path here. No high-severity threat.
</threat_model>

<verification>
- Detection self-clears on non-blocked / negative; upserts only on a high-precision positive with a resolved founder.
- The one-liner is polishTldr-voiced and truncated.
- The heartbeat introduces no new wake path / subscription / timer (grep gate).
- `node --check` passes on both worker files.
</verification>

<success_criteria>
- WAIT-01 (structured declaration): the Editor-Agent produces the machine-readable wait row from prose, additively in the plugin namespace.
- WAIT-02 (needs-you): a real human decision surfaces as needs-you (via the engine in 17-01/17-02), not parked in Watch.
- Degrade-safe: Editor down → no rows → conservative floor; precision tuned so false positives stay out of the centerpiece tier.
</success_criteria>

<output>
Create `.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-03-SUMMARY.md` when done.
</output>
