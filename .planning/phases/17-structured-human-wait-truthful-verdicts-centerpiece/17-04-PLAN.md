---
phase: 17-structured-human-wait-truthful-verdicts-centerpiece
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/handlers/issue-reader.ts
  - src/ui/surfaces/reader/breadcrumb.tsx
  - src/ui/surfaces/reader/ref-card.tsx
autonomous: true
requirements: [WAIT-02]
must_haves:
  truths:
    - "The Reader breadcrumb no longer dumps the 1k+ char company-mission goal segment (D-11)."
    - "Every breadcrumb segment that is NOT a confirmed-routable host page renders as plain non-clickable text — zero dead links, zero 404 (D-12)."
    - "The only linked segment is the issue/parent, prefixed to /<companyPrefix>/issues/<identifier>."
    - "Ref-cards lead with the plain-English title; BEAAA-NNN is demoted but recoverable (data-ref-id kept); Stuck/Standby code-chips read as plain words (D-13)."
  artifacts:
    - path: "src/worker/handlers/issue-reader.ts"
      provides: "deriveAncestry emits a routable flag + drops the root mission goal segment"
      contains: "routable"
    - path: "src/ui/surfaces/reader/breadcrumb.tsx"
      provides: "conditional <a> (routable) vs <span> (plain text)"
      contains: "routable"
    - path: "src/ui/surfaces/reader/ref-card.tsx"
      provides: "title-first card, demoted recoverable id, plain-word status"
      contains: "data-ref-id"
  key_links:
    - from: "src/ui/surfaces/reader/breadcrumb.tsx"
      to: "extractCompanyPrefixFromPathname(useHostLocation().pathname)"
      via: "prefix only the routable issue/parent segment"
      pattern: "extractCompanyPrefixFromPathname"
    - from: "src/worker/handlers/issue-reader.ts"
      to: "AncestrySegment.routable"
      via: "routable:true only for issue/parent; goal/project routable:false; root mission goal dropped"
      pattern: "routable"
---

<objective>
Fix the two operator-seeded Reader legibility bugs: the breadcrumb dumps the entire company-mission paragraph and 404s on every segment, and the "ANCHORED TO" ref-cards read like task-tracker plumbing instead of plain English.

Purpose: This is the legibility surface of the truthful verdict — the Reader is where the operator lands to understand a needs-you item. A mission-dump crumb that 404s and code-chip ref-cards are exactly the "rabbit-hole" the milestone exists to kill. These were folded into Phase 17 by explicit operator decision (STATE.md PHASE 17 SEED). Independent of the engine work — no dependency on 17-01/02/03.
Output: deriveAncestry emits a routable flag + drops the mission goal; breadcrumb branches link-vs-plain-text; ref-card leads title-first with demoted-but-recoverable ids and plain-word status.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-CONTEXT.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-RESEARCH.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-PATTERNS.md
@.claude/skills/sketch-findings-clarity-pack/SKILL.md

<interfaces>
<!-- Verified from source. -->

Current AncestrySegment (breadcrumb.tsx:12): { id: string; title: string; url: string }
Current Ancestry (breadcrumb.tsx:14-18): { project: AncestrySegment | null; milestone: AncestrySegment | null; parent: AncestrySegment | null }
  // milestone = the GOAL segment (issue-reader.ts:411-415) whose g.title is the 1k+ char mission — the D-11 drop target.

deriveAncestry (issue-reader.ts:357-429) builds prefix-less URLs that 404:
  parent.url  = `/issues/${parentKey}`   (376)  — the ONLY confirmed-routable segment
  project.url = `/projects/${p.id}`      (394)  — no confirmed host route -> plain text
  goal.url    = `/goals/${g.id}`         (414)  — no confirmed host route; root mission goal DROPPED, any non-root goal -> plain text

Host-route confirmation (17-RESEARCH Area 7, RESOLVED): ONLY /<companyPrefix>/issues/<identifier> routes (30+ live call sites + memory paperclip-issue-url-pattern; key on the issue IDENTIFIER, not UUID). Project + goal routes unconfirmed -> plain text.

companyPrefix source (already used in reader/index.tsx:53,251 and ref-card.tsx:27,47):
  extractCompanyPrefixFromPathname(useHostLocation().pathname)

Navigation primitive (keep — SCAF-09, no raw <a href>): useHostNavigation().linkProps(href) (breadcrumb.tsx:10,28,36)

Ref-card (ref-card.tsx:29-104): statusToPill maps in_progress->'Working', blocked->'Stuck', done/todo/default->'Standby'; header (79-85) renders card.id FIRST, then title, then a StatePill (Stuck/Standby); data-ref-id={card.id} at line 80 keeps the id recoverable.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: deriveAncestry — drop mission goal + emit routable flag (D-11/D-12)</name>
  <files>src/worker/handlers/issue-reader.ts</files>
  <read_first>
    - src/worker/handlers/issue-reader.ts:357-429 (deriveAncestry — the three prefix-less URL builds + the goal mission-title at 411-415)
    - src/ui/surfaces/reader/breadcrumb.tsx:12-18 (the AncestrySegment + Ancestry types this handler populates)
    - 17-RESEARCH.md Area 7 (lines 288-301, the host-route confirmation table) — confirms Option (b): worker stays instance-agnostic, UI prepends the prefix
    - 17-PATTERNS.md (the issue-reader section, lines 304-313)
  </read_first>
  <action>
    Extend AncestrySegment with `routable: boolean` (and keep emitting the issue/parent IDENTIFIER, not the UUID, as the segment value the UI will prefix). In deriveAncestry: (1) DROP the root company-mission goal segment entirely (D-11) — when the goal is the company-mission root, do NOT populate ancestry.milestone (the title is the 1k+ char mission, never a useful nav target). Truncate any OTHER (non-root) long goal/project/parent title to a short label. (2) Set routable: true ONLY on the issue/parent segment; set routable: false on project and any surviving goal segment (no confirmed host route → the UI renders them plain text). (3) Keep the worker INSTANCE-AGNOSTIC: emit a prefix-less canonical (the issue identifier for the parent) and let breadcrumb.tsx prepend /<companyPrefix>/issues/ — do NOT build the company-prefixed URL in the worker (Option (b), 17-RESEARCH Area 7). Do NOT guess a /projects/ or /goals/ route.
  </action>
  <verify>
    <automated>node --check src/worker/handlers/issue-reader.ts && grep -q "routable" src/worker/handlers/issue-reader.ts</automated>
  </verify>
  <acceptance_criteria>
    - AncestrySegment carries a `routable` flag; deriveAncestry sets routable:true only for the issue/parent segment and false for project/goal.
    - deriveAncestry no longer emits the root company-mission goal segment (the milestone goal.title mission-dump is dropped; no 1k+ char title reaches the breadcrumb).
    - The worker emits a prefix-less canonical (issue identifier) — `grep -niE 'companyPrefix|/COU/|/BEAAA/' src/worker/handlers/issue-reader.ts` shows no company-prefix literal added by this change (instance-agnostic; the UI prepends the prefix).
  </acceptance_criteria>
  <done>The breadcrumb data no longer carries the mission dump, and each segment declares whether it is safe to link.</done>
</task>

<task type="auto">
  <name>Task 2: breadcrumb.tsx — conditional link vs plain text (D-12)</name>
  <files>src/ui/surfaces/reader/breadcrumb.tsx</files>
  <read_first>
    - src/ui/surfaces/reader/breadcrumb.tsx (full file — the unconditional <a {...nav.linkProps(s.url)}> at 35-44 that 404s)
    - src/ui/surfaces/reader/index.tsx:53,251-252 (the extractCompanyPrefixFromPathname / useHostLocation usage to mirror)
    - src/ui/surfaces/reader/ref-card.tsx:27,47-48 (a second extractCompanyPrefixFromPathname usage reference)
    - 17-PATTERNS.md (the breadcrumb section, lines 317-332)
  </read_first>
  <action>
    Update AncestrySegment to `{ id: string; title: string; url: string | null; routable: boolean }`. In the segment map, branch: when `s.routable` (the issue/parent), render `<a {...nav.linkProps(\`/${companyPrefix}/issues/${s.url}\`)} className="clarity-breadcrumb-segment">{s.title}</a>` (s.url is the prefix-less issue identifier from Task 1; keep useHostNavigation().linkProps — no raw <a href>, SCAF-09); otherwise render `<span className="clarity-breadcrumb-segment clarity-breadcrumb-segment--plain">{s.title}</span>` (non-clickable plain text). Source companyPrefix via `extractCompanyPrefixFromPathname(useHostLocation().pathname)` — call the hooks unconditionally at the top, before the early `if (!ancestry) return null`. Keep the existing `·` separator behavior. The `--plain` class is a local utility only (Tailwind inherited from host; no new stylesheet).
  </action>
  <verify>
    <automated>node --check src/ui/surfaces/reader/breadcrumb.tsx && grep -q "extractCompanyPrefixFromPathname" src/ui/surfaces/reader/breadcrumb.tsx && grep -q "clarity-breadcrumb-segment--plain" src/ui/surfaces/reader/breadcrumb.tsx</automated>
  </verify>
  <acceptance_criteria>
    - breadcrumb.tsx renders a non-routable segment (project/goal) as a `<span>`, not an `<a>` — zero dead links.
    - The routable issue/parent segment links to `/<companyPrefix>/issues/<identifier>` via useHostNavigation().linkProps (no raw `<a href>`).
    - companyPrefix is sourced from extractCompanyPrefixFromPathname(useHostLocation().pathname); the hooks are called unconditionally before the null early-return.
  </acceptance_criteria>
  <done>Only confirmed-routable segments are clickable; everything else is plain text; the linked URL is correctly company-prefixed.</done>
</task>

<task type="auto">
  <name>Task 3: ref-card.tsx — title-first, demote id, plain-word status (D-13)</name>
  <files>src/ui/surfaces/reader/ref-card.tsx</files>
  <read_first>
    - src/ui/surfaces/reader/ref-card.tsx:20-104 (the RefCard render at 79-104, the header at 79-85, statusToPill at 29-42, data-ref-id at 80)
    - .claude/skills/sketch-findings-clarity-pack/SKILL.md (title-forward chips; IDs in Geist Mono uppercase letter-spaced de-emphasized — the D-13 visual direction)
    - .claude/skills/sketch-findings-clarity-pack/references/inline-references.md (title-forward resolved-chip direction)
    - 17-PATTERNS.md (the ref-card section, lines 336-345)
  </read_first>
  <action>
    UI-only label/order substitution within the existing card structure (NOT a redesign): (1) lead with `card.title` — move the `<strong className="clarity-ref-card-title">` first in the header; demote `card.id` (BEAAA-NNN) to a subtle secondary position (de-emphasized, per the SKILL Geist-Mono direction). KEEP `data-ref-id={card.id}` so the identifier stays recoverable. (2) Translate the statusToPill vocabulary out of the code-chip words: `blocked → "needs attention"`, `in_progress → "in progress"`, `done → "done"`, todo/default → a plain neutral word (or drop the chip) — replace the `Stuck`/`Standby` vocabulary. (3) Leave the owner line (already degrades to 'unassigned', NO_UUID_LEAK ok). Any new class is a local utility only (Tailwind inherited from host; no new stylesheet).
  </action>
  <verify>
    <automated>node --check src/ui/surfaces/reader/ref-card.tsx && grep -q "data-ref-id" src/ui/surfaces/reader/ref-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The ref-card header renders card.title before card.id (title-first); data-ref-id={card.id} is retained (id recoverable).
    - statusToPill no longer emits the 'Stuck'/'Standby' code-chip vocabulary; blocked reads as plain words like "needs attention" (in_progress -> "in progress", done -> "done").
    - `grep -niE "'Stuck'|'Standby'" src/ui/surfaces/reader/ref-card.tsx` returns no remaining code-chip status label.
  </acceptance_criteria>
  <done>The ref-card leads plain-English, demotes machine codes while keeping them recoverable, and shows human-readable status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker ancestry data → Reader UI link construction | Untrusted segment ids/identifiers flow into a constructed href; a malformed/injected segment could produce an open-redirect or a broken URL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-11 | Tampering / open-redirect | breadcrumb URL construction | mitigate | Only the issue/parent identifier is interpolated into a fixed `/${companyPrefix}/issues/${id}` template via useHostNavigation().linkProps (host router intercept), never a raw `<a href>` to an arbitrary URL; non-routable segments render as plain `<span>` with no href at all — no untrusted scheme/host can be injected. |
| T-17-12 | Information disclosure | mission-paragraph dump | mitigate | D-11 drops the company-mission goal segment entirely — the 1k+ char mission no longer renders in the breadcrumb; the surface is purely the operator's own company data (no cross-company exposure). |
| T-17-SC | Tampering | npm installs | accept | Zero packages installed (UI label/order edits only). Residual: none. |

Residual (low, not blocking): a truncated label could omit context — acceptable per D-11 intent (short nav label, full title recoverable on the target page). No high-severity threat.
</threat_model>

<verification>
- Breadcrumb renders non-routable segments as plain spans (no 404), links only the prefixed issue/parent.
- The mission goal segment is dropped from deriveAncestry.
- Ref-card leads title-first with a recoverable demoted id and plain-word status.
- `node --check` passes on all three files.
</verification>

<success_criteria>
- D-11/D-12: zero mission-dump, zero dead links in the Reader breadcrumb.
- D-13: ref-cards read as plain English with machine codes demoted but recoverable.
- Instance-agnostic (no company-prefix literal in the worker; the UI sources the prefix at runtime).
</success_criteria>

<output>
Create `.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-04-SUMMARY.md` when done.
</output>
