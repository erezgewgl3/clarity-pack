---
phase: 17-structured-human-wait-truthful-verdicts-centerpiece
plan: 05
type: execute
wave: 3
depends_on: [17-02, 17-03]
files_modified:
  - test/worker/blocked-no-edge-verdict-consistency.test.mjs
autonomous: true
requirements: [WAIT-03, WAIT-04]
must_haves:
  truths:
    - "Every blocked-no-edge class classifies truthfully: blocked+agent-owned -> AWAITING_AGENT_STUCK, blocked+human-owned -> AWAITING_HUMAN, blocked+unowned -> UNOWNED, structured-human-wait -> AWAITING_HUMAN."
    - "The structured-human-wait case asserts the wait WINS even when an agent assignee is present (the D-07 core assertion)."
    - "Every surface x every terminal kind reads ONE consistent verdict (4 surfaces x 8 kinds)."
    - "blocker-chain.ts stays pure — the determinism + AI-token grep guards still pass."
  artifacts:
    - path: "test/worker/blocked-no-edge-verdict-consistency.test.mjs"
      provides: "the extended SC5 full surface x terminal-kind matrix"
      contains: "structured-human-wait"
  key_links:
    - from: "test/worker/blocked-no-edge-verdict-consistency.test.mjs"
      to: "the worker builders + engine"
      via: "table-driven assertion of BlockerChainResult equality across surfaces"
      pattern: "structured-human-wait"
---

<objective>
Extend the existing SC5 cross-surface consistency guard from 3 cases across 2 surfaces into the FULL matrix: 4 surfaces × 8 terminal kinds, with the new structured-human-wait case asserting it wins over a present agent assignee — the anti-regression that pins the BEAAA-972 fix in place.

Purpose: WAIT-04 is the durable guarantee that "one verdict everywhere" holds for the new structured-wait path. This is the test that fails loudly if any of 17-02's three write sites diverges. Phase 17 SHIPS the matrix test; Phase 20 (HYG-01) wires it into CI — keep it self-contained so Phase 20 only adds an invocation, not a rewrite.
Output: the extended blocked-no-edge-verdict-consistency.test.mjs with the structured-wait case + the widened surface/kind matrix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-CONTEXT.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-RESEARCH.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-PATTERNS.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-02-SUMMARY.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-03-SUMMARY.md

<interfaces>
<!-- Verified from 17-RESEARCH Area 9 + the seed test. -->

Seed test: test/worker/blocked-no-edge-verdict-consistency.test.mjs
  - MATRIX (159-193): 3 cases today — blocked+agent-owned->AWAITING_AGENT_STUCK, blocked+human-owned->AWAITING_HUMAN, blocked+no-owner->UNOWNED.
  - Runs the Reader path (walkBlockerChain -> buildHandlerResult) AND the SR path (buildEmployeesRollup) against ONE synthetic root per case and asserts they agree on terminal.kind.
  - Plus a NOT-blocked regression (232-253) and a NO_UUID_LEAK assertion (259-285).
  - Existing case shape (166-178): { name, root: () => ({ id, identifier, title, status, assigneeAgentId, assigneeUserId, ownerUserId, lastActivityAt }), expectKind }.

The 8 terminal kinds (assert all): AWAITING_HUMAN, AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, SELF_RESOLVING, UNOWNED, EXTERNAL, CYCLE, UNCLASSIFIED.
The 4 surfaces: reader, sr (situation room), bulletin, chat. All four consume the SAME BlockerChainResult — assert verdict-object equality at the producer boundary (cheap; render-level is Phase-20 territory, 17-RESEARCH Open Question 3).

Structured-wait fixture: a synthetic root WITH a clarity_human_waits row (owner=founder) AND an agent assignee present; assert the wait wins (D-07). The applyStructuredWait merge (17-02) injects structuredWaitOwnerUserId into the root nodeMeta; the engine (17-01) emits AWAITING_HUMAN.

Purity guards (must stay green, do NOT modify): test/shared/blocker-chain.test.mjs:287-309 (determinism) + :408-415 (AI-token grep).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the structured-human-wait matrix case (D-07 wins-over-agent)</name>
  <files>test/worker/blocked-no-edge-verdict-consistency.test.mjs</files>
  <read_first>
    - test/worker/blocked-no-edge-verdict-consistency.test.mjs (full file — the MATRIX at 159-193, the case shape at 166-178, the Reader + SR path runners, the NOT-blocked + NO_UUID_LEAK assertions)
    - src/worker/situation/apply-structured-wait.ts (from 17-02 — how the wait is merged into nodeMeta, to construct the fixture)
    - src/shared/blocker-chain.ts (from 17-01 — the structuredWaitOwnerUserId branch the fixture exercises)
    - 17-RESEARCH.md Area 9 (lines 318-326) + 17-PATTERNS.md (the matrix-test section, lines 349-366)
  </read_first>
  <action>
    Add a 4th MATRIX case `structured-human-wait → AWAITING_HUMAN`: a synthetic blocked root that carries BOTH a structured wait (a clarity_human_waits row whose owner = a founder uuid, merged into the root nodeMeta exactly as applyStructuredWait does) AND a non-null assigneeAgentId. Assert the resulting terminal.kind is AWAITING_HUMAN on BOTH the Reader and SR paths (the wait wins over the agent assignee — the D-07 core assertion). Mirror the existing case shape (166-178); feed the wait through the same merge path the builders use so the test exercises the real 17-02 code, not a hand-set field.
  </action>
  <verify>
    <automated>node --test test/worker/blocked-no-edge-verdict-consistency.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The MATRIX includes a `structured-human-wait` case expecting AWAITING_HUMAN with an agent assignee also present.
    - The case asserts AWAITING_HUMAN on both the Reader and SR paths (wait wins over agent ownership, D-07).
    - All four blocked-no-edge classes (agent-owned, human-owned, unowned, structured-wait) are present and each asserts its honest terminal kind (WAIT-03).
    - `node --test test/worker/blocked-no-edge-verdict-consistency.test.mjs` exits 0.
  </acceptance_criteria>
  <done>The structured-wait case proves WAIT-03's 4th class and the D-07 wins-over-agent rule across both existing surfaces.</done>
</task>

<task type="auto">
  <name>Task 2: Widen to 4 surfaces × 8 kinds (WAIT-04 full matrix)</name>
  <files>test/worker/blocked-no-edge-verdict-consistency.test.mjs</files>
  <read_first>
    - test/worker/blocked-no-edge-verdict-consistency.test.mjs (the Reader + SR path runners to generalize to bulletin + chat)
    - src/shared/types.ts:43-51 (the 8-kind Terminal union — the kind axis)
    - 17-RESEARCH.md Area 9 (lines 322-326, the 4-surface × 8-kind extension + the "assert at the verdict-object boundary" recommendation) + Open Question 3
    - test/shared/blocker-chain.test.mjs:287-309 + :408-415 (the purity guards that must stay green — do NOT touch the engine)
  </read_first>
  <action>
    Widen the surface axis from 2 (reader, sr) to 4 by adding the Bulletin and Chat surfaces. Since all four consume the same BlockerChainResult, assert verdict-object equality at the PRODUCER boundary (the recommended cheap encoding, 17-RESEARCH Open Question 3) rather than rendering each UI. Encode table-driven: `for (surface of [reader, sr, bulletin, chat]) for (kind of EIGHT_KINDS) assert sameVerdict` — every surface × every terminal kind reads ONE consistent verdict. Cover all 8 kinds (AWAITING_HUMAN, AWAITING_AGENT_WORKING, AWAITING_AGENT_STUCK, SELF_RESOLVING, UNOWNED, EXTERNAL, CYCLE, UNCLASSIFIED) with a synthetic fixture per kind. Keep the test self-contained (node:test, no external harness) so Phase 20 (HYG-01) only adds a CI invocation. Do NOT add any I/O or AI to blocker-chain.ts — the test exercises the worker builders + engine; the determinism + AI-token guards stay untouched and green.
  </action>
  <verify>
    <automated>node --test test/worker/blocked-no-edge-verdict-consistency.test.mjs && node --test test/shared/blocker-chain.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The test iterates 4 surfaces (reader, sr, bulletin, chat) × 8 terminal kinds and asserts one consistent verdict per cell.
    - `node --test test/worker/blocked-no-edge-verdict-consistency.test.mjs` exits 0.
    - `node --test test/shared/blocker-chain.test.mjs` exits 0 — the determinism + AI-token purity guards remain green (no engine change in this plan).
    - The test is self-contained (node:test only, no external harness) so Phase 20 can wire it into CI with an invocation alone.
  </acceptance_criteria>
  <done>The SC5 guard is a full 4×8 matrix; every surface × terminal kind reads one verdict; engine purity preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → engine/builders | Test-only synthetic data; no production trust boundary. The value is as a regression guard over the real SC5 boundary (the BEAAA-972 divergence class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-13 | Tampering (regression) | SC5 cross-surface verdict consistency | mitigate | The full 4×8 matrix is the standing guard that any future divergence (a write site dropping the merge) fails the build — the anti-regression for the BEAAA-972 bug class. |
| T-17-14 | Tampering | engine purity under test extension | mitigate | The matrix exercises the worker builders + engine only; the determinism + AI-token grep guards (blocker-chain.test.mjs) are asserted green in the same verification — no AI/I/O may enter the engine. |
| T-17-SC | Tampering | npm installs | accept | Zero packages installed (test-only). Residual: none. |

No high-severity threat — this plan adds a regression guard, not a runtime surface.
</threat_model>

<verification>
- The structured-human-wait case asserts AWAITING_HUMAN wins over a present agent assignee on Reader + SR.
- All four blocked-no-edge classes classify truthfully (WAIT-03).
- The matrix spans 4 surfaces × 8 kinds with one consistent verdict per cell (WAIT-04).
- The engine purity guards stay green.
</verification>

<success_criteria>
- WAIT-03: every blocked-no-edge class (incl. structured-wait) classifies truthfully.
- WAIT-04: the SC5 guard is a full surface × terminal-kind matrix, self-contained for Phase 20 CI wiring.
- blocker-chain.ts stays pure.
</success_criteria>

<output>
Create `.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-05-SUMMARY.md` when done.
</output>
