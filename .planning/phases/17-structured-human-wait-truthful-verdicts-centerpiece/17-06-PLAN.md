---
phase: 17-structured-human-wait-truthful-verdicts-centerpiece
plan: 06
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03, 17-04, 17-05]
files_modified:
  - package.json
  - src/manifest.ts
autonomous: false
requirements: [WAIT-01, WAIT-02, WAIT-03, WAIT-04]
user_setup:
  - service: BEAAA (AriClaw DO droplet)
    why: "Live install + drill of the structured-wait centerpiece requires the local tunnel+SSH window and a bookended DO snapshot"
    dashboard_config:
      - task: "Confirm/trigger the automated DO backup (the bookend) before reinstall"
        location: "DigitalOcean droplet backups"
must_haves:
  truths:
    - "The full test suite is green (engine purity + SC5 full matrix + parity)."
    - "The plugin builds clean and the version is bumped in BOTH package.json AND src/manifest.ts to the same literal."
    - "The new build is reinstalled on BEAAA, bookended by a verified DO snapshot."
    - "The BEAAA-972 row reads needs-you EVERYWHERE (Reader, Situation Room, Bulletin, Chat) after the deploy — the centerpiece is live-proven."
    - "No storm: installing/running the new build does NOT drive a CPU storm or multi-agent wake cascade (Phase 16.1 guarantee holds)."
  artifacts:
    - path: "src/manifest.ts"
      provides: "the bumped version literal (must match package.json)"
      contains: "version:"
    - path: "package.json"
      provides: "the bumped version field"
      contains: "version"
  key_links:
    - from: "package.json"
      to: "src/manifest.ts"
      via: "two-source version bump — byte-identical literal"
      pattern: "1\\.6\\.0"
---

<objective>
Ship the centerpiece: full-suite green gate, a two-source version bump (1.5.1 → 1.6.0), a clean rebuild, and a bookended live BEAAA reinstall with a verification drill that the BEAAA-972 row now reads needs-you on every surface — and that the install causes no storm.

Purpose: This is the proof the deep BEAAA-972 fix actually lands in production. Per the autonomous-deploy authorization (daily DO backup + rehearsed Phase-1 restore satisfy the bookended-by-snapshots rule), this runs without pausing — but it needs a LOCAL tunnel+SSH window, so it is not auto-advanceable.
Output: the version bump, the rebuild, the bookended reinstall, and a recorded drill verdict (BEAAA-972 reads needs-you everywhere; no storm).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-CONTEXT.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-RESEARCH.md
@.planning/DEPLOY-RUNBOOK.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-01-SUMMARY.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-02-SUMMARY.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-03-SUMMARY.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-04-SUMMARY.md
@.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-05-SUMMARY.md

<deploy_context>
- Current live version: v1.5.1. This phase ships a NEW feature (the structured-wait centerpiece) → bump to v1.6.0 (minor).
- Two-source version bump (memory plugin-version-bump-two-sources): version lives in BOTH package.json AND the hardcoded literal in src/manifest.ts (line ~645). The host reads dist/manifest.js built from src/manifest.ts. Bump BOTH to 1.6.0 — byte-identical (the kill-switch version-scoping reads the shipped build's version).
- BEAAA deploy mechanics (memory beaaa-deploy-mechanics): AriClaw DO droplet, SSH `ariclaw` → root, Paperclip runs as beai-agent via npx from ~ (NOT /root/paperclip), NO safety-CLI on the box (bookend = DO backup), local-tarball install (extract-dir not tgz; uninstall-then-install). PowerShell here-string CRLF leaks into ssh-bash → scp an LF script + `sed -i 's/\r$//'` (memory phase-16.1-loop07-gap).
- Migration 0018 applies on install (additive, plugin namespace) — the host runs migrations in filename order before worker startup.
- Build/test commands: `pnpm test` (node --test test/**/*.test.mjs), `pnpm typecheck` (tsc --noEmit), `pnpm build` (worker + ui + manifest).
</deploy_context>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite green gate + two-source version bump + rebuild</name>
  <files>package.json, src/manifest.ts</files>
  <read_first>
    - package.json (the version field + the build/test/typecheck scripts)
    - src/manifest.ts (the version literal at ~line 645 — confirm current '1.5.1')
    - .planning/DEPLOY-RUNBOOK.md (the build + two-source-bump steps)
    - 17-RESEARCH.md Pitfall 7 (two-source version bump)
  </read_first>
  <action>
    First run the full gate: `pnpm typecheck` and `pnpm test` must both pass (this exercises 17-01's engine test, 17-05's SC5 full matrix, 17-02's parity test, and the existing determinism + AI-token purity guards). Fix any failures before proceeding. Then bump the version in BOTH package.json (the `version` field) AND src/manifest.ts (the hardcoded literal at ~line 645) from 1.5.1 to 1.6.0 — byte-identical. Then `pnpm build` (worker + ui + manifest) and confirm dist/manifest.js carries the 1.6.0 literal (the host reads the built manifest, not src). Confirm migrations/0018_structured_human_wait.sql is present in the build output / package files so it ships.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm test && grep -q "1.6.0" src/manifest.ts && grep -q "\"version\": \"1.6.0\"" package.json && grep -q "1.6.0" dist/manifest.js</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm typecheck` and `pnpm test` both exit 0 (engine purity, SC5 full matrix, parity, and existing guards all green).
    - The version literal is 1.6.0 in BOTH package.json AND src/manifest.ts (byte-identical).
    - dist/manifest.js (the host-read artifact) carries 1.6.0 after `pnpm build`.
    - migrations/0018_structured_human_wait.sql is included in the shipped package files.
  </acceptance_criteria>
  <done>The suite is green, the version is bumped in both sources, and the build carries the new label + the 0018 migration.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Bookend snapshot + live BEAAA reinstall</name>
  <action>From the LOCAL tunnel+SSH window, take/confirm the bookend DO snapshot, then uninstall-then-install the v1.6.0 extract-dir onto BEAAA as beai-agent per DEPLOY-RUNBOOK + beaaa-deploy-mechanics, confirm migration 0018 applied, and confirm the plugin-list version label reads 1.6.0. The detailed steps are in how-to-verify.</action>
  <what-built>v1.6.0 build with the structured-wait centerpiece (migration 0018, the Editor-Agent populator, the engine branch, the SC5 full matrix) + the Reader fold-ins, ready for the local-tarball install onto BEAAA.</what-built>
  <how-to-verify>
    From the LOCAL tunnel+SSH window (the deploy needs a local window, not cloud):
    1. Confirm the automated DO backup (the bookend) is ON / take a fresh DO snapshot of the AriClaw droplet BEFORE touching the install (bookended-by-snapshots rule; no safety-CLI on the box → DO backup IS the bookend).
    2. Build the local tarball / extract dir per DEPLOY-RUNBOOK + memory beaaa-deploy-mechanics (extract-dir not tgz; uninstall-then-install; scp an LF install script + `sed -i 's/\r$//'` to avoid the PowerShell CRLF→ssh-bash leak).
    3. As beai-agent, uninstall the old clarity-pack and install v1.6.0 via `npx -y paperclipai plugin install <extract-dir>` from ~ (NOT /root/paperclip). Confirm migration 0018 applied (the plugin loads; no validator rejection).
    4. Confirm the plugin-list version label reads 1.6.0.
    Report: snapshot taken (id), install succeeded, 0018 applied, version label = 1.6.0 — or the failure + rollback taken.
  </how-to-verify>
  <resume-signal>Type "installed" with the snapshot id + version confirmation, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live drill — BEAAA-972 reads needs-you everywhere + no storm</name>
  <action>From the LOCAL window, after the Editor-Agent heartbeat compiles, verify the BEAAA-972 row reads needs-you on all four surfaces, the Reader fold-ins render correctly, no storm occurs, and no false structured-wait fired; record the verdict + file a MemPalace drawer. The detailed checks are in how-to-verify.</action>
  <what-built>The structured-wait centerpiece live on BEAAA v1.6.0. The Editor-Agent heartbeat populates the wait row from comments; the engine classifies AWAITING_HUMAN; every surface reads one verdict.</what-built>
  <how-to-verify>
    From the LOCAL window, after the Editor-Agent heartbeat has had a chance to compile (or trigger a compile pass):
    1. Open the BEAAA-972 row (the canonical confusion case) on the Situation Room — confirm it reads NEEDS-YOU (not parked in Watch), showing "<owner> to decide: <one-liner>" in plain English.
    2. Confirm the SAME row reads needs-you consistently on the Reader, Bulletin, and Chat surfaces (SC5 one-verdict-everywhere live).
    3. Confirm the Reader breadcrumb no longer dumps the mission paragraph and its segments do not 404 (D-11/D-12); confirm a ref-card leads plain-English with a demoted id and plain-word status (D-13).
    4. NO-STORM: confirm creating/changing an issue and the Editor heartbeat do NOT drive a CPU storm or multi-agent wake cascade — Clarity worker stays near-idle, wakes within the governor ceiling, no 502 (the Phase 16.1 guarantee must still hold with the new detection path riding the existing heartbeat).
    5. Precision check: confirm no FALSE structured-wait appeared on a row that is not actually awaiting a human decision (D-03 precision posture).
    Report the verdict (PASS/FAIL per check) with the screenshots/evidence. File a MemPalace drawer (clarity_pack/decisions) with the drill outcome.
  </how-to-verify>
  <resume-signal>Type "drill PASS" with evidence, or describe the failing check (which surface, what it read) so a --gaps closure can target it.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local deploy window → live BEAAA host | The install writes the plugin + applies migration 0018 to the live single-tenant filesystem-persistent instance. |
| new build → live agents | The new detection path rides the live Editor heartbeat; a regression could re-introduce the storm class. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-15 | Denial of service | live install / running the new build | mitigate | Bookended by a verified DO snapshot (the rehearsed restore path bounds blast radius at minutes); the no-storm drill (Task 3 step 4) confirms the Phase-16.1 governance still holds with the new detection path before sign-off. |
| T-17-16 | Tampering | migration 0018 on the live DB | mitigate | Additive-only (CREATE TABLE IF NOT EXISTS, plugin namespace) — plugin disable/uninstall preserves data; the host validator gates the migration at install; the snapshot bookend is the rollback. |
| T-17-17 | Spoofing | version-label mismatch (old code under new label) | mitigate | Two-source bump verified (Task 1 + plugin-list label check in Task 2) so the kill-switch version-scoping reads the shipped build's version. |
| T-17-SC | Tampering | npm/install path | mitigate | The deploy is a local-tarball/extract-dir install of the project's own build (no new third-party package); the install script is scp'd as LF and CRLF-stripped (no injection of stray content). Residual: none high. |

No high-severity residual once the snapshot bookend + no-storm drill pass.
</threat_model>

<verification>
- Full suite + typecheck green before bump.
- Version 1.6.0 in both sources + dist/manifest.js.
- Bookend snapshot taken; v1.6.0 installed; 0018 applied; label correct.
- Live drill: BEAAA-972 reads needs-you on all four surfaces; breadcrumb/ref-card fold-ins verified; no storm; no false positive.
</verification>

<success_criteria>
- WAIT-01..04 proven LIVE: the structured wait is captured, classified needs-you, every blocked-no-edge class is truthful, and SC5 holds across all four surfaces on the live BEAAA-972 row.
- The centerpiece ships under the correct version label, bookended, with no storm.
</success_criteria>

<output>
Create `.planning/phases/17-structured-human-wait-truthful-verdicts-centerpiece/17-06-SUMMARY.md` when done.
</output>
