---
phase: 18-no-rabbit-holes-plain-english
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/scrub-human-action.ts
  - src/ui/surfaces/chat/topic-strip.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - test/shared/scrub-human-action.test.mjs
  - test/worker/handlers/flatten-blocker-chain-scrub.test.mjs
  - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs
  - test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs
  - test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs
  - test/shared/rescrub-persisted.test.mjs
autonomous: true
requirements: [LEG-02]
user_setup: []

must_haves:
  truths:
    - "The scrub last-resort fallback emits the literal 'an agent', never agent#<hex> — at all six emission sites"
    - "A real human name/role still resolves first (nameOf) before the 'an agent' fallback"
    - "The NO_UUID_LEAK guard FAILS on agent#<hex{6,}> and bare UUIDs, anchored (not a blanket short-hex rule)"
    - "The previously-blessing tests are inverted to assert agent#<hex> FAILS"
    - "Chat CHT-<8> resolves to the topic title; run·<8> resolves to agent name/role or 'an agent' — zero hex fragments"
    - "Already-persisted leaked strings are re-scrubbed at read-time (rescrubPersisted), additive, zero new DB fetches"
    - "Covers decisions D-08 (chat chips humanized at the UI shared-helper render layer) and D-09 (CHT-<8> → topic title; run·<8> → agent name/role or 'an agent'; never the raw id.slice(0,8) hex slice)"
  artifacts:
    - path: "src/shared/scrub-human-action.ts"
      provides: "Single scrub vocabulary: AGENT_FALLBACK, PARTIAL_HEX_RE, rescrubPersisted, humanizeChatChip"
      contains: "AGENT_FALLBACK"
    - path: "test/shared/scrub-human-action.test.mjs"
      provides: "Inverted guard tests — agent#<hex> asserted to FAIL"
    - path: "test/shared/rescrub-persisted.test.mjs"
      provides: "rescrubPersisted unit + idempotency"
  key_links:
    - from: "src/shared/scrub-human-action.ts"
      to: "AGENT_FALLBACK"
      via: "every fallback emits AGENT_FALLBACK not agent#<hex>"
      pattern: "AGENT_FALLBACK"
    - from: "src/ui/surfaces/chat/topic-strip.tsx"
      to: "humanizeChatChip / AGENT_FALLBACK"
      via: "chtLabel resolves to title, never id.slice(0,8)"
      pattern: "humanizeChatChip|AGENT_FALLBACK"
---

<objective>
LEG-02 — eliminate every raw or partial agent/UUID identifier from human-facing text on all four surfaces. The single-source fix lives in `src/shared/scrub-human-action.ts`: swap the six `agent#${uuid.slice(0,8)}` fallbacks to the literal `'an agent'` (real name/role still resolved first), add the anchored `PARTIAL_HEX_RE` + `rescrubPersisted` read-time pass + `humanizeChatChip`, INVERT the tests that currently bless `agent#<hex>`, extend the anchored NO_UUID_LEAK render-scan guards, and humanize the chat `CHT-<8>` / `run·<8>` chips.

Purpose: plain-English legibility for a non-builder. Live anchor — Reader BEAAA-972 reads "...CEO stuck on agent#04fcac7c is stuck"; post-fix it must read a human name or "an agent" with no hash.
Output: the extended shared module + 4-surface render-scrub + inverted/extended guards + humanized chat chips.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-no-rabbit-holes-plain-english/18-SPEC.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-CONTEXT.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-PATTERNS.md

<interfaces>
<!-- The six fallback emission sites (PATTERNS verified) and the blessing tests. -->

src/shared/scrub-human-action.ts existing exports: UUID_RE, UUID_RE_G (lines 22-23), scrubHumanAction(...).
Six leak sites: :65 :66 (step-2 UNCLASSIFIED double-scrub), :71 (step-3 embedded), :78 (step-4 AWAITING_HUMAN viewer), :86 (step-5 belt-and-suspenders). nameOf(uuid) resolves a real name BEFORE the fallback.

NEW exports to add alongside UUID_RE/UUID_RE_G:
  export const AGENT_FALLBACK = 'an agent';
  export const PARTIAL_HEX_RE = /\bagent#[0-9a-f]{6,}\b/i;
  export function rescrubPersisted(text: string): string;   // replace UUID_RE_G + PARTIAL_HEX_RE → AGENT_FALLBACK; idempotent
  export function humanizeChatChip(...): string;             // D-08/D-09

Blessing tests to INVERT (verified):
  test/shared/scrub-human-action.test.mjs :94 /agent#eeeeeeee/  :123 /agent#dddddddd/  :144 /agent#12345678/
  test/worker/handlers/flatten-blocker-chain-scrub.test.mjs ~:121 includes('agent#')

Chat leaks:
  topic-strip.tsx :82  return id.slice(0,8).toUpperCase()   (chtLabel :78-83; topic.title on ChatTopic :41-45)
  message-thread.tsx :1121  run · {(row.runId ?? '').slice(0,8)}   (run_link row carries runId + title; agent_link row carries row.name — A4: run_link may LACK agent name)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend scrub module + swap fallback + invert guard tests</name>
  <files>src/shared/scrub-human-action.ts, test/shared/scrub-human-action.test.mjs, test/worker/handlers/flatten-blocker-chain-scrub.test.mjs, test/shared/rescrub-persisted.test.mjs</files>
  <read_first>
    - src/shared/scrub-human-action.ts (full — UUID_RE/UUID_RE_G :22-23, the six fallback sites :65/66/71/78/86, doc-comments :33-34/42/44)
    - test/shared/scrub-human-action.test.mjs (Tests 4b/5/6 at :94/:123/:144 — the blessing assertions)
    - test/worker/handlers/flatten-blocker-chain-scrub.test.mjs (~:121 includes('agent#'))
    - .planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md §3 (shared wording helper design) + landmines #4/#5
    - .planning/phases/18-no-rabbit-holes-plain-english/18-PATTERNS.md §"scrub-human-action.ts" (exact emission excerpts)
  </read_first>
  <behavior>
    - scrubHumanAction with an UNRESOLVED uuid (nameOf → null) returns text containing "an agent" and NOT matching /agent#[0-9a-f]{6,}/i (covers AWAITING_AGENT_STUCK, EXTERNAL, belt-and-suspenders cases — the three inverted tests).
    - scrubHumanAction with a RESOLVED uuid (nameOf → "Ada Lovelace") still returns the human name, never "an agent" (resolution-first preserved).
    - PARTIAL_HEX_RE matches "agent#04fcac7c" and "agent#deadbeef12" but NOT a bare git SHA like "deadbeef" or a hex color "#0E0D0A" (anchored to `agent#`, not blanket short-hex — landmine #5).
    - rescrubPersisted("...stuck on agent#04fcac7c is stuck") → "...stuck on an agent is stuck"; rescrubPersisted over already-clean text is a no-op (idempotent); a bare UUID in the string is also replaced.
  </behavior>
  <action>
    Add the new exports to scrub-human-action.ts: AGENT_FALLBACK = 'an agent', PARTIAL_HEX_RE = /\bagent#[0-9a-f]{6,}\b/i, rescrubPersisted (replace UUID_RE_G then PARTIAL_HEX_RE → AGENT_FALLBACK, idempotent), and humanizeChatChip (implemented in Task 2 but exported here as the single vocabulary). Replace every `?? \`agent#${uuid.slice(0,8)}\`` and bare `() => \`agent#...\`` at :65/66/71/78/86 with `?? AGENT_FALLBACK` / `() => AGENT_FALLBACK`; the UNCLASSIFIED double-scrub (:65-66) collapses to one `.replace(UUID_RE_G, (uuid) => nameOf(uuid) ?? AGENT_FALLBACK)`. Update the doc-comments at :33-34/42/44 from "agent#<8> fallback" to "'an agent' fallback / NEVER a raw UUID or partial hash" (landmine #4 — stale comments documenting removed behavior).

    INVERT the blessing tests: scrub-human-action.test.mjs :94/:123/:144 flip from `assert.match(..., /agent#.../)` to `assert.doesNotMatch(out, /agent#[0-9a-f]{6,}/i)` + `assert.match(out, /an agent/)`. Invert flatten-blocker-chain-scrub.test.mjs ~:121 (`includes('agent#')` → asserts NOT included). Create test/shared/rescrub-persisted.test.mjs for the rescrubPersisted behaviors above (incl. idempotency + anchored PARTIAL_HEX_RE non-match on git SHAs). A green suite after only changing runtime code means the inversion was NOT done — the inversion IS the proof (landmine #4).
  </action>
  <acceptance_criteria>
    - `node --test test/shared/scrub-human-action.test.mjs` passes WITH the inverted assertions (the three sites now assert agent#<hex> FAILS + "an agent" present)
    - `node --test test/worker/handlers/flatten-blocker-chain-scrub.test.mjs` passes with the inverted `agent#` assertion
    - `node --test test/shared/rescrub-persisted.test.mjs` passes incl. idempotency and PARTIAL_HEX_RE NOT matching a bare git SHA / hex color
    - grep over src/shared/scrub-human-action.ts finds ZERO `agent#${` template literals remaining; all fallbacks reference AGENT_FALLBACK
  </acceptance_criteria>
  <done>Fallback emits "an agent" (resolution-first preserved); guards inverted; rescrubPersisted + PARTIAL_HEX_RE shipped and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Humanize chat chips (CHT-<8> / run·<8>)</name>
  <files>src/ui/surfaces/chat/topic-strip.tsx, src/ui/surfaces/chat/message-thread.tsx</files>
  <read_first>
    - src/ui/surfaces/chat/topic-strip.tsx (chtLabel :78-83; leak at :82 id.slice(0,8); ChatTopic.title :41-45, topic.employeeAgentId)
    - src/ui/surfaces/chat/message-thread.tsx (run_link case ~:1117-1125; row.runId + row.title; agent_link case carries row.name — A4)
    - src/shared/scrub-human-action.ts (import AGENT_FALLBACK / humanizeChatChip — pure, type-only, UI-safe to import)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-CONTEXT.md D-08/D-09
    - .planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md §"Chat chips (LEG-02d)" + assumption A4
  </read_first>
  <action>
    Implement humanizeChatChip in scrub-human-action.ts (D-08/D-09): given a topic ({title, topicId}) resolve CHT-<8> → topic.title; given a run ({runId, agentName?, title?}) → agentName/role or AGENT_FALLBACK. Keep the legitimate ordinal branches (CHT-\d+ and bare numeric → CHT-NN) in chtLabel; replace ONLY the :82 raw-hex return `id.slice(0,8).toUpperCase()` with the title-resolving path. In message-thread.tsx run_link (~:1121), replace `run · {runId.slice(0,8)}` with a humanized label resolving to the run's agent name/role or AGENT_FALLBACK (never the hex slice); keep the optional ` · {title}` suffix.

    A4 VERIFY STEP: the run_link row in the current payload carries runId + title but NOT an agent name (the agent_link row carries row.name). Inspect the run_link row shape. If the agent name is genuinely absent from the run_link payload, fall back to AGENT_FALLBACK for the run-chip (acceptable per D-09 last-resort) and note in the SUMMARY that the worker-side payload could later carry the agent name (D-08 escape hatch — a worker addition is allowed but only if needed). Do NOT block the chip humanization on a worker change; the leak removal (no raw hex) is the hard requirement.
  </action>
  <acceptance_criteria>
    - grep over topic-strip.tsx and message-thread.tsx finds ZERO `.slice(0, 8)` rendering raw id hex into a chip
    - chtLabel returns topic.title for a non-ordinal topicId (verified by a small unit test or render-string assertion); the CHT-\d+ / numeric ordinal branches are preserved
    - run_link chip renders agent name/role or "an agent" — never `run · <hex>`
    - SUMMARY records the A4 outcome (run_link payload carries agent name? Y/N; if N, AGENT_FALLBACK used)
  </acceptance_criteria>
  <done>Chat CHT-<8> resolves to title; run·<8> resolves to a name/role or "an agent"; zero hex fragments render.</done>
</task>

<task type="auto">
  <name>Task 3: Extend anchored NO_UUID_LEAK render-scan guards + wire read-time re-scrub</name>
  <files>test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs, test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs, test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs</files>
  <read_first>
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs (the source-grep convention; local UUID_RE :29; stripComments)
    - test/ui/surfaces/situation-room/pulse-header-no-uuid-leak.test.mjs
    - test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs
    - src/shared/scrub-human-action.ts (import PARTIAL_HEX_RE so guard + runtime never drift)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md landmine #5 (anchored only — no blanket /[0-9a-f]{8,}/) + LEG-02e (read-time re-scrub, zero new fetches)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-PATTERNS.md §"NO_UUID_LEAK guard" + §"Read-time re-scrub"
  </read_first>
  <action>
    Extend each of the three *-no-uuid-leak.test.mjs guards: in addition to the existing local UUID_RE source-grep, ALSO assert no match of PARTIAL_HEX_RE (`/\bagent#[0-9a-f]{6,}\b/i`) — import PARTIAL_HEX_RE from scrub-human-action.ts so the guard and the runtime use the SAME anchor and can never drift. Do NOT add a blanket `/[0-9a-f]{8,}/` (false-positives on git SHAs, hex colors — landmine #5). Keep the render-scan scoped to verdict/awaited-party/focus/chip strings, not arbitrary DOM text.

    Wire the LEG-02e read-time re-scrub: apply `rescrubPersisted(str)` at each surface's render over strings ALREADY in hand (verdict/awaited-party lines, focus lines, bulletin bodies) — Reader, Situation Room, Bulletin. Chat is covered by Task 2's humanization. The re-scrub is regex over in-memory strings ONLY — ZERO new DB fetches on the snapshot path (landmine #3). Idempotent (re-running over clean text is a no-op). This cleans any historical persisted `agent#<8>` (e.g. tldr_cache.body) at read-time with no destructive migration (additive — coexistence #3/#6).
  </action>
  <acceptance_criteria>
    - `node --test "test/ui/surfaces/**/*-no-uuid-leak.test.mjs"` passes; each guard now also fails-on PARTIAL_HEX_RE and a bare UUID
    - Each guard imports PARTIAL_HEX_RE from scrub-human-action.ts (no locally-redefined drifting anchor)
    - No guard contains a blanket `/[0-9a-f]{8,}/` rule (grep confirms — must not false-positive on git SHAs)
    - The render-time rescrubPersisted calls add ZERO new DB queries (verified: regex over already-fetched strings only — no new ctx.db.query in the changed render paths)
  </acceptance_criteria>
  <done>Anchored guards extended (no SHA false-positives); read-time re-scrub wired on Reader/SR/Bulletin with zero new fetches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted string → DOM | tldr_cache/focus/bulletin strings rendered as React text |
| chat payload labels → chip render | topic.title / agent name substituted into chip labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.02-I | Information Disclosure | agent UUID / partial hash in human text | mitigate | AGENT_FALLBACK replaces every partial-hash fallback; rescrubPersisted re-scrubs persisted leaks at read; anchored NO_UUID_LEAK guard fails the build on any agent#<hex{6,}>/UUID |
| T-18.02-XSS | Injection | label substitution into DOM | mitigate | all substituted labels (title, name, "an agent") render as React text — never dangerouslySetInnerHTML; rescrubPersisted returns a plain string rendered as text (text-not-HTML control) |
| T-18.02-SC | Tampering | npm installs | accept | zero packages added (TS/TSX/mjs edits only); no install task |

No high-severity threats. Text-not-HTML substitution is the XSS control; the anchored guard + AGENT_FALLBACK are the leak controls. The anchored regex specifically avoids the git-SHA false-positive failure mode.
</threat_model>

<verification>
- `node --test test/shared/scrub-human-action.test.mjs` green WITH inverted assertions.
- `node --test test/shared/rescrub-persisted.test.mjs` green (incl. idempotency + anchored non-match on SHAs).
- `node --test "test/ui/surfaces/**/*-no-uuid-leak.test.mjs"` green with PARTIAL_HEX_RE + UUID assertions.
- grep: zero `agent#${` in scrub-human-action.ts; zero `.slice(0, 8)` hex chip renders in chat; no blanket short-hex guard.
- Live: BEAAA-972 Reader reads a human name or "an agent", no hash (carried by Plan 04 drill); a `SELECT ... WHERE body LIKE '%agent#%'` historical row reads clean on next read.
</verification>

<success_criteria>
- One scrub vocabulary owns AGENT_FALLBACK/PARTIAL_HEX_RE/rescrubPersisted/humanizeChatChip.
- Fallback emits "an agent" (resolution-first); guards inverted + anchored; chat chips humanized; persisted strings re-scrubbed at read with zero new fetches.
</success_criteria>

<output>
Create `.planning/phases/18-no-rabbit-holes-plain-english/18-02-SUMMARY.md` when done. Record the A4 outcome (run_link agent-name present? AGENT_FALLBACK used?).
</output>
