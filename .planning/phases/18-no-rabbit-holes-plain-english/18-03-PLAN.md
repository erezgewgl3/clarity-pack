---
phase: 18-no-rabbit-holes-plain-english
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/db/tldr-cache.ts
  - src/worker/situation/build-employees-rollup.ts
  - src/ui/surfaces/situation-room/looks-done-affordance.tsx
  - src/ui/surfaces/reader/index.tsx
  - src/ui/surfaces/situation-room/employee-row.tsx
  - src/shared/looks-done.ts
  - test/shared/looks-done.test.mjs
  - test/worker/db/tldr-bodies-batch.test.mjs
  - test/ui/surfaces/looks-done-affordance.test.mjs
autonomous: true
requirements: [LEG-03]
user_setup: []

must_haves:
  truths:
    - "When the TL;DR done-regex fires AND the engine verdict is blocked-family (needsYou), a 'Looks done — close it?' affordance appears on BOTH the Reader and the SR needs-you row"
    - "The affordance is absent when TL;DR and engine agree, and absent when either input is missing (degrade-safe, no false prompt)"
    - "Clicking opens a confirm ('Close as done' / 'Keep blocked'); it NEVER auto-closes"
    - "The SR-row done signal is a SINGLE batched tldr_cache read scoped to the needs-you set (O(1) queries, not O(rows)), degrade-wrapped"
    - "build-employees-rollup focusLine (polishTldr(title)) is UNCHANGED"
    - "blocker-chain.ts is NOT edited (verdict is read, never computed)"
    - "Covers decisions D-05 (deterministic anchored completion-phrase regex over tldr_cache.body), D-06 (high-precision tuning — fire only on explicit completion phrases), D-07 (divergence = TL;DR-done AND engine verdict in the blocked family; degrade-safe absent when either input missing)"
  artifacts:
    - path: "src/shared/looks-done.ts"
      provides: "Deterministic high-precision completion-phrase regex (D-05/D-06)"
      contains: "export function looksDone"
    - path: "src/worker/db/tldr-cache.ts"
      provides: "Batched getTldrBodiesByScopeIds (= ANY, one query)"
      contains: "getTldrBodiesByScopeIds"
    - path: "src/ui/surfaces/situation-room/looks-done-affordance.tsx"
      provides: "Confirm-gated close affordance (analog: owner-picker-popover)"
      contains: "Close as done"
  key_links:
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "getTldrBodiesByScopeIds"
      via: "ONE batched read keyed by selectAwaitingYouIssueIds, degrade-wrapped"
      pattern: "getTldrBodiesByScopeIds\\("
    - from: "src/ui/surfaces/reader/index.tsx"
      to: "looksDone(data.tldr.body) + panel needsYou"
      via: "done-flag beside TldrStrip; no new Reader DB read"
      pattern: "looksDone\\("
---

<objective>
LEG-03 — surface the honest divergence: when the AI TL;DR reads "done" but the deterministic engine still classifies the item blocked (needsYou), offer a non-destructive "Looks done — close it?" confirm on BOTH the Reader and the SR needs-you row instead of hiding the contradiction. Trigger = a deterministic high-precision completion-phrase regex over `tldr_cache.body` (D-05/D-06) AND a blocked-family verdict (D-07). Degrade-safe: missing TL;DR or missing verdict → no affordance. The SR-row signal is a SINGLE batched `tldr_cache` read scoped to the needs-you set (O(1), degrade-wrapped) — the one new access into the Phase-16-hardened snapshot hot path.

Purpose: honest divergence as an action, never silently inconsistent; confirm-gated by construction (never auto-closes).
Output: the done-regex helper + the batched read + the SR-rollup wiring + the Reader-side signal + the confirm affordance + fixture/perf tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-no-rabbit-holes-plain-english/18-SPEC.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-CONTEXT.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-PATTERNS.md

<interfaces>
<!-- LEG-03 signals already exist; the only NEW data access is one batched tldr_cache read. -->

Engine verdict (read, never compute) — src/shared/types.ts:44-61: BlockerChainResult.needsYou (true for AWAITING_HUMAN / UNOWNED per D-13). DO NOT edit src/shared/blocker-chain.ts (landmine #6: determinism + AI-token grep guards).

Needs-you set (PURE, exists) — src/worker/situation/awaiting-you-selector.ts: selectAwaitingYouIssueIds(rows): string[] (de-duped needs-you issue UUIDs).

Batched read template — src/worker/db/tldr-cache.ts getTldrByScope :97-111 (single-row). New variant mirrors with `WHERE surface = $1 AND scope_id = ANY($2)` + DISTINCT ON (scope_id) ... ORDER BY scope_id, generated_at DESC → Map<scope_id, body>. BRIDGE GOTCHA (file header v0.6.5 Bug 2): text[] params may need `$N::text[]` cast / toPgTextArrayLiteral (see upsertTldr :57-62/:77).

SR rollup — src/worker/situation/build-employees-rollup.ts:384-390 focusLine = polishTldr(focusIssue.title), reads NO tldr_cache today. KEEP UNCHANGED. Add a SEPARATE batched done-flag after the per-employee Promise.all.

Reader — src/ui/surfaces/reader/index.tsx: data.tldr → <TldrStrip> :406 (data.tldr.body in hand at index level); <LiveBlockerPanel issueId={entityId}> :416 owns its needsYou fetch. No new Reader DB read. Wrap affordance in a SectionErrorBoundary like siblings (:405/:415).

Affordance analog — src/ui/surfaces/situation-room/owner-picker-popover.tsx: NORMAL typed React component (NOT a slot-root), open/closed state (:85-87), usePluginAction (:89, situation.assignOwner), UUID-as-dispatch-only prop (:59-66, never rendered). COPY the scaffolding, INVERT the immediate-apply posture (:22) to confirm-gated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deterministic done-regex helper + batched getTldrBodiesByScopeIds</name>
  <files>src/shared/looks-done.ts, src/worker/db/tldr-cache.ts, test/shared/looks-done.test.mjs, test/worker/db/tldr-bodies-batch.test.mjs</files>
  <read_first>
    - src/worker/db/tldr-cache.ts (getTldrByScope :97-111 template; file header bridge gotcha; upsertTldr :57-62/:77 text[] cast pattern)
    - migrations/0002_tldrs_and_editor.sql:35-46 (tldr_cache = body + tags only; NO structured done column — confirms regex, no schema change)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-CONTEXT.md D-05/D-06 (high precision)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md §"LEG-03 trigger" + landmine #1 (one query, not per-row) + assumption A6
  </read_first>
  <behavior>
    - looksDone("This task is done and shipped.") → true; looksDone("Work is complete.") / "...merged" / "...delivered" / "...resolved" → true (explicit completion phrases).
    - looksDone("Almost done, blocked on review.") → false IF the precision tuning excludes hedged phrasing (bias precision per D-06 — tolerate misses over false prompts). looksDone("") / looksDone(null) → false.
    - getTldrBodiesByScopeIds(ctx, 'issue', []) returns an empty Map WITHOUT issuing a query (skip on empty set).
    - getTldrBodiesByScopeIds(ctx, 'issue', [a, b]) issues exactly ONE query (= ANY), returns the most-recent body per scope_id (DISTINCT ON).
  </behavior>
  <action>
    Create src/shared/looks-done.ts exporting `looksDone(body: string | null | undefined): boolean` — an anchored, high-precision regex over completion phrases ("is done / complete / shipped / merged / delivered / resolved"). Tune for precision (D-06): fire only on explicit completion phrasing; tolerate missing some genuine done-but-blocked cases rather than cry wolf. NO schema change, NO AI/LLM token (deterministic — must not pull this regex into blocker-chain.ts; it is a UI/worker concern, landmine #6).

    Add `getTldrBodiesByScopeIds(ctx, surface, scopeIds[]): Promise<Map<string,string>>` to tldr-cache.ts mirroring getTldrByScope but with `WHERE surface = $1 AND scope_id = ANY($2)` + `DISTINCT ON (scope_id) ... ORDER BY scope_id, generated_at DESC`. Return an empty Map immediately on an empty scopeIds array (skip the query). Apply the file's documented text[] bridge handling (`$2::text[]` / toPgTextArrayLiteral per upsertTldr) so ANY binds correctly through the host bridge.

    Tests: test/shared/looks-done.test.mjs covers the behaviors above (precision-biased phrase set + null/empty). test/worker/db/tldr-bodies-batch.test.mjs uses a fake ctx.db.query to assert: empty input → zero queries; non-empty input → exactly ONE query (query-count assertion — the SPEC O(1) acceptance, line 82) returning the DISTINCT-ON most-recent body per scope.
  </action>
  <acceptance_criteria>
    - `node --test test/shared/looks-done.test.mjs` passes (precision-biased phrase matches + null/empty → false)
    - `node --test test/worker/db/tldr-bodies-batch.test.mjs` passes; the query-count assertion proves empty→0 queries, non-empty→exactly 1 query (O(1), not O(rows))
    - getTldrBodiesByScopeIds uses `= ANY` with the documented text[] cast; grep confirms no per-row loop calling getTldrByScope
    - grep over src/shared/looks-done.ts finds no AI/LLM provider token (openai|anthropic|claude|llm|gpt) — deterministic floor preserved. NOTE: do NOT ban the word "completion" — it is this helper's legitimate domain vocabulary ("completion-phrase regex"); the real determinism guard scans `blocker-chain.ts` only, which this file is correctly kept out of
  </acceptance_criteria>
  <done>Deterministic high-precision done-regex + a single-query batched read (degrade-ready), both unit-proven incl. query-count.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the SR-row done-flag (batched, degrade-wrapped) — focusLine untouched</name>
  <files>src/worker/situation/build-employees-rollup.ts</files>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts:360-390 (focusLine area — KEEP UNCHANGED; per-employee Promise.all)
    - src/worker/situation/awaiting-you-selector.ts (selectAwaitingYouIssueIds — pure de-duped needs-you UUIDs)
    - src/worker/db/tldr-cache.ts (getTldrBodiesByScopeIds — from Task 1)
    - src/shared/looks-done.ts (looksDone — from Task 1)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md landmines #1 (one query, not per-row) + #2 (degrade-wrap) + #10 (do NOT fold focusLine-from-tldr rewrite)
  </read_first>
  <action>
    AFTER the per-employee Promise.all rollup (NOT inside the per-row loop — landmine #1): build the needs-you set `const ids = selectAwaitingYouIssueIds(employees)`; issue ONE batched read `getTldrBodiesByScopeIds(ctx, 'issue', ids)` wrapped in try/catch (landmine #2) — a throw or slow read yields an empty Map, the affordance is simply absent, focusLine is untouched, the render never blocks or slows. For each needs-you row where `chain.needsYou === true` AND `looksDone(body)` fires, attach a `looksDone: true` flag to the row.

    KEEP focusLine = polishTldr(focusIssue.title) EXACTLY as today (:384-390) — do NOT fold the superseded focusLine-from-tldr enrichment into this phase (landmine #10). LEG-03 adds a SEPARATE done-flag, not a focusLine source change. The `needsYou` verdict is read from BlockerChainResult — never computed; no blocker-chain.ts edit (landmine #6).
  </action>
  <acceptance_criteria>
    - The done-flag read is a SINGLE call to getTldrBodiesByScopeIds OUTSIDE any per-row loop (grep: no getTldrByScope / getTldrBodiesByScopeIds inside the Promise.all body)
    - The read is wrapped in try/catch; on throw the rollup still returns rows with focusLine intact and no looksDone flag (degrade-safe — covered by an existing or new rollup test that injects a throwing ctx.db)
    - `git diff` shows the focusLine block (:384-390) UNCHANGED
    - blocker-chain.ts is NOT in the diff; `node --test test/shared/blocker-chain.test.mjs` (determinism + AI-token grep) stays green
  </acceptance_criteria>
  <done>SR needs-you rows carry a degrade-wrapped looksDone flag from one batched read; focusLine and the engine untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm-gated "Looks done — close it?" affordance on Reader + SR row</name>
  <files>src/ui/surfaces/situation-room/looks-done-affordance.tsx, src/ui/surfaces/reader/index.tsx, src/ui/surfaces/situation-room/employee-row.tsx, test/ui/surfaces/looks-done-affordance.test.mjs</files>
  <read_first>
    - src/ui/surfaces/situation-room/owner-picker-popover.tsx (full — analog: typed component :28-29, state :85-87, usePluginAction :89, UUID-dispatch-only :59-66, immediate-apply :22 to be INVERTED)
    - src/ui/surfaces/reader/index.tsx (TldrStrip :406, LiveBlockerPanel :416, SectionErrorBoundary siblings :405/:415; data.tldr.body in hand)
    - src/ui/surfaces/situation-room/employee-row.tsx (needs-you row render — where the row carries the looksDone flag from Plan 03 Task 2)
    - src/shared/looks-done.ts (looksDone)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-RESEARCH.md §"Reader side" / §"Confirm-gated" + assumption A7 (close mutation reuses ctx.issues.update; UUID dispatch-only)
    - Skill("sketch-findings-clarity-pack") — warm-dark palette, --alert/--warn accents, "degraded states name themselves"; new class must pass check-css-scope.mjs (scoped under [data-clarity-surface]) + bundle-size gate
  </read_first>
  <action>
    Create src/ui/surfaces/situation-room/looks-done-affordance.tsx as a NORMAL typed React component (NOT a slot-root) copying owner-picker-popover.tsx scaffolding — open/closed local state, outside-click + Esc close, usePluginAction for the host close mutation (reuse the existing issue-update path the SR assign-owner uses, A7), carry the close-target issue UUID as a DISPATCH-ONLY prop (never rendered — NO_UUID_LEAK). INVERT the immediate-apply posture: the affordance shows "Looks done — close it?" and on click opens a confirm with "Close as done" / "Keep blocked"; it NEVER mutates without the explicit "Close as done" selection. Style within the unchanged mockup contract (sketch skill: --alert/--warn divergence accent, Geist Mono meta), scoped under [data-clarity-surface].

    Reader wiring (reader/index.tsx): compute the done-flag from `looksDone(data.tldr.body)` AND the panel's needsYou verdict at index level (lift needsYou up or pass the done-flag into the TldrStrip area) — render the affordance beside the TL;DR, wrapped in a SectionErrorBoundary like its siblings. No new Reader DB read. SR wiring (employee-row.tsx): render the affordance on a needs-you row when the row's `looksDone` flag (from Task 2) is set. Degrade-safe everywhere: no TL;DR body OR no needsYou verdict → affordance absent.

    Test (test/ui/surfaces/looks-done-affordance.test.mjs): fixture where done=true + needsYou=true → affordance present; done+needsYou agree (both false, or done=false) → absent; either input missing → absent; assert the component does NOT dispatch the close mutation without the "Close as done" selection (confirm-gated by construction).
  </action>
  <acceptance_criteria>
    - `node --test test/ui/surfaces/looks-done-affordance.test.mjs` passes: present on (done=true ∧ needsYou=true); absent on agreement; absent on missing input; no mutation without explicit "Close as done"
    - looks-done-affordance.tsx carries the issue UUID as a dispatch-only prop — grep confirms it is never rendered as text (NO_UUID_LEAK)
    - The Reader path adds NO new ctx.db / fetch call (uses data.tldr.body already in hand) — grep the diff
    - Any new CSS class passes `node scripts/check-css-scope.mjs` (scoped under [data-clarity-surface]) and the bundle-size gate
    - The affordance never auto-closes (no close dispatch in a mount/effect path; only on the "Close as done" handler)
  </acceptance_criteria>
  <done>The confirm-gated divergence affordance renders on Reader + SR row when done⊥blocked, is absent otherwise, and never auto-closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin UI → host issue mutation | "Close as done" dispatches ctx.issues.update via usePluginAction |
| batched tldr_cache read → snapshot path | one new DB read into the Phase-16-hardened hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.03-STATE | Tampering | LEG-03 close mutation | mitigate | confirm-gated BY CONSTRUCTION — no close dispatch fires without the explicit "Close as done" selection; no mount/effect auto-close path (a missing confirm = an unintended-state-change threat, eliminated by construction + a test asserting no mutation absent the selection) |
| T-18.03-I | Information Disclosure | close-target issue UUID | mitigate | UUID carried as a dispatch-only prop (owner-picker pattern); never rendered as text; host validates the UUID server-side on ctx.issues.update |
| T-18.03-DOS | Denial of Service | new batched read on snapshot hot path | mitigate | single O(1) `= ANY` query scoped to the needs-you set, degrade-wrapped (throw/slow → drop affordance, focusLine intact); cannot re-introduce the 25.7s cold cliff |
| T-18.03-SC | Tampering | npm installs | accept | zero packages added (TS/TSX/mjs edits only) |

No high-severity threats. The LEG-03 close-confirm gating (never auto-close) and the dispatch-only UUID are the controls; the degrade-wrapped single batched read is the perf/DoS control.
</threat_model>

<verification>
- `node --test test/shared/looks-done.test.mjs` + `test/worker/db/tldr-bodies-batch.test.mjs` + `test/ui/surfaces/looks-done-affordance.test.mjs` green.
- query-count test proves O(1) batched needs-you read; degrade test proves a throwing ctx.db leaves focusLine intact.
- `git diff` shows focusLine :384-390 unchanged and blocker-chain.ts absent; `test/shared/blocker-chain.test.mjs` green.
- `node scripts/check-css-scope.mjs` green for any new class.
- Live (Plan 04 drill): affordance appears on a done-but-blocked item on Reader + SR; absent when agreeing/missing; warm recompute stays < ~500ms with LEG-03 active.
</verification>

<success_criteria>
- Honest divergence surfaced as a confirm-gated action on both surfaces; degrade-safe; never auto-closes.
- The SR-row signal is a single batched O(1) read, degrade-wrapped; focusLine and the deterministic engine untouched.
</success_criteria>

<output>
Create `.planning/phases/18-no-rabbit-holes-plain-english/18-03-SUMMARY.md` when done. Record the completion-phrase set chosen (D-06 precision tuning) and the query-count proof.
</output>
