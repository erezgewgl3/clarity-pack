---
phase: 18-no-rabbit-holes-plain-english
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02, 18-03]
files_modified:
  - package.json
  - src/manifest.ts
autonomous: false
requirements: [LEG-01, LEG-02, LEG-03]
user_setup: []

must_haves:
  truths:
    - "Version is bumped in BOTH package.json AND src/manifest.ts (two-source) — byte-identical literal"
    - "A DO backup/snapshot bookends the install (rollback path verified available)"
    - "Open↗ from each of the 4 surfaces lands on the Clarity Reader in a live BEAAA drill"
    - "BEAAA-972 Reader reads a human name or 'an agent' — no agent#<hex> hash"
    - "Chat surface shows zero CHT-<8>/run·<8> hex fragments"
    - "The 'Looks done — close it?' affordance appears on a done-but-blocked item (Reader + SR) and is absent when agreeing/missing"
    - "Warm recompute stays < ~500ms with LEG-03 active; the needs-you done read issues O(1) batched queries"
    - "A historical persisted agent#<8> row reads clean on the next read (read-time re-scrub)"
  artifacts:
    - path: "package.json"
      provides: "Bumped version (source 1)"
    - path: "src/manifest.ts"
      provides: "Bumped version literal (source 2 — host reads dist/manifest.js)"
  key_links:
    - from: "src/manifest.ts version literal"
      to: "package.json version"
      via: "two-source bump, byte-identical"
      pattern: "version:"
---

<objective>
Ship Phase 18 (LEG-01 + LEG-02 + LEG-03) to live BEAAA behind the deploy bookend, and run the live drill that IS the phase's true acceptance: Open↗ lands on the Reader from every surface, BEAAA-972 reads clean, chat chips are humanized, the divergence affordance appears/absent correctly, and the warm-recompute timing holds.

Purpose: the in-repo tests prove the mechanics; only a live BEAAA drill proves the user-facing guarantees on real data (BEAAA-972, persisted strings, the Phase-16 perf floor).
Output: a two-source version bump + a bookended install + a recorded live drill verdict.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-no-rabbit-holes-plain-english/18-SPEC.md
@.planning/DEPLOY-RUNBOOK.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-01-SUMMARY.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-02-SUMMARY.md
@.planning/phases/18-no-rabbit-holes-plain-english/18-03-SUMMARY.md

<interfaces>
<!-- Two-source bump (landmine #9 / plugin-version-bump-two-sources memory). Host reads dist/manifest.js
     compiled from src/manifest.ts via tsconfig.manifest.json — NOT package.json. -->
package.json: "version": "1.6.0"   → bump
src/manifest.ts:645  version: '1.6.0',   → bump to the SAME literal (byte-identical)

Build/deploy per DEPLOY-RUNBOOK: build-worker + build-ui + `npx tsc --project tsconfig.manifest.json`, then npm pack; install on BEAAA (uninstall-then-install local tarball); confirm `status=ready version=<new>`. Bookend = DO backup/snapshot (the safety-CLI snapshot tooling does NOT apply to this box). Rollback = DO snapshot restore.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-source version bump + build the tarball</name>
  <files>package.json, src/manifest.ts</files>
  <read_first>
    - .planning/DEPLOY-RUNBOOK.md (build sequence; tsconfig.manifest.json; npm pack; status=ready confirmation)
    - package.json (current "version": "1.6.0")
    - src/manifest.ts:645 (version: '1.6.0')
  </read_first>
  <action>
    Bump the version in BOTH package.json AND src/manifest.ts:645 to the SAME byte-identical literal (a v_new bundle with a v_old manifest label is the classic Clarity gotcha — landmine #9; the host reads dist/manifest.js compiled from src/manifest.ts, NOT package.json). Choose the next version per the v1.5.0/v1.6.x trail (Phase 18 is a feature increment — propose e.g. 1.7.0 and confirm with Eric if uncertain). Build per DEPLOY-RUNBOOK: build-worker, build-ui, `npx tsc --project tsconfig.manifest.json`, then `npm pack` to produce the local tarball. Verify dist/manifest.js carries the new literal.
  </action>
  <acceptance_criteria>
    - grep: package.json version === src/manifest.ts version literal (byte-identical)
    - `npm pack` produces a clarity-pack-<new>.tgz; dist/manifest.js contains the new version (grep)
    - The full local test suite is green before packing (`node --test "test/**/*.test.mjs"` — 44+ suites incl. the new/inverted Phase-18 tests)
  </acceptance_criteria>
  <done>Both version sources bumped byte-identically; the tarball is built; dist/manifest.js carries the new label; suite green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Bookend — verify DO backup/snapshot before install</name>
  <files></files>
  <read_first>
    - .planning/DEPLOY-RUNBOOK.md (rollback bookend = DO automated daily backups + operator-taken DO snapshots; safety-CLI does NOT apply to this box)
  </read_first>
  <action>
    Per the bookended-by-snapshots rule (CLAUDE.md): before the install runs against live BEAAA, confirm a DO backup/snapshot exists as the verified rollback path. Automated DO daily backups satisfy the bookend; if an operator-taken pre-install snapshot is preferred, Eric takes one via the DO dashboard (AriClaw → Backups & Snapshots). This is a true human-only step — taking/confirming a DO snapshot has no Clarity CLI.
  </action>
  <acceptance_criteria>
    - A DO backup or snapshot is confirmed available as the rollback bookend immediately before install (recorded date/id).
  </acceptance_criteria>
  <resume-signal>Type "backup confirmed" (with the snapshot date/id) to proceed to install.</resume-signal>
  <done>Rollback bookend confirmed; install may proceed.</done>
</task>

<task type="auto">
  <name>Task 3: Install the new tarball on BEAAA</name>
  <files></files>
  <read_first>
    - .planning/DEPLOY-RUNBOOK.md (Path A scp + install; uninstall-then-install local tarball; status=ready confirmation; pm2 repair note)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-01-SUMMARY.md (the LEG-01 tier chosen — affects what Open↗ does live)
  </read_first>
  <action>
    Deploy the clarity-pack-<new>.tgz to BEAAA per DEPLOY-RUNBOOK (Path A scp+install if SSH works; Path B repo-public web-console route if SSH blocked). Uninstall-then-install the local tarball; confirm `status=ready version=<new>`. Run as the documented beai-agent / npx flow (NOT /root/paperclip). If pm2 lost the paperclip process, repair per the runbook.
  </action>
  <acceptance_criteria>
    - Plugin list shows `key=clarity-pack status=ready version=<new>` on BEAAA (new label, not the prior version)
    - The Situation Room and a Reader issue render (no 502) post-install
  </acceptance_criteria>
  <done>The new build is live on BEAAA at status=ready with the bumped version label.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live BEAAA drill — the phase acceptance</name>
  <files></files>
  <read_first>
    - .planning/phases/18-no-rabbit-holes-plain-english/18-SPEC.md Acceptance Criteria (the 11 checkboxes)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-01-SUMMARY.md (LEG-01 tier — Open↗ destination shape)
    - .planning/phases/18-no-rabbit-holes-plain-english/18-02-SUMMARY.md (A4 chat outcome)
  </read_first>
  <action>
    Run the live drill covering all three requirements' live acceptance. This is the phase's true verdict — the in-repo tests prove mechanics; the drill proves the user-facing guarantees on BEAAA's real data.

    LEG-01: click Open↗ from each of the 4 surfaces (Reader cross-ref, SR row, blocked-backlog expander, Bulletin footer) — each lands on the inline-resolved Clarity Reader (TL;DR/refs visible), not the classic wall. If Plan 01 shipped Tier-2 and the host cannot select the tab, record the actual terminal landing against the SPEC bar and flag any acceptance-criterion shortfall (do not silently pass).
    LEG-02: BEAAA-972 Reader reads a human name or "an agent" — no agent#<hex>; the Chat surface shows zero CHT-<8>/run·<8> hex fragments; run a `SELECT ... WHERE body LIKE '%agent#%'` (A5) and confirm a historical hit reads clean on the next read (read-time re-scrub).
    LEG-03: find/seed a done-but-blocked item (TL;DR done-phrase + engine needsYou) and confirm the "Looks done — close it?" affordance appears on BOTH Reader and the SR needs-you row; confirm it is absent on an agreeing item and on a missing-input item; confirm clicking "Keep blocked" does NOT close.
    PERF: record warm recompute timing (must stay < ~500ms with LEG-03 active) and confirm the needs-you done read issues O(1) batched queries (not O(rows)).
  </action>
  <acceptance_criteria>
    - LEG-01: Open↗ from all 4 surfaces lands on the Clarity Reader (or the Tier-2 terminal landing recorded with any acceptance shortfall flagged)
    - LEG-02: BEAAA-972 reads clean (human name or "an agent", no hash); zero chat hex chips; a persisted agent#<8> row reads clean on next read
    - LEG-03: affordance present on done⊥blocked (Reader + SR), absent on agree/missing, never auto-closes
    - PERF: warm recompute < ~500ms with LEG-03 active; needs-you done read is O(1) batched (query-count observed)
  </acceptance_criteria>
  <what-built>Phase 18 live on BEAAA: re-pointed Open↗, "an agent" scrub + humanized chat chips + read-time re-scrub, and the confirm-gated divergence affordance.</what-built>
  <how-to-verify>
    1. Open↗ from Reader cross-ref / SR row / blocked-backlog expander / Bulletin footer → each lands on the Clarity Reader.
    2. Open BEAAA-972 Reader → no agent#<hex>; open Chat → no CHT-<8>/run·<8> hex.
    3. On a done-but-blocked item → the "Looks done — close it?" affordance shows on Reader + SR; "Keep blocked" does not close; an agreeing/missing item shows no affordance.
    4. Record warm recompute timing and the needs-you done query count.
  </how-to-verify>
  <resume-signal>Type "drill pass" (with timings) or list the failures for gap-closure planning.</resume-signal>
  <done>The live drill verdict is recorded across LEG-01/02/03 + perf; failures (if any) routed to gap-closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → live BEAAA host | a new plugin bundle installed against a production single-tenant box |
| version label → host kill-switch scoping | the manifest version literal scopes the wake-governor version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.04-A | Availability | live install against production | mitigate | bookended-by-snapshot rule — DO backup/snapshot confirmed before install; rollback = DO snapshot restore; additive-only migration (none this phase) |
| T-18.04-T | Tampering | two-source version mismatch | mitigate | byte-identical bump in package.json AND src/manifest.ts; the kill-switch version-scoping reads the manifest literal — a mismatch is a governance hazard, eliminated by the byte-identical check |
| T-18.04-SC | Tampering | npm installs | accept | no new packages this phase; the install is the existing local-tarball plugin install, not a registry fetch |

No high-severity threats beyond the standard production-deploy availability risk, mitigated by the DO-snapshot bookend. The two-source byte-identical bump is the governance control.
</threat_model>

<verification>
- Two-source version bump byte-identical; tarball built; full suite green.
- DO backup bookend confirmed before install; `status=ready version=<new>` on BEAAA.
- Live drill verdict recorded for LEG-01 (4-surface Open↗ → Reader), LEG-02 (BEAAA-972 clean, chat chips, persisted re-scrub), LEG-03 (affordance present/absent/never-auto-close), and perf (warm < ~500ms, O(1) needs-you read).
</verification>

<success_criteria>
- Phase 18 is live on BEAAA with a byte-identical two-source version, bookended by a verified DO rollback path.
- The live drill confirms all three requirements' user-facing guarantees + the perf non-regression; any shortfall is flagged, not absorbed.
</success_criteria>

<output>
Create `.planning/phases/18-no-rabbit-holes-plain-english/18-04-SUMMARY.md` when done. Record the new version, the bookend snapshot id, and the full live-drill verdict (incl. timings + any LEG-01 Tier-2 acceptance note).
</output>
