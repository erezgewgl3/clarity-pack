---
phase: 19-action-cards-async-re-architecture-last-flag-gated
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0019_action_cards_flag.sql
  - src/worker/db/action-cards-flag-repo.ts
  - src/worker/agents/editor.ts
  - src/worker/handlers/situation-room.ts
  - src/worker/agents/action-cards.ts
  - test/worker/db/action-cards-flag-repo.test.mjs
  - test/migrations/0019-validator.test.mjs
autonomous: true
requirements: [CARD-02, CARD-03]
must_haves:
  truths:
    - "A new additive plugin-namespace table action_cards_flag exists; default state is OFF (absent row or enabled=false)."
    - "isActionCardsEnabled returns false when the flag row is absent and false when the read throws (degrade-safe to OFF, D-02)."
    - "isActionCardsEnabled returns true only when a row exists with enabled=true, and is NOT version-scoped (ON survives a version bump, D-01 divergence)."
    - "The two compile-time ACTION_CARDS_ENABLED reads (editor.ts:387, situation-room.ts) are replaced by runtime isActionCardsEnabled reads; with no flag row the room behaves exactly as today (deterministic floor)."
  artifacts:
    - path: "migrations/0019_action_cards_flag.sql"
      provides: "Additive action_cards_flag table (one row per company, default OFF, not version-scoped)"
      contains: "CREATE TABLE IF NOT EXISTS plugin_clarity_pack_cdd6bda4bd.action_cards_flag"
    - path: "src/worker/db/action-cards-flag-repo.ts"
      provides: "Degrade-to-OFF runtime flag read isActionCardsEnabled + setActionCardsEnabled upsert"
      exports: ["isActionCardsEnabled", "setActionCardsEnabled"]
    - path: "test/worker/db/action-cards-flag-repo.test.mjs"
      provides: "absent-row->OFF, unreadable->OFF, ON round-trip coverage"
  key_links:
    - from: "src/worker/agents/editor.ts"
      to: "src/worker/db/action-cards-flag-repo.ts"
      via: "await isActionCardsEnabled(ctx, payload.companyId) replacing the const guard at :387"
      pattern: "isActionCardsEnabled"
    - from: "src/worker/handlers/situation-room.ts"
      to: "src/worker/db/action-cards-flag-repo.ts"
      via: "runtime flag read replaces the ACTION_CARDS_ENABLED import"
      pattern: "isActionCardsEnabled"
---

<objective>
Ship the runtime kill-switch safety primitive for action cards: an additive plugin-namespace
table (`0019_action_cards_flag`), a degrade-to-OFF flag repo (clone of `wake-kill-switch-repo.ts`
with INVERTED polarity and NOT version-scoped), and the swap of the two compile-time
`ACTION_CARDS_ENABLED` const reads to runtime flag reads.

Purpose: This is the safety foundation every later wave gates on. Because the flag defaults OFF
(absent row), this entire plan is INERT in behavior — the room renders exactly as it does today
(deterministic floor) — while establishing the live-flippable OFF/ON control that makes the
two-step enablement (D-08) reversible with no redeploy.
Output: migration 0019, the flag repo + tests, a migration-validator regression test, and the
runtime-read swap at both call sites.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-CONTEXT.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-RESEARCH.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-PATTERNS.md

<interfaces>
<!-- The exact clone template (read it before writing the new repo). -->
Analog repo: src/worker/db/wake-kill-switch-repo.ts
  - isEngaged(ctx, companyId): degrade-to-OFF read, but version-scoped (filters plugin_version).
  - engage(ctx, companyId, reason): atomic upsert against UNIQUE(company_id).
  - The new repo INVERTS polarity (default OFF) and DROPS the plugin_version filter (D-01 divergence).

Migration analog: migrations/0017_loop_governor.sql (the wake_kill_switch block + the legality header).
  - Highest migration on disk is 0018; next is 0019. The 0012 gap is harmless, NOT reused.

Const being promoted: src/worker/agents/action-cards.ts:131 `export const ACTION_CARDS_ENABLED = false`.
Import sites: editor.ts:66 (used at :387), situation-room.ts:85 (used in the compile block ~:606).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Additive migration 0019 + flag repo + tests</name>
  <files>migrations/0019_action_cards_flag.sql, src/worker/db/action-cards-flag-repo.ts, test/worker/db/action-cards-flag-repo.test.mjs, test/migrations/0019-validator.test.mjs</files>
  <read_first>
    - migrations/0017_loop_governor.sql (legality header :1-29; wake_kill_switch CREATE TABLE block :84-95)
    - src/worker/db/wake-kill-switch-repo.ts (full file — clone target)
    - test/worker/agents/action-cards.test.mjs (test-style reference for the new repo test)
    - test/loop/storm-safety.test.mjs (the makeStormCtx SQL-regex fake-db keying pattern, ~:48-90, to model the fake db)
    - any existing ddl-prefix / migration validator regression test referenced by the 0017 header (grep for ddl-prefix-validator to find the runner)
  </read_first>
  <behavior>
    - Test: isActionCardsEnabled with NO matching row returns false (D-02 default OFF).
    - Test: isActionCardsEnabled when ctx.db.query throws returns false (D-02 degrade-safe).
    - Test: isActionCardsEnabled with a row {enabled:true} returns true.
    - Test: isActionCardsEnabled with a row {enabled:false} returns false.
    - Test: the read query contains NO plugin_version filter (assert the SQL string passed to the fake db does not match /plugin_version/) — the D-01 NOT-version-scoped divergence.
    - Test: setActionCardsEnabled issues an UPSERT against UNIQUE(company_id) setting enabled + set_at + set_by (assert the SQL is an INSERT ... ON CONFLICT).
    - Test (migration validator): 0019_action_cards_flag.sql passes the existing ddl-prefix/keyword-scan validator (fully-qualified namespace literal, CREATE TABLE IF NOT EXISTS, inline UNIQUE, apostrophe-free COMMENT) exactly as 0017/0018 do.
  </behavior>
  <action>
Create `migrations/0019_action_cards_flag.sql` by cloning the `wake_kill_switch` block from `0017_loop_governor.sql`. Copy the 0017 legality header verbatim (additive-only / zero public.* DDL / fully-qualified namespace literal / CREATE TABLE IF NOT EXISTS / inline UNIQUE / apostrophe-free COMMENT). Table `plugin_clarity_pack_cdd6bda4bd.action_cards_flag` with columns: `id bigserial PRIMARY KEY`, `company_id text NOT NULL`, `enabled boolean NOT NULL DEFAULT false`, `set_at timestamptz`, `set_by text`, `UNIQUE (company_id)`. NO `plugin_version` column (the D-01 divergence — ON must survive a version bump). Add an apostrophe-free `COMMENT ON TABLE` stating: default OFF (absent row or enabled=false), operator flips ON live with no redeploy, read degrade-safe (unreadable then OFF), NOT version-scoped, additive plugin-namespace table preserves data on disable. Use double-hyphen (`--`) not apostrophes in the comment body per the 0017 legality note.

Create `src/worker/db/action-cards-flag-repo.ts` by cloning `wake-kill-switch-repo.ts`. Export `isActionCardsEnabled(ctx, companyId): Promise<boolean>` — SELECT `enabled` from `plugin_clarity_pack_cdd6bda4bd.action_cards_flag WHERE company_id = $1 LIMIT 1` with NO plugin_version predicate; return `!!rows[0]?.enabled`; the catch block returns `false` (degrade-to-OFF, the inverted polarity vs `isEngaged`'s fail-open). Export `setActionCardsEnabled(ctx, companyId, enabled, setBy)` — atomic UPSERT `INSERT ... (company_id, enabled, set_at, set_by) VALUES ($1, $2, now(), $3) ON CONFLICT (company_id) DO UPDATE SET enabled = $2, set_at = now(), set_by = $3` using parameterized binds only (T-161-01 discipline). Reuse the same `PluginDatabaseClient` ctx type shape as the analog repo. Add a header comment documenting the two divergences from `wake-kill-switch-repo.ts`: (1) inverted polarity — default OFF, catch returns false; (2) NOT version-scoped — drop the plugin_version filter so the operator's ON state survives a two-source version bump (D-01 / Open Q #1 resolution).

Create `test/worker/db/action-cards-flag-repo.test.mjs` modeling a fake `ctx.db` keyed off SQL regex like `makeStormCtx` in storm-safety.test.mjs. Cover all the behavior cases above. For the throw case, make the fake `query` reject and assert `isActionCardsEnabled` resolves false (never rejects).

Create `test/migrations/0019-validator.test.mjs` that runs the existing migration validator (the same one the 0017 header references) against `0019_action_cards_flag.sql` and asserts it is legal. If the validator is exposed as a function, import and call it; if it is a standalone script, invoke it the way the existing migration regression test does (find that test first via grep and mirror its harness).
  </action>
  <verify>
    <automated>node --test test/worker/db/action-cards-flag-repo.test.mjs test/migrations/0019-validator.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - migrations/0019_action_cards_flag.sql exists, creates only the namespaced action_cards_flag table, has no plugin_version column, and passes the migration validator test.
    - isActionCardsEnabled returns false for absent-row, false on throw, true for enabled=true, false for enabled=false; the read SQL contains no plugin_version filter.
    - setActionCardsEnabled emits an INSERT ... ON CONFLICT (company_id) DO UPDATE upsert with parameterized binds only.
    - Both test files pass under `node --test`.
  </acceptance_criteria>
  <done>The flag table + degrade-to-OFF repo exist and are unit-proven; the migration is validator-legal.</done>
</task>

<task type="auto">
  <name>Task 2: Swap compile-time const reads to runtime flag reads (inert at default OFF)</name>
  <files>src/worker/agents/editor.ts, src/worker/handlers/situation-room.ts, src/worker/agents/action-cards.ts</files>
  <read_first>
    - src/worker/agents/action-cards.ts:110-135 (the ACTION_CARDS_ENABLED const + the v1.4.1 hotfix header)
    - src/worker/agents/editor.ts:60-70 (the import) and :378-414 (the heartbeat trigger; the `if (!ACTION_CARDS_ENABLED) return` at :387)
    - src/worker/handlers/situation-room.ts:82-88 (the import) and :584-630 (the compile block + the attach :621-627)
    - src/worker/db/action-cards-flag-repo.ts (the new repo from Task 1)
  </read_first>
  <action>
At `editor.ts`: replace the `ACTION_CARDS_ENABLED` import (currently from `./action-cards.ts` at :66) with an import of `isActionCardsEnabled` from `../db/action-cards-flag-repo.ts`. Replace the const guard at :387 (`if (!ACTION_CARDS_ENABLED) return;`) with `if (!(await isActionCardsEnabled(ctx, payload.companyId))) return;`. The surrounding heartbeat trigger body (:388-414, `buildEmployeesRollup` then `driveActionCardsStep`) is unchanged — that IS the governed pull path and stays for Plan 19-02 to bound. Keep the heartbeat trigger wrapped best-effort exactly as today.

At `situation-room.ts`: replace the `ACTION_CARDS_ENABLED` import at :85 with an import of `isActionCardsEnabled` from `../db/action-cards-flag-repo.ts`. For THIS plan only do the minimal swap: change the `if (ACTION_CARDS_ENABLED && needsYouRows.length > 0)` guard (~:606) to `if ((await isActionCardsEnabled(ctx, companyId)) && needsYouRows.length > 0)`. Do NOT yet delete the on-request compile block — that deletion is Plan 19-02 (CARD-01). At default OFF the guard is false so the block is inert exactly as today. (This keeps wave-1 a pure flag-read swap with zero behavior change; 19-02 owns the request-path removal.)

In `action-cards.ts`: keep the `ACTION_CARDS_ENABLED` const export in place for now (other code/tests may still import it) but update its header comment to note it is superseded at the two call sites by the runtime `isActionCardsEnabled` flag (Phase 19 D-01) and is retained only as a legacy/no-op export pending Plan 19-02 cleanup. Do NOT delete it in this plan (avoids churn with 19-02's deletions).
  </action>
  <verify>
    <automated>node --test test/worker/agents/action-cards.test.mjs && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - editor.ts:387 now reads the runtime flag (`isActionCardsEnabled(ctx, payload.companyId)`); the const import is gone from editor.ts.
    - situation-room.ts gates the compile block on the runtime flag; with no flag row the guard is false (inert, deterministic floor — no behavior change vs today).
    - tsc --noEmit passes; existing action-cards tests stay green.
  </acceptance_criteria>
  <done>Both compile decisions read the runtime flag; default OFF keeps the room behavior identical to today (deterministic floor everywhere).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/RPC → flag write | The flag UPSERT changes a live runtime control; must be parameterized + operator-scoped (the write handler ships in 19-05). |
| worker → plugin Postgres | The flag read/write crosses into the plugin namespace; SQL-injection surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | action-cards-flag-repo SQL | mitigate | Parameterized `$1/$2/$3` binds only; never identifier interpolation (T-161-01 discipline). |
| T-19-02 | Denial of Service | flag read on the snapshot/heartbeat hot path | mitigate | Degrade-to-OFF catch — an unreadable flag returns false (deterministic floor), never wedges the path. |
| T-19-03 | Elevation of Privilege | migration touching public.* | accept/mitigate | Migration is additive plugin-namespace only (zero public.* DDL), validator-enforced by the 0019-validator test. |
</threat_model>

<verification>
- `node --test test/worker/db/action-cards-flag-repo.test.mjs test/migrations/0019-validator.test.mjs test/worker/agents/action-cards.test.mjs` all green.
- `npx tsc --noEmit` clean.
- Determinism + AI-token grep guards on `src/shared/blocker-chain.ts` still green (this plan touches no engine code).
- With no `action_cards_flag` row present, the snapshot handler and editor heartbeat behave exactly as before this plan (deterministic floor; no cards attached).
</verification>

<success_criteria>
- The additive `action_cards_flag` table + degrade-to-OFF repo exist, are unit-proven, and validator-legal.
- Both compile decisions (editor heartbeat, snapshot handler) read the runtime flag; default OFF is inert.
- No version-scoping on the flag read (ON will survive the v1.8.0 bump).
</success_criteria>

<output>
Create `.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-01-SUMMARY.md` when done.
</output>
