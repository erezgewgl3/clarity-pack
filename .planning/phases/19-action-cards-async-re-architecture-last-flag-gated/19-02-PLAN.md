---
phase: 19-action-cards-async-re-architecture-last-flag-gated
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/worker/handlers/situation-room.ts
  - src/worker/db/action-cards-repo.ts
  - src/worker/agents/action-cards.ts
  - test/worker/db/action-cards-repo-batch.test.mjs
  - test/worker/handlers/no-on-request-compile.static.test.mjs
  - test/worker/agents/action-cards.test.mjs
autonomous: true
requirements: [CARD-01]
must_haves:
  truths:
    - "The situation.snapshot DATA handler NEVER calls driveActionCardsStep — it reads cached cards only (D-04, the CARD-01 core)."
    - "Action-card cards are read in batch via getActionCardsBySources (newest-per-source) and attached read-only."
    - "The SWR serve-last-good path strips actionCard from served rows when the flag is OFF (one-row panic-OFF returns to floor with no deploy latency, Pitfall 4/Open Q#3)."
    - "driveActionCardsStep compiles at most DEFAULT_WARM_MAX_ROWS (5) stale rows per heartbeat (bounded-warm, D-06/Pitfall 5)."
    - "A static gate fails the build if any src/worker/handlers/* data handler imports or calls driveActionCardsStep (CARD-01 anti-regression)."
    - "The action-card op-issue mark-done writes are confirmed non-notifying (reuse the 16.1 provenance path; D-07/Pitfall 1) — no second op-issue path is introduced."
  artifacts:
    - path: "src/worker/db/action-cards-repo.ts"
      provides: "Batch newest-per-source cached read getActionCardsBySources"
      exports: ["getActionCardsBySources"]
    - path: "test/worker/handlers/no-on-request-compile.static.test.mjs"
      provides: "Static gate: no driveActionCardsStep in any data handler"
  key_links:
    - from: "src/worker/handlers/situation-room.ts"
      to: "src/worker/db/action-cards-repo.ts"
      via: "cardsBySource = await getActionCardsBySources(ctx, companyId, leafUuids) replacing the compile block"
      pattern: "getActionCardsBySources"
    - from: "src/worker/agents/action-cards.ts"
      to: "DEFAULT_WARM_MAX_ROWS cap"
      via: "compileRows capped to 5 per heartbeat"
      pattern: "DEFAULT_WARM_MAX_ROWS|slice\\(0, 5\\)"
---

<objective>
Deliver CARD-01: take ALL action-card AI work off the HTTP request path. Delete the on-request
compile block in the snapshot handler and repoint the attach to a NEW batch cached read; add the
SWR serve-path flag strip so a panic-OFF flip floors instantly; bound the governed heartbeat
compile to <=5 stale rows; add a static gate that forbids `driveActionCardsStep` in any data
handler; and verify (do not rebuild) that the existing op-issue path is non-notifying.

Purpose: The on-request compile is the exact 502 + BEAAA-2092 storm cause. Removing it is the
heart of CARD-01. The compile that remains rides the already-governed Editor heartbeat
(`startAgentTask` → `recordOwnOperationIssue` + `checkAndRecordWake`) — we bound its fan-out and
prove it is quiet rather than inventing a second path.
Output: read-cached-only snapshot handler, batch repo read + binding probe, bounded-warm cap,
static anti-regression gate, and the non-notifying op-issue verification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-CONTEXT.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-RESEARCH.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-PATTERNS.md

<interfaces>
Single-row analog to clone: getActionCardBySource in src/worker/db/action-cards-repo.ts:92-106.
  - Batch version uses DISTINCT ON (source_issue_id) ... ORDER BY source_issue_id, generated_at DESC.
  - Bind text[] via toPgTextArrayLiteral (already imported at action-cards-repo.ts:23) — same discipline as upsertActionCard's $N::text[] at :68.
  - A2 PROBE REQUIRED: confirm `= ANY($2::text[])` binds through ctx.db.query the same way the INSERT path does.

Snapshot handler edit sites (situation-room.ts):
  - :584-619 on-request compile block → DELETE.
  - :621-627 read-cached attach → KEEP unchanged (consumes cardsBySource[leafUuid]).
  - SWR serve-last-good return ~:718 returns slice.situation_employees → strip actionCard when flag OFF.

Bounded-warm constant: DEFAULT_WARM_MAX_ROWS = 5 lives at editor.ts:440 (the TL;DR warm cap).
Governed path (already wired, DO NOT rebuild): startAgentTask in agent-task-delivery.ts:426-552
  does recordOwnOperationIssue (:498) + checkAndRecordWake (:529); op-issues are
  surfaceVisibility:'plugin_operation' (:473). Mark-done writes to verify: action-cards.ts:510 and :680.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave-0 binding probe + batch cached read getActionCardsBySources</name>
  <files>src/worker/db/action-cards-repo.ts, test/worker/db/action-cards-repo-batch.test.mjs</files>
  <read_first>
    - src/worker/db/action-cards-repo.ts (full file — getActionCardBySource :92-106, upsertActionCard text[] bind :68, ActionCardRow shape, toPgTextArrayLiteral import :23)
    - src/worker/db/tldr-cache.ts (the toPgTextArrayLiteral definition — confirm its output shape)
    - test/loop/storm-safety.test.mjs (makeStormCtx fake-db SQL-regex keying to model the batch-read test)
  </read_first>
  <behavior>
    - A2 PROBE: a fake ctx.db.query records the SQL + params getActionCardsBySources sends; assert it binds the source ids exactly the way upsertActionCard binds text[] (via toPgTextArrayLiteral) — proving `= ANY($2::text[])` is wired consistently. If the probe shows the host bridge cannot bind ANY($2::text[]) the same way, fall back to a per-id query loop or an inlined IN-list built with toPgTextArrayLiteral, and document the chosen binding in the function header.
    - Test: getActionCardsBySources([]) returns {} without querying.
    - Test: given fake rows for two source ids each with two generations, the result maps each source_issue_id to its NEWEST (DISTINCT ON + generated_at DESC) row.
    - Test: the result is a Record keyed by source_issue_id; absent ids are simply missing from the map.
  </behavior>
  <action>
Add `getActionCardsBySources(ctx, companyId, sourceIssueIds): Promise<Record<string, ActionCardRow>>` to `action-cards-repo.ts`, cloning `getActionCardBySource`'s SELECT column list and ctx type. Use `SELECT DISTINCT ON (source_issue_id) <cols> FROM plugin_clarity_pack_cdd6bda4bd.action_cards WHERE company_id = $1 AND source_issue_id = ANY($2::text[]) ORDER BY source_issue_id, generated_at DESC`. Bind `[companyId, toPgTextArrayLiteral(sourceIssueIds)]`. Early-return `{}` for an empty input. Build the output map keyed by `source_issue_id`.

Run the A2 binding probe FIRST (Wave 0). If `= ANY($2::text[])` does not bind cleanly through the host bridge, switch to the documented fallback (per-id queries reusing getActionCardBySource, or an inlined sanitized IN list) and note it in the function header. The downstream caller contract (a `Record<string, ActionCardRow>`) must not change regardless of the binding chosen.
  </action>
  <verify>
    <automated>node --test test/worker/db/action-cards-repo-batch.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - getActionCardsBySources returns {} for empty input and the newest row per source for populated input.
    - The text[] binding probe passes (or the documented fallback is in place with the same return contract).
    - Parameterized binds only; no identifier interpolation.
  </acceptance_criteria>
  <done>A batch newest-per-source cached read exists, is unit-proven, and its text[] binding is verified against the host bridge.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the on-request compile; read-cached-only attach + SWR serve-path flag strip + bounded-warm cap</name>
  <files>src/worker/handlers/situation-room.ts, src/worker/agents/action-cards.ts</files>
  <read_first>
    - src/worker/handlers/situation-room.ts:82-88 (imports), :584-630 (the compile block to delete + the attach :621-627 to keep), :686-740 (SWR serve-last-good return + cache-miss recompute)
    - src/worker/agents/action-cards.ts:120-200 (the hotfix header, ACTION_CARDS_ENABLED const, isActionCardFresh :184, ACTION_CARD_STALE_MS :139), and the compileRows derivation (~:581)
    - src/worker/agents/editor.ts:440 (DEFAULT_WARM_MAX_ROWS = 5)
    - src/worker/db/action-cards-repo.ts (getActionCardsBySources from Task 1)
    - src/worker/db/action-cards-flag-repo.ts (isActionCardsEnabled from 19-01)
  </read_first>
  <action>
In `situation-room.ts`: DELETE the entire on-request compile block at :584-619 (the `needsYouRows` map, the `try`, and the `if (... && needsYouRows.length > 0) { driveActionCardsStep(...) }` call). Remove the now-unused `driveActionCardsStep` / `ActionCardsCtx` / `ActionCardSourceRow` imports at :82-88; keep the `ActionCard` type import. Replace the deleted block with a flag-gated batch read: `let cardsBySource: Record<string, ActionCard> = {};` then `if (await isActionCardsEnabled(ctx, companyId)) { const leafUuids = employees.filter(e => e.blockerChain?.needsYou).map(e => e.blockerChain!.targetIssueUuid ?? e.blockerChain!.leafIssueUuid).filter((x): x is string => !!x); cardsBySource = mapRowsToCards(await getActionCardsBySources(ctx, companyId, leafUuids)); }` — mapping `ActionCardRow → ActionCard` via the existing rowToCard shape used elsewhere in the file/repo, and applying the liveness arm (`isActionCardFresh` age <= ACTION_CARD_STALE_MS) so a long-idle stale card floors out (RESEARCH Pattern 2). KEEP the :621-627 attach unchanged.

SWR serve-path flag strip (Pitfall 4 / Open Q#3): in the SERVE-LAST-GOOD return (~:686-720) the handler returns `slice.situation_employees` directly. Add a single flag read in that serve path; when `isActionCardsEnabled` is false, map `slice.situation_employees` to rows with `actionCard: null` before returning (and before computing needsYou/pulse, which already ignore actionCard). This makes a panic-OFF flip floor the room even when a FRESH cached slice already has cards baked in — the literal "one row back to floor" guarantee. The flag read here degrades to OFF (so a read failure also floors — safe).

Bounded-warm cap (D-06 / Pitfall 5): in `action-cards.ts`, cap the `compileRows` set (~:581) to at most `DEFAULT_WARM_MAX_ROWS` (5) rows per heartbeat invocation — import or re-declare the constant consistently with editor.ts:440. The remaining stale rows compile on subsequent heartbeats (degrade-safe). Add a short comment tying this to D-06.
  </action>
  <verify>
    <automated>node --test test/worker/agents/action-cards.test.mjs && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - situation-room.ts contains NO driveActionCardsStep call/import; cardsBySource comes only from getActionCardsBySources, flag-gated.
    - The SWR serve path strips actionCard (sets null) when the flag is OFF.
    - driveActionCardsStep compiles at most 5 rows per heartbeat (compileRows capped).
    - tsc clean; action-cards tests green.
  </acceptance_criteria>
  <done>The request path does zero AI work on cards; the heartbeat compile is bounded; panic-OFF floors instantly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Static no-on-request-compile gate + non-notifying op-issue verification (extend storm-safety)</name>
  <files>test/worker/handlers/no-on-request-compile.static.test.mjs, test/loop/storm-safety.test.mjs, src/worker/agents/action-cards.ts</files>
  <read_first>
    - test/loop/storm-safety.test.mjs (makeStormCtx :48-90; the existing 16.1 no-wake static gate, if present, as the static-test template — grep for the no-wake static test)
    - src/worker/agents/action-cards.ts:500-520 and :670-690 (the two ctx.issues.update(op.id,{status:'done'}) mark-done sites — A1 / Pitfall 1 notification vector)
    - src/worker/agents/agent-task-delivery.ts:460-552 (startAgentTask: recordOwnOperationIssue :498, checkAndRecordWake :529, surfaceVisibility:'plugin_operation' :473)
  </read_first>
  <behavior>
    - Static gate test: scan every file under src/worker/handlers/ and assert NONE imports or references `driveActionCardsStep` (CARD-01 anti-regression; mirror the 16.1 no-wake static gate). The gate must fail loudly if a future edit re-introduces an on-request compile.
    - Storm-safety extension: simulate a burst of action-card op-issue creates + the two mark-done writes across a simulated worker restart; assert (a) agent wakes stay within the governor ceiling (CLARITY_WAKE_CEILING_PER_MIN), (b) the action-card op-issues are recorded in own_operation_issues (provenance suppression — they cannot re-enter ingress and re-trigger compile), (c) the mark-done writes go through the plugin_operation op-issue path (surfaceVisibility) and do NOT route through any user-notifying write.
    - A1 verification (the headline CARD-01 check): assert in the harness that the mark-done path writes via the plugin_operation op-issue (not a human-board issue update). If the code path as-shipped cannot guarantee non-notifying mark-done by construction, add a code change in action-cards.ts to make the mark-done a status-only / non-notifying write (the 16.1 mechanism) rather than a user-visible issues.update — and cover it with the test.
  </behavior>
  <action>
Create `test/worker/handlers/no-on-request-compile.static.test.mjs`: read all `.ts` files under `src/worker/handlers/`, assert none contains `driveActionCardsStep`. Mirror the 16.1 no-wake static gate's file-scan style (find it via grep and copy its harness).

Extend `test/loop/storm-safety.test.mjs`: add an `action_cards_flag` branch (returning the flag row) and an `action_cards` branch to `makeStormCtx`'s SQL-regex fake-db, then add a test that bursts action-card op-issue create + mark-done writes across a simulated restart and asserts bounded wakes + provenance suppression + non-notifying mark-done (per the behavior cases). This reuses the REAL governor + provenance repo — do NOT mock them.

A1 resolution: if the storm harness (or a targeted unit assertion against the mark-done sites at action-cards.ts:510/:680) shows the mark-done could raise a user "Someone updated" notification, change those writes to the non-notifying 16.1 path (status-only write on the plugin_operation op-issue, the same quiet-write the TL;DR compile uses) and assert the change. If they are already non-notifying by construction (op-issue is plugin_operation off the human board), document that conclusion in the test and the action-cards.ts header — no code change, but the assertion stays as a standing guard. Note in the SUMMARY that the live empirical confirmation happens in the Plan 19-05 Step-1 quiet window (A1 is partly empirical).
  </action>
  <verify>
    <automated>node --test test/worker/handlers/no-on-request-compile.static.test.mjs test/loop/storm-safety.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The static gate fails if any data handler references driveActionCardsStep, and passes for the current tree.
    - The storm-safety burst asserts bounded wakes (within ceiling), provenance suppression of the action-card op-issues, and non-notifying mark-done across a simulated restart.
    - No second op-issue path or bespoke quiet-write mechanism is introduced — only the 16.1 path is used.
  </acceptance_criteria>
  <done>CARD-01 is regression-proofed: no on-request compile possible, and the heartbeat op-issue path is proven non-notifying + governed in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → snapshot handler | Untrusted callers hit the snapshot RPC; it must do ZERO AI/compile work (the 502 + storm surface). |
| Editor op-issue write → host ingress | A self-authored op-issue could re-enter ingress and re-trigger compile (self-trigger storm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04 | Denial of Service | snapshot RPC on-request compile | mitigate | Delete the compile block; static gate forbids re-introduction; request path is read-cached-only. |
| T-19-05 | Denial of Service | self-trigger storm via op-issue ingress | mitigate | own_operation_issues provenance + checkAndRecordWake ceiling (16.1 path, already wired) — asserted in storm-safety. |
| T-19-06 | Tampering | batch read SQL | mitigate | Parameterized binds + toPgTextArrayLiteral for text[]; A2 probe confirms safe binding. |
| T-19-07 | Repudiation/Information | mark-done op-issue notification (BEAAA-2092) | mitigate | Verify (and if needed convert to) non-notifying plugin_operation write; live-confirmed in 19-05 Step-1. |
</threat_model>

<verification>
- `node --test test/worker/db/action-cards-repo-batch.test.mjs test/worker/handlers/no-on-request-compile.static.test.mjs test/loop/storm-safety.test.mjs test/worker/agents/action-cards.test.mjs` all green.
- `npx tsc --noEmit` clean.
- `src/shared/blocker-chain.ts` untouched; determinism + AI-token grep guards green.
- grep confirms zero `driveActionCardsStep` references under `src/worker/handlers/`.
</verification>

<success_criteria>
- The snapshot handler does no AI work on cards (read-cached-only); the static gate enforces it.
- The heartbeat compile is bounded to <=5 rows/heartbeat and rides the existing governed non-notifying op-issue path.
- A panic-OFF flip floors the room even with a fresh cached slice.
</success_criteria>

<output>
Create `.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-02-SUMMARY.md` when done.
</output>
