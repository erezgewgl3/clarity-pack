---
phase: 19-action-cards-async-re-architecture-last-flag-gated
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/worker/handlers/flatten-blocker-chain.ts
  - src/worker/handlers/bulletin-by-cycle.ts
  - src/worker/handlers/chat-active-tasks.ts
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/ui/surfaces/bulletin/action-inbox.tsx
  - src/ui/surfaces/chat/context-rail.tsx
  - test/ui/surfaces/reader-action-card.test.mjs
  - test/ui/surfaces/bulletin-action-card.test.mjs
  - test/ui/surfaces/chat-action-card.test.mjs
  - test/ui/surfaces/no-uuid-leak-surfaces.test.mjs
autonomous: true
requirements: [CARD-02]
must_haves:
  truths:
    - "When the flag is ON and a fresh card exists, the Editor named-action prose renders on needs-you rows on all four surfaces (SR + Reader + Bulletin + Chat), per D-09."
    - "Each new surface falls back to its existing deterministic floor when the card is stale/absent OR the flag is OFF (degrade-safe, D-09/D-10)."
    - "No new surface ever renders sourceIssueUuid or an unscrubbed party — every card display string goes through rescrubPersisted (NO_UUID_LEAK, D-10/Pitfall 2)."
    - "Each surface's worker read path attaches cards read-only via getActionCardsBySources, flag-gated, never compiling on the request path."
  artifacts:
    - path: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      provides: "card.namedAction render with blockerLine(data) deterministic fallback"
    - path: "src/ui/surfaces/bulletin/action-inbox.tsx"
      provides: "card render with the existing awaiting-you deterministic floor"
    - path: "src/ui/surfaces/chat/context-rail.tsx"
      provides: "card render with deterministic fallback"
    - path: "test/ui/surfaces/no-uuid-leak-surfaces.test.mjs"
      provides: "render-scan NO_UUID_LEAK guard across the 3 new surfaces"
  key_links:
    - from: "src/worker/handlers/flatten-blocker-chain.ts"
      to: "src/worker/db/action-cards-repo.ts"
      via: "flag-gated getActionCardsBySources attach threaded to the Reader panel"
      pattern: "getActionCardsBySources"
    - from: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      to: "rescrubPersisted"
      via: "every card display string re-scrubbed at render"
      pattern: "rescrubPersisted"
---

<objective>
Deliver CARD-02's four-surface scope (D-09): wire the cached action-card read into the Reader,
Bulletin, and Chat worker handlers (read-only, flag-gated) and render the Editor named-action
prose on their needs-you rows, each keeping its existing deterministic floor. The Situation Room
already attaches+renders (employee-row.tsx) — this plan brings the other three to parity and
extends the NO_UUID_LEAK render-scan to all three new render paths.

Purpose: The operator explicitly chose the wider four-surface scope over SR-only. The fresh card
widens WHERE plain-English named-action prose appears, without weakening degrade-safety — every
surface still floors to its deterministic line when the card is stale/absent or the flag is OFF.
Output: three worker-handler attaches, three UI renders with deterministic fallback, and the
NO_UUID_LEAK + flag-OFF-floor tests for each.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-CONTEXT.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-RESEARCH.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-PATTERNS.md

<interfaces>
Shared render analog for ALL THREE: src/ui/surfaces/situation-room/employee-row.tsx:374-404
  (card ?? deterministic; rescrubPersisted on card.namedAction + card.awaitedParty; estBucketLabel;
  NEVER passes card.sourceIssueUuid to a render node).

Per-surface deterministic floor + read path:
  - Reader: handler src/worker/handlers/flatten-blocker-chain.ts feeds live-blocker-panel.tsx.
    Floor = blockerLine(data) (live-blocker-panel.tsx :85-127, rendered :356, used as namedAction :374/:439).
    rescrubPersisted imported at live-blocker-panel.tsx:34.
  - Bulletin: handler src/worker/handlers/bulletin-by-cycle.ts feeds action-inbox.tsx.
    Today action-inbox.tsx has NO actionCard/namedAction (a true D-09 gap). Floor = its existing awaiting-you line.
  - Chat: handler src/worker/handlers/chat-active-tasks.ts feeds the needs-you context rail (context-rail.tsx).
    Today reads no card. Floor = its existing deterministic line.

Batch read (from 19-02): getActionCardsBySources(ctx, companyId, sourceIssueIds) in action-cards-repo.ts.
Flag read (from 19-01): isActionCardsEnabled(ctx, companyId) in action-cards-flag-repo.ts (degrade-to-OFF).
NO_UUID_LEAK test template: test/ui/surfaces/.../employee-row-no-uuid-leak.test.mjs (grep to find exact path).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reader four-surface attach + render + tests</name>
  <files>src/worker/handlers/flatten-blocker-chain.ts, src/ui/surfaces/reader/live-blocker-panel.tsx, test/ui/surfaces/reader-action-card.test.mjs</files>
  <read_first>
    - src/worker/handlers/flatten-blocker-chain.ts (full file — the data the Reader panel consumes; where to thread the card)
    - src/ui/surfaces/reader/live-blocker-panel.tsx (blockerLine :85-127, render :356, namedAction use :374/:439, rescrubPersisted import :34, usePluginData :191)
    - src/ui/surfaces/situation-room/employee-row.tsx:374-404 (the card ?? deterministic render to mirror)
    - the SR NO_UUID_LEAK render-scan test (grep employee-row-no-uuid-leak) as the test template
  </read_first>
  <behavior>
    - Worker: when isActionCardsEnabled is true, flatten-blocker-chain attaches the cached card (via getActionCardsBySources on the leaf/target uuid) to its returned payload, read-only, applying the liveness arm so a stale card is dropped. When the flag is OFF, the payload carries no card.
    - UI: when a fresh card is present, live-blocker-panel renders card.namedAction (rescrubbed) in place of / above blockerLine(data); otherwise it renders blockerLine(data) exactly as today.
    - Test: flag OFF -> panel renders blockerLine(data) (floor), no card prose.
    - Test: fresh card present -> panel renders card.namedAction (rescrubbed) + the awaited-party/est line.
    - Test: stale card (liveness arm fails) -> floor.
    - Test (NO_UUID_LEAK): a card whose namedAction/awaitedParty contains a raw uuid/short-hex renders SCRUBBED; sourceIssueUuid never appears in the DOM.
  </behavior>
  <action>
In `flatten-blocker-chain.ts`: add a flag-gated, read-only card attach. Read `isActionCardsEnabled(ctx, companyId)`; when ON, call `getActionCardsBySources(ctx, companyId, [leafOrTargetUuid])` for the chain's leaf/target, apply the `isActionCardFresh` liveness arm, and attach the resulting `ActionCard | null` to the payload field the Reader panel reads (add the field to the payload type). When OFF or stale/absent, attach null. Do NOT compile — read-only only (the CARD-01 invariant extends here; the no-on-request-compile static gate from 19-02 covers this handler too).

In `live-blocker-panel.tsx`: mirror employee-row.tsx:374-404. When a fresh `card` is present, render `rescrubPersisted(card.namedAction)` as the named action and the `waiting on ${rescrubPersisted(card.awaitedParty)}${est}` line; otherwise fall through to the existing `blockerLine(data)` floor (unchanged). NEVER reference `card.sourceIssueUuid` in any render node. Reuse the existing `rescrubPersisted` import (:34) and `estBucketLabel`.

Create `test/ui/surfaces/reader-action-card.test.mjs` covering the four behavior cases, modeled on the SR NO_UUID_LEAK test harness.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/reader-action-card.test.mjs && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - flatten-blocker-chain attaches a card only when the flag is ON and the card is fresh; never compiles.
    - live-blocker-panel renders card.namedAction (rescrubbed) when fresh, else blockerLine(data) floor.
    - No uuid/short-hex/sourceIssueUuid leaks in any rendered card string.
  </acceptance_criteria>
  <done>Reader renders the named-action prose when ON+fresh and the deterministic floor otherwise, scrub-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bulletin + Chat attach + render + tests</name>
  <files>src/worker/handlers/bulletin-by-cycle.ts, src/worker/handlers/chat-active-tasks.ts, src/ui/surfaces/bulletin/action-inbox.tsx, src/ui/surfaces/chat/context-rail.tsx, test/ui/surfaces/bulletin-action-card.test.mjs, test/ui/surfaces/chat-action-card.test.mjs</files>
  <read_first>
    - src/worker/handlers/bulletin-by-cycle.ts (the awaiting-you items the bulletin renders; where to attach the card per item)
    - src/worker/handlers/chat-active-tasks.ts (the needs-you tasks the chat rail renders)
    - src/ui/surfaces/bulletin/action-inbox.tsx (the existing awaiting-you line = the deterministic floor)
    - src/ui/surfaces/chat/context-rail.tsx (the existing needs-you line = the deterministic floor)
    - src/ui/surfaces/situation-room/employee-row.tsx:374-404 (the render mirror)
    - src/worker/db/action-cards-repo.ts (getActionCardsBySources), src/worker/db/action-cards-flag-repo.ts (isActionCardsEnabled)
  </read_first>
  <behavior>
    - Bulletin worker: flag ON -> attach the cached fresh card per awaiting-you item (batch read over the items' leaf/target uuids); OFF/stale -> null.
    - Bulletin UI: fresh card -> render card.namedAction (rescrubbed) + await/est line; else the existing awaiting-you floor.
    - Chat worker: flag ON -> attach the cached fresh card per needs-you task; OFF/stale -> null.
    - Chat UI: fresh card -> render card.namedAction (rescrubbed); else the existing needs-you floor.
    - Tests (both surfaces): flag OFF -> floor; fresh card -> prose; stale -> floor; NO_UUID_LEAK on the card strings.
  </behavior>
  <action>
In `bulletin-by-cycle.ts` and `chat-active-tasks.ts`: add the same flag-gated read-only attach as the Reader handler — `isActionCardsEnabled` gate, `getActionCardsBySources` batch read over the surface's needs-you leaf/target uuids, `isActionCardFresh` liveness arm, attach `ActionCard | null` per item to the payload (extend the payload types). Never compile.

In `action-inbox.tsx` and `context-rail.tsx`: mirror employee-row.tsx:374-404 — `rescrubPersisted(card.namedAction)` (+ await/est for bulletin) when a fresh card is present, else the existing deterministic line unchanged. Never render `card.sourceIssueUuid`. Reuse / import `rescrubPersisted` consistently with the Reader panel.

Create `test/ui/surfaces/bulletin-action-card.test.mjs` and `test/ui/surfaces/chat-action-card.test.mjs` covering the four behavior cases each.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/bulletin-action-card.test.mjs test/ui/surfaces/chat-action-card.test.mjs && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - Bulletin and Chat handlers attach cards only when ON + fresh; never compile.
    - Both UIs render card.namedAction (rescrubbed) when fresh, else their existing deterministic floor.
    - No uuid/short-hex/sourceIssueUuid leaks.
  </acceptance_criteria>
  <done>Bulletin and Chat reach four-surface parity, scrub-safe and degrade-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Consolidated NO_UUID_LEAK + flag-OFF-floor render-scan across the 3 new surfaces</name>
  <files>test/ui/surfaces/no-uuid-leak-surfaces.test.mjs</files>
  <read_first>
    - the SR employee-row-no-uuid-leak render-scan test (grep to find it) — the regex set + DOM-scan harness to extend
    - the three new UI files from Tasks 1-2
  </read_first>
  <behavior>
    - For each of Reader/Bulletin/Chat: feed a card whose namedAction + awaitedParty contain a raw uuid, a partial/short-hex hash, and a sourceIssueUuid; assert the rendered output contains NONE of those tokens (extends the D-10 / LEG-02 NO_UUID_LEAK guard to the three new render paths).
    - For each surface: assert flag-OFF (no card) renders the deterministic floor and zero card prose.
  </behavior>
  <action>
Create `test/ui/surfaces/no-uuid-leak-surfaces.test.mjs` extending the SR NO_UUID_LEAK render-scan pattern (same uuid + short-hex + sourceIssueUuid regex set) to the three new surfaces. This is the single standing guard that a future surface edit cannot leak an identifier (D-10). Include the flag-OFF-floor assertion per surface so CARD-03's "OFF -> floor everywhere" is regression-proofed at the render layer.
  </action>
  <verify>
    <automated>node --test test/ui/surfaces/no-uuid-leak-surfaces.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The render-scan fails the build if any of the three new surfaces renders a uuid/short-hex/sourceIssueUuid in a card string.
    - flag-OFF renders the deterministic floor (no card prose) on all three surfaces.
  </acceptance_criteria>
  <done>NO_UUID_LEAK + flag-OFF-floor are CI-guarded across every new card-render path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted card string → React render | Card text is AI-generated and may contain raw identifiers; the only place new code can leak a UUID (Pitfall 2). |
| request → 3 new surface handlers | Each must stay read-only on cards (no on-request compile leaking back in). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | Information disclosure | new surface card render | mitigate | rescrubPersisted on every display string; sourceIssueUuid never passed to a render node; NO_UUID_LEAK render-scan extended to all 3. |
| T-19-09 | Denial of Service | on-request compile re-introduced via a surface handler | mitigate | All 3 handlers read-only; the 19-02 no-on-request-compile static gate covers src/worker/handlers/*. |
| T-19-10 | Tampering | LLM prompt injection surfacing into card text | accept | sanitizePromptInput already strips instruction-prefix + caps length at compile time (Phase 13); render-time scrub is the second layer. |
</threat_model>

<verification>
- `node --test test/ui/surfaces/reader-action-card.test.mjs test/ui/surfaces/bulletin-action-card.test.mjs test/ui/surfaces/chat-action-card.test.mjs test/ui/surfaces/no-uuid-leak-surfaces.test.mjs` all green.
- `npx tsc --noEmit` clean.
- The 19-02 no-on-request-compile static gate still green (the 3 new handlers are read-only).
- `src/shared/blocker-chain.ts` untouched; determinism + AI-token grep guards green.
</verification>

<success_criteria>
- The Editor named-action prose renders on needs-you rows on all four surfaces when ON+fresh.
- Every surface floors to its deterministic line when stale/absent or OFF.
- No new render path can leak an identifier (CI-guarded).
</success_criteria>

<output>
Create `.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-03-SUMMARY.md` when done.
</output>
