---
phase: 19-action-cards-async-re-architecture-last-flag-gated
plan: 04
type: execute
wave: 4
depends_on: ["19-02", "19-03"]
files_modified:
  - src/worker/handlers/set-action-cards-flag.ts
  - src/worker/index.ts
  - src/manifest.ts
  - test/worker/handlers/set-action-cards-flag.test.mjs
  - test/loop/storm-safety.test.mjs
  - test/worker/agents/action-cards-flag-gate.test.mjs
autonomous: true
requirements: [CARD-03]
must_haves:
  truths:
    - "An RPC handler set-action-cards-flag flips the flag ON/OFF via a parameterized namespaced UPSERT (so the Step-2 enable + panic-OFF are RPC gestures, not psql — BEAAA has no psql)."
    - "CARD-03 OFF-state proof: flag OFF => deterministic floor everywhere (compile decision AND attach decision AND SWR serve path), CI-asserted."
    - "CARD-03 ON-state proof: flag ON + an action-card op-issue burst across a simulated worker restart => bounded wakes (within the governor ceiling), zero self-trigger recursion, no notification storm — via the storm-safety makeStormCtx harness."
    - "The flip handler is operator/admin-scoped like set-opt-in (access control, V4)."
  artifacts:
    - path: "src/worker/handlers/set-action-cards-flag.ts"
      provides: "Operator RPC to flip the action-cards flag ON/OFF (Step-2 enable + panic-OFF)"
      exports: ["registerSetActionCardsFlag"]
    - path: "test/worker/agents/action-cards-flag-gate.test.mjs"
      provides: "flag OFF => floor at every gate; flag ON => cards attach"
  key_links:
    - from: "src/worker/handlers/set-action-cards-flag.ts"
      to: "src/worker/db/action-cards-flag-repo.ts"
      via: "setActionCardsEnabled UPSERT"
      pattern: "setActionCardsEnabled"
    - from: "src/worker/index.ts"
      to: "src/worker/handlers/set-action-cards-flag.ts"
      via: "registerSetActionCardsFlag(ctx) wired into worker setup"
      pattern: "registerSetActionCardsFlag"
---

<objective>
Deliver CARD-03's runtime-safety proof and the redeploy-free flip mechanism: a `set-action-cards-flag`
RPC handler (clone of `set-opt-in.ts`) so the Step-2 ON-flip and panic-OFF are operator RPC gestures
(BEAAA has no `psql`), plus the CI tests that prove OFF => deterministic floor everywhere and ON =>
bounded/quiet (no storm, no 502 surface) using the existing storm-safety harness.

Purpose: CARD-03 is the slip-safety contract — OFF is the known-good floor, ON must be storm-free.
The flip handler makes "flip ONE DB row, room back to floor with zero deploy latency" a literal RPC,
and the storm-safety extension makes the ON-state safety falsifiable in CI before the live drill.
Output: the flip handler + registration + manifest action, and the OFF-floor + ON-no-storm tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-CONTEXT.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-RESEARCH.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-PATTERNS.md

<interfaces>
Flip handler analog: src/worker/handlers/set-opt-in.ts (register + param-guard + namespaced UPSERT :44-63).
  - Register pattern: ctx.actions.register('set-action-cards-flag', async (params) => {...}).
  - Wire into worker setup wherever set-opt-in is registered (grep registerSetOptIn in src/worker/index.ts).
  - Manifest: add the action id alongside set-opt-in (grep 'set-opt-in' in src/manifest.ts).

Flag write: setActionCardsEnabled(ctx, companyId, enabled, setBy) in action-cards-flag-repo.ts (from 19-01).
Storm harness: test/loop/storm-safety.test.mjs makeStormCtx (:48-90) — already extended in 19-02 with
  the action_cards_flag + action_cards fake-db branches. Reuse them here for the ON-burst.
Gate points to assert OFF=floor: editor.ts:387 (compile), situation-room.ts compile+attach+SWR serve (from 19-02).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: set-action-cards-flag RPC handler + registration + manifest action</name>
  <files>src/worker/handlers/set-action-cards-flag.ts, src/worker/index.ts, src/manifest.ts, test/worker/handlers/set-action-cards-flag.test.mjs</files>
  <read_first>
    - src/worker/handlers/set-opt-in.ts (full file — register/param-guard/UPSERT clone target)
    - src/worker/index.ts (where registerSetOptIn is wired — grep registerSetOptIn)
    - src/manifest.ts (the actions/capabilities list where set-opt-in is declared — grep set-opt-in)
    - src/worker/db/action-cards-flag-repo.ts (setActionCardsEnabled from 19-01)
  </read_first>
  <behavior>
    - Test: set-action-cards-flag with {companyId, enabled:true, setBy} performs the ON UPSERT (INSERT ... ON CONFLICT DO UPDATE SET enabled=true) — assert the SQL/params via a fake db.
    - Test: {companyId, enabled:false} performs the OFF write.
    - Test: missing/invalid companyId throws a clear param error (mirror set-opt-in's userId guard).
    - Test: the handler routes its write through setActionCardsEnabled (parameterized; no identifier interpolation).
  </behavior>
  <action>
Create `src/worker/handlers/set-action-cards-flag.ts` cloning `set-opt-in.ts`: export `registerSetActionCardsFlag(ctx)` that registers the `set-action-cards-flag` action. Validate `companyId` (non-empty string) and `enabled` (boolean) from params, throwing a clear error if missing (mirror the set-opt-in userId guard). Call `setActionCardsEnabled(ctx, companyId, enabled, setBy ?? 'operator')`. Scope it operator/admin like set-opt-in (the same capability/visibility set-opt-in uses — match it, do not broaden). Return `{ companyId, enabled }`.

Wire `registerSetActionCardsFlag(ctx)` into `src/worker/index.ts` next to `registerSetOptIn`. Declare the `set-action-cards-flag` action in `src/manifest.ts` alongside `set-opt-in` (same shape/capability). Do NOT bump the version here — the version bump is Plan 19-05.

Create `test/worker/handlers/set-action-cards-flag.test.mjs` covering the four behavior cases with a fake db keyed off SQL regex.
  </action>
  <verify>
    <automated>node --test test/worker/handlers/set-action-cards-flag.test.mjs && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - The handler is registered and declared in the manifest; ON/OFF writes go through setActionCardsEnabled (parameterized).
    - Invalid params throw a clear error; access scope matches set-opt-in.
    - tsc clean.
  </acceptance_criteria>
  <done>The flag is flippable via RPC (Step-2 enable + panic-OFF) with no psql and no redeploy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CARD-03 OFF-floor + ON-no-storm CI tests</name>
  <files>test/worker/agents/action-cards-flag-gate.test.mjs, test/loop/storm-safety.test.mjs</files>
  <read_first>
    - test/loop/storm-safety.test.mjs (makeStormCtx + the action_cards_flag/action_cards branches added in 19-02)
    - src/worker/agents/editor.ts:378-414 (the flag-gated compile trigger)
    - src/worker/handlers/situation-room.ts (the flag-gated attach + SWR serve strip from 19-02)
    - src/worker/db/action-cards-flag-repo.ts (isActionCardsEnabled)
  </read_first>
  <behavior>
    - OFF-floor (CARD-03): with the flag OFF, assert (a) the editor heartbeat compile trigger returns early (no driveActionCardsStep call), (b) the snapshot attach produces cardsBySource = {} so every row floors, (c) the SWR serve path strips actionCard to null. Three gate points, one consistent OFF=floor result (D-03).
    - ON-no-storm (CARD-03): with the flag ON, drive an action-card op-issue burst (reuse the 19-02 storm extension) across a simulated worker restart; assert agent wakes stay within CLARITY_WAKE_CEILING_PER_MIN, zero self-trigger recursion (provenance suppression of the action-card op-issues), and the bounded-warm cap (<=5 compileRows/heartbeat) holds so the queue cannot fan out into a storm. This is the falsifiable "ON yields no 502 / no notification storm" proof (the 502 cause — on-request compile — is gone by construction from 19-02; this proves the heartbeat path stays bounded).
  </behavior>
  <action>
Create `test/worker/agents/action-cards-flag-gate.test.mjs` asserting the OFF-floor behavior at all three gate points (compile trigger, snapshot attach, SWR serve strip) using the flag-repo fake set to OFF, and the ON behavior (cards attach) with the flag set ON.

Extend / consolidate the storm-safety ON-burst assertions in `test/loop/storm-safety.test.mjs`: with the flag ON, a burst of action-card op-issue creates + mark-done writes across a simulated restart asserts bounded wakes + provenance suppression + the bounded-warm cap. (If the burst was already added in 19-02 Task 3, fold the CARD-03 ceiling/bounded-warm assertions into that same test rather than duplicating it.)
  </action>
  <verify>
    <automated>node --test test/worker/agents/action-cards-flag-gate.test.mjs test/loop/storm-safety.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - flag OFF => deterministic floor at all three gate points (compile, attach, SWR serve).
    - flag ON => bounded wakes within the ceiling, zero self-trigger recursion, <=5 compileRows/heartbeat.
    - The on-request compile (the 502 cause) is absent by construction (covered by the 19-02 static gate).
  </acceptance_criteria>
  <done>CARD-03 is falsifiably proven in CI: OFF floors, ON stays storm-free, before the live drill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator RPC → flag flip | A runtime control flip; must be parameterized + operator-scoped. |
| flag ON → heartbeat compile burst | The ON state must stay within the governor ceiling (no storm). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-11 | Elevation of Privilege | set-action-cards-flag RPC | mitigate | Operator/admin scope matching set-opt-in; parameterized UPSERT only. |
| T-19-12 | Denial of Service | flag-ON compile burst | mitigate | checkAndRecordWake ceiling + bounded-warm <=5 + provenance suppression — asserted in storm-safety. |
| T-19-13 | Tampering | flip handler SQL | mitigate | setActionCardsEnabled parameterized binds; no identifier interpolation. |
</threat_model>

<verification>
- `node --test test/worker/handlers/set-action-cards-flag.test.mjs test/worker/agents/action-cards-flag-gate.test.mjs test/loop/storm-safety.test.mjs` all green.
- `npx tsc --noEmit` clean.
- Full `node --test` over `test/` green (the phase CI gate) + determinism + AI-token grep guards on `blocker-chain.ts` green.
</verification>

<success_criteria>
- The flag is flippable via an operator RPC (Step-2 + panic-OFF), no psql, no redeploy.
- OFF => deterministic floor everywhere; ON => bounded, storm-free — both proven in CI.
</success_criteria>

<output>
Create `.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-04-SUMMARY.md` when done.
</output>
