---
phase: 19-action-cards-async-re-architecture-last-flag-gated
plan: 05
type: execute
wave: 5
depends_on: ["19-01", "19-02", "19-03", "19-04"]
files_modified:
  - package.json
  - src/manifest.ts
autonomous: false
requirements: [CARD-01, CARD-02, CARD-03]
user_setup: []
must_haves:
  truths:
    - "Both version sources read v1.8.0 (package.json AND src/manifest.ts) and dist/manifest.js is rebuilt from them (D-12)."
    - "v1.8.0 is reinstalled on BEAAA bookended by a verified DO backup, with the action_cards_flag absent/OFF — the room runs the deterministic floor exactly as today (Step-1 quiet)."
    - "Step-1 quiet window confirms: no 502 on the snapshot, worker CPU ~0%, and NO 'Someone updated' notification from any action-card op-issue (A1 live confirmation, the headline CARD-01 check)."
    - "Step-2: the flag is flipped ON via the set-action-cards-flag RPC in a monitored window with the panic-OFF armed; named-action prose appears on needs-you rows across the four surfaces with no storm and no 502; a panic-OFF returns the room to the floor with no redeploy."
  artifacts:
    - path: "package.json"
      provides: "version 1.8.0"
      contains: "\"version\": \"1.8.0\""
    - path: "src/manifest.ts"
      provides: "version: '1.8.0'"
      contains: "version: '1.8.0'"
  key_links:
    - from: "src/manifest.ts"
      to: "dist/manifest.js"
      via: "clean rebuild (host reads dist/manifest.js)"
      pattern: "1.8.0"
---

<objective>
Ship Phase 19 to BEAAA as the two-step, reversible enablement (D-08): bump both version sources to
v1.8.0, clean rebuild, bookended reinstall with the flag OFF (Step-1 quiet verify), then the
monitored ON-flip drill via the RPC with the kill-switch armed (Step-2). The two steps are SEPARATE
— never couple the unproven re-arch with live enablement (the BEAAA-2092 history mandates it).

Purpose: This is the slip-safe landing of the LAST feature phase. Step-1 proves the re-arch is inert
and quiet (the room still works, no storm, A1 notification-vector confirmed live). Step-2 brings the
named-action prose live with one RPC flip and the panic-OFF one RPC away.
Output: the v1.8.0 two-source bump + the Step-1/Step-2 live drill evidence.

NOTE: This plan has checkpoints (autonomous: false). The deploy + live drill run from a LOCAL window
(tunnel + SSH) per the autonomous-deploy authorization; do NOT pause for deploy approval, but DO pause
at the human-verify checkpoints to confirm the live observations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-CONTEXT.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-RESEARCH.md
@.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-PATTERNS.md

<interfaces>
Two-source version bump (memory plugin-version-bump-two-sources): current on-disk = 1.7.5 in BOTH
  package.json (:3) and src/manifest.ts (:690). Bump BOTH to 1.8.0 (feature minor). Host reads dist/manifest.js.
BEAAA deploy mechanics (memory beaaa-deploy-mechanics): DO droplet via SSH `ariclaw` -> root; Paperclip
  runs as beai-agent via npx from ~ (NOT /root/paperclip); NO safety-CLI and NO psql on the box; deploy =
  local-tarball extract-dir (not tgz) + uninstall-then-install + `plugin enable`; bookend = DO backup
  (the v1.6.0 blank-UI incident lesson: clean reinstall + plugin enable on a restarted host, data preserved).
Step-2 flip RPC: set-action-cards-flag (from 19-04) — the ON enable + panic-OFF gestures (no psql).
Full build: the repo's build script producing dist/manifest.js + dist/worker.js (grep the build script in package.json).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-source version bump to v1.8.0 + clean rebuild + full suite green</name>
  <files>package.json, src/manifest.ts</files>
  <read_first>
    - package.json (:3 version + the build/test scripts)
    - src/manifest.ts (:690 version literal + the set-action-cards-flag action declared in 19-04)
  </read_first>
  <action>
Bump `package.json` `version` from `1.7.5` to `1.8.0` and `src/manifest.ts` `version` from `1.7.5` to `1.8.0` (BOTH sources — the host reads dist/manifest.js built from src/manifest.ts; a one-source bump ships v1.8.0 code with a v1.7.5 label, the documented failure mode). Run the full clean build (the repo's build script) so `dist/manifest.js` and `dist/worker.js` regenerate from the bumped sources. Run the full test suite (`node --test` over `test/`) and the determinism + AI-token grep guards on `src/shared/blocker-chain.ts` — all must be green before deploy.
  </action>
  <verify>
    <automated>grep -c '1.8.0' package.json && grep -c "1.8.0" src/manifest.ts && node --test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both package.json and src/manifest.ts read 1.8.0; dist/manifest.js rebuilt and contains 1.8.0.
    - Full test suite green; blocker-chain.ts determinism + AI-token guards green.
  </acceptance_criteria>
  <done>v1.8.0 is built two-source-consistent with a green suite, ready for the bookended reinstall.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Step-1 — bookended BEAAA reinstall (flag OFF) + quiet verify</name>
  <what-built>
    v1.8.0 built two-source-consistent. The action-card re-arch is shipped but INERT: the
    action_cards_flag row is absent (default OFF), so the room renders the deterministic floor exactly
    as v1.7.5 did. This step deploys it bookended by a verified DO backup and confirms it is quiet.
  </what-built>
  <action>Execute the Step-1 deploy + quiet verification described in <how-to-verify> below: take the DO backup bookend, reinstall v1.8.0 with the action_cards_flag absent (default OFF), and confirm the room is quiet (snapshot 200, CPU ~0%, no action-card notification). This is a human-verified checkpoint — Claude runs the deploy from the local window, then pauses for the operator to confirm the live observations.</action>
  <how-to-verify>
    Deploy from the LOCAL window (tunnel + SSH `ariclaw`) per beaaa-deploy-mechanics:
    1. Confirm/take the DO backup (the bookend) BEFORE touching the live plugin; verify the rehearsed
       restore path is reachable.
    2. Reinstall v1.8.0 as beai-agent (local-tarball extract-dir, uninstall-then-install, `plugin enable`);
       confirm the plugin-list version label reads 1.8.0 and the UI is NOT blank (the v1.6.0 incident check:
       ui-contributions 200 AND index.js 200; worker not crashed on EADDRINUSE:3100).
    3. With NO flag row (default OFF), open the Situation Room and confirm: snapshot returns 200 (no 502),
       the room shows the deterministic floor (no named-action prose — expected, flag OFF), worker CPU ~0%.
    4. HEADLINE A1 CHECK (CARD-01): leave the SR open through at least one Editor heartbeat and confirm
       NO 'Someone updated' notification referencing an action-card op-issue fires. (The compile is OFF at
       the flag, so no op-issue should even be created — confirm the heartbeat trigger returns early. If
       any action-card op-issue notification appears with the flag OFF, STOP — that is a gating bug.)
    Report: version label, snapshot status, CPU, and the notification observation.
  </how-to-verify>
  <resume-signal>Type "step1-quiet" to confirm the OFF deploy is quiet and proceed to Step-2, or describe the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Step-2 — monitored ON-flip drill (kill-switch armed) + panic-OFF rehearsal</name>
  <what-built>
    The set-action-cards-flag RPC (from 19-04) flips the flag ON live with no redeploy. This step turns
    action cards ON in a monitored window with the panic-OFF one RPC away, watches for storm/502, and
    confirms the named-action prose renders across the four surfaces.
  </what-built>
  <action>Execute the Step-2 monitored ON-flip drill described in <how-to-verify> below: flip the flag ON via the set-action-cards-flag RPC with the kill-switch armed, watch wakes/CPU/snapshot for storm or 502, confirm the four-surface named-action prose, and rehearse the panic-OFF. This is a human-verified checkpoint — Claude drives the RPC + monitoring, then pauses for the operator to confirm the live observations and the leave-ON / slip-to-v1.6 decision.</action>
  <how-to-verify>
    In a monitored window (LOCAL tunnel + SSH), kill-switch armed:
    1. Invoke set-action-cards-flag ON for the BEAAA company (the RPC, NOT psql — there is no psql on the box).
    2. Watch worker CPU + the governor wake ceiling (CLARITY_WAKE_CEILING_PER_MIN, default 6/min) for several
       Editor heartbeats: confirm wakes stay within the ceiling, no CPU storm, no multi-agent cascade.
    3. Confirm the snapshot still returns 200 (no 502) under the ON state.
    4. On a needs-you row, confirm the Editor named-action prose (what unblocks this + who + ~when) renders —
       and confirm it renders on all four surfaces (Situation Room, Reader, Bulletin, Chat) where a fresh card
       exists; a brand-new needs-you row may show the deterministic floor until the next heartbeat compiles
       its card (degrade-safe by design, D-06 — expected).
    5. Confirm NO 'Someone updated' notification storm from the action-card op-issues under the ON state
       (the live A1 confirmation under load).
    6. PANIC-OFF REHEARSAL: invoke set-action-cards-flag OFF and confirm the room returns to the deterministic
       floor across all surfaces with NO redeploy (the literal "one row back to floor" guarantee). Decide
       whether to leave it ON (proven) or OFF (slip to v1.6) and record the decision.
    Report: wake counts vs ceiling, CPU, snapshot status, the four-surface prose observation, notification
    observation, and the panic-OFF result.
  </how-to-verify>
  <resume-signal>Type "step2-pass" (cards live, no storm) / "step2-floor-and-slip" (panic-OFF held, slip to v1.6) / or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local deploy → live BEAAA | A misbehaving plugin can hit Paperclip APIs directly (same-origin trust); the DO backup bookend bounds the blast radius. |
| flag ON → live agent population | Turning compile ON could storm the company's agents (the BEAAA-2092 / 2026-06-04 class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-14 | Denial of Service | live ON-flip storm | mitigate | Two-step (OFF-first quiet, then monitored ON); kill-switch armed; panic-OFF rehearsed; governor ceiling. |
| T-19-15 | Repudiation/Information | live 'Someone updated' notification | mitigate | A1 confirmed quiet in Step-1 (OFF) and under load in Step-2 (ON); op-issues are plugin_operation off the human board. |
| T-19-16 | Tampering | wrong-version deploy (one-source bump) | mitigate | Two-source bump asserted (grep both files + dist/manifest.js) before reinstall. |
| T-19-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — Phase 19 installs ZERO external packages (RESEARCH Package Legitimacy Audit: none). No slopcheck checkpoint required. |
</threat_model>

<verification>
- `grep -c '1.8.0' package.json` and `grep -c 1.8.0 src/manifest.ts` both >= 1; dist/manifest.js contains 1.8.0.
- Full `node --test` over `test/` green; determinism + AI-token grep guards on `blocker-chain.ts` green.
- Step-1: live snapshot 200, CPU ~0%, no action-card notification with flag OFF.
- Step-2: wakes within ceiling, snapshot 200, four-surface prose present, panic-OFF returns to floor with no redeploy.
</verification>

<success_criteria>
- v1.8.0 ships two-source-consistent, bookended, with the flag OFF (room unchanged, quiet).
- The monitored ON-flip brings the named-action prose live on the four surfaces with no storm and no 502, and the panic-OFF returns to the deterministic floor with one RPC and zero deploy latency.
</success_criteria>

<output>
Create `.planning/phases/19-action-cards-async-re-architecture-last-flag-gated/19-05-SUMMARY.md` when done.
</output>
