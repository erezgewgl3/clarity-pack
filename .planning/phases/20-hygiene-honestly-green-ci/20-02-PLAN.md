---
phase: 20-hygiene-honestly-green-ci
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - .github/workflows/scaffold-check.yml
autonomous: true
requirements: [HYG-01]
must_haves:
  truths:
    - "The SC5 full surface x terminal-kind matrix (blocked-no-edge-verdict-consistency.test.mjs) actually executes in the CI run, not just locally"
    - "The CI test step recurses into test/worker/** and test/ui/** so the nested worker/ui suites (including the now-fixed watchdog + prefetch tests) run in CI — closing the silent-green gap where ~126 nested tests never ran"
    - "The safety-CLI suite (scripts/safety/test/**) runs in CI honestly green (pass or documented-skip) now that Plan 20-01 provided its deps"
    - "blocker-chain.ts is untouched and the determinism + AI-token grep guards stay green"
  artifacts:
    - path: ".github/workflows/scaffold-check.yml"
      provides: "CI test glob that runs the SC5 matrix + the full nested worker/ui suites + the safety-CLI suite"
      contains: "blocked-no-edge-verdict-consistency"
  key_links:
    - from: ".github/workflows/scaffold-check.yml"
      to: "test/worker/blocked-no-edge-verdict-consistency.test.mjs"
      via: "the CI node --test glob includes the SC5 matrix file"
      pattern: "test/worker"
    - from: ".github/workflows/scaffold-check.yml"
      to: "scripts/safety/test"
      via: "CI runs the safety-CLI suite that Plan 20-01 made loadable"
      pattern: "safety"
---

<objective>
Wire the Phase-17 SC5 full-matrix into CI so the one-verdict-everywhere guarantee is continuously enforced (HYG-01 / D-03), and widen the CI test glob so the nested worker/ui suites (and the safety-CLI suite, now loadable after Plan 20-01) actually run — making the green checkmark honest.

Purpose: Today CI runs only the single-level `test/{shared,worker,ui}/*.test.mjs` glob, which silently SKIPS ~126 nested tests (101 in test/worker/**, 25 in test/ui/**) — including the very watchdog/prefetch tests this phase fixes. A green CI must mean the suite actually ran.
Output: An updated scaffold-check workflow whose test step provably executes the SC5 matrix, the full nested suites, and the safety-CLI suite.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-hygiene-honestly-green-ci/20-CONTEXT.md

<interfaces>
<!-- Investigated during planning — executor should NOT re-explore the glob gap. -->

CI runner = .github/workflows/scaffold-check.yml. Its test step (line 63-64) currently runs a
SINGLE-LEVEL glob over test/shared, test/worker, test/ui. Measured during planning:
  - test/worker top-level             = 30 files (the SC5 matrix
    blocked-no-edge-verdict-consistency.test.mjs IS top-level, so it DOES already run)
  - test/worker nested subdirs        = 101 files (NOT run by CI today)
  - test/ui nested subdirs            = 25 files  (NOT run by CI today)
  - recursive glob test/**/*.test.mjs = 282 files (the full suite)

So CI is green partly because it never runs the nested suites. The U7 flake lives in the NESTED
file test/worker/chat/chat-messages.test.mjs and the prefetch drift lives in the NESTED file
test/worker/situation/snapshot-prefetch.test.mjs — both invisible to CI today.

The package.json `test` script already uses the correct recursive glob (node --test over
test/**/*.test.mjs). Node's `node --test` expands `**` itself (verified: 282 files), so widening
the CI glob to the recursive form is the mechanical fix. The safety-CLI tests live OUTSIDE test/
(scripts/safety/test/**) so they must be added as a SEPARATE glob segment.

Other CI workflows are scoped single-purpose and MUST stay as-is (distinct node-version /
strip-types needs): a11y-check.yml (test/ci/check-a11y.test.mjs), coexistence.yml
(test/ci/coexistence-checklist.test.mjs). Do NOT fold those into scaffold-check.

Watch-out: a recursive test/ glob may now pull in test/visual/* (Playwright sketch regression).
Check whether those self-skip on SKIP_VISUAL; if they need a browser the step does not guarantee,
exclude test/visual explicitly so the step stays green. Prefer the smallest change.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the CI test glob to run the SC5 matrix + the full nested suites + the safety-CLI suite (HYG-01 / D-03)</name>
  <read_first>
    - .github/workflows/scaffold-check.yml (the whole file — esp. the test step at line 63-64)
    - package.json (the `test` script — the recursive glob to mirror)
    - test/worker/blocked-no-edge-verdict-consistency.test.mjs (the SC5 matrix — confirm it is the 4x8 surface x terminal-kind table at top-level test/worker/)
    - test/visual/ (check whether the sketch-regression test self-skips when SKIP_VISUAL/no browser, to decide whether to exclude it from the recursive glob)
  </read_first>
  <action>
    In .github/workflows/scaffold-check.yml, replace the single-level test-step glob with a recursive
    glob covering the full suite plus the out-of-tree safety-CLI suite. The new command runs node --test
    over the recursive test tree (mirror package.json's test/**/*.test.mjs form) AND over
    scripts/safety/test (the safety-CLI tests live outside test/). Rename the step so the coverage is
    explicit (e.g. mention "full recursive suite incl. SC5 matrix + safety-CLI"). Add a short inline
    comment noting the recursive glob is REQUIRED so the SC5 full surface x terminal-kind matrix and the
    nested worker/ui suites run in CI — the HYG-01 honest-green guard. If the recursive glob would pull
    in test/visual sketch tests that need a browser the step does not install, exclude test/visual
    explicitly (or rely on its self-skip if confirmed) so the step stays green. Leave the Playwright
    Chromium install step and every other step untouched. Do NOT alter a11y-check.yml or
    coexistence.yml. Do NOT touch any src/ file or blocker-chain.ts (D-07).
  </action>
  <verify>
    <automated>grep -q "test/\*\*/\*.test.mjs" .github/workflows/scaffold-check.yml && grep -q "scripts/safety/test" .github/workflows/scaffold-check.yml && echo "CI glob widened OK"</automated>
  </verify>
  <acceptance_criteria>
    - The scaffold-check test step uses a recursive glob over test/ (string `test/**/*.test.mjs` present) AND includes a `scripts/safety/test` segment.
    - The SC5 matrix file is covered by the glob (it is under test/worker, matched by the recursive form).
    - a11y-check.yml and coexistence.yml are unchanged (`git diff --name-only` lists only scaffold-check.yml for this task).
    - No src/ file changed; blocker-chain.ts untouched.
  </acceptance_criteria>
  <done>The CI test step provably runs the SC5 matrix, the full nested worker/ui suites, and the safety-CLI suite.</done>
</task>

<task type="auto">
  <name>Task 2: Honest-green sweep verification + invariant guard</name>
  <read_first>
    - .github/workflows/scaffold-check.yml (the edited test step from Task 1)
    - scripts/ (check for an existing determinism / AI-token grep guard script the project already runs, e.g. check-css-scope.mjs is run in CI — find the analogous blocker-chain purity guard if one exists)
    - src/shared/blocker-chain.ts (read-only — to confirm it is NOT in any plan's files_modified and stays byte-identical)
  </read_first>
  <action>
    Run the FULL local sweep exactly as CI now will (the recursive test/ glob + scripts/safety/test)
    and confirm it is honestly green: every test either passes or is counted under a node:test
    `skipped N` with a logged reason (no silent red, no silent truncation — the no-silent-caps
    invariant). Confirm the SC5 matrix subtests appear in the run (grep the run output for the matrix
    describe/test names). Confirm `pnpm typecheck` is clean. Confirm the determinism + AI-token grep
    guards still pass (run whatever guard the repo already ships — e.g. the blocker-chain purity /
    no-AI-token grep — do NOT invent a new one; if none is a standalone script, assert via grep that
    src/shared/blocker-chain.ts contains no AI-token imports and was not modified this phase). Record
    the final counts (pass/skip/fail) and the list of any documented-skips in the SUMMARY. Make NO code
    changes in this task — it is a verification + evidence-capture task; if the sweep surfaces a NEW
    red that Plan 20-01 did not cover, STOP and report it (do not silently fix or skip).
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs" "scripts/safety/test/**/*.test.mjs"</automated>
  </verify>
  <acceptance_criteria>
    - The full recursive sweep reports `fail 0`; any `skipped N` entries each carry a documented reason.
    - The SC5 matrix subtests are present in the run output (matrix actually executed).
    - `pnpm typecheck` is clean.
    - src/shared/blocker-chain.ts is byte-identical to its pre-phase state (`git diff --stat src/shared/blocker-chain.ts` shows no change) and contains no AI-token imports.
    - No new untracked red surfaced; if one did, it is reported, not hidden.
  </acceptance_criteria>
  <done>The full sweep CI will run is honestly green end-to-end, the SC5 matrix is proven to execute, and the blocker-chain purity invariant holds.</done>
</task>

</tasks>

<verification>
- The scaffold-check test step uses the recursive test/ glob + the scripts/safety/test segment.
- `node --test "test/**/*.test.mjs" "scripts/safety/test/**/*.test.mjs"` → `fail 0`, skips documented.
- SC5 matrix subtests appear in the run output.
- `pnpm typecheck` clean; blocker-chain.ts unchanged; determinism + AI-token guards green.
- Only .github/workflows/scaffold-check.yml changed by this plan (Task 2 is verification-only).
</verification>

<success_criteria>
HYG-01 satisfied: the SC5 full-matrix runs in CI as a standing guard, the previously-silent nested suites now run, and the full sweep is honestly green (pass or documented-skip, no silent red).
</success_criteria>

<output>
Create `.planning/phases/20-hygiene-honestly-green-ci/20-02-SUMMARY.md` when done. Record the final sweep counts (pass/skip/fail), the documented-skip list, proof the SC5 matrix executed, and confirmation blocker-chain.ts is unchanged.
</output>
