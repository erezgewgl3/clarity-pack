---
phase: 20-hygiene-honestly-green-ci
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [HYG-04]
must_haves:
  truths:
    - "The plugin version label is verified = 1.8.0 in BOTH sources (package.json AND src/manifest.ts) — already bumped in Phase 19, NOT re-bumped here"
    - "The operator confirms in the DigitalOcean dashboard that automated daily backups are ON for the AriClaw droplet — the continuous-deploy rollback bookend prerequisite"
  artifacts: []
  key_links:
    - from: "package.json version"
      to: "src/manifest.ts version"
      via: "two-source version label must agree (1.8.0)"
      pattern: "1.8.0"
---

<objective>
Close HYG-04: verify (do NOT re-bump) that the two-source version label is 1.8.0, and capture the operator-gated confirmation that automated DO backups are ON for the AriClaw droplet — the rollback bookend that makes continuous flag-gated BEAAA deploy safe.

Purpose: D-02 — Phase 19 already bumped both version sources to 1.8.0, so the version arm is satisfied by construction; only the live DO-backup confirmation remains. That confirmation is an operator action in the DigitalOcean dashboard (Claude cannot read the DO control panel), so it is a blocking human checkpoint, BATCHED with the end-of-milestone operator window alongside Phase 19's live deploy — it must NOT block the autonomous CI/test work in Plans 20-01/20-02.
Output: A recorded version-label verification + an operator-confirmed DO-backup state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-hygiene-honestly-green-ci/20-CONTEXT.md
@.planning/DEPLOY-RUNBOOK.md

<interfaces>
<!-- Investigated during planning. -->

Version label (HYG-04 D-02): ALREADY 1.8.0 in both sources (verified during planning):
  - package.json `version` = 1.8.0
  - src/manifest.ts line 702: version literal '1.8.0'
Per the two-sources memory, the host reads dist/manifest.js (built from src/manifest.ts), NOT
package.json — both must agree. They do. This plan VERIFIES; it does NOT bump (D-02 — no re-bump).
A surprise bump would also silently revert the action-cards flag (the action_cards_flag repo is
deliberately NOT version-scoped) — another reason to verify-only.

DO backup bookend (DEPLOY-RUNBOOK.md line 13/26): the live host is the DigitalOcean droplet
"AriClaw" (NOT Hostinger/Countermoves). The safety-CLI snapshot tooling does NOT apply to this box;
"DO automated daily backups + operator-taken DO snapshots" ARE the rollback path. Confirming
"automated daily backups ON" is a read of the DO dashboard (Droplets -> AriClaw -> Backups tab) —
an operator action; Claude has no DO control-panel access. This is the only remaining HYG-04 work.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the two-source version label is 1.8.0 (D-02 — verify, do NOT bump)</name>
  <read_first>
    - package.json (the `version` field)
    - src/manifest.ts (the version literal, ~line 702)
  </read_first>
  <action>
    Confirm WITHOUT modifying anything that the plugin version label is 1.8.0 in BOTH sources:
    package.json `version` and the hardcoded version literal in src/manifest.ts. Per D-02 this was
    already bumped by Phase 19 — this task is a read-only verification, NOT a re-bump. If (and only if)
    the two sources DISAGREE or either is not 1.8.0, STOP and report the discrepancy to the operator —
    do not silently bump (a surprise bump would silently revert the action-cards flag per the
    not-version-scoped repo note, and contradicts D-02). Record the verified value in the SUMMARY.
  </action>
  <verify>
    <automated>node scripts/check-version-pin.mjs || node -e "const p=require('./package.json'); const m=require('fs').readFileSync('src/manifest.ts','utf8'); const mv=(m.match(/version:\s*'([^']+)'/)||[])[1]; if(p.version==='1.8.0'&&mv==='1.8.0'){console.log('two-source version label OK: 1.8.0');}else{console.error('version mismatch pkg='+p.version+' manifest='+mv);process.exit(1);}"</automated>
  </verify>
  <acceptance_criteria>
    - package.json `version` === "1.8.0".
    - src/manifest.ts version literal === "1.8.0".
    - Neither file was modified by this task (`git diff --name-only` empty for package.json and src/manifest.ts).
  </acceptance_criteria>
  <done>The two-source version label is confirmed 1.8.0 with no re-bump.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator confirms automated DO backups are ON for AriClaw (HYG-04 bookend)</name>
  <what-built>
    Plans 20-01 and 20-02 made the test suite + CI honestly green (autonomous). The remaining HYG-04
    arm is the deploy-bookend confirmation: that automated daily backups are enabled for the live
    AriClaw DigitalOcean droplet — the rehearsed rollback path that makes continuous flag-gated BEAAA
    deploy safe (per DEPLOY-RUNBOOK.md: the safety-CLI snapshot tooling does NOT cover this box; DO
    automated backups + operator DO snapshots are the rollback). Claude cannot read the DO control
    panel, so this is an operator confirmation, batched into the end-of-milestone operator window.
  </what-built>
  <how-to-verify>
    1. Open the DigitalOcean dashboard → Droplets → AriClaw.
    2. Click the "Backups" tab (or Settings → Backups).
    3. Confirm "Automated Backups" (the weekly/daily backup add-on) is ENABLED and shows a recent
       successful backup (within the expected cadence).
    4. If it is OFF or has no recent successful backup: enable it (or trigger/await the first backup)
       before approving — the bookend is a hard prerequisite for continuous deploy.
    5. Note the most-recent backup date/time so the SUMMARY can record the confirmed state.
  </how-to-verify>
  <resume-signal>Type "approved — DO backups ON (last backup <date>)" or describe what was OFF/missing so it can be enabled.</resume-signal>
</task>

</tasks>

<verification>
- package.json + src/manifest.ts both pin 1.8.0 (no re-bump, no file change).
- Operator confirms automated DO backups ON for AriClaw with a recent successful backup.
</verification>

<success_criteria>
HYG-04 closed: the two-source version label is confirmed 1.8.0 and the automated-DO-backup deploy bookend is operator-confirmed ON — without blocking the autonomous CI/test work in Plans 20-01/20-02.
</success_criteria>

<output>
Create `.planning/phases/20-hygiene-honestly-green-ci/20-03-SUMMARY.md` when done. Record the verified version label and the operator-confirmed DO-backup state (including the last-backup date the operator reported).
</output>
