---
phase: 21-stuck-agent-reply-in-place
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/blocker-chain.ts
  - src/shared/reply-reachable.ts
  - test/worker/blocked-no-edge-verdict-consistency.test.mjs
  - test/shared/reply-reachable.test.mjs
autonomous: true
requirements: [STUCK-03]
must_haves:
  truths:
    - "classifyVerdict(AWAITING_AGENT_STUCK) returns actionAffordance 'nudge' (was 'assign'), tier 'watch', needsYou false"
    - "isReplyReachable('AWAITING_AGENT_STUCK') returns true (was false)"
    - "The engine stays AI-free / pure / I/O-free (purity grep guards pass)"
    - "Every surface reads ONE consistent verdict for AWAITING_AGENT_STUCK (SC5 matrix cell flips to 'nudge')"
  artifacts:
    - path: "src/shared/blocker-chain.ts"
      provides: "AWAITING_AGENT_STUCK verdict triple { tier:'watch', actionAffordance:'nudge', needsYou:false }"
      contains: "case 'AWAITING_AGENT_STUCK'"
    - path: "src/shared/reply-reachable.ts"
      provides: "isReplyReachable AWAITING_AGENT_STUCK → true"
      contains: "case 'AWAITING_AGENT_STUCK'"
  key_links:
    - from: "src/shared/blocker-chain.ts classifyVerdict"
      to: "isReplyReachable + every UI surface"
      via: "actionAffordance 'nudge' on the stuck verdict triple"
      pattern: "actionAffordance: 'nudge'"
---

<objective>
Flip the engine gate that has kept stuck agents out of the reply loop. Two pure
edits: (1) `classifyVerdict(AWAITING_AGENT_STUCK)` returns `actionAffordance:'nudge'`
(was `'assign'`), with `tier:'watch'` and `needsYou:false` UNCHANGED; (2)
`isReplyReachable('AWAITING_AGENT_STUCK')` returns `true` (was `false`). Then update
the two engine-level tests that pin the OLD values so the suite stays honestly green.

Purpose: The handler (`situation.replyAndResume`) and the shared `<ReplyInPlace>`
primitive already resume a `status='blocked'` leaf (Phase-10 Shape B proven). The
ONLY thing routing stuck rows away from reply was this verdict triple + this
predicate (the Phase-12 D-05 lock). Reversing them activates the dormant-but-reserved
`'nudge'` affordance for the stuck kind. No new terminal kind, no migration.

Output: A stuck row now carries `actionAffordance:'nudge'` and `isReplyReachable`
returns true for it — the structural precondition every UI surface (Wave 2) consumes.

SEED DIVERGENCE (surface in SUMMARY): V1.6-SEED.md said "blocker-chain.ts stays
untouched" and "loosen a terminal-kind gate in situation-reply-and-resume.ts". Live
code is the opposite: the handler NEVER gated on terminal kind (it acts on the
caller-supplied `needsDurabilityFlip` boolean), and the real gate is exactly this
engine verdict + the reachable predicate. This plan corrects the seed.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md

<interfaces>
<!-- The exact verdict-triple union the engine returns and every surface reads. -->
From src/shared/types.ts:
```typescript
actionAffordance: 'reply' | 'nudge' | 'assign' | 'open' | 'none';
type Terminal =
  | { kind: 'AWAITING_AGENT_STUCK'; agentId: string; label: string }
  | ... // 7 other kinds
```
Engine return shape (classifyVerdict): { tier, actionAffordance, needsYou }.
isReplyReachable input: Terminal['kind'] (the discriminant string ALONE).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flip the engine verdict triple + reply-reachable predicate for AWAITING_AGENT_STUCK</name>
  <read_first>
    - src/shared/blocker-chain.ts (the classifyVerdict AWAITING_AGENT_STUCK case at ~line 80-86; the exhaustive switch + `never` guard idiom; makeResult / makeDegradedResult that consume the triple)
    - src/shared/reply-reachable.ts (the isReplyReachable AWAITING_AGENT_STUCK case at ~line 56-59; the purity contract in the header comment)
    - src/shared/types.ts:43-98 (the Terminal union + BlockerChainResult.actionAffordance comment that documents 'nudge' as dormant)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-1, D-2)
  </read_first>
  <behavior>
    - classifyVerdict({ kind:'AWAITING_AGENT_STUCK', ... }) returns { tier:'watch', actionAffordance:'nudge', needsYou:false }
    - tier stays 'watch' and needsYou stays false (a stuck agent NEVER enters the loud Needs-you tier — Phase-12 D-04 / Phase-15 lock preserved)
    - isReplyReachable('AWAITING_AGENT_STUCK') returns true
    - All 7 other classifyVerdict cases and all 7 other isReplyReachable cases are byte-unchanged
    - The exhaustive switch + `never` guard stays intact in both files (no new kind, one-triple edit + one-boolean edit)
  </behavior>
  <action>
    In `src/shared/blocker-chain.ts` `classifyVerdict`, change ONLY the
    `case 'AWAITING_AGENT_STUCK'` return from `actionAffordance: 'assign'` to
    `actionAffordance: 'nudge'`; leave `tier: 'watch'` and `needsYou: false`. Replace
    the Plan-12 D-05 comment block on that case with a Phase-21 note: the reserved
    `'nudge'` slot is now active for the reply-to-unstick loop (cite 21-CONTEXT D-1);
    `tier`/`needsYou` deliberately unchanged so stuck stays a QUIET Watch affordance,
    not a Needs-you promotion. Do NOT touch `makeResult`, `makeDegradedResult`,
    `makeBlockerFreeResult`, `pickTopChains`, or any other case.

    In `src/shared/reply-reachable.ts` `isReplyReachable`, change the
    `case 'AWAITING_AGENT_STUCK'` body from `return false;` to `return true;`.
    Rewrite that case's comment: remove the "DEFERRED / Phase 12 D-05 LOCK / stays
    assign / OwnerPicker handles it" rationale; replace with a Phase-21 note — a stuck
    agent's blocked issue resumes via the same answer-comment recipe (Phase-10 Shape B
    proven), so reply-in-place is reachable (cite 21-CONTEXT D-2). Also update the
    file header's SCOPE block (lines ~9-18) so it no longer claims AWAITING_AGENT_STUCK
    is deferred. Keep the predicate pure: input is the kind discriminant alone — do
    NOT read awaitedPartyLabel/ownerName/targetAgentUuid/clock/network.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/shared/blocker-chain.ts` AWAITING_AGENT_STUCK case returns `actionAffordance: 'nudge'`
    - `src/shared/blocker-chain.ts` AWAITING_AGENT_STUCK case still returns `tier: 'watch'` and `needsYou: false`
    - `src/shared/reply-reachable.ts` AWAITING_AGENT_STUCK case returns `true`
    - `npx tsc --noEmit` exits 0 (the `never` guards still cover all 8 kinds)
    - No other `case` body in either file changed (git diff shows only the two cases + their comments + the reply-reachable header)
  </acceptance_criteria>
  <done>The engine and reachable predicate route AWAITING_AGENT_STUCK to the reply loop ('nudge' + reachable:true); tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the verdict-consistency matrix + reply-reachable tests to the new stuck values</name>
  <read_first>
    - test/worker/blocked-no-edge-verdict-consistency.test.mjs (TDD 1 line ~159-172 asserts Reader stuck → actionAffordance 'assign'; the MATRIX line ~194-238 and the SC5 4×8 matrix; the verdictKey projection at ~409-416 includes actionAffordance)
    - test/shared/reply-reachable.test.mjs (the AWAITING_AGENT_STUCK → false test at ~line 36-38 and the "exactly ONE reachable" test at ~line 64-77 that asserts `['AWAITING_HUMAN']`)
    - src/shared/blocker-chain.ts (the just-edited verdict — source of truth for the new expected value)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-8 — extend, don't rewrite)
  </read_first>
  <behavior>
    - The verdict-consistency test's Reader AWAITING_AGENT_STUCK assertion expects actionAffordance 'nudge' (not 'assign'); tier 'watch' and needsYou false unchanged
    - The SC5 4×8 matrix still asserts one consistent verdict per surface for AWAITING_AGENT_STUCK (now keyed on 'nudge')
    - reply-reachable test: AWAITING_AGENT_STUCK → true; the reachable-set test asserts ['AWAITING_HUMAN', 'AWAITING_AGENT_STUCK'] (or the order the predicate yields) — exactly TWO reachable kinds
    - Every other matrix cell + reachable case assertion is unchanged
  </behavior>
  <action>
    In `test/worker/blocked-no-edge-verdict-consistency.test.mjs`: update TDD 1's
    Reader assertion `assert.equal(result.actionAffordance, 'assign')` to `'nudge'`
    (the AWAITING_AGENT_STUCK row). Leave its tier='watch' / needsYou=false asserts.
    The SC5 4×8 matrix derives expected from the canonical engine verdict via
    `verdictKey` (which includes actionAffordance), so the AWAITING_AGENT_STUCK column
    now flows 'nudge' automatically — confirm no hard-coded 'assign' literal for that
    kind remains; if the rename test or any cell hard-codes 'assign' for STUCK, flip it
    to 'nudge'. Do NOT change the structured-wait, human-owned, no-owner, or
    not-blocked cases.

    In `test/shared/reply-reachable.test.mjs`: change the AWAITING_AGENT_STUCK test
    body assertion from `false` to `true` and retitle it (drop "DEFERRED"; cite
    Phase-21 activation). Update the "exactly ONE reachable" test (~line 64-77) so its
    expected `reachable` array now contains BOTH 'AWAITING_HUMAN' AND
    'AWAITING_AGENT_STUCK' in the order `KINDS.filter` yields them, and rename it to
    "exactly TWO of the 8 kinds are reachable". Leave the purity guards untouched.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && node --test test/shared/reply-reachable.test.mjs test/worker/blocked-no-edge-verdict-consistency.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test test/shared/reply-reachable.test.mjs` exits 0 with the stuck case asserting true and the two-reachable assertion passing
    - `node --test test/worker/blocked-no-edge-verdict-consistency.test.mjs` exits 0 with the stuck cell asserting 'nudge'
    - `grep -n "AWAITING_AGENT_STUCK" test/shared/reply-reachable.test.mjs | grep -c "true"` returns at least 1
    - No test in either file still asserts AWAITING_AGENT_STUCK → 'assign' or → reachable false
  </acceptance_criteria>
  <done>Both engine-level test files pin the new stuck values and pass; no stale 'assign'/false-stuck assertion remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure engine → every surface | The verdict triple flows untrusted to UI; a wrong triple silently mis-routes every consumer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | classifyVerdict / isReplyReachable | mitigate | exhaustive switch + `never` guard makes a malformed kind a compile error; tsc gate in Task 1 verify |
| T-21-02 | Information disclosure | engine label | accept | engine emits raw labels by design; the worker scrub (flatten-blocker-chain) + UI render-scan strip UUIDs downstream — unchanged this plan, no new render path here |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | no package installs in this plan; no audit table needed |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (both `never` guards still total over the 8 kinds).
- `node --test test/shared/reply-reachable.test.mjs test/worker/blocked-no-edge-verdict-consistency.test.mjs` exits 0.
- The existing engine purity grep guard in `test/shared/blocker-chain.test.mjs` still passes (run it: `node --test test/shared/blocker-chain.test.mjs`).
</verification>

<success_criteria>
- AWAITING_AGENT_STUCK carries actionAffordance 'nudge', tier 'watch', needsYou false.
- isReplyReachable('AWAITING_AGENT_STUCK') === true.
- Engine stays AI-free/pure (purity guards green).
- Engine-level tests updated and green; no migration, no capability change.
</success_criteria>

<output>
Create `.planning/phases/21-stuck-agent-reply-in-place/21-01-SUMMARY.md` when done.
Record the seed divergence (handler never gated on kind; engine verdict + reachable
predicate were the real gate).
</output>
