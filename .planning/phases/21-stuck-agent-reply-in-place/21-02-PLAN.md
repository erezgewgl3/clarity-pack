---
phase: 21-stuck-agent-reply-in-place
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/surfaces/_shared/reply-in-place.tsx
autonomous: true
requirements: [STUCK-05]
must_haves:
  truths:
    - "ReplyInPlaceProps accepts an OPTIONAL variant?: 'answer' | 'nudge' prop, default 'answer'"
    - "variant='nudge' changes ONLY the affordance copy (button/placeholder/aria-label/label), never dispatch/behavior"
    - "An AWAITING_HUMAN caller that omits variant gets byte-identical copy to today (zero regression)"
  artifacts:
    - path: "src/ui/surfaces/_shared/reply-in-place.tsx"
      provides: "variant-aware stuck-context copy on the single shared primitive"
      contains: "variant"
  key_links:
    - from: "ReplyInPlace variant prop"
      to: "the rendered placeholder/aria-label/Send button/named-action label"
      via: "a pure copy-selector keyed on variant (no behavior branch)"
      pattern: "variant === 'nudge'"
---

<objective>
Add an OPTIONAL, backward-compatible `variant?: 'answer' | 'nudge'` prop to the ONE
shared `<ReplyInPlace>` primitive. `variant='nudge'` changes ONLY the affordance COPY
(placeholder, aria-label, Send-button label, and the default named-action wording) to
stuck-context phrasing ("Nudge to unstick" / "Reply to unstick — your note resumes
{awaitedPartyLabel}"). Default `'answer'` keeps today's exact AWAITING_HUMAN wording.

Purpose: STUCK-05 — the reply copy must read appropriately for the stuck context,
distinct from the human-decision wording. The Phase-14 SC3 rule (ONE primitive, no
copies) is preserved: this is a copy prop, NOT a fork. No dispatch, no
`situation.replyAndResume` arg, no `needsDurabilityFlip`/`messageUuid` logic changes.

Output: The primitive can render stuck-context copy on demand; the Wave-2 surfaces
(employee-row, live-blocker-panel) pass `variant='nudge'` on the `'nudge'` branch.
This plan runs in parallel with 21-01 (different file, no overlap).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md

<interfaces>
<!-- The current props + the render sites that carry copy. Extracted from source. -->
From src/ui/surfaces/_shared/reply-in-place.tsx:
```typescript
export type ReplyInPlaceProps = {
  leafIssueId: string | null;
  leafIssueUuid: string | null;
  awaitedPartyLabel: string;
  namedAction: string;
  reachable: boolean;
  needsDurabilityFlip: boolean;
  decisionOptions: string[] | null;
  companyId: string; userId: string; companyPrefix: string;
  navigate: (to: string) => void;
  onActed: () => void;
};
```
Copy render sites today (all use awaitedPartyLabel):
- input aria-label: `Reply to ${awaitedPartyLabel}`
- input placeholder: `Reply to ${awaitedPartyLabel}…`
- Send button text node: `sending ? 'Sending…' : 'Send'`
- the namedAction <p> (caller-supplied; surface passes a stuck sentence in Wave 2)
Dispatch (UNCHANGED): reply({ companyId, leafIssueUuid, leafIssueId, body, userId, messageUuid, needsDurabilityFlip }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the optional variant copy prop to ReplyInPlace</name>
  <read_first>
    - src/ui/surfaces/_shared/reply-in-place.tsx (the ReplyInPlaceProps type ~line 45-75; the dispatchReply useCallback ~line 146-219 — copy is NOT here; the reachable===false branch ~line 224-241; the reachable===true render ~line 249-294 with the input placeholder/aria-label and the Send button)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-4)
  </read_first>
  <action>
    Add `variant?: 'answer' | 'nudge';` to `ReplyInPlaceProps` with a doc comment: copy
    ONLY, default 'answer', never changes dispatch (cite D-4). Destructure `variant`
    in the component signature with a default of `'answer'`.

    Introduce a single pure copy-selector inside the component (a const object or a
    small `const copy = variant === 'nudge' ? {...} : {...}`) producing four strings:
    `inputAriaLabel`, `inputPlaceholder`, `sendLabel`, and `defaultNamedActionPrefix`
    (used only when the caller passes no namedAction — but callers DO pass one, so
    primarily the aria-label/placeholder/sendLabel matter). For 'answer' the strings
    are byte-identical to today: aria-label `Reply to ${awaitedPartyLabel}`, placeholder
    `Reply to ${awaitedPartyLabel}…`, send `Send`. For 'nudge': aria-label
    `Reply to unstick ${awaitedPartyLabel}`, placeholder
    `Reply to unstick — your note resumes ${awaitedPartyLabel}…`, send `Nudge to unstick`.
    Replace the inline placeholder/aria-label string literals and the `'Send'` text node
    with the selected strings (keep `sending ? 'Sending…' : sendLabel`). Do NOT touch
    the chips path, dispatchReply, the reachable===false Open↗ branch logic, or any
    dispatch arg. `variant` MUST NOT appear anywhere inside `dispatchReply` or its
    dependency array — it is render-copy only.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && npx tsc --noEmit -p tsconfig.json && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `reply-in-place.tsx` ReplyInPlaceProps contains `variant?: 'answer' | 'nudge'`
    - The component destructures `variant` with default `'answer'`
    - `grep -n "Nudge to unstick" src/ui/surfaces/_shared/reply-in-place.tsx` returns a match
    - The 'answer' branch strings are byte-identical to the pre-edit literals (git diff shows the 'answer' copy unchanged in value)
    - `variant` does NOT appear inside the dispatchReply useCallback body or its dependency array (grep the callback range)
    - `npx tsc --noEmit` exits 0 and `node scripts/build-ui.mjs` exits 0
  </acceptance_criteria>
  <done>The shared primitive renders stuck-context copy under variant='nudge' and is byte-identical for default 'answer'; dispatch untouched; UI bundle builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → primitive copy | a variant value chooses display strings only; cannot reach dispatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03 | Tampering | ReplyInPlace dispatch | mitigate | variant is render-copy only; acceptance criterion asserts it is absent from dispatchReply + deps |
| T-21-04 | Information disclosure | rendered copy | mitigate | copy interpolates only awaitedPartyLabel (already scrubbed upstream); no *Uuid in any new string |
| T-21-SC | Tampering | npm installs | mitigate | no package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `node scripts/build-ui.mjs` exits 0 (the same-origin ESM bundle builds with the new prop).
- Default-variant copy is byte-identical to pre-edit (regression-free for AWAITING_HUMAN callers).
</verification>

<success_criteria>
- ReplyInPlaceProps has an optional `variant` defaulting to 'answer'.
- variant='nudge' changes only copy; AWAITING_HUMAN callers (no variant) keep today's exact wording.
- No dispatch/behavior change; ONE primitive (no copy).
</success_criteria>

<output>
Create `.planning/phases/21-stuck-agent-reply-in-place/21-02-SUMMARY.md` when done.
</output>
