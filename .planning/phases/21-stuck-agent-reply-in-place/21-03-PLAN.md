---
phase: 21-stuck-agent-reply-in-place
plan: 03
type: execute
wave: 2
depends_on: ["21-01", "21-02"]
files_modified:
  - src/ui/surfaces/situation-room/employee-row.tsx
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/worker/agents/action-cards.ts
autonomous: true
requirements: [STUCK-01, STUCK-02, STUCK-04]
must_haves:
  truths:
    - "A stuck agent's row in the Situation Room employee row shows a reply-in-place affordance in the QUIET Watch-tier body (STUCK-01)"
    - "A stuck agent's row in the Reader live-blocker panel shows the same shared <ReplyInPlace> affordance via a 'nudge' branch (STUCK-02)"
    - "Both surfaces mount the SAME shared <ReplyInPlace> primitive (no copies) with variant='nudge'"
    - "Loading/viewing a stuck row performs zero mutation — resume fires only on explicit Send (STUCK-04)"
    - "The stuck row stays in Watch tier (visualTierOf unchanged); it is NOT promoted to Needs-you"
    - "action-cards actionKindFromAffordance maps 'nudge' → 'answer' (no new migration; respects the 0015 CHECK)"
  artifacts:
    - path: "src/ui/surfaces/situation-room/employee-row.tsx"
      provides: "showNudge gate mounting <ReplyInPlace variant='nudge'> in the Watch-tier body"
      contains: "showNudge"
    - path: "src/ui/surfaces/reader/live-blocker-panel.tsx"
      provides: "'nudge' branch mounting <ReplyInPlace variant='nudge'> (re-pointed from the wake call)"
      contains: "variant=\"nudge\""
    - path: "src/worker/agents/action-cards.ts"
      provides: "actionKindFromAffordance case 'nudge' returning 'answer'"
      contains: "case 'nudge'"
  key_links:
    - from: "employee-row.tsx Watch-tier body (showNudge)"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "<ReplyInPlace variant='nudge' reachable={isReplyReachable(chain.terminalKind)} />"
      pattern: "ReplyInPlace"
    - from: "live-blocker-panel.tsx 'nudge' affordance"
      to: "src/ui/surfaces/_shared/reply-in-place.tsx"
      via: "isNudgeBranch render mounting <ReplyInPlace variant='nudge'>"
      pattern: "variant=\"nudge\""
---

<objective>
Mount the SHARED `<ReplyInPlace>` primitive on the `'nudge'` affordance across BOTH
blocker surfaces, and close the one worker consumer that maps the affordance. Three
edits, all consuming the Wave-1 engine flip (stuck → 'nudge' + reachable:true) and the
Wave-1 primitive `variant` prop:

1. **Situation Room** (`employee-row.tsx`): add `showNudge` (=== 'nudge') alongside
   `showReply`/`showAssign`; in the QUIET Watch-tier body, mount `<ReplyInPlace
   variant='nudge'>` for stuck rows — replacing the current "agent stuck · assign an
   owner" dead-end copy. The row stays in Watch (no tier promotion).
2. **Reader** (`live-blocker-panel.tsx`): the panel already has a `case 'nudge'` in
   `primaryActionLabel` + the onAction switch, but today it wires nudge to a
   `issues.requestWakeup` button. Re-point the `'nudge'` affordance to mount
   `<ReplyInPlace variant='nudge'>` (mirror the existing `isReplyBranch` render path)
   and stop rendering the wake button for it.
3. **Worker** (`action-cards.ts`): `actionKindFromAffordance` add
   `case 'nudge': return 'answer'` (a nudge IS an answer-comment) — respects the 0015
   CHECK constraint, NO new migration.

Purpose: STUCK-01 (SR affordance), STUCK-02 (Reader affordance), STUCK-04 (no
auto-resume — the primitive dispatches only on Send; viewing fires nothing).

SEED/LIVE NOTE (surface in SUMMARY): the Reader panel's `'nudge'` case was a
requestWakeup button, NOT a missing case — this is a re-wire to the reply primitive.
The employee-row Watch-tier stuck row previously showed an "assign an owner" affordance
(via showAssign) — after the engine flip it is `'nudge'`, so the OwnerPicker no longer
fires for stuck rows; reply-to-unstick replaces it (owner reassignment stays reachable
via Open↗, per 21-CONTEXT Deferred Ideas).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md
@.planning/phases/21-stuck-agent-reply-in-place/21-01-SUMMARY.md
@.planning/phases/21-stuck-agent-reply-in-place/21-02-SUMMARY.md

<interfaces>
<!-- The render gates + the nudge primitive contract the surfaces consume. -->
From src/ui/surfaces/situation-room/employee-row.tsx:
```typescript
const showAssign = chain?.actionAffordance === 'assign'; // ~line 218
const showReply  = chain?.actionAffordance === 'reply';  // ~line 225
const visualTier = visualTierOf(row);                    // 'needs-you'|'in-motion'|'watch'
// Watch-tier body: visualTier === 'watch' && chain && !isChainlessIdle (~line 512)
// Today: showAssign in Watch renders "— agent stuck · assign an owner" + OwnerPickerPopover
```
From src/ui/surfaces/reader/live-blocker-panel.tsx:
```typescript
// primaryActionLabel case 'nudge' → `Nudge ${awaitedPartyLabel}` (~line 62)
// onAction switch case 'nudge' → void nudge(issueDispatchTarget) (requestWakeup) (~line 310)
// isReplyBranch render mounts <ReplyInPlace reachable={isReplyReachable(data.terminal.kind)} /> (~line 411)
```
ReplyInPlace now accepts variant?: 'answer' | 'nudge' (21-02). The Reader's needsDurabilityFlip
must stay false (BlockerChainResult has no leaf status field — comment-only, spike-safe).
The SR row carries chain.needsDurabilityFlip (real, from leaf status — 14-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Situation Room employee row — mount ReplyInPlace (variant='nudge') in the Watch-tier body</name>
  <read_first>
    - src/ui/surfaces/situation-room/employee-row.tsx (the showAssign/showReply gates ~line 218-225; the existing reply-branch <ReplyInPlace> usage ~line 433-448 to mirror props; the Watch-tier body block ~line 512-556 with the "agent stuck · assign an owner" copy and the OwnerPickerPopover; visualTierOf import ~line 49)
    - src/ui/surfaces/_shared/reply-in-place.tsx (the props incl. the new variant; reachable, needsDurabilityFlip, decisionOptions, navigate, onActed)
    - src/ui/surfaces/situation-room/tier-utils.ts (visualTierOf — DO NOT modify; a stuck row is tier 'watch')
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-3, D-5; the "stuck stays Watch" lock)
  </read_first>
  <action>
    Add `const showNudge = chain?.actionAffordance === 'nudge';` next to `showAssign`/
    `showReply`. In the Watch-tier body (`visualTier === 'watch' && chain &&
    !isChainlessIdle`), branch on `showNudge`: render `<ReplyInPlace>` with
    `variant="nudge"`, `leafIssueId={chain.leafIssueId}`, `leafIssueUuid={chain.leafIssueUuid}`,
    `awaitedPartyLabel={rescrubPersisted(chain.awaitedPartyLabel)}`,
    `namedAction` = a stuck-context sentence (e.g.
    `rescrubPersisted(row.actionCard?.namedAction ?? \`Reply to unstick ${chain.awaitedPartyLabel}\`)`),
    `decisionOptions={row.actionCard?.decisionOptions ?? null}`,
    `needsDurabilityFlip={chain.needsDurabilityFlip}`,
    `reachable={isReplyReachable(chain.terminalKind)}`, and the existing companyId/userId/
    companyPrefix/navigate props, with `onActed={onAssignSuccess}`. Mirror the reply-branch
    usage at ~line 433-448 exactly for prop shape. Replace the current stuck
    "— agent stuck · assign an owner to unblock" line + its OwnerPickerPopover with the
    nudge affordance for `showNudge` rows; keep the existing `showAssign` (now UNOWNED-only
    in Watch is impossible since UNOWNED is needsYou → needs-you tier, so the Watch
    showAssign path is effectively dead for stuck — leave external/cycle 'open' and
    self-resolving 'none' paths untouched). Do NOT change `visualTierOf`, do NOT move the
    row to needs-you, and do NOT alter the needs-you-tier reply branch.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && npx tsc --noEmit -p tsconfig.json && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `employee-row.tsx` contains `const showNudge = chain?.actionAffordance === 'nudge'`
    - The Watch-tier body mounts `<ReplyInPlace` with `variant="nudge"` for showNudge rows
    - The nudge ReplyInPlace passes `reachable={isReplyReachable(chain.terminalKind)}` and `needsDurabilityFlip={chain.needsDurabilityFlip}`
    - `visualTierOf` / tier-utils.ts is NOT modified (git diff shows no change there)
    - No `chain.target*Uuid` or `chain.leafIssueUuid` appears inside a `{...}` text-render expression on the new path (NO_UUID_LEAK — passed as a prop only)
    - `npx tsc --noEmit` and `node scripts/build-ui.mjs` exit 0
  </acceptance_criteria>
  <done>Stuck rows in the SR show a quiet Watch-tier reply-to-unstick affordance via the shared primitive; tier unchanged; bundle builds.</done>
</task>

<task type="auto">
  <name>Task 2: Reader live-blocker panel — re-point the 'nudge' affordance to <ReplyInPlace variant='nudge'></name>
  <read_first>
    - src/ui/surfaces/reader/live-blocker-panel.tsx (primaryActionLabel case 'nudge' ~line 62; the `nudge` requestWakeup useCallback ~line 259-273; the onAction switch case 'nudge' ~line 310-313; the isReplyBranch render ~line 411-428 to mirror; the blockerLine suppression for reply ~line 384; the showButton computation ~line 350)
    - src/ui/surfaces/_shared/reply-in-place.tsx (props incl. variant; the reachable===false degrade)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-3 — new render branch via the exhaustive switch; D-8 degrade-safe)
  </read_first>
  <action>
    Add `const isNudgeBranch = data.actionAffordance === 'nudge';` alongside
    `isReplyBranch`. Mount `<ReplyInPlace>` for BOTH reply and nudge: render the primitive
    when `isReplyBranch || isNudgeBranch`, passing `variant={isNudgeBranch ? 'nudge' : 'answer'}`.
    For the nudge case use `leafIssueId={data.pathIds.length <= 1 ? issueId : null}`,
    `leafIssueUuid={issueDispatchTarget ?? null}`,
    `awaitedPartyLabel={data.awaitedPartyLabel}`, `namedAction={blockerLine(data)}`,
    `decisionOptions={null}`, `needsDurabilityFlip={false}` (the Reader's
    BlockerChainResult carries no leaf status — comment-only, spike-safe, NEVER proxied
    from terminal.kind), `reachable={isReplyReachable(data.terminal.kind)}`, and
    companyId/userId/companyPrefix/navigate, `onActed={() => { refresh(); }}` — mirror the
    existing reply-branch render at ~line 411-428.

    Remove the `'nudge'` wake wiring: in the onAction switch, the `case 'nudge'` becomes
    `onAction = null;` (handled by the primitive, like the reply case). Delete the now-unused
    `nudge` requestWakeup useCallback (and the `wakeAction`/`busy` state ONLY if nothing else
    uses them — confirm via grep; if `busy`/`wakeAction` have no other consumer after removal,
    delete them; otherwise leave). Suppress the standalone `blockerLine` <p> for the nudge
    branch too (extend the existing `data.actionAffordance !== 'reply'` guard at ~line 384 to
    also exclude 'nudge', so it becomes `!isReplyBranch && !isNudgeBranch`), since
    `<ReplyInPlace>` renders its own namedAction line. Keep `primaryActionLabel` case 'nudge'
    intact (it is now dead for the button path but the exhaustive switch + `never` guard must
    still cover it — do NOT delete the case). `showButton` must be false for nudge (onAction
    null → no dead button).
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && npx tsc --noEmit -p tsconfig.json && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `live-blocker-panel.tsx` contains `const isNudgeBranch = data.actionAffordance === 'nudge'`
    - The panel mounts `<ReplyInPlace` with `variant={isNudgeBranch ? 'nudge' : 'answer'}` (or an equivalent that yields variant="nudge" for the nudge branch)
    - The onAction switch `case 'nudge'` sets `onAction = null` (no requestWakeup button for nudge)
    - The blockerLine `<p>` suppression now excludes BOTH reply and nudge branches
    - `primaryActionLabel` still has `case 'nudge'` (the exhaustive switch + `never` guard intact)
    - No `data.target*Uuid` rendered as text on the new path (NO_UUID_LEAK)
    - `npx tsc --noEmit` and `node scripts/build-ui.mjs` exit 0
  </acceptance_criteria>
  <done>Reader stuck rows mount the shared reply-to-unstick primitive instead of a wake button; degrade-safe; no dead button; bundle builds.</done>
</task>

<task type="auto">
  <name>Task 3: Worker action-cards — map 'nudge' affordance to 'answer' action_kind (no migration)</name>
  <read_first>
    - src/worker/agents/action-cards.ts (actionKindFromAffordance ~line 242-258 — 'nudge' currently falls through to default → 'none'; the ActionCardSourceRow.actionAffordance union already includes 'nudge'; the 0015 CHECK constraint note at ~line 53-54 of 21-CONTEXT / the IN-01 'decide' reservation comment)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-6, D-9 — NO new migration; action_kind ∈ {answer,assign,decide,none})
  </read_first>
  <action>
    In `actionKindFromAffordance`, add `case 'nudge': return 'answer';` (a nudge IS an
    answer-comment) before the default. Add a one-line comment citing D-6: maps to the
    existing 'answer' action_kind enumerated by the 0015 CHECK constraint — NO new
    migration. Do NOT touch the 'reply'/'assign' cases, the 'decide' reservation comment,
    or the default. Confirm (do not change) that `ActionCardSourceRow.actionAffordance`
    already lists `'nudge'` so the call site type-checks.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `action-cards.ts` actionKindFromAffordance contains `case 'nudge':` returning `'answer'`
    - No new file under `migrations/` is created (git status shows no new `.sql`)
    - The 0015 CHECK enum (answer/assign/decide/none) is not altered
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>'nudge' affordance maps to the 'answer' action_kind within the existing CHECK constraint; no migration.</done>
</task>

<task type="auto">
  <name>Task 4: Audit every actionAffordance === 'assign' consumer for the now-missing stuck rows (D-7)</name>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts (the UNOWNED needs-you partition ~line 905-932 — the `unowned` filter requires `r.blockerChain.needsYou === true && actionAffordance === 'assign'`)
    - src/ui/surfaces/situation-room/needs-you-banner.tsx (~line 67-72 — needs-you banner counts `group === 'needs_you' && blockerChain?.actionAffordance === 'assign'`)
    - src/ui/surfaces/situation-room/blocked-backlog-expander.tsx (~line 105-156 — `row.actionAffordance === 'assign'` → OwnerPickerPopover; `=== 'reply'` → ReplyInPlace)
    - src/worker/handlers/org-blocked-backlog.ts (~line 131 — the comment describing assign ⇔ UNOWNED + AWAITING_AGENT_STUCK)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-7 — UNOWNED still 'assign'; only stuck moved to 'nudge')
  </read_first>
  <action>
    AUDIT (read + reason, minimal edits): after the Wave-1 engine flip, stuck rows carry
    `'nudge'` and `needsYou:false`, so they were NEVER in the rollup `unowned` needs-you
    partition (which requires `needsYou === true`) nor the needs-you-banner count (same
    gate) — those stay correct and UNCHANGED (confirm and note in SUMMARY: UNOWNED-only by
    construction). The behavioral change is ONLY where stuck rows actually surfaced
    `'assign'`: the SR Watch-tier body (fixed in Task 1) and the Reader panel (fixed in
    Task 2).

    For `blocked-backlog-expander.tsx`: a stuck row reaching the backlog now carries
    `'nudge'`, so the current `row.actionAffordance === 'assign'` branch will NOT render
    an OwnerPicker for it, and the `=== 'reply'` branch will NOT mount ReplyInPlace for it
    — it would fall to the bare "Open ↗". Decide: if stuck rows can appear in the backlog,
    mount `<ReplyInPlace variant='nudge'>` for `row.actionAffordance === 'nudge'` mirroring
    the existing `=== 'reply'` branch (using `row.identifier`, `row.leafIssueUuid`,
    `row.terminalKind`, `row.needsDurabilityFlip`); if stuck rows cannot reach this
    surface, leave it and document why in SUMMARY. Prefer the affordance mount for
    completeness (it reuses the same primitive — no copy).

    Update the stale comment in `org-blocked-backlog.ts:131` and any `employee-row.tsx`
    header comment that claims "assign ⇔ UNOWNED + AWAITING_AGENT_STUCK" to reflect: after
    Phase-21, assign ⇔ UNOWNED only; stuck ⇔ 'nudge' (reply-to-unstick). Comment-only edits.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && npx tsc --noEmit -p tsconfig.json && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The rollup `unowned` partition + needs-you-banner count still gate on `needsYou === true && actionAffordance === 'assign'` (UNOWNED-only; unchanged) — confirmed in SUMMARY with a one-line rationale
    - `blocked-backlog-expander.tsx` either mounts `<ReplyInPlace variant='nudge'>` on `actionAffordance === 'nudge'` OR the SUMMARY documents why stuck cannot reach the backlog
    - Stale "assign ⇔ UNOWNED + AWAITING_AGENT_STUCK" comments updated to "assign ⇔ UNOWNED; stuck ⇔ nudge"
    - `npx tsc --noEmit` and `node scripts/build-ui.mjs` exit 0
  </acceptance_criteria>
  <done>Every `=== 'assign'` consumer is audited; UNOWNED-only paths confirmed unchanged; backlog stuck rows handled or documented; comments corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client UI → situation.replyAndResume | the same-origin plugin UI dispatches the resume mutation; the handler's company-scope gate (14-01) is the trust check (unchanged) |
| passive render → mutation | a view must NOT trigger resume (STUCK-04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Elevation of privilege | ReplyInPlace on stuck rows | accept | reuses the existing company-scoped situation.replyAndResume handler (14-01) which validates leafIssueUuid ∈ companyId before any write; no new dispatch path |
| T-21-06 | Tampering | passive view auto-resume | mitigate | the primitive dispatches ONLY in dispatchReply (Send/chip onClick); mount fires no effect — STUCK-04 verified by 21-04 test |
| T-21-07 | Information disclosure | new nudge render paths | mitigate | only leafIssueId/awaitedPartyLabel render; *Uuid are dispatch-only props; acceptance criteria forbid *Uuid in render exprs; NO_UUID_LEAK render-scan re-run in 21-04 |
| T-21-SC | Tampering | npm installs | mitigate | no package installs in this plan |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (exhaustive switches + `never` guards still total).
- `node scripts/build-ui.mjs` exits 0.
- Manual reasoning recorded in SUMMARY: UNOWNED needs-you partitions are unaffected by the stuck→nudge move (gated on needsYou === true).
- No new `migrations/*.sql`; no new manifest capability.
</verification>

<success_criteria>
- Stuck rows show reply-to-unstick on BOTH the SR employee row (Watch tier) and the Reader live-blocker panel, via the ONE shared primitive (variant='nudge').
- Stuck rows stay in Watch (no Needs-you promotion).
- action-cards maps 'nudge' → 'answer' with no migration.
- Every 'assign' consumer audited; UNOWNED-only intent preserved; no auto-resume on view.
</success_criteria>

<output>
Create `.planning/phases/21-stuck-agent-reply-in-place/21-03-SUMMARY.md` when done.
Record the Reader re-wire (nudge was a wake button) and the backlog-expander decision.
</output>
