---
phase: 21-stuck-agent-reply-in-place
plan: 04
type: execute
wave: 3
depends_on: ["21-01", "21-02", "21-03"]
files_modified:
  - test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs
  - test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs
  - test/ui/surfaces/_shared/reply-in-place.test.mjs
  - test/worker/handlers/situation-reply-and-resume.test.mjs
autonomous: true
requirements: [STUCK-04, STUCK-06]
must_haves:
  truths:
    - "A test pins the SR Watch-tier stuck row mounting <ReplyInPlace variant='nudge'> via showNudge"
    - "A test pins the Reader 'nudge' branch mounting <ReplyInPlace variant='nudge'> (no wake button)"
    - "A test pins the variant prop changing copy only (nudge copy present; default copy unchanged)"
    - "A test confirms the situation.replyAndResume handler is terminal-kind-agnostic (needs NO change)"
    - "A test pins no-auto-resume on mount (dispatch only on Send/chip onClick) — STUCK-04"
    - "NO_UUID_LEAK render-scan stays green on all new nudge render paths — STUCK-06"
  artifacts:
    - path: "test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs"
      provides: "nudge-branch mount assertion + no-wake-button assertion"
      contains: "nudge"
    - path: "test/worker/handlers/situation-reply-and-resume.test.mjs"
      provides: "terminal-kind-agnostic confirmation (no kind inspection)"
      contains: "needsDurabilityFlip"
  key_links:
    - from: "the test suite"
      to: "the Wave-2 nudge render paths + the Wave-1 engine flip"
      via: "source-grep + behavioral assertions"
      pattern: "variant"
---

<objective>
Extend (don't rewrite) the Phase-14 reply-and-resume test suite to the stuck/nudge
path, and confirm the handler needs no change. Per 21-CONTEXT D-8: cover the new render
+ dispatch path, prove degrade-safety + no-auto-resume (STUCK-04) and NO_UUID_LEAK
(STUCK-06), and keep every existing assertion honest after the Wave-2 edits.

Purpose: The Wave-2 surface edits changed the SR Watch-tier stuck copy and re-wired the
Reader nudge branch; the existing source-grep tests must be updated to pin the NEW
behavior (and any assertion that pinned the OLD "agent stuck · assign an owner" copy or
the Reader wake button must flip). STUCK-04 (no resume on view) and STUCK-06 (degrade-
safe + NO_UUID_LEAK) get explicit coverage on the nudge path.

Output: a green test suite that locks the stuck reply-to-unstick behavior across both
surfaces, the variant copy prop, and the handler's kind-agnostic resume.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md
@.planning/phases/21-stuck-agent-reply-in-place/21-01-SUMMARY.md
@.planning/phases/21-stuck-agent-reply-in-place/21-02-SUMMARY.md
@.planning/phases/21-stuck-agent-reply-in-place/21-03-SUMMARY.md

<interfaces>
<!-- The test convention: source-grep over stripComments(src), no jsdom. -->
Existing tests use: readFileSync(src) → stripComments → assert.match / assert.doesNotMatch.
employee-row-reply-in-place.test.mjs already asserts the reply branch + NO_UUID_LEAK JSX-text-node scan.
situation-reply-and-resume.test.mjs is a behavioral handler test (mock ctx.issues, assert ok/error + flip).
The handler reads needsDurabilityFlip (caller-supplied boolean), NEVER terminal.kind.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the SR + Reader + primitive source-grep tests to the nudge path</name>
  <read_first>
    - test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs (the SC3 import pins; the reply-branch assertions; the OwnerPickerPopover/showAssign assertion ~line 112-115 that may now be stale for stuck; the NO_UUID_LEAK JSX-text-node scan ~line 121-134)
    - test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs (mirror; the existing reply-branch + any nudge/wake assertions)
    - test/ui/surfaces/_shared/reply-in-place.test.mjs (the primitive's prop/copy assertions)
    - src/ui/surfaces/situation-room/employee-row.tsx (post-Wave-2: showNudge + Watch-tier <ReplyInPlace variant="nudge">)
    - src/ui/surfaces/reader/live-blocker-panel.tsx (post-Wave-2: isNudgeBranch + <ReplyInPlace variant="nudge">, onAction 'nudge' = null)
    - src/ui/surfaces/_shared/reply-in-place.tsx (post-Wave-2: the variant prop + nudge copy)
  </read_first>
  <behavior>
    - employee-row test: a new assertion matches `showNudge` (=== 'nudge') AND a Watch-tier `<ReplyInPlace` with `variant="nudge"`; the reachable + needsDurabilityFlip pins still hold for the nudge path
    - live-blocker-panel test: a new assertion matches `isNudgeBranch` AND `<ReplyInPlace` with variant="nudge"; an assertion proves the 'nudge' onAction is null (no requestWakeup button for nudge)
    - reply-in-place primitive test: asserts the optional `variant?: 'answer' | 'nudge'` prop, the 'nudge' copy literal ("Nudge to unstick"), and that the default-'answer' copy literals are unchanged
    - any stale assertion pinning the OLD stuck Watch copy ("agent stuck · assign an owner") or the Reader nudge wake button is updated/removed
    - all existing reply-branch (AWAITING_HUMAN) assertions still pass unchanged
  </behavior>
  <action>
    In `employee-row-reply-in-place.test.mjs`: add tests asserting `CODE` matches
    `showNudge` gated on `actionAffordance === 'nudge'` and a `<ReplyInPlace` usage with
    `variant="nudge"` in the Watch-tier path; assert the nudge mount passes
    `reachable={isReplyReachable(chain.terminalKind)}` and
    `needsDurabilityFlip={chain.needsDurabilityFlip}`. If the existing OwnerPicker/showAssign
    test now contradicts the Watch-tier change, scope it to UNOWNED (it still applies to the
    needs-you UNOWNED path) and remove any assertion that the stuck Watch body renders
    "assign an owner". Re-run the NO_UUID_LEAK JSX-text-node scan unchanged (it already covers
    the whole file → covers the new path).

    In `live-blocker-panel-reply-in-place.test.mjs`: add tests asserting `isNudgeBranch`,
    a `<ReplyInPlace` with `variant="nudge"`, and that the `case 'nudge'` onAction is `null`
    (assert the source does NOT call `nudge(` / `requestWakeup` from the nudge onAction path —
    assert.doesNotMatch on the `onAction = () => { void nudge(` pattern). Keep the reply-branch
    assertions.

    In `_shared/reply-in-place.test.mjs`: add tests for the `variant` prop in
    ReplyInPlaceProps, the presence of the "Nudge to unstick" literal, and the unchanged
    'answer' copy literals ("Send", and the `Reply to ${awaitedPartyLabel}` aria-label).
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && node --test test/ui/surfaces/situation-room/employee-row-reply-in-place.test.mjs test/ui/surfaces/reader/live-blocker-panel-reply-in-place.test.mjs test/ui/surfaces/_shared/reply-in-place.test.mjs test/ui/surfaces/_shared/reply-in-place-no-uuid-leak.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - employee-row test asserts `showNudge` + a Watch-tier `<ReplyInPlace` with `variant="nudge"`
    - live-blocker-panel test asserts `isNudgeBranch` + `variant="nudge"` AND the nudge onAction is null (no requestWakeup button)
    - reply-in-place test asserts the `variant` prop + the "Nudge to unstick" literal + unchanged 'answer' copy
    - No test still asserts the OLD stuck Watch "assign an owner" copy or the Reader nudge wake button
    - `node --test` over the four files exits 0
  </acceptance_criteria>
  <done>The SR, Reader, and primitive tests pin the nudge render paths + variant copy and stay green; NO_UUID_LEAK scan green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm the handler is kind-agnostic + pin no-auto-resume (STUCK-04) and the nudge dispatch path</name>
  <read_first>
    - test/worker/handlers/situation-reply-and-resume.test.mjs (the existing behavioral cases — ok/error returns, the Shape-B durable flip on needsDurabilityFlip, the dedup/idempotency case)
    - src/worker/handlers/situation-reply-and-resume.ts (the handler — confirm it reads needsDurabilityFlip and NEVER inspects terminal.kind; lines ~82-87 + the header comment)
    - src/ui/surfaces/_shared/reply-in-place.tsx (dispatchReply fires only inside Send/chip onClick — the no-auto-resume guarantee)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (D-8 — extend; the no-auto-resume STUCK-04 rule)
  </read_first>
  <behavior>
    - A test asserts (source-grep over the handler) that it reads `needsDurabilityFlip` and does NOT reference `terminal.kind` / `terminalKind` anywhere — proving it is kind-agnostic and needs NO change for the stuck path
    - A test exercises the existing behavioral handler with needsDurabilityFlip=true (the Shape-B stuck shape: status='blocked') and asserts the comment posts + the durable {status:'in_progress'} flip applies + { ok, durable:true } — reusing the existing mock-ctx pattern (this is the stuck resume path)
    - A test (source-grep over reply-in-place.tsx) asserts the situation.replyAndResume dispatch occurs ONLY inside dispatchReply, which is only invoked from Send/chip onClick (no useEffect/mount dispatch) — STUCK-04 no-auto-resume on view
  </behavior>
  <action>
    In `situation-reply-and-resume.test.mjs`: add a source-grep test that reads the handler
    source and asserts `assert.doesNotMatch(code, /terminal\.kind|terminalKind/)` and
    `assert.match(code, /needsDurabilityFlip/)` — pinning the kind-agnostic contract (the
    handler needs NO change for stuck; cite the seed divergence). Add (or confirm) a
    behavioral case with `needsDurabilityFlip: true` representing the stuck Shape-B resume:
    using the existing mock ctx.issues (get → a same-company issue, createComment → ok,
    update → ok), assert the result is `{ ok:true, durable:true }` and that
    `ctx.issues.update` was called with `{ status: 'in_progress' }`. Reuse the file's existing
    mock helpers; do not introduce jsdom.

    Add a source-grep test over `src/ui/surfaces/_shared/reply-in-place.tsx` asserting the
    `reply(` dispatch appears ONLY within `dispatchReply` and that `dispatchReply` is NOT
    called from a `React.useEffect` (assert.doesNotMatch for a useEffect that calls
    dispatchReply) — the no-auto-resume-on-mount guarantee (STUCK-04). Place this in the
    handler test file or a small companion; keep it self-contained node:test.
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && node --test test/worker/handlers/situation-reply-and-resume.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - A test asserts the handler source matches `needsDurabilityFlip` and does NOT match `terminal.kind`/`terminalKind`
    - A behavioral case with needsDurabilityFlip=true asserts `{ ok:true, durable:true }` and an `issues.update({ status:'in_progress' })` call
    - A test asserts the primitive dispatches only inside dispatchReply and not from a useEffect (STUCK-04)
    - `node --test test/worker/handlers/situation-reply-and-resume.test.mjs` exits 0
    - No edit was made to `src/worker/handlers/situation-reply-and-resume.ts` (git diff shows the handler unchanged — confirmation only)
  </acceptance_criteria>
  <done>The handler is proven kind-agnostic (unchanged), the stuck Shape-B resume is exercised, and no-auto-resume-on-view is pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| passive view → mutation | tests must prove a mount fires no resume (STUCK-04) |
| render → human-facing text | tests must prove no raw UUID renders on the nudge path (STUCK-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-08 | Tampering | auto-resume on view | mitigate | Task 2 pins dispatch-only-on-Send (no useEffect dispatch) — STUCK-04 |
| T-21-09 | Information disclosure | nudge render paths | mitigate | Task 1 re-runs the NO_UUID_LEAK JSX-text-node scan over the whole file — STUCK-06 |
| T-21-SC | Tampering | npm installs | mitigate | no package installs in this plan |
</threat_model>

<verification>
- `node --test` over the four UI test files + the handler test exits 0.
- The handler source is unchanged (kind-agnostic confirmation, not an edit).
- The full nested suite (run in 21-05 before deploy) stays honestly green.
</verification>

<success_criteria>
- Nudge render paths (SR Watch + Reader) are pinned by source-grep tests.
- The variant copy prop is pinned (nudge copy present, default unchanged).
- The handler is proven kind-agnostic and the stuck Shape-B resume is exercised.
- STUCK-04 (no auto-resume on view) and STUCK-06 (NO_UUID_LEAK) covered; suite green.
</success_criteria>

<output>
Create `.planning/phases/21-stuck-agent-reply-in-place/21-04-SUMMARY.md` when done.
</output>
