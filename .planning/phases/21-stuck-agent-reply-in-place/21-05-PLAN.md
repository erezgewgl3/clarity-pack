---
phase: 21-stuck-agent-reply-in-place
plan: 05
type: execute
wave: 4
depends_on: ["21-01", "21-02", "21-03", "21-04"]
files_modified:
  - package.json
  - src/manifest.ts
autonomous: false
requirements: [STUCK-06]
user_setup:
  - service: AriClaw DO droplet (BEAAA Paperclip host)
    why: "Live bookended deploy + stuck-agent replyâresume drill (no doctl/psql on box)"
    dashboard_config:
      - task: "Confirm the automated DO backup ran (the deploy bookend) before any install/upgrade"
        location: "DigitalOcean dashboard â Droplets â AriClaw â Backups"
must_haves:
  truths:
    - "package.json version and src/manifest.ts version literal are BOTH bumped to 1.8.1 (two-source bump, byte-identical)"
    - "A clean rebuild produces a UI bundle + worker bundle with no tsc/build errors"
    - "The full nested test suite is honestly green before deploy"
    - "A bookended live BEAAA reinstall completes (DO backup confirmed before; rehearsed rollback path verified)"
    - "A real stuck-agent replyâresume drill succeeds: an AWAITING_AGENT_STUCK row shows reply-to-unstick, an operator Send posts a comment and resumes the agent, with no raw UUID in any human-facing text (STUCK-06)"
  artifacts:
    - path: "package.json"
      provides: "version 1.8.1"
      contains: "1.8.1"
    - path: "src/manifest.ts"
      provides: "manifest version literal 1.8.1"
      contains: "1.8.1"
  key_links:
    - from: "package.json version"
      to: "src/manifest.ts version literal"
      via: "two-source byte-identical bump (host reads dist/manifest.js)"
      pattern: "1.8.1"
---

<objective>
Ship Phase 21 to the live BEAAA instance and prove it with a real stuck-agent
replyâresume drill. Two-source version bump (1.8.0 â 1.8.1 â a UI/handler/engine-verdict
gate, NO migration, NO new capability), a clean rebuild, the full honest-green test
sweep, then a bookended live reinstall + the drill.

Purpose: STUCK-06 + live acceptance â the phase is not done until an operator can unstick
a STUCK agent from the cockpit on BEAAA, with the deploy bookended by a confirmed
automated DO backup and a rehearsed rollback path. This plan is `autonomous:false`: the
deploy + drill run in the operator's local window (tunnel + SSH), pausing at the live
checkpoints.

VERSION: current live = v1.8.0 (package.json + src/manifest.ts:702). Bump to 1.8.1 (no
schema migration â patch is correct). Do NOT choose 1.9.0 unless a migration is
introduced (it must not be â D-9).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md
@.planning/phases/21-stuck-agent-reply-in-place/21-01-SUMMARY.md
@.planning/phases/21-stuck-agent-reply-in-place/21-02-SUMMARY.md
@.planning/phases/21-stuck-agent-reply-in-place/21-03-SUMMARY.md
@.planning/phases/21-stuck-agent-reply-in-place/21-04-SUMMARY.md

<interfaces>
<!-- Deploy facts (21-CONTEXT live drill + BEAAA deploy mechanics memory). -->
BEAAA plugin id: a763176a-2f4d-4986-b190-b5151e42cc00
BEAAA company id (CID): 59f8876e-e729-4dda-98f9-1317c2b50492
Host: AriClaw DO droplet; Paperclip runs as beai-agent via `npx -y paperclipai`, cd ~ (NOT /root/paperclip).
No doctl/psql/safety-CLI on box â bookend = confirmed automated DO backup.
fail2ban: ONE deliberate SSH connection per step; detached jobs via setsid.
Root / is HOT (~98%) â temp/artifacts to /mnt/paperclipdata.
`plugin upgrade` is registry-only â uninstall-then-install from the extract-dir (NOT the tgz).
Two-source version: package.json + src/manifest.ts:702 literal (host reads dist/manifest.js, built from src/manifest.ts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two-source version bump to 1.8.1 + clean rebuild + full honest-green sweep</name>
  <read_first>
    - package.json (the `version` field â currently 1.8.0)
    - src/manifest.ts (the `version:` literal at ~line 702 â currently '1.8.0'; the two-source-bump comments at ~635-662 explaining the kill-switch version-scoping reads the shipped build's version)
    - scripts/build-ui.mjs (the UI bundle build)
    - .planning/phases/21-stuck-agent-reply-in-place/21-CONTEXT.md (the live-drill section; two-source bump rule)
  </read_first>
  <action>
    Bump `package.json` `version` from `1.8.0` to `1.8.1`. Bump the `src/manifest.ts`
    `version` literal from `'1.8.0'` to `'1.8.1'` (the two MUST be byte-identical â the host
    reads dist/manifest.js built from src/manifest.ts; a mismatch ships v_new code under a
    v_old label, per the plugin-version-bump-two-sources rule). Do NOT add a migration, do
    NOT add a manifest capability. Run `npx tsc --noEmit`, then `node scripts/build-ui.mjs`,
    then the FULL nested test sweep (the recursive glob wired in Phase 20 â invoke the repo's
    test command, e.g. `node --test` over `test/**`). Fix any red before proceeding; the
    sweep must be honestly green (not green-by-skip).
  </action>
  <verify>
    <automated>cd "$CLAUDE_PROJECT_DIR" && node -e "const p=require('./package.json'); const m=require('fs').readFileSync('src/manifest.ts','utf8'); if(p.version!=='1.8.1') throw new Error('package.json='+p.version); if(!m.includes(\"version: '1.8.1'\")) throw new Error('manifest version literal not 1.8.1'); console.log('two-source bump ok 1.8.1')" && npx tsc --noEmit -p tsconfig.json && node scripts/build-ui.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` version === `1.8.1`
    - `src/manifest.ts` contains `version: '1.8.1'`
    - `npx tsc --noEmit` exits 0 and `node scripts/build-ui.mjs` exits 0
    - The full nested test sweep exits 0 (honestly green â record the suite count in SUMMARY)
    - No new `migrations/*.sql` and no new manifest capability (git status confirms)
  </acceptance_criteria>
  <done>Version is bumped in both sources to 1.8.1, the build is clean, and the full suite is honestly green â the artifact is ready to package.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm the automated DO backup bookend</name>
  <action>Operator confirms the bookend before any live mutation; see <how-to-verify>. No code change.</action>
  <what-built>The bookend prerequisite: a confirmed automated DO backup of the AriClaw droplet before any install/upgrade touches the live BEAAA instance.</what-built>
  <how-to-verify>
    1. In the DigitalOcean dashboard, open Droplets â AriClaw â Backups.
    2. Confirm a recent automated backup exists (taken before this deploy window).
    3. Confirm you can articulate the rehearsed rollback: uninstall-then-reinstall the
       prior v1.8.0 extract-dir (the additive plugin-namespace schema means disable/uninstall
       preserves data; no --purge).
  </how-to-verify>
  <resume-signal>Type "backup confirmed" (with the backup timestamp) or describe the gap.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint: Package + bookended uninstall-then-install v1.8.1 on BEAAA</name>
  <action>Operator packages and installs per <how-to-verify> (uninstall-then-install from the extract-dir; no migration; data preserved).</action>
  <what-built>The packaged v1.8.1 plugin tarball, ready to copy to /mnt/paperclipdata on AriClaw and install via uninstall-then-install from the extract-dir.</what-built>
  <how-to-verify>
    Operator-run in the local window (tunnel + SSH), ONE deliberate SSH connection per step,
    detached jobs via setsid, artifacts under /mnt/paperclipdata (root / is HOT):
    1. `npm pack` the v1.8.1 build locally; scp the tgz to /mnt/paperclipdata on AriClaw.
    2. Extract to a build dir under /mnt/paperclipdata.
    3. As beai-agent, from ~ : `npx -y paperclipai plugin uninstall` the clarity-pack id
       (a763176a-2f4d-4986-b190-b5151e42cc00) â data preserved (additive namespace; no --purge).
    4. `npx -y paperclipai plugin install` from the EXTRACT-DIR (NOT the tgz; `plugin upgrade`
       is registry-only).
    5. `npx -y paperclipai plugin enable` if needed; confirm the plugin list shows version
       1.8.1 (the version-label refresh).
  </how-to-verify>
  <resume-signal>Type "installed 1.8.1" or paste the failing step output.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Live stuck-agent reply→resume drill</name>
  <action>Operator runs the live drill per <how-to-verify> and records the verdict in the SUMMARY. No code change.</action>
  <what-built>The live stuck-agent replyâresume drill â the phase's live acceptance (STUCK-06).</what-built>
  <how-to-verify>
    On the live BEAAA Situation Room + Reader (opted-in session), against a real or staged
    AWAITING_AGENT_STUCK row:
    1. SITUATION ROOM: confirm the stuck agent's row sits in the QUIET Watch tier (NOT the
       loud Needs-you cluster) and shows a reply-to-unstick affordance (the shared
       <ReplyInPlace> with the "Nudge to unstick" copy) â NOT an "assign an owner" dead-end.
    2. NO-AUTO-RESUME: load/view the row and confirm the agent does NOT resume merely from
       viewing (STUCK-04) â no comment posted, no status change, until you Send.
    3. SEND: type a plain reply and Send. Confirm a canonical comment posts on the leaf issue
       and the stuck agent resumes (picks the work back up on its next heartbeat) â the
       Phase-10 Shape B recipe.
    4. READER: open the same issue's Reader live-blocker panel; confirm the same reply-to-
       unstick affordance appears (no wake button) and a Send there also resumes.
    5. NO_UUID_LEAK: confirm no raw/partial agent id or UUID appears anywhere in the
       affordance, its prose, the awaited-party label, or the resumed comment.
    6. Confirm the room did not storm (no agent-wake cascade) after the reply.
  </how-to-verify>
  <resume-signal>Type "drill PASS" (with notes) or describe what failed for a gap-closure pass.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local operator â live BEAAA host | SSH + plugin install against the production single-tenant filesystem-persistent box |
| plugin install â live Postgres | additive plugin-namespace only; disable/uninstall preserves data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Denial of service | live deploy | mitigate | bookended by confirmed automated DO backup + rehearsed uninstall-then-reinstall rollback; ONE SSH connection per step (fail2ban); artifacts to /mnt/paperclipdata (root / HOT) |
| T-21-11 | Tampering | live data | mitigate | additive plugin-namespace schema, NO migration this phase; uninstall preserves data (no --purge) |
| T-21-12 | Repudiation | the resume mutation | accept | the resume comment + Shape-B flip are operator-attributed (actorUserId) by the existing 14-01 handler â audited, unchanged |
| T-21-13 | Information disclosure | live human-facing text | mitigate | the drill step 5 explicitly verifies NO_UUID_LEAK on every new nudge surface (STUCK-06) |
| T-21-SC | Tampering | the published/installed artifact | mitigate | install is the locally-built v1.8.1 tarball from the extract-dir (internal-only, no npm publish per clarity-pack-internal-only memory); no third-party package added |
</threat_model>

<verification>
- Two-source version bump verified byte-identical to 1.8.1.
- Clean rebuild (tsc + UI bundle) and full honest-green test sweep before any deploy.
- Bookend: automated DO backup confirmed before install; rollback path rehearsed.
- Live drill: stuck row â reply-to-unstick on both surfaces â Send resumes the agent; no auto-resume on view; NO_UUID_LEAK clean; no storm.
- No new migration, no new capability anywhere in the phase.
</verification>

<success_criteria>
- v1.8.1 shipped to BEAAA, bookended and rolled-back-rehearsed.
- A real AWAITING_AGENT_STUCK row is unstuck from the cockpit via reply-in-place on both the SR and the Reader.
- STUCK-06 verified live (degrade-safe + NO_UUID_LEAK); the plugin-list label reads 1.8.1.
</success_criteria>

<output>
Create `.planning/phases/21-stuck-agent-reply-in-place/21-05-SUMMARY.md` when done.
Record the drill verdict, the backup timestamp (bookend), and any live riders for gap closure.
</output>
