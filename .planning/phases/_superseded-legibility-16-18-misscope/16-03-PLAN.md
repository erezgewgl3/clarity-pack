---
phase: 16-legibility-no-raw-identifiers-pass
plan: 03
type: execute
wave: 2
depends_on: [16-02]
files_modified:
  - src/worker/situation/build-employees-rollup.ts
  - test/worker/situation/build-employees-rollup-focusline.test.mjs
autonomous: true
requirements: [LEG-04]
must_haves:
  truths:
    - "The Situation Room focus line shows the plain-English TL;DR-cache summary when a cache row exists for the focus issue."
    - "When the cache read misses or throws, the focus line falls back to the existing polished issue title — never blanks, never blocks the row."
    - "The cache read is one read per FOCUS issue only (not per open issue) and is degrade-wrapped so it cannot stall the snapshot."
  artifacts:
    - path: "src/worker/situation/build-employees-rollup.ts"
      provides: "focusLine enriched from getTldrByScope(ctx,'issue',focusIssue.id), degrade-wrapped to polishTldr(title)"
      contains: "getTldrByScope"
    - path: "test/worker/situation/build-employees-rollup-focusline.test.mjs"
      provides: "Cache-hit + degrade-path (throw/miss → title) test for focusLine"
  key_links:
    - from: "src/worker/situation/build-employees-rollup.ts"
      to: "src/worker/db/tldr-cache.ts"
      via: "getTldrByScope(ctx, 'issue', focusIssue.id)"
      pattern: "getTldrByScope"
---

<objective>
Enrich the Situation Room focus line from the TL;DR cache (LEG-04): prefer the cached plain-English summary over the polished issue title, falling back to the title on any miss/throw — degrade-wrapped so it cannot stall or blank the snapshot.

Purpose: `build-employees-rollup.ts:344-350` currently uses `polishTldr(focusIssue.title)` only. LEG-04 wires the long-deferred `getTldrByScope` read. The cold snapshot is already 25.7s (~4s from the 30s cliff); PERF re-arch is Phase 18, so this read MUST be cheap (one read per focus issue) and degrade-safe — a throw/miss falls back to the existing title path, never blocks the per-agent `Promise.all` row.
Output: focusLine reads the cache first, degrade-wrapped; cache-hit + degrade-path test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-CONTEXT.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-RESEARCH.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-PATTERNS.md

<interfaces>
<!-- The cache read to CONSUME (read-only — do NOT modify tldr-cache.ts) -->
From src/worker/db/tldr-cache.ts:
- `export async function getTldrByScope(ctx: TldrCacheCtx, surface: TldrRow['surface'], scopeId: string): Promise<TldrRow | null>`
- `TldrRow.body` = plain-text summary; returns `null` when no row exists.

<!-- The focusLine site + the degrade-to-null precedent already in this file -->
From src/worker/situation/build-employees-rollup.ts:
- focusLine block at :344-350: `if (state !== 'idle' && state !== 'stale') { const rawFocus = focusIssue?.title ?? ''; const polished = polishTldr(rawFocus); focusLine = polished.length > 80 ? \`${polished.slice(0,77)}…\` : polished || null; }`
- `focusIssue.id` is available and already used at :355,:360,:371 (the blockerChain branch). `focusIssue?.title` at :347.
- The agent-name resolver block at :415-430 uses the `try { ... } catch { ...set null }` D-09 degrade-to-null idiom — the wrapper model to mirror.
- The rollup runs per-agent inside `Promise.all` with per-row try/catch.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read getTldrByScope for the focus issue, degrade-wrapped to the title path</name>
  <files>src/worker/situation/build-employees-rollup.ts, test/worker/situation/build-employees-rollup-focusline.test.mjs</files>
  <read_first>
    - src/worker/situation/build-employees-rollup.ts:340-355 (the focusLine block + the surrounding state guard), :415-430 (the D-09 degrade-to-null `try/catch` resolver pattern to mirror for the cache read)
    - src/worker/db/tldr-cache.ts:97-111 (the `getTldrByScope` signature + TldrRow.body shape — read-only consume)
    - test/worker/situation (sibling rollup tests — match the existing harness/fixture shape for the new test)
  </read_first>
  <behavior>
    - Cache HIT: when `getTldrByScope(ctx,'issue',focusIssue.id)` returns a row, focusLine uses `tldr.body` (truncated ≤80, same `slice(0,77)+…` rule as the title path).
    - Cache MISS (null): focusLine falls back to the existing `polishTldr(focusIssue.title)` path.
    - Cache THROW: the read is wrapped so a thrown error degrades silently to the `polishTldr(title)` path — never blanks focusLine, never rejects the per-agent Promise.
    - idle/stale state: focusLine stays null (unchanged — the read only runs in the non-idle/non-stale branch, and only when focusIssue.id exists).
    - One read per FOCUS issue only — NOT a read per open issue.
  </behavior>
  <action>
    In `build-employees-rollup.ts`, inside the `if (state !== 'idle' && state !== 'stale')` block (:344-350): before computing the title path, when `focusIssue?.id` exists, attempt `const tldr = await getTldrByScope(ctx, 'issue', focusIssue.id);` inside a `try/catch` (mirror the :415-430 D-09 idiom — catch → null). On a hit with a non-empty `tldr.body`, set `rawFocus` (or focusLine source) to `tldr.body`; otherwise keep the existing `focusIssue?.title ?? ''` source. Run the SAME `polishTldr` + ≤80 truncation on whichever source won, so the output format is identical. Import `getTldrByScope` from `../db/tldr-cache.ts` (verify the relative path against the file's existing imports). Add a comment citing LEG-04 + the degrade-safety + "one read per focus issue, NOT per open issue; cold snapshot is 25.7s, PERF re-arch is Phase 18 — keep this cheap and degrade-wrapped".
    Do NOT add a read for non-focus issues. Do NOT modify `tldr-cache.ts`. Do NOT introduce a per-row blocking await outside the existing per-agent flow.
    Create `test/worker/situation/build-employees-rollup-focusline.test.mjs` (node:test): with a stub `ctx` whose `getTldrByScope`-backing `ctx.db.query` returns a TldrRow → assert focusLine equals the polished/truncated `body`; with a query returning `[]` (miss) → assert focusLine equals the polished title; with a query that THROWS → assert focusLine still equals the polished title (degrade path, no throw propagates).
  </action>
  <acceptance_criteria>
    - Grep `build-employees-rollup.ts`: `getTldrByScope` is called exactly once in the focusLine path, inside a `try/catch`.
    - The cache read is gated on `focusIssue?.id` and lives in the non-idle/non-stale branch.
    - `tldr-cache.ts` is NOT in `files_modified` (read-only reuse).
    - `node --test test/worker/situation/build-employees-rollup-focusline.test.mjs` exits 0 with all three cases (hit / miss / throw→title) green.
    - `pnpm typecheck` exits 0 and `pnpm build:worker` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm build:worker && node --test test/worker/situation/build-employees-rollup-focusline.test.mjs</automated>
  </verify>
  <done>focusLine prefers the cached plain-English body on a hit and falls back to the polished title on miss/throw, proven by the three-case test; the read is one-per-focus-issue and degrade-wrapped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB read→snapshot row | A new per-focus cache read on the synchronous snapshot hot path (cold time near the 30s cliff) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08 | Denial of Service | build-employees-rollup focusLine read | mitigate | One read per FOCUS issue only (not per open issue); degrade-wrapped try/catch → title path so a slow/throwing read never stalls or blanks the row; cold-time checked on BEAAA during drill |
| T-16-09 | Tampering | tldr-cache.ts | mitigate | Read-only consume; not modified |
| T-16-10 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan |
</threat_model>

<verification>
- focusLine uses the cached body on a hit, the polished title on miss/throw.
- The read is gated on focusIssue.id and only runs for the focus issue.
- A throwing cache read does NOT reject the per-agent Promise (degrade test green).
- Cold-snapshot time must not materially regress (operator note for the BEAAA drill — PERF is Phase 18).
</verification>

<success_criteria>
- LEG-04: the SR focus line shows the plain-English TL;DR summary when available, polished title otherwise, never the bare/raw title and never a blank.
- `pnpm typecheck && pnpm build:worker && pnpm test` pass.
</success_criteria>

<output>
Create `.planning/phases/16-legibility-no-raw-identifiers-pass/16-03-SUMMARY.md` when done
</output>
