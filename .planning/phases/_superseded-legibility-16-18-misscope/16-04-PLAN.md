---
phase: 16-legibility-no-raw-identifiers-pass
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/surfaces/chat/topic-strip.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - src/ui/surfaces/chat/index.tsx
  - test/ui/surfaces/chat/chat-no-hex-fragments.test.mjs
autonomous: true
requirements: [LEG-01]
must_haves:
  truths:
    - "The Chat topic chip never renders an `id.slice(0,8).toUpperCase()` hex fragment (CHT-04FCAC7C); it shows a human-readable label."
    - "The Chat run-link chip never renders a raw `runId.slice(0,8)` hex fragment; it shows a friendly run reference."
    - "The task-created toast never renders an `issueId.slice(0,8)` hex fragment; the assignee name (or a friendly identifier) carries the meaning."
  artifacts:
    - path: "src/ui/surfaces/chat/topic-strip.tsx"
      provides: "chtLabel hex-fragment branch replaced with topic.title (or friendly fallback); numeric CHT-NN form preserved"
      contains: "topic.title"
    - path: "test/ui/surfaces/chat/chat-no-hex-fragments.test.mjs"
      provides: "Source-grep guard asserting no raw .slice(0,8) hex-fragment render in the three chat files"
  key_links:
    - from: "src/ui/surfaces/chat/message-thread.tsx"
      to: "row.title / friendly run label"
      via: "run-link chip humanization"
      pattern: "clarity-ref-chip"
---

<objective>
Humanize the Chat surface id-fragment chips (LEG-01, A3): replace `CHT-<8hex>` topic chips, `run·<8hex>` run-id fragments, and the `issueId.slice(0,8)` task-toast fragment with human-readable labels already in hand at each site.

Purpose: These three chat sites slice ids into deliberate short hex labels and bypass `scrubHumanAction` entirely (they are issue/run/topic ids, not agent-verdict labels), so the scrub-layer fix (16-02) does NOT reach them. A3 widens Phase 16 to humanize them with the friendly labels already adjacent at each render: `topic.title`, `row.title`, `employee.name`. Numeric `CHT-NN` (a legitimate human-readable issue identifier) stays; only the hex-fragment branches are leaks. This plan touches only the chat surface (no overlap with 16-01/16-02) so it runs in parallel in wave 1.
Output: three chat sites humanized; source-grep guard against re-introducing a hex-fragment render.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-CONTEXT.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-RESEARCH.md
@.planning/phases/16-legibility-no-raw-identifiers-pass/16-PATTERNS.md

<interfaces>
<!-- The exact leak sites + the friendly labels ALREADY in hand at each -->
From src/ui/surfaces/chat/topic-strip.tsx:
- `chtLabel(topic)` at :78-83: `return id ? id.slice(0, 8).toUpperCase() : 'CHT-—';` is the hex-fragment branch (CHT-04FCAC7C). The `/^CHT-\d+$/i` and `/^\d+$/` branches are legit numeric ids — KEEP.
- Render at :299-300: `<span className="topic-title">{topic.title}</span>` then `<span className="id">{chtLabel(topic)}</span>` — `topic.title` is the friendly label, already adjacent. `ChatTopic.originIssueIdentifier` (BEAAA-NNN, :53-59) is a server-resolved friendly id where present.

From src/ui/surfaces/chat/message-thread.tsx:1116-1124 (run-link case):
- `run · {(row.runId ?? '').slice(0, 8)}{row.title ? \` · ${row.title}\` : ''}` — `row.title` already interpolated. The sibling `agent_link` case (:1107-1115) renders `{row.name ?? 'agent'}` — the clean noun-fallback model to copy.

From src/ui/surfaces/chat/index.tsx:759-769 (task-created toast):
- `const shortId = result.issueId ? result.issueId.slice(0, 8) : '—';` then `↗ Task created — ${shortId}, assigned to ${employeeName}.` — `employee?.name ?? 'employee'` at :765 is the existing noun-fallback model.

Scoped CSS gate: all chat render markup stays under `[data-clarity-surface]`; bundle-size CI ceiling applies (text-only changes must not exceed it).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Humanize the topic chip and the run-link chip</name>
  <files>src/ui/surfaces/chat/topic-strip.tsx, src/ui/surfaces/chat/message-thread.tsx</files>
  <read_first>
    - src/ui/surfaces/chat/topic-strip.tsx:76-83 (chtLabel — the hex-fragment branch), :288-301 (the chip render with `topic.title` already adjacent), the `ChatTopic` type incl. `title` (:44) and `originIssueIdentifier` (:53-59)
    - src/ui/surfaces/chat/message-thread.tsx:1107-1115 (the `agent_link` noun-fallback model `{row.name ?? 'agent'}`), :1116-1124 (the `run_link` hex-fragment chip to fix)
  </read_first>
  <action>
    In `topic-strip.tsx`: change the `chtLabel` fallback branch (:82) so the hex-fragment path `id.slice(0,8).toUpperCase()` is NOT used as the chip label. KEEP the numeric `/^CHT-\d+$/i` and `/^\d+$/` branches (legit human-readable ids). For the non-numeric/raw-uuid case, prefer `topic.originIssueIdentifier` if present (BEAAA-NNN), else fall back to a friendly non-hex label — since `topic.title` is already rendered in the adjacent `.topic-title` span, the `.id` chip for a raw-uuid topic should render a non-hex reference (e.g. drop to a neutral `'CHT-—'`-style placeholder or the originIssueIdentifier) rather than the 8-hex slug. Do not duplicate the title across both spans. No CSS change to `.id` / `.topic-title` (text-only).
    In `message-thread.tsx` run_link case (:1116-1124): drop the raw `(row.runId ?? '').slice(0, 8)` hex fragment. Render a friendly run reference instead — prefer `row.title` when present (already interpolated as the suffix), else a noun label like `'latest run'` (mirror the sibling `agent_link` `{row.name ?? 'agent'}` noun-fallback). The chip must contain NO 8-hex run-id fragment.
  </action>
  <acceptance_criteria>
    - Grep `topic-strip.tsx`: the chip-label path no longer renders `id.slice(0, 8).toUpperCase()` as the visible label; numeric `CHT-NN` branches preserved.
    - Grep `message-thread.tsx` run_link case: no `runId ?? '').slice(0, 8)` rendered; the chip shows `row.title` or a noun label.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm build:ui</automated>
  </verify>
  <done>The topic chip and run-link chip render human-readable labels with no 8-hex fragment; numeric ids preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Humanize the task-created toast + add a chat hex-fragment source-grep guard</name>
  <files>src/ui/surfaces/chat/index.tsx, test/ui/surfaces/chat/chat-no-hex-fragments.test.mjs</files>
  <read_first>
    - src/ui/surfaces/chat/index.tsx:755-772 (the task-created toast with `shortId = result.issueId.slice(0,8)` and `employeeName = employee?.name ?? 'employee'`)
    - test/ui/surfaces/situation-room/employee-row-no-uuid-leak.test.mjs:1-55 (the `stripComments` + source-grep render-scan pattern to mirror for the new chat guard — no jsdom)
  </read_first>
  <action>
    In `index.tsx` (:759-769): remove the `result.issueId.slice(0, 8)` hex fragment from the toast. Either drop the short-id token from the copy entirely (the assignee name `employeeName` carries the meaning — `↗ Task created, assigned to ${employeeName}.`) OR, if a friendly identifier is available on the success payload, use it; do NOT render the 8-hex slice. Keep the `employee?.name ?? 'employee'` noun-fallback.
    Create `test/ui/surfaces/chat/chat-no-hex-fragments.test.mjs` (node:test) mirroring the employee-row-no-uuid-leak render-scan: read the three chat source files (topic-strip.tsx, message-thread.tsx, index.tsx), `stripComments`, and assert NONE renders a raw hex-fragment slice as visible text — assert the stripped source contains no `.slice(0, 8).toUpperCase()` chip-label render and no `runId ?? '').slice(0, 8)` / `issueId.slice(0, 8)` rendered-token pattern. Scope the assertion to the render expressions, not arbitrary slice usage elsewhere (anchor on the specific patterns above, per A2's anchored-not-blanket principle).
  </action>
  <acceptance_criteria>
    - Grep `index.tsx`: the toast no longer renders `result.issueId.slice(0, 8)`.
    - `test/ui/surfaces/chat/chat-no-hex-fragments.test.mjs` exists and asserts the three files render no anchored hex-fragment token.
    - `node --test test/ui/surfaces/chat/chat-no-hex-fragments.test.mjs` exits 0.
    - `pnpm test` passes; `node scripts/check-css-scope.mjs` exits 0; `pnpm check-ui-bundle-size` passes (note the ceiling).
  </acceptance_criteria>
  <verify>
    <automated>pnpm typecheck && pnpm build:ui && pnpm test && node scripts/check-css-scope.mjs && pnpm check-ui-bundle-size</automated>
  </verify>
  <done>The task toast carries no hex fragment, and a source-grep guard prevents re-introducing any of the three chat hex-fragment renders; build/test/scope/bundle gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| id field→chat chip render | Topic/run/issue ids sliced to hex fragments cross into operator-visible chat DOM, bypassing scrubHumanAction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Information Disclosure | chat topic / run / toast hex chips | mitigate | Replace 8-hex slices with the friendly labels already in hand (topic.title / row.title / employee.name); source-grep guard prevents regression |
| T-16-12 | Tampering | over-broad chat slice scan | accept | Guard anchored to the three specific render patterns (not all `.slice` usage) per A2 — accepts non-render slice usage elsewhere is unscanned |
| T-16-13 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this plan |
</threat_model>

<verification>
- None of the three chat sites renders an 8-hex id fragment as visible text.
- Numeric `CHT-NN` human-readable ids are preserved (not over-scrubbed).
- The chat source-grep guard is anchored to the specific render patterns (no false-positive on unrelated slices).
- All markup stays under `[data-clarity-surface]`; bundle ceiling respected.
</verification>

<success_criteria>
- LEG-01 (Chat): the Chat surface carries no raw CHT-/run·/issueId hex fragments; agent/topic/run references read human.
- `pnpm typecheck && pnpm build:ui && pnpm test && node scripts/check-css-scope.mjs && pnpm check-ui-bundle-size` all pass.
</success_criteria>

<output>
Create `.planning/phases/16-legibility-no-raw-identifiers-pass/16-04-SUMMARY.md` when done
</output>
