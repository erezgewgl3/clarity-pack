---
phase: quick-260516-gx4
plan: 01
status: complete
type: execute
wave: 1
depends_on: []
files_modified:
  - test/helpers/host-faithful-agents.mjs
  - test/helpers/host-faithful-sessions.mjs
  - test/helpers/host-faithful-ctx.mjs
  - test/worker/bulletin/compile-bulletin-host-faithful.test.mjs
  - test/worker/bulletin/compile-pass-1-json-extraction.test.mjs
  - src/worker/bulletin/compile-pass-1.ts
autonomous: true
requirements: [BULL-05, BULL-06, BULL-09]
must_haves:
  truths:
    - "Running registerCompileBulletinJob against a host-faithful ctx fails locally in node --test on any of the 9 host-contract violations"
    - "ctx.agents.pause/get/resume reject a non-UUID agentId exactly as the live host does (invalid input syntax for type uuid)"
    - "ctx.agents.sessions.sendMessage throws 'Agent wakeup was skipped by heartbeat policy' when the agent is not invokable"
    - "compilePass1 extracts and parses BulletinDraft JSON whether the agent returns raw JSON, fenced ```json, or JSON with a prose preamble"
    - "compilePass1 still rejects genuinely non-JSON output as 'LLM output was not valid JSON'"
  artifacts:
    - path: "test/helpers/host-faithful-agents.mjs"
      provides: "Reusable host-faithful ctx.agents fake (get/resume/pause UUID enforcement + managed.reconcile + heartbeat-policy sendMessage rejection)"
      min_lines: 80
    - path: "test/helpers/host-faithful-ctx.mjs"
      provides: "Assembled host-faithful ctx covering every surface compile-bulletin.ts touches"
      min_lines: 60
    - path: "test/worker/bulletin/compile-bulletin-host-faithful.test.mjs"
      provides: "End-to-end test running registerCompileBulletinJob against the assembled host-faithful ctx"
      min_lines: 60
    - path: "test/worker/bulletin/compile-pass-1-json-extraction.test.mjs"
      provides: "RED tests for fenced/prose-wrapped JSON extraction"
      min_lines: 40
  key_links:
    - from: "test/worker/bulletin/compile-bulletin-host-faithful.test.mjs"
      to: "src/worker/jobs/compile-bulletin.ts"
      via: "registerCompileBulletinJob(ctx) with the assembled host-faithful ctx"
      pattern: "registerCompileBulletinJob"
    - from: "src/worker/bulletin/compile-pass-1.ts"
      to: "extractJsonObject helper"
      via: "compilePass1 calls extractJsonObject(raw) before JSON.parse"
      pattern: "extractJsonObject"
---

<objective>
Make the bulletin compile path testable LOCALLY against host-faithful fakes so host-contract violations fail in `node --test`, not on a Countermoves VPS reinstall — then fix Defect B (compilePass1 rejecting valid-but-wrapped LLM JSON).

Purpose: Eric's 2026-05-16 decision — stop finding compile-path defects one VPS reinstall at a time. The 9-item host-constraint catalogue (each a real live-drill defect) must be encoded into reusable test fakes, and Defect B must be TDD-fixed against a scripted host-faithful session.

Output:
- `test/helpers/host-faithful-agents.mjs` — new reusable host-faithful `ctx.agents` fake.
- `test/helpers/host-faithful-ctx.mjs` — assembled host-faithful ctx covering every surface `compile-bulletin.ts` touches.
- Extended `test/helpers/host-faithful-sessions.mjs` — adds the heartbeat-policy rejection.
- `compile-bulletin-host-faithful.test.mjs` — end-to-end test that fails locally on any host-contract violation.
- `compile-pass-1-json-extraction.test.mjs` + a JSON-extraction fix in `compile-pass-1.ts` (Defect B).
</objective>

<execution_context>
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/workflows/execute-plan.md
@C:/Users/erezg/.claude/plugins/cache/gsd-plugin/gsd/2.38.8/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/worker/jobs/compile-bulletin.ts
@src/worker/bulletin/compile-pass-1.ts
@src/worker/agents/session-llm-adapter.ts
@test/helpers/host-faithful-db.mjs
@test/helpers/host-faithful-sessions.mjs
@test/worker/bulletin/compile-bulletin-end-to-end.test.mjs

<interfaces>
<!-- Contracts the executor needs. Do not re-explore the codebase. -->

src/worker/jobs/compile-bulletin.ts — `registerCompileBulletinJob(ctx: CompileBulletinCtx): void`.
The job, on each fire, touches EXACTLY these ctx surfaces:
  - `ctx.companies.list()` → Company[]
  - `ctx.agents.managed.reconcile(EDITOR_AGENT_KEY, companyId)` → { agentId, agent, status }
  - `ctx.agents.get(agentId, companyId)` → Agent | null   (agentId MUST be a real UUID)
  - `ctx.agents.resume(agentId, companyId)` / `ctx.agents.pause(agentId, companyId)`  (agentId MUST be a real UUID)
  - `ctx.agents.sessions.create/sendMessage/close` (via sessionLlmAdapter)
  - `ctx.db.query(sql, params)` (SELECT-only) / `ctx.db.execute(sql, params)` (DML, returns {rowCount})
  - `ctx.issues.list({companyId})` / `ctx.issues.create(args)` / `ctx.issues.createComment(...)`
  - `ctx.logger.info/warn/error`
  - `ctx.jobs.register(key, fn)`

EDITOR_AGENT_KEY (manifest agents[] key) = 'editor-agent' (imported from src/worker/agents/editor.ts).
EDITOR_AGENT_ID_TAG = 'clarity-pack-editor-agent' — a TEXT attribution tag ONLY, NEVER a UUID.

src/worker/bulletin/compile-pass-1.ts — `compilePass1(ctx, args): Promise<BulletinDraft>`.
Currently at L193-204: `parsed = JSON.parse(raw)` directly on the adapter output;
a non-bare-JSON `raw` throws → `recordFailure(... reason: 'llm_output_not_json: ...')`
→ `throw new Error('compilePass1: LLM output was not valid JSON')`. THIS is Defect B.

circuit-breaker — `ctx.agents.pause(agentId, companyId)` SDK signature; agentId is the resolved UUID.

EXISTING test/helpers/host-faithful-db.mjs — `wrapHostFaithfulDb(fakeDb)` already encodes catalogue item 1
(query SELECT-only/one-statement; execute DML-in-namespace, returns {rowCount}, never rows). REUSE — do not reimplement.

EXISTING test/helpers/host-faithful-sessions.mjs — `makeHostFaithfulAgents(opts)` exports
`SESSION_TASKKEY_PREFIX` and encodes catalogue item 2 (taskKey namespace contract).
It already has an `opts` surface: agentStatus, agentNull, notFoundForFirstNSends,
sendMessageRejection, events, replayNone. EXTEND this file — do not fork it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the host-faithful agents fake + extend the sessions fake (catalogue items 3, 4, 8)</name>
  <files>test/helpers/host-faithful-agents.mjs, test/helpers/host-faithful-sessions.mjs</files>
  <behavior>
    Catalogue item 3 — UUID enforcement (NEW reusable fake):
    - host-faithful-agents.mjs `get(agentId, companyId)`, `resume(agentId, companyId)`,
      `pause(agentId, companyId)` MUST throw `Error('invalid input syntax for type uuid: "<agentId>"')`
      when agentId is not a valid UUID v1-5 (regex /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i).
    - Test: `pause('clarity-pack-editor-agent', 'COU')` rejects with /invalid input syntax for type uuid/.
    - Test: `pause('11111111-1111-4111-8111-111111111111', 'COU')` resolves.
    - `managed.reconcile(agentKey, companyId)` returns `{ agentId: <a real UUID>, agent: {...}, status: 'resolved' }` —
      the resolved agentId is ALWAYS a UUID (never the agentKey string).
    Catalogue item 4 — heartbeat-policy rejection (EXTEND host-faithful-sessions.mjs):
    - Add an opt `heartbeatPolicySkip` (boolean). When true, `sendMessage` throws
      `Error('Agent wakeup was skipped by heartbeat policy')` — a DISTINCT message from
      'Session not found'. Test: with heartbeatPolicySkip the adapter does NOT retry it
      (isTransientSessionNotFound returns false for it).
    Catalogue item 8 — editor-agent key/tag distinction:
    - host-faithful-agents.mjs exports `EDITOR_AGENT_KEY = 'editor-agent'` and
      `EDITOR_AGENT_ID_TAG = 'clarity-pack-editor-agent'`. Test asserts the two are
      different strings and that ID_TAG fails the UUID regex.
  </behavior>
  <action>
    RED first: create test/helpers/host-faithful-agents.test.mjs (or add cases to a new
    section of an existing helper test) with the assertions in <behavior> — these MUST fail
    because host-faithful-agents.mjs does not yet exist and host-faithful-sessions.mjs has
    no heartbeatPolicySkip opt.

    GREEN: Create `test/helpers/host-faithful-agents.mjs` exporting
    `makeHostFaithfulAgents(opts)` (a richer agents fake than host-faithful-sessions.mjs's
    — covers get/resume/pause/managed.reconcile) and the `isUuid`, `EDITOR_AGENT_KEY`,
    `EDITOR_AGENT_ID_TAG` constants. The pause/get/resume methods classify agentId through
    isUuid BEFORE doing anything and throw the host-faithful uuid-syntax error on a miss.
    `managed.reconcile` always returns a UUID agentId.

    Compose, do not duplicate: host-faithful-agents.mjs should accept an optional
    `sessions` slice (default: delegate to makeHostFaithfulAgents from host-faithful-sessions.mjs)
    so the assembled ctx in Task 2 gets ONE coherent `ctx.agents` object whose `sessions`
    surface still enforces the taskKey contract.

    Extend host-faithful-sessions.mjs: add the `heartbeatPolicySkip` opt to
    `makeHostFaithfulAgents`; when set, `sendMessage` throws the verbatim
    'Agent wakeup was skipped by heartbeat policy' string before the taskKey lookup.
    Add a one-line doc comment referencing catalogue item 4. Do NOT change any existing opt.

    NO production code changes in this task — test infrastructure only.
  </action>
  <verify>
    <automated>node --test "test/helpers/host-faithful-agents.test.mjs" && node --test "test/worker/agents/session-llm-adapter.test.mjs"</automated>
  </verify>
  <done>host-faithful-agents.mjs exists and enforces catalogue items 3 + 8; host-faithful-sessions.mjs has the heartbeatPolicySkip opt (item 4); the existing session-llm-adapter suite still passes (no regression to host-faithful-sessions.mjs's existing opts).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assemble the host-faithful ctx + the compile-bulletin end-to-end host-contract test</name>
  <files>test/helpers/host-faithful-ctx.mjs, test/worker/bulletin/compile-bulletin-host-faithful.test.mjs</files>
  <behavior>
    host-faithful-ctx.mjs `makeHostFaithfulCompileCtx(opts)` assembles a ctx that runs
    `registerCompileBulletinJob` end-to-end and fails LOCALLY on any host-contract violation.
    It composes the existing fakes — wrapHostFaithfulDb (item 1), host-faithful-sessions
    (item 2 + 4), host-faithful-agents (items 3 + 8) — plus thin host-faithful
    companies/issues/jobs/logger fakes. It must encode the remaining catalogue items:
      - Item 5: the logger fake DROPS any metadata object passed as a 2nd arg — only the
        message string is retained in `loggedMessages`. A test asserts evidence the job
        needs must be IN the message string.
      - Item 6: not exercised by the compile job at runtime (it is a migration-validator
        concern already covered by ddl-prefix-validator.test.mjs); host-faithful-ctx.mjs
        documents this as out-of-scope-for-runtime in a comment — no fake surface needed.
      - Item 7: the in-memory bulletins table treats `cycle_number 0` as the bootstrap
        sentinel; `MAX(cycle_number)` over published rows ignores cycle 0; first real
        publish is cycle 1. A test asserts a fresh company bootstraps at cycle 0 then
        publishes at cycle 1.
      - Item 9: `ctx.issues.list` returns Issue rows with `assigneeUserId` and NO
        `lastActorId`/`lastActorName` field (the lineage path must use assigneeUserId).
    The compile-bulletin-host-faithful.test.mjs runs `registerCompileBulletinJob` against
    this ctx and asserts the happy path publishes exactly one issue, AND that each
    host-contract trap (write-via-query, non-UUID pause, non-conforming session taskKey,
    heartbeat-policy skip) makes the job fail observably (recordCompileFailure / no publish)
    rather than silently passing.
  </behavior>
  <action>
    RED first: write compile-bulletin-host-faithful.test.mjs. At least these cases —
    each MUST genuinely fail before host-faithful-ctx.mjs exists / is correct:
      1. happy path: due company, scripted valid session → exactly one ctx.issues.create,
         a bulletins row at compile_status 'published', cycle_number 1.
      2. bootstrap: a company with no prior bulletin row writes a cycle_number 0 sentinel
         row and does NOT publish on the first fire (item 7).
      3. non-UUID pause trap: drive 3 consecutive verifier rejections; assert the breaker's
         ctx.agents.pause receives a real UUID (the resolved reconcile output) and does NOT
         throw 'invalid input syntax for type uuid' (item 3).
      4. heartbeat-policy trap: a session whose sendMessage throws the heartbeat-policy
         message → recordCompileFailure with that message, no publish, no hang (item 4).
      5. logger-metadata trap: assert a chosen diagnostic value (e.g. companyId, cycle)
         appears inside a logged MESSAGE string, never only in dropped metadata (item 5).

    GREEN: create `test/helpers/host-faithful-ctx.mjs` exporting
    `makeHostFaithfulCompileCtx(opts)`. Reuse the in-memory bulletins/issues table logic
    from the EXISTING compile-bulletin-end-to-end.test.mjs makeFakeCtx (lift it into the
    helper — that test already models the bulletins/issues SQL faithfully). Wrap db with
    `wrapHostFaithfulDb`. Build `ctx.agents` from host-faithful-agents.mjs composed with
    host-faithful-sessions.mjs. The logger fake stores ONLY the message string.

    Keep the existing compile-bulletin-end-to-end.test.mjs file UNCHANGED — this new file
    is the host-contract net; the old file stays as the behavioral regression suite.

    NO production code changes in this task — test infrastructure only. If a host-contract
    test surfaces a REAL production defect (beyond Defect B), STOP and report it rather than
    silently patching production here.
  </action>
  <verify>
    <automated>node --test "test/worker/bulletin/compile-bulletin-host-faithful.test.mjs" "test/worker/bulletin/compile-bulletin-end-to-end.test.mjs"</automated>
  </verify>
  <done>host-faithful-ctx.mjs assembles a ctx encoding catalogue items 1,2,3,4,5,7,9; compile-bulletin-host-faithful.test.mjs passes (happy path + 4 trap cases); the existing end-to-end suite still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Defect B — robust JSON extraction in compilePass1</name>
  <files>test/worker/bulletin/compile-pass-1-json-extraction.test.mjs, src/worker/bulletin/compile-pass-1.ts</files>
  <behavior>
    A new exported pure helper `extractJsonObject(raw: string): string` in compile-pass-1.ts:
      - raw bare JSON object `{"masthead":...}` → returned unchanged.
      - fenced ` ```json\n{...}\n``` ` → the inner `{...}` returned.
      - fenced ` ```\n{...}\n``` ` (no language tag) → inner `{...}` returned.
      - prose preamble `Here is the bulletin:\n{...}` → the `{...}` substring returned
        (first `{` to its matching `}`, brace-balanced, quote-aware so braces inside
        strings do not miscount).
      - prose preamble + fenced block → the fenced JSON wins.
      - genuinely non-JSON (`I could not complete this`) → THROW (no `{` found) so
        compilePass1 still rejects it as 'LLM output was not valid JSON'.
    compilePass1: replace the direct `JSON.parse(raw)` at L195 with
    `JSON.parse(extractJsonObject(raw))`. The existing recordFailure-on-bad-JSON path and
    the 'LLM output was not valid JSON' error message stay byte-identical for the
    genuinely-non-JSON case.
  </behavior>
  <action>
    RED first: write compile-pass-1-json-extraction.test.mjs. Two test groups —
      (a) direct `extractJsonObject` unit cases per <behavior>;
      (b) `compilePass1` integration cases using the host-faithful sessions fake from
          Task 1/2: script the agent to stream (i) fenced JSON, (ii) JSON-with-prose-preamble,
          (iii) raw JSON, (iv) non-JSON prose. Cases (i)-(iii) MUST yield a parsed
          BulletinDraft; case (iv) MUST still reject with /LLM output was not valid JSON/.
    These MUST fail before the fix — `extractJsonObject` does not yet exist and
    compilePass1 currently JSON.parses the raw fenced string and throws.

    GREEN: add `extractJsonObject` to compile-pass-1.ts. Implementation order:
      1. If a ```` ```json ```` or bare ```` ``` ```` fence is present, take the content
         between the first fence pair.
      2. Else (or if the fenced content still has surrounding prose), scan for the first
         `{`, then walk forward counting brace depth, respecting `"`-delimited strings and
         `\"` escapes, and return the substring through the matching `}`.
      3. If no `{` exists, throw `Error('no JSON object found in LLM output')`.
    Wire it into compilePass1: `const extracted = extractJsonObject(raw); parsed = JSON.parse(extracted);`
    inside the existing try/catch — so an extraction throw OR a parse throw both still hit
    the recordFailure path and the 'LLM output was not valid JSON' message. Do NOT change
    any other compilePass1 behavior, the recordFailure calls, or the token cap.

    This is the ONLY production change in the plan. Run `npx tsc --noEmit` after.
  </action>
  <verify>
    <automated>node --test "test/worker/bulletin/compile-pass-1-json-extraction.test.mjs" "test/worker/bulletin/compile-pass-1.test.mjs"</automated>
  </verify>
  <done>extractJsonObject handles fenced/prose/raw JSON and throws on non-JSON; compilePass1 parses wrapped JSON into a BulletinDraft; genuinely-non-JSON output still rejects as 'LLM output was not valid JSON'; the existing compile-pass-1 suite still passes; `npx tsc --noEmit` is clean.</done>
</task>

</tasks>

<verification>
- `node --test "test/**/*.test.mjs"` — full suite green (591 baseline + new tests; 0 fail).
- `npx tsc --noEmit` — clean.
- `node scripts/build-worker.mjs` — worker bundle builds (the only file with a production change is compile-pass-1.ts).
- Each new test file genuinely RED before its GREEN commit (TDD discipline — the whole point is the tests catch host-contract violations).
</verification>

<success_criteria>
- A reusable host-faithful `ctx.agents` fake (`host-faithful-agents.mjs`) encodes catalogue items 3 + 8.
- `host-faithful-sessions.mjs` extended with the heartbeat-policy rejection (item 4) — no regression to existing opts.
- `host-faithful-ctx.mjs` assembles a ctx covering every surface `compile-bulletin.ts` touches, encoding catalogue items 1,2,3,4,5,7,9.
- `compile-bulletin-host-faithful.test.mjs` runs `registerCompileBulletinJob` against the assembled ctx and fails locally on any of the 4 host-contract traps.
- Defect B fixed: `compilePass1` extracts and parses fenced / prose-wrapped JSON; genuinely non-JSON output still rejects as 'LLM output was not valid JSON'.
- Production behavior unchanged beyond the `extractJsonObject` JSON-extraction fix in `compile-pass-1.ts`.
</success_criteria>

<output>
After completion, create `.planning/quick/260516-gx4-compile-path-host-faithful-test-hardenin/260516-gx4-SUMMARY.md`
</output>
