---
quick_id: 260524-s2y
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/surfaces/reader/index.tsx
  - src/ui/surfaces/reader/ac-checklist.tsx
  - test/ui/ac-checklist-refresh-on-toggle.test.mjs
  - package.json
  - src/manifest.ts
autonomous: true
tags: [reader, ac-checklist, sdk-2026.512.0, rc.6]
out_of_scope:
  - "No `actions[]` entry will be added to src/manifest.ts. The operator's literal request — 'add an `invalidates` declaration to the ac-toggle action in src/manifest.ts' — was based on a misread of the SDK shape. `PaperclipPluginManifestV1` in @paperclipai/plugin-sdk@2026.512.0 exposes NO `actions:` manifest field and the SDK type tree contains ZERO occurrences of `invalidat*` (verified by reading dist/types.d.ts + grepping the SDK directory). `ctx.actions.register(key, handler)` is a runtime-only registration; data invalidation is a UI-side concern via `PluginDataResult.refresh()` returned from `usePluginData`. Adding a phantom `actions[].invalidates` field to the manifest would (a) fail tsc against `PaperclipPluginManifestV1`, (b) be ignored at runtime even if `as any`-cast, and (c) not cause `issue.reader` to refetch. This plan delivers the same OUTCOME the operator asked for ('issue.reader refetches after a successful manual checkbox toggle') via the UI-side refresh primitive the SDK actually exposes."
  - "No worker-handler changes. `src/worker/handlers/ac-checklist.ts` already returns the discriminated `{ok:true}` / `{ok:false, error}` shape the new UI wiring keys on — no shape change needed."
  - "No schema, no migration, no capability list change, no Editor-Agent change."
---

<objective>
Fix the manual-AC-toggle-does-not-refresh-Reader-data UX gap by wiring `usePluginData('issue.reader').refresh` + `usePluginData('reader.ac.autostatus').refresh` from `ReaderViewReady` (index.tsx) down through `AcChecklist` (ac-checklist.tsx) as an `onMutated` callback, called ONLY when `usePluginAction('ac-toggle')` resolves to `{ok:true}`. Pin the behavior with a new node:test source-grep + a small DOM-free behavioral test. Bump rc.5 → rc.6 in BOTH `package.json` AND `src/manifest.ts`. Build + suite GREEN. Pack `clarity-pack-1.0.0-rc.6.tgz`.

Purpose: the operator clicks a manual AC checkbox, the row toggles locally (React state via `checked={it.checked}` + onChange), the worker handler persists the row, but the cached `issue.reader` payload (carrying `data.acItems`) is never re-fetched, so any cross-effect (auto-status indicator, downstream derived state, future re-render of `acItems`) stays stale until the surface unmounts. The desired outcome — `issue.reader` (and `reader.ac.autostatus`) refetch after a successful manual toggle — is achieved via the SDK's `PluginDataResult.refresh` returned by `usePluginData`, NOT via a non-existent manifest-side `invalidates` declaration.

Output: 5 files modified, 1 file added (the new test), 1 tarball produced.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@./CLAUDE.md
@src/ui/surfaces/reader/index.tsx
@src/ui/surfaces/reader/ac-checklist.tsx
@src/worker/handlers/ac-checklist.ts
@test/ui/ac-checklist-autostatus.test.mjs
@test/ui/reader-view.test.mjs
@package.json
@src/manifest.ts

<sdk_gap_evidence>
Verified by reading the installed SDK and grepping the type tree:

- `node_modules/.pnpm/@paperclipai+plugin-sdk@2026.512.0_react@19.2.6/node_modules/@paperclipai/plugin-sdk/dist/types.d.ts` — `PaperclipPluginManifestV1` has fields: `id`, `apiVersion`, `version`, `displayName`, `description`, `author`, `categories`, `capabilities`, `entrypoints`, `database`, `ui`, `instanceConfigSchema`, `jobs`, `agents`, `tools`. There is NO `actions:` field.
- Same file lines ~685-700 — `PluginActionsClient` exposes `register(key, handler)` (RUNTIME registration via the worker `ctx.actions.register`). No declarative manifest counterpart, no invalidation hint surface.
- `node_modules/.pnpm/@paperclipai+plugin-sdk@2026.512.0_react@19.2.6/node_modules/@paperclipai/plugin-sdk/dist/ui/types.d.ts` lines ~308-329 — `PluginDataResult<T>` exposes `data`, `loading`, `error`, AND `refresh: () => Promise<void>`. THIS is the UI-side primitive that delivers the desired outcome.
- A grep across the SDK directory for the substring `invalidat` returns ZERO matches. There is no invalidation concept anywhere in the SDK type tree.

Conclusion: the operator's literal request cannot be implemented as written. This plan delivers the SAME OUTCOME via the UI primitive the SDK actually exposes.
</sdk_gap_evidence>

<interfaces>
From `@paperclipai/plugin-sdk/dist/ui/types.d.ts` (the contract the UI tier consumes):

```typescript
// usePluginData returns this on every call
export interface PluginDataResult<T> {
  data: T | null;
  loading: boolean;
  error: string | null;
  refresh: () => Promise<void>; // <-- this is what we will destructure + thread through
}
```

From `src/worker/handlers/ac-checklist.ts` (the contract the action call resolves to):

```typescript
// Discriminated return shape — already in production, no change needed:
//   success path: { ok: true }
//   failure path: { ok: false, error: string }
```

From the existing `usePluginAction` typing in the SDK: the returned function resolves to `unknown` (the SDK does not statically narrow action return shapes). The new UI wiring must runtime-check `(res as {ok?: boolean})?.ok === true` before calling `onMutated`.

From `src/ui/surfaces/reader/ac-checklist.tsx` (current call site, line 125-127):

```typescript
onChange={(e) => {
  void toggleAc({ id: it.id, checked: e.target.checked, userId: userId ?? null });
}}
```

From `src/ui/surfaces/reader/index.tsx` (current usePluginData calls, lines 226-242):

```typescript
const { data, loading } = usePluginData<ReaderViewData | { error: string }>('issue.reader', {...});
const { data: acAutoData } = usePluginData<...>('reader.ac.autostatus', {...});
```

The fix replaces those two destructures with ones that also extract `refresh`, then passes a combined `onMutated={() => { void refresh(); void refreshAcAuto(); }}` to `<AcChecklist .../>` (line 299).
</interfaces>

<version_bump_rule>
Per project memory `plugin-version-bump-two-sources`: version lives in BOTH `package.json` AND `src/manifest.ts` (hardcoded string literal at the `version:` key). The host reads `dist/manifest.js` (built from `src/manifest.ts`), not `package.json`. Bumping only one ships v_new code with a v_old label. Both files MUST be updated in the same commit-set.

Current: `1.0.0-rc.5` in both. Target: `1.0.0-rc.6` in both.
</version_bump_rule>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire usePluginData.refresh from ReaderViewReady to AcChecklist via onMutated</name>
  <files>src/ui/surfaces/reader/index.tsx, src/ui/surfaces/reader/ac-checklist.tsx</files>
  <behavior>
    - Behavior 1 (HAPPY PATH): when a manual AC checkbox toggle resolves with `{ok: true}`, the AcChecklist calls its `onMutated` prop exactly once.
    - Behavior 2 (FAILURE PATH): when the toggle resolves with `{ok: false, error: 'invalid_id'}`, `onMutated` is NOT called.
    - Behavior 3 (WIRING): ReaderViewReady destructures `refresh` from BOTH `usePluginData('issue.reader', ...)` AND `usePluginData('reader.ac.autostatus', ...)`, and passes an `onMutated` callback that calls BOTH refreshes (the auto-status indicator depends on the same toggle state — refreshing only `issue.reader` would leave the auto-status caption stale until a manual nav-away).
    - Behavior 4 (BACKWARD COMPAT): the `onMutated` prop is optional on `AcChecklist`. Existing call sites + tests that do not pass it must keep compiling and rendering with no functional change (toggle still calls the worker; just no refresh).
    - Behavior 5 (NO_UUID_LEAK preservation): no change to the existing auto-status indicator JSX; the NO_UUID_LEAK regression-pin from Plan 05-03 must still hold (no `sourceAuthorAgentId` references in rendered JSX).
  </behavior>
  <action>
    Modify `src/ui/surfaces/reader/ac-checklist.tsx`:
      1. Extend the `AcChecklist` props type to accept `onMutated?: () => void`.
      2. In the `<input type="checkbox"> onChange` handler (currently line 125-127), change `void toggleAc({...});` to `void toggleAc({...}).then((res) => { if ((res as { ok?: boolean } | null | undefined)?.ok === true) { onMutated?.(); } });`. Use the runtime cast pattern shown — `usePluginAction` returns `Promise<unknown>` and the worker handler's discriminated `{ok:true} | {ok:false, error}` shape is the runtime contract.
      3. Add a brief header comment block (paragraph form, no fenced code) explaining: the `onMutated` callback exists because @paperclipai/plugin-sdk@2026.512.0 has NO manifest-side `actions[].invalidates` declaration (verified — `PaperclipPluginManifestV1` has no `actions:` field; SDK type tree contains zero `invalidat*` occurrences). Data invalidation is a UI-side concern via `PluginDataResult.refresh()`. The callback fires ONLY on `{ok:true}` so a worker-side validation failure (`invalid_id`) does not trigger an unnecessary refetch.

    Modify `src/ui/surfaces/reader/index.tsx`:
      1. In `ReaderViewReady` (function body starting line 210), change the existing two `usePluginData` destructures to also pull `refresh`. The `issue.reader` destructure becomes `const { data, loading, refresh } = usePluginData<...>('issue.reader', {...});`. The `reader.ac.autostatus` destructure becomes `const { data: acAutoData, refresh: refreshAcAuto } = usePluginData<...>('reader.ac.autostatus', {...});`.
      2. At the `<AcChecklist issueId={entityId} items={data.acItems} userId={userId} autoStatus={acAutoStatus} />` call (line 299), add a new prop: `onMutated={() => { void refresh(); void refreshAcAuto(); }}`. Keep the existing props in the same order; insert `onMutated` last for diff clarity.
      3. Add a 4-6-line header comment ABOVE the modified usePluginData call set explaining the SDK gap rationale (mirrors the ac-checklist.tsx comment): "SDK 2026.512.0 has no manifest-side `actions[].invalidates` field — `PluginDataResult.refresh` (returned by usePluginData) is the UI-side primitive that delivers the equivalent post-mutation refetch. Both `issue.reader` AND `reader.ac.autostatus` are refreshed on a successful manual AC toggle because the auto-status caption derives from the same row state."

    Hard rules for this task:
      - NO change to `src/manifest.ts` capabilities / actions / agents / any other field (manifest version bump is Task 3).
      - NO change to `src/worker/handlers/ac-checklist.ts` — its `{ok:true}/{ok:false}` shape is the contract we key on.
      - NO change to existing test files (other than Task 2 below adding a NEW test file).
      - Preserve the NO_UUID_LEAK regression pin: do NOT touch the auto-status caption JSX block.
      - The `?.ok === true` runtime check is the single-point-of-truth gate — refusing to refresh on `{ok:false}` is part of the contract Task 2 pins.
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>pnpm build</automated>
    <automated>node --test test/ui/ac-checklist-autostatus.test.mjs test/ui/reader-view.test.mjs</automated>
  </verify>
  <done>
    - `tsc --noEmit` clean.
    - `pnpm build` produces dist/ui/index.js + dist/manifest.js + dist/worker.js with no errors.
    - The existing AC checklist + Reader view source-grep test suites still pass (the NO_UUID_LEAK pin in ac-checklist-autostatus.test.mjs and the structural pins in reader-view.test.mjs must remain GREEN — Task 1 is purely additive at the JSX level).
    - `grep -n "onMutated" src/ui/surfaces/reader/ac-checklist.tsx` shows the prop declaration + the conditional invocation in the onChange handler.
    - `grep -n "refresh" src/ui/surfaces/reader/index.tsx` shows BOTH new destructures.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test/ui/ac-checklist-refresh-on-toggle.test.mjs pinning the contract</name>
  <files>test/ui/ac-checklist-refresh-on-toggle.test.mjs</files>
  <behavior>
    - The new test file follows the existing source-grep idiom from `test/ui/ac-checklist-autostatus.test.mjs` (Node's `node:test` runner does not load `.tsx`, so behavioral tests on tsx files are source-grep contracts, not runtime DOM tests).
    - Test A (PROP DECLARATION): `ac-checklist.tsx` declares an optional `onMutated?: () => void` prop on AcChecklist.
    - Test B (CONDITIONAL INVOCATION): the source contains a `.then(` continuation on the toggleAc promise that runtime-checks the `ok` field and calls `onMutated?.()` only when `ok === true`.
    - Test C (NO UNCONDITIONAL CALL): the source does NOT contain a bare `onMutated()` or `onMutated?.()` call at the top level of the onChange handler — the call MUST be gated by the ok-check. (Mechanically: assert there's no `onMutated\?\.\(\)` outside of a `.then(` block. Use a simple before/after split on `.then(`.)
    - Test D (READER WIRING — index.tsx): `src/ui/surfaces/reader/index.tsx` destructures `refresh` from BOTH `usePluginData('issue.reader', ...)` AND `usePluginData('reader.ac.autostatus', ...)`. Assert the literals `{ data, loading, refresh }` and `refresh: refreshAcAuto` both appear after stripping comments.
    - Test E (READER WIRING — onMutated passthrough): `index.tsx` passes an `onMutated=` prop to `<AcChecklist` whose body calls both `refresh()` and `refreshAcAuto()`. Assert both call sites appear within an `onMutated={` JSX prop region.
    - Test F (SDK GAP RATIONALE COMMENT): both `index.tsx` AND `ac-checklist.tsx` contain a comment that names the SDK gap — assert each file's RAW (un-stripped) source matches a regex like `/no manifest-?side[\s\S]*invalidat/i` so the rationale survives future edits and a reader of the diff understands why the manifest was NOT touched.
  </behavior>
  <action>
    Create `test/ui/ac-checklist-refresh-on-toggle.test.mjs` modeled on `test/ui/ac-checklist-autostatus.test.mjs`. Use the same comment-stripping helper pattern (the autostatus test uses `replace(/\/\*[\s\S]*?\*\//g, '').replace(/^\s*\/\/.*$/gm, '')` to strip comments before regex matching JSX). For Test F (rationale comment), read the raw source WITHOUT stripping comments so the regex hits the comment block.

    Use `import test from 'node:test'`, `import { strict as assert } from 'node:assert'`, `import { readFileSync } from 'node:fs'`, `import path from 'node:path'`, `import { fileURLToPath } from 'node:url'` — exactly the import set used by the existing autostatus test.

    Resolve both source files via `path.resolve(HERE, '..', '..', 'src', 'ui', 'surfaces', 'reader', '<filename>.tsx')`.

    All 6 assertions (A-F) MUST be expressible as source-grep regexes against the post-comment-strip source (or the raw source, for Test F only). Do not introduce any DOM mocking, React rendering, or `jsdom` — the project's test idiom is source-grep at this tier.

    Test count: exactly 6 `test(...)` calls. Each `test()` body contains 1-3 `assert.match` / `assert.doesNotMatch` calls.
  </action>
  <verify>
    <automated>node --test test/ui/ac-checklist-refresh-on-toggle.test.mjs</automated>
    <automated>node --test "test/**/*.test.mjs"</automated>
  </verify>
  <done>
    - New test file exists and runs.
    - All 6 tests in the new file PASS against the Task-1 source state.
    - Full suite delta: previous 1380 tests → expected ~1386 (+6 net, 0 fail, 2 pre-existing skip). Acceptable range: 1383-1390 pass, 0 fail.
    - `node --test "test/**/*.test.mjs"` exits 0 (or with only the 2 known pre-existing skips).
  </done>
</task>

<task type="auto">
  <name>Task 3: Version bump rc.5 → rc.6 in both package.json and src/manifest.ts</name>
  <files>package.json, src/manifest.ts</files>
  <action>
    Per project memory `plugin-version-bump-two-sources` (the host reads `dist/manifest.js` built from `src/manifest.ts`, NOT `package.json`):

    1. `package.json` line 3: change `"version": "1.0.0-rc.5"` → `"version": "1.0.0-rc.6"`.
    2. `src/manifest.ts`: change the `version: '1.0.0-rc.5',` line (currently line 322) to `version: '1.0.0-rc.6',`.
    3. Prepend a short version-header comment block immediately ABOVE the existing 0.8.3 header block (line 21) — same format / voice as the existing 0.8.3/0.8.2/0.7.8 etc. entries. Suggested content (paragraph form, your wording, the substance below is required):
       - "1.0.0-rc.6 (Quick fix 260524-s2y — AC manual toggle → Reader refetch)."
       - "Wires `PluginDataResult.refresh` from `usePluginData('issue.reader', ...)` AND `usePluginData('reader.ac.autostatus', ...)` down through `AcChecklist` as an `onMutated` callback, called ONLY when `usePluginAction('ac-toggle')` resolves to `{ok:true}`."
       - "RATIONALE FOR NO MANIFEST CHANGE: @paperclipai/plugin-sdk@2026.512.0 exposes no `actions[]` field on `PaperclipPluginManifestV1` and no `invalidat*` concept anywhere in the SDK type tree (verified by reading dist/types.d.ts + grep). The operator's literal framing — 'add an invalidates declaration in src/manifest.ts' — was based on a misread of the SDK shape. Data invalidation in this SDK is a UI-side concern; this rc.6 ships the equivalent fix at the UI tier."
       - "UI + 1 test only — no manifest shape, capability, schema, agent, job, or worker-contract change."

    DO NOT change any other field in `src/manifest.ts`. DO NOT change `capabilities[]`, `agents[]`, `jobs[]`, `entrypoints`, `database`, `instanceConfigSchema`, `ui.slots[]`, `apiVersion`, `id`. The ONLY changes to manifest.ts are: (a) the `version:` literal, (b) the new header comment block.

    DO NOT bump any other version anywhere else (no `tarball.test.mjs` style pins for rc.6 unless one already explicitly pins rc.5 — if so, update it; check `test/manifest/` for any version assertion).
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
    <automated>node -e "import('./dist/manifest.js').then(m => { if (m.default.version !== '1.0.0-rc.6') { console.error('FAIL: dist/manifest.js version is', m.default.version); process.exit(1); } else { console.log('OK: dist/manifest.js version is 1.0.0-rc.6'); } })"</automated>
    <automated>node -e "const p = require('./package.json'); if (p.version !== '1.0.0-rc.6') { console.error('FAIL: package.json version is', p.version); process.exit(1); } else { console.log('OK: package.json version is 1.0.0-rc.6'); }"</automated>
    <automated>node --test "test/**/*.test.mjs"</automated>
  </verify>
  <done>
    - `package.json` line 3 reads `"version": "1.0.0-rc.6"`.
    - `src/manifest.ts` version line reads `version: '1.0.0-rc.6',`.
    - After `pnpm build`, `dist/manifest.js` reports `version === '1.0.0-rc.6'` (verified by the inline node script above).
    - `pnpm typecheck` clean.
    - Full suite still passes (no version-string pin regression; any test that pinned literal `1.0.0-rc.5` is updated).
    - Header comment block names the SDK gap so a future reader of the diff understands why the manifest's `actions[]` / `invalidates` was NOT added.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full build + suite + npm pack — produce clarity-pack-1.0.0-rc.6.tgz</name>
  <files>(no source changes — verification + artifact production only)</files>
  <action>
    Run the project's standard pre-ship sequence in order:

    1. `pnpm build` — builds worker + UI + manifest. Confirm zero errors. Inspect that `dist/manifest.js` was regenerated (timestamp newer than the edit to `src/manifest.ts`).
    2. `pnpm typecheck` — confirm tsc --noEmit clean.
    3. `node --test "test/**/*.test.mjs"` — confirm full suite passes. Target: ~1386 pass, 0 fail, 2 pre-existing skip (acceptable range 1383-1390 pass).
    4. `node scripts/check-css-scope.mjs` — confirm exit 0 (no CSS scope regression from the UI edit — Task 1 added no CSS at all but this is a standing gate).
    5. `node scripts/check-a11y.mjs` — confirm exit 0 (no a11y regression — Task 1 added no new interactive control, just wired an existing onChange).
    6. `npm pack` — produces `clarity-pack-1.0.0-rc.6.tgz` at the repo root. Capture the tarball's byte size + sha256 for the SUMMARY.

    If `npm pack` produces a different filename (e.g. `clarity-pack-1.0.0-rc.6.tgz` is the expected pattern based on the rc.5 precedent — `clarity-pack-1.0.0-rc.5.tgz`), confirm the literal filename matches `clarity-pack-1.0.0-rc.6.tgz`. If not, stop and surface the discrepancy in the SUMMARY rather than silently shipping a differently-named tarball.

    Capture for the SUMMARY:
      - tarball filename (must be `clarity-pack-1.0.0-rc.6.tgz`)
      - byte size (the rc.5 precedent was 145,380 bytes; rc.6 will be within ~1KB of that since the diff is tiny)
      - sha256 (compute via `node -e "const crypto=require('crypto'); const fs=require('fs'); const h=crypto.createHash('sha256'); h.update(fs.readFileSync('clarity-pack-1.0.0-rc.6.tgz')); console.log(h.digest('hex'));"` on Windows-friendly PowerShell-or-bash path)
      - suite count delta (1380 → expected ~1386)
      - confirmation that all 5 quality gates (build / typecheck / suite / check-css-scope / check-a11y) returned exit 0

    DO NOT `npm publish`. DO NOT push to a registry. DO NOT install onto the live Countermoves VPS. This plan ships the local artifact only; the operator drill (snapshot-bookended install) is deferred to Eric's post-quick-fix sweep, exactly like Plan 05-03's rc.5 drill was deferred.
  </action>
  <verify>
    <automated>pnpm build</automated>
    <automated>pnpm typecheck</automated>
    <automated>node --test "test/**/*.test.mjs"</automated>
    <automated>node scripts/check-css-scope.mjs</automated>
    <automated>node scripts/check-a11y.mjs</automated>
    <automated>npm pack</automated>
    <automated>node -e "const fs=require('fs'); if (!fs.existsSync('clarity-pack-1.0.0-rc.6.tgz')) { console.error('FAIL: tarball not found'); process.exit(1); } else { console.log('OK: tarball produced, size=', fs.statSync('clarity-pack-1.0.0-rc.6.tgz').size); }"</automated>
  </verify>
  <done>
    - `dist/manifest.js` reports `version: '1.0.0-rc.6'`.
    - All 5 quality gates exit 0 (build, typecheck, suite, check-css-scope, check-a11y).
    - `clarity-pack-1.0.0-rc.6.tgz` exists at the repo root.
    - sha256 + byte size captured (will land in the SUMMARY).
    - No `npm publish`, no VPS install — local artifact only.
  </done>
</task>

</tasks>

<verification>
- The rc.6 tarball ships the UI-tier refresh wiring, NOT a manifest-tier actions[] entry.
- A clean run of `pnpm typecheck && pnpm build && node --test "test/**/*.test.mjs"` returns exit 0 across the board.
- The new test `test/ui/ac-checklist-refresh-on-toggle.test.mjs` passes all 6 assertions.
- The NO_UUID_LEAK regression-pin in `test/ui/ac-checklist-autostatus.test.mjs` still passes.
- `grep -c "onMutated" src/ui/surfaces/reader/ac-checklist.tsx` returns ≥ 2 (prop declaration + conditional invocation).
- `grep -c "refresh" src/ui/surfaces/reader/index.tsx` returns ≥ 3 (two destructures + at least one onMutated call site).
- `grep -nE "actions\s*:" src/manifest.ts` returns 0 NEW matches (the only pre-existing `actions:`-substring matches, if any, are inside comment prose discussing the OLD plan-04.2 chat handlers — verify no NEW manifest-field `actions:` was added).
- BOTH `package.json` AND `dist/manifest.js` (post-build) report `version: '1.0.0-rc.6'`.
</verification>

<success_criteria>
1. Manual AC checkbox toggle on the live Reader view fires `usePluginData('issue.reader').refresh()` AND `usePluginData('reader.ac.autostatus').refresh()` after a `{ok:true}` action result. (Code path proven by Task 2's source-grep contract + Task 1's onChange wiring; live operator verification deferred to Eric's post-quick-fix sweep.)
2. A worker-side `{ok:false, error:'invalid_id'}` does NOT trigger a refetch. (Pinned by Task 2 Test C.)
3. `src/manifest.ts` carries NO `actions:` field — the SDK gap is documented in the new header comment for future-reader audit.
4. Version is `1.0.0-rc.6` in both package.json AND dist/manifest.js.
5. `clarity-pack-1.0.0-rc.6.tgz` exists at the repo root with sha256 + byte size captured.
6. All 5 standing quality gates exit 0 (build, typecheck, full suite, check-css-scope, check-a11y).
7. Test suite delta: 1380 → ~1386 (+6 net from the new file, 0 fail, 2 pre-existing skip).
</success_criteria>

<output>
On completion, create `.planning/quick/260524-s2y-fix-ac-toggle-invalidates-manifest-gap-a/260524-s2y-SUMMARY.md` with:
  - tarball filename + byte size + sha256
  - commit shas for each task's atomic commit(s)
  - suite count before/after
  - explicit "MANIFEST UNCHANGED — SDK GAP DOCUMENTED" call-out so the rationale survives in the canonical project history
  - the operator-facing one-liner: "rc.5 → rc.6 — manual AC toggle now refreshes Reader data (UI-tier fix; SDK has no manifest-side invalidates)"
</output>
