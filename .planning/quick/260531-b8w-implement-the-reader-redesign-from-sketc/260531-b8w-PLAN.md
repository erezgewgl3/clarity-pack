---
phase: quick-260531-b8w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/surfaces/reader/index.tsx
  - src/ui/surfaces/reader/live-blocker-panel.tsx
  - src/ui/surfaces/reader/tldr-strip.tsx
  - src/ui/surfaces/reader/prose-with-ref-chips.tsx
  - src/ui/primitives/ref-chip.tsx
  - src/ui/primitives/safe-markdown.tsx
  - src/ui/primitives/theme.css
  - src/manifest.ts
  - package.json
  - test/ui/reader-view.test.mjs
autonomous: true
requirements: [QUICK-260531-b8w]
must_haves:
  truths:
    - "Reader detail tab renders ONE reading column (~760px max-width); the plugin's own right rail is gone."
    - "Reading order is TL;DR-first: briefing (gold rule, '◆ Your briefing' kicker) → Acceptance → Deliverable → raw body collapsed behind a 'Show full task' disclosure."
    - "LiveBlockerPanel renders inline (full-width, in-column) only when it has a live blocker; healthy/empty state stays quiet."
    - "Reader body type is host-native: body = system-ui sans, TL;DR/BLUF editorial voice = Newsreader (real 400/600 weights), IDs = JetBrains Mono — scoped to [data-clarity-surface='reader'] only."
    - "Mid-sentence body refs render the light inline form (no border, status dot, underline-on-hover); standalone anchored-to refs keep the full bordered chip. Hover-peek stays on BOTH."
    - "Version reads 1.2.2 in BOTH package.json AND src/manifest.ts; the UI + worker + manifest build compiles."
  artifacts:
    - path: "src/ui/surfaces/reader/index.tsx"
      provides: "No-rail single-column layout, TL;DR-first order, <details> disclosure, relocated LiveBlockerPanel banner"
      contains: "Show full task"
    - path: "src/ui/primitives/theme.css"
      provides: "Reader-scoped host-native font override + light inline ref-chip (.clarity-ref-chip--inline) rules"
      contains: "Newsreader"
    - path: "src/manifest.ts"
      provides: "version literal bumped to 1.2.2"
      contains: "version: '1.2.2'"
  key_links:
    - from: "src/ui/primitives/safe-markdown.tsx"
      to: "src/ui/primitives/ref-chip.tsx"
      via: "refVariant prop threaded SafeMarkdown → renderInline → <RefChip variant=...>"
      pattern: "variant=\\{refVariant"
    - from: "src/ui/surfaces/reader/prose-with-ref-chips.tsx"
      to: "src/ui/primitives/safe-markdown.tsx"
      via: "refVariant='inline' passed for body prose"
      pattern: "refVariant=[\"']inline[\"']"
---

<objective>
Translate the LOCKED Reader-redesign sketches (003 variant B + 004 variant B) into the live React/CSS build and ship as v1.2.2.

Two visual changes, one ship:
- **003-B — TL;DR-first, no-rail layout.** Drop the plugin's redundant right rail (host Properties owns the right side); collapse to a single ~760px reading column; lead with the TL;DR briefing under a gold rule; collapse the raw task body behind a "Show full task" disclosure; relocate the LiveBlockerPanel from the rail into the column as a conditional inline banner.
- **004-B — host-native editorial type + two-weight chips.** Scope the Reader surface to host fonts (system-ui body, Newsreader BLUF, JetBrains Mono IDs); give ref-chips a second, lighter inline form for mid-sentence refs so dense ref-runs read as prose.

Purpose: the live BEAAA Reader reads "fonts all over the place" with a cramped ~424px column and two competing right panels. This is the fix the sketches locked.
Output: rebuilt `dist/` artifacts + committed source at version 1.2.2. This is an INTERNAL plugin — no npm publish (memory `clarity-pack-internal-only-no-npm`); building + committing source IS the ship.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<!-- PIXEL GROUND TRUTH — these are the contract. Read the winning variant's CSS + markup. -->
@.planning/sketches/003-reader-layout-hierarchy/index.html
@.planning/sketches/004-tldr-type-and-chips/index.html
@.planning/sketches/MANIFEST.md

<!-- Files being restructured -->
@src/ui/surfaces/reader/index.tsx
@src/ui/surfaces/reader/live-blocker-panel.tsx
@src/ui/surfaces/reader/tldr-strip.tsx
@src/ui/surfaces/reader/prose-with-ref-chips.tsx
@src/ui/primitives/ref-chip.tsx
@src/ui/primitives/safe-markdown.tsx
@src/ui/primitives/theme.css

Load the project skill `sketch-findings-clarity-pack` for the design tokens + the zero-rabbit-holes inline-peek mechanic.

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase, no exploration required. -->

RefChip (src/ui/primitives/ref-chip.tsx) — current signature:
  export function RefChip({ refId }: { refId: string }): React.ReactElement
  Renders a `.clarity-ref-chip-wrap` span containing a `.clarity-ref-chip` anchor/span
  + the hover-peek popover (`.clarity-ref-chip-peek`). The peek + wrap (onMouseEnter/
  Leave, touch long-press) is shared by EVERY render path. The label is
  `<span class="clarity-ref-chip-label"><span class="clarity-ref-chip-id">{id}</span>
   — {title}<span class="clarity-ref-chip-status" data-status={status}>{status}</span></span>`.

SafeMarkdown (src/ui/primitives/safe-markdown.tsx):
  export function SafeMarkdown({ text, linkRefs, companyPrefix }: {
    text: string | null | undefined; linkRefs?: boolean; companyPrefix?: string | null;
  }): React.ReactElement | null
  Internally renderInline() maps a `ref` InlineSpan to `<RefChip key={key} refId={span.refId} />`.
  parseMarkdownBlocks() in safe-markdown.ts produces the token tree — NO parser change is needed for this plan.

ProseWithRefChips (src/ui/surfaces/reader/prose-with-ref-chips.tsx):
  export function ProseWithRefChips({ body }: { body: string | null | undefined })
  Delegates entirely to <SafeMarkdown text={body} linkRefs companyPrefix={...} />.
  Prop shape { body } is pinned by prose-with-ref-chips.test.mjs — do NOT change it.

LiveBlockerPanel (src/ui/surfaces/reader/live-blocker-panel.tsx):
  export function LiveBlockerPanel({ issueId }: { issueId: string }): React.ReactElement | null
  Returns null while resolvers load AND when data is absent. The inner component renders
  `.clarity-blocker-panel` with data-terminal-kind. KEEP all data hooks + the null returns —
  this is a layout/CSS move, not a logic rewrite.

TldrStrip (src/ui/surfaces/reader/tldr-strip.tsx):
  export function TldrStrip({ tldr, status, truncated }): React.ReactElement
  Populated path renders `.clarity-tldr-strip` > `.clarity-tldr-body` (SafeMarkdown linkRefs)
  + `.clarity-tldr-stamp`. The empty/compiling/paused branches are byte-locked by tests — do
  not touch their literals.

theme.css scoping (enforced by scripts/check-css-scope.mjs):
  EVERY top-level selector MUST start with `[data-clarity-surface]` (bare) or
  `[data-clarity-surface='<name>']`. The global font lives on the base block:
    [data-clarity-surface] { font-family: 'Geist', system-ui, ...; }
  DO NOT edit that line. Add a reader-scoped override block instead.
</interfaces>

<sketch-line-map>
<!-- The exact sketch lines that are the contract for each change. -->
003-B markup .................. 003/index.html lines 224-288 (variant-b)
  - single column, no rail, max-width 760px ... line 237
  - TL;DR-first w/ gold rule + kicker ......... lines 242-255 (.tldr.brief / .kicker "◆ Your briefing")
  - Acceptance card then Deliverable card ..... lines 257-262
  - raw body in <details>"Show full task" ..... lines 264-274 (.disc summary)
003-B CSS ..................... 003/index.html
  - .tldr.brief gold left rule ................ line 72 (border-left: 3px solid var(--you))
  - .kicker ................................... lines 73-74
  - .disc / summary / .chev rotate ........... lines 97-103
  - .card / .card h5 ......................... lines 106-107
003-C on-you banner (the "+C" half) .......... 003/index.html lines 81-88 (.onyou) + 308 (usage)
  - relocate LiveBlockerPanel as this banner; render only when a live blocker exists.
004-B type ................... 004/index.html
  - .vb .bluf Newsreader 600 ................. line 46 (font-family var(--font-serif-text); font-weight 600)
  - .vb .body Newsreader 400 ................. line 47
  - host font names + fallbacks ............... see <font-stack> below
004 two-weight chips ......... 004/index.html
  - full .chip (border + badge) .............. lines 56-68
  - light .iref (no border, dot, underline) ... lines 70-78
  - the shared demo (full vs inline) ......... lines 156-180
</sketch-line-map>

<font-stack>
<!-- CLAUDE.md trust model: host fonts are available same-origin. Reference by name + fallback. -->
Reader body sans : system-ui, -apple-system, sans-serif
Editorial serif  : Newsreader, Georgia, serif        (real 400/600 weights — true bold, NOT faked)
ID mono          : 'JetBrains Mono', ui-monospace, monospace
SCOPE: every one of these is applied ONLY under [data-clarity-surface='reader'].
Chat / bulletin / situation-room KEEP Geist + Geist Mono + Instrument Serif (MANIFEST note:
"the standalone chat mockup keeps its lock"). Do NOT touch the base [data-clarity-surface]
font-family declaration — add reader-scoped overrides on top of it.
</font-stack>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 003-B layout — drop the rail, TL;DR-first order, "Show full task" disclosure, relocate LiveBlockerPanel as a conditional inline banner</name>
  <files>src/ui/surfaces/reader/index.tsx, src/ui/surfaces/reader/live-blocker-panel.tsx, src/ui/primitives/theme.css</files>
  <action>
Restructure the POPULATED render of ReaderViewReady (index.tsx, the `return (<ClaritySurfaceRoot name="reader">...` block starting ~line 361) to the 003-B single-column structure. Contract is 003/index.html variant-b, lines 237-274.

HARD INVARIANTS — DO NOT regress (these are scarred-in fixes; see the comment block atop index.tsx):
  - Keep the resolver chain intact: ReaderView → ReaderViewOptedIn → ReaderViewWithCompany → ReaderViewReady, and ALL loading/error placeholders (opt-in, "Resolving company context…", "Resolving viewer identity…", "Loading Reader view…"). Touch ONLY the final populated JSX in ReaderViewReady.
  - EVERY section stays wrapped in its own `<SectionErrorBoundary name="..." resetKey={entityId}>`. Moved/new sections KEEP a boundary. The wrap-tag format `<SectionErrorBoundary name="<kebab>" resetKey={entityId}>` is pinned by reader-yaml-body-render.test.mjs line 192 regex — preserve it exactly (name attribute, then resetKey={entityId}).
  - The required section names + child components stay present (reader-yaml-body-render.test.mjs WRAPPED_SECTIONS lines 144-169): surface-header/ClaritySurfaceHeader, agent-pause-banner/AgentPauseBanner, continue-in-chat/ContinueInChatButton, reverse-topics/ReverseTopicsLink, breadcrumb/Breadcrumb, tldr/TldrStrip, prose/ProseWithRefChips, anchored-to/AnchoredToCards, deliverable/DeliverablePreview, ac-checklist/AcChecklist, activity/ActivityTimeline, live-blocker/LiveBlockerPanel, pause-banner/PauseBanner. All 13 must still be rendered and wrapped.
  - <PauseBanner /> stays mounted LAST (footer), wrapped in name="pause-banner". Its literal is pinned by reader-view.test.mjs.
  - Leave the scroll-stability resolveReaderData / lastGoodRef pattern + the TL;DR compile-poll useEffect untouched.

New populated layout (replace the `clarity-reader-body` grid + `<aside class="clarity-reader-rail">` subtree). The header chrome (surface-header, agent-pause-banner, the header-actions row with continue-in-chat + reverse-topics, breadcrumb) is UNCHANGED — only what comes after breadcrumb changes:
  1. tldr/TldrStrip boundary — FIRST after breadcrumb (already there; keep position).
  2. live-blocker/LiveBlockerPanel boundary — render DIRECTLY after the TL;DR as a full-width inline banner (the 003-C "+C" on-you line). LiveBlockerPanel already returns null when there is no live blocker, so its quiet/healthy state needs no extra guard — keep that null behavior; this is the "only when it has a live blocker" requirement satisfied by construction. Add a wrapper div className="clarity-reader-onyou" around the boundary so theme.css can style the relocated banner full-width (Task adds the rule).
  3. Wrap the column in a single `<div className="clarity-reader-column">` (replaces clarity-reader-body grid + clarity-reader-main + clarity-reader-rail). Inside, in order:
     a. anchored-to/AnchoredToCards (the standalone "anchored-to" refs — these KEEP the full chip form; the ref-card component renders the full bordered card and is unchanged).
     b. ac-checklist/AcChecklist (Acceptance card — keep existing props incl. onMutated/autoStatus).
     c. deliverable/DeliverablePreview (keep existing props).
     d. A `<details className="clarity-reader-disclosure">` element with `<summary>Show full task</summary>` (literal "Show full task" required — must_haves.contains). INSIDE the <details>, in their own SectionErrorBoundary wraps: prose/ProseWithRefChips (the raw body — moved here, no longer always-visible), then activity/ActivityTimeline. Keep both boundaries.
  4. pause-banner/PauseBanner boundary stays last (footer), OUTSIDE the column div.

NOTE on AC/Deliverable order vs sketch: 003-B shows Acceptance then Deliverable as inline cards above the disclosure (lines 257-262); the current code order is anchored-to → deliverable → ac-checklist → activity. Reorder the populated render to: anchored-to, ac-checklist (Acceptance), deliverable, then the disclosure containing prose + activity. Per the design contract reading order: TL;DR → (on-you banner) → Acceptance → Deliverable → collapsed body. anchored-to cards stay above the cards block (they are resolved "anchored-to" refs, the substantive context for the task).

live-blocker-panel.tsx: NO logic change. The relocation is purely the index.tsx move + the CSS. Leave its data hooks, null returns, and the `.clarity-blocker-panel` markup intact. (If a className tweak is needed for full-width inline rendering, do it via the new `.clarity-reader-onyou` wrapper in index.tsx + theme.css — do NOT alter the panel's internal markup.)

theme.css (003-B layout rules) — add under [data-clarity-surface='reader'] (every selector top-level-scoped per check-css-scope.mjs):
  - REPLACE the `.clarity-reader-body` grid (lines 228-233: `display:grid; grid-template-columns: minmax(0,1fr) 320px`) — either delete it or repurpose. Add `.clarity-reader-column { display:flex; flex-direction:column; gap:1.25rem; }` and constrain the whole reader surface to a single column: add `max-width: 760px;` to the reader container. The reader content is rendered inside ClaritySurfaceRoot name="reader"; apply the max-width via a scoped rule on the surface root (e.g. `[data-clarity-surface='reader'] { max-width: 760px; }` if the root carries the data attribute) OR on `.clarity-reader-column` and the header region. Match 003-B line 237's `max-width: 760px`. Verify against ClaritySurfaceRoot's rendered element — read clarity-surface-root.tsx to confirm where data-clarity-surface lands before choosing the selector.
  - DELETE or neutralize `.clarity-reader-main` (235-239) and `.clarity-reader-rail` (241-245) — the rail is gone. (Leaving them as dead rules is acceptable if no element uses them, but prefer removing to keep theme.css honest.)
  - `.clarity-reader-onyou` — full-width wrapper for the relocated blocker banner: margin 0 0 1rem; (the panel inside keeps its own border/background). Per 003-C .onyou lines 81-88 the banner is a full-width accented row; the existing `.clarity-blocker-panel` styling already provides the accented box — the wrapper just gives it block-level full-width placement in the column.
  - `.clarity-reader-disclosure` + `summary` + chevron — per 003-B .disc lines 97-103: `summary { cursor:pointer; list-style:none; font-size:0.85rem; color: var(--clarity-muted); padding:0.5rem 0; }`, `summary::-webkit-details-marker { display:none; }`, and a rotating chevron is optional (the sketch uses a ▸ that rotates on [open]); a plain "Show full task" summary is acceptable if a chevron complicates the markup. Keep it minimal and scoped.
  - Add a `.clarity-reader-cards`-style treatment only if you wrap AC/Deliverable in cards; otherwise reuse existing `.clarity-ac-checklist` / `.clarity-deliverable` rules (they already render bordered boxes — matches 003-B .card look closely enough; pixel-perfect parity is not required, "recognizable design parity" is the bar per the situation-room precedent at theme.css line 573).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack"; node scripts/check-css-scope.mjs; node --test test/ui/surfaces/reader/reader-yaml-body-render.test.mjs test/ui/reader-view.test.mjs</automated>
  </verify>
  <done>index.tsx renders a single `.clarity-reader-column` (no `<aside class="clarity-reader-rail">`, no `clarity-reader-body` grid); TL;DR is first after breadcrumb, the LiveBlockerPanel banner follows it, then Acceptance + Deliverable, then the raw body + activity are inside a `<details>` with summary "Show full task". All 13 SectionErrorBoundary name/child wraps still present with resetKey={entityId}. check-css-scope passes; reader-yaml-body-render + reader-view tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: 004-B host-native type system + two-weight ref-chips (light inline variant)</name>
  <files>src/ui/primitives/theme.css, src/ui/surfaces/reader/tldr-strip.tsx, src/ui/primitives/ref-chip.tsx, src/ui/primitives/safe-markdown.tsx, src/ui/surfaces/reader/prose-with-ref-chips.tsx</files>
  <action>
Two coupled changes: (A) reader-scoped host-native fonts, (B) a light inline ref-chip variant for mid-sentence body refs. Contract is 004/index.html variant-b (lines 45-47) + the two-weight chip demo (lines 56-78, 156-180) and the <font-stack> block above.

(A) HOST-NATIVE TYPE — theme.css, all reader-scoped:
  - Add a reader-scoped font override. Do NOT touch the base `[data-clarity-surface] { font-family: 'Geist', ... }` at line 61. Add:
      `[data-clarity-surface='reader'] { font-family: system-ui, -apple-system, sans-serif; --clarity-reader-serif: Newsreader, Georgia, serif; --clarity-reader-mono: 'JetBrains Mono', ui-monospace, monospace; }`
    (Defining the two custom props on the reader block keeps the host-native fonts in one place and scoped — they resolve only on the reader surface.)
  - TL;DR body (line 289-294): change `.clarity-tldr-body` font-family from `'Instrument Serif', Georgia, serif` to `var(--clarity-reader-serif)` and set `font-weight: 400` for the body voice. Per 004-B line 47: Newsreader 400, ~1.12rem, line-height 1.55. The TL;DR's **bold lead** (rendered by SafeMarkdown's `<strong>`) must be TRUE bold — add `[data-clarity-surface='reader'] .clarity-tldr-body strong { font-weight: 600; }` so the BLUF lead uses Newsreader 600 (004-B line 46), real weight not faked.
  - IDs: the ref-chip id already uses `'Geist Mono'` (line 139) and ref-card id uses `'Geist Mono'` (line 344) and activity-kind (line 453). Within the reader surface ONLY, these should read JetBrains Mono. The chip-id rule at line 135 is scoped to bare `[data-clarity-surface]` (applies to all surfaces) — do NOT change that shared rule (chat/situation-room must keep Geist Mono). Instead add reader-scoped OVERRIDES that win by specificity:
      `[data-clarity-surface='reader'] .clarity-ref-chip-id, [data-clarity-surface='reader'] .clarity-ref-card-id, [data-clarity-surface='reader'] .clarity-activity-kind { font-family: var(--clarity-reader-mono); }`
    Same `[data-clarity-surface='reader'] X` specificity as the base `[data-clarity-surface] X` rules but more specific (qualified value) → wins. Confirm with a quick read of the existing selectors that no `!important` is present (none is).
  - The breadcrumb `.clarity-r-crumb` analog: the current breadcrumb (line 253) uses muted sans — leave it; the sketch's crumb is mono (003 line 67) but this is cosmetic and the breadcrumb component renders its own markup. OPTIONAL: if breadcrumb has a mono class, point it at var(--clarity-reader-mono). Skip if it complicates — not load-bearing.

(B) TWO-WEIGHT REF-CHIPS — light inline variant for mid-sentence body refs:
  - ref-chip.tsx: add an optional prop `variant?: 'full' | 'inline'` to RefChip, defaulting to `'full'`. When `variant === 'inline'`, render the LIGHT form per 004 .iref (lines 70-78): the wrap span (`.clarity-ref-chip-wrap`) and the hover-peek MUST stay (the no-navigate core mechanic stays on BOTH forms — design contract). Change ONLY the inner element's className from `clarity-ref-chip` to `clarity-ref-chip clarity-ref-chip--inline` AND the label structure to the light shape: `<span class="clarity-ref-chip-id">{id}</span><span class="clarity-ref-chip-dot" data-status={status} /> {title}` — i.e. id (mono) + a small status-colored dot + plain title, no bordered status badge. Keep the resolved anchor (nav.linkProps to /<prefix>/issues/<id>) for the inline form too — clicking still navigates; hover still peeks. The loading pill, hiddenAsRef inert-text, and no-prefix span paths should also respect variant (loading/inert can stay visually identical — they are transient). Keep all hooks (useResolvedUserId, usePluginData('resolve-refs'), peek state, long-press) unchanged.
  - safe-markdown.tsx: thread a `refVariant?: 'full' | 'inline'` prop through `SafeMarkdown` → `renderInline` so the `case 'ref':` renders `<RefChip key={key} refId={span.refId} variant={refVariant} />`. Default `refVariant = 'full'` so all existing callers (TL;DR strip, chat message-thread) are byte-unchanged. renderInline currently takes (spans, keyPrefix); add refVariant as a closure or extra arg — simplest is to read it from the SafeMarkdown component scope and pass it down through renderBlock → renderInline (thread it, or define renderInline inside SafeMarkdown to close over it). Pick the lowest-churn wiring; the must_haves key_link pins `variant={refVariant`.
  - prose-with-ref-chips.tsx: pass `refVariant="inline"` to its `<SafeMarkdown text={body} linkRefs companyPrefix={companyPrefix} refVariant="inline" />`. Keep the { body } prop shape, the clarity-reader-prose wrapper, the empty-body guard, and the useHostLocation/extractCompanyPrefixFromPathname derivation (all pinned by prose-with-ref-chips.test.mjs). So body refs render light; the standalone anchored-to refs (ref-card.tsx, full card) and the TL;DR follow-on refs (default full chip) keep the full form — matching the design contract.
  - theme.css (light chip rules) — per 004 .iref lines 70-78, reader-scoped:
      `[data-clarity-surface='reader'] .clarity-ref-chip--inline { display: inline; border: none; padding: 0; background: transparent; border-bottom: 1px solid var(--clarity-line-bright); white-space: nowrap; cursor: pointer; }`
      `[data-clarity-surface='reader'] .clarity-ref-chip--inline:hover { border-bottom-color: var(--clarity-ink-2); }`
      `[data-clarity-surface='reader'] .clarity-ref-chip-dot { display:inline-block; width:6px; height:6px; border-radius:50%; margin:0 1px 1px 3px; vertical-align:middle; background: var(--clarity-line-bright); }`
      `[data-clarity-surface='reader'] .clarity-ref-chip-dot[data-status='done'] { background: var(--clarity-state-working-bg); }`
      `[data-clarity-surface='reader'] .clarity-ref-chip-dot[data-status='blocked'] { background: var(--clarity-state-stuck-bg); }`
    The inline form intentionally has NO bordered status badge (the dot replaces it) and NO box border — so a dense ref-run reads as prose (004 demo lines 168-176). The full `.clarity-ref-chip` rules (theme.css 104-181) are untouched, so anchored-to + TL;DR follow-on chips keep the bordered look.
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack"; node scripts/check-css-scope.mjs; node --test test/ui/prose-with-ref-chips.test.mjs test/ui/safe-markdown.test.mjs test/ui/reader-view.test.mjs</automated>
  </verify>
  <done>theme.css has a reader-scoped block setting body=system-ui + the --clarity-reader-serif (Newsreader) / --clarity-reader-mono (JetBrains Mono) props; .clarity-tldr-body uses Newsreader with strong=600; reader ref-chip-id/ref-card-id/activity-kind override to JetBrains Mono (chat/situation-room Geist Mono untouched). RefChip accepts variant='inline' rendering the light .clarity-ref-chip--inline form (dot, no border, peek retained); SafeMarkdown threads refVariant; ProseWithRefChips passes refVariant="inline". check-css-scope + the three tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Version bump to 1.2.2 (both sources), update structure-pinning tests, and run the full build</name>
  <files>package.json, src/manifest.ts, test/ui/reader-view.test.mjs</files>
  <action>
VERSION BUMP — TWO sources (project memory `plugin-version-bump-two-sources`: the host reads dist/manifest.js built from src/manifest.ts, NOT package.json; missing either ships v1.2.2 code with a v1.2.1 label):
  - package.json line 3: `"version": "1.2.1"` → `"version": "1.2.2"`.
  - src/manifest.ts line 602: `version: '1.2.1',` → `version: '1.2.2',`.

TEST UPDATES — the layout reorder (Task 1) + the type/chip changes (Task 2) do NOT break the existing source-grep tests by construction (reader-view.test.mjs lines 62-78 only require each `<Child` to be RENDERED, which they still are; reader-yaml-body-render.test.mjs only requires each section WRAPPED, which they still are; prose/safe-markdown tests pin delegation + prop shape, all preserved). Run the full suite FIRST and only touch tests that actually fail:
  - If reader-view.test.mjs or reader-yaml-body-render.test.mjs fails, the cause is a moved wrap that lost its `name="..."` or `resetKey={entityId}` — FIX index.tsx (Task 1), not the test (the structural contract is correct; preserve it).
  - If a test asserts a literal the redesign legitimately changed (e.g. an Instrument-Serif assertion, a `clarity-reader-rail`/`clarity-reader-body` grid assertion, or a "body always visible" assertion), UPDATE that test to the new contract and add a one-line comment citing this quick plan (260531-b8w) + the sketch (003-B / 004-B). GREP the test tree first: search test/**/*.test.mjs for `reader-rail`, `reader-body`, `Instrument Serif`, `clarity-tldr-body`, and `grid-template-columns` to find any literal/structure assertion that the redesign invalidates, and reconcile each.
  - Do NOT weaken the SectionErrorBoundary coverage tests or the PauseBanner literal test — those invariants survive the redesign.

BUILD — the deliverable is built artifacts (internal plugin, no npm publish). Run the full build per package.json scripts:
  - `pnpm build` (runs build:worker → build:ui → build:manifest). Confirm it exits 0 and emits dist/ui/index.js + dist/worker.js + dist/manifest.js.
  - Then `pnpm typecheck` (tsc --noEmit) — must pass (the new variant prop + refVariant threading must typecheck).
  - Confirm dist/manifest.js carries version 1.2.2 (grep the built file).
  </action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack"; pnpm build; pnpm typecheck; node --test "test/**/*.test.mjs"; node -e "const m=require('node:fs').readFileSync('dist/manifest.js','utf8'); if(!/1\.2\.2/.test(m)) throw new Error('dist/manifest.js missing 1.2.2'); const p=require('./package.json'); if(p.version!=='1.2.2') throw new Error('package.json version != 1.2.2'); console.log('version 1.2.2 verified in both sources + dist');"</automated>
  </verify>
  <done>package.json AND src/manifest.ts both read 1.2.2; dist/manifest.js (built) contains 1.2.2; `pnpm build` + `pnpm typecheck` exit 0; the full `node --test` suite passes (any test touched is updated to the 003-B/004-B contract with a cited comment, and no invariant test was weakened).</done>
</task>

</tasks>

<verification>
- check-css-scope.mjs passes (every theme.css top-level selector scoped under [data-clarity-surface]).
- Full `node --test "test/**/*.test.mjs"` passes.
- `pnpm build` + `pnpm typecheck` exit 0.
- Reader scoping confined: grep theme.css confirms Newsreader / JetBrains-Mono / system-ui appear ONLY under `[data-clarity-surface='reader']`; the base `[data-clarity-surface]` font-family (Geist) and all situation-room / chat font rules are unchanged.
- Visual self-check against the sketches: single column, TL;DR-first with gold rule + "◆ Your briefing", Acceptance + Deliverable cards, "Show full task" disclosure hiding the body; body refs render as light underlined inline refs with a status dot while standalone/anchored refs stay full bordered chips; hover-peek works on both.
</verification>

<success_criteria>
- 003-B layout live: no plugin rail, single ~760px column, TL;DR-first reading order, raw body behind "Show full task", LiveBlockerPanel relocated inline (quiet when healthy).
- 004-B type live: Reader body = system-ui, TL;DR/BLUF = Newsreader (true 400/600), IDs = JetBrains Mono — all scoped to the reader surface; chat/bulletin/situation-room type unchanged.
- Two-weight chips: mid-sentence body refs use the light inline form; standalone anchored-to + TL;DR follow-on refs keep the full chip; hover-peek on both.
- All hard invariants preserved (resolver chain, per-section error boundaries, scroll-stability, footer PauseBanner literal, additive/coexistence posture).
- Version 1.2.2 in package.json AND src/manifest.ts AND the built dist/manifest.js; build + typecheck + full test suite green.
</success_criteria>

<output>
Create `.planning/quick/260531-b8w-implement-the-reader-redesign-from-sketc/260531-b8w-SUMMARY.md` when done.
</output>
