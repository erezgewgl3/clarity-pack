---
phase: quick-260619-r4v
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/surfaces/chat/true-task/true-task-dialog.tsx
  - src/worker/handlers/chat-true-task.ts
  - src/worker/chat/true-task.ts
  - src/worker/handlers/chat-active-tasks.ts
  - src/worker/db/chat-topics-repo.ts
  - src/ui/surfaces/chat/active-tasks-owned.tsx
  - src/worker/handlers/chat-topic-task-updates.ts
  - src/ui/surfaces/chat/true-task/inline-task-card.tsx
  - src/ui/surfaces/chat/message-thread.tsx
  - test/worker/chat/true-task.test.mjs
  - test/worker/chat/chat-true-task.test.mjs
  - test/worker/chat/chat-active-tasks.test.mjs
  - test/worker/chat/chat-topic-task-updates.test.mjs
  - package.json
  - src/manifest.ts
autonomous: true
requirements:
  - SPEC-CHAT-LOOP-PIECE-1
  - SPEC-CHAT-LOOP-PIECE-2
  - SPEC-CHAT-LOOP-PIECE-3
  - SPEC-CHAT-LOOP-ANTISTORM

must_haves:
  truths:
    - "Every operator-created task from chat is topic-linked (no cold/standalone path remains); it always writes the chat_topic_tasks back-link and is always visible in chat."
    - "The create-task dialog requires a topic: it defaults to the open topic, the Standalone option is gone, and Create is disabled until an existing topic is selected OR a new-topic name is non-empty."
    - "A task created with newTopicTitle atomically creates the topic (CHT-N + parent + chat_topics row) AND the topic-linked task (marker comment + chat_topic_tasks back-link) in one worker flow."
    - "The Active tasks owned rail shows ALL chat-created tasks owned by the selected employee company-wide, grouped by LIVE assignee, so a reassigned task follows its owner and never silently disappears."
    - "The rail is bounded (M=100) and never silently incomplete: a visible scope label + 'showing N of M' renders when capped, and enrich failures surface as a labeled count."
    - "Each in-thread Task-created marker renders as a live task-update card showing {assignee} · {COU-NN} · {live status} + the agent's latest comment (polished), refreshing on the existing 15s poll; blocked tasks show 'Blocked — needs you: <named action>'."
    - "All new read paths perform ZERO writes, ZERO requestWakeup, ZERO event subscriptions, and ZERO ctx.issues.list across BOTH populated and empty inputs (anti-storm invariant)."
  artifacts:
    - path: "src/ui/surfaces/chat/true-task/true-task-dialog.tsx"
      provides: "Topic-required dialog: default-to-open-topic, Standalone removed, inline +New topic, Create gated on topic|new-name"
    - path: "src/worker/handlers/chat-true-task.ts"
      provides: "chat.createTrueTask handler accepting newTopicTitle; cold-path branch removed"
    - path: "src/worker/chat/true-task.ts"
      provides: "createTrueTask helper with atomic new-topic creation; isCold/cold-task originId branch removed"
      contains: "newTopicTitle"
    - path: "src/worker/db/chat-topics-repo.ts"
      provides: "listChatTopicTasksForCompany (company-wide enumerate) + listChatTopicTasksForTopicAll (per-topic for cards)"
      contains: "listChatTopicTasksForCompany"
    - path: "src/worker/handlers/chat-active-tasks.ts"
      provides: "chat.taskOwned re-scoped company-wide, grouped by live assignee, M=100 cap with N-of-M + labeled enrich failures, bounded parallel enrich"
    - path: "src/ui/surfaces/chat/active-tasks-owned.tsx"
      provides: "Rail UI grouped by assignee with scope label + showing-N-of-M + labeled-skipped line"
    - path: "src/worker/handlers/chat-topic-task-updates.ts"
      provides: "chat.topicTaskUpdates per-topic read-time-reflection handler: enrich each linked task via issues.get + latest agent comment via listComments, polished, blocked-via-isTopicStuck, cap ~20"
      exports: ["registerChatTopicTaskUpdates"]
    - path: "src/ui/surfaces/chat/true-task/inline-task-card.tsx"
      provides: "Card upgraded to live task-update card: latest agent comment line, blocked-needs-you amber state, relative time"
    - path: "test/worker/chat/chat-topic-task-updates.test.mjs"
      provides: "Card-handler render-state tests + anti-storm guard (zero list/write/wake) on populated AND empty paths"
  key_links:
    - from: "src/ui/surfaces/chat/true-task/true-task-dialog.tsx"
      to: "chat.createTrueTask"
      via: "usePluginAction with newTopicTitle param"
      pattern: "newTopicTitle"
    - from: "src/worker/handlers/chat-active-tasks.ts"
      to: "listChatTopicTasksForCompany"
      via: "company-wide side-table SELECT then per-row ctx.issues.get"
      pattern: "listChatTopicTasksForCompany"
    - from: "src/ui/surfaces/chat/message-thread.tsx"
      to: "chat.topicTaskUpdates"
      via: "per-topic poll feeding InlineTaskCard live status + latest comment"
      pattern: "topicTaskUpdates"
---

<objective>
Close the chat create-task / agent-feedback loop in three coupled pieces, local code only:

1. **Piece 1 — Topic-required create-task dialog.** Delete the cold/standalone path so every operator-created task is topic-linked (COU-56 fix). Add atomic new-topic creation.
2. **Piece 2 — Assignee-scoped, company-wide "Active tasks owned" rail.** Re-scope from topic-only to company-wide, grouped by live assignee, bounded + never silently incomplete.
3. **Piece 3 — In-thread live task-update cards (loop closure, Approach A = read-time reflection).** Upgrade the existing in-thread marker into a live card showing live status + the agent's latest comment, so Eric sees the reply inside chat (COU-57 fix).

Purpose: the chat surface must let Eric check in with an agent and see what they did / are doing / completed-or-not, WITHOUT the reply silently landing on an issue he never opens.

Output: updated dialog/handlers/repo/UI + one new per-topic read handler + tests (including the load-bearing anti-storm guards). No deploy, no live drill.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/SPEC-2026-06-19-chat-create-task-loop-closure.md
@./CLAUDE.md

<!-- THE AUTHORITATIVE BUILD CONTRACT is the SPEC above. It is LOCKED and APPROVED — do not re-litigate its decisions. -->

<hard_invariants>
READ-TIME ONLY for all new read paths (Pieces 2 + 3). Across populated AND empty inputs:
- ZERO writes: no ctx.issues.create / ctx.issues.update / ctx.issues.createComment / ctx.db.execute on read paths.
- ZERO ctx.issues.requestWakeup.
- ZERO event subscriptions (ctx.events.*).
- ZERO ctx.issues.list (the host silently ignores originId filters — PROBE-OQ2-FILTER; the chat_topic_tasks side table is the enumeration source).
All host reads (ctx.issues.get / ctx.issues.listComments / ctx.db.query) run INSIDE an existing dispatch (the data-handler call / the 15s thread poll) → invocation-scope-safe (PR #6547). This is what makes the Phase-16.1 wake-storm structurally impossible.

Additive-only plugin-namespace SQL. chat_topic_tasks ALREADY exists (migration 0007) — NO new migration. Reads are SELECT-only via ctx.db.query / ctx.issues.*. Opt-in gating via wrapDataHandler / wrapActionHandler. NO_UUID_LEAK scrub on every read-time display string. polishTldr applies to agent-authored text ONLY — operator text is never polished.
</hard_invariants>

<interfaces>
<!-- Extracted from the codebase. Executor: use these directly. -->

From src/worker/chat/true-task.ts (the shared create helper — REMOVE the isCold branch; ADD newTopicTitle atomic create):
```typescript
export type TrueTaskCtx = { issues: PluginIssuesClient; logger?: PluginLogger; db?: ChatTopicsRepoCtx['db'] };
export type CreateTrueTaskInput = {
  companyId: string; title: string; description: string;
  assigneeAgentId: string; topicIssueId: string | null;
  sourceCommentId: string | null; employeeName: string; userId?: string | null;
};
export async function createTrueTask(ctx, input): Promise<{ issueId: string }>;
// Marker body LOCKED + pinned by test 6 of true-task.test.mjs:
//   `Task created — ${issue.id}, assigned to ${input.employeeName}.`
// originId for topic-linked task: `chat-task:${topicIssueId}:${sourceCommentId ?? 'composer'}`
```

From src/worker/db/chat-topics-repo.ts (mirror these existing patterns for the new queries):
```typescript
// Existing per-topic enumerate (LIMIT 50). Add the two new queries beside it.
export async function listChatTopicTasksForTopic(ctx, companyId, topicIssueId): Promise<string[]>;
export async function insertChatTopicTask(ctx, companyId, topicIssueId, taskIssueId): Promise<void>;
// Topic-create primitives to reuse for the atomic new-topic path:
export async function getEmployeeParentIssueId(ctx, companyId, employeeAgentId): Promise<string | null>;
export async function insertEmployeeParent(ctx, companyId, employeeAgentId, parentIssueId): Promise<string>;
export async function allocateChtNumber(ctx, companyId): Promise<string>; // returns "CHT-N"
export async function insertChatTopic(ctx, row & { originIssueId?: string|null }): Promise<ChatTopicRow>;
export const NON_TERMINAL_CONVERSATION_STATUS = 'in_progress'; // from topic-watchdog.ts
```

From src/worker/chat/topic-watchdog.ts (reuse for Piece 3 blocked state — do NOT call its write paths):
```typescript
export function isTopicStuck(issue): boolean; // activeRecoveryAction set OR successfulRunHandoff.exhausted
// activeRecoveryAction?.recoveryOwnerName provides the named human action (see chat-messages.ts:259-263)
```

From src/worker/agents/compile-tldr.ts and action-cards.ts:
```typescript
export function polishTldr(input: string): string; // agent-authored bodies ONLY
// NO_UUID_LEAK strip pattern (action-cards.ts:267): s.replace(UUID_RE_G, '').replace(/\s{2,}/g,' ').trim()
```

From src/worker/handlers/chat-active-tasks.ts — chat.taskOwned response shape consumed by the rail:
```typescript
{ kind: 'taskOwned', topicIssueId, tasks: ActiveTaskEntry[] } | { error: '<KEY>_REQUIRED' | 'TASKS_FAILED' | 'OPT_IN_REQUIRED' }
```

Worker registration site: src/worker.ts (registerChatMessages:370, registerChatTrueTask:376, registerChatActiveTasks:394). Register the new chat.topicTaskUpdates handler alongside these with a matching `ctx as unknown as <Ctx>` cast + import.

Marker regex already wired in message-thread.tsx:520:
```
/^Task created — ([^,]+), assigned to (.+)\.$/  // capture[1]=issueId, capture[2]=assignee
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Piece 1): Topic-required create-task dialog + worker newTopicTitle atomic create + cold-path removal</name>
  <files>src/ui/surfaces/chat/true-task/true-task-dialog.tsx, src/worker/handlers/chat-true-task.ts, src/worker/chat/true-task.ts, test/worker/chat/true-task.test.mjs, test/worker/chat/chat-true-task.test.mjs</files>
  <behavior>
    - Worker createTrueTask: given input.newTopicTitle (non-empty) and no topicIssueId, atomically resolves/creates the parent issue, allocates CHT-N, creates the topic issue at NON_TERMINAL_CONVERSATION_STATUS assigned to assigneeAgentId, inserts the chat_topics row, THEN creates the topic-linked task (top-level issue + marker comment + chat_topic_tasks back-link). Returns the new topic + task ids.
    - Worker createTrueTask: given a string topicIssueId, behaves exactly as today (marker + back-link). The locked marker body and originId (`chat-task:...`) are unchanged — test 6 of true-task.test.mjs still passes.
    - Cold path is GONE: no `isCold` branch, no `cold-task:` originId, ever emitted. A call with topicIssueId === null AND no newTopicTitle returns a structured error (e.g. TOPIC_REQUIRED), never a cold task.
    - Dialog: Topic dropdown defaults to the currently-open topic; the "Standalone (not linked to any topic)" option is deleted; label is "TOPIC" (not "TOPIC (OPTIONAL)"); order is [current topic first] → other non-archived topics → inline "+ New topic" affordance revealing a name input. Zero topics → the new-topic name input is required. Create button is disabled until an existing topic is selected OR a new-topic name is non-empty.
  </behavior>
  <action>Worker (src/worker/chat/true-task.ts): add an optional `newTopicTitle?: string | null` to CreateTrueTaskInput. REMOVE the `isCold` discriminator and the `cold-task:<userId>:<unix-ms>` originId branch and its early-return entirely. New flow: if `topicIssueId` is a non-empty string → existing topic-linked path (unchanged: top-level create, locked marker comment, insertChatTopicTask back-link). Else if `newTopicTitle` is a non-empty string → atomic new-topic path: mirror chat-topics.ts:306-367 (getEmployeeParentIssueId → bootstrap parent via ctx.issues.create + insertEmployeeParent if absent; allocateChtNumber; ctx.issues.create child topic at NON_TERMINAL_CONVERSATION_STATUS assigned to assigneeAgentId with originId `chat-topic-${topicId}`; insertChatTopic row) THEN run the topic-linked task create against that new topic issue id. Best-effort symmetry preserved (marker + back-link try/catch + warn-log, never bubble; the topic + task creates themselves DO surface failure → CREATE_FAILED). Else (no topic, no newTopicTitle) → throw so the handler returns an error. The atomic path needs employeeAgentId for the parent/topic — it is already a required input. Thread employeeName for the parent issue title via formatParentIssueTitle (import from chat-topics.ts or replicate its existing helper). Handler (src/worker/handlers/chat-true-task.ts): remove the null-accepting topicIssueId branch comment block (lines 50-83 cold logic); accept `newTopicTitle` as an optional string param; require topicIssueId-string OR newTopicTitle-non-empty (return { error: 'TOPIC_REQUIRED' } otherwise); drop the cold `description` branch — always use the chat-anchored description. Dialog (true-task-dialog.tsx): delete the `<option value="">Standalone…</option>` (line 356); relabel "TOPIC (OPTIONAL)" → "TOPIC" (line 348); preselect topicIssueId to the open topic (thread the open topic via a new `currentTopic` prop or reuse sourceTopic for cold mode — default state = open topic's issueId); add a "+ New topic" `<option value="__new__">` (or a small reveal button) that toggles a controlled new-topic-name `<input>`; compute `canSubmit = trimmedTitle && assignee && (topicIssueId || newTopicName.trim()) && !busy`; pass `newTopicTitle: newTopicName.trim() || undefined` and `topicIssueId` (the selected existing id, or null when creating new) to createTrueTask. Drop/rewrite the now-false `topicHelper` "Standalone tasks won't appear…" string. Keep T-04-18 (no dangerouslySetInnerHTML; usePluginAction only). DO NOT use "v1"/"placeholder"/"for now" — this is the full topic-required behavior per SPEC Piece 1.</action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/chat/true-task.test.mjs test/worker/chat/chat-true-task.test.mjs && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>true-task.test.mjs (incl. locked-marker test 6) and chat-true-task.test.mjs pass; the suite proves: newTopicTitle creates topic+task+marker+back-link in one flow; existing topicIssueId path unchanged; no `cold-task:` originId is ever emitted; a no-topic/no-newTopicTitle call returns TOPIC_REQUIRED. tsc clean. Dialog has no Standalone option, label reads TOPIC, Create gated on topic|new-name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Piece 2): Company-wide assignee-scoped Active-tasks-owned rail (worker query + handler re-scope + UI grouping)</name>
  <files>src/worker/db/chat-topics-repo.ts, src/worker/handlers/chat-active-tasks.ts, src/ui/surfaces/chat/active-tasks-owned.tsx, test/worker/chat/chat-active-tasks.test.mjs</files>
  <behavior>
    - New repo query listChatTopicTasksForCompany(ctx, companyId, limit) returns distinct task_issue_ids for the company, newest-first, bounded by limit (M=100). SELECT-only, plugin-namespace.
    - chat.taskOwned enriches each task via ctx.issues.get, groups by LIVE assignee (the issues.get response's assignee), and a task reassigned out-of-band surfaces under its NEW owner. Enrich runs bounded-parallel (not sequential for…await).
    - When the side table returns the cap M, the response carries a `capped: true` + `total`/`shown` so the UI renders "showing N of M". Per-row enrich failures are COUNTED (`skipped: number`) and surfaced, never silently dropped.
    - ANTI-STORM: across populated AND empty inputs the handler calls ctx.issues.list ZERO times, performs ZERO writes (no ctx.db.execute, no createComment/update/create), and ZERO requestWakeup.
  </behavior>
  <action>Repo (chat-topics-repo.ts): add `listChatTopicTasksForCompany(ctx, companyId, limit)` mirroring listChatTopicTasksForTopic but dropping the topic_issue_id filter, `SELECT DISTINCT task_issue_id ... WHERE company_id = $1 ORDER BY created_at DESC LIMIT $2`. Handler (chat-active-tasks.ts): change the chat.taskOwned body so the enumeration source is listChatTopicTasksForCompany(ctx, companyId, 100) instead of the topic-scoped query (topicIssueId stays an accepted/ignored param for back-compat, or is dropped from the required set — keep companyId + userId required). Replace the sequential `for…await` enrich with a bounded-parallel enrich (Promise.all over the capped id list, each wrapped in try/catch returning null on failure; count nulls as `skipped`). After enrich, GROUP surviving tasks by their live assignee field from the issues.get row (read whatever assignee field the host returns — assigneeAgentId / assignee.id; degrade to an "Unassigned" bucket, NO_UUID_LEAK: never render a raw UUID, use the assignee display name when present else "Unassigned"). Response shape: `{ kind: 'taskOwned', groups: Array<{ assignee: string; tasks: ActiveTaskEntry[] }>, total, shown, capped, skipped }` (keep `tasks` flat too if needed for the existing action-card attach block — preserve the Phase-19 flag-gated read-only action-card attach exactly as-is; it stays READ-ONLY). Keep wrapDataHandler opt-in gate and the Pitfall-5 zero-issues.list invariant. UI (active-tasks-owned.tsx): render grouped-by-assignee sections (one header per assignee, rows underneath reusing the existing `.task-row` markup + ChatTaskStatusPill + RefChip + host-routed title anchor); add a visible scope label ("Tasks owned by <employee>, company-wide") and a "showing N of M" line when `capped`; add a labeled "(N could not be loaded)" line when `skipped > 0`. Update the useChatActiveTasks hook + ChatActiveTask type to the grouped shape. DO NOT reintroduce originId filtering or ctx.issues.list. Bound = M=100 per SPEC. No "v1"/"basic" reductions.</action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/chat/chat-active-tasks.test.mjs && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>chat-active-tasks.test.mjs passes including: company-wide enumerate query used; group-by-live-assignee; reassignment-follows-owner; cap + shown/total/capped; per-row failure counted in `skipped` not dropped silently; bounded-parallel enrich. The existing Test 10 (NEVER calls ctx.issues.list, populated AND empty) still passes. tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Piece 3): chat.topicTaskUpdates read-time-reflection handler (per-topic live cards) + repo query</name>
  <files>src/worker/handlers/chat-topic-task-updates.ts, src/worker/db/chat-topics-repo.ts, src/worker.ts, test/worker/chat/chat-topic-task-updates.test.mjs</files>
  <behavior>
    - chat.topicTaskUpdates(companyId, userId, topicIssueId) enumerates THIS topic's linked tasks (chat_topic_tasks, cap ~20), enriches each via ctx.issues.get for { identifier, status, assignee }, and reads the latest agent-authored comment via ctx.issues.listComments(taskIssueId) — newest agent comment only.
    - Latest agent comment is polished via polishTldr (agent text only); operator-authored comments are never selected/polished. No comment yet → status-only ("Working…").
    - Blocked / did-not-complete: when isTopicStuck(taskIssue) is true, the card carries a blocked state + the named recovery action (activeRecoveryAction.recoveryOwnerName), surfaced as "Blocked — needs you: <action>" instead of a status pill.
    - NO_UUID_LEAK scrub on every display string. Cap ~20 cards with shown/total/capped; per-row failure degrades to status-only or is counted, never a silent total drop.
    - ANTI-STORM: across populated AND empty inputs the handler calls ctx.issues.list ZERO times, performs ZERO writes, ZERO requestWakeup, ZERO event subscriptions. All reads (issues.get, listComments, db.query) run in-dispatch.
  </behavior>
  <action>Repo (chat-topics-repo.ts): add `listChatTopicTasksForTopicAll(ctx, companyId, topicIssueId, limit)` — same as listChatTopicTasksForTopic but with a caller-supplied limit (default 20) so the per-card read is independently bounded from the rail's 50. New handler (src/worker/handlers/chat-topic-task-updates.ts): register `chat.topicTaskUpdates` via wrapDataHandler (mirror chat-active-tasks.ts data-handler conventions: COMPANY_ID_REQUIRED / USER_ID_REQUIRED / TOPIC_ISSUE_ID_REQUIRED structured errors; OPT_IN_REQUIRED via wrapper). Body: SELECT the topic's task ids (cap 20); bounded-parallel per task → ctx.issues.get(taskIssueId) for status/identifier/assignee + isTopicStuck(row) → if stuck, read activeRecoveryAction.recoveryOwnerName for the named action (mirror chat-messages.ts:248-264); ctx.issues.listComments(taskIssueId, companyId) → pick the newest AGENT-authored comment (authorAgentId set / sender not operator — use the same agent-vs-operator discrimination the codebase uses: agent comment = has authorAgentId and not a chat_messages sender_kind:'user' row; for task issues there is no operator chat row so authorAgentId presence is sufficient), polishTldr its body, clamp is a UI concern (return full body + createdAt; UI clamps). Apply the NO_UUID_LEAK strip (action-cards.ts pattern) to every returned string (assignee display, latest-comment text, named action). Response: `{ kind: 'topicTaskUpdates', topicIssueId, cards: Array<{ issueId, identifier, status, assignee, latestComment: { text, createdAt } | null, blocked: boolean, blockedAction: string | null }>, total, shown, capped, skipped }`. Per-row failure → that card is skipped/counted; never fail the whole response (return cards:[] + the error counts on total-failure). Register in src/worker.ts beside registerChatActiveTasks (import + `registerChatTopicTaskUpdates(ctx as unknown as ChatTopicTaskUpdatesCtx)`). Test file: mirror chat-active-tasks.test.mjs's makeCtx spy harness (issueListCalls / wrapHostFaithfulDb). This handler is SEPARATE from chat.messages and the company-wide rail (SPEC: keep the scopes independent). Reuse — do NOT re-implement — isTopicStuck and polishTldr.</action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/chat/chat-topic-task-updates.test.mjs && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>chat-topic-task-updates.test.mjs passes covering render states: working (no comment) / in-review / done / blocked-needs-you / uuid-scrub; per-topic enrich + cap (shown/total/capped) + per-row failure tolerance; latest-AGENT-comment selection (operator comment never chosen) + polishTldr applied to agent text only. Handler registered in worker.ts. tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4 (Piece 3): Upgrade InlineTaskCard into a live task-update card + wire message-thread to chat.topicTaskUpdates</name>
  <files>src/ui/surfaces/chat/true-task/inline-task-card.tsx, src/ui/surfaces/chat/message-thread.tsx</files>
  <action>message-thread.tsx: add a per-topic poll for `chat.topicTaskUpdates` (companyId, userId, topicIssueId) at the existing 15s cadence with pauseOnHidden (mirror useChatActiveTasks in active-tasks-owned.tsx — usePluginData + usePoll, dedupeBy 'off'). At the existing marker-interception spot (lines 507-560), look up the card data for the parsed issueId from the topicTaskUpdates result (keyed by issueId) INSTEAD of (or in addition to) the activeTasks title-only lookup, and pass the live { status, latestComment, blocked, blockedAction, identifier, assignee } into InlineTaskCard at its existing chronological position. Keep the existing marker regex and the skeleton/race fallback (title null → skeleton until the poll resolves). inline-task-card.tsx: extend the props with `latestComment?: { text: string; createdAt: string } | null`, `blocked?: boolean`, `blockedAction?: string | null`. Render: when blocked → replace the status pill with an amber "Blocked — needs you: <blockedAction>" element (new className e.g. `.inline-task-card-blocked` — CSS is additive in chat.css; a minimal inline style or existing amber token is acceptable, do not ship a broken layout). Otherwise render the existing {assignee} · {ref} · {status} row PLUS a latest-comment line: 1–2 line clamp of latestComment.text with a relative-time suffix and an expand → open-issue link (reuse the existing host-routed `/<companyPrefix>/issues/<identifier>` anchor); when latestComment is null show "Working…". Depth = latest update + status only (NOT a running narrative). Keep T-04-18 (React text only, no dangerouslySetInnerHTML). Operator text is never shown here (only the agent's latest comment, already polished worker-side). No "v1"/"placeholder" — full card content per SPEC Piece 3.</action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && npx tsc --noEmit -p tsconfig.json 2>&1 | head -20 && node scripts/build-ui.mjs 2>&1 | tail -5</automated>
  </verify>
  <done>tsc clean; UI bundle builds. InlineTaskCard renders the live status + latest agent comment line (or "Working…") and the amber "Blocked — needs you: <action>" state when blocked; message-thread polls chat.topicTaskUpdates per-topic at 15s and feeds each marker's card by issueId at its existing chronological spot.</done>
</task>

<task type="auto">
  <name>Task 5: Cross-cutting anti-storm guard tests + full suite + typecheck + build + version bump</name>
  <files>test/worker/chat/chat-active-tasks.test.mjs, test/worker/chat/chat-topic-task-updates.test.mjs, package.json, src/manifest.ts</files>
  <action>Add (or confirm) the load-bearing anti-storm guard tests, mirroring the existing Test 10 in chat-active-tasks.test.mjs, for BOTH new read paths — populated AND empty inputs each: (a) chat.taskOwned (company-wide) asserts issueListCalls.length === 0, ZERO ctx.db.execute calls, ZERO requestWakeup, ZERO ctx.events subscriptions; (b) chat.topicTaskUpdates asserts the same four zeros. Use the spy harness pattern (issueListCalls array on ctx.issues.list; a requestWakeup spy that records calls; a db.execute spy that records calls; assert all empty). These are their own explicit tests, not buried in a render test. Then run the FULL worker + UI test suite to catch regressions in the locked-marker test and the dialog/key-audit tests. Bump version to 1.8.10 (or next) in BOTH package.json line 3 AND src/manifest.ts version field (memory: plugin-version-bump-two-sources — host reads dist/manifest.js, so both must match). LOCAL ONLY — no deploy, no live drill.</action>
  <verify>
    <automated>cd "c:/Users/erezg/Documents/Claude/Projects/Clarity Pack" && node --test test/worker/ 2>&1 | tail -25 && npx tsc --noEmit -p tsconfig.json 2>&1 | head -5 && node scripts/build-ui.mjs 2>&1 | tail -3 && grep -n '"version"' package.json | head -1 && grep -n "version:" src/manifest.ts | head -1</automated>
  </verify>
  <done>Two explicit anti-storm guard tests (one per new read path) pass on populated AND empty inputs, each asserting zero ctx.issues.list, zero db.execute, zero requestWakeup, zero event subscriptions. Full `node --test test/worker/` suite green (incl. locked-marker test 6 + dialog key-audit). tsc clean; UI bundle builds. package.json and src/manifest.ts both show the bumped version (matching). No deploy step run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin UI (same-origin) → worker data/action handlers | Operator params (companyId, userId, topicIssueId, title, newTopicTitle) cross into the worker; opt-in gate + reqStr validation are the entry checks. |
| worker → host (ctx.issues.* / ctx.db) | All new reads are SELECT-only / get / listComments, in-dispatch (PR #6547). The only writes are in Piece 1's create path (existing capability surface), never on a read path. |
| agent-authored comment text → rendered card | Untrusted agent output reaches the operator's card; polishTldr + NO_UUID_LEAK + React-text-only rendering are the mitigations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-r4v-01 | Denial of Service | new read paths (Pieces 2+3) re-entering host RPC | mitigate | READ-TIME ONLY invariant: zero requestWakeup / event-subs / issues.list; all reads in-dispatch. Bounded (M=100 rail, ~20/topic cards) + bounded-parallel enrich. Dedicated anti-storm guard tests (Task 5) pin the four zeros on populated AND empty paths — this is the Phase-16.1 wake-storm prevention by construction. |
| T-r4v-02 | Information disclosure | UUID leak in card/rail display strings | mitigate | NO_UUID_LEAK scrub (action-cards.ts stripUuids pattern) applied to assignee labels, latest-comment text, named actions; assignee falls back to "Unassigned" never a raw UUID. Test covers uuid-scrub render state. |
| T-r4v-03 | Tampering | additive-only schema invariant | accept (by construction) | No new migration — chat_topic_tasks (0007) already exists; all SQL is plugin-namespace SELECT plus the existing Piece-1 create writes. CTT-07 (never mutate public.issues.updated_at on a read path) holds because read paths issue zero writes. |
| T-r4v-04 | Elevation / injection | agent comment text interpolated into card | mitigate | React text-only render (no dangerouslySetInnerHTML, T-04-18); polishTldr is a deterministic transform, not an eval; worker-side scrub before the string crosses to the UI. |
| T-r4v-SC | Tampering | npm/pip/cargo installs | mitigate | No new dependencies introduced by this plan; if any install is attempted, it requires a blocking human-verify checkpoint. (No package-manager installs are part of these tasks.) |
</threat_model>

<verification>
- `node --test test/worker/` fully green, including: true-task locked-marker test 6, chat-true-task TOPIC_REQUIRED + newTopicTitle atomic path, chat-active-tasks group-by-assignee + cap + Test-10 zero-list, chat-topic-task-updates render states + anti-storm zeros.
- Two dedicated anti-storm guard tests (chat.taskOwned company-wide + chat.topicTaskUpdates), each asserting zero issues.list / db.execute / requestWakeup / event-subs on populated AND empty inputs.
- `npx tsc --noEmit -p tsconfig.json` clean.
- `node scripts/build-ui.mjs` builds the UI bundle.
- package.json version and src/manifest.ts version match the bump.
- NO deploy, NO live drill (gated on Eric's explicit go-ahead).
</verification>

<success_criteria>
- Piece 1: every operator-created task is topic-linked (cold path removed); dialog requires a topic (default open topic, no Standalone, inline +New topic, Create gated); newTopicTitle creates topic+task atomically.
- Piece 2: rail is company-wide, grouped by live assignee, reassignment follows owner, bounded M=100 with showing-N-of-M and labeled enrich failures.
- Piece 3: in-thread marker renders a live card ({assignee}·{COU-NN}·{status} + latest agent comment, polished, relative time; "Working…" when no comment; amber "Blocked — needs you: <action>" when stuck); per-topic, independent of the rail.
- Anti-storm invariant proven by dedicated tests across populated + empty paths.
- Local build + typecheck + full test suite green. No deploy.
</success_criteria>

<output>
Create `.planning/quick/260619-r4v-chat-create-task-topic-linkage-agent-fee/260619-r4v-SUMMARY.md` when done.
</output>
