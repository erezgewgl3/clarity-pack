#!/usr/bin/env bash
# Fixture stub for Plan 01-05 Task 1 test P2.
# Linux/macOS bundles of @embedded-postgres include client tools (pg_dump, psql).
# This stub exists so locatePgDump's bundled-discovery test (P2) has something to find.
# Real bundles ship the genuine PostgreSQL pg_dump binary; the locator does NOT
# validate the binary contents — only existence.
echo "pg_dump (PostgreSQL) 18.1 (fixture)"
